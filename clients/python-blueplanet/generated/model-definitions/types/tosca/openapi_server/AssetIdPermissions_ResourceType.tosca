"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server AssetIdPermissions"
package     = openapi_server
version     = "1.0"
description = "Defines a AssetIdPermissions"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
  AssetGroupBinding = openapi_server.resourceTypes.AssetGroupBinding
  import re = openapi_server.resourceTypes.import re
}
resourceTypes {

  AssetIdPermissions {
    title = AssetIdPermissions
    description = AssetIdPermissions
    derivedFrom = Root
    properties {
        asset_group_info {
          type = AssetGroupBinding
          description = ""
          optional = false
        }
        asset_id {
          type =  string
          description = "Unique identifier of a business asset."
          optional = false
        }
        asset_type {
          type =  string
          description = "Type of asset. Currently we only support AD_ACCOUNT, PROFILE, ASSET_GROUP and CATALOG."
          optional = false
        }
        permissions {
          type = array
          items.type =  string
          description = "Permission levels member or partner has on an asset."
          optional = false
        }
    }
  }
}

serviceTemplates {

  AssetIdPermissions {
    title = AssetIdPermissions
    description = AssetIdPermissions
    implements = openapi_server.resourceTypes.AssetIdPermissions

  }
}
