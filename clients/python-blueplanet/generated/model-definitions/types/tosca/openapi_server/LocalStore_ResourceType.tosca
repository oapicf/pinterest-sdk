"$schema"   = "http://cyaninc.com/json-schemas/tosca-lite-v1/definition-module#"
title       = "openapi_server LocalStore"
package     = openapi_server
version     = "1.0"
description = "Defines a LocalStore"
authors     = ["F. Bar (foo@bar.baz)"]

imports {
  Root = tosca.resourceTypes.Root
  Country = openapi_server.resourceTypes.Country
  import re = openapi_server.resourceTypes.import re
}
resourceTypes {

  LocalStore {
    title = LocalStore
    description = LocalStore
    derivedFrom = Root
    properties {
        address_primary {
          type =  string
          description = "Primary address line of the store."
          optional = false
        }
        address_secondary {
          type =  string
          description = "Secondary address line of the store."
          optional = false
        }
        city {
          type =  string
          description = "City where the store is located."
          optional = false
        }
        country {
          type = Country
          description = "Country code where the store is located."
          optional = true
        }
        created_at {
          type =  string
          description = "Creation timestamp"
          optional = true
        }
        id {
          type =  string
          description = "The ID of the local store."
          optional = true
        }
        latitude {
          type =  integer
          description = "Geographic latitude coordinate of the store."
          optional = false
        }
        longitude {
          type =  integer
          description = "Geographic longitude coordinate of the store."
          optional = false
        }
        name {
          type =  string
          description = "The name of the local store."
          optional = true
        }
        postal_code {
          type =  string
          description = "Postal or ZIP code of the store."
          optional = false
        }
        region {
          type =  string
          description = "State or region code where the store is located."
          optional = false
        }
        store_code {
          type =  string
          description = "Merchant provided code for the local store. Unique within the merchant&#39;s catalog."
          optional = true
        }
        updated_at {
          type =  string
          description = "Last update timestamp"
          optional = true
        }
    }
  }
}

serviceTemplates {

  LocalStore {
    title = LocalStore
    description = LocalStore
    implements = openapi_server.resourceTypes.LocalStore

  }
}
