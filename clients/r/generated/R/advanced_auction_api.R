#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AdvancedAuction operations
#' @description AdvancedAuctionApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdvancedAuctionItemsGetPost  ####################
#'
#' library(openapi)
#' var_advanced_auction_items_get_request <- AdvancedAuctionItemsGetRequest$new("catalog_id_example", c(AdvancedAuctionKey$new(Country$new(), "item_id_example", Language$new()))) # AdvancedAuctionItemsGetRequest | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get item bid options (POST)
#' api_instance <- AdvancedAuctionApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvancedAuctionItemsGetPost(var_advanced_auction_items_get_request, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$AdvancedAuctionItemsGetPost(var_advanced_auction_items_get_request, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  AdvancedAuctionItemsSubmitPost  ####################
#'
#' library(openapi)
#' var_advanced_auction_items_submit_request <- AdvancedAuctionItemsSubmitRequest$new("catalog_id_example", c(AdvancedAuctionItemsSubmitRecord$new(AdvancedAuctionBidOptions$new(AppTypeMultipliers$new(TargetingSpecAppType$new()), 123, PlacementMultipliers$new(PlacementType$new())), Country$new(), "item_id_example", Language$new(), "DELETE", c(UpdateMaskBidOptionField$new()), c(AdvancedAuctionOperationError$new(123, "message_example"))))) # AdvancedAuctionItemsSubmitRequest | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Operate on item level bid options
#' api_instance <- AdvancedAuctionApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvancedAuctionItemsSubmitPost(var_advanced_auction_items_submit_request, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$AdvancedAuctionItemsSubmitPost(var_advanced_auction_items_submit_request, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AdvancedAuctionApi <- R6::R6Class(
  "AdvancedAuctionApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AdvancedAuctionApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get item bid options (POST)
    #'
    #' @param advanced_auction_items_get_request 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvancedAuctionItems
    AdvancedAuctionItemsGetPost = function(advanced_auction_items_get_request, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvancedAuctionItemsGetPostWithHttpInfo(advanced_auction_items_get_request, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get item bid options (POST)
    #'
    #' @param advanced_auction_items_get_request 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvancedAuctionItems) with additional information such as HTTP status code, headers
    AdvancedAuctionItemsGetPostWithHttpInfo = function(advanced_auction_items_get_request, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`advanced_auction_items_get_request`)) {
        stop("Missing required parameter `advanced_auction_items_get_request`.")
      }

      if (!missing(`advanced_auction_items_get_request`) && is.null(`advanced_auction_items_get_request`)) {
        stop("Invalid value for `advanced_auction_items_get_request` when calling AdvancedAuctionApi$AdvancedAuctionItemsGetPost, `advanced_auction_items_get_request` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsGetPost, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsGetPost, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsGetPost, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`advanced_auction_items_get_request`)) {
        local_var_body <- `advanced_auction_items_get_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/advanced_auction/items/get"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvancedAuctionItems"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Operate on item level bid options
    #'
    #' @param advanced_auction_items_submit_request 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvancedAuctionProcessedItems
    AdvancedAuctionItemsSubmitPost = function(advanced_auction_items_submit_request, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvancedAuctionItemsSubmitPostWithHttpInfo(advanced_auction_items_submit_request, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Operate on item level bid options
    #'
    #' @param advanced_auction_items_submit_request 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvancedAuctionProcessedItems) with additional information such as HTTP status code, headers
    AdvancedAuctionItemsSubmitPostWithHttpInfo = function(advanced_auction_items_submit_request, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`advanced_auction_items_submit_request`)) {
        stop("Missing required parameter `advanced_auction_items_submit_request`.")
      }

      if (!missing(`advanced_auction_items_submit_request`) && is.null(`advanced_auction_items_submit_request`)) {
        stop("Invalid value for `advanced_auction_items_submit_request` when calling AdvancedAuctionApi$AdvancedAuctionItemsSubmitPost, `advanced_auction_items_submit_request` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsSubmitPost, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsSubmitPost, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdvancedAuctionApi$AdvancedAuctionItemsSubmitPost, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`advanced_auction_items_submit_request`)) {
        local_var_body <- `advanced_auction_items_submit_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/advanced_auction/items/submit"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvancedAuctionProcessedItems"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
