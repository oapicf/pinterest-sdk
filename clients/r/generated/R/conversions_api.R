#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Conversions operations
#' @description ConversionsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdvertiserDefinedEventsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_advertiser_defined_events_create_request <- AdvertiserDefinedEventsCreateRequest$new(c(AdvertiserDefinedEventInput$new(AdvertiserDefinedEventMappingType$new(), "name_example"))) # AdvertiserDefinedEventsCreateRequest | 
#'
#' #Create advertiser defined events
#' api_instance <- ConversionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvertiserDefinedEventsCreate(var_ad_account_id, var_advertiser_defined_events_create_requestdata_file = "result.txt")
#' result <- api_instance$AdvertiserDefinedEventsCreate(var_ad_account_id, var_advertiser_defined_events_create_request)
#' dput(result)
#'
#'
#' ####################  AdvertiserDefinedEventsDelete  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_event_names <- c("inner_example") # array[character] | List of event names to delete
#'
#' #Delete advertiser defined events
#' api_instance <- ConversionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvertiserDefinedEventsDelete(var_ad_account_id, var_event_namesdata_file = "result.txt")
#' result <- api_instance$AdvertiserDefinedEventsDelete(var_ad_account_id, var_event_names)
#' dput(result)
#'
#'
#' ####################  AdvertiserDefinedEventsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#'
#' #Get advertiser defined events
#' api_instance <- ConversionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvertiserDefinedEventsGet(var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$AdvertiserDefinedEventsGet(var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  AdvertiserDefinedEventsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_advertiser_defined_events_create_request <- AdvertiserDefinedEventsCreateRequest$new(c(AdvertiserDefinedEventInput$new(AdvertiserDefinedEventMappingType$new(), "name_example"))) # AdvertiserDefinedEventsCreateRequest | 
#'
#' #Update advertiser defined events
#' api_instance <- ConversionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdvertiserDefinedEventsUpdate(var_ad_account_id, var_advertiser_defined_events_create_requestdata_file = "result.txt")
#' result <- api_instance$AdvertiserDefinedEventsUpdate(var_ad_account_id, var_advertiser_defined_events_create_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ConversionsApi <- R6::R6Class(
  "ConversionsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ConversionsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param advertiser_defined_events_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvertiserDefinedEventsCreate200Response
    AdvertiserDefinedEventsCreate = function(ad_account_id, advertiser_defined_events_create_request, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvertiserDefinedEventsCreateWithHttpInfo(ad_account_id, advertiser_defined_events_create_request, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param advertiser_defined_events_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvertiserDefinedEventsCreate200Response) with additional information such as HTTP status code, headers
    AdvertiserDefinedEventsCreateWithHttpInfo = function(ad_account_id, advertiser_defined_events_create_request, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`advertiser_defined_events_create_request`)) {
        stop("Missing required parameter `advertiser_defined_events_create_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`advertiser_defined_events_create_request`) && is.null(`advertiser_defined_events_create_request`)) {
        stop("Invalid value for `advertiser_defined_events_create_request` when calling ConversionsApi$AdvertiserDefinedEventsCreate, `advertiser_defined_events_create_request` is not nullable")
      }

      if (!is.null(`advertiser_defined_events_create_request`)) {
        local_var_body <- `advertiser_defined_events_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/advertiser_defined_events"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvertiserDefinedEventsCreate200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param event_names List of event names to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvertiserDefinedEventsCreate200Response
    AdvertiserDefinedEventsDelete = function(ad_account_id, event_names, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvertiserDefinedEventsDeleteWithHttpInfo(ad_account_id, event_names, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param event_names List of event names to delete
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvertiserDefinedEventsCreate200Response) with additional information such as HTTP status code, headers
    AdvertiserDefinedEventsDeleteWithHttpInfo = function(ad_account_id, event_names, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`event_names`)) {
        stop("Missing required parameter `event_names`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsDelete, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsDelete, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`event_names`) && is.null(`event_names`)) {
        stop("Invalid value for `event_names` when calling ConversionsApi$AdvertiserDefinedEventsDelete, `event_names` is not nullable")
      }

      # no explore
      query_params[["event_names"]] <- I(paste(lapply(`event_names`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/ad_accounts/{ad_account_id}/advertiser_defined_events"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvertiserDefinedEventsCreate200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvertiserDefinedEventsGet200Response
    AdvertiserDefinedEventsGet = function(ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvertiserDefinedEventsGetWithHttpInfo(ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvertiserDefinedEventsGet200Response) with additional information such as HTTP status code, headers
    AdvertiserDefinedEventsGetWithHttpInfo = function(ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/advertiser_defined_events"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvertiserDefinedEventsGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param advertiser_defined_events_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdvertiserDefinedEventsCreate200Response
    AdvertiserDefinedEventsUpdate = function(ad_account_id, advertiser_defined_events_create_request, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdvertiserDefinedEventsUpdateWithHttpInfo(ad_account_id, advertiser_defined_events_create_request, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update advertiser defined events
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param advertiser_defined_events_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdvertiserDefinedEventsCreate200Response) with additional information such as HTTP status code, headers
    AdvertiserDefinedEventsUpdateWithHttpInfo = function(ad_account_id, advertiser_defined_events_create_request, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`advertiser_defined_events_create_request`)) {
        stop("Missing required parameter `advertiser_defined_events_create_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionsApi$AdvertiserDefinedEventsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`advertiser_defined_events_create_request`) && is.null(`advertiser_defined_events_create_request`)) {
        stop("Invalid value for `advertiser_defined_events_create_request` when calling ConversionsApi$AdvertiserDefinedEventsUpdate, `advertiser_defined_events_create_request` is not nullable")
      }

      if (!is.null(`advertiser_defined_events_create_request`)) {
        local_var_body <- `advertiser_defined_events_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/advertiser_defined_events"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdvertiserDefinedEventsCreate200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
