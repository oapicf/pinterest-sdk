#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Resources operations
#' @description ResourcesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdAccountCountriesGet  ####################
#'
#' library(openapi)
#'
#' #Get ad accounts countries
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdAccountCountriesGet(data_file = "result.txt")
#' result <- api_instance$AdAccountCountriesGet()
#' dput(result)
#'
#'
#' ####################  DeliveryMetricsGet  ####################
#'
#' library(openapi)
#' var_report_type <- "report_type_example" # character | Report type. (Optional)
#'
#' #Get available metrics' definitions
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeliveryMetricsGet(report_type = var_report_typedata_file = "result.txt")
#' result <- api_instance$DeliveryMetricsGet(report_type = var_report_type)
#' dput(result)
#'
#'
#' ####################  InterestTargetingOptionsGet  ####################
#'
#' library(openapi)
#' var_interest_id <- "interest_id_example" # character | Unique identifier of an interest.
#'
#' #Get interest details
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$InterestTargetingOptionsGet(var_interest_iddata_file = "result.txt")
#' result <- api_instance$InterestTargetingOptionsGet(var_interest_id)
#' dput(result)
#'
#'
#' ####################  LeadFormQuestionsGet  ####################
#'
#' library(openapi)
#'
#' #Get lead form questions
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$LeadFormQuestionsGet()
#'
#'
#' ####################  MetricsReadyStateGet  ####################
#'
#' library(openapi)
#' var_date <- "2022-07-13" # character | Analytics reports request date (UTC). Format: YYYY-MM-DD
#'
#' #Get metrics ready state
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$MetricsReadyStateGet(var_datedata_file = "result.txt")
#' result <- api_instance$MetricsReadyStateGet(var_date)
#' dput(result)
#'
#'
#' ####################  TargetingOptionsGet  ####################
#'
#' library(openapi)
#' var_targeting_type <- "APPTYPE" # character | Public targeting type.
#' var_client_id <- "1094834" # character | Client ID. (Optional)
#' var_oauth_signature <- "8209f" # character | Oauth signature (Optional)
#' var_timestamp <- "1618338184277" # character | Timestamp (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get targeting options
#' api_instance <- ResourcesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TargetingOptionsGet(var_targeting_type, client_id = var_client_id, oauth_signature = var_oauth_signature, timestamp = var_timestamp, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$TargetingOptionsGet(var_targeting_type, client_id = var_client_id, oauth_signature = var_oauth_signature, timestamp = var_timestamp, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ResourcesApi <- R6::R6Class(
  "ResourcesApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ResourcesApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get ad accounts countries
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AdAccountsCountryResponse
    AdAccountCountriesGet = function(data_file = NULL, ...) {
      local_var_response <- self$AdAccountCountriesGetWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get ad accounts countries
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AdAccountsCountryResponse) with additional information such as HTTP status code, headers
    AdAccountCountriesGetWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/resources/ad_account_countries"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AdAccountsCountryResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get available metrics' definitions
    #'
    #' @param report_type (optional) Report type.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeliveryMetricsResponse
    DeliveryMetricsGet = function(report_type = NULL, data_file = NULL, ...) {
      local_var_response <- self$DeliveryMetricsGetWithHttpInfo(report_type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get available metrics' definitions
    #'
    #' @param report_type (optional) Report type.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeliveryMetricsResponse) with additional information such as HTTP status code, headers
    DeliveryMetricsGetWithHttpInfo = function(report_type = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!is.null(`report_type`) && !(`report_type` %in% c("SYNC", "ASYNC"))) {
        stop("Invalid value for report_type when calling ResourcesApi$DeliveryMetricsGet. Must be [SYNC, ASYNC].")
      }
      query_params[["report_type"]] <- `report_type`

      local_var_url_path <- "/resources/delivery_metrics"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read pins:read user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeliveryMetricsResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get interest details
    #'
    #' @param interest_id Unique identifier of an interest.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return SingleInterestTargetingOptionResponse
    InterestTargetingOptionsGet = function(interest_id, data_file = NULL, ...) {
      local_var_response <- self$InterestTargetingOptionsGetWithHttpInfo(interest_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get interest details
    #'
    #' @param interest_id Unique identifier of an interest.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (SingleInterestTargetingOptionResponse) with additional information such as HTTP status code, headers
    InterestTargetingOptionsGetWithHttpInfo = function(interest_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`interest_id`)) {
        stop("Missing required parameter `interest_id`.")
      }

      if (nchar(`interest_id`) > 18) {
        stop("Invalid length for `interest_id` when calling ResourcesApi$InterestTargetingOptionsGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`interest_id`, "^\\d+$")) {
        stop("Invalid value for `interest_id` when calling ResourcesApi$InterestTargetingOptionsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/resources/targeting/interests/{interest_id}"
      if (!missing(`interest_id`)) {
        local_var_url_path <- gsub("\\{interest_id\\}", URLencode(as.character(`interest_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SingleInterestTargetingOptionResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get lead form questions
    #'
    #' @param ... Other optional arguments
    #'
    #' @return void
    LeadFormQuestionsGet = function(...) {
      local_var_response <- self$LeadFormQuestionsGetWithHttpInfo(...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get lead form questions
    #'
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LeadFormQuestionsGetWithHttpInfo = function(...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/resources/lead_form_questions"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get metrics ready state
    #'
    #' @param date Analytics reports request date (UTC). Format: YYYY-MM-DD
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BookClosedResponse
    MetricsReadyStateGet = function(date, data_file = NULL, ...) {
      local_var_response <- self$MetricsReadyStateGetWithHttpInfo(date, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get metrics ready state
    #'
    #' @param date Analytics reports request date (UTC). Format: YYYY-MM-DD
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BookClosedResponse) with additional information such as HTTP status code, headers
    MetricsReadyStateGetWithHttpInfo = function(date, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`date`)) {
        stop("Missing required parameter `date`.")
      }

      if (!str_detect(`date`, "^(\\d{4})-(\\d{2})-(\\d{2})$")) {
        stop("Invalid value for `date` when calling ResourcesApi$MetricsReadyStateGet, must conform to the pattern ^(\\d{4})-(\\d{2})-(\\d{2})$.")
      }

      query_params[["date"]] <- `date`

      local_var_url_path <- "/resources/metrics_ready_state"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BookClosedResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get targeting options
    #'
    #' @param targeting_type Public targeting type.
    #' @param client_id (optional) Client ID.
    #' @param oauth_signature (optional) Oauth signature
    #' @param timestamp (optional) Timestamp
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[object]
    TargetingOptionsGet = function(targeting_type, client_id = NULL, oauth_signature = NULL, timestamp = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$TargetingOptionsGetWithHttpInfo(targeting_type, client_id, oauth_signature, timestamp, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get targeting options
    #'
    #' @param targeting_type Public targeting type.
    #' @param client_id (optional) Client ID.
    #' @param oauth_signature (optional) Oauth signature
    #' @param timestamp (optional) Timestamp
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[object]) with additional information such as HTTP status code, headers
    TargetingOptionsGetWithHttpInfo = function(targeting_type, client_id = NULL, oauth_signature = NULL, timestamp = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`targeting_type`)) {
        stop("Missing required parameter `targeting_type`.")
      }


      if (nchar(`client_id`) > 18) {
        stop("Invalid length for `client_id` when calling ResourcesApi$TargetingOptionsGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`client_id`, "^\\d+$")) {
        stop("Invalid value for `client_id` when calling ResourcesApi$TargetingOptionsGet, must conform to the pattern ^\\d+$.")
      }


      if (!str_detect(`timestamp`, "\\d+")) {
        stop("Invalid value for `timestamp` when calling ResourcesApi$TargetingOptionsGet, must conform to the pattern \\d+.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ResourcesApi$TargetingOptionsGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ResourcesApi$TargetingOptionsGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["client_id"]] <- `client_id`

      query_params[["oauth_signature"]] <- `oauth_signature`

      query_params[["timestamp"]] <- `timestamp`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/resources/targeting/{targeting_type}"
      if (!missing(`targeting_type`)) {
        local_var_url_path <- gsub("\\{targeting_type\\}", URLencode(as.character(`targeting_type`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[object]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
