#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Labels operations
#' @description LabelsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  LabelsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_label_create_request <- LabelCreateRequest$new(c(LabelCreateRequest_labels_inner$new(LabelType$new(), "value_example")), "parent_id_example") # LabelCreateRequest | 
#'
#' #Create labels
#' api_instance <- LabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LabelsCreate(var_ad_account_id, var_label_create_requestdata_file = "result.txt")
#' result <- api_instance$LabelsCreate(var_ad_account_id, var_label_create_request)
#' dput(result)
#'
#'
#' ####################  LabelsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_campaign_ids <- c("inner_example") # array[character] | List of Campaign Ids to use to filter the results. (Optional)
#' var_label_ids <- c("inner_example") # array[character] | List of Label Ids to use to filter the results. (Optional)
#' var_entity_statuses <- c("ACTIVE") # array[character] | Label entity status (Optional)
#' var_label_types <- c("BRAND") # array[character] | Label type. (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #List labels
#' api_instance <- LabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LabelsList(var_ad_account_id, campaign_ids = var_campaign_ids, label_ids = var_label_ids, entity_statuses = var_entity_statuses, label_types = var_label_types, page_size = var_page_size, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$LabelsList(var_ad_account_id, campaign_ids = var_campaign_ids, label_ids = var_label_ids, entity_statuses = var_entity_statuses, label_types = var_label_types, page_size = var_page_size, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  LabelsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_label_update_request <- LabelUpdateRequest$new(c(LabelUpdateRequest_labels_inner$new("id_example", LabelStatus$new(), "value_example"))) # LabelUpdateRequest | 
#'
#' #Update labels
#' api_instance <- LabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LabelsUpdate(var_ad_account_id, var_label_update_requestdata_file = "result.txt")
#' result <- api_instance$LabelsUpdate(var_ad_account_id, var_label_update_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LabelsApi <- R6::R6Class(
  "LabelsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new LabelsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param label_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LabelsResponse
    LabelsCreate = function(ad_account_id, label_create_request, data_file = NULL, ...) {
      local_var_response <- self$LabelsCreateWithHttpInfo(ad_account_id, label_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param label_create_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LabelsResponse) with additional information such as HTTP status code, headers
    LabelsCreateWithHttpInfo = function(ad_account_id, label_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`label_create_request`)) {
        stop("Missing required parameter `label_create_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LabelsApi$LabelsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`label_create_request`) && is.null(`label_create_request`)) {
        stop("Invalid value for `label_create_request` when calling LabelsApi$LabelsCreate, `label_create_request` is not nullable")
      }

      if (!is.null(`label_create_request`)) {
        local_var_body <- `label_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/labels"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LabelsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param label_ids (optional) List of Label Ids to use to filter the results.
    #' @param entity_statuses (optional) Label entity status (default value: ["ACTIVE"])
    #' @param label_types (optional) Label type. (default value: ["BRAND","CUSTOM"])
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LabelsList200Response
    LabelsList = function(ad_account_id, campaign_ids = NULL, label_ids = NULL, entity_statuses = ["ACTIVE"], label_types = ["BRAND","CUSTOM"], page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$LabelsListWithHttpInfo(ad_account_id, campaign_ids, label_ids, entity_statuses, label_types, page_size, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param label_ids (optional) List of Label Ids to use to filter the results.
    #' @param entity_statuses (optional) Label entity status (default value: ["ACTIVE"])
    #' @param label_types (optional) Label type. (default value: ["BRAND","CUSTOM"])
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LabelsList200Response) with additional information such as HTTP status code, headers
    LabelsListWithHttpInfo = function(ad_account_id, campaign_ids = NULL, label_ids = NULL, entity_statuses = ["ACTIVE"], label_types = ["BRAND","CUSTOM"], page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LabelsApi$LabelsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`campaign_ids`) && is.null(`campaign_ids`)) {
        stop("Invalid value for `campaign_ids` when calling LabelsApi$LabelsList, `campaign_ids` is not nullable")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) > 250) {
        stop("Invalid length for `campaign_ids` when calling LabelsApi$LabelsList, number of items must be less than or equal to 250.")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) < 1) {
        stop("Invalid length for `campaign_ids` when calling LabelsApi$LabelsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`label_ids`) && is.null(`label_ids`)) {
        stop("Invalid value for `label_ids` when calling LabelsApi$LabelsList, `label_ids` is not nullable")
      }
      if (!is.null(`label_ids`) && length(`label_ids`) > 250) {
        stop("Invalid length for `label_ids` when calling LabelsApi$LabelsList, number of items must be less than or equal to 250.")
      }
      if (!is.null(`label_ids`) && length(`label_ids`) < 1) {
        stop("Invalid length for `label_ids` when calling LabelsApi$LabelsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`entity_statuses`) && is.null(`entity_statuses`)) {
        stop("Invalid value for `entity_statuses` when calling LabelsApi$LabelsList, `entity_statuses` is not nullable")
      }

      if (!missing(`label_types`) && is.null(`label_types`)) {
        stop("Invalid value for `label_types` when calling LabelsApi$LabelsList, `label_types` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling LabelsApi$LabelsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling LabelsApi$LabelsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling LabelsApi$LabelsList, must be bigger than or equal to 1.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling LabelsApi$LabelsList, `bookmark` is not nullable")
      }

      # explore
      for (query_item in `campaign_ids`) {
        query_params[["campaign_ids"]] <- c(query_params[["campaign_ids"]], list(`campaign_ids` = query_item))
      }

      # explore
      for (query_item in `label_ids`) {
        query_params[["label_ids"]] <- c(query_params[["label_ids"]], list(`label_ids` = query_item))
      }

      # explore
      for (query_item in `entity_statuses`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("ACTIVE", "ARCHIVED"))) {
          stop("Invalid value for entity_statuses when calling LabelsApi$LabelsList. Must be [ACTIVE, ARCHIVED].")
        }
        query_params[["entity_statuses"]] <- c(query_params[["entity_statuses"]], list(`entity_statuses` = query_item))
      }

      # explore
      for (query_item in `label_types`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("BRAND", "CUSTOM"))) {
          stop("Invalid value for label_types when calling LabelsApi$LabelsList. Must be [BRAND, CUSTOM].")
        }
        query_params[["label_types"]] <- c(query_params[["label_types"]], list(`label_types` = query_item))
      }

      query_params[["page_size"]] <- `page_size`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/labels"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LabelsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param label_update_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LabelsResponse
    LabelsUpdate = function(ad_account_id, label_update_request, data_file = NULL, ...) {
      local_var_response <- self$LabelsUpdateWithHttpInfo(ad_account_id, label_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update labels
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param label_update_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LabelsResponse) with additional information such as HTTP status code, headers
    LabelsUpdateWithHttpInfo = function(ad_account_id, label_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`label_update_request`)) {
        stop("Missing required parameter `label_update_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LabelsApi$LabelsUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LabelsApi$LabelsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`label_update_request`) && is.null(`label_update_request`)) {
        stop("Invalid value for `label_update_request` when calling LabelsApi$LabelsUpdate, `label_update_request` is not nullable")
      }

      if (!is.null(`label_update_request`)) {
        local_var_body <- `label_update_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/labels"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LabelsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
