#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Trends operations
#' @description TrendsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  TrendsEditorialArticlesList  ####################
#'
#' library(openapi)
#' var_region <- ProductCategoryRegion$new() # ProductCategoryRegion |      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
#'
#' #Returns editorial articles for a given region
#' api_instance <- TrendsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TrendsEditorialArticlesList(var_regiondata_file = "result.txt")
#' result <- api_instance$TrendsEditorialArticlesList(var_region)
#' dput(result)
#'
#'
#' ####################  TrendsFeaturedTopicsList  ####################
#'
#' library(openapi)
#' var_region <- ProductCategoryRegion$new() # ProductCategoryRegion |      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
#' var_interest <- InterestsEnum$new() # InterestsEnum | Interest to filter by (Optional)
#'
#' #Get featured topics
#' api_instance <- TrendsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TrendsFeaturedTopicsList(var_region, interest = var_interestdata_file = "result.txt")
#' result <- api_instance$TrendsFeaturedTopicsList(var_region, interest = var_interest)
#' dput(result)
#'
#'
#' ####################  TrendsProductCategoriesDetailsList  ####################
#'
#' library(openapi)
#' var_product_categories <- c(ProductCategoryEnum$new()) # array[ProductCategoryEnum] | List of product categories
#' var_region <- ProductCategoryRegion$new() # ProductCategoryRegion |      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
#' var_lookback_window <- ProductCategoryDetailLookbackWindow$new() # ProductCategoryDetailLookbackWindow |   Time period for historical data analysis in days. The lookback window defines how far back in time the API will analyze data to compute trend metrics.   - `90` - Last 90 days (3 months)   - `180` - Last 180 days (6 months)   - `365` - Last 365 days (1 year)   - `730` - Last 730 days (2 years) (Optional)
#' var_engagement_type <- ProductCategoriesEngagementType$new() # ProductCategoriesEngagementType |     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves (Optional)
#'
#' #Get product category details
#' api_instance <- TrendsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TrendsProductCategoriesDetailsList(var_product_categories, var_region, lookback_window = var_lookback_window, engagement_type = var_engagement_typedata_file = "result.txt")
#' result <- api_instance$TrendsProductCategoriesDetailsList(var_product_categories, var_region, lookback_window = var_lookback_window, engagement_type = var_engagement_type)
#' dput(result)
#'
#'
#' ####################  TrendsProductCategoriesTrendingList  ####################
#'
#' library(openapi)
#' var_region <- ProductCategoryRegion$new() # ProductCategoryRegion |      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
#' var_verticals <- c(VerticalProductCategory$new()) # array[VerticalProductCategory] | List of verticals to filter by (Optional)
#' var_ages <- c(AgeTrendsBucket$new()) # array[AgeTrendsBucket] | Age to filter by. If not provided, the results will be filtered by all ages. (Optional)
#' var_genders <- c(GenderBucket$new()) # array[GenderBucket] | Gender to filter by, If not provided, the results will be filtered by all genders. (Optional)
#' var_engagement_type <- ProductCategoriesEngagementType$new() # ProductCategoriesEngagementType |     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves (Optional)
#'
#' #Get a list of growing Shopping Product Categories
#' api_instance <- TrendsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$TrendsProductCategoriesTrendingList(var_region, verticals = var_verticals, ages = var_ages, genders = var_genders, engagement_type = var_engagement_typedata_file = "result.txt")
#' result <- api_instance$TrendsProductCategoriesTrendingList(var_region, verticals = var_verticals, ages = var_ages, genders = var_genders, engagement_type = var_engagement_type)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
TrendsApi <- R6::R6Class(
  "TrendsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new TrendsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Returns editorial articles for a given region
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[TrendsEditorial]
    TrendsEditorialArticlesList = function(region, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TrendsEditorialArticlesListWithHttpInfo(region, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Returns editorial articles for a given region
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[TrendsEditorial]) with additional information such as HTTP status code, headers
    TrendsEditorialArticlesListWithHttpInfo = function(region, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`region`)) {
        stop("Missing required parameter `region`.")
      }

      if (!missing(`region`) && is.null(`region`)) {
        stop("Invalid value for `region` when calling TrendsApi$TrendsEditorialArticlesList, `region` is not nullable")
      }

      query_params[["region"]] <- `region`

      local_var_url_path <- "/trends/editorial_articles"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[TrendsEditorial]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get featured topics
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param interest (optional) Interest to filter by
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[FeaturedTrend]
    TrendsFeaturedTopicsList = function(region, interest = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TrendsFeaturedTopicsListWithHttpInfo(region, interest, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get featured topics
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param interest (optional) Interest to filter by
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[FeaturedTrend]) with additional information such as HTTP status code, headers
    TrendsFeaturedTopicsListWithHttpInfo = function(region, interest = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`region`)) {
        stop("Missing required parameter `region`.")
      }

      if (!missing(`region`) && is.null(`region`)) {
        stop("Invalid value for `region` when calling TrendsApi$TrendsFeaturedTopicsList, `region` is not nullable")
      }

      if (!missing(`interest`) && is.null(`interest`)) {
        stop("Invalid value for `interest` when calling TrendsApi$TrendsFeaturedTopicsList, `interest` is not nullable")
      }

      query_params[["interest"]] <- `interest`

      query_params[["region"]] <- `region`

      local_var_url_path <- "/trends/topics/featured"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[FeaturedTrend]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get product category details
    #'
    #' @param product_categories List of product categories
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param lookback_window (optional)   Time period for historical data analysis in days. The lookback window defines how far back in time the API will analyze data to compute trend metrics.   - `90` - Last 90 days (3 months)   - `180` - Last 180 days (6 months)   - `365` - Last 365 days (1 year)   - `730` - Last 730 days (2 years)
    #' @param engagement_type (optional)     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[ProductCategoryDetails]
    TrendsProductCategoriesDetailsList = function(product_categories, region, lookback_window = NULL, engagement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TrendsProductCategoriesDetailsListWithHttpInfo(product_categories, region, lookback_window, engagement_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get product category details
    #'
    #' @param product_categories List of product categories
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param lookback_window (optional)   Time period for historical data analysis in days. The lookback window defines how far back in time the API will analyze data to compute trend metrics.   - `90` - Last 90 days (3 months)   - `180` - Last 180 days (6 months)   - `365` - Last 365 days (1 year)   - `730` - Last 730 days (2 years)
    #' @param engagement_type (optional)     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[ProductCategoryDetails]) with additional information such as HTTP status code, headers
    TrendsProductCategoriesDetailsListWithHttpInfo = function(product_categories, region, lookback_window = NULL, engagement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`product_categories`)) {
        stop("Missing required parameter `product_categories`.")
      }

      if (missing(`region`)) {
        stop("Missing required parameter `region`.")
      }

      if (!missing(`product_categories`) && is.null(`product_categories`)) {
        stop("Invalid value for `product_categories` when calling TrendsApi$TrendsProductCategoriesDetailsList, `product_categories` is not nullable")
      }
      if (!is.null(`product_categories`) && length(`product_categories`) > 20) {
        stop("Invalid length for `product_categories` when calling TrendsApi$TrendsProductCategoriesDetailsList, number of items must be less than or equal to 20.")
      }
      if (!is.null(`product_categories`) && length(`product_categories`) < 1) {
        stop("Invalid length for `product_categories` when calling TrendsApi$TrendsProductCategoriesDetailsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`region`) && is.null(`region`)) {
        stop("Invalid value for `region` when calling TrendsApi$TrendsProductCategoriesDetailsList, `region` is not nullable")
      }

      if (!missing(`lookback_window`) && is.null(`lookback_window`)) {
        stop("Invalid value for `lookback_window` when calling TrendsApi$TrendsProductCategoriesDetailsList, `lookback_window` is not nullable")
      }

      if (!missing(`engagement_type`) && is.null(`engagement_type`)) {
        stop("Invalid value for `engagement_type` when calling TrendsApi$TrendsProductCategoriesDetailsList, `engagement_type` is not nullable")
      }

      # explore
      for (query_item in `product_categories`) {
        query_params[["product_categories"]] <- c(query_params[["product_categories"]], list(`product_categories` = query_item))
      }

      query_params[["region"]] <- `region`

      query_params[["lookback_window"]] <- `lookback_window`

      query_params[["engagement_type"]] <- `engagement_type`

      local_var_url_path <- "/trends/product_categories/details"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[ProductCategoryDetails]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get a list of growing Shopping Product Categories
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param verticals (optional) List of verticals to filter by
    #' @param ages (optional) Age to filter by. If not provided, the results will be filtered by all ages.
    #' @param genders (optional) Gender to filter by, If not provided, the results will be filtered by all genders.
    #' @param engagement_type (optional)     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[TrendingProductCategory]
    TrendsProductCategoriesTrendingList = function(region, verticals = NULL, ages = NULL, genders = NULL, engagement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$TrendsProductCategoriesTrendingListWithHttpInfo(region, verticals, ages, genders, engagement_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get a list of growing Shopping Product Categories
    #'
    #' @param region      The geographic region of interest. Only top product categories within the specified region will be returned.      The `region` parameter is formatted as ISO 3166-2 country codes delimited by `+`.      - `US` - United States     - `GB+IE` - Great Britain & Ireland     - `CA` - Canada
    #' @param verticals (optional) List of verticals to filter by
    #' @param ages (optional) Age to filter by. If not provided, the results will be filtered by all ages.
    #' @param genders (optional) Gender to filter by, If not provided, the results will be filtered by all genders.
    #' @param engagement_type (optional)     Type of engagement metric to analyze. - `ENGAGEMENT` - Overall engagement metric - `OUTBOUND_CLICK` - Number of outbound clicks - `SAVE` - Number of pin saves
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[TrendingProductCategory]) with additional information such as HTTP status code, headers
    TrendsProductCategoriesTrendingListWithHttpInfo = function(region, verticals = NULL, ages = NULL, genders = NULL, engagement_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`region`)) {
        stop("Missing required parameter `region`.")
      }

      if (!missing(`region`) && is.null(`region`)) {
        stop("Invalid value for `region` when calling TrendsApi$TrendsProductCategoriesTrendingList, `region` is not nullable")
      }

      if (!missing(`verticals`) && is.null(`verticals`)) {
        stop("Invalid value for `verticals` when calling TrendsApi$TrendsProductCategoriesTrendingList, `verticals` is not nullable")
      }

      if (!missing(`ages`) && is.null(`ages`)) {
        stop("Invalid value for `ages` when calling TrendsApi$TrendsProductCategoriesTrendingList, `ages` is not nullable")
      }

      if (!missing(`genders`) && is.null(`genders`)) {
        stop("Invalid value for `genders` when calling TrendsApi$TrendsProductCategoriesTrendingList, `genders` is not nullable")
      }

      if (!missing(`engagement_type`) && is.null(`engagement_type`)) {
        stop("Invalid value for `engagement_type` when calling TrendsApi$TrendsProductCategoriesTrendingList, `engagement_type` is not nullable")
      }

      query_params[["region"]] <- `region`

      # explore
      for (query_item in `verticals`) {
        query_params[["verticals"]] <- c(query_params[["verticals"]], list(`verticals` = query_item))
      }

      # explore
      for (query_item in `ages`) {
        query_params[["ages"]] <- c(query_params[["ages"]], list(`ages` = query_item))
      }

      # explore
      for (query_item in `genders`) {
        query_params[["genders"]] <- c(query_params[["genders"]], list(`genders` = query_item))
      }

      query_params[["engagement_type"]] <- `engagement_type`

      local_var_url_path <- "/trends/product_categories/trending"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[TrendingProductCategory]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
