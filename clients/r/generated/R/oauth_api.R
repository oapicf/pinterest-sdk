#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Oauth operations
#' @description OauthApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  OauthConversionToken  ####################
#'
#' library(openapi)
#'
#' #Generate OAuth access token for conversion API
#' api_instance <- OauthApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OauthConversionToken(data_file = "result.txt")
#' result <- api_instance$OauthConversionToken()
#' dput(result)
#'
#'
#' ####################  OauthToken  ####################
#'
#' library(openapi)
#' var_grant_type <- TokenGrantType$new() # TokenGrantType | 
#' var_code <- "code_example" # character |  (Optional)
#' var_continuous_refresh <- "continuous_refresh_example" # character |   If your app was created before **September 25, 2025**, set to `true` to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token. (Optional)
#' var_redirect_uri <- "redirect_uri_example" # character |  (Optional)
#' var_refresh_token <- "refresh_token_example" # character |  (Optional)
#' var_scope <- "scope_example" # character |  (Optional)
#'
#' #Generate OAuth access token
#' api_instance <- OauthApi$new()
#'
#' # Configure HTTP basic authorization: basic
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OauthToken(var_grant_type, code = var_code, continuous_refresh = var_continuous_refresh, redirect_uri = var_redirect_uri, refresh_token = var_refresh_token, scope = var_scopedata_file = "result.txt")
#' result <- api_instance$OauthToken(var_grant_type, code = var_code, continuous_refresh = var_continuous_refresh, redirect_uri = var_redirect_uri, refresh_token = var_refresh_token, scope = var_scope)
#' dput(result)
#'
#'
#' ####################  TokenRevoke  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | The token to revoke.
#' var_token_type_hint <- TokenTypeHint$new() # TokenTypeHint | The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information. (Optional)
#'
#' #Revoke a token
#' api_instance <- OauthApi$new()
#'
#' # Configure HTTP basic authorization: basic
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' api_instance$TokenRevoke(var_token, token_type_hint = var_token_type_hint)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OauthApi <- R6::R6Class(
  "OauthApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new OauthApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Generate OAuth access token for conversion API
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ConversionAccessToken
    OauthConversionToken = function(data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$OauthConversionTokenWithHttpInfo(data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Generate OAuth access token for conversion API
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ConversionAccessToken) with additional information such as HTTP status code, headers
    OauthConversionTokenWithHttpInfo = function(data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/oauth/conversion_token"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ConversionAccessToken"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Generate OAuth access token
    #'
    #' @param grant_type 
    #' @param code (optional) No description
    #' @param continuous_refresh (optional)   If your app was created before **September 25, 2025**, set to `true` to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token.
    #' @param redirect_uri (optional) No description
    #' @param refresh_token (optional) No description
    #' @param scope (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return OauthAccessToken
    OauthToken = function(grant_type, code = NULL, continuous_refresh = NULL, redirect_uri = NULL, refresh_token = NULL, scope = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$OauthTokenWithHttpInfo(grant_type, code, continuous_refresh, redirect_uri, refresh_token, scope, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Generate OAuth access token
    #'
    #' @param grant_type 
    #' @param code (optional) No description
    #' @param continuous_refresh (optional)   If your app was created before **September 25, 2025**, set to `true` to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token.
    #' @param redirect_uri (optional) No description
    #' @param refresh_token (optional) No description
    #' @param scope (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (OauthAccessToken) with additional information such as HTTP status code, headers
    OauthTokenWithHttpInfo = function(grant_type, code = NULL, continuous_refresh = NULL, redirect_uri = NULL, refresh_token = NULL, scope = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`grant_type`)) {
        stop("Missing required parameter `grant_type`.")
      }

      if (!missing(`grant_type`) && is.null(`grant_type`)) {
        stop("Invalid value for `grant_type` when calling OauthApi$OauthToken, `grant_type` is not nullable")
      }

      if (!missing(`code`) && is.null(`code`)) {
        stop("Invalid value for `code` when calling OauthApi$OauthToken, `code` is not nullable")
      }

      if (!missing(`continuous_refresh`) && is.null(`continuous_refresh`)) {
        stop("Invalid value for `continuous_refresh` when calling OauthApi$OauthToken, `continuous_refresh` is not nullable")
      }

      if (!missing(`redirect_uri`) && is.null(`redirect_uri`)) {
        stop("Invalid value for `redirect_uri` when calling OauthApi$OauthToken, `redirect_uri` is not nullable")
      }

      if (!missing(`refresh_token`) && is.null(`refresh_token`)) {
        stop("Invalid value for `refresh_token` when calling OauthApi$OauthToken, `refresh_token` is not nullable")
      }

      if (!missing(`scope`) && is.null(`scope`)) {
        stop("Invalid value for `scope` when calling OauthApi$OauthToken, `scope` is not nullable")
      }

      form_params["code"] <- `code`
      form_params["continuous_refresh"] <- `continuous_refresh`
      form_params["grant_type"] <- `grant_type`
      form_params["redirect_uri"] <- `redirect_uri`
      form_params["refresh_token"] <- `refresh_token`
      form_params["scope"] <- `scope`
      local_var_url_path <- "/oauth/token"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "OauthAccessToken"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Revoke a token
    #'
    #' @param token The token to revoke.
    #' @param token_type_hint (optional) The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information.
    #' @param ... Other optional arguments
    #'
    #' @return void
    TokenRevoke = function(token, token_type_hint = NULL, ...) {
      local_var_response <- self$TokenRevokeWithHttpInfo(token, token_type_hint, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Revoke a token
    #'
    #' @param token The token to revoke.
    #' @param token_type_hint (optional) The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    TokenRevokeWithHttpInfo = function(token, token_type_hint = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling OauthApi$TokenRevoke, `token` is not nullable")
      }

      if (!missing(`token_type_hint`) && is.null(`token_type_hint`)) {
        stop("Invalid value for `token_type_hint` when calling OauthApi$TokenRevoke, `token_type_hint` is not nullable")
      }

      form_params["token"] <- `token`
      form_params["token_type_hint"] <- `token_type_hint`
      local_var_url_path <- "/oauth/token/revoke"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
