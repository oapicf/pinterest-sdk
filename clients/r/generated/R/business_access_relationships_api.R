#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BusinessAccessRelationships operations
#' @description BusinessAccessRelationshipsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  DeleteBusinessMembership  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Business id
#' var_members_to_delete_body <- MembersToDeleteBody$new(c(MembersToDeleteBody_members_inner$new("member_id_example", BusinessRoleForMembers$new()))) # MembersToDeleteBody | List of members with role to delete.
#'
#' #Terminate business memberships
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteBusinessMembership(var_business_id, var_members_to_delete_bodydata_file = "result.txt")
#' result <- api_instance$DeleteBusinessMembership(var_business_id, var_members_to_delete_body)
#' dput(result)
#'
#'
#' ####################  DeleteBusinessPartners  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_delete_partners_request <- DeletePartnersRequest$new(c("partner_ids_example"), "partner_type_example") # DeletePartnersRequest | An object containing a \"partner_ids\" property composed of a list of partner IDs and a \"partners_type\" property specifying the type of partners to delete. 
#'
#' #Terminate business partnerships
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteBusinessPartners(var_business_id, var_delete_partners_requestdata_file = "result.txt")
#' result <- api_instance$DeleteBusinessPartners(var_business_id, var_delete_partners_request)
#' dput(result)
#'
#'
#' ####################  GetBusinessEmployers  ####################
#'
#' library(openapi)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #List business employers for user
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBusinessEmployers(page_size = var_page_size, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$GetBusinessEmployers(page_size = var_page_size, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  GetBusinessMembers  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_assets_summary <- FALSE # character | Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (Optional)
#' var_business_roles <- c(MemberBusinessRole$new()) # array[MemberBusinessRole] | A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned. (Optional)
#' var_member_ids <- "00101010101,2222220101" # character | A list of business members ids separated by comma. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get business members
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBusinessMembers(var_business_id, assets_summary = var_assets_summary, business_roles = var_business_roles, member_ids = var_member_ids, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$GetBusinessMembers(var_business_id, assets_summary = var_assets_summary, business_roles = var_business_roles, member_ids = var_member_ids, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  GetBusinessPartners  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_assets_summary <- FALSE # character | Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (Optional)
#' var_partner_type <- PartnerType$new() # PartnerType | Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset. (Optional)
#' var_partner_ids <- "00101010101,2222220101" # character | A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #Get business partners
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBusinessPartners(var_business_id, assets_summary = var_assets_summary, partner_type = var_partner_type, partner_ids = var_partner_ids, start_index = var_start_index, page_size = var_page_size, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$GetBusinessPartners(var_business_id, assets_summary = var_assets_summary, partner_type = var_partner_type, partner_ids = var_partner_ids, start_index = var_start_index, page_size = var_page_size, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  UpdateBusinessMemberships  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Business id
#' var_update_member_business_role_body <- c(UpdateMemberBusinessRoleBody$new(BusinessRoleForMembers$new(), "member_id_example")) # array[UpdateMemberBusinessRoleBody] | List of objects with the member id and the business_role.
#'
#' #Update member's business role
#' api_instance <- BusinessAccessRelationshipsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBusinessMemberships(var_business_id, var_update_member_business_role_bodydata_file = "result.txt")
#' result <- api_instance$UpdateBusinessMemberships(var_business_id, var_update_member_business_role_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BusinessAccessRelationshipsApi <- R6::R6Class(
  "BusinessAccessRelationshipsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BusinessAccessRelationshipsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Terminate business memberships
    #'
    #' @param business_id Business id
    #' @param members_to_delete_body List of members with role to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeletedMembersResponse
    DeleteBusinessMembership = function(business_id, members_to_delete_body, data_file = NULL, ...) {
      local_var_response <- self$DeleteBusinessMembershipWithHttpInfo(business_id, members_to_delete_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Terminate business memberships
    #'
    #' @param business_id Business id
    #' @param members_to_delete_body List of members with role to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeletedMembersResponse) with additional information such as HTTP status code, headers
    DeleteBusinessMembershipWithHttpInfo = function(business_id, members_to_delete_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`members_to_delete_body`)) {
        stop("Missing required parameter `members_to_delete_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessMembership, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessMembership, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessMembership, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`members_to_delete_body`)) {
        local_var_body <- `members_to_delete_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeletedMembersResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Terminate business partnerships
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partners_request An object containing a \"partner_ids\" property composed of a list of partner IDs and a \"partners_type\" property specifying the type of partners to delete. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeletePartnersResponse
    DeleteBusinessPartners = function(business_id, delete_partners_request, data_file = NULL, ...) {
      local_var_response <- self$DeleteBusinessPartnersWithHttpInfo(business_id, delete_partners_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Terminate business partnerships
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partners_request An object containing a \"partner_ids\" property composed of a list of partner IDs and a \"partners_type\" property specifying the type of partners to delete. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeletePartnersResponse) with additional information such as HTTP status code, headers
    DeleteBusinessPartnersWithHttpInfo = function(business_id, delete_partners_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`delete_partners_request`)) {
        stop("Missing required parameter `delete_partners_request`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessPartners, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessPartners, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessRelationshipsApi$DeleteBusinessPartners, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`delete_partners_request`)) {
        local_var_body <- `delete_partners_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/partners"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeletePartnersResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' List business employers for user
    #'
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GetBusinessEmployers200Response
    GetBusinessEmployers = function(page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetBusinessEmployersWithHttpInfo(page_size, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' List business employers for user
    #'
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GetBusinessEmployers200Response) with additional information such as HTTP status code, headers
    GetBusinessEmployersWithHttpInfo = function(page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessEmployers, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessEmployers, must be bigger than or equal to 1.")
      }


      query_params[["page_size"]] <- `page_size`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/businesses/employers"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetBusinessEmployers200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get business members
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param assets_summary (optional) Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (default value: FALSE)
    #' @param business_roles (optional) A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned.
    #' @param member_ids (optional) A list of business members ids separated by comma.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GetBusinessMembers200Response
    GetBusinessMembers = function(business_id, assets_summary = FALSE, business_roles = NULL, member_ids = NULL, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$GetBusinessMembersWithHttpInfo(business_id, assets_summary, business_roles, member_ids, start_index, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get business members
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param assets_summary (optional) Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (default value: FALSE)
    #' @param business_roles (optional) A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned.
    #' @param member_ids (optional) A list of business members ids separated by comma.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GetBusinessMembers200Response) with additional information such as HTTP status code, headers
    GetBusinessMembersWithHttpInfo = function(business_id, assets_summary = FALSE, business_roles = NULL, member_ids = NULL, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must conform to the pattern ^\\d+$.")
      }



      if (nchar(`member_ids`) > 500) {
        stop("Invalid length for `member_ids` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be smaller than or equal to 500.")
      }

      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be bigger than or equal to 0.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessMembers, must be bigger than or equal to 1.")
      }

      query_params[["assets_summary"]] <- `assets_summary`

      # explore
      for (query_item in `business_roles`) {
        query_params[["business_roles"]] <- c(query_params[["business_roles"]], list(`business_roles` = query_item))
      }

      query_params[["member_ids"]] <- `member_ids`

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/members"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetBusinessMembers200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get business partners
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param assets_summary (optional) Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (default value: FALSE)
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
    #' @param partner_ids (optional) A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GetBusinessPartners200Response
    GetBusinessPartners = function(business_id, assets_summary = FALSE, partner_type = NULL, partner_ids = NULL, start_index = 0, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetBusinessPartnersWithHttpInfo(business_id, assets_summary, partner_type, partner_ids, start_index, page_size, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get business partners
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param assets_summary (optional) Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are (default value: FALSE)
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
    #' @param partner_ids (optional) A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GetBusinessPartners200Response) with additional information such as HTTP status code, headers
    GetBusinessPartnersWithHttpInfo = function(business_id, assets_summary = FALSE, partner_type = NULL, partner_ids = NULL, start_index = 0, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must conform to the pattern ^\\d+$.")
      }



      if (nchar(`partner_ids`) > 500) {
        stop("Invalid length for `partner_ids` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be smaller than or equal to 500.")
      }

      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be bigger than or equal to 0.")
      }

      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessRelationshipsApi$GetBusinessPartners, must be bigger than or equal to 1.")
      }


      query_params[["assets_summary"]] <- `assets_summary`

      query_params[["partner_type"]] <- `partner_type`

      query_params[["partner_ids"]] <- `partner_ids`

      query_params[["start_index"]] <- `start_index`

      query_params[["page_size"]] <- `page_size`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/businesses/{business_id}/partners"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetBusinessPartners200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update member's business role
    #'
    #' @param business_id Business id
    #' @param update_member_business_role_body List of objects with the member id and the business_role.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UpdateMemberResultsResponseArray
    UpdateBusinessMemberships = function(business_id, update_member_business_role_body, data_file = NULL, ...) {
      local_var_response <- self$UpdateBusinessMembershipsWithHttpInfo(business_id, update_member_business_role_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update member's business role
    #'
    #' @param business_id Business id
    #' @param update_member_business_role_body List of objects with the member id and the business_role.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UpdateMemberResultsResponseArray) with additional information such as HTTP status code, headers
    UpdateBusinessMembershipsWithHttpInfo = function(business_id, update_member_business_role_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_member_business_role_body`)) {
        stop("Missing required parameter `update_member_business_role_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$UpdateBusinessMemberships, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessRelationshipsApi$UpdateBusinessMemberships, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessRelationshipsApi$UpdateBusinessMemberships, must conform to the pattern ^\\d+$.")
      }

      if (length(`update_member_business_role_body`) < 1) {
        stop("Invalid length for `update_member_business_role_body` when calling BusinessAccessRelationshipsApi$UpdateBusinessMemberships, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`update_member_business_role_body`)) {
        body.items <- paste(unlist(lapply(`update_member_business_role_body`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "UpdateMemberResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
