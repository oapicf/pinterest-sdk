#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CustomerListUploads operations
#' @description CustomerListUploadsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CustomerListUploadsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_customer_list_id <- "customer_list_id_example" # character | Unique identifier of a customer list
#' var_customer_list_upload_create_request <- CustomerListUploadCreateRequest$new(UserListOperationType$new(), 123) # CustomerListUploadCreateRequest | Parameters to create a customer list upload request
#'
#' #Create customer list upload
#' api_instance <- CustomerListUploadsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CustomerListUploadsCreate(var_ad_account_id, var_customer_list_id, var_customer_list_upload_create_requestdata_file = "result.txt")
#' result <- api_instance$CustomerListUploadsCreate(var_ad_account_id, var_customer_list_id, var_customer_list_upload_create_request)
#' dput(result)
#'
#'
#' ####################  CustomerListUploadsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_customer_list_id <- "customer_list_id_example" # character | Unique identifier of a customer list
#' var_customer_list_upload_id <- "customer_list_upload_id_example" # character | Unique identifier of a customer list upload
#'
#' #Get customer list upload
#' api_instance <- CustomerListUploadsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CustomerListUploadsGet(var_ad_account_id, var_customer_list_id, var_customer_list_upload_iddata_file = "result.txt")
#' result <- api_instance$CustomerListUploadsGet(var_ad_account_id, var_customer_list_id, var_customer_list_upload_id)
#' dput(result)
#'
#'
#' ####################  CustomerListUploadsRun  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_customer_list_id <- "customer_list_id_example" # character | Unique identifier of a customer list
#' var_customer_list_upload_id <- "customer_list_upload_id_example" # character | Unique identifier of a customer list upload
#'
#' #Run customer list upload
#' api_instance <- CustomerListUploadsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CustomerListUploadsRun(var_ad_account_id, var_customer_list_id, var_customer_list_upload_iddata_file = "result.txt")
#' result <- api_instance$CustomerListUploadsRun(var_ad_account_id, var_customer_list_id, var_customer_list_upload_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CustomerListUploadsApi <- R6::R6Class(
  "CustomerListUploadsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CustomerListUploadsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_create_request Parameters to create a customer list upload request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CustomerListUploadCreateResponse
    CustomerListUploadsCreate = function(ad_account_id, customer_list_id, customer_list_upload_create_request, data_file = NULL, ...) {
      local_var_response <- self$CustomerListUploadsCreateWithHttpInfo(ad_account_id, customer_list_id, customer_list_upload_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_create_request Parameters to create a customer list upload request
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CustomerListUploadCreateResponse) with additional information such as HTTP status code, headers
    CustomerListUploadsCreateWithHttpInfo = function(ad_account_id, customer_list_id, customer_list_upload_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`customer_list_id`)) {
        stop("Missing required parameter `customer_list_id`.")
      }

      if (missing(`customer_list_upload_create_request`)) {
        stop("Missing required parameter `customer_list_upload_create_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_id`) && is.null(`customer_list_id`)) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, `customer_list_id` is not nullable")
      }
      if (!is.null(`customer_list_id`) && nchar(`customer_list_id`) > 18) {
        stop("Invalid length for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`customer_list_id`) && !stringr::str_detect(`customer_list_id`, "^\\d+$")) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_upload_create_request`) && is.null(`customer_list_upload_create_request`)) {
        stop("Invalid value for `customer_list_upload_create_request` when calling CustomerListUploadsApi$CustomerListUploadsCreate, `customer_list_upload_create_request` is not nullable")
      }

      if (!is.null(`customer_list_upload_create_request`)) {
        local_var_body <- `customer_list_upload_create_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`customer_list_id`)) {
        local_var_url_path <- gsub("\\{customer_list_id\\}", URLencode(as.character(`customer_list_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CustomerListUploadCreateResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_id Unique identifier of a customer list upload
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CustomerListUploadResponse
    CustomerListUploadsGet = function(ad_account_id, customer_list_id, customer_list_upload_id, data_file = NULL, ...) {
      local_var_response <- self$CustomerListUploadsGetWithHttpInfo(ad_account_id, customer_list_id, customer_list_upload_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_id Unique identifier of a customer list upload
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CustomerListUploadResponse) with additional information such as HTTP status code, headers
    CustomerListUploadsGetWithHttpInfo = function(ad_account_id, customer_list_id, customer_list_upload_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`customer_list_id`)) {
        stop("Missing required parameter `customer_list_id`.")
      }

      if (missing(`customer_list_upload_id`)) {
        stop("Missing required parameter `customer_list_upload_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_id`) && is.null(`customer_list_id`)) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, `customer_list_id` is not nullable")
      }
      if (!is.null(`customer_list_id`) && nchar(`customer_list_id`) > 18) {
        stop("Invalid length for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`customer_list_id`) && !stringr::str_detect(`customer_list_id`, "^\\d+$")) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_upload_id`) && is.null(`customer_list_upload_id`)) {
        stop("Invalid value for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, `customer_list_upload_id` is not nullable")
      }
      if (!is.null(`customer_list_upload_id`) && nchar(`customer_list_upload_id`) > 18) {
        stop("Invalid length for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`customer_list_upload_id`) && !stringr::str_detect(`customer_list_upload_id`, "^\\d+$")) {
        stop("Invalid value for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`customer_list_id`)) {
        local_var_url_path <- gsub("\\{customer_list_id\\}", URLencode(as.character(`customer_list_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`customer_list_upload_id`)) {
        local_var_url_path <- gsub("\\{customer_list_upload_id\\}", URLencode(as.character(`customer_list_upload_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CustomerListUploadResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Run customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_id Unique identifier of a customer list upload
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CustomerListUploadResponse
    CustomerListUploadsRun = function(ad_account_id, customer_list_id, customer_list_upload_id, data_file = NULL, ...) {
      local_var_response <- self$CustomerListUploadsRunWithHttpInfo(ad_account_id, customer_list_id, customer_list_upload_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Run customer list upload
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param customer_list_id Unique identifier of a customer list
    #' @param customer_list_upload_id Unique identifier of a customer list upload
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CustomerListUploadResponse) with additional information such as HTTP status code, headers
    CustomerListUploadsRunWithHttpInfo = function(ad_account_id, customer_list_id, customer_list_upload_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`customer_list_id`)) {
        stop("Missing required parameter `customer_list_id`.")
      }

      if (missing(`customer_list_upload_id`)) {
        stop("Missing required parameter `customer_list_upload_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_id`) && is.null(`customer_list_id`)) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, `customer_list_id` is not nullable")
      }
      if (!is.null(`customer_list_id`) && nchar(`customer_list_id`) > 18) {
        stop("Invalid length for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must be smaller than or equal to 18.")
      }
      if (!is.null(`customer_list_id`) && !stringr::str_detect(`customer_list_id`, "^\\d+$")) {
        stop("Invalid value for `customer_list_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`customer_list_upload_id`) && is.null(`customer_list_upload_id`)) {
        stop("Invalid value for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, `customer_list_upload_id` is not nullable")
      }
      if (!is.null(`customer_list_upload_id`) && nchar(`customer_list_upload_id`) > 18) {
        stop("Invalid length for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must be smaller than or equal to 18.")
      }
      if (!is.null(`customer_list_upload_id`) && !stringr::str_detect(`customer_list_upload_id`, "^\\d+$")) {
        stop("Invalid value for `customer_list_upload_id` when calling CustomerListUploadsApi$CustomerListUploadsRun, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}/run"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`customer_list_id`)) {
        local_var_url_path <- gsub("\\{customer_list_id\\}", URLencode(as.character(`customer_list_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`customer_list_upload_id`)) {
        local_var_url_path <- gsub("\\{customer_list_upload_id\\}", URLencode(as.character(`customer_list_upload_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CustomerListUploadResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
