#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CatalogSupplemental operations
#' @description CatalogSupplementalApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CatalogsLocalInventoryItemsBatchOperate  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_local_inventory_items_batch_create <- LocalInventoryItemsBatchCreate$new(c(LocalInventoryOperation$new(RetailLocalInventoryItemAttributes$new(ItemAvailability$new(), "price_example", "ad_link_example", "sale_price_example"), "item_id_example", "DELETE", "store_code_example"))) # LocalInventoryItemsBatchCreate | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Operate on local inventory item batch
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalInventoryItemsBatchOperate(var_catalog_id, var_local_inventory_items_batch_create, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsLocalInventoryItemsBatchOperate(var_catalog_id, var_local_inventory_items_batch_create, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CatalogsLocalInventoryItemsPost  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_local_inventory_items_get_create <- LocalInventoryItemsGetCreate$new(c(ItemIdStoreCodePair$new("item_id_example", "store_code_example"))) # LocalInventoryItemsGetCreate | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get local inventory items (POST)
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalInventoryItemsPost(var_catalog_id, var_local_inventory_items_get_create, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsLocalInventoryItemsPost(var_catalog_id, var_local_inventory_items_get_create, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CatalogsLocalStoresCreate  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_local_store_create <- c(LocalStoreCreate$new(Country$new(), "name_example", "store_code_example", "address_primary_example", "address_secondary_example", "city_example", 123, 123, "postal_code_example", "region_example")) # array[LocalStoreCreate] | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Create local stores
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalStoresCreate(var_catalog_id, var_local_store_create, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsLocalStoresCreate(var_catalog_id, var_local_store_create, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CatalogsLocalStoresDelete  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_ids <- c("inner_example") # array[character] | List of local store IDs to filter by.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Delete local stores
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalStoresDelete(var_catalog_id, var_ids, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsLocalStoresDelete(var_catalog_id, var_ids, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CatalogsLocalStoresList  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_ids <- c("inner_example") # array[character] | List of local store IDs to filter by. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #List local stores
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalStoresList(var_catalog_id, ids = var_ids, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$CatalogsLocalStoresList(var_catalog_id, ids = var_ids, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  CatalogsLocalStoresUpdate  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_local_store_batch_update <- c(LocalStoreBatchUpdate$new("id_example", "address_primary_example", "address_secondary_example", "city_example", Country$new(), 123, 123, "name_example", "postal_code_example", "region_example", "store_code_example")) # array[LocalStoreBatchUpdate] | 
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Update local stores
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsLocalStoresUpdate(var_catalog_id, var_local_store_batch_update, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsLocalStoresUpdate(var_catalog_id, var_local_store_batch_update, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CatalogsSupplementalItemsBatchGet  ####################
#'
#' library(openapi)
#' var_catalog_id <- "catalog_id_example" # character | Unique identifier of a catalog.
#' var_batch_id <- "batch_id_example" # character | Unique identifier of an items batch operation.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get supplemental items batch status
#' api_instance <- CatalogSupplementalApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CatalogsSupplementalItemsBatchGet(var_catalog_id, var_batch_id, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CatalogsSupplementalItemsBatchGet(var_catalog_id, var_batch_id, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CatalogSupplementalApi <- R6::R6Class(
  "CatalogSupplementalApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CatalogSupplementalApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Operate on local inventory item batch
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_inventory_items_batch_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SupplementalItemsBatchResponse
    CatalogsLocalInventoryItemsBatchOperate = function(catalog_id, local_inventory_items_batch_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalInventoryItemsBatchOperateWithHttpInfo(catalog_id, local_inventory_items_batch_create, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Operate on local inventory item batch
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_inventory_items_batch_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SupplementalItemsBatchResponse) with additional information such as HTTP status code, headers
    CatalogsLocalInventoryItemsBatchOperateWithHttpInfo = function(catalog_id, local_inventory_items_batch_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`local_inventory_items_batch_create`)) {
        stop("Missing required parameter `local_inventory_items_batch_create`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`local_inventory_items_batch_create`) && is.null(`local_inventory_items_batch_create`)) {
        stop("Invalid value for `local_inventory_items_batch_create` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, `local_inventory_items_batch_create` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsBatchOperate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`local_inventory_items_batch_create`)) {
        local_var_body <- `local_inventory_items_batch_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/catalogs/{catalog_id}/local_inventory_items/batch"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SupplementalItemsBatchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get local inventory items (POST)
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_inventory_items_get_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return LocalInventoryItemsGet
    CatalogsLocalInventoryItemsPost = function(catalog_id, local_inventory_items_get_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalInventoryItemsPostWithHttpInfo(catalog_id, local_inventory_items_get_create, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get local inventory items (POST)
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_inventory_items_get_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (LocalInventoryItemsGet) with additional information such as HTTP status code, headers
    CatalogsLocalInventoryItemsPostWithHttpInfo = function(catalog_id, local_inventory_items_get_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`local_inventory_items_get_create`)) {
        stop("Missing required parameter `local_inventory_items_get_create`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`local_inventory_items_get_create`) && is.null(`local_inventory_items_get_create`)) {
        stop("Invalid value for `local_inventory_items_get_create` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, `local_inventory_items_get_create` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalInventoryItemsPost, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`local_inventory_items_get_create`)) {
        local_var_body <- `local_inventory_items_get_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/catalogs/{catalog_id}/local_inventory_items/query"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "LocalInventoryItemsGet"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_store_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CatalogsLocalStoresCreate200ResponseInner]
    CatalogsLocalStoresCreate = function(catalog_id, local_store_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalStoresCreateWithHttpInfo(catalog_id, local_store_create, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_store_create 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CatalogsLocalStoresCreate200ResponseInner]) with additional information such as HTTP status code, headers
    CatalogsLocalStoresCreateWithHttpInfo = function(catalog_id, local_store_create, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`local_store_create`)) {
        stop("Missing required parameter `local_store_create`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`local_store_create`) && is.null(`local_store_create`)) {
        stop("Invalid value for `local_store_create` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, `local_store_create` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresCreate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`local_store_create`)) {
        body.items <- paste(unlist(lapply(`local_store_create`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/catalogs/{catalog_id}/local_stores"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CatalogsLocalStoresCreate200ResponseInner]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param ids List of local store IDs to filter by.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CatalogsLocalStoresDelete200ResponseInner]
    CatalogsLocalStoresDelete = function(catalog_id, ids, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalStoresDeleteWithHttpInfo(catalog_id, ids, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param ids List of local store IDs to filter by.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CatalogsLocalStoresDelete200ResponseInner]) with additional information such as HTTP status code, headers
    CatalogsLocalStoresDeleteWithHttpInfo = function(catalog_id, ids, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`ids`)) {
        stop("Missing required parameter `ids`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ids`) && is.null(`ids`)) {
        stop("Invalid value for `ids` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, `ids` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresDelete, must conform to the pattern ^\\d+$.")
      }

      # no explore
      query_params[["ids"]] <- I(paste(lapply(`ids`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/catalogs/{catalog_id}/local_stores"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CatalogsLocalStoresDelete200ResponseInner]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param ids (optional) List of local store IDs to filter by.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CatalogsLocalStoresList200Response
    CatalogsLocalStoresList = function(catalog_id, ids = NULL, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalStoresListWithHttpInfo(catalog_id, ids, ad_account_id, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param ids (optional) List of local store IDs to filter by.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CatalogsLocalStoresList200Response) with additional information such as HTTP status code, headers
    CatalogsLocalStoresListWithHttpInfo = function(catalog_id, ids = NULL, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ids`) && is.null(`ids`)) {
        stop("Invalid value for `ids` when calling CatalogSupplementalApi$CatalogsLocalStoresList, `ids` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling CatalogSupplementalApi$CatalogsLocalStoresList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling CatalogSupplementalApi$CatalogsLocalStoresList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling CatalogSupplementalApi$CatalogsLocalStoresList, must be bigger than or equal to 1.")
      }

      # no explore
      query_params[["ids"]] <- I(paste(lapply(`ids`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/catalogs/{catalog_id}/local_stores"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CatalogsLocalStoresList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_store_batch_update 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CatalogsLocalStoresCreate200ResponseInner]
    CatalogsLocalStoresUpdate = function(catalog_id, local_store_batch_update, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsLocalStoresUpdateWithHttpInfo(catalog_id, local_store_batch_update, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update local stores
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param local_store_batch_update 
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CatalogsLocalStoresCreate200ResponseInner]) with additional information such as HTTP status code, headers
    CatalogsLocalStoresUpdateWithHttpInfo = function(catalog_id, local_store_batch_update, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`local_store_batch_update`)) {
        stop("Missing required parameter `local_store_batch_update`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`local_store_batch_update`) && is.null(`local_store_batch_update`)) {
        stop("Invalid value for `local_store_batch_update` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, `local_store_batch_update` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsLocalStoresUpdate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`local_store_batch_update`)) {
        body.items <- paste(unlist(lapply(`local_store_batch_update`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/catalogs/{catalog_id}/local_stores"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CatalogsLocalStoresCreate200ResponseInner]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get supplemental items batch status
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param batch_id Unique identifier of an items batch operation.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SupplementalItemsBatchResponse
    CatalogsSupplementalItemsBatchGet = function(catalog_id, batch_id, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CatalogsSupplementalItemsBatchGetWithHttpInfo(catalog_id, batch_id, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get supplemental items batch status
    #'
    #' @param catalog_id Unique identifier of a catalog.
    #' @param batch_id Unique identifier of an items batch operation.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SupplementalItemsBatchResponse) with additional information such as HTTP status code, headers
    CatalogsSupplementalItemsBatchGetWithHttpInfo = function(catalog_id, batch_id, ad_account_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalog_id`)) {
        stop("Missing required parameter `catalog_id`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }

      if (!missing(`catalog_id`) && is.null(`catalog_id`)) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, `catalog_id` is not nullable")
      }
      if (!is.null(`catalog_id`) && nchar(`catalog_id`) > 19) {
        stop("Invalid length for `catalog_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, must be smaller than or equal to 19.")
      }
      if (!is.null(`catalog_id`) && !stringr::str_detect(`catalog_id`, "^\\d+$")) {
        stop("Invalid value for `catalog_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`batch_id`) && is.null(`batch_id`)) {
        stop("Invalid value for `batch_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, `batch_id` is not nullable")
      }
      if (!is.null(`batch_id`) && !stringr::str_detect(`batch_id`, "^\\d+$")) {
        stop("Invalid value for `batch_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogSupplementalApi$CatalogsSupplementalItemsBatchGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/catalogs/{catalog_id}/supplemental_items/batch/{batch_id}"
      if (!missing(`catalog_id`)) {
        local_var_url_path <- gsub("\\{catalog_id\\}", URLencode(as.character(`catalog_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`batch_id`)) {
        local_var_url_path <- gsub("\\{batch_id\\}", URLencode(as.character(`batch_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SupplementalItemsBatchResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
