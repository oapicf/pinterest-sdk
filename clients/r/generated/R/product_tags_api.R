#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ProductTags operations
#' @description ProductTagsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ProductTagsBulkAdd  ####################
#'
#' library(openapi)
#' var_pin_id <- "pin_id_example" # character | Unique identifier of the hero pin that will receive product tags.
#' var_product_tags_bulk_add_request <- ProductTagsBulkAddRequest$new(c(ProductTagItem$new("pin_id_example"))) # ProductTagsBulkAddRequest | 
#'
#' #Add product tags to pin
#' api_instance <- ProductTagsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ProductTagsBulkAdd(var_pin_id, var_product_tags_bulk_add_requestdata_file = "result.txt")
#' result <- api_instance$ProductTagsBulkAdd(var_pin_id, var_product_tags_bulk_add_request)
#' dput(result)
#'
#'
#' ####################  ProductTagsBulkDelete  ####################
#'
#' library(openapi)
#' var_pin_id <- "pin_id_example" # character | Unique identifier of the hero pin that will receive product tags.
#' var_product_tags_bulk_delete_request <- ProductTagsBulkDeleteRequest$new(c(ProductTagItem$new("pin_id_example"))) # ProductTagsBulkDeleteRequest | 
#'
#' #Delete product tags from pin
#' api_instance <- ProductTagsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$ProductTagsBulkDelete(var_pin_id, var_product_tags_bulk_delete_request)
#'
#'
#' ####################  ProductTagsList  ####################
#'
#' library(openapi)
#' var_pin_id <- "pin_id_example" # character | Unique identifier of the hero pin that will receive product tags.
#'
#' #Get product tags for pin
#' api_instance <- ProductTagsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ProductTagsList(var_pin_iddata_file = "result.txt")
#' result <- api_instance$ProductTagsList(var_pin_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ProductTagsApi <- R6::R6Class(
  "ProductTagsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ProductTagsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Add product tags to pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param product_tags_bulk_add_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProductTagsResponse
    ProductTagsBulkAdd = function(pin_id, product_tags_bulk_add_request, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ProductTagsBulkAddWithHttpInfo(pin_id, product_tags_bulk_add_request, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add product tags to pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param product_tags_bulk_add_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProductTagsResponse) with additional information such as HTTP status code, headers
    ProductTagsBulkAddWithHttpInfo = function(pin_id, product_tags_bulk_add_request, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`pin_id`)) {
        stop("Missing required parameter `pin_id`.")
      }

      if (missing(`product_tags_bulk_add_request`)) {
        stop("Missing required parameter `product_tags_bulk_add_request`.")
      }

      if (!missing(`pin_id`) && is.null(`pin_id`)) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsBulkAdd, `pin_id` is not nullable")
      }
      if (!is.null(`pin_id`) && !stringr::str_detect(`pin_id`, "^\\d+$")) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsBulkAdd, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`product_tags_bulk_add_request`) && is.null(`product_tags_bulk_add_request`)) {
        stop("Invalid value for `product_tags_bulk_add_request` when calling ProductTagsApi$ProductTagsBulkAdd, `product_tags_bulk_add_request` is not nullable")
      }

      if (!is.null(`product_tags_bulk_add_request`)) {
        local_var_body <- `product_tags_bulk_add_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/pins/{pin_id}/product_tags"
      if (!missing(`pin_id`)) {
        local_var_url_path <- gsub("\\{pin_id\\}", URLencode(as.character(`pin_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write pins:read pins:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProductTagsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete product tags from pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param product_tags_bulk_delete_request 
    #' @param ... Other optional arguments
    #'
    #' @return void
    ProductTagsBulkDelete = function(pin_id, product_tags_bulk_delete_request, ...) {
      local_var_response <- self$ProductTagsBulkDeleteWithHttpInfo(pin_id, product_tags_bulk_delete_request, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete product tags from pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param product_tags_bulk_delete_request 
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ProductTagsBulkDeleteWithHttpInfo = function(pin_id, product_tags_bulk_delete_request, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`pin_id`)) {
        stop("Missing required parameter `pin_id`.")
      }

      if (missing(`product_tags_bulk_delete_request`)) {
        stop("Missing required parameter `product_tags_bulk_delete_request`.")
      }

      if (!missing(`pin_id`) && is.null(`pin_id`)) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsBulkDelete, `pin_id` is not nullable")
      }
      if (!is.null(`pin_id`) && !stringr::str_detect(`pin_id`, "^\\d+$")) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsBulkDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`product_tags_bulk_delete_request`) && is.null(`product_tags_bulk_delete_request`)) {
        stop("Invalid value for `product_tags_bulk_delete_request` when calling ProductTagsApi$ProductTagsBulkDelete, `product_tags_bulk_delete_request` is not nullable")
      }

      if (!is.null(`product_tags_bulk_delete_request`)) {
        local_var_body <- `product_tags_bulk_delete_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/pins/{pin_id}/product_tags/bulk-delete"
      if (!missing(`pin_id`)) {
        local_var_url_path <- gsub("\\{pin_id\\}", URLencode(as.character(`pin_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write pins:read pins:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get product tags for pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return ProductTagsResponse
    ProductTagsList = function(pin_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ProductTagsListWithHttpInfo(pin_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get product tags for pin
    #'
    #' @param pin_id Unique identifier of the hero pin that will receive product tags.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (ProductTagsResponse) with additional information such as HTTP status code, headers
    ProductTagsListWithHttpInfo = function(pin_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`pin_id`)) {
        stop("Missing required parameter `pin_id`.")
      }

      if (!missing(`pin_id`) && is.null(`pin_id`)) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsList, `pin_id` is not nullable")
      }
      if (!is.null(`pin_id`) && !stringr::str_detect(`pin_id`, "^\\d+$")) {
        stop("Invalid value for `pin_id` when calling ProductTagsApi$ProductTagsList, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/pins/{pin_id}/product_tags"
      if (!missing(`pin_id`)) {
        local_var_url_path <- gsub("\\{pin_id\\}", URLencode(as.character(`pin_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read pins:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ProductTagsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
