#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.12.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Search operations
#' @description SearchApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SearchPartnerPins } \emph{ Search pins by a given search term }
#' <strong>This endpoint is currently in beta and not available to all apps. <a href='/docs/new/about-beta-access/'>Learn more</a>.</strong>  Get the top 10 Pins by a given search term.
#'
#' \itemize{
#' \item \emph{ @param } term character
#' \item \emph{ @param } country_code character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } locale character
#' \item \emph{ @param } limit integer
#' \item \emph{ @returnType } \link{SearchPartnerPins200Response} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : SearchPartnerPins200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid pins
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SearchUserBoardsGet } \emph{ Search user&#39;s boards }
#' Search for boards for the \"operation user_account\". This includes boards of all board types. - By default, the \"operation user_account\" is the token user_account.  If using Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". See <a href='/docs/reference/business-access/'>Understanding Business Access</a> for more information.
#'
#' \itemize{
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @param } query character
#' \item \emph{ @returnType } \link{SearchUserBoardsGet200Response} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : SearchUserBoardsGet200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SearchUserPinsList } \emph{ Search user&#39;s Pins }
#' Search for pins for the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.  If using Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". See <a href='/docs/reference/business-access/'>Understanding Business Access</a> for more information.
#'
#' \itemize{
#' \item \emph{ @param } query character
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @returnType } \link{PinsList200Response} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : PinsList200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | User not found
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SearchPartnerPins  ####################
#'
#' library(openapi)
#' var_term <- "term_example" # character | Search term to look up pins.
#' var_country_code <- "US" # character | Two letter country code (ISO 3166-1 alpha-2)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_locale <- "locale_example" # character | Search locale. (Optional)
#' var_limit <- 10 # integer | Max search result size (Optional)
#'
#' #Search pins by a given search term
#' api_instance <- SearchApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchPartnerPins(var_term, var_country_code, bookmark = var_bookmark, locale = var_locale, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$SearchPartnerPins(var_term, var_country_code, bookmark = var_bookmark, locale = var_locale, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  SearchUserBoardsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#' var_query <- "query_example" # character | Search query. Can contain pin description keywords or comma-separated pin IDs. (Optional)
#'
#' #Search user's boards
#' api_instance <- SearchApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchUserBoardsGet(ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, query = var_querydata_file = "result.txt")
#' result <- api_instance$SearchUserBoardsGet(ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, query = var_query)
#' dput(result)
#'
#'
#' ####################  SearchUserPinsList  ####################
#'
#' library(openapi)
#' var_query <- "Plants" # character | Search query. Can contain pin description keywords or comma-separated pin IDs.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #Search user's Pins
#' api_instance <- SearchApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SearchUserPinsList(var_query, ad_account_id = var_ad_account_id, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$SearchUserPinsList(var_query, ad_account_id = var_ad_account_id, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SearchApi <- R6::R6Class(
  "SearchApi",
  public = list(
    api_client = NULL,
    #' Initialize a new SearchApi.
    #'
    #' @description
    #' Initialize a new SearchApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Search pins by a given search term
    #'
    #' @description
    #' Search pins by a given search term
    #'
    #' @param term Search term to look up pins.
    #' @param country_code Two letter country code (ISO 3166-1 alpha-2)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param locale (optional) Search locale.
    #' @param limit (optional) Max search result size (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchPartnerPins200Response
    #' @export
    SearchPartnerPins = function(term, country_code, bookmark = NULL, locale = NULL, limit = 10, data_file = NULL, ...) {
      local_var_response <- self$SearchPartnerPinsWithHttpInfo(term, country_code, bookmark, locale, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Search pins by a given search term
    #'
    #' @description
    #' Search pins by a given search term
    #'
    #' @param term Search term to look up pins.
    #' @param country_code Two letter country code (ISO 3166-1 alpha-2)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param locale (optional) Search locale.
    #' @param limit (optional) Max search result size (default value: 10)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchPartnerPins200Response) with additional information such as HTTP status code, headers
    #' @export
    SearchPartnerPinsWithHttpInfo = function(term, country_code, bookmark = NULL, locale = NULL, limit = 10, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`term`)) {
        stop("Missing required parameter `term`.")
      }

      if (missing(`country_code`)) {
        stop("Missing required parameter `country_code`.")
      }





      if (`limit` > 50) {
        stop("Invalid value for `limit` when calling SearchApi$SearchPartnerPins, must be smaller than or equal to 50.")
      }
      if (`limit` < 1) {
        stop("Invalid value for `limit` when calling SearchApi$SearchPartnerPins, must be bigger than or equal to 1.")
      }

      query_params[["term"]] <- `term`

      query_params[["country_code"]] <- `country_code`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["locale"]] <- `locale`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/search/partner/pins"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read pins:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchPartnerPins200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Search user's boards
    #'
    #' @description
    #' Search user's boards
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param query (optional) Search query. Can contain pin description keywords or comma-separated pin IDs.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchUserBoardsGet200Response
    #' @export
    SearchUserBoardsGet = function(ad_account_id = NULL, bookmark = NULL, page_size = 25, query = NULL, data_file = NULL, ...) {
      local_var_response <- self$SearchUserBoardsGetWithHttpInfo(ad_account_id, bookmark, page_size, query, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Search user's boards
    #'
    #' @description
    #' Search user's boards
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param query (optional) Search query. Can contain pin description keywords or comma-separated pin IDs.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchUserBoardsGet200Response) with additional information such as HTTP status code, headers
    #' @export
    SearchUserBoardsGetWithHttpInfo = function(ad_account_id = NULL, bookmark = NULL, page_size = 25, query = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling SearchApi$SearchUserBoardsGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling SearchApi$SearchUserBoardsGet, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling SearchApi$SearchUserBoardsGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling SearchApi$SearchUserBoardsGet, must be bigger than or equal to 1.")
      }


      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["query"]] <- `query`

      local_var_url_path <- "/search/boards"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:read_secret"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchUserBoardsGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Search user's Pins
    #'
    #' @description
    #' Search user's Pins
    #'
    #' @param query Search query. Can contain pin description keywords or comma-separated pin IDs.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PinsList200Response
    #' @export
    SearchUserPinsList = function(query, ad_account_id = NULL, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$SearchUserPinsListWithHttpInfo(query, ad_account_id, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Search user's Pins
    #'
    #' @description
    #' Search user's Pins
    #'
    #' @param query Search query. Can contain pin description keywords or comma-separated pin IDs.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PinsList200Response) with additional information such as HTTP status code, headers
    #' @export
    SearchUserPinsListWithHttpInfo = function(query, ad_account_id = NULL, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`query`)) {
        stop("Missing required parameter `query`.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling SearchApi$SearchUserPinsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling SearchApi$SearchUserPinsList, must conform to the pattern ^\\d+$.")
      }


      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["query"]] <- `query`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/search/pins"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:read_secret pins:read pins:read_secret"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PinsList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
