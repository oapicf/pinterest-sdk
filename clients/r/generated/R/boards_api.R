#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.12.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Boards operations
#' @description BoardsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ BoardSectionsCreate } \emph{ Create board section }
#' Create a board section on a board owned by the \"operation user_account\" - or on a group board that has been shared with this account. Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } board_section \link{BoardSection}
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @returnType } \link{BoardSection} \cr
#'
#'
#' \item status code : 201 | response
#'
#' \item return type : BoardSection
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid board section parameters.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to create board sections.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Could not get exclusive access to the board to create a new section.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Could not create a new board section.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardSectionsDelete } \emph{ Delete board section }
#' Delete a board section on a board owned by the \"operation user_account\" - or on a group board that has been shared with this account. Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } section_id character
#' \item \emph{ @param } ad_account_id character
#'
#'
#' \item status code : 204 | Board section deleted successfully
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to delete board section.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Board section not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Board section conflict.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardSectionsList } \emph{ List board sections }
#' Get a list of all board sections from a board owned by the \"operation user_account\" - or a group board that has been shared with this account. Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @returnType } \link{BoardSectionsList200Response} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : BoardSectionsList200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardSectionsListPins } \emph{ List Pins on board section }
#' Get a list of the Pins on a board section of a board owned by the \"operation user_account\" - or on a group board that has been shared with this account. Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } section_id character
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @returnType } \link{BoardsListPins200Response} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : BoardsListPins200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to access Pins on board section.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Board or section not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Board section conflict.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardSectionsUpdate } \emph{ Update board section }
#' Update a board section on a board owned by the \"operation user_account\" - or on a group board that has been shared with this account. Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } section_id character
#' \item \emph{ @param } board_section \link{BoardSection}
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @returnType } \link{BoardSection} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : BoardSection
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid board section parameters.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to update board section.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Board section conflict.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsCreate } \emph{ Create board }
#' Create a board owned by the \"operation user_account\". Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board \link{Board}
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @returnType } \link{Board} \cr
#'
#'
#' \item status code : 201 | response
#'
#' \item return type : Board
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | The board name is invalid or duplicated.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsDelete } \emph{ Delete board }
#' Delete a board owned by the \"operation user_account\". - Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } ad_account_id character
#'
#'
#' \item status code : 204 | Board deleted successfully
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to delete the board.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Board not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Could not get exclusive access to delete the board.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | This request exceeded a rate limit. This can happen if the client exceeds one of the published rate limits or if multiple write operations are applied to an object within a short time window.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsGet } \emph{ Get board }
#' Get a board owned by the operation user_account - or a group board that has been shared with this account. - Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @returnType } \link{Board} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : Board
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Board not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsList } \emph{ List boards }
#' Get a list of the boards owned by the \"operation user_account\" + group boards where this account is a collaborator Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". Optional: Specify a privacy type (public, protected, or secret) to indicate which boards to return. - If no privacy is specified, all boards that can be returned (based on the scopes of the token and ad_account role if applicable) will be returned.
#'
#' \itemize{
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @param } privacy Enum < [ALL, PROTECTED, PUBLIC, SECRET, PUBLIC_AND_SECRET] >
#' \item \emph{ @returnType } \link{BoardsList200Response} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : BoardsList200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsListPins } \emph{ List Pins on board }
#' Get a list of the Pins on a board owned by the \"operation user_account\" - or on a group board that has been shared with this account. - Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @param } creative_types Enum < [REGULAR, VIDEO, SHOPPING, CAROUSEL, MAX_VIDEO, SHOP_THE_PIN, COLLECTION, IDEA] >
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } pin_metrics character
#' \item \emph{ @returnType } \link{BoardsListPins200Response} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : BoardsListPins200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Board not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ BoardsUpdate } \emph{ Update board }
#' Update a board owned by the \"operating user_account\". - Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". - By default, the \"operation user_account\" is the token user_account.
#'
#' \itemize{
#' \item \emph{ @param } board_id character
#' \item \emph{ @param } board_update \link{BoardUpdate}
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @returnType } \link{Board} \cr
#'
#'
#' \item status code : 200 | response
#'
#' \item return type : Board
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid board parameters.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Not authorized to update the board.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 429 | This request exceeded a rate limit. This can happen if the client exceeds one of the published rate limits or if multiple write operations are applied to an object within a short time window.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  BoardSectionsCreate  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_board_section <- BoardSection$new("name_example", "id_example") # BoardSection | Create a board section.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Create board section
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardSectionsCreate(var_board_id, var_board_section, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardSectionsCreate(var_board_id, var_board_section, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  BoardSectionsDelete  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_section_id <- "section_id_example" # character | Unique identifier of a board section.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Delete board section
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$BoardSectionsDelete(var_board_id, var_section_id, ad_account_id = var_ad_account_id)
#'
#'
#' ####################  BoardSectionsList  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #List board sections
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardSectionsList(var_board_id, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BoardSectionsList(var_board_id, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BoardSectionsListPins  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_section_id <- "section_id_example" # character | Unique identifier of a board section.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #List Pins on board section
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardSectionsListPins(var_board_id, var_section_id, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BoardSectionsListPins(var_board_id, var_section_id, ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BoardSectionsUpdate  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_section_id <- "section_id_example" # character | Unique identifier of a board section.
#' var_board_section <- BoardSection$new("name_example", "id_example") # BoardSection | Update a board section.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Update board section
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardSectionsUpdate(var_board_id, var_section_id, var_board_section, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardSectionsUpdate(var_board_id, var_section_id, var_board_section, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  BoardsCreate  ####################
#'
#' library(openapi)
#' var_board <- Board$new("name_example", "id_example", "created_at_example", "board_pins_modified_at_example", "description_example", 123, 123, 123, Board_media$new("image_cover_url_example", c("pin_thumbnail_urls_example")), Board_owner$new("username_example"), "PUBLIC") # Board | Create a board using a single board json object.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Create board
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsCreate(var_board, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardsCreate(var_board, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  BoardsDelete  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Delete board
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$BoardsDelete(var_board_id, ad_account_id = var_ad_account_id)
#'
#'
#' ####################  BoardsGet  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get board
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsGet(var_board_id, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardsGet(var_board_id, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  BoardsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#' var_privacy <- "privacy_example" # character | Privacy setting for a board. (Optional)
#'
#' #List boards
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsList(ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, privacy = var_privacydata_file = "result.txt")
#' result <- api_instance$BoardsList(ad_account_id = var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, privacy = var_privacy)
#' dput(result)
#'
#'
#' ####################  BoardsListPins  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#' var_creative_types <- c("REGULAR") # array[character] | Pin creative types filter. </p><strong>Note:</strong> SHOP_THE_PIN has been deprecated. Please use COLLECTION instead. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_pin_metrics <- FALSE # character | Specify whether to return 90d and lifetime Pin metrics. Total comments and total reactions are only available with lifetime Pin metrics. If Pin was created before <code>2023-03-20</code> lifetime metrics will only be available for Video and Idea Pin formats. Lifetime metrics are available for all Pin formats since then. (Optional)
#'
#' #List Pins on board
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsListPins(var_board_id, bookmark = var_bookmark, page_size = var_page_size, creative_types = var_creative_types, ad_account_id = var_ad_account_id, pin_metrics = var_pin_metricsdata_file = "result.txt")
#' result <- api_instance$BoardsListPins(var_board_id, bookmark = var_bookmark, page_size = var_page_size, creative_types = var_creative_types, ad_account_id = var_ad_account_id, pin_metrics = var_pin_metrics)
#' dput(result)
#'
#'
#' ####################  BoardsUpdate  ####################
#'
#' library(openapi)
#' var_board_id <- "board_id_example" # character | Unique identifier of a board.
#' var_board_update <- BoardUpdate$new("name_example", "description_example", "PUBLIC") # BoardUpdate | Update a board.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Update board
#' api_instance <- BoardsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsUpdate(var_board_id, var_board_update, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardsUpdate(var_board_id, var_board_update, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BoardsApi <- R6::R6Class(
  "BoardsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new BoardsApi.
    #'
    #' @description
    #' Initialize a new BoardsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Create board section
    #'
    #' @description
    #' Create board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param board_section Create a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardSection
    #' @export
    BoardSectionsCreate = function(board_id, board_section, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardSectionsCreateWithHttpInfo(board_id, board_section, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create board section
    #'
    #' @description
    #' Create board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param board_section Create a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardSection) with additional information such as HTTP status code, headers
    #' @export
    BoardSectionsCreateWithHttpInfo = function(board_id, board_section, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (missing(`board_section`)) {
        stop("Missing required parameter `board_section`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardSectionsCreate, must conform to the pattern ^\\d+$.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardSectionsCreate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardSectionsCreate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`board_section`)) {
        local_var_body <- `board_section`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/boards/{board_id}/sections"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardSection", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete board section
    #'
    #' @description
    #' Delete board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    BoardSectionsDelete = function(board_id, section_id, ad_account_id = NULL, ...) {
      local_var_response <- self$BoardSectionsDeleteWithHttpInfo(board_id, section_id, ad_account_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete board section
    #'
    #' @description
    #' Delete board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    BoardSectionsDeleteWithHttpInfo = function(board_id, section_id, ad_account_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardSectionsDelete, must conform to the pattern ^\\d+$.")
      }

      if (!str_detect(`section_id`, "^\\d+$")) {
        stop("Invalid value for `section_id` when calling BoardsApi$BoardSectionsDelete, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardSectionsDelete, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardSectionsDelete, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/boards/{board_id}/sections/{section_id}"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List board sections
    #'
    #' @description
    #' List board sections
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardSectionsList200Response
    #' @export
    BoardSectionsList = function(board_id, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$BoardSectionsListWithHttpInfo(board_id, ad_account_id, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List board sections
    #'
    #' @description
    #' List board sections
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardSectionsList200Response) with additional information such as HTTP status code, headers
    #' @export
    BoardSectionsListWithHttpInfo = function(board_id, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardSectionsList, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardSectionsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardSectionsList, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardSectionsList, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardSectionsList, must be bigger than or equal to 1.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/boards/{board_id}/sections"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardSectionsList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List Pins on board section
    #'
    #' @description
    #' List Pins on board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardsListPins200Response
    #' @export
    BoardSectionsListPins = function(board_id, section_id, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$BoardSectionsListPinsWithHttpInfo(board_id, section_id, ad_account_id, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List Pins on board section
    #'
    #' @description
    #' List Pins on board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardsListPins200Response) with additional information such as HTTP status code, headers
    #' @export
    BoardSectionsListPinsWithHttpInfo = function(board_id, section_id, ad_account_id = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardSectionsListPins, must conform to the pattern ^\\d+$.")
      }

      if (!str_detect(`section_id`, "^\\d+$")) {
        stop("Invalid value for `section_id` when calling BoardsApi$BoardSectionsListPins, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardSectionsListPins, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardSectionsListPins, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardSectionsListPins, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardSectionsListPins, must be bigger than or equal to 1.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/boards/{board_id}/sections/{section_id}/pins"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read pins:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardsListPins200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Update board section
    #'
    #' @description
    #' Update board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param board_section Update a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardSection
    #' @export
    BoardSectionsUpdate = function(board_id, section_id, board_section, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardSectionsUpdateWithHttpInfo(board_id, section_id, board_section, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Update board section
    #'
    #' @description
    #' Update board section
    #'
    #' @param board_id Unique identifier of a board.
    #' @param section_id Unique identifier of a board section.
    #' @param board_section Update a board section.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardSection) with additional information such as HTTP status code, headers
    #' @export
    BoardSectionsUpdateWithHttpInfo = function(board_id, section_id, board_section, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }

      if (missing(`board_section`)) {
        stop("Missing required parameter `board_section`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardSectionsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!str_detect(`section_id`, "^\\d+$")) {
        stop("Invalid value for `section_id` when calling BoardsApi$BoardSectionsUpdate, must conform to the pattern ^\\d+$.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardSectionsUpdate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardSectionsUpdate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`board_section`)) {
        local_var_body <- `board_section`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/boards/{board_id}/sections/{section_id}"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardSection", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create board
    #'
    #' @description
    #' Create board
    #'
    #' @param board Create a board using a single board json object.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Board
    #' @export
    BoardsCreate = function(board, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardsCreateWithHttpInfo(board, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create board
    #'
    #' @description
    #' Create board
    #'
    #' @param board Create a board using a single board json object.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Board) with additional information such as HTTP status code, headers
    #' @export
    BoardsCreateWithHttpInfo = function(board, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board`)) {
        stop("Missing required parameter `board`.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsCreate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsCreate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`board`)) {
        local_var_body <- `board`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/boards"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Board", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete board
    #'
    #' @description
    #' Delete board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    BoardsDelete = function(board_id, ad_account_id = NULL, ...) {
      local_var_response <- self$BoardsDeleteWithHttpInfo(board_id, ad_account_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete board
    #'
    #' @description
    #' Delete board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    BoardsDeleteWithHttpInfo = function(board_id, ad_account_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardsDelete, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsDelete, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsDelete, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/boards/{board_id}"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get board
    #'
    #' @description
    #' Get board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Board
    #' @export
    BoardsGet = function(board_id, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardsGetWithHttpInfo(board_id, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get board
    #'
    #' @description
    #' Get board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Board) with additional information such as HTTP status code, headers
    #' @export
    BoardsGetWithHttpInfo = function(board_id, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardsGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/boards/{board_id}"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Board", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List boards
    #'
    #' @description
    #' List boards
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param privacy (optional) Privacy setting for a board.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardsList200Response
    #' @export
    BoardsList = function(ad_account_id = NULL, bookmark = NULL, page_size = 25, privacy = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardsListWithHttpInfo(ad_account_id, bookmark, page_size, privacy, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List boards
    #'
    #' @description
    #' List boards
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param privacy (optional) Privacy setting for a board.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardsList200Response) with additional information such as HTTP status code, headers
    #' @export
    BoardsListWithHttpInfo = function(ad_account_id = NULL, bookmark = NULL, page_size = 25, privacy = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsList, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardsList, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardsList, must be bigger than or equal to 1.")
      }


      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      if (!(`privacy` %in% c("ALL", "PROTECTED", "PUBLIC", "SECRET", "PUBLIC_AND_SECRET"))) {
        stop("Invalid value for privacy when calling BoardsApi$BoardsList. Must be [ALL, PROTECTED, PUBLIC, SECRET, PUBLIC_AND_SECRET].")
      }
      query_params[["privacy"]] <- `privacy`

      local_var_url_path <- "/boards"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardsList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List Pins on board
    #'
    #' @description
    #' List Pins on board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param creative_types (optional) Pin creative types filter. </p><strong>Note:</strong> SHOP_THE_PIN has been deprecated. Please use COLLECTION instead.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param pin_metrics (optional) Specify whether to return 90d and lifetime Pin metrics. Total comments and total reactions are only available with lifetime Pin metrics. If Pin was created before <code>2023-03-20</code> lifetime metrics will only be available for Video and Idea Pin formats. Lifetime metrics are available for all Pin formats since then. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return BoardsListPins200Response
    #' @export
    BoardsListPins = function(board_id, bookmark = NULL, page_size = 25, creative_types = NULL, ad_account_id = NULL, pin_metrics = FALSE, data_file = NULL, ...) {
      local_var_response <- self$BoardsListPinsWithHttpInfo(board_id, bookmark, page_size, creative_types, ad_account_id, pin_metrics, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List Pins on board
    #'
    #' @description
    #' List Pins on board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param creative_types (optional) Pin creative types filter. </p><strong>Note:</strong> SHOP_THE_PIN has been deprecated. Please use COLLECTION instead.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param pin_metrics (optional) Specify whether to return 90d and lifetime Pin metrics. Total comments and total reactions are only available with lifetime Pin metrics. If Pin was created before <code>2023-03-20</code> lifetime metrics will only be available for Video and Idea Pin formats. Lifetime metrics are available for all Pin formats since then. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (BoardsListPins200Response) with additional information such as HTTP status code, headers
    #' @export
    BoardsListPinsWithHttpInfo = function(board_id, bookmark = NULL, page_size = 25, creative_types = NULL, ad_account_id = NULL, pin_metrics = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardsListPins, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardsListPins, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BoardsApi$BoardsListPins, must be bigger than or equal to 1.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsListPins, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsListPins, must conform to the pattern ^\\d+$.")
      }


      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      # explore
      for (query_item in `creative_types`) {
        # validate enum values
        if (!(query_item %in% c("REGULAR", "VIDEO", "SHOPPING", "CAROUSEL", "MAX_VIDEO", "SHOP_THE_PIN", "COLLECTION", "IDEA"))) {
          stop("Invalid value for creative_types when calling BoardsApi$BoardsListPins. Must be [REGULAR, VIDEO, SHOPPING, CAROUSEL, MAX_VIDEO, SHOP_THE_PIN, COLLECTION, IDEA].")
        }
        query_params[["creative_types"]] <- c(query_params[["creative_types"]], list(`creative_types` = query_item))
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["pin_metrics"]] <- `pin_metrics`

      local_var_url_path <- "/boards/{board_id}/pins"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read pins:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BoardsListPins200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Update board
    #'
    #' @description
    #' Update board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param board_update Update a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Board
    #' @export
    BoardsUpdate = function(board_id, board_update, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardsUpdateWithHttpInfo(board_id, board_update, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Update board
    #'
    #' @description
    #' Update board
    #'
    #' @param board_id Unique identifier of a board.
    #' @param board_update Update a board.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Board) with additional information such as HTTP status code, headers
    #' @export
    BoardsUpdateWithHttpInfo = function(board_id, board_update, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`board_id`)) {
        stop("Missing required parameter `board_id`.")
      }

      if (missing(`board_update`)) {
        stop("Missing required parameter `board_update`.")
      }

      if (!str_detect(`board_id`, "^\\d+$")) {
        stop("Invalid value for `board_id` when calling BoardsApi$BoardsUpdate, must conform to the pattern ^\\d+$.")
      }


      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling BoardsApi$BoardsUpdate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling BoardsApi$BoardsUpdate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`board_update`)) {
        local_var_body <- `board_update`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/boards/{board_id}"
      if (!missing(`board_id`)) {
        local_var_url_path <- gsub("\\{board_id\\}", URLencode(as.character(`board_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "boards:read boards:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Board", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
