#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Promotions operations
#' @description PromotionsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  PromotionsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_promotion_create_request <- c(PromotionCreateRequest$new("promotion_title_example", PromotionType$new(), "OTHER", 123, "external_id_example", "platform_type_example", "promotion_code_example", "promotion_custom_id_example", 123, c(PromotionTemplateValue$new(123, Currency$new(), "custom_text_example", 123)))) # array[PromotionCreateRequest] | List of promotions to create, size limit [1, 30].
#'
#' #Create promotions
#' api_instance <- PromotionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PromotionsCreate(var_ad_account_id, var_promotion_create_requestdata_file = "result.txt")
#' result <- api_instance$PromotionsCreate(var_ad_account_id, var_promotion_create_request)
#' dput(result)
#'
#'
#' ####################  PromotionsDelete  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_promotion_id <- "promotion_id_example" # character | Unique identifier of a promotion
#'
#' #Delete promotion by id
#' api_instance <- PromotionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$PromotionsDelete(var_ad_account_id, var_promotion_id)
#'
#'
#' ####################  PromotionsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_promotion_id <- "promotion_id_example" # character | Unique identifier of a promotion
#'
#' #Get promotion by id
#' api_instance <- PromotionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PromotionsGet(var_ad_account_id, var_promotion_iddata_file = "result.txt")
#' result <- api_instance$PromotionsGet(var_ad_account_id, var_promotion_id)
#' dput(result)
#'
#'
#' ####################  PromotionsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_order <- "ASCENDING" # character | The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #Get promotions
#' api_instance <- PromotionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PromotionsList(var_ad_account_id, page_size = var_page_size, order = var_order, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$PromotionsList(var_ad_account_id, page_size = var_page_size, order = var_order, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  PromotionsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_promotion_update_request <- c(PromotionUpdateRequest$new("id_example", "OTHER", 123, "external_id_example", "platform_type_example", "promotion_code_example", "promotion_custom_id_example", "promotion_title_example", PromotionType$new(), 123, c(PromotionTemplateValue$new(123, Currency$new(), "custom_text_example", 123)), EntityStatus$new())) # array[PromotionUpdateRequest] | List of promotions to create, size limit [1, 30].
#'
#' #Update promotions
#' api_instance <- PromotionsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PromotionsUpdate(var_ad_account_id, var_promotion_update_requestdata_file = "result.txt")
#' result <- api_instance$PromotionsUpdate(var_ad_account_id, var_promotion_update_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PromotionsApi <- R6::R6Class(
  "PromotionsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new PromotionsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_create_request List of promotions to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PromotionsResponse
    PromotionsCreate = function(ad_account_id, promotion_create_request, data_file = NULL, ...) {
      local_var_response <- self$PromotionsCreateWithHttpInfo(ad_account_id, promotion_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_create_request List of promotions to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PromotionsResponse) with additional information such as HTTP status code, headers
    PromotionsCreateWithHttpInfo = function(ad_account_id, promotion_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`promotion_create_request`)) {
        stop("Missing required parameter `promotion_create_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling PromotionsApi$PromotionsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`promotion_create_request`) && is.null(`promotion_create_request`)) {
        stop("Invalid value for `promotion_create_request` when calling PromotionsApi$PromotionsCreate, `promotion_create_request` is not nullable")
      }
      if (!is.null(`promotion_create_request`) && length(`promotion_create_request`) > 30) {
        stop("Invalid length for `promotion_create_request` when calling PromotionsApi$PromotionsCreate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`promotion_create_request`) && length(`promotion_create_request`) < 1) {
        stop("Invalid length for `promotion_create_request` when calling PromotionsApi$PromotionsCreate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`promotion_create_request`)) {
        body.items <- paste(unlist(lapply(`promotion_create_request`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotions"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PromotionsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete promotion by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_id Unique identifier of a promotion
    #' @param ... Other optional arguments
    #'
    #' @return void
    PromotionsDelete = function(ad_account_id, promotion_id, ...) {
      local_var_response <- self$PromotionsDeleteWithHttpInfo(ad_account_id, promotion_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete promotion by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_id Unique identifier of a promotion
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    PromotionsDeleteWithHttpInfo = function(ad_account_id, promotion_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`promotion_id`)) {
        stop("Missing required parameter `promotion_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsDelete, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling PromotionsApi$PromotionsDelete, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`promotion_id`) && is.null(`promotion_id`)) {
        stop("Invalid value for `promotion_id` when calling PromotionsApi$PromotionsDelete, `promotion_id` is not nullable")
      }
      if (!is.null(`promotion_id`) && nchar(`promotion_id`) > 18) {
        stop("Invalid length for `promotion_id` when calling PromotionsApi$PromotionsDelete, must be smaller than or equal to 18.")
      }
      if (!is.null(`promotion_id`) && !stringr::str_detect(`promotion_id`, "^\\d+$")) {
        stop("Invalid value for `promotion_id` when calling PromotionsApi$PromotionsDelete, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotions/{promotion_id}"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`promotion_id`)) {
        local_var_url_path <- gsub("\\{promotion_id\\}", URLencode(as.character(`promotion_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get promotion by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_id Unique identifier of a promotion
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PromotionResponse
    PromotionsGet = function(ad_account_id, promotion_id, data_file = NULL, ...) {
      local_var_response <- self$PromotionsGetWithHttpInfo(ad_account_id, promotion_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get promotion by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_id Unique identifier of a promotion
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PromotionResponse) with additional information such as HTTP status code, headers
    PromotionsGetWithHttpInfo = function(ad_account_id, promotion_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`promotion_id`)) {
        stop("Missing required parameter `promotion_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling PromotionsApi$PromotionsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`promotion_id`) && is.null(`promotion_id`)) {
        stop("Invalid value for `promotion_id` when calling PromotionsApi$PromotionsGet, `promotion_id` is not nullable")
      }
      if (!is.null(`promotion_id`) && nchar(`promotion_id`) > 18) {
        stop("Invalid length for `promotion_id` when calling PromotionsApi$PromotionsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`promotion_id`) && !stringr::str_detect(`promotion_id`, "^\\d+$")) {
        stop("Invalid value for `promotion_id` when calling PromotionsApi$PromotionsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotions/{promotion_id}"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`promotion_id`)) {
        local_var_url_path <- gsub("\\{promotion_id\\}", URLencode(as.character(`promotion_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PromotionResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PromotionsList200Response
    PromotionsList = function(ad_account_id, page_size = 25, order = NULL, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$PromotionsListWithHttpInfo(ad_account_id, page_size, order, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PromotionsList200Response) with additional information such as HTTP status code, headers
    PromotionsListWithHttpInfo = function(ad_account_id, page_size = 25, order = NULL, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling PromotionsApi$PromotionsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling PromotionsApi$PromotionsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling PromotionsApi$PromotionsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling PromotionsApi$PromotionsList, must be bigger than or equal to 1.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling PromotionsApi$PromotionsList, `order` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling PromotionsApi$PromotionsList, `bookmark` is not nullable")
      }

      query_params[["page_size"]] <- `page_size`

      if (!is.null(`order`) && !(`order` %in% c("ASCENDING", "DESCENDING"))) {
        stop("Invalid value for order when calling PromotionsApi$PromotionsList. Must be [ASCENDING, DESCENDING].")
      }
      query_params[["order"]] <- `order`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotions"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PromotionsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_update_request List of promotions to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PromotionsResponse
    PromotionsUpdate = function(ad_account_id, promotion_update_request, data_file = NULL, ...) {
      local_var_response <- self$PromotionsUpdateWithHttpInfo(ad_account_id, promotion_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update promotions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_update_request List of promotions to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PromotionsResponse) with additional information such as HTTP status code, headers
    PromotionsUpdateWithHttpInfo = function(ad_account_id, promotion_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`promotion_update_request`)) {
        stop("Missing required parameter `promotion_update_request`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling PromotionsApi$PromotionsUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling PromotionsApi$PromotionsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`promotion_update_request`) && is.null(`promotion_update_request`)) {
        stop("Invalid value for `promotion_update_request` when calling PromotionsApi$PromotionsUpdate, `promotion_update_request` is not nullable")
      }
      if (!is.null(`promotion_update_request`) && length(`promotion_update_request`) > 30) {
        stop("Invalid length for `promotion_update_request` when calling PromotionsApi$PromotionsUpdate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`promotion_update_request`) && length(`promotion_update_request`) < 1) {
        stop("Invalid length for `promotion_update_request` when calling PromotionsApi$PromotionsUpdate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`promotion_update_request`)) {
        body.items <- paste(unlist(lapply(`promotion_update_request`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotions"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "PromotionsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
