#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ConversionEqs operations
#' @description ConversionEqsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ConversionEqsList  ####################
#'
#' library(openapi)
#' var_lookback_period <- LookbackPeriodOptions$new() # LookbackPeriodOptions | Lookback window (number of days).
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_source_platform <- SourcePlatformOptions$new() # SourcePlatformOptions | Source platform of event. (Optional)
#' var_ingestion_source <- IngestionSourceOptions$new() # IngestionSourceOptions | Ingestion source of event. (Optional)
#'
#' #Get event quality score (EQS)
#' api_instance <- ConversionEqsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ConversionEqsList(var_lookback_period, var_ad_account_id, source_platform = var_source_platform, ingestion_source = var_ingestion_sourcedata_file = "result.txt")
#' result <- api_instance$ConversionEqsList(var_lookback_period, var_ad_account_id, source_platform = var_source_platform, ingestion_source = var_ingestion_source)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ConversionEqsApi <- R6::R6Class(
  "ConversionEqsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ConversionEqsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get event quality score (EQS)
    #'
    #' @param lookback_period Lookback window (number of days).
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param source_platform (optional) Source platform of event.
    #' @param ingestion_source (optional) Ingestion source of event.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[EventQualityScore]
    ConversionEqsList = function(lookback_period, ad_account_id, source_platform = NULL, ingestion_source = NULL, data_file = NULL, ...) {
      local_var_response <- self$ConversionEqsListWithHttpInfo(lookback_period, ad_account_id, source_platform, ingestion_source, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get event quality score (EQS)
    #'
    #' @param lookback_period Lookback window (number of days).
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param source_platform (optional) Source platform of event.
    #' @param ingestion_source (optional) Ingestion source of event.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[EventQualityScore]) with additional information such as HTTP status code, headers
    ConversionEqsListWithHttpInfo = function(lookback_period, ad_account_id, source_platform = NULL, ingestion_source = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`lookback_period`)) {
        stop("Missing required parameter `lookback_period`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`lookback_period`) && is.null(`lookback_period`)) {
        stop("Invalid value for `lookback_period` when calling ConversionEqsApi$ConversionEqsList, `lookback_period` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling ConversionEqsApi$ConversionEqsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionEqsApi$ConversionEqsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionEqsApi$ConversionEqsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`source_platform`) && is.null(`source_platform`)) {
        stop("Invalid value for `source_platform` when calling ConversionEqsApi$ConversionEqsList, `source_platform` is not nullable")
      }

      if (!missing(`ingestion_source`) && is.null(`ingestion_source`)) {
        stop("Invalid value for `ingestion_source` when calling ConversionEqsApi$ConversionEqsList, `ingestion_source` is not nullable")
      }

      query_params[["lookback_period"]] <- `lookback_period`

      query_params[["source_platform"]] <- `source_platform`

      query_params[["ingestion_source"]] <- `ingestion_source`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/conversion_eqs"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[EventQualityScore]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
