#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BusinessAccessAssets operations
#' @description BusinessAccessAssetsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AssetGroupCreate  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_asset_group_input_create <- AssetGroupInputCreate$new("asset_group_description_example", "asset_group_name_example", c(AssetGroupType$new()), AssetGroupBinding$new(c("ad_accounts_ids_example"), "asset_group_description_example", "asset_group_name_example", c("asset_group_types_example"), c("catalogs_ids_example"), BusinessAccessUserSummary$new("email_example", "id_example", "username_example"), 123, "id_example", BusinessAccessUserSummary$new("email_example", "id_example", "username_example"), c("profiles_ids_example"), 123)) # AssetGroupInputCreate | 
#'
#' #Create a new asset group.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupCreate(var_business_id, var_asset_group_input_createdata_file = "result.txt")
#' result <- api_instance$AssetGroupCreate(var_business_id, var_asset_group_input_create)
#' dput(result)
#'
#'
#' ####################  AssetGroupDelete  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_asset_group_deletion_delete <- AssetGroupDeletionDelete$new(c("asset_groups_to_delete_example")) # AssetGroupDeletionDelete | 
#'
#' #Delete asset groups.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupDelete(var_business_id, var_asset_group_deletion_deletedata_file = "result.txt")
#' result <- api_instance$AssetGroupDelete(var_business_id, var_asset_group_deletion_delete)
#' dput(result)
#'
#'
#' ####################  AssetGroupUpdate  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_asset_group_modification_read_or_update <- AssetGroupModificationReadOrUpdate$new(c(AssetGroupUpdateItemReadOrUpdateItem$new("asset_group_id_example", c(AssetGroupType$new()), c("assets_to_add_example"), c("assets_to_remove_example"), "description_example", "name_example")), c(AssetGroupUpdateError$new("asset_group_id_example", 123, "message_example")), c(AssetGroupBinding$new(c("ad_accounts_ids_example"), "asset_group_description_example", "asset_group_name_example", c("asset_group_types_example"), c("catalogs_ids_example"), BusinessAccessUserSummary$new("email_example", "id_example", "username_example"), 123, "id_example", BusinessAccessUserSummary$new("email_example", "id_example", "username_example"), c("profiles_ids_example"), 123))) # AssetGroupModificationReadOrUpdate | 
#'
#' #Update asset groups.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupUpdate(var_business_id, var_asset_group_modification_read_or_updatedata_file = "result.txt")
#' result <- api_instance$AssetGroupUpdate(var_business_id, var_asset_group_modification_read_or_update)
#' dput(result)
#'
#'
#' ####################  BusinessAssetMembersGet  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_asset_id <- "asset_id_example" # character | Unique identifier of a business asset.
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_fetch_system_users <- FALSE # character | Fetches system users if True. Fetches regular user employees if False. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #Get members with access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetMembersGet(var_business_id, var_asset_id, start_index = var_start_index, fetch_system_users = var_fetch_system_users, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAssetMembersGet(var_business_id, var_asset_id, start_index = var_start_index, fetch_system_users = var_fetch_system_users, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessAssetPartnersGet  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_asset_id <- "asset_id_example" # character | Unique identifier of a business asset.
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #Get partners with access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetPartnersGet(var_business_id, var_asset_id, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAssetPartnersGet(var_business_id, var_asset_id, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessAssetsGet  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_permissions <- c(PermissionsWithOwner$new()) # array[PermissionsWithOwner] | A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned. (Optional)
#' var_child_asset_id <- "child_asset_id_example" # character | A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child. (Optional)
#' var_asset_group_id <- "asset_group_id_example" # character | An asset group unique identifier. Used to fetch assets contained within the specified asset group. (Optional)
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #List business assets
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetsGet(var_business_id, permissions = var_permissions, child_asset_id = var_child_asset_id, asset_group_id = var_asset_group_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAssetsGet(var_business_id, permissions = var_permissions, child_asset_id = var_child_asset_id, asset_group_id = var_asset_group_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessMemberAssetsGet  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_member_id <- "member_id_example" # character | The member id to fetch assets for.
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_sort_by <- AssetSortBy$new() # AssetSortBy | The field to sort member assets by (Optional)
#' var_sort_ascending <- TRUE # character | Sort assets in ascending order (Optional)
#' var_search_by <- AssetSearchBy$new() # AssetSearchBy | The field to search member assets by (Optional)
#' var_search_value <- "search_value_example" # character | The value to search for (Optional)
#' var_asset_permission_type <- AssetPermissionType$new() # AssetPermissionType | The type of asset permission to filter by (Optional)
#' var_ad_account_statuses <- c(NonDraftEntityStatus$new()) # array[NonDraftEntityStatus] | A list of ad account statuses to filter the assets by. Only used when asset_type is AD_ACCOUNT. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #Get assets assigned to a member
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMemberAssetsGet(var_business_id, var_member_id, asset_type = var_asset_type, start_index = var_start_index, sort_by = var_sort_by, sort_ascending = var_sort_ascending, search_by = var_search_by, search_value = var_search_value, asset_permission_type = var_asset_permission_type, ad_account_statuses = var_ad_account_statuses, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessMemberAssetsGet(var_business_id, var_member_id, asset_type = var_asset_type, start_index = var_start_index, sort_by = var_sort_by, sort_ascending = var_sort_ascending, search_by = var_search_by, search_value = var_search_value, asset_permission_type = var_asset_permission_type, ad_account_statuses = var_ad_account_statuses, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessMembersAssetAccessDelete  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_business_members_asset_access_delete_body <- BusinessMembersAssetAccessDeleteBody$new(c(DeleteMemberAssetAccessItem$new("asset_id_example", "member_id_example"))) # BusinessMembersAssetAccessDeleteBody | 
#'
#' #Delete member access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMembersAssetAccessDelete(var_business_id, var_business_members_asset_access_delete_bodydata_file = "result.txt")
#' result <- api_instance$BusinessMembersAssetAccessDelete(var_business_id, var_business_members_asset_access_delete_body)
#' dput(result)
#'
#'
#' ####################  BusinessMembersAssetAccessUpdate  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_update_member_asset_access_body <- UpdateMemberAssetAccessBody$new(c(UpdateMemberAssetAccessItem$new("asset_id_example", "member_id_example", c(Permissions$new())))) # UpdateMemberAssetAccessBody | 
#'
#' #Assign/Update member asset permissions
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMembersAssetAccessUpdate(var_business_id, var_update_member_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$BusinessMembersAssetAccessUpdate(var_business_id, var_update_member_asset_access_body)
#' dput(result)
#'
#'
#' ####################  BusinessPartnerAssetAccessGet  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_partner_id <- "partner_id_example" # character | The partner id to be bound to the Business
#' var_partner_type <- "INTERNAL" # character | Specifies whether to fetch internal or external (shared) partners.  If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.  If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset. (Optional)
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_sort_by <- AssetSortBy$new() # AssetSortBy | The field to sort member assets by (Optional)
#' var_sort_ascending <- TRUE # character | Sort assets in ascending order (Optional)
#' var_search_by <- AssetSearchBy$new() # AssetSearchBy | The field to search member assets by (Optional)
#' var_search_value <- "search_value_example" # character | The value to search for (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #Get assets assigned to a partner or assets assigned by a partner
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessPartnerAssetAccessGet(var_business_id, var_partner_id, partner_type = var_partner_type, asset_type = var_asset_type, start_index = var_start_index, sort_by = var_sort_by, sort_ascending = var_sort_ascending, search_by = var_search_by, search_value = var_search_value, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessPartnerAssetAccessGet(var_business_id, var_partner_id, partner_type = var_partner_type, asset_type = var_asset_type, start_index = var_start_index, sort_by = var_sort_by, sort_ascending = var_sort_ascending, search_by = var_search_by, search_value = var_search_value, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  DeletePartnerAssetAccessHandlerImpl  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_delete_partner_asset_access_body <- DeletePartnerAssetAccessBody$new(c(DeletePartnerAssetAccessItem$new("asset_id_example", "partner_id_example", "INTERNAL"))) # DeletePartnerAssetAccessBody | 
#'
#' #Delete partner access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeletePartnerAssetAccessHandlerImpl(var_business_id, var_delete_partner_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$DeletePartnerAssetAccessHandlerImpl(var_business_id, var_delete_partner_asset_access_body)
#' dput(result)
#'
#'
#' ####################  UpdatePartnerAssetAccessHandlerImpl  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_update_partner_asset_access_body <- UpdatePartnerAssetAccessBody$new(c(UpdatePartnerAssetAccessItem$new("asset_id_example", "partner_id_example", c(Permissions$new())))) # UpdatePartnerAssetAccessBody | 
#'
#' #Assign/Update partner asset permissions
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePartnerAssetAccessHandlerImpl(var_business_id, var_update_partner_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$UpdatePartnerAssetAccessHandlerImpl(var_business_id, var_update_partner_asset_access_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BusinessAccessAssetsApi <- R6::R6Class(
  "BusinessAccessAssetsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BusinessAccessAssetsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create a new asset group.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_input_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetGroupInput
    AssetGroupCreate = function(business_id, asset_group_input_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssetGroupCreateWithHttpInfo(business_id, asset_group_input_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a new asset group.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_input_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetGroupInput) with additional information such as HTTP status code, headers
    AssetGroupCreateWithHttpInfo = function(business_id, asset_group_input_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_group_input_create`)) {
        stop("Missing required parameter `asset_group_input_create`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_group_input_create`) && is.null(`asset_group_input_create`)) {
        stop("Invalid value for `asset_group_input_create` when calling BusinessAccessAssetsApi$AssetGroupCreate, `asset_group_input_create` is not nullable")
      }

      if (!is.null(`asset_group_input_create`)) {
        local_var_body <- `asset_group_input_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetGroupInput"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_deletion_delete 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetGroupDeletion
    AssetGroupDelete = function(business_id, asset_group_deletion_delete, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssetGroupDeleteWithHttpInfo(business_id, asset_group_deletion_delete, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_deletion_delete 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetGroupDeletion) with additional information such as HTTP status code, headers
    AssetGroupDeleteWithHttpInfo = function(business_id, asset_group_deletion_delete, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_group_deletion_delete`)) {
        stop("Missing required parameter `asset_group_deletion_delete`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_group_deletion_delete`) && is.null(`asset_group_deletion_delete`)) {
        stop("Invalid value for `asset_group_deletion_delete` when calling BusinessAccessAssetsApi$AssetGroupDelete, `asset_group_deletion_delete` is not nullable")
      }

      if (!is.null(`asset_group_deletion_delete`)) {
        local_var_body <- `asset_group_deletion_delete`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetGroupDeletion"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_modification_read_or_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AssetGroupModification
    AssetGroupUpdate = function(business_id, asset_group_modification_read_or_update, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AssetGroupUpdateWithHttpInfo(business_id, asset_group_modification_read_or_update, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_group_modification_read_or_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AssetGroupModification) with additional information such as HTTP status code, headers
    AssetGroupUpdateWithHttpInfo = function(business_id, asset_group_modification_read_or_update, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_group_modification_read_or_update`)) {
        stop("Missing required parameter `asset_group_modification_read_or_update`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_group_modification_read_or_update`) && is.null(`asset_group_modification_read_or_update`)) {
        stop("Invalid value for `asset_group_modification_read_or_update` when calling BusinessAccessAssetsApi$AssetGroupUpdate, `asset_group_modification_read_or_update` is not nullable")
      }

      if (!is.null(`asset_group_modification_read_or_update`)) {
        local_var_body <- `asset_group_modification_read_or_update`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AssetGroupModification"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get members with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param fetch_system_users (optional) Fetches system users if True. Fetches regular user employees if False. (default value: FALSE)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessAssetMembersGet200Response
    BusinessAssetMembersGet = function(business_id, asset_id, start_index = 0, fetch_system_users = FALSE, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessAssetMembersGetWithHttpInfo(business_id, asset_id, start_index, fetch_system_users, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get members with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param fetch_system_users (optional) Fetches system users if True. Fetches regular user employees if False. (default value: FALSE)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessAssetMembersGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetMembersGetWithHttpInfo = function(business_id, asset_id, start_index = 0, fetch_system_users = FALSE, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `asset_id` is not nullable")
      }
      if (!is.null(`asset_id`) && nchar(`asset_id`) > 20) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`asset_id`) && nchar(`asset_id`) < 1) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`asset_id`) && !stringr::str_detect(`asset_id`, "^\\d+$")) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`start_index`) && is.null(`start_index`)) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `start_index` is not nullable")
      }
      if (!is.null(`start_index`) && `start_index` <  0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 0.")
      }

      if (!missing(`fetch_system_users`) && is.null(`fetch_system_users`)) {
        stop("Invalid value for `fetch_system_users` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `fetch_system_users` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }

      query_params[["start_index"]] <- `start_index`

      query_params[["fetch_system_users"]] <- `fetch_system_users`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/assets/{asset_id}/members"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_id`)) {
        local_var_url_path <- gsub("\\{asset_id\\}", URLencode(as.character(`asset_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessAssetMembersGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get partners with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessAssetMembersGet200Response
    BusinessAssetPartnersGet = function(business_id, asset_id, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessAssetPartnersGetWithHttpInfo(business_id, asset_id, start_index, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get partners with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessAssetMembersGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetPartnersGetWithHttpInfo = function(business_id, asset_id, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_id`) && is.null(`asset_id`)) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, `asset_id` is not nullable")
      }
      if (!is.null(`asset_id`) && nchar(`asset_id`) > 20) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`asset_id`) && nchar(`asset_id`) < 1) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`asset_id`) && !stringr::str_detect(`asset_id`, "^\\d+$")) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`start_index`) && is.null(`start_index`)) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, `start_index` is not nullable")
      }
      if (!is.null(`start_index`) && `start_index` <  0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 0.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/assets/{asset_id}/partners"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_id`)) {
        local_var_url_path <- gsub("\\{asset_id\\}", URLencode(as.character(`asset_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessAssetMembersGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List business assets
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param permissions (optional) A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned.
    #' @param child_asset_id (optional) A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child.
    #' @param asset_group_id (optional) An asset group unique identifier. Used to fetch assets contained within the specified asset group.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessAssetsGet200Response
    BusinessAssetsGet = function(business_id, permissions = NULL, child_asset_id = NULL, asset_group_id = NULL, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessAssetsGetWithHttpInfo(business_id, permissions, child_asset_id, asset_group_id, asset_type, start_index, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List business assets
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param permissions (optional) A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned.
    #' @param child_asset_id (optional) A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child.
    #' @param asset_group_id (optional) An asset group unique identifier. Used to fetch assets contained within the specified asset group.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessAssetsGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetsGetWithHttpInfo = function(business_id, permissions = NULL, child_asset_id = NULL, asset_group_id = NULL, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`permissions`) && is.null(`permissions`)) {
        stop("Invalid value for `permissions` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `permissions` is not nullable")
      }

      if (!missing(`child_asset_id`) && is.null(`child_asset_id`)) {
        stop("Invalid value for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `child_asset_id` is not nullable")
      }
      if (!is.null(`child_asset_id`) && nchar(`child_asset_id`) > 20) {
        stop("Invalid length for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`child_asset_id`) && nchar(`child_asset_id`) < 1) {
        stop("Invalid length for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`child_asset_id`) && !stringr::str_detect(`child_asset_id`, "^\\d+$")) {
        stop("Invalid value for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_group_id`) && is.null(`asset_group_id`)) {
        stop("Invalid value for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `asset_group_id` is not nullable")
      }
      if (!is.null(`asset_group_id`) && nchar(`asset_group_id`) > 20) {
        stop("Invalid length for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`asset_group_id`) && nchar(`asset_group_id`) < 1) {
        stop("Invalid length for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`asset_group_id`) && !stringr::str_detect(`asset_group_id`, "^\\d+$")) {
        stop("Invalid value for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `asset_type` is not nullable")
      }

      if (!missing(`start_index`) && is.null(`start_index`)) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `start_index` is not nullable")
      }
      if (!is.null(`start_index`) && `start_index` <  0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 0.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetsGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }

      # explore
      for (query_item in `permissions`) {
        query_params[["permissions"]] <- c(query_params[["permissions"]], list(`permissions` = query_item))
      }

      query_params[["child_asset_id"]] <- `child_asset_id`

      query_params[["asset_group_id"]] <- `asset_group_id`

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP", "CATALOG", "CONSUMER"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessAssetsGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP, CATALOG, CONSUMER].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessAssetsGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get assets assigned to a member
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param member_id The member id to fetch assets for.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param sort_by (optional) The field to sort member assets by
    #' @param sort_ascending (optional) Sort assets in ascending order (default value: TRUE)
    #' @param search_by (optional) The field to search member assets by
    #' @param search_value (optional) The value to search for
    #' @param asset_permission_type (optional) The type of asset permission to filter by
    #' @param ad_account_statuses (optional) A list of ad account statuses to filter the assets by. Only used when asset_type is AD_ACCOUNT.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessMemberAssetsGetResponse
    BusinessMemberAssetsGet = function(business_id, member_id, asset_type = "AD_ACCOUNT", start_index = 0, sort_by = NULL, sort_ascending = TRUE, search_by = NULL, search_value = NULL, asset_permission_type = NULL, ad_account_statuses = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessMemberAssetsGetWithHttpInfo(business_id, member_id, asset_type, start_index, sort_by, sort_ascending, search_by, search_value, asset_permission_type, ad_account_statuses, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get assets assigned to a member
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param member_id The member id to fetch assets for.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param sort_by (optional) The field to sort member assets by
    #' @param sort_ascending (optional) Sort assets in ascending order (default value: TRUE)
    #' @param search_by (optional) The field to search member assets by
    #' @param search_value (optional) The value to search for
    #' @param asset_permission_type (optional) The type of asset permission to filter by
    #' @param ad_account_statuses (optional) A list of ad account statuses to filter the assets by. Only used when asset_type is AD_ACCOUNT.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessMemberAssetsGetResponse) with additional information such as HTTP status code, headers
    BusinessMemberAssetsGetWithHttpInfo = function(business_id, member_id, asset_type = "AD_ACCOUNT", start_index = 0, sort_by = NULL, sort_ascending = TRUE, search_by = NULL, search_value = NULL, asset_permission_type = NULL, ad_account_statuses = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`member_id`)) {
        stop("Missing required parameter `member_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`member_id`) && is.null(`member_id`)) {
        stop("Invalid value for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `member_id` is not nullable")
      }
      if (!is.null(`member_id`) && nchar(`member_id`) > 20) {
        stop("Invalid length for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`member_id`) && nchar(`member_id`) < 1) {
        stop("Invalid length for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`member_id`) && !stringr::str_detect(`member_id`, "^\\d+$")) {
        stop("Invalid value for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `asset_type` is not nullable")
      }

      if (!missing(`start_index`) && is.null(`start_index`)) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `start_index` is not nullable")
      }
      if (!is.null(`start_index`) && `start_index` <  0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 0.")
      }

      if (!missing(`sort_by`) && is.null(`sort_by`)) {
        stop("Invalid value for `sort_by` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `sort_by` is not nullable")
      }

      if (!missing(`sort_ascending`) && is.null(`sort_ascending`)) {
        stop("Invalid value for `sort_ascending` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `sort_ascending` is not nullable")
      }

      if (!missing(`search_by`) && is.null(`search_by`)) {
        stop("Invalid value for `search_by` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `search_by` is not nullable")
      }

      if (!missing(`search_value`) && is.null(`search_value`)) {
        stop("Invalid value for `search_value` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `search_value` is not nullable")
      }

      if (!missing(`asset_permission_type`) && is.null(`asset_permission_type`)) {
        stop("Invalid value for `asset_permission_type` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `asset_permission_type` is not nullable")
      }

      if (!missing(`ad_account_statuses`) && is.null(`ad_account_statuses`)) {
        stop("Invalid value for `ad_account_statuses` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `ad_account_statuses` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP", "CATALOG", "CONSUMER", "CONVERSION_TAG"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP, CATALOG, CONSUMER, CONVERSION_TAG].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["sort_by"]] <- `sort_by`

      query_params[["sort_ascending"]] <- `sort_ascending`

      query_params[["search_by"]] <- `search_by`

      query_params[["search_value"]] <- `search_value`

      query_params[["asset_permission_type"]] <- `asset_permission_type`

      # explore
      for (query_item in `ad_account_statuses`) {
        query_params[["ad_account_statuses"]] <- c(query_params[["ad_account_statuses"]], list(`ad_account_statuses` = query_item))
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/members/{member_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`member_id`)) {
        local_var_url_path <- gsub("\\{member_id\\}", URLencode(as.character(`member_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessMemberAssetsGetResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete member access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_members_asset_access_delete_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DeleteMemberAccessResultsResponseArray
    BusinessMembersAssetAccessDelete = function(business_id, business_members_asset_access_delete_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessMembersAssetAccessDeleteWithHttpInfo(business_id, business_members_asset_access_delete_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete member access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_members_asset_access_delete_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DeleteMemberAccessResultsResponseArray) with additional information such as HTTP status code, headers
    BusinessMembersAssetAccessDeleteWithHttpInfo = function(business_id, business_members_asset_access_delete_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`business_members_asset_access_delete_body`)) {
        stop("Missing required parameter `business_members_asset_access_delete_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`business_members_asset_access_delete_body`) && is.null(`business_members_asset_access_delete_body`)) {
        stop("Invalid value for `business_members_asset_access_delete_body` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, `business_members_asset_access_delete_body` is not nullable")
      }

      if (!is.null(`business_members_asset_access_delete_body`)) {
        local_var_body <- `business_members_asset_access_delete_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeleteMemberAccessResultsResponseArray"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign/Update member asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_member_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return UpdateMemberAssetsResultsResponseArray
    BusinessMembersAssetAccessUpdate = function(business_id, update_member_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessMembersAssetAccessUpdateWithHttpInfo(business_id, update_member_asset_access_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign/Update member asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_member_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (UpdateMemberAssetsResultsResponseArray) with additional information such as HTTP status code, headers
    BusinessMembersAssetAccessUpdateWithHttpInfo = function(business_id, update_member_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_member_asset_access_body`)) {
        stop("Missing required parameter `update_member_asset_access_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`update_member_asset_access_body`) && is.null(`update_member_asset_access_body`)) {
        stop("Invalid value for `update_member_asset_access_body` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, `update_member_asset_access_body` is not nullable")
      }

      if (!is.null(`update_member_asset_access_body`)) {
        local_var_body <- `update_member_asset_access_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UpdateMemberAssetsResultsResponseArray"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get assets assigned to a partner or assets assigned by a partner
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param partner_id The partner id to be bound to the Business
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners.  If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.  If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset. (default value: "INTERNAL")
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param sort_by (optional) The field to sort member assets by
    #' @param sort_ascending (optional) Sort assets in ascending order (default value: TRUE)
    #' @param search_by (optional) The field to search member assets by
    #' @param search_value (optional) The value to search for
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessPartnerAssetAccessGet200Response
    BusinessPartnerAssetAccessGet = function(business_id, partner_id, partner_type = "INTERNAL", asset_type = "AD_ACCOUNT", start_index = 0, sort_by = NULL, sort_ascending = TRUE, search_by = NULL, search_value = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessPartnerAssetAccessGetWithHttpInfo(business_id, partner_id, partner_type, asset_type, start_index, sort_by, sort_ascending, search_by, search_value, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get assets assigned to a partner or assets assigned by a partner
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param partner_id The partner id to be bound to the Business
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners.  If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.  If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset. (default value: "INTERNAL")
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param sort_by (optional) The field to sort member assets by
    #' @param sort_ascending (optional) Sort assets in ascending order (default value: TRUE)
    #' @param search_by (optional) The field to search member assets by
    #' @param search_value (optional) The value to search for
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessPartnerAssetAccessGet200Response) with additional information such as HTTP status code, headers
    BusinessPartnerAssetAccessGetWithHttpInfo = function(business_id, partner_id, partner_type = "INTERNAL", asset_type = "AD_ACCOUNT", start_index = 0, sort_by = NULL, sort_ascending = TRUE, search_by = NULL, search_value = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`partner_id`)) {
        stop("Missing required parameter `partner_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`partner_id`) && is.null(`partner_id`)) {
        stop("Invalid value for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `partner_id` is not nullable")
      }
      if (!is.null(`partner_id`) && nchar(`partner_id`) > 20) {
        stop("Invalid length for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 20.")
      }
      if (!is.null(`partner_id`) && nchar(`partner_id`) < 1) {
        stop("Invalid length for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }
      if (!is.null(`partner_id`) && !stringr::str_detect(`partner_id`, "^\\d+$")) {
        stop("Invalid value for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`partner_type`) && is.null(`partner_type`)) {
        stop("Invalid value for `partner_type` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `partner_type` is not nullable")
      }

      if (!missing(`asset_type`) && is.null(`asset_type`)) {
        stop("Invalid value for `asset_type` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `asset_type` is not nullable")
      }

      if (!missing(`start_index`) && is.null(`start_index`)) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `start_index` is not nullable")
      }
      if (!is.null(`start_index`) && `start_index` <  0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 0.")
      }

      if (!missing(`sort_by`) && is.null(`sort_by`)) {
        stop("Invalid value for `sort_by` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `sort_by` is not nullable")
      }

      if (!missing(`sort_ascending`) && is.null(`sort_ascending`)) {
        stop("Invalid value for `sort_ascending` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `sort_ascending` is not nullable")
      }

      if (!missing(`search_by`) && is.null(`search_by`)) {
        stop("Invalid value for `search_by` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `search_by` is not nullable")
      }

      if (!missing(`search_value`) && is.null(`search_value`)) {
        stop("Invalid value for `search_value` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `search_value` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }

      if (!is.null(`partner_type`) && !(`partner_type` %in% c("INTERNAL", "EXTERNAL"))) {
        stop("Invalid value for partner_type when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet. Must be [INTERNAL, EXTERNAL].")
      }
      query_params[["partner_type"]] <- `partner_type`

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP", "PINNER_LIST", "CONVERSION_TAG", "CATALOG", "CONSUMER", "CONVERSION_SEGMENT"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP, PINNER_LIST, CONVERSION_TAG, CATALOG, CONSUMER, CONVERSION_SEGMENT].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["sort_by"]] <- `sort_by`

      query_params[["sort_ascending"]] <- `sort_ascending`

      query_params[["search_by"]] <- `search_by`

      query_params[["search_value"]] <- `search_value`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/partners/{partner_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`partner_id`)) {
        local_var_url_path <- gsub("\\{partner_id\\}", URLencode(as.character(`partner_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessPartnerAssetAccessGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete partner access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DeletePartnerAssetAccessResultsResponseArray
    DeletePartnerAssetAccessHandlerImpl = function(business_id, delete_partner_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$DeletePartnerAssetAccessHandlerImplWithHttpInfo(business_id, delete_partner_asset_access_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete partner access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DeletePartnerAssetAccessResultsResponseArray) with additional information such as HTTP status code, headers
    DeletePartnerAssetAccessHandlerImplWithHttpInfo = function(business_id, delete_partner_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`delete_partner_asset_access_body`)) {
        stop("Missing required parameter `delete_partner_asset_access_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`delete_partner_asset_access_body`) && is.null(`delete_partner_asset_access_body`)) {
        stop("Invalid value for `delete_partner_asset_access_body` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, `delete_partner_asset_access_body` is not nullable")
      }

      if (!is.null(`delete_partner_asset_access_body`)) {
        local_var_body <- `delete_partner_asset_access_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/partners/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DeletePartnerAssetAccessResultsResponseArray"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign/Update partner asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return UpdatePartnerAssetsResultsResponseArray
    UpdatePartnerAssetAccessHandlerImpl = function(business_id, update_partner_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdatePartnerAssetAccessHandlerImplWithHttpInfo(business_id, update_partner_asset_access_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign/Update partner asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (UpdatePartnerAssetsResultsResponseArray) with additional information such as HTTP status code, headers
    UpdatePartnerAssetAccessHandlerImplWithHttpInfo = function(business_id, update_partner_asset_access_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_partner_asset_access_body`)) {
        stop("Missing required parameter `update_partner_asset_access_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`update_partner_asset_access_body`) && is.null(`update_partner_asset_access_body`)) {
        stop("Invalid value for `update_partner_asset_access_body` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, `update_partner_asset_access_body` is not nullable")
      }

      if (!is.null(`update_partner_asset_access_body`)) {
        local_var_body <- `update_partner_asset_access_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/partners/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UpdatePartnerAssetsResultsResponseArray"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
