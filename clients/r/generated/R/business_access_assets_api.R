#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BusinessAccessAssets operations
#' @description BusinessAccessAssetsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AssetGroupCreate  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_create_asset_group_body <- CreateAssetGroupBody$new("asset_group_name_example", "asset_group_description_example", c(AssetGroupType$new())) # CreateAssetGroupBody | 
#'
#' #Create a new asset group.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupCreate(var_business_id, var_create_asset_group_bodydata_file = "result.txt")
#' result <- api_instance$AssetGroupCreate(var_business_id, var_create_asset_group_body)
#' dput(result)
#'
#'
#' ####################  AssetGroupDelete  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_delete_asset_group_body <- DeleteAssetGroupBody$new(c("asset_groups_to_delete_example")) # DeleteAssetGroupBody | 
#'
#' #Delete asset groups.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupDelete(var_business_id, var_delete_asset_group_bodydata_file = "result.txt")
#' result <- api_instance$AssetGroupDelete(var_business_id, var_delete_asset_group_body)
#' dput(result)
#'
#'
#' ####################  AssetGroupUpdate  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_update_asset_group_body <- UpdateAssetGroupBody$new(c(UpdateAssetGroupBody_asset_groups_to_update_inner$new("asset_group_id_example", "name_example", "description_example", c(AssetGroupType$new()), c("assets_to_add_example"), c("assets_to_remove_example")))) # UpdateAssetGroupBody | 
#'
#' #Update asset groups.
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetGroupUpdate(var_business_id, var_update_asset_group_bodydata_file = "result.txt")
#' result <- api_instance$AssetGroupUpdate(var_business_id, var_update_asset_group_body)
#' dput(result)
#'
#'
#' ####################  BusinessAssetMembersGet  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_asset_id <- "729090764583391194" # character | Unique identifier of a business asset.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#'
#' #Get members with access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetMembersGet(var_business_id, var_asset_id, bookmark = var_bookmark, page_size = var_page_size, start_index = var_start_indexdata_file = "result.txt")
#' result <- api_instance$BusinessAssetMembersGet(var_business_id, var_asset_id, bookmark = var_bookmark, page_size = var_page_size, start_index = var_start_index)
#' dput(result)
#'
#'
#' ####################  BusinessAssetPartnersGet  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_asset_id <- "729090764583391194" # character | Unique identifier of a business asset.
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get partners with access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetPartnersGet(var_business_id, var_asset_id, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAssetPartnersGet(var_business_id, var_asset_id, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessAssetsGet  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_permissions <- c(PermissionsWithOwner$new()) # array[PermissionsWithOwner] | A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned. (Optional)
#' var_child_asset_id <- "549764894835" # character | A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child. (Optional)
#' var_asset_group_id <- "7078106104032" # character | An asset group unique identifier. Used to fetch assets contained within the specified asset group. (Optional)
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #List business assets
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAssetsGet(var_business_id, permissions = var_permissions, child_asset_id = var_child_asset_id, asset_group_id = var_asset_group_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAssetsGet(var_business_id, permissions = var_permissions, child_asset_id = var_child_asset_id, asset_group_id = var_asset_group_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessMemberAssetsGet  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_member_id <- "729090764583391194" # character | The member id to fetch assets for.
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get assets assigned to a member
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMemberAssetsGet(var_business_id, var_member_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessMemberAssetsGet(var_business_id, var_member_id, asset_type = var_asset_type, start_index = var_start_index, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessMembersAssetAccessDelete  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_business_members_asset_access_delete_request <- business_members_asset_access_delete_request$new(c(business_members_asset_access_delete_request_accesses_inner$new("asset_id_example", "member_id_example"))) # BusinessMembersAssetAccessDeleteRequest | List member assset permissions to delete.
#'
#' #Delete member access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMembersAssetAccessDelete(var_business_id, var_business_members_asset_access_delete_requestdata_file = "result.txt")
#' result <- api_instance$BusinessMembersAssetAccessDelete(var_business_id, var_business_members_asset_access_delete_request)
#' dput(result)
#'
#'
#' ####################  BusinessMembersAssetAccessUpdate  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_update_member_asset_access_body <- UpdateMemberAssetAccessBody$new(c(UpdateMemberAssetAccessBody_accesses_inner$new("asset_id_example", "member_id_example", c(Permissions$new())))) # UpdateMemberAssetAccessBody | List of member asset permissions to create or update.
#'
#' #Assign/Update member asset permissions
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessMembersAssetAccessUpdate(var_business_id, var_update_member_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$BusinessMembersAssetAccessUpdate(var_business_id, var_update_member_asset_access_body)
#' dput(result)
#'
#'
#' ####################  BusinessPartnerAssetAccessGet  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_partner_id <- "729090764583391194" # character | The partner id to be bound to the Business
#' var_partner_type <- PartnerType$new() # PartnerType | Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset. (Optional)
#' var_asset_type <- "AD_ACCOUNT" # character | A resource type to filter the assets by. Only assets of the specified type will be returned. (Optional)
#' var_start_index <- 0 # integer | An index to start fetching the results from. Only the results starting from this index will be returned. (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #Get assets assigned to a partner or assets assigned by a partner
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessPartnerAssetAccessGet(var_business_id, var_partner_id, partner_type = var_partner_type, asset_type = var_asset_type, start_index = var_start_index, page_size = var_page_size, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$BusinessPartnerAssetAccessGet(var_business_id, var_partner_id, partner_type = var_partner_type, asset_type = var_asset_type, start_index = var_start_index, page_size = var_page_size, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  DeletePartnerAssetAccessHandlerImpl  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_delete_partner_asset_access_body <- DeletePartnerAssetAccessBody$new(c(DeletePartnerAssetAccessBody_accesses_inner$new("partner_id_example", "asset_id_example", "INTERNAL"))) # DeletePartnerAssetAccessBody | 
#'
#' #Delete partner access to asset
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeletePartnerAssetAccessHandlerImpl(var_business_id, var_delete_partner_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$DeletePartnerAssetAccessHandlerImpl(var_business_id, var_delete_partner_asset_access_body)
#' dput(result)
#'
#'
#' ####################  UpdatePartnerAssetAccessHandlerImpl  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_update_partner_asset_access_body <- UpdatePartnerAssetAccessBody$new(c(UpdatePartnerAssetAccessBody_accesses_inner$new("partner_id_example", "asset_id_example", c(Permissions$new())))) # UpdatePartnerAssetAccessBody | A list of assets and permissions to assign to your partners.
#'
#' #Assign/Update partner asset permissions
#' api_instance <- BusinessAccessAssetsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePartnerAssetAccessHandlerImpl(var_business_id, var_update_partner_asset_access_bodydata_file = "result.txt")
#' result <- api_instance$UpdatePartnerAssetAccessHandlerImpl(var_business_id, var_update_partner_asset_access_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BusinessAccessAssetsApi <- R6::R6Class(
  "BusinessAccessAssetsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BusinessAccessAssetsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create a new asset group.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CreateAssetGroupResponse
    AssetGroupCreate = function(business_id, create_asset_group_body, data_file = NULL, ...) {
      local_var_response <- self$AssetGroupCreateWithHttpInfo(business_id, create_asset_group_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create a new asset group.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CreateAssetGroupResponse) with additional information such as HTTP status code, headers
    AssetGroupCreateWithHttpInfo = function(business_id, create_asset_group_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`create_asset_group_body`)) {
        stop("Missing required parameter `create_asset_group_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupCreate, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`create_asset_group_body`)) {
        local_var_body <- `create_asset_group_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateAssetGroupResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteAssetGroupResponse
    AssetGroupDelete = function(business_id, delete_asset_group_body, data_file = NULL, ...) {
      local_var_response <- self$AssetGroupDeleteWithHttpInfo(business_id, delete_asset_group_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteAssetGroupResponse) with additional information such as HTTP status code, headers
    AssetGroupDeleteWithHttpInfo = function(business_id, delete_asset_group_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`delete_asset_group_body`)) {
        stop("Missing required parameter `delete_asset_group_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupDelete, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`delete_asset_group_body`)) {
        local_var_body <- `delete_asset_group_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteAssetGroupResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UpdateAssetGroupResponse
    AssetGroupUpdate = function(business_id, update_asset_group_body, data_file = NULL, ...) {
      local_var_response <- self$AssetGroupUpdateWithHttpInfo(business_id, update_asset_group_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update asset groups.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_asset_group_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UpdateAssetGroupResponse) with additional information such as HTTP status code, headers
    AssetGroupUpdateWithHttpInfo = function(business_id, update_asset_group_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_asset_group_body`)) {
        stop("Missing required parameter `update_asset_group_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$AssetGroupUpdate, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`update_asset_group_body`)) {
        local_var_body <- `update_asset_group_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/asset_groups"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "UpdateAssetGroupResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get members with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BusinessAssetMembersGet200Response
    BusinessAssetMembersGet = function(business_id, asset_id, bookmark = NULL, page_size = 25, start_index = 0, data_file = NULL, ...) {
      local_var_response <- self$BusinessAssetMembersGetWithHttpInfo(business_id, asset_id, bookmark, page_size, start_index, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get members with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BusinessAssetMembersGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetMembersGetWithHttpInfo = function(business_id, asset_id, bookmark = NULL, page_size = 25, start_index = 0, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`asset_id`) > 20) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 20.")
      }
      if (nchar(`asset_id`) < 1) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`asset_id`, "^\\d+$")) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must conform to the pattern ^\\d+$.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 1.")
      }

      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetMembersGet, must be bigger than or equal to 0.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["start_index"]] <- `start_index`

      local_var_url_path <- "/businesses/{business_id}/assets/{asset_id}/members"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_id`)) {
        local_var_url_path <- gsub("\\{asset_id\\}", URLencode(as.character(`asset_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BusinessAssetMembersGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get partners with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BusinessAssetPartnersGet200Response
    BusinessAssetPartnersGet = function(business_id, asset_id, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$BusinessAssetPartnersGetWithHttpInfo(business_id, asset_id, start_index, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get partners with access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param asset_id Unique identifier of a business asset.
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BusinessAssetPartnersGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetPartnersGetWithHttpInfo = function(business_id, asset_id, start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`asset_id`)) {
        stop("Missing required parameter `asset_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`asset_id`) > 20) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 20.")
      }
      if (nchar(`asset_id`) < 1) {
        stop("Invalid length for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`asset_id`, "^\\d+$")) {
        stop("Invalid value for `asset_id` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must conform to the pattern ^\\d+$.")
      }

      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 0.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetPartnersGet, must be bigger than or equal to 1.")
      }

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/assets/{asset_id}/partners"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`asset_id`)) {
        local_var_url_path <- gsub("\\{asset_id\\}", URLencode(as.character(`asset_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BusinessAssetPartnersGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' List business assets
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param permissions (optional) A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned.
    #' @param child_asset_id (optional) A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child.
    #' @param asset_group_id (optional) An asset group unique identifier. Used to fetch assets contained within the specified asset group.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BusinessAssetsGet200Response
    BusinessAssetsGet = function(business_id, permissions = NULL, child_asset_id = NULL, asset_group_id = NULL, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$BusinessAssetsGetWithHttpInfo(business_id, permissions, child_asset_id, asset_group_id, asset_type, start_index, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' List business assets
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param permissions (optional) A list of asset permissions used to filter the assets. Only assets where the requesting business has at least one of the specified permissions will be returned.
    #' @param child_asset_id (optional) A child asset unique identifier. Used to fetch asset groups that contain the asset id as a child.
    #' @param asset_group_id (optional) An asset group unique identifier. Used to fetch assets contained within the specified asset group.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BusinessAssetsGet200Response) with additional information such as HTTP status code, headers
    BusinessAssetsGetWithHttpInfo = function(business_id, permissions = NULL, child_asset_id = NULL, asset_group_id = NULL, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }


      if (nchar(`child_asset_id`) > 20) {
        stop("Invalid length for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (nchar(`child_asset_id`) < 1) {
        stop("Invalid length for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`child_asset_id`, "^\\d+$")) {
        stop("Invalid value for `child_asset_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`asset_group_id`) > 20) {
        stop("Invalid length for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 20.")
      }
      if (nchar(`asset_group_id`) < 1) {
        stop("Invalid length for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`asset_group_id`, "^\\d+$")) {
        stop("Invalid value for `asset_group_id` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must conform to the pattern ^\\d+$.")
      }


      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 0.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessAssetsGet, must be bigger than or equal to 1.")
      }

      # explore
      for (query_item in `permissions`) {
        query_params[["permissions"]] <- c(query_params[["permissions"]], list(`permissions` = query_item))
      }

      query_params[["child_asset_id"]] <- `child_asset_id`

      query_params[["asset_group_id"]] <- `asset_group_id`

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessAssetsGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BusinessAssetsGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get assets assigned to a member
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param member_id The member id to fetch assets for.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BusinessMemberAssetsGet200Response
    BusinessMemberAssetsGet = function(business_id, member_id, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$BusinessMemberAssetsGetWithHttpInfo(business_id, member_id, asset_type, start_index, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get assets assigned to a member
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param member_id The member id to fetch assets for.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BusinessMemberAssetsGet200Response) with additional information such as HTTP status code, headers
    BusinessMemberAssetsGetWithHttpInfo = function(business_id, member_id, asset_type = "AD_ACCOUNT", start_index = 0, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`member_id`)) {
        stop("Missing required parameter `member_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`member_id`) > 20) {
        stop("Invalid length for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 20.")
      }
      if (nchar(`member_id`) < 1) {
        stop("Invalid length for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`member_id`, "^\\d+$")) {
        stop("Invalid value for `member_id` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must conform to the pattern ^\\d+$.")
      }


      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 0.")
      }


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet, must be bigger than or equal to 1.")
      }

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessMemberAssetsGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/members/{member_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`member_id`)) {
        local_var_url_path <- gsub("\\{member_id\\}", URLencode(as.character(`member_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BusinessMemberAssetsGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete member access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_members_asset_access_delete_request List member assset permissions to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteMemberAccessResultsResponseArray
    BusinessMembersAssetAccessDelete = function(business_id, business_members_asset_access_delete_request, data_file = NULL, ...) {
      local_var_response <- self$BusinessMembersAssetAccessDeleteWithHttpInfo(business_id, business_members_asset_access_delete_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete member access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_members_asset_access_delete_request List member assset permissions to delete.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteMemberAccessResultsResponseArray) with additional information such as HTTP status code, headers
    BusinessMembersAssetAccessDeleteWithHttpInfo = function(business_id, business_members_asset_access_delete_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`business_members_asset_access_delete_request`)) {
        stop("Missing required parameter `business_members_asset_access_delete_request`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessDelete, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`business_members_asset_access_delete_request`)) {
        local_var_body <- `business_members_asset_access_delete_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteMemberAccessResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Assign/Update member asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_member_asset_access_body List of member asset permissions to create or update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UpdateMemberAssetsResultsResponseArray
    BusinessMembersAssetAccessUpdate = function(business_id, update_member_asset_access_body, data_file = NULL, ...) {
      local_var_response <- self$BusinessMembersAssetAccessUpdateWithHttpInfo(business_id, update_member_asset_access_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Assign/Update member asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_member_asset_access_body List of member asset permissions to create or update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UpdateMemberAssetsResultsResponseArray) with additional information such as HTTP status code, headers
    BusinessMembersAssetAccessUpdateWithHttpInfo = function(business_id, update_member_asset_access_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_member_asset_access_body`)) {
        stop("Missing required parameter `update_member_asset_access_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessMembersAssetAccessUpdate, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`update_member_asset_access_body`)) {
        local_var_body <- `update_member_asset_access_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/members/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "UpdateMemberAssetsResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get assets assigned to a partner or assets assigned by a partner
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param partner_id The partner id to be bound to the Business
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BusinessPartnerAssetAccessGet200Response
    BusinessPartnerAssetAccessGet = function(business_id, partner_id, partner_type = NULL, asset_type = "AD_ACCOUNT", start_index = 0, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$BusinessPartnerAssetAccessGetWithHttpInfo(business_id, partner_id, partner_type, asset_type, start_index, page_size, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get assets assigned to a partner or assets assigned by a partner
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param partner_id The partner id to be bound to the Business
    #' @param partner_type (optional) Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets.<br> If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
    #' @param asset_type (optional) A resource type to filter the assets by. Only assets of the specified type will be returned. (default value: "AD_ACCOUNT")
    #' @param start_index (optional) An index to start fetching the results from. Only the results starting from this index will be returned. (default value: 0)
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BusinessPartnerAssetAccessGet200Response) with additional information such as HTTP status code, headers
    BusinessPartnerAssetAccessGetWithHttpInfo = function(business_id, partner_id, partner_type = NULL, asset_type = "AD_ACCOUNT", start_index = 0, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`partner_id`)) {
        stop("Missing required parameter `partner_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`partner_id`) > 20) {
        stop("Invalid length for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 20.")
      }
      if (nchar(`partner_id`) < 1) {
        stop("Invalid length for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }
      if (!str_detect(`partner_id`, "^\\d+$")) {
        stop("Invalid value for `partner_id` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must conform to the pattern ^\\d+$.")
      }



      if (`start_index` < 0) {
        stop("Invalid value for `start_index` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 0.")
      }

      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet, must be bigger than or equal to 1.")
      }


      query_params[["partner_type"]] <- `partner_type`

      if (!is.null(`asset_type`) && !(`asset_type` %in% c("AD_ACCOUNT", "PROFILE", "ASSET_GROUP"))) {
        stop("Invalid value for asset_type when calling BusinessAccessAssetsApi$BusinessPartnerAssetAccessGet. Must be [AD_ACCOUNT, PROFILE, ASSET_GROUP].")
      }
      query_params[["asset_type"]] <- `asset_type`

      query_params[["start_index"]] <- `start_index`

      query_params[["page_size"]] <- `page_size`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/businesses/{business_id}/partners/{partner_id}/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`partner_id`)) {
        local_var_url_path <- gsub("\\{partner_id\\}", URLencode(as.character(`partner_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "BusinessPartnerAssetAccessGet200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete partner access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeletePartnerAssetsResultsResponseArray
    DeletePartnerAssetAccessHandlerImpl = function(business_id, delete_partner_asset_access_body, data_file = NULL, ...) {
      local_var_response <- self$DeletePartnerAssetAccessHandlerImplWithHttpInfo(business_id, delete_partner_asset_access_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete partner access to asset
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param delete_partner_asset_access_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeletePartnerAssetsResultsResponseArray) with additional information such as HTTP status code, headers
    DeletePartnerAssetAccessHandlerImplWithHttpInfo = function(business_id, delete_partner_asset_access_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`delete_partner_asset_access_body`)) {
        stop("Missing required parameter `delete_partner_asset_access_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$DeletePartnerAssetAccessHandlerImpl, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`delete_partner_asset_access_body`)) {
        local_var_body <- `delete_partner_asset_access_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/partners/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeletePartnerAssetsResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Assign/Update partner asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_partner_asset_access_body A list of assets and permissions to assign to your partners.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UpdatePartnerAssetsResultsResponseArray
    UpdatePartnerAssetAccessHandlerImpl = function(business_id, update_partner_asset_access_body, data_file = NULL, ...) {
      local_var_response <- self$UpdatePartnerAssetAccessHandlerImplWithHttpInfo(business_id, update_partner_asset_access_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Assign/Update partner asset permissions
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param update_partner_asset_access_body A list of assets and permissions to assign to your partners.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UpdatePartnerAssetsResultsResponseArray) with additional information such as HTTP status code, headers
    UpdatePartnerAssetAccessHandlerImplWithHttpInfo = function(business_id, update_partner_asset_access_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_partner_asset_access_body`)) {
        stop("Missing required parameter `update_partner_asset_access_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessAssetsApi$UpdatePartnerAssetAccessHandlerImpl, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`update_partner_asset_access_body`)) {
        local_var_body <- `update_partner_asset_access_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/partners/assets"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "UpdatePartnerAssetsResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
