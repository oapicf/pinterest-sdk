#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title UserAccount operations
#' @description UserAccountApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  BoardsUserFollowsList  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_explicit_following <- FALSE # character | Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #List following boards
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BoardsUserFollowsList(bookmark = var_bookmark, page_size = var_page_size, explicit_following = var_explicit_following, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$BoardsUserFollowsList(bookmark = var_bookmark, page_size = var_page_size, explicit_following = var_explicit_following, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  FollowUserUpdate  ####################
#'
#' library(openapi)
#' var_username <- "username" # character | A valid username
#' var_follow_user_request <- FollowUserRequest$new("auto_follow_example") # FollowUserRequest | Follow a user.
#'
#' #Follow user
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FollowUserUpdate(var_username, var_follow_user_requestdata_file = "result.txt")
#' result <- api_instance$FollowUserUpdate(var_username, var_follow_user_request)
#' dput(result)
#'
#'
#' ####################  FollowersList  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #List followers
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FollowersList(bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$FollowersList(bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  LinkedBusinessAccountsGet  ####################
#'
#' library(openapi)
#'
#' #List linked businesses
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LinkedBusinessAccountsGet(data_file = "result.txt")
#' result <- api_instance$LinkedBusinessAccountsGet()
#' dput(result)
#'
#'
#' ####################  UnverifyWebsiteDelete  ####################
#'
#' library(openapi)
#' var_website <- "mysite.test" # character | Website with path or domain only
#'
#' #Unverify website
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$UnverifyWebsiteDelete(var_website)
#'
#'
#' ####################  UserAccountAnalytics  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_from_claimed_content <- "BOTH" # character | Filter on Pins that match your claimed domain. (Optional)
#' var_pin_format <- "ALL" # character | Pin formats to get data for, default is all. (Optional)
#' var_app_types <- "ALL" # character | Apps or devices to get data for, default is all. (Optional)
#' var_content_type <- "ALL" # character | Filter to paid or organic data. Default is all. (Optional)
#' var_source <- "ALL" # character | Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (Optional)
#' var_metric_types <- c("ENGAGEMENT") # array[character] | Metric types to get data for, default is all.  (Optional)
#' var_split_field <- "NO_SPLIT" # character | How to split the data into groups. Not including this param means data won't be split. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get user account analytics
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserAccountAnalytics(var_start_date, var_end_date, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, split_field = var_split_field, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$UserAccountAnalytics(var_start_date, var_end_date, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, split_field = var_split_field, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  UserAccountAnalyticsTopPins  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_sort_by <- "sort_by_example" # character | Specify sorting order for metrics
#' var_from_claimed_content <- "BOTH" # character | Filter on Pins that match your claimed domain. (Optional)
#' var_pin_format <- "ALL" # character | Pin formats to get data for, default is all. (Optional)
#' var_app_types <- "ALL" # character | Apps or devices to get data for, default is all. (Optional)
#' var_content_type <- "ALL" # character | Filter to paid or organic data. Default is all. (Optional)
#' var_source <- "ALL" # character | Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (Optional)
#' var_metric_types <- c("ENGAGEMENT") # array[character] | Metric types to get data for, default is all.  (Optional)
#' var_num_of_pins <- 10 # integer | Number of pins to include, default is 10. Max is 50. (Optional)
#' var_created_in_last_n_days <- 30 # integer | Get metrics for pins created in the last \"n\" days. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get user account top pins analytics
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserAccountAnalyticsTopPins(var_start_date, var_end_date, var_sort_by, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, num_of_pins = var_num_of_pins, created_in_last_n_days = var_created_in_last_n_days, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$UserAccountAnalyticsTopPins(var_start_date, var_end_date, var_sort_by, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, num_of_pins = var_num_of_pins, created_in_last_n_days = var_created_in_last_n_days, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  UserAccountAnalyticsTopVideoPins  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_sort_by <- "sort_by_example" # character | Specify sorting order for video metrics
#' var_from_claimed_content <- "BOTH" # character | Filter on Pins that match your claimed domain. (Optional)
#' var_pin_format <- "ALL" # character | Pin formats to get data for, default is all. (Optional)
#' var_app_types <- "ALL" # character | Apps or devices to get data for, default is all. (Optional)
#' var_content_type <- "ALL" # character | Filter to paid or organic data. Default is all. (Optional)
#' var_source <- "ALL" # character | Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (Optional)
#' var_metric_types <- c("IMPRESSION") # array[character] | Metric types to get video data for, default is all.  (Optional)
#' var_num_of_pins <- 10 # integer | Number of pins to include, default is 10. Max is 50. (Optional)
#' var_created_in_last_n_days <- 30 # integer | Get metrics for pins created in the last \"n\" days. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get user account top video pins analytics
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserAccountAnalyticsTopVideoPins(var_start_date, var_end_date, var_sort_by, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, num_of_pins = var_num_of_pins, created_in_last_n_days = var_created_in_last_n_days, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$UserAccountAnalyticsTopVideoPins(var_start_date, var_end_date, var_sort_by, from_claimed_content = var_from_claimed_content, pin_format = var_pin_format, app_types = var_app_types, content_type = var_content_type, source = var_source, metric_types = var_metric_types, num_of_pins = var_num_of_pins, created_in_last_n_days = var_created_in_last_n_days, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  UserAccountFollowedInterests  ####################
#'
#' library(openapi)
#' var_username <- "username" # character | A valid username
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #List following interests
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserAccountFollowedInterests(var_username, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$UserAccountFollowedInterests(var_username, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  UserAccountGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get user account
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserAccountGet(ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$UserAccountGet(ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  UserFollowingGet  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_feed_type <- UserFollowingFeedType$new() # UserFollowingFeedType | Thrift param specifying what type of followees will be kept. Default to include all followees. (Optional)
#' var_explicit_following <- FALSE # character | Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (Optional)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #List following
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserFollowingGet(bookmark = var_bookmark, page_size = var_page_size, feed_type = var_feed_type, explicit_following = var_explicit_following, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$UserFollowingGet(bookmark = var_bookmark, page_size = var_page_size, feed_type = var_feed_type, explicit_following = var_explicit_following, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  UserWebsitesGet  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get user websites
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UserWebsitesGet(bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$UserWebsitesGet(bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  VerifyWebsiteUpdate  ####################
#'
#' library(openapi)
#' var_user_website_verify_request <- UserWebsiteVerifyRequest$new("website_example", "FILENAME") # UserWebsiteVerifyRequest | Verify a website.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Verify website
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$VerifyWebsiteUpdate(var_user_website_verify_request, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$VerifyWebsiteUpdate(var_user_website_verify_request, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  WebsiteVerificationGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get user verification code for website claiming
#' api_instance <- UserAccountApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$WebsiteVerificationGet(ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$WebsiteVerificationGet(ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
UserAccountApi <- R6::R6Class(
  "UserAccountApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new UserAccountApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' List following boards
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param explicit_following (optional) Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (default value: FALSE)
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return BoardsUserFollowsList200Response
    BoardsUserFollowsList = function(bookmark = NULL, page_size = 25, explicit_following = FALSE, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$BoardsUserFollowsListWithHttpInfo(bookmark, page_size, explicit_following, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List following boards
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param explicit_following (optional) Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (default value: FALSE)
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (BoardsUserFollowsList200Response) with additional information such as HTTP status code, headers
    BoardsUserFollowsListWithHttpInfo = function(bookmark = NULL, page_size = 25, explicit_following = FALSE, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling UserAccountApi$BoardsUserFollowsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling UserAccountApi$BoardsUserFollowsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling UserAccountApi$BoardsUserFollowsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling UserAccountApi$BoardsUserFollowsList, must be bigger than or equal to 1.")
      }

      if (!missing(`explicit_following`) && is.null(`explicit_following`)) {
        stop("Invalid value for `explicit_following` when calling UserAccountApi$BoardsUserFollowsList, `explicit_following` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$BoardsUserFollowsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$BoardsUserFollowsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$BoardsUserFollowsList, must conform to the pattern ^\\d+$.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["explicit_following"]] <- `explicit_following`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/following/boards"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BoardsUserFollowsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Follow user
    #'
    #' @param username A valid username
    #' @param follow_user_request Follow a user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserSummary
    FollowUserUpdate = function(username, follow_user_request, data_file = NULL, ...) {
      local_var_response <- self$FollowUserUpdateWithHttpInfo(username, follow_user_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Follow user
    #'
    #' @param username A valid username
    #' @param follow_user_request Follow a user.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserSummary) with additional information such as HTTP status code, headers
    FollowUserUpdateWithHttpInfo = function(username, follow_user_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (missing(`follow_user_request`)) {
        stop("Missing required parameter `follow_user_request`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling UserAccountApi$FollowUserUpdate, `username` is not nullable")
      }
      if (!is.null(`username`) && !stringr::str_detect(`username`, "(?!^\\d+$)^.+$")) {
        stop("Invalid value for `username` when calling UserAccountApi$FollowUserUpdate, must conform to the pattern (?!^\\d+$)^.+$.")
      }

      if (!missing(`follow_user_request`) && is.null(`follow_user_request`)) {
        stop("Invalid value for `follow_user_request` when calling UserAccountApi$FollowUserUpdate, `follow_user_request` is not nullable")
      }

      if (!is.null(`follow_user_request`)) {
        local_var_body <- `follow_user_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/user_account/following/{username}"
      if (!missing(`username`)) {
        local_var_url_path <- gsub("\\{username\\}", URLencode(as.character(`username`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserSummary"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List followers
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return FollowersList200Response
    FollowersList = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$FollowersListWithHttpInfo(bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List followers
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (FollowersList200Response) with additional information such as HTTP status code, headers
    FollowersListWithHttpInfo = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling UserAccountApi$FollowersList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling UserAccountApi$FollowersList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling UserAccountApi$FollowersList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling UserAccountApi$FollowersList, must be bigger than or equal to 1.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/user_account/followers"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FollowersList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List linked businesses
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[LinkedBusiness]
    LinkedBusinessAccountsGet = function(data_file = NULL, ...) {
      local_var_response <- self$LinkedBusinessAccountsGetWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List linked businesses
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[LinkedBusiness]) with additional information such as HTTP status code, headers
    LinkedBusinessAccountsGetWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/user_account/businesses"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[LinkedBusiness]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Unverify website
    #'
    #' @param website Website with path or domain only
    #' @param ... Other optional arguments
    #'
    #' @return void
    UnverifyWebsiteDelete = function(website, ...) {
      local_var_response <- self$UnverifyWebsiteDeleteWithHttpInfo(website, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Unverify website
    #'
    #' @param website Website with path or domain only
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UnverifyWebsiteDeleteWithHttpInfo = function(website, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`website`)) {
        stop("Missing required parameter `website`.")
      }

      if (!missing(`website`) && is.null(`website`)) {
        stop("Invalid value for `website` when calling UserAccountApi$UnverifyWebsiteDelete, `website` is not nullable")
      }

      query_params[["website"]] <- `website`

      local_var_url_path <- "/user_account/websites"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user account analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get data for, default is all. 
    #' @param split_field (optional) How to split the data into groups. Not including this param means data won't be split. (default value: "NO_SPLIT")
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return map(AnalyticsMetricsResponse)
    UserAccountAnalytics = function(start_date, end_date, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, split_field = "NO_SPLIT", ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$UserAccountAnalyticsWithHttpInfo(start_date, end_date, from_claimed_content, pin_format, app_types, content_type, source, metric_types, split_field, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user account analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get data for, default is all. 
    #' @param split_field (optional) How to split the data into groups. Not including this param means data won't be split. (default value: "NO_SPLIT")
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (map(AnalyticsMetricsResponse)) with additional information such as HTTP status code, headers
    UserAccountAnalyticsWithHttpInfo = function(start_date, end_date, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, split_field = "NO_SPLIT", ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling UserAccountApi$UserAccountAnalytics, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling UserAccountApi$UserAccountAnalytics, `end_date` is not nullable")
      }

      if (!missing(`from_claimed_content`) && is.null(`from_claimed_content`)) {
        stop("Invalid value for `from_claimed_content` when calling UserAccountApi$UserAccountAnalytics, `from_claimed_content` is not nullable")
      }

      if (!missing(`pin_format`) && is.null(`pin_format`)) {
        stop("Invalid value for `pin_format` when calling UserAccountApi$UserAccountAnalytics, `pin_format` is not nullable")
      }

      if (!missing(`app_types`) && is.null(`app_types`)) {
        stop("Invalid value for `app_types` when calling UserAccountApi$UserAccountAnalytics, `app_types` is not nullable")
      }

      if (!missing(`content_type`) && is.null(`content_type`)) {
        stop("Invalid value for `content_type` when calling UserAccountApi$UserAccountAnalytics, `content_type` is not nullable")
      }

      if (!missing(`source`) && is.null(`source`)) {
        stop("Invalid value for `source` when calling UserAccountApi$UserAccountAnalytics, `source` is not nullable")
      }

      if (!missing(`metric_types`) && is.null(`metric_types`)) {
        stop("Invalid value for `metric_types` when calling UserAccountApi$UserAccountAnalytics, `metric_types` is not nullable")
      }

      if (!missing(`split_field`) && is.null(`split_field`)) {
        stop("Invalid value for `split_field` when calling UserAccountApi$UserAccountAnalytics, `split_field` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalytics, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$UserAccountAnalytics, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalytics, must conform to the pattern ^\\d+$.")
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      if (!is.null(`from_claimed_content`) && !(`from_claimed_content` %in% c("OTHER", "CLAIMED", "BOTH"))) {
        stop("Invalid value for from_claimed_content when calling UserAccountApi$UserAccountAnalytics. Must be [OTHER, CLAIMED, BOTH].")
      }
      query_params[["from_claimed_content"]] <- `from_claimed_content`

      if (!is.null(`pin_format`) && !(`pin_format` %in% c("ALL", "ORGANIC_IMAGE", "ORGANIC_PRODUCT", "ORGANIC_VIDEO", "ADS_STANDARD", "ADS_PRODUCT", "ADS_VIDEO", "ADS_IDEA"))) {
        stop("Invalid value for pin_format when calling UserAccountApi$UserAccountAnalytics. Must be [ALL, ORGANIC_IMAGE, ORGANIC_PRODUCT, ORGANIC_VIDEO, ADS_STANDARD, ADS_PRODUCT, ADS_VIDEO, ADS_IDEA].")
      }
      query_params[["pin_format"]] <- `pin_format`

      if (!is.null(`app_types`) && !(`app_types` %in% c("ALL", "MOBILE", "TABLET", "WEB"))) {
        stop("Invalid value for app_types when calling UserAccountApi$UserAccountAnalytics. Must be [ALL, MOBILE, TABLET, WEB].")
      }
      query_params[["app_types"]] <- `app_types`

      if (!is.null(`content_type`) && !(`content_type` %in% c("ALL", "PAID", "ORGANIC"))) {
        stop("Invalid value for content_type when calling UserAccountApi$UserAccountAnalytics. Must be [ALL, PAID, ORGANIC].")
      }
      query_params[["content_type"]] <- `content_type`

      if (!is.null(`source`) && !(`source` %in% c("ALL", "YOUR_PINS", "OTHER_PINS"))) {
        stop("Invalid value for source when calling UserAccountApi$UserAccountAnalytics. Must be [ALL, YOUR_PINS, OTHER_PINS].")
      }
      query_params[["source"]] <- `source`

      # no explore
      # validate enum values
      for (query_item in `metric_types`) {
        if (!is.null(query_item) && !(query_item %in% c("ENGAGEMENT", "ENGAGEMENT_RATE", "IMPRESSION", "OUTBOUND_CLICK", "OUTBOUND_CLICK_RATE", "PIN_CLICK", "PIN_CLICK_RATE", "SAVE", "SAVE_RATE"))) {
          stop("Invalid value for metric_types when calling UserAccountApi$UserAccountAnalytics. Must be [ENGAGEMENT, ENGAGEMENT_RATE, IMPRESSION, OUTBOUND_CLICK, OUTBOUND_CLICK_RATE, PIN_CLICK, PIN_CLICK_RATE, SAVE, SAVE_RATE].")
        }
      }
      query_params[["metric_types"]] <- I(paste(lapply(`metric_types`, URLencode, reserved = TRUE), collapse = ","))

      if (!is.null(`split_field`) && !(`split_field` %in% c("NO_SPLIT", "APP_TYPE", "OWNED_CONTENT", "SOURCE", "PIN_FORMAT"))) {
        stop("Invalid value for split_field when calling UserAccountApi$UserAccountAnalytics. Must be [NO_SPLIT, APP_TYPE, OWNED_CONTENT, SOURCE, PIN_FORMAT].")
      }
      query_params[["split_field"]] <- `split_field`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/analytics"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "map(AnalyticsMetricsResponse)"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user account top pins analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param sort_by Specify sorting order for metrics
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get data for, default is all. 
    #' @param num_of_pins (optional) Number of pins to include, default is 10. Max is 50. (default value: 10)
    #' @param created_in_last_n_days (optional) Get metrics for pins created in the last \"n\" days.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return TopPinsAnalyticsResponse
    UserAccountAnalyticsTopPins = function(start_date, end_date, sort_by, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, num_of_pins = 10, created_in_last_n_days = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$UserAccountAnalyticsTopPinsWithHttpInfo(start_date, end_date, sort_by, from_claimed_content, pin_format, app_types, content_type, source, metric_types, num_of_pins, created_in_last_n_days, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user account top pins analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param sort_by Specify sorting order for metrics
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get data for, default is all. 
    #' @param num_of_pins (optional) Number of pins to include, default is 10. Max is 50. (default value: 10)
    #' @param created_in_last_n_days (optional) Get metrics for pins created in the last \"n\" days.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (TopPinsAnalyticsResponse) with additional information such as HTTP status code, headers
    UserAccountAnalyticsTopPinsWithHttpInfo = function(start_date, end_date, sort_by, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, num_of_pins = 10, created_in_last_n_days = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`sort_by`)) {
        stop("Missing required parameter `sort_by`.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling UserAccountApi$UserAccountAnalyticsTopPins, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling UserAccountApi$UserAccountAnalyticsTopPins, `end_date` is not nullable")
      }

      if (!missing(`sort_by`) && is.null(`sort_by`)) {
        stop("Invalid value for `sort_by` when calling UserAccountApi$UserAccountAnalyticsTopPins, `sort_by` is not nullable")
      }

      if (!missing(`from_claimed_content`) && is.null(`from_claimed_content`)) {
        stop("Invalid value for `from_claimed_content` when calling UserAccountApi$UserAccountAnalyticsTopPins, `from_claimed_content` is not nullable")
      }

      if (!missing(`pin_format`) && is.null(`pin_format`)) {
        stop("Invalid value for `pin_format` when calling UserAccountApi$UserAccountAnalyticsTopPins, `pin_format` is not nullable")
      }

      if (!missing(`app_types`) && is.null(`app_types`)) {
        stop("Invalid value for `app_types` when calling UserAccountApi$UserAccountAnalyticsTopPins, `app_types` is not nullable")
      }

      if (!missing(`content_type`) && is.null(`content_type`)) {
        stop("Invalid value for `content_type` when calling UserAccountApi$UserAccountAnalyticsTopPins, `content_type` is not nullable")
      }

      if (!missing(`source`) && is.null(`source`)) {
        stop("Invalid value for `source` when calling UserAccountApi$UserAccountAnalyticsTopPins, `source` is not nullable")
      }

      if (!missing(`metric_types`) && is.null(`metric_types`)) {
        stop("Invalid value for `metric_types` when calling UserAccountApi$UserAccountAnalyticsTopPins, `metric_types` is not nullable")
      }

      if (!missing(`num_of_pins`) && is.null(`num_of_pins`)) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopPins, `num_of_pins` is not nullable")
      }
      if (!is.null(`num_of_pins`) && `num_of_pins` >  50) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopPins, must be smaller than or equal to 50.")
      }
      if (!is.null(`num_of_pins`) && `num_of_pins` <  1) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopPins, must be bigger than or equal to 1.")
      }

      if (!missing(`created_in_last_n_days`) && is.null(`created_in_last_n_days`)) {
        stop("Invalid value for `created_in_last_n_days` when calling UserAccountApi$UserAccountAnalyticsTopPins, `created_in_last_n_days` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopPins, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopPins, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopPins, must conform to the pattern ^\\d+$.")
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      if (!is.null(`sort_by`) && !(`sort_by` %in% c("ENGAGEMENT", "IMPRESSION", "OUTBOUND_CLICK", "PIN_CLICK", "SAVE"))) {
        stop("Invalid value for sort_by when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ENGAGEMENT, IMPRESSION, OUTBOUND_CLICK, PIN_CLICK, SAVE].")
      }
      query_params[["sort_by"]] <- `sort_by`

      if (!is.null(`from_claimed_content`) && !(`from_claimed_content` %in% c("OTHER", "CLAIMED", "BOTH"))) {
        stop("Invalid value for from_claimed_content when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [OTHER, CLAIMED, BOTH].")
      }
      query_params[["from_claimed_content"]] <- `from_claimed_content`

      if (!is.null(`pin_format`) && !(`pin_format` %in% c("ALL", "ORGANIC_IMAGE", "ORGANIC_PRODUCT", "ORGANIC_VIDEO", "ADS_STANDARD", "ADS_PRODUCT", "ADS_VIDEO", "ADS_IDEA"))) {
        stop("Invalid value for pin_format when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ALL, ORGANIC_IMAGE, ORGANIC_PRODUCT, ORGANIC_VIDEO, ADS_STANDARD, ADS_PRODUCT, ADS_VIDEO, ADS_IDEA].")
      }
      query_params[["pin_format"]] <- `pin_format`

      if (!is.null(`app_types`) && !(`app_types` %in% c("ALL", "MOBILE", "TABLET", "WEB"))) {
        stop("Invalid value for app_types when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ALL, MOBILE, TABLET, WEB].")
      }
      query_params[["app_types"]] <- `app_types`

      if (!is.null(`content_type`) && !(`content_type` %in% c("ALL", "PAID", "ORGANIC"))) {
        stop("Invalid value for content_type when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ALL, PAID, ORGANIC].")
      }
      query_params[["content_type"]] <- `content_type`

      if (!is.null(`source`) && !(`source` %in% c("ALL", "YOUR_PINS", "OTHER_PINS"))) {
        stop("Invalid value for source when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ALL, YOUR_PINS, OTHER_PINS].")
      }
      query_params[["source"]] <- `source`

      # no explore
      # validate enum values
      for (query_item in `metric_types`) {
        if (!is.null(query_item) && !(query_item %in% c("ENGAGEMENT", "ENGAGEMENT_RATE", "IMPRESSION", "OUTBOUND_CLICK", "OUTBOUND_CLICK_RATE", "PIN_CLICK", "PIN_CLICK_RATE", "SAVE", "SAVE_RATE"))) {
          stop("Invalid value for metric_types when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [ENGAGEMENT, ENGAGEMENT_RATE, IMPRESSION, OUTBOUND_CLICK, OUTBOUND_CLICK_RATE, PIN_CLICK, PIN_CLICK_RATE, SAVE, SAVE_RATE].")
        }
      }
      query_params[["metric_types"]] <- I(paste(lapply(`metric_types`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["num_of_pins"]] <- `num_of_pins`

      if (!is.null(`created_in_last_n_days`) && !(`created_in_last_n_days` %in% c("30"))) {
        stop("Invalid value for created_in_last_n_days when calling UserAccountApi$UserAccountAnalyticsTopPins. Must be [30].")
      }
      query_params[["created_in_last_n_days"]] <- `created_in_last_n_days`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/analytics/top_pins"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "pins:read user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "pins:read user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TopPinsAnalyticsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user account top video pins analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param sort_by Specify sorting order for video metrics
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get video data for, default is all. 
    #' @param num_of_pins (optional) Number of pins to include, default is 10. Max is 50. (default value: 10)
    #' @param created_in_last_n_days (optional) Get metrics for pins created in the last \"n\" days.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return TopVideoPinsAnalyticsResponse
    UserAccountAnalyticsTopVideoPins = function(start_date, end_date, sort_by, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, num_of_pins = 10, created_in_last_n_days = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$UserAccountAnalyticsTopVideoPinsWithHttpInfo(start_date, end_date, sort_by, from_claimed_content, pin_format, app_types, content_type, source, metric_types, num_of_pins, created_in_last_n_days, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user account top video pins analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param sort_by Specify sorting order for video metrics
    #' @param from_claimed_content (optional) Filter on Pins that match your claimed domain. (default value: "BOTH")
    #' @param pin_format (optional) Pin formats to get data for, default is all. (default value: "ALL")
    #' @param app_types (optional) Apps or devices to get data for, default is all. (default value: "ALL")
    #' @param content_type (optional) Filter to paid or organic data. Default is all. (default value: "ALL")
    #' @param source (optional) Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts (default value: "ALL")
    #' @param metric_types (optional) Metric types to get video data for, default is all. 
    #' @param num_of_pins (optional) Number of pins to include, default is 10. Max is 50. (default value: 10)
    #' @param created_in_last_n_days (optional) Get metrics for pins created in the last \"n\" days.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (TopVideoPinsAnalyticsResponse) with additional information such as HTTP status code, headers
    UserAccountAnalyticsTopVideoPinsWithHttpInfo = function(start_date, end_date, sort_by, from_claimed_content = "BOTH", pin_format = "ALL", app_types = "ALL", content_type = "ALL", source = "ALL", metric_types = NULL, num_of_pins = 10, created_in_last_n_days = NULL, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`sort_by`)) {
        stop("Missing required parameter `sort_by`.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `end_date` is not nullable")
      }

      if (!missing(`sort_by`) && is.null(`sort_by`)) {
        stop("Invalid value for `sort_by` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `sort_by` is not nullable")
      }

      if (!missing(`from_claimed_content`) && is.null(`from_claimed_content`)) {
        stop("Invalid value for `from_claimed_content` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `from_claimed_content` is not nullable")
      }

      if (!missing(`pin_format`) && is.null(`pin_format`)) {
        stop("Invalid value for `pin_format` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `pin_format` is not nullable")
      }

      if (!missing(`app_types`) && is.null(`app_types`)) {
        stop("Invalid value for `app_types` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `app_types` is not nullable")
      }

      if (!missing(`content_type`) && is.null(`content_type`)) {
        stop("Invalid value for `content_type` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `content_type` is not nullable")
      }

      if (!missing(`source`) && is.null(`source`)) {
        stop("Invalid value for `source` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `source` is not nullable")
      }

      if (!missing(`metric_types`) && is.null(`metric_types`)) {
        stop("Invalid value for `metric_types` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `metric_types` is not nullable")
      }

      if (!missing(`num_of_pins`) && is.null(`num_of_pins`)) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `num_of_pins` is not nullable")
      }
      if (!is.null(`num_of_pins`) && `num_of_pins` >  50) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, must be smaller than or equal to 50.")
      }
      if (!is.null(`num_of_pins`) && `num_of_pins` <  1) {
        stop("Invalid value for `num_of_pins` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, must be bigger than or equal to 1.")
      }

      if (!missing(`created_in_last_n_days`) && is.null(`created_in_last_n_days`)) {
        stop("Invalid value for `created_in_last_n_days` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `created_in_last_n_days` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountAnalyticsTopVideoPins, must conform to the pattern ^\\d+$.")
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      if (!is.null(`sort_by`) && !(`sort_by` %in% c("IMPRESSION", "SAVE", "OUTBOUND_CLICK", "VIDEO_MRC_VIEW", "VIDEO_AVG_WATCH_TIME", "VIDEO_V50_WATCH_TIME", "QUARTILE_95_PERCENT_VIEW", "VIDEO_10S_VIEW", "VIDEO_START"))) {
        stop("Invalid value for sort_by when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [IMPRESSION, SAVE, OUTBOUND_CLICK, VIDEO_MRC_VIEW, VIDEO_AVG_WATCH_TIME, VIDEO_V50_WATCH_TIME, QUARTILE_95_PERCENT_VIEW, VIDEO_10S_VIEW, VIDEO_START].")
      }
      query_params[["sort_by"]] <- `sort_by`

      if (!is.null(`from_claimed_content`) && !(`from_claimed_content` %in% c("OTHER", "CLAIMED", "BOTH"))) {
        stop("Invalid value for from_claimed_content when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [OTHER, CLAIMED, BOTH].")
      }
      query_params[["from_claimed_content"]] <- `from_claimed_content`

      if (!is.null(`pin_format`) && !(`pin_format` %in% c("ALL", "ORGANIC_IMAGE", "ORGANIC_PRODUCT", "ORGANIC_VIDEO", "ADS_STANDARD", "ADS_PRODUCT", "ADS_VIDEO", "ADS_IDEA"))) {
        stop("Invalid value for pin_format when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [ALL, ORGANIC_IMAGE, ORGANIC_PRODUCT, ORGANIC_VIDEO, ADS_STANDARD, ADS_PRODUCT, ADS_VIDEO, ADS_IDEA].")
      }
      query_params[["pin_format"]] <- `pin_format`

      if (!is.null(`app_types`) && !(`app_types` %in% c("ALL", "MOBILE", "TABLET", "WEB"))) {
        stop("Invalid value for app_types when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [ALL, MOBILE, TABLET, WEB].")
      }
      query_params[["app_types"]] <- `app_types`

      if (!is.null(`content_type`) && !(`content_type` %in% c("ALL", "PAID", "ORGANIC"))) {
        stop("Invalid value for content_type when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [ALL, PAID, ORGANIC].")
      }
      query_params[["content_type"]] <- `content_type`

      if (!is.null(`source`) && !(`source` %in% c("ALL", "YOUR_PINS", "OTHER_PINS"))) {
        stop("Invalid value for source when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [ALL, YOUR_PINS, OTHER_PINS].")
      }
      query_params[["source"]] <- `source`

      # no explore
      # validate enum values
      for (query_item in `metric_types`) {
        if (!is.null(query_item) && !(query_item %in% c("IMPRESSION", "SAVE", "VIDEO_MRC_VIEW", "VIDEO_AVG_WATCH_TIME", "VIDEO_V50_WATCH_TIME", "QUARTILE_95_PERCENT_VIEW", "VIDEO_10S_VIEW", "VIDEO_START", "OUTBOUND_CLICK"))) {
          stop("Invalid value for metric_types when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [IMPRESSION, SAVE, VIDEO_MRC_VIEW, VIDEO_AVG_WATCH_TIME, VIDEO_V50_WATCH_TIME, QUARTILE_95_PERCENT_VIEW, VIDEO_10S_VIEW, VIDEO_START, OUTBOUND_CLICK].")
        }
      }
      query_params[["metric_types"]] <- I(paste(lapply(`metric_types`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["num_of_pins"]] <- `num_of_pins`

      if (!is.null(`created_in_last_n_days`) && !(`created_in_last_n_days` %in% c("30"))) {
        stop("Invalid value for created_in_last_n_days when calling UserAccountApi$UserAccountAnalyticsTopVideoPins. Must be [30].")
      }
      query_params[["created_in_last_n_days"]] <- `created_in_last_n_days`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/analytics/top_video_pins"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "pins:read user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "pins:read user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TopVideoPinsAnalyticsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List following interests
    #'
    #' @param username A valid username
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserAccountFollowedInterests200Response
    UserAccountFollowedInterests = function(username, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$UserAccountFollowedInterestsWithHttpInfo(username, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List following interests
    #'
    #' @param username A valid username
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserAccountFollowedInterests200Response) with additional information such as HTTP status code, headers
    UserAccountFollowedInterestsWithHttpInfo = function(username, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`username`)) {
        stop("Missing required parameter `username`.")
      }

      if (!missing(`username`) && is.null(`username`)) {
        stop("Invalid value for `username` when calling UserAccountApi$UserAccountFollowedInterests, `username` is not nullable")
      }
      if (!is.null(`username`) && !stringr::str_detect(`username`, "(?!^\\d+$)^.+$")) {
        stop("Invalid value for `username` when calling UserAccountApi$UserAccountFollowedInterests, must conform to the pattern (?!^\\d+$)^.+$.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling UserAccountApi$UserAccountFollowedInterests, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserAccountFollowedInterests, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserAccountFollowedInterests, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserAccountFollowedInterests, must be bigger than or equal to 1.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/users/{username}/interests/follow"
      if (!missing(`username`)) {
        local_var_url_path <- gsub("\\{username\\}", URLencode(as.character(`username`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserAccountFollowedInterests200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user account
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Account
    UserAccountGet = function(ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$UserAccountGetWithHttpInfo(ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user account
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Account) with additional information such as HTTP status code, headers
    UserAccountGetWithHttpInfo = function(ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$UserAccountGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserAccountGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Account"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List following
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param feed_type (optional) Thrift param specifying what type of followees will be kept. Default to include all followees.
    #' @param explicit_following (optional) Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (default value: FALSE)
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserFollowingGet200Response
    UserFollowingGet = function(bookmark = NULL, page_size = 25, feed_type = NULL, explicit_following = FALSE, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$UserFollowingGetWithHttpInfo(bookmark, page_size, feed_type, explicit_following, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List following
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param feed_type (optional) Thrift param specifying what type of followees will be kept. Default to include all followees.
    #' @param explicit_following (optional) Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows. (default value: FALSE)
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserFollowingGet200Response) with additional information such as HTTP status code, headers
    UserFollowingGetWithHttpInfo = function(bookmark = NULL, page_size = 25, feed_type = NULL, explicit_following = FALSE, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling UserAccountApi$UserFollowingGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserFollowingGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserFollowingGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserFollowingGet, must be bigger than or equal to 1.")
      }

      if (!missing(`feed_type`) && is.null(`feed_type`)) {
        stop("Invalid value for `feed_type` when calling UserAccountApi$UserFollowingGet, `feed_type` is not nullable")
      }

      if (!missing(`explicit_following`) && is.null(`explicit_following`)) {
        stop("Invalid value for `explicit_following` when calling UserAccountApi$UserFollowingGet, `explicit_following` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserFollowingGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$UserFollowingGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$UserFollowingGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["feed_type"]] <- `feed_type`

      query_params[["explicit_following"]] <- `explicit_following`

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/following"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserFollowingGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user websites
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserWebsitesGet200Response
    UserWebsitesGet = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$UserWebsitesGetWithHttpInfo(bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user websites
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserWebsitesGet200Response) with additional information such as HTTP status code, headers
    UserWebsitesGetWithHttpInfo = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling UserAccountApi$UserWebsitesGet, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserWebsitesGet, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserWebsitesGet, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling UserAccountApi$UserWebsitesGet, must be bigger than or equal to 1.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/user_account/websites"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserWebsitesGet200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Verify website
    #'
    #' @param user_website_verify_request Verify a website.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserWebsiteSummary
    VerifyWebsiteUpdate = function(user_website_verify_request, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$VerifyWebsiteUpdateWithHttpInfo(user_website_verify_request, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Verify website
    #'
    #' @param user_website_verify_request Verify a website.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserWebsiteSummary) with additional information such as HTTP status code, headers
    VerifyWebsiteUpdateWithHttpInfo = function(user_website_verify_request, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_website_verify_request`)) {
        stop("Missing required parameter `user_website_verify_request`.")
      }

      if (!missing(`user_website_verify_request`) && is.null(`user_website_verify_request`)) {
        stop("Invalid value for `user_website_verify_request` when calling UserAccountApi$VerifyWebsiteUpdate, `user_website_verify_request` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$VerifyWebsiteUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$VerifyWebsiteUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$VerifyWebsiteUpdate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`user_website_verify_request`)) {
        local_var_body <- `user_website_verify_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/user_account/websites"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserWebsiteSummary"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get user verification code for website claiming
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UserWebsiteVerificationCode
    WebsiteVerificationGet = function(ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$WebsiteVerificationGetWithHttpInfo(ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get user verification code for website claiming
    #'
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UserWebsiteVerificationCode) with additional information such as HTTP status code, headers
    WebsiteVerificationGetWithHttpInfo = function(ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$WebsiteVerificationGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling UserAccountApi$WebsiteVerificationGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling UserAccountApi$WebsiteVerificationGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      local_var_url_path <- "/user_account/websites/verification"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "user_accounts:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "UserWebsiteVerificationCode"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
