#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AdGroups operations
#' @description AdGroupsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdGroupsAnalytics  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_ad_group_ids <- c("inner_example") # array[character] | List of Ad group Ids to use to filter the results.
#' var_columns <- c(ReportingColumnSync$new()) # array[ReportingColumnSync] | Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
#' var_granularity <- Granularity$new() # Granularity |   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_click_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (Optional)
#' var_engagement_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (Optional)
#' var_view_window_days <- 1 # numeric | Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (Optional)
#' var_conversion_report_time <- "TIME_OF_AD_ACTION" # character | The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (Optional)
#' var_aggregate_report_rows <- FALSE # character | Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (Optional)
#' var_reporting_timezone <- ReportingTimeZone$new() # ReportingTimeZone | Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users. (Optional)
#'
#' #Get ad group analytics
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsAnalytics(var_start_date, var_end_date, var_ad_group_ids, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, aggregate_report_rows = var_aggregate_report_rows, reporting_timezone = var_reporting_timezonedata_file = "result.txt")
#' result <- api_instance$AdGroupsAnalytics(var_start_date, var_end_date, var_ad_group_ids, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, aggregate_report_rows = var_aggregate_report_rows, reporting_timezone = var_reporting_timezone)
#' dput(result)
#'
#'
#' ####################  AdGroupsAudienceSizing  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_audience_sizing_create <- AdGroupAudienceSizingCreate$new("auto_targeting_enabled_example", c(AdGroupAudienceSizingCreativeTypes$new()), c(AdGroupAudienceSizingKeyword$new(MatchType$new(), "value_example")), AdgroupPlacementGroupType$new(), c("product_group_ids_example"), TargetingSpecOptimal$new(c(TargetingSpecAgeBucket$new()), c(TargetingSpecAppType$new()), c("AUDIENCE_EXCLUDE_example"), c("AUDIENCE_INCLUDE_example"), c(TargetingSpecGender$new()), c("GEO_example"), c("GEO_EXCLUDE_example"), c("INTEREST_example"), c("LOCALE_example"), c("LOCATION_example"), c("LOCATION_EXCLUDE_example"), "MAXIMUM_AGE_example", "MINIMUM_AGE_example", c(TargetingSpecShoppingRetargeting$new(123, 123, c(123))), c(TargetingStrategy$new()))) # AdGroupAudienceSizingCreate | 
#'
#' #Get audience sizing
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsAudienceSizing(var_ad_account_id, var_ad_group_audience_sizing_createdata_file = "result.txt")
#' result <- api_instance$AdGroupsAudienceSizing(var_ad_account_id, var_ad_group_audience_sizing_create)
#' dput(result)
#'
#'
#' ####################  AdGroupsBidFloorGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_bid_floor_create <- BidFloorCreate$new(c(BidFloorSpec$new(ActionType$new(), Currency$new(), c(Country$new()), CreativeType$new(), BidFloorObjectiveType$new(), OptimizationGoalMetadata$new(ConversionTagV3GoalMetadata$new(AttributionWindows$new(123, 123, 123), ConversionEvent$new(), "conversion_tag_id_example", "cpa_goal_value_in_micro_currency_example", "is_roas_optimized_example", "reporting_event_example"), FrequencyGoalMetadata$new(123, FrequencyGoalMetadataTimerange$new()), ScrollupGoalMetadata$new("scrollup_goal_value_in_micro_currency_example")))), TargetingSpecOptimal$new(c(TargetingSpecAgeBucket$new()), c(TargetingSpecAppType$new()), c("AUDIENCE_EXCLUDE_example"), c("AUDIENCE_INCLUDE_example"), c(TargetingSpecGender$new()), c("GEO_example"), c("GEO_EXCLUDE_example"), c("INTEREST_example"), c("LOCALE_example"), c("LOCATION_example"), c("LOCATION_EXCLUDE_example"), "MAXIMUM_AGE_example", "MINIMUM_AGE_example", c(TargetingSpecShoppingRetargeting$new(123, 123, c(123))), c(TargetingStrategy$new()))) # BidFloorCreate | 
#'
#' #Get bid floors
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsBidFloorGet(var_ad_account_id, var_bid_floor_createdata_file = "result.txt")
#' result <- api_instance$AdGroupsBidFloorGet(var_ad_account_id, var_bid_floor_create)
#' dput(result)
#'
#'
#' ####################  AdGroupsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_create_create <- c(AdGroupCreateCreate$new(ActionType$new(), "campaign_id_example", "name_example", "auto_targeting_enabled_example", 123, 123, BidStrategyType$new(), 123, BudgetType$new(), "customer_segment_id_example", 123, AdgroupTrackingFeatures$new(c(AdgroupTrackingFeatureType$new())), "feed_profile_id_example", "is_creative_optimization_example", "is_local_inventory_example", 123, 123, NullableOptimizationGoalMetadata$new(ConversionTagV3GoalMetadata$new(AttributionWindows$new(123, 123, 123), ConversionEvent$new(), "conversion_tag_id_example", "cpa_goal_value_in_micro_currency_example", "is_roas_optimized_example", "reporting_event_example"), FrequencyGoalMetadata$new(123, FrequencyGoalMetadataTimerange$new()), ScrollupGoalMetadata$new("scrollup_goal_value_in_micro_currency_example")), PacingDeliveryType$new(), PerformancePlusCampaignSettings$new("boost_prospecting_ad_group_bid_example", c("pinner_list_exclusions_example")), PlacementGroupType$new(), PlacementTrafficType$new(), PromotionApplicationLevel$new(), "promotion_id_example", c("promotion_ids_example"), 123, EntityStatus$new(), TargetingSpecOptimal$new(c(TargetingSpecAgeBucket$new()), c(TargetingSpecAppType$new()), c("AUDIENCE_EXCLUDE_example"), c("AUDIENCE_INCLUDE_example"), c(TargetingSpecGender$new()), c("GEO_example"), c("GEO_EXCLUDE_example"), c("INTEREST_example"), c("LOCALE_example"), c("LOCATION_example"), c("LOCATION_EXCLUDE_example"), "MAXIMUM_AGE_example", "MINIMUM_AGE_example", c(TargetingSpecShoppingRetargeting$new(123, 123, c(123))), c(TargetingStrategy$new())), c("targeting_template_ids_example"), AdGroupTrackingURLs$new(c("audience_verification_example"), c("buyable_button_example"), c("click_example"), c("engagement_example"), c("impression_example")))) # array[AdGroupCreateCreate] | 
#'
#' #Create ad groups
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsCreate(var_ad_account_id, var_ad_group_create_createdata_file = "result.txt")
#' result <- api_instance$AdGroupsCreate(var_ad_account_id, var_ad_group_create_create)
#' dput(result)
#'
#'
#' ####################  AdGroupsDynamicTitlesDownloadCsv  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_id <- "ad_group_id_example" # character | Ad group ID.
#'
#' #Get dynamic titles CSV download URL
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsDynamicTitlesDownloadCsv(var_ad_account_id, var_ad_group_iddata_file = "result.txt")
#' result <- api_instance$AdGroupsDynamicTitlesDownloadCsv(var_ad_account_id, var_ad_group_id)
#' dput(result)
#'
#'
#' ####################  AdGroupsDynamicTitlesGetStatus  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_id <- "ad_group_id_example" # character | Ad group ID.
#'
#' #Get dynamic titles status
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsDynamicTitlesGetStatus(var_ad_account_id, var_ad_group_iddata_file = "result.txt")
#' result <- api_instance$AdGroupsDynamicTitlesGetStatus(var_ad_account_id, var_ad_group_id)
#' dput(result)
#'
#'
#' ####################  AdGroupsDynamicTitlesGetUploadUrl  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_id <- "ad_group_id_example" # character | Ad group ID.
#'
#' #Get dynamic titles upload URL
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsDynamicTitlesGetUploadUrl(var_ad_account_id, var_ad_group_iddata_file = "result.txt")
#' result <- api_instance$AdGroupsDynamicTitlesGetUploadUrl(var_ad_account_id, var_ad_group_id)
#' dput(result)
#'
#'
#' ####################  AdGroupsDynamicTitlesProcessCsv  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_id <- "ad_group_id_example" # character | Ad group ID.
#' var_dynamic_titles_process_csv_create <- DynamicTitlesProcessCSVCreate$new("request_id_example") # DynamicTitlesProcessCSVCreate | 
#'
#' #Process dynamic titles CSV
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsDynamicTitlesProcessCsv(var_ad_account_id, var_ad_group_id, var_dynamic_titles_process_csv_createdata_file = "result.txt")
#' result <- api_instance$AdGroupsDynamicTitlesProcessCsv(var_ad_account_id, var_ad_group_id, var_dynamic_titles_process_csv_create)
#' dput(result)
#'
#'
#' ####################  AdGroupsGet  ####################
#'
#' library(openapi)
#' var_ad_group_id <- "ad_group_id_example" # character | Ad group ID.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#'
#' #Get ad group
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsGet(var_ad_group_id, var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$AdGroupsGet(var_ad_group_id, var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  AdGroupsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#' var_order <- Pinterest.Lib.PaginationOrder$new() # PinterestLibPaginationOrder | The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_campaign_ids <- c("inner_example") # array[character] | List of Campaign Ids to use to filter the results. (Optional)
#' var_ad_group_ids <- c("inner_example") # array[character] | List of Ad group Ids to retrieve keywords from. This feature is currently in BETA and is not available to all users. (Optional)
#' var_entity_statuses <- c(EntityStatus$new()) # array[EntityStatus] | Entity status (Optional)
#' var_translate_interests_to_names <- FALSE # character | Return interests as text names (if value is true) rather than topic IDs. (Optional)
#'
#' #List ad groups
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsList(var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, order = var_order, campaign_ids = var_campaign_ids, ad_group_ids = var_ad_group_ids, entity_statuses = var_entity_statuses, translate_interests_to_names = var_translate_interests_to_namesdata_file = "result.txt")
#' result <- api_instance$AdGroupsList(var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, order = var_order, campaign_ids = var_campaign_ids, ad_group_ids = var_ad_group_ids, entity_statuses = var_entity_statuses, translate_interests_to_names = var_translate_interests_to_names)
#' dput(result)
#'
#'
#' ####################  AdGroupsTargetingAnalyticsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_ids <- c("inner_example") # array[character] | List of Ad group Ids to use to filter the results.
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_targeting_types <- c(AdsAnalyticsAdGroupTargetingType$new()) # array[AdsAnalyticsAdGroupTargetingType] | Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\", \"CREATIVE_ENHANCEMENTS\"] are in BETA and not yet available to all users.
#' var_columns <- c(ReportingColumnSync$new()) # array[ReportingColumnSync] | Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
#' var_granularity <- Granularity$new() # Granularity |   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
#' var_click_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (Optional)
#' var_engagement_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (Optional)
#' var_view_window_days <- 1 # numeric | Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (Optional)
#' var_conversion_report_time <- "TIME_OF_AD_ACTION" # character | The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (Optional)
#' var_attribution_types <- c(ConversionReportAttributionType$new()) # array[ConversionReportAttributionType] | List of types of attribution for the conversion report (Optional)
#' var_reporting_timezone <- ReportingTimeZone$new() # ReportingTimeZone | Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users. (Optional)
#' var_sort_columns <- c("inner_example") # array[character] | Sort Columns. (Optional)
#' var_sort_ascending <- "sort_ascending_example" # character | Sort ascending. (Optional)
#'
#' #Get targeting analytics for ad groups
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsTargetingAnalyticsGet(var_ad_account_id, var_ad_group_ids, var_start_date, var_end_date, var_targeting_types, var_columns, var_granularity, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, attribution_types = var_attribution_types, reporting_timezone = var_reporting_timezone, sort_columns = var_sort_columns, sort_ascending = var_sort_ascendingdata_file = "result.txt")
#' result <- api_instance$AdGroupsTargetingAnalyticsGet(var_ad_account_id, var_ad_group_ids, var_start_date, var_end_date, var_targeting_types, var_columns, var_granularity, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, attribution_types = var_attribution_types, reporting_timezone = var_reporting_timezone, sort_columns = var_sort_columns, sort_ascending = var_sort_ascending)
#' dput(result)
#'
#'
#' ####################  AdGroupsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_group_update_batch_update <- c(AdGroupUpdateBatchUpdate$new("id_example", "auto_targeting_enabled_example", 123, 123, BidStrategyType$new(), ActionType$new(), 123, BudgetType$new(), "campaign_id_example", "customer_segment_id_example", 123, AdgroupTrackingFeatures$new(c(AdgroupTrackingFeatureType$new())), "feed_profile_id_example", "is_creative_optimization_example", "is_local_inventory_example", 123, 123, "name_example", NullableOptimizationGoalMetadata$new(ConversionTagV3GoalMetadata$new(AttributionWindows$new(123, 123, 123), ConversionEvent$new(), "conversion_tag_id_example", "cpa_goal_value_in_micro_currency_example", "is_roas_optimized_example", "reporting_event_example"), FrequencyGoalMetadata$new(123, FrequencyGoalMetadataTimerange$new()), ScrollupGoalMetadata$new("scrollup_goal_value_in_micro_currency_example")), PacingDeliveryType$new(), PerformancePlusCampaignSettings$new("boost_prospecting_ad_group_bid_example", c("pinner_list_exclusions_example")), PlacementGroupType$new(), PlacementTrafficType$new(), PromotionApplicationLevel$new(), "promotion_id_example", c("promotion_ids_example"), 123, EntityStatus$new(), TargetingSpecOptimal$new(c(TargetingSpecAgeBucket$new()), c(TargetingSpecAppType$new()), c("AUDIENCE_EXCLUDE_example"), c("AUDIENCE_INCLUDE_example"), c(TargetingSpecGender$new()), c("GEO_example"), c("GEO_EXCLUDE_example"), c("INTEREST_example"), c("LOCALE_example"), c("LOCATION_example"), c("LOCATION_EXCLUDE_example"), "MAXIMUM_AGE_example", "MINIMUM_AGE_example", c(TargetingSpecShoppingRetargeting$new(123, 123, c(123))), c(TargetingStrategy$new())), c(TargetingSpecOperations$new("MINIMUM_AGE", "SET", c(TargetingSpecShoppingRetargeting$new(123, 123, c(123))), "value_example")), c("targeting_template_ids_example"), AdGroupTrackingURLs$new(c("audience_verification_example"), c("buyable_button_example"), c("click_example"), c("engagement_example"), c("impression_example")))) # array[AdGroupUpdateBatchUpdate] | 
#'
#' #Update ad groups
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdGroupsUpdate(var_ad_account_id, var_ad_group_update_batch_updatedata_file = "result.txt")
#' result <- api_instance$AdGroupsUpdate(var_ad_account_id, var_ad_group_update_batch_update)
#' dput(result)
#'
#'
#' ####################  GetAdGroupsByPromotionIdsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_promotion_ids <- c("inner_example") # array[character] | List of Promotion IDs to use to filter the results.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#' var_order <- Pinterest.Lib.PaginationOrder$new() # PinterestLibPaginationOrder | The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#'
#' #List of ad groups using promotions IDs.
#' api_instance <- AdGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAdGroupsByPromotionIdsList(var_ad_account_id, var_promotion_ids, bookmark = var_bookmark, page_size = var_page_size, order = var_orderdata_file = "result.txt")
#' result <- api_instance$GetAdGroupsByPromotionIdsList(var_ad_account_id, var_promotion_ids, bookmark = var_bookmark, page_size = var_page_size, order = var_order)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AdGroupsApi <- R6::R6Class(
  "AdGroupsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AdGroupsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get ad group analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param ad_group_ids List of Ad group Ids to use to filter the results.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param aggregate_report_rows (optional) Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (default value: FALSE)
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AdGroupsAnalyticsMetrics]
    AdGroupsAnalytics = function(start_date, end_date, ad_group_ids, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", aggregate_report_rows = FALSE, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsAnalyticsWithHttpInfo(start_date, end_date, ad_group_ids, columns, granularity, ad_account_id, click_window_days, engagement_window_days, view_window_days, conversion_report_time, aggregate_report_rows, reporting_timezone, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get ad group analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param ad_group_ids List of Ad group Ids to use to filter the results.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param aggregate_report_rows (optional) Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (default value: FALSE)
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AdGroupsAnalyticsMetrics]) with additional information such as HTTP status code, headers
    AdGroupsAnalyticsWithHttpInfo = function(start_date, end_date, ad_group_ids, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", aggregate_report_rows = FALSE, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`ad_group_ids`)) {
        stop("Missing required parameter `ad_group_ids`.")
      }

      if (missing(`columns`)) {
        stop("Missing required parameter `columns`.")
      }

      if (missing(`granularity`)) {
        stop("Missing required parameter `granularity`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling AdGroupsApi$AdGroupsAnalytics, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling AdGroupsApi$AdGroupsAnalytics, `end_date` is not nullable")
      }

      if (!missing(`ad_group_ids`) && is.null(`ad_group_ids`)) {
        stop("Invalid value for `ad_group_ids` when calling AdGroupsApi$AdGroupsAnalytics, `ad_group_ids` is not nullable")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) > 250) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsAnalytics, number of items must be less than or equal to 250.")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) < 1) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsAnalytics, number of items must be greater than or equal to 1.")
      }

      if (!missing(`columns`) && is.null(`columns`)) {
        stop("Invalid value for `columns` when calling AdGroupsApi$AdGroupsAnalytics, `columns` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling AdGroupsApi$AdGroupsAnalytics, `granularity` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsAnalytics, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsAnalytics, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsAnalytics, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`click_window_days`) && is.null(`click_window_days`)) {
        stop("Invalid value for `click_window_days` when calling AdGroupsApi$AdGroupsAnalytics, `click_window_days` is not nullable")
      }

      if (!missing(`engagement_window_days`) && is.null(`engagement_window_days`)) {
        stop("Invalid value for `engagement_window_days` when calling AdGroupsApi$AdGroupsAnalytics, `engagement_window_days` is not nullable")
      }

      if (!missing(`view_window_days`) && is.null(`view_window_days`)) {
        stop("Invalid value for `view_window_days` when calling AdGroupsApi$AdGroupsAnalytics, `view_window_days` is not nullable")
      }

      if (!missing(`conversion_report_time`) && is.null(`conversion_report_time`)) {
        stop("Invalid value for `conversion_report_time` when calling AdGroupsApi$AdGroupsAnalytics, `conversion_report_time` is not nullable")
      }

      if (!missing(`aggregate_report_rows`) && is.null(`aggregate_report_rows`)) {
        stop("Invalid value for `aggregate_report_rows` when calling AdGroupsApi$AdGroupsAnalytics, `aggregate_report_rows` is not nullable")
      }

      if (!missing(`reporting_timezone`) && is.null(`reporting_timezone`)) {
        stop("Invalid value for `reporting_timezone` when calling AdGroupsApi$AdGroupsAnalytics, `reporting_timezone` is not nullable")
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      # explore
      for (query_item in `ad_group_ids`) {
        query_params[["ad_group_ids"]] <- c(query_params[["ad_group_ids"]], list(`ad_group_ids` = query_item))
      }

      # no explore
      query_params[["columns"]] <- I(paste(lapply(`columns`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["granularity"]] <- `granularity`

      if (!is.null(`click_window_days`) && !(`click_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for click_window_days when calling AdGroupsApi$AdGroupsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["click_window_days"]] <- `click_window_days`

      if (!is.null(`engagement_window_days`) && !(`engagement_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for engagement_window_days when calling AdGroupsApi$AdGroupsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["engagement_window_days"]] <- `engagement_window_days`

      if (!is.null(`view_window_days`) && !(`view_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for view_window_days when calling AdGroupsApi$AdGroupsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["view_window_days"]] <- `view_window_days`

      if (!is.null(`conversion_report_time`) && !(`conversion_report_time` %in% c("TIME_OF_AD_ACTION", "TIME_OF_CONVERSION"))) {
        stop("Invalid value for conversion_report_time when calling AdGroupsApi$AdGroupsAnalytics. Must be [TIME_OF_AD_ACTION, TIME_OF_CONVERSION].")
      }
      query_params[["conversion_report_time"]] <- `conversion_report_time`

      query_params[["aggregate_report_rows"]] <- `aggregate_report_rows`

      query_params[["reporting_timezone"]] <- `reporting_timezone`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/analytics"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AdGroupsAnalyticsMetrics]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get audience sizing
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_audience_sizing_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroupAudienceSizing
    AdGroupsAudienceSizing = function(ad_account_id, ad_group_audience_sizing_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsAudienceSizingWithHttpInfo(ad_account_id, ad_group_audience_sizing_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get audience sizing
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_audience_sizing_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroupAudienceSizing) with additional information such as HTTP status code, headers
    AdGroupsAudienceSizingWithHttpInfo = function(ad_account_id, ad_group_audience_sizing_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_audience_sizing_create`)) {
        stop("Missing required parameter `ad_group_audience_sizing_create`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsAudienceSizing, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsAudienceSizing, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsAudienceSizing, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_audience_sizing_create`) && is.null(`ad_group_audience_sizing_create`)) {
        stop("Invalid value for `ad_group_audience_sizing_create` when calling AdGroupsApi$AdGroupsAudienceSizing, `ad_group_audience_sizing_create` is not nullable")
      }

      if (!is.null(`ad_group_audience_sizing_create`)) {
        local_var_body <- `ad_group_audience_sizing_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/audience_sizing"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroupAudienceSizing"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get bid floors
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bid_floor_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BidFloor
    AdGroupsBidFloorGet = function(ad_account_id, bid_floor_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsBidFloorGetWithHttpInfo(ad_account_id, bid_floor_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get bid floors
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bid_floor_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BidFloor) with additional information such as HTTP status code, headers
    AdGroupsBidFloorGetWithHttpInfo = function(ad_account_id, bid_floor_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`bid_floor_create`)) {
        stop("Missing required parameter `bid_floor_create`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsBidFloorGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsBidFloorGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsBidFloorGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`bid_floor_create`) && is.null(`bid_floor_create`)) {
        stop("Invalid value for `bid_floor_create` when calling AdGroupsApi$AdGroupsBidFloorGet, `bid_floor_create` is not nullable")
      }

      if (!is.null(`bid_floor_create`)) {
        local_var_body <- `bid_floor_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/bid_floor"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BidFloor"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_create_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroupsCreate200Response
    AdGroupsCreate = function(ad_account_id, ad_group_create_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsCreateWithHttpInfo(ad_account_id, ad_group_create_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_create_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroupsCreate200Response) with additional information such as HTTP status code, headers
    AdGroupsCreateWithHttpInfo = function(ad_account_id, ad_group_create_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_create_create`)) {
        stop("Missing required parameter `ad_group_create_create`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_create_create`) && is.null(`ad_group_create_create`)) {
        stop("Invalid value for `ad_group_create_create` when calling AdGroupsApi$AdGroupsCreate, `ad_group_create_create` is not nullable")
      }
      if (!is.null(`ad_group_create_create`) && length(`ad_group_create_create`) > 30) {
        stop("Invalid length for `ad_group_create_create` when calling AdGroupsApi$AdGroupsCreate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`ad_group_create_create`) && length(`ad_group_create_create`) < 1) {
        stop("Invalid length for `ad_group_create_create` when calling AdGroupsApi$AdGroupsCreate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`ad_group_create_create`)) {
        body.items <- paste(unlist(lapply(`ad_group_create_create`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroupsCreate200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get dynamic titles CSV download URL
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DynamicTitlesDownloadCSV
    AdGroupsDynamicTitlesDownloadCsv = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsDynamicTitlesDownloadCsvWithHttpInfo(ad_account_id, ad_group_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get dynamic titles CSV download URL
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DynamicTitlesDownloadCSV) with additional information such as HTTP status code, headers
    AdGroupsDynamicTitlesDownloadCsvWithHttpInfo = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_id`)) {
        stop("Missing required parameter `ad_group_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_id`) && is.null(`ad_group_id`)) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, `ad_group_id` is not nullable")
      }
      if (!is.null(`ad_group_id`) && nchar(`ad_group_id`) > 18) {
        stop("Invalid length for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_group_id`) && !stringr::str_detect(`ad_group_id`, "^\\d+$")) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesDownloadCsv, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/csv"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_group_id`)) {
        local_var_url_path <- gsub("\\{ad_group_id\\}", URLencode(as.character(`ad_group_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DynamicTitlesDownloadCSV"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get dynamic titles status
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DynamicTitlesGetStatus
    AdGroupsDynamicTitlesGetStatus = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsDynamicTitlesGetStatusWithHttpInfo(ad_account_id, ad_group_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get dynamic titles status
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DynamicTitlesGetStatus) with additional information such as HTTP status code, headers
    AdGroupsDynamicTitlesGetStatusWithHttpInfo = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_id`)) {
        stop("Missing required parameter `ad_group_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_id`) && is.null(`ad_group_id`)) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, `ad_group_id` is not nullable")
      }
      if (!is.null(`ad_group_id`) && nchar(`ad_group_id`) > 18) {
        stop("Invalid length for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_group_id`) && !stringr::str_detect(`ad_group_id`, "^\\d+$")) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetStatus, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/status"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_group_id`)) {
        local_var_url_path <- gsub("\\{ad_group_id\\}", URLencode(as.character(`ad_group_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DynamicTitlesGetStatus"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get dynamic titles upload URL
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DynamicTitlesUploadURL
    AdGroupsDynamicTitlesGetUploadUrl = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsDynamicTitlesGetUploadUrlWithHttpInfo(ad_account_id, ad_group_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get dynamic titles upload URL
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DynamicTitlesUploadURL) with additional information such as HTTP status code, headers
    AdGroupsDynamicTitlesGetUploadUrlWithHttpInfo = function(ad_account_id, ad_group_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_id`)) {
        stop("Missing required parameter `ad_group_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_id`) && is.null(`ad_group_id`)) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, `ad_group_id` is not nullable")
      }
      if (!is.null(`ad_group_id`) && nchar(`ad_group_id`) > 18) {
        stop("Invalid length for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_group_id`) && !stringr::str_detect(`ad_group_id`, "^\\d+$")) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesGetUploadUrl, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/uploads"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_group_id`)) {
        local_var_url_path <- gsub("\\{ad_group_id\\}", URLencode(as.character(`ad_group_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DynamicTitlesUploadURL"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Process dynamic titles CSV
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param dynamic_titles_process_csv_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return DynamicTitlesProcessCSV
    AdGroupsDynamicTitlesProcessCsv = function(ad_account_id, ad_group_id, dynamic_titles_process_csv_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsDynamicTitlesProcessCsvWithHttpInfo(ad_account_id, ad_group_id, dynamic_titles_process_csv_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Process dynamic titles CSV
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_id Ad group ID.
    #' @param dynamic_titles_process_csv_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (DynamicTitlesProcessCSV) with additional information such as HTTP status code, headers
    AdGroupsDynamicTitlesProcessCsvWithHttpInfo = function(ad_account_id, ad_group_id, dynamic_titles_process_csv_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_id`)) {
        stop("Missing required parameter `ad_group_id`.")
      }

      if (missing(`dynamic_titles_process_csv_create`)) {
        stop("Missing required parameter `dynamic_titles_process_csv_create`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_id`) && is.null(`ad_group_id`)) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, `ad_group_id` is not nullable")
      }
      if (!is.null(`ad_group_id`) && nchar(`ad_group_id`) > 18) {
        stop("Invalid length for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_group_id`) && !stringr::str_detect(`ad_group_id`, "^\\d+$")) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`dynamic_titles_process_csv_create`) && is.null(`dynamic_titles_process_csv_create`)) {
        stop("Invalid value for `dynamic_titles_process_csv_create` when calling AdGroupsApi$AdGroupsDynamicTitlesProcessCsv, `dynamic_titles_process_csv_create` is not nullable")
      }

      if (!is.null(`dynamic_titles_process_csv_create`)) {
        local_var_body <- `dynamic_titles_process_csv_create`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_group_id`)) {
        local_var_url_path <- gsub("\\{ad_group_id\\}", URLencode(as.character(`ad_group_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "DynamicTitlesProcessCSV"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get ad group
    #'
    #' @param ad_group_id Ad group ID.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroup
    AdGroupsGet = function(ad_group_id, ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsGetWithHttpInfo(ad_group_id, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get ad group
    #'
    #' @param ad_group_id Ad group ID.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroup) with additional information such as HTTP status code, headers
    AdGroupsGetWithHttpInfo = function(ad_group_id, ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_group_id`)) {
        stop("Missing required parameter `ad_group_id`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_group_id`) && is.null(`ad_group_id`)) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsGet, `ad_group_id` is not nullable")
      }
      if (!is.null(`ad_group_id`) && nchar(`ad_group_id`) > 18) {
        stop("Invalid length for `ad_group_id` when calling AdGroupsApi$AdGroupsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_group_id`) && !stringr::str_detect(`ad_group_id`, "^\\d+$")) {
        stop("Invalid value for `ad_group_id` when calling AdGroupsApi$AdGroupsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}"
      if (!missing(`ad_group_id`)) {
        local_var_url_path <- gsub("\\{ad_group_id\\}", URLencode(as.character(`ad_group_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroup"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param ad_group_ids (optional) List of Ad group Ids to retrieve keywords from. This feature is currently in BETA and is not available to all users.
    #' @param entity_statuses (optional) Entity status (default value: ["ACTIVE","PAUSED"])
    #' @param translate_interests_to_names (optional) Return interests as text names (if value is true) rather than topic IDs. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroupsList200Response
    AdGroupsList = function(ad_account_id, bookmark = NULL, page_size = 25, order = NULL, campaign_ids = NULL, ad_group_ids = NULL, entity_statuses = ["ACTIVE","PAUSED"], translate_interests_to_names = FALSE, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsListWithHttpInfo(ad_account_id, bookmark, page_size, order, campaign_ids, ad_group_ids, entity_statuses, translate_interests_to_names, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param ad_group_ids (optional) List of Ad group Ids to retrieve keywords from. This feature is currently in BETA and is not available to all users.
    #' @param entity_statuses (optional) Entity status (default value: ["ACTIVE","PAUSED"])
    #' @param translate_interests_to_names (optional) Return interests as text names (if value is true) rather than topic IDs. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroupsList200Response) with additional information such as HTTP status code, headers
    AdGroupsListWithHttpInfo = function(ad_account_id, bookmark = NULL, page_size = 25, order = NULL, campaign_ids = NULL, ad_group_ids = NULL, entity_statuses = ["ACTIVE","PAUSED"], translate_interests_to_names = FALSE, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling AdGroupsApi$AdGroupsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$AdGroupsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$AdGroupsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$AdGroupsList, must be bigger than or equal to 1.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AdGroupsApi$AdGroupsList, `order` is not nullable")
      }

      if (!missing(`campaign_ids`) && is.null(`campaign_ids`)) {
        stop("Invalid value for `campaign_ids` when calling AdGroupsApi$AdGroupsList, `campaign_ids` is not nullable")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) > 250) {
        stop("Invalid length for `campaign_ids` when calling AdGroupsApi$AdGroupsList, number of items must be less than or equal to 250.")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) < 1) {
        stop("Invalid length for `campaign_ids` when calling AdGroupsApi$AdGroupsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`ad_group_ids`) && is.null(`ad_group_ids`)) {
        stop("Invalid value for `ad_group_ids` when calling AdGroupsApi$AdGroupsList, `ad_group_ids` is not nullable")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) > 250) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsList, number of items must be less than or equal to 250.")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) < 1) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`entity_statuses`) && is.null(`entity_statuses`)) {
        stop("Invalid value for `entity_statuses` when calling AdGroupsApi$AdGroupsList, `entity_statuses` is not nullable")
      }

      if (!missing(`translate_interests_to_names`) && is.null(`translate_interests_to_names`)) {
        stop("Invalid value for `translate_interests_to_names` when calling AdGroupsApi$AdGroupsList, `translate_interests_to_names` is not nullable")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["order"]] <- `order`

      # explore
      for (query_item in `campaign_ids`) {
        query_params[["campaign_ids"]] <- c(query_params[["campaign_ids"]], list(`campaign_ids` = query_item))
      }

      # explore
      for (query_item in `ad_group_ids`) {
        query_params[["ad_group_ids"]] <- c(query_params[["ad_group_ids"]], list(`ad_group_ids` = query_item))
      }

      # explore
      for (query_item in `entity_statuses`) {
        query_params[["entity_statuses"]] <- c(query_params[["entity_statuses"]], list(`entity_statuses` = query_item))
      }

      query_params[["translate_interests_to_names"]] <- `translate_interests_to_names`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroupsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get targeting analytics for ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_ids List of Ad group Ids to use to filter the results.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param targeting_types Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\", \"CREATIVE_ENHANCEMENTS\"] are in BETA and not yet available to all users.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param attribution_types (optional) List of types of attribution for the conversion report
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param sort_columns (optional) Sort Columns.
    #' @param sort_ascending (optional) Sort ascending.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MetricsResponse
    AdGroupsTargetingAnalyticsGet = function(ad_account_id, ad_group_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", attribution_types = NULL, reporting_timezone = NULL, sort_columns = NULL, sort_ascending = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsTargetingAnalyticsGetWithHttpInfo(ad_account_id, ad_group_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days, engagement_window_days, view_window_days, conversion_report_time, attribution_types, reporting_timezone, sort_columns, sort_ascending, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get targeting analytics for ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_ids List of Ad group Ids to use to filter the results.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param targeting_types Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\", \"CREATIVE_ENHANCEMENTS\"] are in BETA and not yet available to all users.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param attribution_types (optional) List of types of attribution for the conversion report
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param sort_columns (optional) Sort Columns.
    #' @param sort_ascending (optional) Sort ascending.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MetricsResponse) with additional information such as HTTP status code, headers
    AdGroupsTargetingAnalyticsGetWithHttpInfo = function(ad_account_id, ad_group_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", attribution_types = NULL, reporting_timezone = NULL, sort_columns = NULL, sort_ascending = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_ids`)) {
        stop("Missing required parameter `ad_group_ids`.")
      }

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`targeting_types`)) {
        stop("Missing required parameter `targeting_types`.")
      }

      if (missing(`columns`)) {
        stop("Missing required parameter `columns`.")
      }

      if (missing(`granularity`)) {
        stop("Missing required parameter `granularity`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_ids`) && is.null(`ad_group_ids`)) {
        stop("Invalid value for `ad_group_ids` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `ad_group_ids` is not nullable")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) > 250) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be less than or equal to 250.")
      }
      if (!is.null(`ad_group_ids`) && length(`ad_group_ids`) < 1) {
        stop("Invalid length for `ad_group_ids` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be greater than or equal to 1.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `end_date` is not nullable")
      }

      if (!missing(`targeting_types`) && is.null(`targeting_types`)) {
        stop("Invalid value for `targeting_types` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `targeting_types` is not nullable")
      }
      if (!is.null(`targeting_types`) && length(`targeting_types`) > 14) {
        stop("Invalid length for `targeting_types` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be less than or equal to 14.")
      }
      if (!is.null(`targeting_types`) && length(`targeting_types`) < 1) {
        stop("Invalid length for `targeting_types` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be greater than or equal to 1.")
      }

      if (!missing(`columns`) && is.null(`columns`)) {
        stop("Invalid value for `columns` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `columns` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `granularity` is not nullable")
      }

      if (!missing(`click_window_days`) && is.null(`click_window_days`)) {
        stop("Invalid value for `click_window_days` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `click_window_days` is not nullable")
      }

      if (!missing(`engagement_window_days`) && is.null(`engagement_window_days`)) {
        stop("Invalid value for `engagement_window_days` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `engagement_window_days` is not nullable")
      }

      if (!missing(`view_window_days`) && is.null(`view_window_days`)) {
        stop("Invalid value for `view_window_days` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `view_window_days` is not nullable")
      }

      if (!missing(`conversion_report_time`) && is.null(`conversion_report_time`)) {
        stop("Invalid value for `conversion_report_time` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `conversion_report_time` is not nullable")
      }

      if (!missing(`attribution_types`) && is.null(`attribution_types`)) {
        stop("Invalid value for `attribution_types` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `attribution_types` is not nullable")
      }
      if (!is.null(`attribution_types`) && length(`attribution_types`) > 2) {
        stop("Invalid length for `attribution_types` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be less than or equal to 2.")
      }

      if (!missing(`reporting_timezone`) && is.null(`reporting_timezone`)) {
        stop("Invalid value for `reporting_timezone` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `reporting_timezone` is not nullable")
      }

      if (!missing(`sort_columns`) && is.null(`sort_columns`)) {
        stop("Invalid value for `sort_columns` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `sort_columns` is not nullable")
      }
      if (!is.null(`sort_columns`) && length(`sort_columns`) > 2) {
        stop("Invalid length for `sort_columns` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be less than or equal to 2.")
      }
      if (!is.null(`sort_columns`) && length(`sort_columns`) < 1) {
        stop("Invalid length for `sort_columns` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, number of items must be greater than or equal to 1.")
      }

      if (!missing(`sort_ascending`) && is.null(`sort_ascending`)) {
        stop("Invalid value for `sort_ascending` when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet, `sort_ascending` is not nullable")
      }

      # explore
      for (query_item in `ad_group_ids`) {
        query_params[["ad_group_ids"]] <- c(query_params[["ad_group_ids"]], list(`ad_group_ids` = query_item))
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      # no explore
      query_params[["targeting_types"]] <- I(paste(lapply(`targeting_types`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["columns"]] <- I(paste(lapply(`columns`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["granularity"]] <- `granularity`

      if (!is.null(`click_window_days`) && !(`click_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for click_window_days when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["click_window_days"]] <- `click_window_days`

      if (!is.null(`engagement_window_days`) && !(`engagement_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for engagement_window_days when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["engagement_window_days"]] <- `engagement_window_days`

      if (!is.null(`view_window_days`) && !(`view_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for view_window_days when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["view_window_days"]] <- `view_window_days`

      if (!is.null(`conversion_report_time`) && !(`conversion_report_time` %in% c("TIME_OF_AD_ACTION", "TIME_OF_CONVERSION"))) {
        stop("Invalid value for conversion_report_time when calling AdGroupsApi$AdGroupsTargetingAnalyticsGet. Must be [TIME_OF_AD_ACTION, TIME_OF_CONVERSION].")
      }
      query_params[["conversion_report_time"]] <- `conversion_report_time`

      # no explore
      query_params[["attribution_types"]] <- I(paste(lapply(`attribution_types`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["reporting_timezone"]] <- `reporting_timezone`

      # explore
      for (query_item in `sort_columns`) {
        query_params[["sort_columns"]] <- c(query_params[["sort_columns"]], list(`sort_columns` = query_item))
      }

      query_params[["sort_ascending"]] <- `sort_ascending`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups/targeting_analytics"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MetricsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_update_batch_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroupsCreate200Response
    AdGroupsUpdate = function(ad_account_id, ad_group_update_batch_update, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdGroupsUpdateWithHttpInfo(ad_account_id, ad_group_update_batch_update, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update ad groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_group_update_batch_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroupsCreate200Response) with additional information such as HTTP status code, headers
    AdGroupsUpdateWithHttpInfo = function(ad_account_id, ad_group_update_batch_update, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_group_update_batch_update`)) {
        stop("Missing required parameter `ad_group_update_batch_update`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$AdGroupsUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$AdGroupsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_group_update_batch_update`) && is.null(`ad_group_update_batch_update`)) {
        stop("Invalid value for `ad_group_update_batch_update` when calling AdGroupsApi$AdGroupsUpdate, `ad_group_update_batch_update` is not nullable")
      }
      if (!is.null(`ad_group_update_batch_update`) && length(`ad_group_update_batch_update`) > 30) {
        stop("Invalid length for `ad_group_update_batch_update` when calling AdGroupsApi$AdGroupsUpdate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`ad_group_update_batch_update`) && length(`ad_group_update_batch_update`) < 1) {
        stop("Invalid length for `ad_group_update_batch_update` when calling AdGroupsApi$AdGroupsUpdate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`ad_group_update_batch_update`)) {
        body.items <- paste(unlist(lapply(`ad_group_update_batch_update`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/ad_groups"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroupsCreate200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List of ad groups using promotions IDs.
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_ids List of Promotion IDs to use to filter the results.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdGroupsList200Response
    GetAdGroupsByPromotionIdsList = function(ad_account_id, promotion_ids, bookmark = NULL, page_size = 25, order = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAdGroupsByPromotionIdsListWithHttpInfo(ad_account_id, promotion_ids, bookmark, page_size, order, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List of ad groups using promotions IDs.
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param promotion_ids List of Promotion IDs to use to filter the results.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdGroupsList200Response) with additional information such as HTTP status code, headers
    GetAdGroupsByPromotionIdsListWithHttpInfo = function(ad_account_id, promotion_ids, bookmark = NULL, page_size = 25, order = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`promotion_ids`)) {
        stop("Missing required parameter `promotion_ids`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`promotion_ids`) && is.null(`promotion_ids`)) {
        stop("Invalid value for `promotion_ids` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, `promotion_ids` is not nullable")
      }
      if (!is.null(`promotion_ids`) && length(`promotion_ids`) > 50) {
        stop("Invalid length for `promotion_ids` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, number of items must be less than or equal to 50.")
      }
      if (!is.null(`promotion_ids`) && length(`promotion_ids`) < 1) {
        stop("Invalid length for `promotion_ids` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, must be bigger than or equal to 1.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AdGroupsApi$GetAdGroupsByPromotionIdsList, `order` is not nullable")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["order"]] <- `order`

      # explore
      for (query_item in `promotion_ids`) {
        query_params[["promotion_ids"]] <- c(query_params[["promotion_ids"]], list(`promotion_ids` = query_item))
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/promotion_applied_entities"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdGroupsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
