#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title LeadForms operations
#' @description LeadFormsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  LeadFormGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_lead_form_id <- "1234567890123" # character | Unique identifier of a lead form.
#'
#' #Get lead form by id
#' api_instance <- LeadFormsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LeadFormGet(var_ad_account_id, var_lead_form_iddata_file = "result.txt")
#' result <- api_instance$LeadFormGet(var_ad_account_id, var_lead_form_id)
#' dput(result)
#'
#'
#' ####################  LeadFormTestCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_lead_form_id <- "1234567890123" # character | Unique identifier of a lead form.
#' var_lead_form_test_request <- LeadFormTestRequest$new(c("answers_example")) # LeadFormTestRequest | Subscription to create.
#'
#' #Create lead form test data
#' api_instance <- LeadFormsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LeadFormTestCreate(var_ad_account_id, var_lead_form_id, var_lead_form_test_requestdata_file = "result.txt")
#' result <- api_instance$LeadFormTestCreate(var_ad_account_id, var_lead_form_id, var_lead_form_test_request)
#' dput(result)
#'
#'
#' ####################  LeadFormsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_lead_form_create_request <- c(LeadFormCreateRequest$new("name_example", "privacy_policy_link_example", "has_accepted_terms_example", "completion_message_example", c(LeadFormQuestion$new(LeadFormQuestionType$new(), LeadFormQuestionFieldType$new(), "custom_question_label_example", c("custom_question_options_example"))), LeadFormStatus$new(), "disclosure_language_example", c(LeadFormCommon_policy_links_inner$new("label_example", "link_example")))) # array[LeadFormCreateRequest] | List of lead forms to create, size limit [1, 30].
#'
#' #Create lead forms
#' api_instance <- LeadFormsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LeadFormsCreate(var_ad_account_id, var_lead_form_create_requestdata_file = "result.txt")
#' result <- api_instance$LeadFormsCreate(var_ad_account_id, var_lead_form_create_request)
#' dput(result)
#'
#'
#' ####################  LeadFormsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_order <- "ASCENDING" # character | The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #List lead forms
#' api_instance <- LeadFormsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LeadFormsList(var_ad_account_id, page_size = var_page_size, order = var_order, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$LeadFormsList(var_ad_account_id, page_size = var_page_size, order = var_order, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' ####################  LeadFormsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_lead_form_update_request <- c(LeadFormUpdateRequest$new("id_example", "name_example", "privacy_policy_link_example", "has_accepted_terms_example", "completion_message_example", LeadFormStatus$new(), "disclosure_language_example", c(LeadFormQuestion$new(LeadFormQuestionType$new(), LeadFormQuestionFieldType$new(), "custom_question_label_example", c("custom_question_options_example"))), c(LeadFormCommon_policy_links_inner$new("label_example", "link_example")))) # array[LeadFormUpdateRequest] | List of lead forms to update, size limit [1, 30].
#'
#' #Update lead forms
#' api_instance <- LeadFormsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$LeadFormsUpdate(var_ad_account_id, var_lead_form_update_requestdata_file = "result.txt")
#' result <- api_instance$LeadFormsUpdate(var_ad_account_id, var_lead_form_update_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LeadFormsApi <- R6::R6Class(
  "LeadFormsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new LeadFormsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get lead form by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_id Unique identifier of a lead form.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LeadFormResponse
    LeadFormGet = function(ad_account_id, lead_form_id, data_file = NULL, ...) {
      local_var_response <- self$LeadFormGetWithHttpInfo(ad_account_id, lead_form_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get lead form by id
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_id Unique identifier of a lead form.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LeadFormResponse) with additional information such as HTTP status code, headers
    LeadFormGetWithHttpInfo = function(ad_account_id, lead_form_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`lead_form_id`)) {
        stop("Missing required parameter `lead_form_id`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LeadFormsApi$LeadFormGet, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LeadFormsApi$LeadFormGet, must conform to the pattern ^\\d+$.")
      }

      if (!str_detect(`lead_form_id`, "^\\d+$")) {
        stop("Invalid value for `lead_form_id` when calling LeadFormsApi$LeadFormGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`lead_form_id`)) {
        local_var_url_path <- gsub("\\{lead_form_id\\}", URLencode(as.character(`lead_form_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LeadFormResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Create lead form test data
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_id Unique identifier of a lead form.
    #' @param lead_form_test_request Subscription to create.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LeadFormTestResponse
    LeadFormTestCreate = function(ad_account_id, lead_form_id, lead_form_test_request, data_file = NULL, ...) {
      local_var_response <- self$LeadFormTestCreateWithHttpInfo(ad_account_id, lead_form_id, lead_form_test_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create lead form test data
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_id Unique identifier of a lead form.
    #' @param lead_form_test_request Subscription to create.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LeadFormTestResponse) with additional information such as HTTP status code, headers
    LeadFormTestCreateWithHttpInfo = function(ad_account_id, lead_form_id, lead_form_test_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`lead_form_id`)) {
        stop("Missing required parameter `lead_form_id`.")
      }

      if (missing(`lead_form_test_request`)) {
        stop("Missing required parameter `lead_form_test_request`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LeadFormsApi$LeadFormTestCreate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LeadFormsApi$LeadFormTestCreate, must conform to the pattern ^\\d+$.")
      }

      if (!str_detect(`lead_form_id`, "^\\d+$")) {
        stop("Invalid value for `lead_form_id` when calling LeadFormsApi$LeadFormTestCreate, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`lead_form_test_request`)) {
        local_var_body <- `lead_form_test_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}/test"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`lead_form_id`)) {
        local_var_url_path <- gsub("\\{lead_form_id\\}", URLencode(as.character(`lead_form_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LeadFormTestResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Create lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_create_request List of lead forms to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LeadFormArrayResponse
    LeadFormsCreate = function(ad_account_id, lead_form_create_request, data_file = NULL, ...) {
      local_var_response <- self$LeadFormsCreateWithHttpInfo(ad_account_id, lead_form_create_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_create_request List of lead forms to create, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LeadFormArrayResponse) with additional information such as HTTP status code, headers
    LeadFormsCreateWithHttpInfo = function(ad_account_id, lead_form_create_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`lead_form_create_request`)) {
        stop("Missing required parameter `lead_form_create_request`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LeadFormsApi$LeadFormsCreate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LeadFormsApi$LeadFormsCreate, must conform to the pattern ^\\d+$.")
      }

      if (length(`lead_form_create_request`) > 30) {
        stop("Invalid length for `lead_form_create_request` when calling LeadFormsApi$LeadFormsCreate, number of items must be less than or equal to 30.")
      }
      if (length(`lead_form_create_request`) < 1) {
        stop("Invalid length for `lead_form_create_request` when calling LeadFormsApi$LeadFormsCreate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`lead_form_create_request`)) {
        body.items <- paste(unlist(lapply(`lead_form_create_request`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/lead_forms"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LeadFormArrayResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' List lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LeadFormsList200Response
    LeadFormsList = function(ad_account_id, page_size = 25, order = NULL, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$LeadFormsListWithHttpInfo(ad_account_id, page_size, order, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' List lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LeadFormsList200Response) with additional information such as HTTP status code, headers
    LeadFormsListWithHttpInfo = function(ad_account_id, page_size = 25, order = NULL, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LeadFormsApi$LeadFormsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LeadFormsApi$LeadFormsList, must conform to the pattern ^\\d+$.")
      }

      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling LeadFormsApi$LeadFormsList, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling LeadFormsApi$LeadFormsList, must be bigger than or equal to 1.")
      }



      query_params[["page_size"]] <- `page_size`

      if (!is.null(`order`) && !(`order` %in% c("ASCENDING", "DESCENDING"))) {
        stop("Invalid value for order when calling LeadFormsApi$LeadFormsList. Must be [ASCENDING, DESCENDING].")
      }
      query_params[["order"]] <- `order`

      query_params[["bookmark"]] <- `bookmark`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/lead_forms"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LeadFormsList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_update_request List of lead forms to update, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return LeadFormArrayResponse
    LeadFormsUpdate = function(ad_account_id, lead_form_update_request, data_file = NULL, ...) {
      local_var_response <- self$LeadFormsUpdateWithHttpInfo(ad_account_id, lead_form_update_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update lead forms
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param lead_form_update_request List of lead forms to update, size limit [1, 30].
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (LeadFormArrayResponse) with additional information such as HTTP status code, headers
    LeadFormsUpdateWithHttpInfo = function(ad_account_id, lead_form_update_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`lead_form_update_request`)) {
        stop("Missing required parameter `lead_form_update_request`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling LeadFormsApi$LeadFormsUpdate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling LeadFormsApi$LeadFormsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (length(`lead_form_update_request`) > 30) {
        stop("Invalid length for `lead_form_update_request` when calling LeadFormsApi$LeadFormsUpdate, number of items must be less than or equal to 30.")
      }
      if (length(`lead_form_update_request`) < 1) {
        stop("Invalid length for `lead_form_update_request` when calling LeadFormsApi$LeadFormsUpdate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`lead_form_update_request`)) {
        body.items <- paste(unlist(lapply(`lead_form_update_request`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/lead_forms"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LeadFormArrayResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
