#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Schedules operations
#' @description SchedulesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  SchedulesCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | 
#' var_schedule_create <- c(ScheduleCreate$new("entity_id_example", Schedule_delta_value$new(BidOptionsAgeBucketMultipliers$new(123, 123, 123, 123, 123, 123, 123), BidOptionsAppTypeMultipliers$new(123, 123, 123, 123, 123, 123), c(BidOptionsAudienceMultipliers$new("audience_id_example", 123)), BidOptionsGenderMultipliers$new(123, 123), BidOptionsPlacementMultipliers$new(123, 123, 123)), 123, AdAccountEntityType$new(), "name_example", ScheduleAction$new(), ScheduleStatus$new(), ScheduleType$new(), 123)) # array[ScheduleCreate] | 
#'
#' #Create schedules
#' api_instance <- SchedulesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SchedulesCreate(var_ad_account_id, var_schedule_createdata_file = "result.txt")
#' result <- api_instance$SchedulesCreate(var_ad_account_id, var_schedule_create)
#' dput(result)
#'
#'
#' ####################  SchedulesList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | 
#' var_entity_ids <- c("inner_example") # array[character] | List of Entity IDs, must be associated with the Ad Accound ID provided in the path.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#' var_order <- Pinterest.Lib.PaginationOrder$new() # PinterestLibPaginationOrder | The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_schedule_statuses <- c(ScheduleStatus$new()) # array[ScheduleStatus] | Filter schedules by status (one or more) (Optional)
#' var_schedule_type <- ScheduleType$new() # ScheduleType | Filter schedules by a type (Optional)
#'
#' #Get Schedules
#' api_instance <- SchedulesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SchedulesList(var_ad_account_id, var_entity_ids, bookmark = var_bookmark, page_size = var_page_size, order = var_order, schedule_statuses = var_schedule_statuses, schedule_type = var_schedule_typedata_file = "result.txt")
#' result <- api_instance$SchedulesList(var_ad_account_id, var_entity_ids, bookmark = var_bookmark, page_size = var_page_size, order = var_order, schedule_statuses = var_schedule_statuses, schedule_type = var_schedule_type)
#' dput(result)
#'
#'
#' ####################  SchedulesUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | 
#' var_schedule_batch_update <- c(ScheduleBatchUpdate$new("id_example", "schedule_id_example", Schedule_delta_value$new(BidOptionsAgeBucketMultipliers$new(123, 123, 123, 123, 123, 123, 123), BidOptionsAppTypeMultipliers$new(123, 123, 123, 123, 123, 123), c(BidOptionsAudienceMultipliers$new("audience_id_example", 123)), BidOptionsGenderMultipliers$new(123, 123), BidOptionsPlacementMultipliers$new(123, 123, 123)), 123, "entity_id_example", AdAccountEntityType$new(), "name_example", ScheduleAction$new(), ScheduleStatus$new(), ScheduleType$new(), 123)) # array[ScheduleBatchUpdate] | 
#'
#' #Update schedules
#' api_instance <- SchedulesApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SchedulesUpdate(var_ad_account_id, var_schedule_batch_updatedata_file = "result.txt")
#' result <- api_instance$SchedulesUpdate(var_ad_account_id, var_schedule_batch_update)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SchedulesApi <- R6::R6Class(
  "SchedulesApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new SchedulesApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create schedules
    #'
    #' @param ad_account_id 
    #' @param schedule_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[SchedulesCreate200ResponseInner]
    SchedulesCreate = function(ad_account_id, schedule_create, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SchedulesCreateWithHttpInfo(ad_account_id, schedule_create, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create schedules
    #'
    #' @param ad_account_id 
    #' @param schedule_create 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[SchedulesCreate200ResponseInner]) with additional information such as HTTP status code, headers
    SchedulesCreateWithHttpInfo = function(ad_account_id, schedule_create, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`schedule_create`)) {
        stop("Missing required parameter `schedule_create`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling SchedulesApi$SchedulesCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`schedule_create`) && is.null(`schedule_create`)) {
        stop("Invalid value for `schedule_create` when calling SchedulesApi$SchedulesCreate, `schedule_create` is not nullable")
      }

      if (!is.null(`schedule_create`)) {
        body.items <- paste(unlist(lapply(`schedule_create`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/schedules"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[SchedulesCreate200ResponseInner]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Schedules
    #'
    #' @param ad_account_id 
    #' @param entity_ids List of Entity IDs, must be associated with the Ad Accound ID provided in the path.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param schedule_statuses (optional) Filter schedules by status (one or more)
    #' @param schedule_type (optional) Filter schedules by a type
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SchedulesList200Response
    SchedulesList = function(ad_account_id, entity_ids, bookmark = NULL, page_size = 25, order = NULL, schedule_statuses = NULL, schedule_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SchedulesListWithHttpInfo(ad_account_id, entity_ids, bookmark, page_size, order, schedule_statuses, schedule_type, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Schedules
    #'
    #' @param ad_account_id 
    #' @param entity_ids List of Entity IDs, must be associated with the Ad Accound ID provided in the path.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param schedule_statuses (optional) Filter schedules by status (one or more)
    #' @param schedule_type (optional) Filter schedules by a type
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SchedulesList200Response) with additional information such as HTTP status code, headers
    SchedulesListWithHttpInfo = function(ad_account_id, entity_ids, bookmark = NULL, page_size = 25, order = NULL, schedule_statuses = NULL, schedule_type = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`entity_ids`)) {
        stop("Missing required parameter `entity_ids`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling SchedulesApi$SchedulesList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`entity_ids`) && is.null(`entity_ids`)) {
        stop("Invalid value for `entity_ids` when calling SchedulesApi$SchedulesList, `entity_ids` is not nullable")
      }
      if (!is.null(`entity_ids`) && length(`entity_ids`) < 1) {
        stop("Invalid length for `entity_ids` when calling SchedulesApi$SchedulesList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling SchedulesApi$SchedulesList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling SchedulesApi$SchedulesList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling SchedulesApi$SchedulesList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling SchedulesApi$SchedulesList, must be bigger than or equal to 1.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling SchedulesApi$SchedulesList, `order` is not nullable")
      }

      if (!missing(`schedule_statuses`) && is.null(`schedule_statuses`)) {
        stop("Invalid value for `schedule_statuses` when calling SchedulesApi$SchedulesList, `schedule_statuses` is not nullable")
      }

      if (!missing(`schedule_type`) && is.null(`schedule_type`)) {
        stop("Invalid value for `schedule_type` when calling SchedulesApi$SchedulesList, `schedule_type` is not nullable")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["order"]] <- `order`

      # explore
      for (query_item in `schedule_statuses`) {
        query_params[["schedule_statuses"]] <- c(query_params[["schedule_statuses"]], list(`schedule_statuses` = query_item))
      }

      query_params[["schedule_type"]] <- `schedule_type`

      # explore
      for (query_item in `entity_ids`) {
        query_params[["entity_ids"]] <- c(query_params[["entity_ids"]], list(`entity_ids` = query_item))
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/schedules"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SchedulesList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update schedules
    #'
    #' @param ad_account_id 
    #' @param schedule_batch_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[SchedulesCreate200ResponseInner]
    SchedulesUpdate = function(ad_account_id, schedule_batch_update, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SchedulesUpdateWithHttpInfo(ad_account_id, schedule_batch_update, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update schedules
    #'
    #' @param ad_account_id 
    #' @param schedule_batch_update 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[SchedulesCreate200ResponseInner]) with additional information such as HTTP status code, headers
    SchedulesUpdateWithHttpInfo = function(ad_account_id, schedule_batch_update, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`schedule_batch_update`)) {
        stop("Missing required parameter `schedule_batch_update`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling SchedulesApi$SchedulesUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling SchedulesApi$SchedulesUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`schedule_batch_update`) && is.null(`schedule_batch_update`)) {
        stop("Invalid value for `schedule_batch_update` when calling SchedulesApi$SchedulesUpdate, `schedule_batch_update` is not nullable")
      }

      if (!is.null(`schedule_batch_update`)) {
        body.items <- paste(unlist(lapply(`schedule_batch_update`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/schedules"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[SchedulesCreate200ResponseInner]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
