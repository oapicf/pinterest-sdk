#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Campaigns operations
#' @description CampaignsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdPinsAnalytics  ####################
#'
#' library(openapi)
#' var_campaign_id <- "campaign_id_example" # character | Campaign Id to use to filter the results.
#' var_pin_ids <- c("inner_example") # array[character] | List of Pin IDs.
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_columns <- c(ReportingColumnSync$new()) # array[ReportingColumnSync] | Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
#' var_granularity <- Granularity$new() # Granularity |   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_click_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (Optional)
#' var_engagement_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (Optional)
#' var_view_window_days <- 1 # numeric | Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (Optional)
#' var_conversion_report_time <- "TIME_OF_AD_ACTION" # character | The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (Optional)
#'
#' #Get pins analytics
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdPinsAnalytics(var_campaign_id, var_pin_ids, var_start_date, var_end_date, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_timedata_file = "result.txt")
#' result <- api_instance$AdPinsAnalytics(var_campaign_id, var_pin_ids, var_start_date, var_end_date, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time)
#' dput(result)
#'
#'
#' ####################  CampaignTargetingAnalyticsGet  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_campaign_ids <- c("inner_example") # array[character] | List of Campaign Ids to use to filter the results.
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_targeting_types <- c(AdsAnalyticsCampaignTargetingType$new()) # array[AdsAnalyticsCampaignTargetingType] | Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\"] is in BETA and not yet available to all users.
#' var_columns <- c(ReportingColumnSync$new()) # array[ReportingColumnSync] | Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
#' var_granularity <- Granularity$new() # Granularity |   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
#' var_click_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (Optional)
#' var_engagement_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (Optional)
#' var_view_window_days <- 1 # numeric | Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (Optional)
#' var_conversion_report_time <- "TIME_OF_AD_ACTION" # character | The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (Optional)
#' var_attribution_types <- c(ConversionReportAttributionType$new()) # array[ConversionReportAttributionType] | List of types of attribution for the conversion report (Optional)
#' var_reporting_timezone <- ReportingTimeZone$new() # ReportingTimeZone | Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users. (Optional)
#'
#' #Get targeting analytics for campaigns
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignTargetingAnalyticsGet(var_ad_account_id, var_campaign_ids, var_start_date, var_end_date, var_targeting_types, var_columns, var_granularity, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, attribution_types = var_attribution_types, reporting_timezone = var_reporting_timezonedata_file = "result.txt")
#' result <- api_instance$CampaignTargetingAnalyticsGet(var_ad_account_id, var_campaign_ids, var_start_date, var_end_date, var_targeting_types, var_columns, var_granularity, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, attribution_types = var_attribution_types, reporting_timezone = var_reporting_timezone)
#' dput(result)
#'
#'
#' ####################  CampaignsAnalytics  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
#' var_end_date <- "end_date_example" # character | Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
#' var_campaign_ids <- c("inner_example") # array[character] | List of Campaign Ids to use to filter the results.
#' var_columns <- c(ReportingColumnSync$new()) # array[ReportingColumnSync] | Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
#' var_granularity <- Granularity$new() # Granularity |   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_click_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (Optional)
#' var_engagement_window_days <- 30 # numeric | Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (Optional)
#' var_view_window_days <- 1 # numeric | Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (Optional)
#' var_conversion_report_time <- "TIME_OF_AD_ACTION" # character | The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (Optional)
#' var_aggregate_report_rows <- FALSE # character | Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (Optional)
#' var_reporting_timezone <- ReportingTimeZone$new() # ReportingTimeZone | Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users. (Optional)
#'
#' #Get campaign analytics
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignsAnalytics(var_start_date, var_end_date, var_campaign_ids, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, aggregate_report_rows = var_aggregate_report_rows, reporting_timezone = var_reporting_timezonedata_file = "result.txt")
#' result <- api_instance$CampaignsAnalytics(var_start_date, var_end_date, var_campaign_ids, var_columns, var_granularity, var_ad_account_id, click_window_days = var_click_window_days, engagement_window_days = var_engagement_window_days, view_window_days = var_view_window_days, conversion_report_time = var_conversion_report_time, aggregate_report_rows = var_aggregate_report_rows, reporting_timezone = var_reporting_timezone)
#' dput(result)
#'
#'
#' ####################  CampaignsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_campaign_create_item <- c(CampaignCreateItem$new("name_example", ConversionObjectiveType$new(), "ad_account_id_example", "app_id_example", MobileAppPlatform$new(), CampaignBidOptions$new(AgeBucketMultipliers$new(TargetingSpecAgeBucket$new()), AppTypeMultipliers$new(TargetingSpecAppType$new()), CampaignAudienceMultipliers$new("AUDIENCE_ID_example"), FreqBidMultiplierTimeWindow$new(), FrequencyMultipliers$new("IMPRESSION_COUNT_example"), GenderMultipliers$new(TargetingSpecGender$new()), PlacementMultipliers$new(PlacementType$new())), 123, 123, 123, IntendedPromotionType$new(), "is_automated_campaign_example", "is_campaign_budget_optimization_example", "is_flexible_daily_budgets_example", "is_ltv_optimized_example", "is_performance_plus_example", "is_top_of_search_example", 123, "order_line_id_example", 123, EntityStatus$new(), TrackingUrls$new(c("audience_verification_example"), c("buyable_button_example"), c("click_example"), c("engagement_example"), c("impression_example")))) # array[CampaignCreateItem] | 
#'
#' #Create campaigns
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignsCreate(var_ad_account_id, var_campaign_create_itemdata_file = "result.txt")
#' result <- api_instance$CampaignsCreate(var_ad_account_id, var_campaign_create_item)
#' dput(result)
#'
#'
#' ####################  CampaignsGet  ####################
#'
#' library(openapi)
#' var_campaign_id <- "campaign_id_example" # character | Campaign ID, must be associated with the ad account ID provided in the path.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#'
#' #Get campaign
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignsGet(var_campaign_id, var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$CampaignsGet(var_campaign_id, var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  CampaignsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#' var_order <- Pinterest.Lib.PaginationOrder$new() # PinterestLibPaginationOrder | The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_campaign_ids <- c("inner_example") # array[character] | List of Campaign Ids to use to filter the results. (Optional)
#' var_entity_statuses <- c(EntityStatus$new()) # array[EntityStatus] | Entity status (Optional)
#'
#' #List campaigns
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignsList(var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, order = var_order, campaign_ids = var_campaign_ids, entity_statuses = var_entity_statusesdata_file = "result.txt")
#' result <- api_instance$CampaignsList(var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size, order = var_order, campaign_ids = var_campaign_ids, entity_statuses = var_entity_statuses)
#' dput(result)
#'
#'
#' ####################  CampaignsUpdate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_campaign_batch_update_item <- c(CampaignBatchUpdateItem$new("id_example", "ad_account_id_example", "app_id_example", MobileAppPlatform$new(), CampaignBidOptionsUpdate$new(c(CampaignBidOptionsUpdateMaskItems$new()), AgeBucketMultipliers$new(TargetingSpecAgeBucket$new()), AppTypeMultipliers$new(TargetingSpecAppType$new()), CampaignAudienceMultipliers$new("AUDIENCE_ID_example"), FreqBidMultiplierTimeWindow$new(), FrequencyMultipliers$new("IMPRESSION_COUNT_example"), GenderMultipliers$new(TargetingSpecGender$new()), PlacementMultipliers$new(PlacementType$new())), 123, 123, 123, IntendedPromotionType$new(), "is_automated_campaign_example", "is_campaign_budget_optimization_example", "is_flexible_daily_budgets_example", "is_ltv_optimized_example", "is_performance_plus_example", "is_top_of_search_example", 123, "name_example", ConversionObjectiveType$new(), "order_line_id_example", PerformancePlusCampaignSettings$new("boost_prospecting_ad_group_bid_example", c("pinner_list_exclusions_example")), 123, EntityStatus$new(), TrackingUrls$new(c("audience_verification_example"), c("buyable_button_example"), c("click_example"), c("engagement_example"), c("impression_example")))) # array[CampaignBatchUpdateItem] | 
#'
#' #Update campaigns
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CampaignsUpdate(var_ad_account_id, var_campaign_batch_update_itemdata_file = "result.txt")
#' result <- api_instance$CampaignsUpdate(var_ad_account_id, var_campaign_batch_update_item)
#' dput(result)
#'
#'
#' ####################  GetCampaignDeliveryEstimates  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_campaign_delivery_estimates_campaign <- c(CampaignDeliveryEstimatesCampaign$new(c(AdGroupDeliveryEstimates$new("auto_targeting_enabled_example", c(AdGroupAudienceSizingCreativeTypes$new()), c(AdGroupDeliveryEstimatesKeywordsItems$new(NullalbleMatchType$new(), "value_example")), 123, OptimizationGoalMetadata$new(ConversionTagV3GoalMetadata$new(AttributionWindows$new(..., ..., ...), ConversionEvent$new(), "conversion_tag_id_example", "cpa_goal_value_in_micro_currency_example", "is_roas_optimized_example", "reporting_event_example"), FrequencyGoalMetadata$new(123, FrequencyGoalMetadataTimerange$new()), ScrollupGoalMetadata$new("scrollup_goal_value_in_micro_currency_example")), OptimizationType$new(), PlacementGroupType$new(), c("product_group_ids_example"), TargetingSpecOptimal$new(c(TargetingSpecAgeBucket$new()), c(TargetingSpecAppType$new()), c("AUDIENCE_EXCLUDE_example"), c("AUDIENCE_INCLUDE_example"), c(TargetingSpecGender$new()), c("GEO_example"), c("GEO_EXCLUDE_example"), c("INTEREST_example"), c("LOCALE_example"), c("LOCATION_example"), c("LOCATION_EXCLUDE_example"), "MAXIMUM_AGE_example", "MINIMUM_AGE_example", c(TargetingSpecShoppingRetargeting$new(123, 123, c(...))), c(TargetingStrategy$new())))), BudgetDurationType$new(), DeliveryEstimateObjectiveType$new(), "start_date_example", 123, "end_date_example", 123)) # array[CampaignDeliveryEstimatesCampaign] | 
#'
#' #Get campaign delivery estimates
#' api_instance <- CampaignsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCampaignDeliveryEstimates(var_ad_account_id, var_campaign_delivery_estimates_campaigndata_file = "result.txt")
#' result <- api_instance$GetCampaignDeliveryEstimates(var_ad_account_id, var_campaign_delivery_estimates_campaign)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CampaignsApi <- R6::R6Class(
  "CampaignsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CampaignsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get pins analytics
    #'
    #' @param campaign_id Campaign Id to use to filter the results.
    #' @param pin_ids List of Pin IDs.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[AdPinAnalytics]
    AdPinsAnalytics = function(campaign_id, pin_ids, start_date, end_date, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdPinsAnalyticsWithHttpInfo(campaign_id, pin_ids, start_date, end_date, columns, granularity, ad_account_id, click_window_days, engagement_window_days, view_window_days, conversion_report_time, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get pins analytics
    #'
    #' @param campaign_id Campaign Id to use to filter the results.
    #' @param pin_ids List of Pin IDs.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[AdPinAnalytics]) with additional information such as HTTP status code, headers
    AdPinsAnalyticsWithHttpInfo = function(campaign_id, pin_ids, start_date, end_date, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`pin_ids`)) {
        stop("Missing required parameter `pin_ids`.")
      }

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`columns`)) {
        stop("Missing required parameter `columns`.")
      }

      if (missing(`granularity`)) {
        stop("Missing required parameter `granularity`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`campaign_id`) && is.null(`campaign_id`)) {
        stop("Invalid value for `campaign_id` when calling CampaignsApi$AdPinsAnalytics, `campaign_id` is not nullable")
      }
      if (!is.null(`campaign_id`) && nchar(`campaign_id`) > 18) {
        stop("Invalid length for `campaign_id` when calling CampaignsApi$AdPinsAnalytics, must be smaller than or equal to 18.")
      }
      if (!is.null(`campaign_id`) && !stringr::str_detect(`campaign_id`, "^\\d+$")) {
        stop("Invalid value for `campaign_id` when calling CampaignsApi$AdPinsAnalytics, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`pin_ids`) && is.null(`pin_ids`)) {
        stop("Invalid value for `pin_ids` when calling CampaignsApi$AdPinsAnalytics, `pin_ids` is not nullable")
      }
      if (!is.null(`pin_ids`) && length(`pin_ids`) > 100) {
        stop("Invalid length for `pin_ids` when calling CampaignsApi$AdPinsAnalytics, number of items must be less than or equal to 100.")
      }
      if (!is.null(`pin_ids`) && length(`pin_ids`) < 1) {
        stop("Invalid length for `pin_ids` when calling CampaignsApi$AdPinsAnalytics, number of items must be greater than or equal to 1.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling CampaignsApi$AdPinsAnalytics, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling CampaignsApi$AdPinsAnalytics, `end_date` is not nullable")
      }

      if (!missing(`columns`) && is.null(`columns`)) {
        stop("Invalid value for `columns` when calling CampaignsApi$AdPinsAnalytics, `columns` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling CampaignsApi$AdPinsAnalytics, `granularity` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$AdPinsAnalytics, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$AdPinsAnalytics, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$AdPinsAnalytics, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`click_window_days`) && is.null(`click_window_days`)) {
        stop("Invalid value for `click_window_days` when calling CampaignsApi$AdPinsAnalytics, `click_window_days` is not nullable")
      }

      if (!missing(`engagement_window_days`) && is.null(`engagement_window_days`)) {
        stop("Invalid value for `engagement_window_days` when calling CampaignsApi$AdPinsAnalytics, `engagement_window_days` is not nullable")
      }

      if (!missing(`view_window_days`) && is.null(`view_window_days`)) {
        stop("Invalid value for `view_window_days` when calling CampaignsApi$AdPinsAnalytics, `view_window_days` is not nullable")
      }

      if (!missing(`conversion_report_time`) && is.null(`conversion_report_time`)) {
        stop("Invalid value for `conversion_report_time` when calling CampaignsApi$AdPinsAnalytics, `conversion_report_time` is not nullable")
      }

      query_params[["campaign_id"]] <- `campaign_id`

      # explore
      for (query_item in `pin_ids`) {
        query_params[["pin_ids"]] <- c(query_params[["pin_ids"]], list(`pin_ids` = query_item))
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      # no explore
      query_params[["columns"]] <- I(paste(lapply(`columns`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["granularity"]] <- `granularity`

      if (!is.null(`click_window_days`) && !(`click_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for click_window_days when calling CampaignsApi$AdPinsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["click_window_days"]] <- `click_window_days`

      if (!is.null(`engagement_window_days`) && !(`engagement_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for engagement_window_days when calling CampaignsApi$AdPinsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["engagement_window_days"]] <- `engagement_window_days`

      if (!is.null(`view_window_days`) && !(`view_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for view_window_days when calling CampaignsApi$AdPinsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["view_window_days"]] <- `view_window_days`

      if (!is.null(`conversion_report_time`) && !(`conversion_report_time` %in% c("TIME_OF_AD_ACTION", "TIME_OF_CONVERSION"))) {
        stop("Invalid value for conversion_report_time when calling CampaignsApi$AdPinsAnalytics. Must be [TIME_OF_AD_ACTION, TIME_OF_CONVERSION].")
      }
      query_params[["conversion_report_time"]] <- `conversion_report_time`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/pins/analytics"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[AdPinAnalytics]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get targeting analytics for campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_ids List of Campaign Ids to use to filter the results.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param targeting_types Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\"] is in BETA and not yet available to all users.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param attribution_types (optional) List of types of attribution for the conversion report
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return MetricsResponse
    CampaignTargetingAnalyticsGet = function(ad_account_id, campaign_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", attribution_types = NULL, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignTargetingAnalyticsGetWithHttpInfo(ad_account_id, campaign_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days, engagement_window_days, view_window_days, conversion_report_time, attribution_types, reporting_timezone, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get targeting analytics for campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_ids List of Campaign Ids to use to filter the results.
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param targeting_types Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\"AGE_BUCKET_AND_GENDER\"] is in BETA and not yet available to all users.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param attribution_types (optional) List of types of attribution for the conversion report
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (MetricsResponse) with additional information such as HTTP status code, headers
    CampaignTargetingAnalyticsGetWithHttpInfo = function(ad_account_id, campaign_ids, start_date, end_date, targeting_types, columns, granularity, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", attribution_types = NULL, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`campaign_ids`)) {
        stop("Missing required parameter `campaign_ids`.")
      }

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`targeting_types`)) {
        stop("Missing required parameter `targeting_types`.")
      }

      if (missing(`columns`)) {
        stop("Missing required parameter `columns`.")
      }

      if (missing(`granularity`)) {
        stop("Missing required parameter `granularity`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignTargetingAnalyticsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignTargetingAnalyticsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`campaign_ids`) && is.null(`campaign_ids`)) {
        stop("Invalid value for `campaign_ids` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `campaign_ids` is not nullable")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) > 250) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignTargetingAnalyticsGet, number of items must be less than or equal to 250.")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) < 1) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignTargetingAnalyticsGet, number of items must be greater than or equal to 1.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `end_date` is not nullable")
      }

      if (!missing(`targeting_types`) && is.null(`targeting_types`)) {
        stop("Invalid value for `targeting_types` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `targeting_types` is not nullable")
      }
      if (!is.null(`targeting_types`) && length(`targeting_types`) > 14) {
        stop("Invalid length for `targeting_types` when calling CampaignsApi$CampaignTargetingAnalyticsGet, number of items must be less than or equal to 14.")
      }
      if (!is.null(`targeting_types`) && length(`targeting_types`) < 1) {
        stop("Invalid length for `targeting_types` when calling CampaignsApi$CampaignTargetingAnalyticsGet, number of items must be greater than or equal to 1.")
      }

      if (!missing(`columns`) && is.null(`columns`)) {
        stop("Invalid value for `columns` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `columns` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `granularity` is not nullable")
      }

      if (!missing(`click_window_days`) && is.null(`click_window_days`)) {
        stop("Invalid value for `click_window_days` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `click_window_days` is not nullable")
      }

      if (!missing(`engagement_window_days`) && is.null(`engagement_window_days`)) {
        stop("Invalid value for `engagement_window_days` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `engagement_window_days` is not nullable")
      }

      if (!missing(`view_window_days`) && is.null(`view_window_days`)) {
        stop("Invalid value for `view_window_days` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `view_window_days` is not nullable")
      }

      if (!missing(`conversion_report_time`) && is.null(`conversion_report_time`)) {
        stop("Invalid value for `conversion_report_time` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `conversion_report_time` is not nullable")
      }

      if (!missing(`attribution_types`) && is.null(`attribution_types`)) {
        stop("Invalid value for `attribution_types` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `attribution_types` is not nullable")
      }
      if (!is.null(`attribution_types`) && length(`attribution_types`) > 2) {
        stop("Invalid length for `attribution_types` when calling CampaignsApi$CampaignTargetingAnalyticsGet, number of items must be less than or equal to 2.")
      }

      if (!missing(`reporting_timezone`) && is.null(`reporting_timezone`)) {
        stop("Invalid value for `reporting_timezone` when calling CampaignsApi$CampaignTargetingAnalyticsGet, `reporting_timezone` is not nullable")
      }

      # explore
      for (query_item in `campaign_ids`) {
        query_params[["campaign_ids"]] <- c(query_params[["campaign_ids"]], list(`campaign_ids` = query_item))
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      # no explore
      query_params[["targeting_types"]] <- I(paste(lapply(`targeting_types`, URLencode, reserved = TRUE), collapse = ","))

      # no explore
      query_params[["columns"]] <- I(paste(lapply(`columns`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["granularity"]] <- `granularity`

      if (!is.null(`click_window_days`) && !(`click_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for click_window_days when calling CampaignsApi$CampaignTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["click_window_days"]] <- `click_window_days`

      if (!is.null(`engagement_window_days`) && !(`engagement_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for engagement_window_days when calling CampaignsApi$CampaignTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["engagement_window_days"]] <- `engagement_window_days`

      if (!is.null(`view_window_days`) && !(`view_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for view_window_days when calling CampaignsApi$CampaignTargetingAnalyticsGet. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["view_window_days"]] <- `view_window_days`

      if (!is.null(`conversion_report_time`) && !(`conversion_report_time` %in% c("TIME_OF_AD_ACTION", "TIME_OF_CONVERSION"))) {
        stop("Invalid value for conversion_report_time when calling CampaignsApi$CampaignTargetingAnalyticsGet. Must be [TIME_OF_AD_ACTION, TIME_OF_CONVERSION].")
      }
      query_params[["conversion_report_time"]] <- `conversion_report_time`

      # no explore
      query_params[["attribution_types"]] <- I(paste(lapply(`attribution_types`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["reporting_timezone"]] <- `reporting_timezone`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns/targeting_analytics"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "MetricsResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get campaign analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param campaign_ids List of Campaign Ids to use to filter the results.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param aggregate_report_rows (optional) Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (default value: FALSE)
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[CampaignsAnalyticsMetrics]
    CampaignsAnalytics = function(start_date, end_date, campaign_ids, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", aggregate_report_rows = FALSE, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignsAnalyticsWithHttpInfo(start_date, end_date, campaign_ids, columns, granularity, ad_account_id, click_window_days, engagement_window_days, view_window_days, conversion_report_time, aggregate_report_rows, reporting_timezone, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get campaign analytics
    #'
    #' @param start_date Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
    #' @param end_date Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
    #' @param campaign_ids List of Campaign Ids to use to filter the results.
    #' @param columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.  For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
    #' @param granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param click_window_days (optional) Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. (default value: 30)
    #' @param engagement_window_days (optional) Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**. (default value: 30)
    #' @param view_window_days (optional) Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. (default value: 1)
    #' @param conversion_report_time (optional) The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. (default value: "TIME_OF_AD_ACTION")
    #' @param aggregate_report_rows (optional) Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users. (default value: FALSE)
    #' @param reporting_timezone (optional) Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[CampaignsAnalyticsMetrics]) with additional information such as HTTP status code, headers
    CampaignsAnalyticsWithHttpInfo = function(start_date, end_date, campaign_ids, columns, granularity, ad_account_id, click_window_days = 30, engagement_window_days = 30, view_window_days = 1, conversion_report_time = "TIME_OF_AD_ACTION", aggregate_report_rows = FALSE, reporting_timezone = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`start_date`)) {
        stop("Missing required parameter `start_date`.")
      }

      if (missing(`end_date`)) {
        stop("Missing required parameter `end_date`.")
      }

      if (missing(`campaign_ids`)) {
        stop("Missing required parameter `campaign_ids`.")
      }

      if (missing(`columns`)) {
        stop("Missing required parameter `columns`.")
      }

      if (missing(`granularity`)) {
        stop("Missing required parameter `granularity`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`start_date`) && is.null(`start_date`)) {
        stop("Invalid value for `start_date` when calling CampaignsApi$CampaignsAnalytics, `start_date` is not nullable")
      }

      if (!missing(`end_date`) && is.null(`end_date`)) {
        stop("Invalid value for `end_date` when calling CampaignsApi$CampaignsAnalytics, `end_date` is not nullable")
      }

      if (!missing(`campaign_ids`) && is.null(`campaign_ids`)) {
        stop("Invalid value for `campaign_ids` when calling CampaignsApi$CampaignsAnalytics, `campaign_ids` is not nullable")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) > 250) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignsAnalytics, number of items must be less than or equal to 250.")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) < 1) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignsAnalytics, number of items must be greater than or equal to 1.")
      }

      if (!missing(`columns`) && is.null(`columns`)) {
        stop("Invalid value for `columns` when calling CampaignsApi$CampaignsAnalytics, `columns` is not nullable")
      }

      if (!missing(`granularity`) && is.null(`granularity`)) {
        stop("Invalid value for `granularity` when calling CampaignsApi$CampaignsAnalytics, `granularity` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsAnalytics, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignsAnalytics, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsAnalytics, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`click_window_days`) && is.null(`click_window_days`)) {
        stop("Invalid value for `click_window_days` when calling CampaignsApi$CampaignsAnalytics, `click_window_days` is not nullable")
      }

      if (!missing(`engagement_window_days`) && is.null(`engagement_window_days`)) {
        stop("Invalid value for `engagement_window_days` when calling CampaignsApi$CampaignsAnalytics, `engagement_window_days` is not nullable")
      }

      if (!missing(`view_window_days`) && is.null(`view_window_days`)) {
        stop("Invalid value for `view_window_days` when calling CampaignsApi$CampaignsAnalytics, `view_window_days` is not nullable")
      }

      if (!missing(`conversion_report_time`) && is.null(`conversion_report_time`)) {
        stop("Invalid value for `conversion_report_time` when calling CampaignsApi$CampaignsAnalytics, `conversion_report_time` is not nullable")
      }

      if (!missing(`aggregate_report_rows`) && is.null(`aggregate_report_rows`)) {
        stop("Invalid value for `aggregate_report_rows` when calling CampaignsApi$CampaignsAnalytics, `aggregate_report_rows` is not nullable")
      }

      if (!missing(`reporting_timezone`) && is.null(`reporting_timezone`)) {
        stop("Invalid value for `reporting_timezone` when calling CampaignsApi$CampaignsAnalytics, `reporting_timezone` is not nullable")
      }

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      # explore
      for (query_item in `campaign_ids`) {
        query_params[["campaign_ids"]] <- c(query_params[["campaign_ids"]], list(`campaign_ids` = query_item))
      }

      # no explore
      query_params[["columns"]] <- I(paste(lapply(`columns`, URLencode, reserved = TRUE), collapse = ","))

      query_params[["granularity"]] <- `granularity`

      if (!is.null(`click_window_days`) && !(`click_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for click_window_days when calling CampaignsApi$CampaignsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["click_window_days"]] <- `click_window_days`

      if (!is.null(`engagement_window_days`) && !(`engagement_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for engagement_window_days when calling CampaignsApi$CampaignsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["engagement_window_days"]] <- `engagement_window_days`

      if (!is.null(`view_window_days`) && !(`view_window_days` %in% c("0", "1", "7", "14", "30", "60"))) {
        stop("Invalid value for view_window_days when calling CampaignsApi$CampaignsAnalytics. Must be [0, 1, 7, 14, 30, 60].")
      }
      query_params[["view_window_days"]] <- `view_window_days`

      if (!is.null(`conversion_report_time`) && !(`conversion_report_time` %in% c("TIME_OF_AD_ACTION", "TIME_OF_CONVERSION"))) {
        stop("Invalid value for conversion_report_time when calling CampaignsApi$CampaignsAnalytics. Must be [TIME_OF_AD_ACTION, TIME_OF_CONVERSION].")
      }
      query_params[["conversion_report_time"]] <- `conversion_report_time`

      query_params[["aggregate_report_rows"]] <- `aggregate_report_rows`

      query_params[["reporting_timezone"]] <- `reporting_timezone`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns/analytics"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[CampaignsAnalyticsMetrics]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_create_item 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CampaignBatchWriteResponseModel
    CampaignsCreate = function(ad_account_id, campaign_create_item, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignsCreateWithHttpInfo(ad_account_id, campaign_create_item, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_create_item 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CampaignBatchWriteResponseModel) with additional information such as HTTP status code, headers
    CampaignsCreateWithHttpInfo = function(ad_account_id, campaign_create_item, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`campaign_create_item`)) {
        stop("Missing required parameter `campaign_create_item`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsCreate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`campaign_create_item`) && is.null(`campaign_create_item`)) {
        stop("Invalid value for `campaign_create_item` when calling CampaignsApi$CampaignsCreate, `campaign_create_item` is not nullable")
      }
      if (!is.null(`campaign_create_item`) && length(`campaign_create_item`) > 30) {
        stop("Invalid length for `campaign_create_item` when calling CampaignsApi$CampaignsCreate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`campaign_create_item`) && length(`campaign_create_item`) < 1) {
        stop("Invalid length for `campaign_create_item` when calling CampaignsApi$CampaignsCreate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`campaign_create_item`)) {
        body.items <- paste(unlist(lapply(`campaign_create_item`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CampaignBatchWriteResponseModel"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get campaign
    #'
    #' @param campaign_id Campaign ID, must be associated with the ad account ID provided in the path.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return Campaign
    CampaignsGet = function(campaign_id, ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignsGetWithHttpInfo(campaign_id, ad_account_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get campaign
    #'
    #' @param campaign_id Campaign ID, must be associated with the ad account ID provided in the path.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (Campaign) with additional information such as HTTP status code, headers
    CampaignsGetWithHttpInfo = function(campaign_id, ad_account_id, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`campaign_id`) && is.null(`campaign_id`)) {
        stop("Invalid value for `campaign_id` when calling CampaignsApi$CampaignsGet, `campaign_id` is not nullable")
      }
      if (!is.null(`campaign_id`) && nchar(`campaign_id`) > 18) {
        stop("Invalid length for `campaign_id` when calling CampaignsApi$CampaignsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`campaign_id`) && !stringr::str_detect(`campaign_id`, "^\\d+$")) {
        stop("Invalid value for `campaign_id` when calling CampaignsApi$CampaignsGet, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsGet, must conform to the pattern ^\\d+$.")
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns/{campaign_id}"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaign_id\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Campaign"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param entity_statuses (optional) Entity status (default value: ["ACTIVE","PAUSED"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CampaignsList200Response
    CampaignsList = function(ad_account_id, bookmark = NULL, page_size = 25, order = NULL, campaign_ids = NULL, entity_statuses = ["ACTIVE","PAUSED"], data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignsListWithHttpInfo(ad_account_id, bookmark, page_size, order, campaign_ids, entity_statuses, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param campaign_ids (optional) List of Campaign Ids to use to filter the results.
    #' @param entity_statuses (optional) Entity status (default value: ["ACTIVE","PAUSED"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CampaignsList200Response) with additional information such as HTTP status code, headers
    CampaignsListWithHttpInfo = function(ad_account_id, bookmark = NULL, page_size = 25, order = NULL, campaign_ids = NULL, entity_statuses = ["ACTIVE","PAUSED"], data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling CampaignsApi$CampaignsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling CampaignsApi$CampaignsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling CampaignsApi$CampaignsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling CampaignsApi$CampaignsList, must be bigger than or equal to 1.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling CampaignsApi$CampaignsList, `order` is not nullable")
      }

      if (!missing(`campaign_ids`) && is.null(`campaign_ids`)) {
        stop("Invalid value for `campaign_ids` when calling CampaignsApi$CampaignsList, `campaign_ids` is not nullable")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) > 250) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignsList, number of items must be less than or equal to 250.")
      }
      if (!is.null(`campaign_ids`) && length(`campaign_ids`) < 1) {
        stop("Invalid length for `campaign_ids` when calling CampaignsApi$CampaignsList, number of items must be greater than or equal to 1.")
      }

      if (!missing(`entity_statuses`) && is.null(`entity_statuses`)) {
        stop("Invalid value for `entity_statuses` when calling CampaignsApi$CampaignsList, `entity_statuses` is not nullable")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      query_params[["order"]] <- `order`

      # explore
      for (query_item in `campaign_ids`) {
        query_params[["campaign_ids"]] <- c(query_params[["campaign_ids"]], list(`campaign_ids` = query_item))
      }

      # explore
      for (query_item in `entity_statuses`) {
        query_params[["entity_statuses"]] <- c(query_params[["entity_statuses"]], list(`entity_statuses` = query_item))
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CampaignsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_batch_update_item 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CampaignBatchWriteResponseModel
    CampaignsUpdate = function(ad_account_id, campaign_batch_update_item, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CampaignsUpdateWithHttpInfo(ad_account_id, campaign_batch_update_item, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update campaigns
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_batch_update_item 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CampaignBatchWriteResponseModel) with additional information such as HTTP status code, headers
    CampaignsUpdateWithHttpInfo = function(ad_account_id, campaign_batch_update_item, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`campaign_batch_update_item`)) {
        stop("Missing required parameter `campaign_batch_update_item`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsUpdate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$CampaignsUpdate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$CampaignsUpdate, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`campaign_batch_update_item`) && is.null(`campaign_batch_update_item`)) {
        stop("Invalid value for `campaign_batch_update_item` when calling CampaignsApi$CampaignsUpdate, `campaign_batch_update_item` is not nullable")
      }
      if (!is.null(`campaign_batch_update_item`) && length(`campaign_batch_update_item`) > 30) {
        stop("Invalid length for `campaign_batch_update_item` when calling CampaignsApi$CampaignsUpdate, number of items must be less than or equal to 30.")
      }
      if (!is.null(`campaign_batch_update_item`) && length(`campaign_batch_update_item`) < 1) {
        stop("Invalid length for `campaign_batch_update_item` when calling CampaignsApi$CampaignsUpdate, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`campaign_batch_update_item`)) {
        body.items <- paste(unlist(lapply(`campaign_batch_update_item`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CampaignBatchWriteResponseModel"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get campaign delivery estimates
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_delivery_estimates_campaign 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return CampaignDeliveryEstimatesResponse
    GetCampaignDeliveryEstimates = function(ad_account_id, campaign_delivery_estimates_campaign, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCampaignDeliveryEstimatesWithHttpInfo(ad_account_id, campaign_delivery_estimates_campaign, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get campaign delivery estimates
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param campaign_delivery_estimates_campaign 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (CampaignDeliveryEstimatesResponse) with additional information such as HTTP status code, headers
    GetCampaignDeliveryEstimatesWithHttpInfo = function(ad_account_id, campaign_delivery_estimates_campaign, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`campaign_delivery_estimates_campaign`)) {
        stop("Missing required parameter `campaign_delivery_estimates_campaign`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$GetCampaignDeliveryEstimates, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CampaignsApi$GetCampaignDeliveryEstimates, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CampaignsApi$GetCampaignDeliveryEstimates, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`campaign_delivery_estimates_campaign`) && is.null(`campaign_delivery_estimates_campaign`)) {
        stop("Invalid value for `campaign_delivery_estimates_campaign` when calling CampaignsApi$GetCampaignDeliveryEstimates, `campaign_delivery_estimates_campaign` is not nullable")
      }
      if (!is.null(`campaign_delivery_estimates_campaign`) && length(`campaign_delivery_estimates_campaign`) > 1) {
        stop("Invalid length for `campaign_delivery_estimates_campaign` when calling CampaignsApi$GetCampaignDeliveryEstimates, number of items must be less than or equal to 1.")
      }
      if (!is.null(`campaign_delivery_estimates_campaign`) && length(`campaign_delivery_estimates_campaign`) < 1) {
        stop("Invalid length for `campaign_delivery_estimates_campaign` when calling CampaignsApi$GetCampaignDeliveryEstimates, number of items must be greater than or equal to 1.")
      }

      if (!is.null(`campaign_delivery_estimates_campaign`)) {
        body.items <- paste(unlist(lapply(`campaign_delivery_estimates_campaign`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/campaigns/delivery_estimates"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CampaignDeliveryEstimatesResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
