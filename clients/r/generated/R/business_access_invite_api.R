#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title BusinessAccessInvite operations
#' @description BusinessAccessInviteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AssetAccessRequestsCreate  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_create_asset_access_request_body <- CreateAssetAccessRequestBody$new(c(CreateAssetAccessRequestBody_asset_requests_inner$new("partner_id_example", c(key = c(Permissions$new()))))) # CreateAssetAccessRequestBody | 
#'
#' #Create a request to access an existing partner's assets.
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AssetAccessRequestsCreate(var_business_id, var_create_asset_access_request_bodydata_file = "result.txt")
#' result <- api_instance$AssetAccessRequestsCreate(var_business_id, var_create_asset_access_request_body)
#' dput(result)
#'
#'
#' ####################  CancelInvitesOrRequests  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Business id
#' var_cancel_invites_body <- CancelInvitesBody$new(c("invite_ids_example")) # CancelInvitesBody | A list with invite ids
#'
#' #Cancel invites/requests
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CancelInvitesOrRequests(var_business_id, var_cancel_invites_bodydata_file = "result.txt")
#' result <- api_instance$CancelInvitesOrRequests(var_business_id, var_cancel_invites_body)
#' dput(result)
#'
#'
#' ####################  CreateAssetInvites  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_create_asset_invites_request <- CreateAssetInvitesRequest$new(c(CreateAssetInvitesRequestItem$new("invite_id_example", InviteType$new(), c(key = c(Permissions$new()))))) # CreateAssetInvitesRequest | A list of invites/requests together with the asset permissions to be assigned to the invite/request. 
#'
#' #Update invite/request with an asset permission
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateAssetInvites(var_business_id, var_create_asset_invites_requestdata_file = "result.txt")
#' result <- api_instance$CreateAssetInvites(var_business_id, var_create_asset_invites_request)
#' dput(result)
#'
#'
#' ####################  CreateMembershipOrPartnershipInvites  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Business id
#' var_create_membership_or_partnership_invites_body <- CreateMembershipOrPartnershipInvitesBody$new("EMPLOYEE", InviteType$new(), c("members_example"), c("partners_example")) # CreateMembershipOrPartnershipInvitesBody | An object with the properties: invite_type, partners, members, business_role
#'
#' #Create invites or requests
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateMembershipOrPartnershipInvites(var_business_id, var_create_membership_or_partnership_invites_bodydata_file = "result.txt")
#' result <- api_instance$CreateMembershipOrPartnershipInvites(var_business_id, var_create_membership_or_partnership_invites_body)
#' dput(result)
#'
#'
#' ####################  GetInvites  ####################
#'
#' library(openapi)
#' var_business_id <- "729090764583391194" # character | Unique identifier of the requesting business.
#' var_is_member <- TRUE # character | A boolean field to indicate whether the invite is to create a partnership or a membership. (Optional)
#' var_invite_status <- c("PENDING") # array[character] | A list of invite statuses to filter invites by. Only invites whose status is in the provided statuses will be returned. (Optional)
#' var_invite_type <- InviteType$new() # InviteType | Invite type to filter invites by. Only invites of the specified type will be returned. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get invites/requests
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetInvites(var_business_id, is_member = var_is_member, invite_status = var_invite_status, invite_type = var_invite_type, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$GetInvites(var_business_id, is_member = var_is_member, invite_status = var_invite_status, invite_type = var_invite_type, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  RespondBusinessAccessInvites  ####################
#'
#' library(openapi)
#' var_auth_respond_invites_body <- AuthRespondInvitesBody$new(c(AuthRespondInvitesBody_invites_inner$new(AuthRespondInvitesBody_invites_inner_action$new("accept_invite_example", c(key = c(Permissions$new()))), "invite_id_example"))) # AuthRespondInvitesBody | 
#'
#' #Accept or decline an invite/request
#' api_instance <- BusinessAccessInviteApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RespondBusinessAccessInvites(var_auth_respond_invites_bodydata_file = "result.txt")
#' result <- api_instance$RespondBusinessAccessInvites(var_auth_respond_invites_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
BusinessAccessInviteApi <- R6::R6Class(
  "BusinessAccessInviteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new BusinessAccessInviteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Create a request to access an existing partner's assets.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_access_request_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CreateAssetAccessRequestResponse
    AssetAccessRequestsCreate = function(business_id, create_asset_access_request_body, data_file = NULL, ...) {
      local_var_response <- self$AssetAccessRequestsCreateWithHttpInfo(business_id, create_asset_access_request_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create a request to access an existing partner's assets.
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_access_request_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CreateAssetAccessRequestResponse) with additional information such as HTTP status code, headers
    AssetAccessRequestsCreateWithHttpInfo = function(business_id, create_asset_access_request_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`create_asset_access_request_body`)) {
        stop("Missing required parameter `create_asset_access_request_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$AssetAccessRequestsCreate, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$AssetAccessRequestsCreate, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessInviteApi$AssetAccessRequestsCreate, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`create_asset_access_request_body`)) {
        local_var_body <- `create_asset_access_request_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/requests/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateAssetAccessRequestResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Cancel invites/requests
    #'
    #' @param business_id Business id
    #' @param cancel_invites_body A list with invite ids
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return DeleteInvitesResultsResponseArray
    CancelInvitesOrRequests = function(business_id, cancel_invites_body, data_file = NULL, ...) {
      local_var_response <- self$CancelInvitesOrRequestsWithHttpInfo(business_id, cancel_invites_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Cancel invites/requests
    #'
    #' @param business_id Business id
    #' @param cancel_invites_body A list with invite ids
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (DeleteInvitesResultsResponseArray) with additional information such as HTTP status code, headers
    CancelInvitesOrRequestsWithHttpInfo = function(business_id, cancel_invites_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`cancel_invites_body`)) {
        stop("Missing required parameter `cancel_invites_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CancelInvitesOrRequests, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CancelInvitesOrRequests, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessInviteApi$CancelInvitesOrRequests, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`cancel_invites_body`)) {
        local_var_body <- `cancel_invites_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/invites"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "DeleteInvitesResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update invite/request with an asset permission
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_invites_request A list of invites/requests together with the asset permissions to be assigned to the invite/request. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return UpdateInvitesResultsResponseArray
    CreateAssetInvites = function(business_id, create_asset_invites_request, data_file = NULL, ...) {
      local_var_response <- self$CreateAssetInvitesWithHttpInfo(business_id, create_asset_invites_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update invite/request with an asset permission
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param create_asset_invites_request A list of invites/requests together with the asset permissions to be assigned to the invite/request. 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (UpdateInvitesResultsResponseArray) with additional information such as HTTP status code, headers
    CreateAssetInvitesWithHttpInfo = function(business_id, create_asset_invites_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`create_asset_invites_request`)) {
        stop("Missing required parameter `create_asset_invites_request`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CreateAssetInvites, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CreateAssetInvites, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessInviteApi$CreateAssetInvites, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`create_asset_invites_request`)) {
        local_var_body <- `create_asset_invites_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/invites/assets/access"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "UpdateInvitesResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Create invites or requests
    #'
    #' @param business_id Business id
    #' @param create_membership_or_partnership_invites_body An object with the properties: invite_type, partners, members, business_role
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CreateInvitesResultsResponseArray
    CreateMembershipOrPartnershipInvites = function(business_id, create_membership_or_partnership_invites_body, data_file = NULL, ...) {
      local_var_response <- self$CreateMembershipOrPartnershipInvitesWithHttpInfo(business_id, create_membership_or_partnership_invites_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create invites or requests
    #'
    #' @param business_id Business id
    #' @param create_membership_or_partnership_invites_body An object with the properties: invite_type, partners, members, business_role
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CreateInvitesResultsResponseArray) with additional information such as HTTP status code, headers
    CreateMembershipOrPartnershipInvitesWithHttpInfo = function(business_id, create_membership_or_partnership_invites_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`create_membership_or_partnership_invites_body`)) {
        stop("Missing required parameter `create_membership_or_partnership_invites_body`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CreateMembershipOrPartnershipInvites, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$CreateMembershipOrPartnershipInvites, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessInviteApi$CreateMembershipOrPartnershipInvites, must conform to the pattern ^\\d+$.")
      }


      if (!is.null(`create_membership_or_partnership_invites_body`)) {
        local_var_body <- `create_membership_or_partnership_invites_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/invites"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CreateInvitesResultsResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get invites/requests
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param is_member (optional) A boolean field to indicate whether the invite is to create a partnership or a membership. (default value: TRUE)
    #' @param invite_status (optional) A list of invite statuses to filter invites by. Only invites whose status is in the provided statuses will be returned.
    #' @param invite_type (optional) Invite type to filter invites by. Only invites of the specified type will be returned.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return GetInvites200Response
    GetInvites = function(business_id, is_member = TRUE, invite_status = NULL, invite_type = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$GetInvitesWithHttpInfo(business_id, is_member, invite_status, invite_type, bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get invites/requests
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param is_member (optional) A boolean field to indicate whether the invite is to create a partnership or a membership. (default value: TRUE)
    #' @param invite_status (optional) A list of invite statuses to filter invites by. Only invites whose status is in the provided statuses will be returned.
    #' @param invite_type (optional) Invite type to filter invites by. Only invites of the specified type will be returned.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (GetInvites200Response) with additional information such as HTTP status code, headers
    GetInvitesWithHttpInfo = function(business_id, is_member = TRUE, invite_status = NULL, invite_type = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$GetInvites, must be smaller than or equal to 20.")
      }
      if (nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling BusinessAccessInviteApi$GetInvites, must be bigger than or equal to 1.")
      }
      if (!str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling BusinessAccessInviteApi$GetInvites, must conform to the pattern ^\\d+$.")
      }


      if (length(`invite_status`) < 1) {
        stop("Invalid length for `invite_status` when calling BusinessAccessInviteApi$GetInvites, number of items must be greater than or equal to 1.")
      }



      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling BusinessAccessInviteApi$GetInvites, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling BusinessAccessInviteApi$GetInvites, must be bigger than or equal to 1.")
      }

      query_params[["is_member"]] <- `is_member`

      # explore
      for (query_item in `invite_status`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("PENDING", "EXPIRED"))) {
          stop("Invalid value for invite_status when calling BusinessAccessInviteApi$GetInvites. Must be [PENDING, EXPIRED].")
        }
        query_params[["invite_status"]] <- c(query_params[["invite_status"]], list(`invite_status` = query_item))
      }

      query_params[["invite_type"]] <- `invite_type`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/invites"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetInvites200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Accept or decline an invite/request
    #'
    #' @param auth_respond_invites_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return RespondToInvitesResponseArray
    RespondBusinessAccessInvites = function(auth_respond_invites_body, data_file = NULL, ...) {
      local_var_response <- self$RespondBusinessAccessInvitesWithHttpInfo(auth_respond_invites_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Accept or decline an invite/request
    #'
    #' @param auth_respond_invites_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (RespondToInvitesResponseArray) with additional information such as HTTP status code, headers
    RespondBusinessAccessInvitesWithHttpInfo = function(auth_respond_invites_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`auth_respond_invites_body`)) {
        stop("Missing required parameter `auth_respond_invites_body`.")
      }


      if (!is.null(`auth_respond_invites_body`)) {
        local_var_body <- `auth_respond_invites_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/invites"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "RespondToInvitesResponseArray", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
