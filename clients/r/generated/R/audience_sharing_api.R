#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.28.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AudienceSharing operations
#' @description AudienceSharingApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AdAccountsAudiencesSharedAccountsList  ####################
#'
#' library(openapi)
#' var_audience_id <- "audience_id_example" # character | Unique identifier of the audience to use to filter the results.
#' var_account_type <- AudienceAccountType$new() # AudienceAccountType | Filter accounts by account type.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #List accounts with access to an audience owned by an ad account
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure OAuth2 access token for authorization: client_credentials
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdAccountsAudiencesSharedAccountsList(var_audience_id, var_account_type, var_ad_account_id, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$AdAccountsAudiencesSharedAccountsList(var_audience_id, var_account_type, var_ad_account_id, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  BusinessAccountAudiencesSharedAccountsList  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_audience_id <- "audience_id_example" # character | Unique identifier of the audience to use to filter the results.
#' var_account_type <- AudienceAccountType$new() # AudienceAccountType | Filter accounts by account type.
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #List accounts with access to an audience owned by a business
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$BusinessAccountAudiencesSharedAccountsList(var_business_id, var_audience_id, var_account_type, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$BusinessAccountAudiencesSharedAccountsList(var_business_id, var_audience_id, var_account_type, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  SharedAudiencesForBusinessList  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_order <- Order$new() # Order | The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (Optional)
#'
#' #List received audiences for a business
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SharedAudiencesForBusinessList(var_business_id, order = var_order, bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$SharedAudiencesForBusinessList(var_business_id, order = var_order, bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  UpdateAdAccountToAdAccountSharedAudience  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_account_to_ad_account_shared_audience_update_with_required_body <- AdAccountToAdAccountSharedAudienceUpdateWithRequiredBody$new("audience_id_example", OperationType$new(), c("recipient_account_ids_example")) # AdAccountToAdAccountSharedAudienceUpdateWithRequiredBody | 
#'
#' #Update audience sharing between ad accounts
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateAdAccountToAdAccountSharedAudience(var_ad_account_id, var_ad_account_to_ad_account_shared_audience_update_with_required_bodydata_file = "result.txt")
#' result <- api_instance$UpdateAdAccountToAdAccountSharedAudience(var_ad_account_id, var_ad_account_to_ad_account_shared_audience_update_with_required_body)
#' dput(result)
#'
#'
#' ####################  UpdateAdAccountToBusinessSharedAudience  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_ad_account_to_business_shared_audience_update_with_required_body <- AdAccountToBusinessSharedAudienceUpdateWithRequiredBody$new("audience_id_example", OperationType$new(), c("recipient_business_ids_example")) # AdAccountToBusinessSharedAudienceUpdateWithRequiredBody | 
#'
#' #Update audience sharing from an ad account to businesses
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateAdAccountToBusinessSharedAudience(var_ad_account_id, var_ad_account_to_business_shared_audience_update_with_required_bodydata_file = "result.txt")
#' result <- api_instance$UpdateAdAccountToBusinessSharedAudience(var_ad_account_id, var_ad_account_to_business_shared_audience_update_with_required_body)
#' dput(result)
#'
#'
#' ####################  UpdateBusinessToAdAccountSharedAudience  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_business_to_ad_account_shared_audience_update_with_required_body <- BusinessToAdAccountSharedAudienceUpdateWithRequiredBody$new("audience_id_example", OperationType$new(), c("recipient_account_ids_example")) # BusinessToAdAccountSharedAudienceUpdateWithRequiredBody | 
#'
#' #Update audience sharing from a business to ad accounts
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBusinessToAdAccountSharedAudience(var_business_id, var_business_to_ad_account_shared_audience_update_with_required_bodydata_file = "result.txt")
#' result <- api_instance$UpdateBusinessToAdAccountSharedAudience(var_business_id, var_business_to_ad_account_shared_audience_update_with_required_body)
#' dput(result)
#'
#'
#' ####################  UpdateBusinessToBusinessSharedAudience  ####################
#'
#' library(openapi)
#' var_business_id <- "business_id_example" # character | Unique identifier of the requesting business.
#' var_business_to_business_shared_audience_update_with_required_body <- BusinessToBusinessSharedAudienceUpdateWithRequiredBody$new("audience_id_example", OperationType$new(), c("recipient_business_ids_example")) # BusinessToBusinessSharedAudienceUpdateWithRequiredBody | 
#'
#' #Update audience sharing between businesses
#' api_instance <- AudienceSharingApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBusinessToBusinessSharedAudience(var_business_id, var_business_to_business_shared_audience_update_with_required_bodydata_file = "result.txt")
#' result <- api_instance$UpdateBusinessToBusinessSharedAudience(var_business_id, var_business_to_business_shared_audience_update_with_required_body)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AudienceSharingApi <- R6::R6Class(
  "AudienceSharingApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new AudienceSharingApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' List accounts with access to an audience owned by an ad account
    #'
    #' @param audience_id Unique identifier of the audience to use to filter the results.
    #' @param account_type Filter accounts by account type.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdAccountsAudiencesSharedAccountsList200Response
    AdAccountsAudiencesSharedAccountsList = function(audience_id, account_type, ad_account_id, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$AdAccountsAudiencesSharedAccountsListWithHttpInfo(audience_id, account_type, ad_account_id, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List accounts with access to an audience owned by an ad account
    #'
    #' @param audience_id Unique identifier of the audience to use to filter the results.
    #' @param account_type Filter accounts by account type.
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdAccountsAudiencesSharedAccountsList200Response) with additional information such as HTTP status code, headers
    AdAccountsAudiencesSharedAccountsListWithHttpInfo = function(audience_id, account_type, ad_account_id, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`audience_id`)) {
        stop("Missing required parameter `audience_id`.")
      }

      if (missing(`account_type`)) {
        stop("Missing required parameter `account_type`.")
      }

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (!missing(`audience_id`) && is.null(`audience_id`)) {
        stop("Invalid value for `audience_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, `audience_id` is not nullable")
      }
      if (!is.null(`audience_id`) && nchar(`audience_id`) > 18) {
        stop("Invalid length for `audience_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`audience_id`) && !stringr::str_detect(`audience_id`, "^\\d+$")) {
        stop("Invalid value for `audience_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`account_type`) && is.null(`account_type`)) {
        stop("Invalid value for `account_type` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, `account_type` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$AdAccountsAudiencesSharedAccountsList, must be bigger than or equal to 1.")
      }

      query_params[["audience_id"]] <- `audience_id`

      query_params[["account_type"]] <- `account_type`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/audiences/shared/accounts"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdAccountsAudiencesSharedAccountsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List accounts with access to an audience owned by a business
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param audience_id Unique identifier of the audience to use to filter the results.
    #' @param account_type Filter accounts by account type.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdAccountsAudiencesSharedAccountsList200Response
    BusinessAccountAudiencesSharedAccountsList = function(business_id, audience_id, account_type, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$BusinessAccountAudiencesSharedAccountsListWithHttpInfo(business_id, audience_id, account_type, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List accounts with access to an audience owned by a business
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param audience_id Unique identifier of the audience to use to filter the results.
    #' @param account_type Filter accounts by account type.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdAccountsAudiencesSharedAccountsList200Response) with additional information such as HTTP status code, headers
    BusinessAccountAudiencesSharedAccountsListWithHttpInfo = function(business_id, audience_id, account_type, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`audience_id`)) {
        stop("Missing required parameter `audience_id`.")
      }

      if (missing(`account_type`)) {
        stop("Missing required parameter `account_type`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`audience_id`) && is.null(`audience_id`)) {
        stop("Invalid value for `audience_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, `audience_id` is not nullable")
      }
      if (!is.null(`audience_id`) && nchar(`audience_id`) > 18) {
        stop("Invalid length for `audience_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must be smaller than or equal to 18.")
      }
      if (!is.null(`audience_id`) && !stringr::str_detect(`audience_id`, "^\\d+$")) {
        stop("Invalid value for `audience_id` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`account_type`) && is.null(`account_type`)) {
        stop("Invalid value for `account_type` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, `account_type` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$BusinessAccountAudiencesSharedAccountsList, must be bigger than or equal to 1.")
      }

      query_params[["audience_id"]] <- `audience_id`

      query_params[["account_type"]] <- `account_type`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/audiences/shared/accounts"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdAccountsAudiencesSharedAccountsList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List received audiences for a business
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SharedAudiencesForBusinessList200Response
    SharedAudiencesForBusinessList = function(business_id, order = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SharedAudiencesForBusinessListWithHttpInfo(business_id, order, bookmark, page_size, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List received audiences for a business
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param order (optional) The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SharedAudiencesForBusinessList200Response) with additional information such as HTTP status code, headers
    SharedAudiencesForBusinessListWithHttpInfo = function(business_id, order = NULL, bookmark = NULL, page_size = 25, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$SharedAudiencesForBusinessList, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$SharedAudiencesForBusinessList, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$SharedAudiencesForBusinessList, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$SharedAudiencesForBusinessList, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`order`) && is.null(`order`)) {
        stop("Invalid value for `order` when calling AudienceSharingApi$SharedAudiencesForBusinessList, `order` is not nullable")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling AudienceSharingApi$SharedAudiencesForBusinessList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$SharedAudiencesForBusinessList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$SharedAudiencesForBusinessList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling AudienceSharingApi$SharedAudiencesForBusinessList, must be bigger than or equal to 1.")
      }

      query_params[["order"]] <- `order`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/businesses/{business_id}/audiences"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SharedAudiencesForBusinessList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update audience sharing between ad accounts
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_account_to_ad_account_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdAccountToAdAccountSharedAudience
    UpdateAdAccountToAdAccountSharedAudience = function(ad_account_id, ad_account_to_ad_account_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateAdAccountToAdAccountSharedAudienceWithHttpInfo(ad_account_id, ad_account_to_ad_account_shared_audience_update_with_required_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update audience sharing between ad accounts
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_account_to_ad_account_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdAccountToAdAccountSharedAudience) with additional information such as HTTP status code, headers
    UpdateAdAccountToAdAccountSharedAudienceWithHttpInfo = function(ad_account_id, ad_account_to_ad_account_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_account_to_ad_account_shared_audience_update_with_required_body`)) {
        stop("Missing required parameter `ad_account_to_ad_account_shared_audience_update_with_required_body`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToAdAccountSharedAudience, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToAdAccountSharedAudience, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToAdAccountSharedAudience, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_account_to_ad_account_shared_audience_update_with_required_body`) && is.null(`ad_account_to_ad_account_shared_audience_update_with_required_body`)) {
        stop("Invalid value for `ad_account_to_ad_account_shared_audience_update_with_required_body` when calling AudienceSharingApi$UpdateAdAccountToAdAccountSharedAudience, `ad_account_to_ad_account_shared_audience_update_with_required_body` is not nullable")
      }

      if (!is.null(`ad_account_to_ad_account_shared_audience_update_with_required_body`)) {
        local_var_body <- `ad_account_to_ad_account_shared_audience_update_with_required_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/audiences/ad_accounts/shared"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdAccountToAdAccountSharedAudience"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update audience sharing from an ad account to businesses
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_account_to_business_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return AdAccountToBusinessSharedAudience
    UpdateAdAccountToBusinessSharedAudience = function(ad_account_id, ad_account_to_business_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateAdAccountToBusinessSharedAudienceWithHttpInfo(ad_account_id, ad_account_to_business_shared_audience_update_with_required_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update audience sharing from an ad account to businesses
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param ad_account_to_business_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (AdAccountToBusinessSharedAudience) with additional information such as HTTP status code, headers
    UpdateAdAccountToBusinessSharedAudienceWithHttpInfo = function(ad_account_id, ad_account_to_business_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`ad_account_to_business_shared_audience_update_with_required_body`)) {
        stop("Missing required parameter `ad_account_to_business_shared_audience_update_with_required_body`.")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToBusinessSharedAudience, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToBusinessSharedAudience, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling AudienceSharingApi$UpdateAdAccountToBusinessSharedAudience, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`ad_account_to_business_shared_audience_update_with_required_body`) && is.null(`ad_account_to_business_shared_audience_update_with_required_body`)) {
        stop("Invalid value for `ad_account_to_business_shared_audience_update_with_required_body` when calling AudienceSharingApi$UpdateAdAccountToBusinessSharedAudience, `ad_account_to_business_shared_audience_update_with_required_body` is not nullable")
      }

      if (!is.null(`ad_account_to_business_shared_audience_update_with_required_body`)) {
        local_var_body <- `ad_account_to_business_shared_audience_update_with_required_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/audiences/businesses/shared"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "AdAccountToBusinessSharedAudience"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update audience sharing from a business to ad accounts
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_to_ad_account_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessToAdAccountSharedAudience
    UpdateBusinessToAdAccountSharedAudience = function(business_id, business_to_ad_account_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateBusinessToAdAccountSharedAudienceWithHttpInfo(business_id, business_to_ad_account_shared_audience_update_with_required_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update audience sharing from a business to ad accounts
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_to_ad_account_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessToAdAccountSharedAudience) with additional information such as HTTP status code, headers
    UpdateBusinessToAdAccountSharedAudienceWithHttpInfo = function(business_id, business_to_ad_account_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`business_to_ad_account_shared_audience_update_with_required_body`)) {
        stop("Missing required parameter `business_to_ad_account_shared_audience_update_with_required_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$UpdateBusinessToAdAccountSharedAudience, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$UpdateBusinessToAdAccountSharedAudience, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$UpdateBusinessToAdAccountSharedAudience, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$UpdateBusinessToAdAccountSharedAudience, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`business_to_ad_account_shared_audience_update_with_required_body`) && is.null(`business_to_ad_account_shared_audience_update_with_required_body`)) {
        stop("Invalid value for `business_to_ad_account_shared_audience_update_with_required_body` when calling AudienceSharingApi$UpdateBusinessToAdAccountSharedAudience, `business_to_ad_account_shared_audience_update_with_required_body` is not nullable")
      }

      if (!is.null(`business_to_ad_account_shared_audience_update_with_required_body`)) {
        local_var_body <- `business_to_ad_account_shared_audience_update_with_required_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/audiences/ad_accounts/shared"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessToAdAccountSharedAudience"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update audience sharing between businesses
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_to_business_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return BusinessToBusinessSharedAudience
    UpdateBusinessToBusinessSharedAudience = function(business_id, business_to_business_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$UpdateBusinessToBusinessSharedAudienceWithHttpInfo(business_id, business_to_business_shared_audience_update_with_required_body, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update audience sharing between businesses
    #'
    #' @param business_id Unique identifier of the requesting business.
    #' @param business_to_business_shared_audience_update_with_required_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (BusinessToBusinessSharedAudience) with additional information such as HTTP status code, headers
    UpdateBusinessToBusinessSharedAudienceWithHttpInfo = function(business_id, business_to_business_shared_audience_update_with_required_body, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`business_to_business_shared_audience_update_with_required_body`)) {
        stop("Missing required parameter `business_to_business_shared_audience_update_with_required_body`.")
      }

      if (!missing(`business_id`) && is.null(`business_id`)) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$UpdateBusinessToBusinessSharedAudience, `business_id` is not nullable")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) > 20) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$UpdateBusinessToBusinessSharedAudience, must be smaller than or equal to 20.")
      }
      if (!is.null(`business_id`) && nchar(`business_id`) < 1) {
        stop("Invalid length for `business_id` when calling AudienceSharingApi$UpdateBusinessToBusinessSharedAudience, must be bigger than or equal to 1.")
      }
      if (!is.null(`business_id`) && !stringr::str_detect(`business_id`, "^\\d+$")) {
        stop("Invalid value for `business_id` when calling AudienceSharingApi$UpdateBusinessToBusinessSharedAudience, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`business_to_business_shared_audience_update_with_required_body`) && is.null(`business_to_business_shared_audience_update_with_required_body`)) {
        stop("Invalid value for `business_to_business_shared_audience_update_with_required_body` when calling AudienceSharingApi$UpdateBusinessToBusinessSharedAudience, `business_to_business_shared_audience_update_with_required_body` is not nullable")
      }

      if (!is.null(`business_to_business_shared_audience_update_with_required_body`)) {
        local_var_body <- `business_to_business_shared_audience_update_with_required_body`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/businesses/{business_id}/audiences/businesses/shared"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{business_id\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "biz_access:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "BusinessToBusinessSharedAudience"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
