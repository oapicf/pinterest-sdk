#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.14.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ConversionEvents operations
#' @description ConversionEventsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  EventsCreate  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_conversion_events <- ConversionEvents$new(c(ConversionEvents_data_inner$new("event_name_example", "action_source_example", 123, "event_id_example", ConversionEventsUserData$new(c("em_example"), c("hashed_maids_example"), "client_ip_address_example", "client_user_agent_example", c("ph_example"), c("ge_example"), c("db_example"), c("ln_example"), c("fn_example"), c("ct_example"), c("st_example"), c("zp_example"), c("country_example"), c("external_id_example"), "click_id_example", "partner_id_example"), "event_source_url_example", "opt_out_example", "partner_name_example", ConversionEvents_data_inner_custom_data$new("currency_example", "value_example", c("content_ids_example"), "content_name_example", "content_category_example", "content_brand_example", c(ConversionEvents_data_inner_custom_data_contents_inner$new("id_example", "item_price_example", 123, "item_name_example", "item_category_example", "item_brand_example")), 123, "order_id_example", "search_string_example", "opt_out_type_example", "np_example"), "app_id_example", "app_name_example", "app_version_example", "device_brand_example", "device_carrier_example", "device_model_example", "device_type_example", "os_version_example", "wifi_example", "language_example"))) # ConversionEvents | Conversion events.
#' var_test <- "test_example" # character | Include query param ?test=true to mark the request as a test request. The events will not be recorded but the API will still return the same response messages. Use this mode to verify your requests are working and your events are constructed correctly. Warning: If you use this query parameter, be certain that it is off (set to false or deleted) before sending a legitimate (non-testing) request. (Optional)
#'
#' #Send conversions
#' api_instance <- ConversionEventsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # Configure HTTP bearer authorization: conversion_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$EventsCreate(var_ad_account_id, var_conversion_events, test = var_testdata_file = "result.txt")
#' result <- api_instance$EventsCreate(var_ad_account_id, var_conversion_events, test = var_test)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ConversionEventsApi <- R6::R6Class(
  "ConversionEventsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new ConversionEventsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Send conversions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param conversion_events Conversion events.
    #' @param test (optional) Include query param ?test=true to mark the request as a test request. The events will not be recorded but the API will still return the same response messages. Use this mode to verify your requests are working and your events are constructed correctly. Warning: If you use this query parameter, be certain that it is off (set to false or deleted) before sending a legitimate (non-testing) request.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ConversionApiResponse
    EventsCreate = function(ad_account_id, conversion_events, test = NULL, data_file = NULL, ...) {
      local_var_response <- self$EventsCreateWithHttpInfo(ad_account_id, conversion_events, test, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Send conversions
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param conversion_events Conversion events.
    #' @param test (optional) Include query param ?test=true to mark the request as a test request. The events will not be recorded but the API will still return the same response messages. Use this mode to verify your requests are working and your events are constructed correctly. Warning: If you use this query parameter, be certain that it is off (set to false or deleted) before sending a legitimate (non-testing) request.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ConversionApiResponse) with additional information such as HTTP status code, headers
    EventsCreateWithHttpInfo = function(ad_account_id, conversion_events, test = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (missing(`conversion_events`)) {
        stop("Missing required parameter `conversion_events`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ConversionEventsApi$EventsCreate, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ConversionEventsApi$EventsCreate, must conform to the pattern ^\\d+$.")
      }



      query_params[["test"]] <- `test`

      if (!is.null(`conversion_events`)) {
        local_var_body <- `conversion_events`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/ad_accounts/{ad_account_id}/events"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ConversionApiResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
