#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CatalogReports operations
#' @description CatalogReportsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ReportsCreate  ####################
#'
#' library(openapi)
#' var_catalogs_report_parameters <- CatalogsReportParameters$new(CatalogsType$new(), CatalogsHotelReportParameters_report$new("feed_id_example", "FEED_INGESTION_ISSUES", "processing_result_id_example", "catalog_id_example")) # CatalogsReportParameters | Request object to asynchronously create a report.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Build catalogs report
#' api_instance <- CatalogReportsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ReportsCreate(var_catalogs_report_parameters, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$ReportsCreate(var_catalogs_report_parameters, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  ReportsGet  ####################
#'
#' library(openapi)
#' var_token <- "token_example" # character | Token returned from async build report call
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#'
#' #Get catalogs report
#' api_instance <- CatalogReportsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ReportsGet(var_token, ad_account_id = var_ad_account_iddata_file = "result.txt")
#' result <- api_instance$ReportsGet(var_token, ad_account_id = var_ad_account_id)
#' dput(result)
#'
#'
#' ####################  ReportsStats  ####################
#'
#' library(openapi)
#' var_parameters <- reports_stats_parameters_parameter$new(CatalogsType$new(), CatalogsHotelReportStatsParameters_report$new("feed_id_example", "FEED_INGESTION_ISSUES", "processing_result_id_example", "catalog_id_example")) # ReportsStatsParametersParameter | Contains the parameters for report identification.
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account. (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#'
#' #List report stats
#' api_instance <- CatalogReportsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ReportsStats(var_parameters, ad_account_id = var_ad_account_id, page_size = var_page_size, bookmark = var_bookmarkdata_file = "result.txt")
#' result <- api_instance$ReportsStats(var_parameters, ad_account_id = var_ad_account_id, page_size = var_page_size, bookmark = var_bookmark)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CatalogReportsApi <- R6::R6Class(
  "CatalogReportsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CatalogReportsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Build catalogs report
    #'
    #' @param catalogs_report_parameters Request object to asynchronously create a report.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CatalogsCreateReportResponse
    ReportsCreate = function(catalogs_report_parameters, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$ReportsCreateWithHttpInfo(catalogs_report_parameters, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Build catalogs report
    #'
    #' @param catalogs_report_parameters Request object to asynchronously create a report.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CatalogsCreateReportResponse) with additional information such as HTTP status code, headers
    ReportsCreateWithHttpInfo = function(catalogs_report_parameters, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`catalogs_report_parameters`)) {
        stop("Missing required parameter `catalogs_report_parameters`.")
      }

      if (!missing(`catalogs_report_parameters`) && is.null(`catalogs_report_parameters`)) {
        stop("Invalid value for `catalogs_report_parameters` when calling CatalogReportsApi$ReportsCreate, `catalogs_report_parameters` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsCreate, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogReportsApi$ReportsCreate, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsCreate, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      if (!is.null(`catalogs_report_parameters`)) {
        local_var_body <- `catalogs_report_parameters`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/catalogs/reports"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CatalogsCreateReportResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get catalogs report
    #'
    #' @param token Token returned from async build report call
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return CatalogsReport
    ReportsGet = function(token, ad_account_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$ReportsGetWithHttpInfo(token, ad_account_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get catalogs report
    #'
    #' @param token Token returned from async build report call
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (CatalogsReport) with additional information such as HTTP status code, headers
    ReportsGetWithHttpInfo = function(token, ad_account_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token`)) {
        stop("Missing required parameter `token`.")
      }

      if (!missing(`token`) && is.null(`token`)) {
        stop("Invalid value for `token` when calling CatalogReportsApi$ReportsGet, `token` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsGet, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogReportsApi$ReportsGet, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsGet, must conform to the pattern ^\\d+$.")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["token"]] <- `token`

      local_var_url_path <- "/catalogs/reports"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "CatalogsReport"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List report stats
    #'
    #' @param parameters Contains the parameters for report identification.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return ReportsStats200Response
    ReportsStats = function(parameters, ad_account_id = NULL, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      local_var_response <- self$ReportsStatsWithHttpInfo(parameters, ad_account_id, page_size, bookmark, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List report stats
    #'
    #' @param parameters Contains the parameters for report identification.
    #' @param ad_account_id (optional) Unique identifier of an ad account.
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (ReportsStats200Response) with additional information such as HTTP status code, headers
    ReportsStatsWithHttpInfo = function(parameters, ad_account_id = NULL, page_size = 25, bookmark = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`parameters`)) {
        stop("Missing required parameter `parameters`.")
      }

      if (!missing(`parameters`) && is.null(`parameters`)) {
        stop("Invalid value for `parameters` when calling CatalogReportsApi$ReportsStats, `parameters` is not nullable")
      }

      if (!missing(`ad_account_id`) && is.null(`ad_account_id`)) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsStats, `ad_account_id` is not nullable")
      }
      if (!is.null(`ad_account_id`) && nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling CatalogReportsApi$ReportsStats, must be smaller than or equal to 18.")
      }
      if (!is.null(`ad_account_id`) && !stringr::str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling CatalogReportsApi$ReportsStats, must conform to the pattern ^\\d+$.")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling CatalogReportsApi$ReportsStats, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling CatalogReportsApi$ReportsStats, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling CatalogReportsApi$ReportsStats, must be bigger than or equal to 1.")
      }

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling CatalogReportsApi$ReportsStats, `bookmark` is not nullable")
      }

      query_params[["ad_account_id"]] <- `ad_account_id`

      query_params[["page_size"]] <- `page_size`

      query_params[["bookmark"]] <- `bookmark`

      query_params[["parameters"]] <- `parameters`

      local_var_url_path <- "/catalogs/reports/stats"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "catalogs:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "ReportsStats200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
