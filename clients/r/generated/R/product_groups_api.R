#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.12.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ProductGroups operations
#' @description ProductGroupsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ AdAccountsCatalogsProductGroupsList } \emph{ Get catalog product groups }
#' This endpoint is completely deprecated. Please use <a href='/docs/api/v5/#operation/catalogs_product_groups/list'>List product groups</a> from Catalogs API instead.
#'
#' \itemize{
#' \item \emph{ @param } ad_account_id character
#' \item \emph{ @param } feed_profile_id character
#' \item \emph{ @returnType } \link{AdAccountsCatalogsProductGroupsList200Response} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : AdAccountsCatalogsProductGroupsList200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Invalid ad account ads parameters.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | Access Denied. This can happen if account is not yet approved to operate as Merchant on Pinterest.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Merchant data not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  AdAccountsCatalogsProductGroupsList  ####################
#'
#' library(openapi)
#' var_ad_account_id <- "ad_account_id_example" # character | Unique identifier of an ad account.
#' var_feed_profile_id <- "feed_profile_id_example" # character | The feed profile id whose catalog product groups we want to return. (Optional)
#'
#' #Get catalog product groups
#' api_instance <- ProductGroupsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AdAccountsCatalogsProductGroupsList(var_ad_account_id, feed_profile_id = var_feed_profile_iddata_file = "result.txt")
#' result <- api_instance$AdAccountsCatalogsProductGroupsList(var_ad_account_id, feed_profile_id = var_feed_profile_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ProductGroupsApi <- R6::R6Class(
  "ProductGroupsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ProductGroupsApi.
    #'
    #' @description
    #' Initialize a new ProductGroupsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get catalog product groups
    #'
    #' @description
    #' Get catalog product groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param feed_profile_id (optional) The feed profile id whose catalog product groups we want to return.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AdAccountsCatalogsProductGroupsList200Response
    #' @export
    AdAccountsCatalogsProductGroupsList = function(ad_account_id, feed_profile_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$AdAccountsCatalogsProductGroupsListWithHttpInfo(ad_account_id, feed_profile_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get catalog product groups
    #'
    #' @description
    #' Get catalog product groups
    #'
    #' @param ad_account_id Unique identifier of an ad account.
    #' @param feed_profile_id (optional) The feed profile id whose catalog product groups we want to return.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AdAccountsCatalogsProductGroupsList200Response) with additional information such as HTTP status code, headers
    #' @export
    AdAccountsCatalogsProductGroupsListWithHttpInfo = function(ad_account_id, feed_profile_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ad_account_id`)) {
        stop("Missing required parameter `ad_account_id`.")
      }

      if (nchar(`ad_account_id`) > 18) {
        stop("Invalid length for `ad_account_id` when calling ProductGroupsApi$AdAccountsCatalogsProductGroupsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`ad_account_id`, "^\\d+$")) {
        stop("Invalid value for `ad_account_id` when calling ProductGroupsApi$AdAccountsCatalogsProductGroupsList, must conform to the pattern ^\\d+$.")
      }

      if (nchar(`feed_profile_id`) > 18) {
        stop("Invalid length for `feed_profile_id` when calling ProductGroupsApi$AdAccountsCatalogsProductGroupsList, must be smaller than or equal to 18.")
      }
      if (!str_detect(`feed_profile_id`, "^\\d+$")) {
        stop("Invalid value for `feed_profile_id` when calling ProductGroupsApi$AdAccountsCatalogsProductGroupsList, must conform to the pattern ^\\d+$.")
      }

      query_params[["feed_profile_id"]] <- `feed_profile_id`

      local_var_url_path <- "/ad_accounts/{ad_account_id}/product_groups/catalogs"
      if (!missing(`ad_account_id`)) {
        local_var_url_path <- gsub("\\{ad_account_id\\}", URLencode(as.character(`ad_account_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AdAccountsCatalogsProductGroupsList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
