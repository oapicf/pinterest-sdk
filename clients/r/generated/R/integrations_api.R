#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.23.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Integrations operations
#' @description IntegrationsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  IntegrationsCommerceDel  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#'
#' #Delete commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$IntegrationsCommerceDel(var_external_business_id)
#'
#'
#' ####################  IntegrationsCommerceGet  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#'
#' #Get commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommerceGet(var_external_business_iddata_file = "result.txt")
#' result <- api_instance$IntegrationsCommerceGet(var_external_business_id)
#' dput(result)
#'
#'
#' ####################  IntegrationsCommercePatch  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#' var_integration_request_patch <- IntegrationRequestPatch$new("additional_id_1_example", "connected_advertiser_id_example", "connected_lba_id_example", "connected_merchant_id_example", "connected_tag_id_example", "partner_access_token_example", 123, "partner_metadata_example", "partner_primary_email_example", "partner_refresh_token_example", 123, "scopes_example") # IntegrationRequestPatch | Parameters to get create/update the Integration Metadata
#'
#' #Update commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommercePatch(var_external_business_id, var_integration_request_patchdata_file = "result.txt")
#' result <- api_instance$IntegrationsCommercePatch(var_external_business_id, var_integration_request_patch)
#' dput(result)
#'
#'
#' ####################  IntegrationsCommercePost  ####################
#'
#' library(openapi)
#' var_integration_request <- IntegrationRequest$new("additional_id_1_example", "connected_advertiser_id_example", "connected_lba_id_example", "connected_merchant_id_example", "connected_tag_id_example", "external_business_id_example", "partner_access_token_example", 123, "partner_metadata_example", "partner_primary_email_example", "partner_refresh_token_example", 123, "scopes_example") # IntegrationRequest | Parameters to get create/update the Integration Metadata
#'
#' #Create commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommercePost(var_integration_requestdata_file = "result.txt")
#' result <- api_instance$IntegrationsCommercePost(var_integration_request)
#' dput(result)
#'
#'
#' ####################  IntegrationsGetById  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | Integration ID.
#'
#' #Get integration metadata
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsGetById(var_iddata_file = "result.txt")
#' result <- api_instance$IntegrationsGetById(var_id)
#' dput(result)
#'
#'
#' ####################  IntegrationsGetList  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get integration metadata list
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsGetList(bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$IntegrationsGetList(bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  IntegrationsLogsPost  ####################
#'
#' library(openapi)
#' var_integration_logs_request <- IntegrationLogsRequest$new(c(IntegrationLog$new(123, "APP", "INFO", "advertiser_id_example", "app_version_number_example", IntegrationLogClientError$new("cause_example", 123, "file_name_example", 123, "message_example", "message_detail_example", "name_example", 123, "stack_trace_example"), "external_business_id_example", "feed_profile_id_example", "merchant_id_example", "message_example", "platform_version_number_example", IntegrationLogClientRequest$new("host_example", "GET", "path_example", c(key = "inner_example"), c(key = "inner_example"), 123), "tag_id_example"))) # IntegrationLogsRequest | Ingest log information from external integration application.
#'
#' #Receives batched logs from integration applications.
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsLogsPost(var_integration_logs_requestdata_file = "result.txt")
#' result <- api_instance$IntegrationsLogsPost(var_integration_logs_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
IntegrationsApi <- R6::R6Class(
  "IntegrationsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new IntegrationsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Delete commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param ... Other optional arguments
    #'
    #' @return void
    IntegrationsCommerceDel = function(external_business_id, ...) {
      local_var_response <- self$IntegrationsCommerceDelWithHttpInfo(external_business_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    IntegrationsCommerceDelWithHttpInfo = function(external_business_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }

      if (!missing(`external_business_id`) && is.null(`external_business_id`)) {
        stop("Invalid value for `external_business_id` when calling IntegrationsApi$IntegrationsCommerceDel, `external_business_id` is not nullable")
      }

      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationMetadata
    IntegrationsCommerceGet = function(external_business_id, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommerceGetWithHttpInfo(external_business_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    IntegrationsCommerceGetWithHttpInfo = function(external_business_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }

      if (!missing(`external_business_id`) && is.null(`external_business_id`)) {
        stop("Invalid value for `external_business_id` when calling IntegrationsApi$IntegrationsCommerceGet, `external_business_id` is not nullable")
      }

      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationMetadata"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param integration_request_patch Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationMetadata
    IntegrationsCommercePatch = function(external_business_id, integration_request_patch, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommercePatchWithHttpInfo(external_business_id, integration_request_patch, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param integration_request_patch Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    IntegrationsCommercePatchWithHttpInfo = function(external_business_id, integration_request_patch, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }

      if (missing(`integration_request_patch`)) {
        stop("Missing required parameter `integration_request_patch`.")
      }

      if (!missing(`external_business_id`) && is.null(`external_business_id`)) {
        stop("Invalid value for `external_business_id` when calling IntegrationsApi$IntegrationsCommercePatch, `external_business_id` is not nullable")
      }

      if (!missing(`integration_request_patch`) && is.null(`integration_request_patch`)) {
        stop("Invalid value for `integration_request_patch` when calling IntegrationsApi$IntegrationsCommercePatch, `integration_request_patch` is not nullable")
      }

      if (!is.null(`integration_request_patch`)) {
        local_var_body <- `integration_request_patch`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationMetadata"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create commerce integration
    #'
    #' @param integration_request Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationMetadata
    IntegrationsCommercePost = function(integration_request, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommercePostWithHttpInfo(integration_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create commerce integration
    #'
    #' @param integration_request Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    IntegrationsCommercePostWithHttpInfo = function(integration_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`integration_request`)) {
        stop("Missing required parameter `integration_request`.")
      }

      if (!missing(`integration_request`) && is.null(`integration_request`)) {
        stop("Invalid value for `integration_request` when calling IntegrationsApi$IntegrationsCommercePost, `integration_request` is not nullable")
      }

      if (!is.null(`integration_request`)) {
        local_var_body <- `integration_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/integrations/commerce"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationMetadata"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get integration metadata
    #'
    #' @param id Integration ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationRecord
    IntegrationsGetById = function(id, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsGetByIdWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get integration metadata
    #'
    #' @param id Integration ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationRecord) with additional information such as HTTP status code, headers
    IntegrationsGetByIdWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (!missing(`id`) && is.null(`id`)) {
        stop("Invalid value for `id` when calling IntegrationsApi$IntegrationsGetById, `id` is not nullable")
      }

      local_var_url_path <- "/integrations/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationRecord"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get integration metadata list
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationsGetList200Response
    IntegrationsGetList = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsGetListWithHttpInfo(bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get integration metadata list
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationsGetList200Response) with additional information such as HTTP status code, headers
    IntegrationsGetListWithHttpInfo = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bookmark`) && is.null(`bookmark`)) {
        stop("Invalid value for `bookmark` when calling IntegrationsApi$IntegrationsGetList, `bookmark` is not nullable")
      }

      if (!missing(`page_size`) && is.null(`page_size`)) {
        stop("Invalid value for `page_size` when calling IntegrationsApi$IntegrationsGetList, `page_size` is not nullable")
      }
      if (!is.null(`page_size`) && `page_size` >  250) {
        stop("Invalid value for `page_size` when calling IntegrationsApi$IntegrationsGetList, must be smaller than or equal to 250.")
      }
      if (!is.null(`page_size`) && `page_size` <  1) {
        stop("Invalid value for `page_size` when calling IntegrationsApi$IntegrationsGetList, must be bigger than or equal to 1.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/integrations"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationsGetList200Response"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Receives batched logs from integration applications.
    #'
    #' @param integration_logs_request Ingest log information from external integration application.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return IntegrationLogsSuccessResponse
    IntegrationsLogsPost = function(integration_logs_request, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsLogsPostWithHttpInfo(integration_logs_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Receives batched logs from integration applications.
    #'
    #' @param integration_logs_request Ingest log information from external integration application.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (IntegrationLogsSuccessResponse) with additional information such as HTTP status code, headers
    IntegrationsLogsPostWithHttpInfo = function(integration_logs_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`integration_logs_request`)) {
        stop("Missing required parameter `integration_logs_request`.")
      }

      if (!missing(`integration_logs_request`) && is.null(`integration_logs_request`)) {
        stop("Invalid value for `integration_logs_request` when calling IntegrationsApi$IntegrationsLogsPost, `integration_logs_request` is not nullable")
      }

      if (!is.null(`integration_logs_request`)) {
        local_var_body <- `integration_logs_request`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/integrations/logs"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "IntegrationLogsSuccessResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
