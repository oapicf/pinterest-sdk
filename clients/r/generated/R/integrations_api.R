#' Pinterest REST API
#'
#' Pinterest's REST API
#'
#' The version of the OpenAPI document: 5.12.0
#' Contact: blah+oapicf@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Integrations operations
#' @description IntegrationsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ IntegrationsCommerceDel } \emph{ Delete commerce integration }
#' Delete commerce integration metadata for the given external business ID. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } external_business_id character
#'
#'
#' \item status code : 204 | Commerce Integration deleted successfully
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsCommerceGet } \emph{ Get commerce integration }
#' Get commerce integration metadata associated with the given external business ID. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } external_business_id character
#' \item \emph{ @returnType } \link{IntegrationMetadata} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : IntegrationMetadata
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Integration not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Can't access this integration metadata.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsCommercePatch } \emph{ Update commerce integration }
#' Update commerce integration metadata for the given external business ID. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } external_business_id character
#' \item \emph{ @param } integration_request_patch \link{IntegrationRequestPatch}
#' \item \emph{ @returnType } \link{IntegrationMetadata} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : IntegrationMetadata
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Integration not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Can't access this integration metadata.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsCommercePost } \emph{ Create commerce integration }
#' Create commerce integration metadata to link an external business ID with a Pinterest merchant & ad account. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } integration_request \link{IntegrationRequest}
#' \item \emph{ @returnType } \link{IntegrationMetadata} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : IntegrationMetadata
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Integration not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 409 | Can't access this integration metadata.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsGetById } \emph{ Get integration metadata }
#' Get integration metadata by ID. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{IntegrationRecord} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : IntegrationRecord
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Integration not found.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsGetList } \emph{ Get integration metadata list }
#' Get integration metadata list. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } bookmark character
#' \item \emph{ @param } page_size integer
#' \item \emph{ @returnType } \link{IntegrationsGetList200Response} \cr
#'
#'
#' \item status code : 200 | Success
#'
#' \item return type : IntegrationsGetList200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error.
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IntegrationsLogsPost } \emph{ Receives batched logs from integration applications. }
#' This endpoint receives batched logs from integration applications on partner platforms. Note: If you're interested in joining the beta, please reach out to your Pinterest account manager.
#'
#' \itemize{
#' \item \emph{ @param } integration_logs_request \link{IntegrationLogsRequest}
#' \item \emph{ @returnType } \link{IntegrationLogsSuccessResponse} \cr
#'
#'
#' \item status code : 200 | Success.
#'
#' \item return type : IntegrationLogsSuccessResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Bad request.
#'
#' \item return type : DetailedError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 0 | Unexpected error
#'
#' \item return type : Error
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  IntegrationsCommerceDel  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#'
#' #Delete commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' api_instance$IntegrationsCommerceDel(var_external_business_id)
#'
#'
#' ####################  IntegrationsCommerceGet  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#'
#' #Get commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommerceGet(var_external_business_iddata_file = "result.txt")
#' result <- api_instance$IntegrationsCommerceGet(var_external_business_id)
#' dput(result)
#'
#'
#' ####################  IntegrationsCommercePatch  ####################
#'
#' library(openapi)
#' var_external_business_id <- "external_business_id_example" # character | External business ID for the integration.
#' var_integration_request_patch <- IntegrationRequestPatch$new("connected_merchant_id_example", "connected_advertiser_id_example", "connected_lba_id_example", "connected_tag_id_example", "partner_access_token_example", "partner_refresh_token_example", "partner_primary_email_example", 123, 123, "scopes_example", "additional_id_1_example", "partner_metadata_example") # IntegrationRequestPatch | Parameters to get create/update the Integration Metadata (Optional)
#'
#' #Update commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommercePatch(var_external_business_id, integration_request_patch = var_integration_request_patchdata_file = "result.txt")
#' result <- api_instance$IntegrationsCommercePatch(var_external_business_id, integration_request_patch = var_integration_request_patch)
#' dput(result)
#'
#'
#' ####################  IntegrationsCommercePost  ####################
#'
#' library(openapi)
#' var_integration_request <- IntegrationRequest$new("external_business_id_example", "connected_merchant_id_example", "connected_advertiser_id_example", "connected_lba_id_example", "connected_tag_id_example", "partner_access_token_example", "partner_refresh_token_example", "partner_primary_email_example", 123, 123, "scopes_example", "additional_id_1_example", "partner_metadata_example") # IntegrationRequest | Parameters to get create/update the Integration Metadata (Optional)
#'
#' #Create commerce integration
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsCommercePost(integration_request = var_integration_requestdata_file = "result.txt")
#' result <- api_instance$IntegrationsCommercePost(integration_request = var_integration_request)
#' dput(result)
#'
#'
#' ####################  IntegrationsGetById  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | Integration ID.
#'
#' #Get integration metadata
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsGetById(var_iddata_file = "result.txt")
#' result <- api_instance$IntegrationsGetById(var_id)
#' dput(result)
#'
#'
#' ####################  IntegrationsGetList  ####################
#'
#' library(openapi)
#' var_bookmark <- "bookmark_example" # character | Cursor used to fetch the next page of items (Optional)
#' var_page_size <- 25 # integer | Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (Optional)
#'
#' #Get integration metadata list
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsGetList(bookmark = var_bookmark, page_size = var_page_sizedata_file = "result.txt")
#' result <- api_instance$IntegrationsGetList(bookmark = var_bookmark, page_size = var_page_size)
#' dput(result)
#'
#'
#' ####################  IntegrationsLogsPost  ####################
#'
#' library(openapi)
#' var_integration_logs_request <- IntegrationLogsRequest$new(c(IntegrationLog$new(123, "APP", "INFO", "external_business_id_example", "advertiser_id_example", "merchant_id_example", "tag_id_example", "feed_profile_id_example", "message_example", "app_version_number_example", "platform_version_number_example", IntegrationLogClientError$new("cause_example", 123, "file_name_example", 123, "message_example", "message_detail_example", "name_example", 123, "stack_trace_example"), IntegrationLogClientRequest$new("GET", "host_example", "path_example", c(key = "inner_example"), c(key = "inner_example"), 123)))) # IntegrationLogsRequest | Ingest log information from external integration application.
#'
#' #Receives batched logs from integration applications.
#' api_instance <- IntegrationsApi$new()
#'
#' # Configure OAuth2 access token for authorization: pinterest_oauth2
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IntegrationsLogsPost(var_integration_logs_requestdata_file = "result.txt")
#' result <- api_instance$IntegrationsLogsPost(var_integration_logs_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
IntegrationsApi <- R6::R6Class(
  "IntegrationsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new IntegrationsApi.
    #'
    #' @description
    #' Initialize a new IntegrationsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Delete commerce integration
    #'
    #' @description
    #' Delete commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    IntegrationsCommerceDel = function(external_business_id, ...) {
      local_var_response <- self$IntegrationsCommerceDelWithHttpInfo(external_business_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete commerce integration
    #'
    #' @description
    #' Delete commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsCommerceDelWithHttpInfo = function(external_business_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }


      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get commerce integration
    #'
    #' @description
    #' Get commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationMetadata
    #' @export
    IntegrationsCommerceGet = function(external_business_id, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommerceGetWithHttpInfo(external_business_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get commerce integration
    #'
    #' @description
    #' Get commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsCommerceGetWithHttpInfo = function(external_business_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }


      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationMetadata", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Update commerce integration
    #'
    #' @description
    #' Update commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param integration_request_patch (optional) Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationMetadata
    #' @export
    IntegrationsCommercePatch = function(external_business_id, integration_request_patch = NULL, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommercePatchWithHttpInfo(external_business_id, integration_request_patch, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Update commerce integration
    #'
    #' @description
    #' Update commerce integration
    #'
    #' @param external_business_id External business ID for the integration.
    #' @param integration_request_patch (optional) Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsCommercePatchWithHttpInfo = function(external_business_id, integration_request_patch = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`external_business_id`)) {
        stop("Missing required parameter `external_business_id`.")
      }



      if (!is.null(`integration_request_patch`)) {
        local_var_body <- `integration_request_patch`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/integrations/commerce/{external_business_id}"
      if (!missing(`external_business_id`)) {
        local_var_url_path <- gsub("\\{external_business_id\\}", URLencode(as.character(`external_business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationMetadata", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create commerce integration
    #'
    #' @description
    #' Create commerce integration
    #'
    #' @param integration_request (optional) Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationMetadata
    #' @export
    IntegrationsCommercePost = function(integration_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsCommercePostWithHttpInfo(integration_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create commerce integration
    #'
    #' @description
    #' Create commerce integration
    #'
    #' @param integration_request (optional) Parameters to get create/update the Integration Metadata
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationMetadata) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsCommercePostWithHttpInfo = function(integration_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!is.null(`integration_request`)) {
        local_var_body <- `integration_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/integrations/commerce"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationMetadata", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get integration metadata
    #'
    #' @description
    #' Get integration metadata
    #'
    #' @param id Integration ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationRecord
    #' @export
    IntegrationsGetById = function(id, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsGetByIdWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get integration metadata
    #'
    #' @description
    #' Get integration metadata
    #'
    #' @param id Integration ID.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationRecord) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsGetByIdWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/integrations/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationRecord", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get integration metadata list
    #'
    #' @description
    #' Get integration metadata list
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationsGetList200Response
    #' @export
    IntegrationsGetList = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsGetListWithHttpInfo(bookmark, page_size, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get integration metadata list
    #'
    #' @description
    #' Get integration metadata list
    #'
    #' @param bookmark (optional) Cursor used to fetch the next page of items
    #' @param page_size (optional) Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information. (default value: 25)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationsGetList200Response) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsGetListWithHttpInfo = function(bookmark = NULL, page_size = 25, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (`page_size` > 250) {
        stop("Invalid value for `page_size` when calling IntegrationsApi$IntegrationsGetList, must be smaller than or equal to 250.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling IntegrationsApi$IntegrationsGetList, must be bigger than or equal to 1.")
      }

      query_params[["bookmark"]] <- `bookmark`

      query_params[["page_size"]] <- `page_size`

      local_var_url_path <- "/integrations"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:read"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationsGetList200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Receives batched logs from integration applications.
    #'
    #' @description
    #' Receives batched logs from integration applications.
    #'
    #' @param integration_logs_request Ingest log information from external integration application.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IntegrationLogsSuccessResponse
    #' @export
    IntegrationsLogsPost = function(integration_logs_request, data_file = NULL, ...) {
      local_var_response <- self$IntegrationsLogsPostWithHttpInfo(integration_logs_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Receives batched logs from integration applications.
    #'
    #' @description
    #' Receives batched logs from integration applications.
    #'
    #' @param integration_logs_request Ingest log information from external integration application.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IntegrationLogsSuccessResponse) with additional information such as HTTP status code, headers
    #' @export
    IntegrationsLogsPostWithHttpInfo = function(integration_logs_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`integration_logs_request`)) {
        stop("Missing required parameter `integration_logs_request`.")
      }


      if (!is.null(`integration_logs_request`)) {
        local_var_body <- `integration_logs_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/integrations/logs"
      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "ads:write"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IntegrationLogsSuccessResponse", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
