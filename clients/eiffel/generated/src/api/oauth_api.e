note
 description:"[
		Pinterest REST API
 		Pinterest's REST API
  		The version of the OpenAPI document: 5.28.0
 	    Contact: blah+oapicf@cliffano.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	OAUTH_API

inherit

    API_I


feature -- API Access


	oauth_conversion_token : detachable CONVERSION_ACCESS_TOKEN
			-- Generate OAuth access token for conversion API
			-- Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
			-- 
			-- 
			-- Result CONVERSION_ACCESS_TOKEN
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/oauth/conversion_token"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"pinterest_oauth2">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CONVERSION_ACCESS_TOKEN } l_response.data ({ CONVERSION_ACCESS_TOKEN }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	oauth_token (grant_type: TOKEN_GRANT_TYPE; code: STRING_32; continuous_refresh: STRING_32; redirect_uri: STRING_32; refresh_token: STRING_32; scope: STRING_32): detachable OAUTH_ACCESS_TOKEN
			-- Generate OAuth access token
			-- Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the &#x60;continuous_refresh&#x60; parameter to &#x60;true&#x60; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 
			-- 
			-- argument: grant_type  (required)
			-- 
			-- argument: code  (optional, default to null)
			-- 
			-- argument: continuous_refresh   If your app was created before **September 25, 2025**, set to &#x60;true&#x60; to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token. (optional, default to null)
			-- 
			-- argument: redirect_uri  (optional, default to null)
			-- 
			-- argument: refresh_token  (optional, default to null)
			-- 
			-- argument: scope  (optional, default to null)
			-- 
			-- 
			-- Result OAUTH_ACCESS_TOKEN
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/oauth/token"

			if attached code as l_code then
				l_request.add_form(l_code,"code");
			end
			if attached continuous_refresh as l_continuous_refresh then
				l_request.add_form(l_continuous_refresh,"continuous_refresh");
			end
			if attached grant_type as l_grant_type then
				l_request.add_form(l_grant_type,"grant_type");
			end
			if attached redirect_uri as l_redirect_uri then
				l_request.add_form(l_redirect_uri,"redirect_uri");
			end
			if attached refresh_token as l_refresh_token then
				l_request.add_form(l_refresh_token,"refresh_token");
			end
			if attached scope as l_scope then
				l_request.add_form(l_scope,"scope");
			end

			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/x-www-form-urlencoded">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"basic">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { OAUTH_ACCESS_TOKEN } l_response.data ({ OAUTH_ACCESS_TOKEN }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	token_revoke (token: STRING_32; token_type_hint: detachable TOKEN_TYPE_HINT)
			-- Revoke a token
			-- Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
			-- 
			-- argument: token The token to revoke. (required)
			-- 
			-- argument: token_type_hint The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information. (optional, default to null)
			-- 
			-- 
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/oauth/token/revoke"

			if attached token as l_token then
				l_request.add_form(l_token,"token");
			end
			if attached token_type_hint as l_token_type_hint then
				l_request.add_form(l_token_type_hint,"token_type_hint");
			end

			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/x-www-form-urlencoded">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"basic">>)
			l_response := api_client.call_api (l_path, "Post", l_request, agent serializer, Void)
			if l_response.has_error then
				last_error := l_response.error
			end
		end


end
