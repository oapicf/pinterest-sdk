/*
  Pinterest REST API

  Pinterest's REST API

  The version of the OpenAPI document: 5.14.0

  Contact: blah+oapicf@cliffano.com

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools.services.adsservice;

import "google/protobuf/empty.proto";
import public "models/ad_array_response.proto";
import public "models/ad_create_request.proto";
import public "models/ad_preview_request.proto";
import public "models/ad_preview_url_response.proto";
import public "models/ad_response.proto";
import public "models/ad_update_request.proto";
import public "models/ads_analytics_ad_targeting_type.proto";
import public "models/ads_analytics_response_inner.proto";
import public "models/ads_list200_response.proto";
import public "models/conversion_report_attribution_type.proto";
import public "models/error.proto";
import public "models/granularity.proto";
import public "models/metrics_response.proto";

service AdsService {
  rpc AdPreviewsCreate (AdPreviewsCreateRequest) returns (AdPreviewURLResponse);

  rpc AdTargetingAnalyticsGet (AdTargetingAnalyticsGetRequest) returns (MetricsResponse);

  rpc AdsAnalytics (AdsAnalyticsRequest) returns (AdsAnalyticsResponse);

  rpc AdsCreate (AdsCreateRequest) returns (AdArrayResponse);

  rpc AdsGet (AdsGetRequest) returns (AdResponse);

  rpc AdsList (AdsListRequest) returns (AdsList200Response);

  rpc AdsUpdate (AdsUpdateRequest) returns (AdArrayResponse);

}

message AdPreviewsCreateRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Create ad preview with pin or image.
  AdPreviewRequest adPreviewRequest = 2;

}

message AdTargetingAnalyticsGetRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of Ad Ids to use to filter the results.
  repeated string adIds = 2;
  // Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
  string startDate = 3;
  // Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
  string endDate = 4;
  // Targeting type breakdowns for the report. The reporting per targeting type <br> is independent from each other. [\"AGE_BUCKET_AND_GENDER\"] is in BETA and not yet available to all users.
  repeated AdsAnalyticsAdTargetingType targetingTypes = 5;
  // Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 6;
  // TOTAL - metrics are aggregated over the specified date range.<br> DAY - metrics are broken down daily.<br> HOUR - metrics are broken down hourly.<br>WEEKLY - metrics are broken down weekly.<br>MONTHLY - metrics are broken down monthly
  Granularity granularity = 7;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 8;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 9;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 10;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 11;
  // List of types of attribution for the conversion report
  ConversionReportAttributionType attributionTypes = 12;

}

message AdsAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
  string endDate = 3;
  // Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 4;
  // TOTAL - metrics are aggregated over the specified date range.<br> DAY - metrics are broken down daily.<br> HOUR - metrics are broken down hourly.<br>WEEKLY - metrics are broken down weekly.<br>MONTHLY - metrics are broken down monthly
  Granularity granularity = 5;
  // List of Ad Ids to use to filter the results.
  repeated string adIds = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;
  // List of Pin IDs.
  repeated string pinIds = 11;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 12;

}

message AdsAnalyticsResponse {
  repeated AdsAnalyticsResponseInner data = 1;
}

message AdsCreateRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of ads to create, size limit [1, 30].
  repeated AdCreateRequest adCreateRequest = 2;

}

message AdsGetRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Unique identifier of an ad.
  string adId = 2;

}

message AdsListRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 2;
  // List of Ad group Ids to use to filter the results.
  repeated string adGroupIds = 3;
  // List of Ad Ids to use to filter the results.
  repeated string adIds = 4;
  // Entity status
  repeated string entityStatuses = 5;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/reference/pagination/'>Pagination</a> for more information.
  int32 pageSize = 6;
  // The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
  string order = 7;
  // Cursor used to fetch the next page of items
  string bookmark = 8;

}

message AdsUpdateRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of ads to update, size limit [1, 30]
  repeated AdUpdateRequest adUpdateRequest = 2;

}

