/*
  Pinterest REST API

  Pinterest's REST API

  The version of the OpenAPI document: 5.3.0

  Contact: blah@cliffano.com

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools.services.adaccountsservice;

import "google/protobuf/empty.proto";
import public "models/ads_analytics_create_async_request.proto";
import public "models/ads_analytics_create_async_response.proto";
import public "models/ads_analytics_get_async_response.proto";
import public "models/any_type.proto";
import public "models/error.proto";
import public "models/granularity.proto";
import public "models/paginated.proto";
import public "models/todo_object_mapping.proto";

service AdAccountsService {
  rpc AdAccountAnalytics (AdAccountAnalyticsRequest) returns (AdAccountAnalyticsResponse);

  rpc AdAccountsList (AdAccountsListRequest) returns (Paginated);

  rpc AdGroupsAnalytics (AdGroupsAnalyticsRequest) returns (AdGroupsAnalyticsResponse);

  rpc AdGroupsList (AdGroupsListRequest) returns (Paginated);

  rpc AdsAnalytics (AdsAnalyticsRequest) returns (AdsAnalyticsResponse);

  rpc AdsList (AdsListRequest) returns (Paginated);

  rpc AnalyticsCreateReport (AnalyticsCreateReportRequest) returns (AdsAnalyticsCreateAsyncResponse);

  rpc AnalyticsGetReport (AnalyticsGetReportRequest) returns (AdsAnalyticsGetAsyncResponse);

  rpc CampaignsAnalytics (CampaignsAnalyticsRequest) returns (CampaignsAnalyticsResponse);

  rpc CampaignsList (CampaignsListRequest) returns (Paginated);

  rpc ProductGroupsAnalytics (ProductGroupsAnalyticsRequest) returns (ProductGroupsAnalyticsResponse);

}

message AdAccountAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD
  string endDate = 3;
  // Columns to retrieve. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 4;
  // Granularity
  Granularity granularity = 5;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 6;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 7;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 8;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 9;

}

message AdAccountAnalyticsResponse {
  repeated map data = 1;
}

message AdAccountsListRequest {
  // Cursor used to fetch the next page of items
  string bookmark = 1;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/api/v5/#tag/Pagination'>Pagination</a> for more information.
  int32 pageSize = 2;
  // Include shared ad accounts
  bool includeSharedAccounts = 3;

}

message AdGroupsAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD
  string endDate = 3;
  // List of Ad group Ids to use to filter the results.
  repeated string adGroupIds = 4;
  // Columns to retrieve. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 5;
  // Granularity
  Granularity granularity = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;

}

message AdGroupsAnalyticsResponse {
  repeated map data = 1;
}

message AdGroupsListRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 2;
  // List of Ad group Ids to use to filter the results.
  repeated string adGroupIds = 3;
  // Entity status
  repeated string entityStatuses = 4;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/api/v5/#tag/Pagination'>Pagination</a> for more information.
  int32 pageSize = 5;
  // The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
  string order = 6;
  // Cursor used to fetch the next page of items
  string bookmark = 7;
  // Return interests as text names (if value is true) rather than topic IDs.
  bool translateInterestsToNames = 8;

}

message AdsAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD
  string endDate = 3;
  // List of Ad Ids to use to filter the results.
  repeated string adIds = 4;
  // Columns to retrieve. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 5;
  // Granularity
  Granularity granularity = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;

}

message AdsAnalyticsResponse {
  repeated map data = 1;
}

message AdsListRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 2;
  // List of Ad group Ids to use to filter the results.
  repeated string adGroupIds = 3;
  // List of Ad Ids to use to filter the results.
  repeated string adIds = 4;
  // Entity status
  repeated string entityStatuses = 5;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/api/v5/#tag/Pagination'>Pagination</a> for more information.
  int32 pageSize = 6;
  // The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
  string order = 7;
  // Cursor used to fetch the next page of items
  string bookmark = 8;

}

message AnalyticsCreateReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  AdsAnalyticsCreateAsyncRequest adsAnalyticsCreateAsyncRequest = 2;

}

message AnalyticsGetReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Token returned from the post request creation call
  string token = 2;

}

message CampaignsAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD
  string endDate = 3;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 4;
  // Columns to retrieve. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 5;
  // Granularity
  Granularity granularity = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;

}

message CampaignsAnalyticsResponse {
  repeated map data = 1;
}

message CampaignsListRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // List of Campaign Ids to use to filter the results.
  repeated string campaignIds = 2;
  // Entity status
  repeated string entityStatuses = 3;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/api/v5/#tag/Pagination'>Pagination</a> for more information.
  int32 pageSize = 4;
  // The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
  string order = 5;
  // Cursor used to fetch the next page of items
  string bookmark = 6;

}

message ProductGroupsAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD
  string endDate = 3;
  // List of Product group Ids to use to filter the results.
  repeated string productGroupIds = 4;
  // Columns to retrieve. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 5;
  // Granularity
  Granularity granularity = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;

}

message ProductGroupsAnalyticsResponse {
  repeated map data = 1;
}

