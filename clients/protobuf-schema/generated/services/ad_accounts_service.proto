/*
  Pinterest REST API

  Pinterest's REST API

  The version of the OpenAPI document: 5.12.0

  Contact: blah+oapicf@cliffano.com

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools.services.adaccountsservice;

import "google/protobuf/empty.proto";
import public "models/ad_account.proto";
import public "models/ad_account_analytics_response_inner.proto";
import public "models/ad_account_create_request.proto";
import public "models/ad_accounts_list200_response.proto";
import public "models/ads_analytics_create_async_request.proto";
import public "models/ads_analytics_create_async_response.proto";
import public "models/ads_analytics_get_async_response.proto";
import public "models/ads_analytics_targeting_type.proto";
import public "models/conversion_report_attribution_type.proto";
import public "models/create_mmm_report_request.proto";
import public "models/create_mmm_report_response.proto";
import public "models/error.proto";
import public "models/get_mmm_report_response.proto";
import public "models/granularity.proto";
import public "models/metrics_response.proto";
import public "models/templates_list200_response.proto";

service AdAccountsService {
  rpc AdAccountAnalytics (AdAccountAnalyticsRequest) returns (AdAccountAnalyticsResponse);

  rpc AdAccountTargetingAnalyticsGet (AdAccountTargetingAnalyticsGetRequest) returns (MetricsResponse);

  rpc AdAccountsCreate (AdAccountsCreateRequest) returns (AdAccount);

  rpc AdAccountsGet (AdAccountsGetRequest) returns (AdAccount);

  rpc AdAccountsList (AdAccountsListRequest) returns (AdAccountsList200Response);

  rpc AnalyticsCreateMmmReport (AnalyticsCreateMmmReportRequest) returns (CreateMMMReportResponse);

  rpc AnalyticsCreateReport (AnalyticsCreateReportRequest) returns (AdsAnalyticsCreateAsyncResponse);

  rpc AnalyticsCreateTemplateReport (AnalyticsCreateTemplateReportRequest) returns (AdsAnalyticsCreateAsyncResponse);

  rpc AnalyticsGetMmmReport (AnalyticsGetMmmReportRequest) returns (GetMMMReportResponse);

  rpc AnalyticsGetReport (AnalyticsGetReportRequest) returns (AdsAnalyticsGetAsyncResponse);

  rpc SandboxDelete (SandboxDeleteRequest) returns (SandboxDeleteResponse);

  rpc TemplatesList (TemplatesListRequest) returns (TemplatesList200Response);

}

message AdAccountAnalyticsRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
  string endDate = 3;
  // Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 4;
  // TOTAL - metrics are aggregated over the specified date range.<br> DAY - metrics are broken down daily.<br> HOUR - metrics are broken down hourly.<br>WEEKLY - metrics are broken down weekly.<br>MONTHLY - metrics are broken down monthly
  Granularity granularity = 5;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 6;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 7;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 8;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 9;

}

message AdAccountAnalyticsResponse {
  repeated AdAccountAnalyticsResponseInner data = 1;
}

message AdAccountTargetingAnalyticsGetRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
  string startDate = 2;
  // Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
  string endDate = 3;
  // Targeting type breakdowns for the report. The reporting per targeting type <br> is independent from each other.
  repeated AdsAnalyticsTargetingType targetingTypes = 4;
  // Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile's currency field. For USD,($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it's microdollars. Otherwise, it's in microunits of the advertiser's currency.<br/>For example, if the advertiser's currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).<br/>If a column has no value, it may not be returned
  repeated string columns = 5;
  // TOTAL - metrics are aggregated over the specified date range.<br> DAY - metrics are broken down daily.<br> HOUR - metrics are broken down hourly.<br>WEEKLY - metrics are broken down weekly.<br>MONTHLY - metrics are broken down monthly
  Granularity granularity = 6;
  // Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 clickWindowDays = 7;
  // Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  int32 engagementWindowDays = 8;
  // Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day.
  int32 viewWindowDays = 9;
  // The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  string conversionReportTime = 10;
  // List of types of attribution for the conversion report
  ConversionReportAttributionType attributionTypes = 11;

}

message AdAccountsCreateRequest {
  // Ad account to create.
  AdAccountCreateRequest adAccountCreateRequest = 1;

}

message AdAccountsGetRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;

}

message AdAccountsListRequest {
  // Cursor used to fetch the next page of items
  string bookmark = 1;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information.
  int32 pageSize = 2;
  // Include shared ad accounts
  bool includeSharedAccounts = 3;

}

message AnalyticsCreateMmmReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  CreateMMMReportRequest createMMMReportRequest = 2;

}

message AnalyticsCreateReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  AdsAnalyticsCreateAsyncRequest adsAnalyticsCreateAsyncRequest = 2;

}

message AnalyticsCreateTemplateReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Unique identifier of a template.
  string templateId = 2;
  // Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 2.5 years back from today.
  string startDate = 3;
  // Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 2.5 years past start date.
  string endDate = 4;
  // TOTAL - metrics are aggregated over the specified date range.<br> DAY - metrics are broken down daily.<br> HOUR - metrics are broken down hourly.<br>WEEKLY - metrics are broken down weekly.<br>MONTHLY - metrics are broken down monthly
  Granularity granularity = 5;

}

message AnalyticsGetMmmReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Token returned from the post request creation call
  string token = 2;

}

message AnalyticsGetReportRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Token returned from the post request creation call
  string token = 2;

}

message SandboxDeleteRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;

}

message SandboxDeleteResponse {
  string data = 1;
}

message TemplatesListRequest {
  // Unique identifier of an ad account.
  string adAccountId = 1;
  // Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information.
  int32 pageSize = 2;
  // The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
  string order = 3;
  // Cursor used to fetch the next page of items
  string bookmark = 4;

}

