/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "SchedulesApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

SchedulesApiException::SchedulesApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int SchedulesApiException::getStatus() const
{
    return m_status;
}
const char* SchedulesApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

SchedulesApiAd_accountsAd_account_idSchedulesResource::SchedulesApiAd_accountsAd_account_idSchedulesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/schedules/");
	this->set_method_handler("POST",
		std::bind(&SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

SchedulesApiAd_accountsAd_account_idSchedulesResource::~SchedulesApiAd_accountsAd_account_idSchedulesResource()
{
}

std::pair<int, std::string> SchedulesApiAd_accountsAd_account_idSchedulesResource::handleSchedulesApiException(const SchedulesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> SchedulesApiAd_accountsAd_account_idSchedulesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> SchedulesApiAd_accountsAd_account_idSchedulesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void SchedulesApiAd_accountsAd_account_idSchedulesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void SchedulesApiAd_accountsAd_account_idSchedulesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void SchedulesApiAd_accountsAd_account_idSchedulesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto scheduleCreate = extractJsonArrayBodyParam<ScheduleCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::vector<std::shared_ptr<Schedules_create_200_response_inner>> resultObject = std::vector<std::shared_ptr<Schedules_create_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, scheduleCreate);
    }
    catch(const SchedulesApiException& e) {
        std::tie(status_code, result) = handleSchedulesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Schedules_list_200_response> resultObject = std::make_shared<Schedules_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, entityIds, bookmark, pageSize, order, scheduleStatuses, scheduleType);
    }
    catch(const SchedulesApiException& e) {
        std::tie(status_code, result) = handleSchedulesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto scheduleBatchUpdate = extractJsonArrayBodyParam<ScheduleBatchUpdate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::vector<std::shared_ptr<Schedules_create_200_response_inner>> resultObject = std::vector<std::shared_ptr<Schedules_create_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, scheduleBatchUpdate);
    }
    catch(const SchedulesApiException& e) {
        std::tie(status_code, result) = handleSchedulesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::vector<std::shared_ptr<Schedules_create_200_response_inner>>> SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_POST(
        std::string const & adAccountId, std::vector<std::shared_ptr<ScheduleCreate>> const & scheduleCreate)
{
    throw SchedulesApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Schedules_list_200_response>> SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_GET(
    std::string const & adAccountId, std::vector<std::shared_ptr<std::string>> const & entityIds, std::string const & bookmark, int32_t const & pageSize, std::shared_ptr<Pinterest_Lib_PaginationOrder> const & order, std::vector<std::shared_ptr<ScheduleStatus>> const & scheduleStatuses, std::shared_ptr<ScheduleType> const & scheduleType)
{
    throw SchedulesApiException(501, "Not implemented");
}
std::pair<int, std::vector<std::shared_ptr<Schedules_create_200_response_inner>>> SchedulesApiAd_accountsAd_account_idSchedulesResource::handler_PATCH(
    std::string const & adAccountId, std::vector<std::shared_ptr<ScheduleBatchUpdate>> const & scheduleBatchUpdate)
{
    throw SchedulesApiException(501, "Not implemented");
}

std::string SchedulesApiAd_accountsAd_account_idSchedulesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

SchedulesApi::SchedulesApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

SchedulesApi::~SchedulesApi() {}

void SchedulesApi::setSchedulesApiAd_accountsAd_account_idSchedulesResource(std::shared_ptr<SchedulesApiAd_accountsAd_account_idSchedulesResource> spSchedulesApiAd_accountsAd_account_idSchedulesResource) {
    m_spSchedulesApiAd_accountsAd_account_idSchedulesResource = spSchedulesApiAd_accountsAd_account_idSchedulesResource;
    m_service->publish(m_spSchedulesApiAd_accountsAd_account_idSchedulesResource);
}


void SchedulesApi::publishDefaultResources() {
    if (!m_spSchedulesApiAd_accountsAd_account_idSchedulesResource) {
        setSchedulesApiAd_accountsAd_account_idSchedulesResource(std::make_shared<SchedulesApiAd_accountsAd_account_idSchedulesResource>());
    }
}

std::shared_ptr<restbed::Service> SchedulesApi::service() {
    return m_service;
}


}
}
}
}

