/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CatalogSupplementalApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

CatalogSupplementalApiException::CatalogSupplementalApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CatalogSupplementalApiException::getStatus() const
{
    return m_status;
}
const char* CatalogSupplementalApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_inventory_items/batch/");
	this->set_method_handler("POST",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::~CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource()
{
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto localInventoryItemsBatchCreate = extractJsonModelBodyParam<LocalInventoryItemsBatchCreate>(bodyContent);

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<SupplementalItemsBatchResponse> resultObject = std::make_shared<SupplementalItemsBatchResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(catalogId, localInventoryItemsBatchCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<SupplementalItemsBatchResponse>> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST(
        std::string const & catalogId, std::shared_ptr<LocalInventoryItemsBatchCreate> const & localInventoryItemsBatchCreate, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}


std::string CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_inventory_items/query/");
	this->set_method_handler("POST",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::~CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource()
{
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto localInventoryItemsGetCreate = extractJsonModelBodyParam<LocalInventoryItemsGetCreate>(bodyContent);

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<LocalInventoryItemsGet> resultObject = std::make_shared<LocalInventoryItemsGet>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(catalogId, localInventoryItemsGetCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<LocalInventoryItemsGet>> CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST(
        std::string const & catalogId, std::shared_ptr<LocalInventoryItemsGetCreate> const & localInventoryItemsGetCreate, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}


std::string CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_stores/");
	this->set_method_handler("POST",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("DELETE",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::~CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource()
{
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto localStoreCreate = extractJsonArrayBodyParam<LocalStoreCreate>(bodyContent);

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>> resultObject = std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(catalogId, localStoreCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId_x_extension(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::vector<std::shared_ptr<Catalogs_local_stores_delete_200_response_inner>> resultObject = std::vector<std::shared_ptr<Catalogs_local_stores_delete_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_DELETE(catalogId, ids, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 204) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 204, result.empty() ? "Resource deleted successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId_x_extension(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);

    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Catalogs_local_stores_list_200_response> resultObject = std::make_shared<Catalogs_local_stores_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(catalogId, ids, adAccountId, bookmark, pageSize);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto catalogId = std::make_shared<>(bodyContent);
    auto localStoreBatchUpdate = extractJsonArrayBodyParam<LocalStoreBatchUpdate>(bodyContent);
    auto adAccountId = std::make_shared<>(bodyContent);

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId_x_extension(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>> resultObject = std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(catalogId, localStoreBatchUpdate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>>> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_POST(
        std::string const & catalogId, std::vector<std::shared_ptr<LocalStoreCreate>> const & localStoreCreate, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}

std::pair<int, std::vector<std::shared_ptr<Catalogs_local_stores_delete_200_response_inner>>> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_DELETE(
    std::string const & catalogId, std::vector<std::shared_ptr<std::string>> const & ids, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<Catalogs_local_stores_list_200_response>> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_GET(
    std::string const & catalogId, std::vector<std::shared_ptr<std::string>> const & ids, std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}
std::pair<int, std::vector<std::shared_ptr<Catalogs_local_stores_create_200_response_inner>>> CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::handler_PATCH(
    std::string const & catalogId, std::vector<std::shared_ptr<LocalStoreBatchUpdate>> const & localStoreBatchUpdate, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}

std::string CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/supplemental_items/batch/{batch_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::~CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource()
{
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string catalogId = getPathParam_catalogId(request);
    const std::string batchId = getPathParam_batchId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<SupplementalItemsBatchResponse> resultObject = std::make_shared<SupplementalItemsBatchResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(catalogId, batchId, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<SupplementalItemsBatchResponse>> CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET(
        std::string const & catalogId, std::string const & batchId, std::string const & adAccountId)
{
    throw CatalogSupplementalApiException(501, "Not implemented");
}


std::string CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

CatalogSupplementalApi::CatalogSupplementalApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CatalogSupplementalApi::~CatalogSupplementalApi() {}

void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource(std::shared_ptr<CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource> spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource) {
    m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource = spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource;
    m_service->publish(m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource(std::shared_ptr<CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource> spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource) {
    m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource = spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource;
    m_service->publish(m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource(std::shared_ptr<CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource> spCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource) {
    m_spCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource = spCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource;
    m_service->publish(m_spCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource(std::shared_ptr<CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource> spCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource) {
    m_spCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource = spCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource;
    m_service->publish(m_spCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource);
}


void CatalogSupplementalApi::publishDefaultResources() {
    if (!m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource) {
        setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource(std::make_shared<CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource>());
    }
    if (!m_spCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource) {
        setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource(std::make_shared<CatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource>());
    }
    if (!m_spCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource) {
        setCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource(std::make_shared<CatalogSupplementalApiCatalogsCatalog_idLocal_storesResource>());
    }
    if (!m_spCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource) {
        setCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource(std::make_shared<CatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource>());
    }
}

std::shared_ptr<restbed::Service> CatalogSupplementalApi::service() {
    return m_service;
}


}
}
}
}

