/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "KeywordsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

KeywordsApiException::KeywordsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int KeywordsApiException::getStatus() const
{
    return m_status;
}
const char* KeywordsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/keywords/metrics/");
	this->set_method_handler("GET",
		std::bind(&KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::~KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource()
{
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string countryCode = getQueryParam_countryCode(request);


    int status_code = 500;
    std::shared_ptr<KeywordsMetricsArrayResponse> resultObject = std::make_shared<KeywordsMetricsArrayResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adAccountId, countryCode, keywords);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<KeywordsMetricsArrayResponse>> KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::handler_GET(
        std::string const & adAccountId, std::string const & countryCode, std::vector<std::string> const & keywords)
{
    throw KeywordsApiException(501, "Not implemented");
}


std::string KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
KeywordsApiAd_accountsAd_account_idKeywordsResource::KeywordsApiAd_accountsAd_account_idKeywordsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/keywords/");
	this->set_method_handler("POST",
		std::bind(&KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

KeywordsApiAd_accountsAd_account_idKeywordsResource::~KeywordsApiAd_accountsAd_account_idKeywordsResource()
{
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> KeywordsApiAd_accountsAd_account_idKeywordsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void KeywordsApiAd_accountsAd_account_idKeywordsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void KeywordsApiAd_accountsAd_account_idKeywordsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void KeywordsApiAd_accountsAd_account_idKeywordsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto keywordsCreate = extractJsonModelBodyParam<KeywordsCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<Keywords> resultObject = std::make_shared<Keywords>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, keywordsCreate);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params
    const std::string campaignId = getQueryParam_campaignId_x_extension(request);

    const std::string adGroupId = getQueryParam_adGroupId_x_extension(request);

    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Keywords_get_200_response> resultObject = std::make_shared<Keywords_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, campaignId, adGroupId, adGroupIds, matchTypes, bookmark, pageSize);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto keywordsUpdate = extractJsonModelBodyParam<KeywordsUpdate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Keywords> resultObject = std::make_shared<Keywords>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, keywordsUpdate);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<Keywords>> KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_POST(
        std::string const & adAccountId, std::shared_ptr<KeywordsCreate> const & keywordsCreate)
{
    throw KeywordsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Keywords_get_200_response>> KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_GET(
    std::string const & adAccountId, std::string const & campaignId, std::string const & adGroupId, std::vector<std::shared_ptr<std::string>> const & adGroupIds, std::vector<std::shared_ptr<MatchType>> const & matchTypes, std::string const & bookmark, int32_t const & pageSize)
{
    throw KeywordsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<Keywords>> KeywordsApiAd_accountsAd_account_idKeywordsResource::handler_PATCH(
    std::string const & adAccountId, std::shared_ptr<KeywordsUpdate> const & keywordsUpdate)
{
    throw KeywordsApiException(501, "Not implemented");
}

std::string KeywordsApiAd_accountsAd_account_idKeywordsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::KeywordsApiTrendsKeywordsRegionTopTrend_typeResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/keywords/{region: .*}/top/{trend_type: .*}/");
	this->set_method_handler("GET",
		std::bind(&KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handler_GET_internal, this,
			std::placeholders::_1));
}

KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::~KeywordsApiTrendsKeywordsRegionTopTrend_typeResource()
{
}

std::pair<int, std::string> KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params

    // Getting the query params
    const bool normalizeAgainstGroup = getQueryParam_normalizeAgainstGroup(request);
    const int32_t limit = getQueryParam_limit(request);
    const bool includeDemographics = getQueryParam_includeDemographics(request);


    int status_code = 500;
    std::shared_ptr<TrendingKeywordsResponse> resultObject = std::make_shared<TrendingKeywordsResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(region, trendType, interests, genders, ages, includeKeywords, normalizeAgainstGroup, limit, includeDemographics);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<TrendingKeywordsResponse>> KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::handler_GET(
        std::shared_ptr<TrendsSupportedRegion> const & region, std::shared_ptr<TrendType> const & trendType, std::vector<std::shared_ptr<TrendsL1Interest>> const & interests, std::vector<std::shared_ptr<TrendsGenderFilter>> const & genders, std::vector<std::shared_ptr<TrendsAgeBucket>> const & ages, std::vector<std::shared_ptr<std::string>> const & includeKeywords, bool const & normalizeAgainstGroup, int32_t const & limit, bool const & includeDemographics)
{
    throw KeywordsApiException(501, "Not implemented");
}


std::string KeywordsApiTrendsKeywordsRegionTopTrend_typeResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

KeywordsApi::KeywordsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

KeywordsApi::~KeywordsApi() {}

void KeywordsApi::setKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource(std::shared_ptr<KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource> spKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource) {
    m_spKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource = spKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource;
    m_service->publish(m_spKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource);
}
void KeywordsApi::setKeywordsApiAd_accountsAd_account_idKeywordsResource(std::shared_ptr<KeywordsApiAd_accountsAd_account_idKeywordsResource> spKeywordsApiAd_accountsAd_account_idKeywordsResource) {
    m_spKeywordsApiAd_accountsAd_account_idKeywordsResource = spKeywordsApiAd_accountsAd_account_idKeywordsResource;
    m_service->publish(m_spKeywordsApiAd_accountsAd_account_idKeywordsResource);
}
void KeywordsApi::setKeywordsApiTrendsKeywordsRegionTopTrend_typeResource(std::shared_ptr<KeywordsApiTrendsKeywordsRegionTopTrend_typeResource> spKeywordsApiTrendsKeywordsRegionTopTrend_typeResource) {
    m_spKeywordsApiTrendsKeywordsRegionTopTrend_typeResource = spKeywordsApiTrendsKeywordsRegionTopTrend_typeResource;
    m_service->publish(m_spKeywordsApiTrendsKeywordsRegionTopTrend_typeResource);
}


void KeywordsApi::publishDefaultResources() {
    if (!m_spKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource) {
        setKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource(std::make_shared<KeywordsApiAd_accountsAd_account_idKeywordsMetricsResource>());
    }
    if (!m_spKeywordsApiAd_accountsAd_account_idKeywordsResource) {
        setKeywordsApiAd_accountsAd_account_idKeywordsResource(std::make_shared<KeywordsApiAd_accountsAd_account_idKeywordsResource>());
    }
    if (!m_spKeywordsApiTrendsKeywordsRegionTopTrend_typeResource) {
        setKeywordsApiTrendsKeywordsRegionTopTrend_typeResource(std::make_shared<KeywordsApiTrendsKeywordsRegionTopTrend_typeResource>());
    }
}

std::shared_ptr<restbed::Service> KeywordsApi::service() {
    return m_service;
}


}
}
}
}

