/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "IntegrationsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

IntegrationsApiException::IntegrationsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int IntegrationsApiException::getStatus() const
{
    return m_status;
}
const char* IntegrationsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

IntegrationsApiIntegrationsCommerceExternal_business_idResource::IntegrationsApiIntegrationsCommerceExternal_business_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/integrations/commerce/{external_business_id: .*}/");
	this->set_method_handler("DELETE",
		std::bind(&IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

IntegrationsApiIntegrationsCommerceExternal_business_idResource::~IntegrationsApiIntegrationsCommerceExternal_business_idResource()
{
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handleIntegrationsApiException(const IntegrationsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void IntegrationsApiIntegrationsCommerceExternal_business_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void IntegrationsApiIntegrationsCommerceExternal_business_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void IntegrationsApiIntegrationsCommerceExternal_business_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string externalBusinessId = getPathParam_externalBusinessId(request);



    int status_code = 500;
    std::shared_ptr<IntegrationMetadata> resultObject = std::make_shared<IntegrationMetadata>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_DELETE(externalBusinessId);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 204) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 204, result.empty() ? "Resource deleted successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string externalBusinessId = getPathParam_externalBusinessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<IntegrationMetadata> resultObject = std::make_shared<IntegrationMetadata>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(externalBusinessId);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto externalBusinessId = std::make_shared<>(bodyContent);
    auto integrationMetadataUpdate = extractJsonModelBodyParam<IntegrationMetadataUpdate>(bodyContent);

    // Getting the path params
    const std::string externalBusinessId = getPathParam_externalBusinessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<IntegrationMetadata> resultObject = std::make_shared<IntegrationMetadata>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(externalBusinessId, integrationMetadataUpdate);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<IntegrationMetadata>> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_DELETE(
        std::string const & externalBusinessId)
{
    throw IntegrationsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<IntegrationMetadata>> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_GET(
    std::string const & externalBusinessId)
{
    throw IntegrationsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<IntegrationMetadata>> IntegrationsApiIntegrationsCommerceExternal_business_idResource::handler_PATCH(
    std::string const & externalBusinessId, std::shared_ptr<IntegrationMetadataUpdate> const & integrationMetadataUpdate)
{
    throw IntegrationsApiException(501, "Not implemented");
}

std::string IntegrationsApiIntegrationsCommerceExternal_business_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
IntegrationsApiIntegrationsCommerceResource::IntegrationsApiIntegrationsCommerceResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/integrations/commerce/");
	this->set_method_handler("POST",
		std::bind(&IntegrationsApiIntegrationsCommerceResource::handler_POST_internal, this,
			std::placeholders::_1));
}

IntegrationsApiIntegrationsCommerceResource::~IntegrationsApiIntegrationsCommerceResource()
{
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceResource::handleIntegrationsApiException(const IntegrationsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsCommerceResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void IntegrationsApiIntegrationsCommerceResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void IntegrationsApiIntegrationsCommerceResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void IntegrationsApiIntegrationsCommerceResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void IntegrationsApiIntegrationsCommerceResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto integrationMetadataCreate = extractJsonModelBodyParam<IntegrationMetadataCreate>(bodyContent);




    int status_code = 500;
    std::shared_ptr<IntegrationMetadata> resultObject = std::make_shared<IntegrationMetadata>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(integrationMetadataCreate);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<IntegrationMetadata>> IntegrationsApiIntegrationsCommerceResource::handler_POST(
        std::shared_ptr<IntegrationMetadataCreate> const & integrationMetadataCreate)
{
    throw IntegrationsApiException(501, "Not implemented");
}


std::string IntegrationsApiIntegrationsCommerceResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
IntegrationsApiIntegrationsLogsResource::IntegrationsApiIntegrationsLogsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/integrations/logs/");
	this->set_method_handler("POST",
		std::bind(&IntegrationsApiIntegrationsLogsResource::handler_POST_internal, this,
			std::placeholders::_1));
}

IntegrationsApiIntegrationsLogsResource::~IntegrationsApiIntegrationsLogsResource()
{
}

std::pair<int, std::string> IntegrationsApiIntegrationsLogsResource::handleIntegrationsApiException(const IntegrationsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsLogsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsLogsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void IntegrationsApiIntegrationsLogsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void IntegrationsApiIntegrationsLogsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void IntegrationsApiIntegrationsLogsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void IntegrationsApiIntegrationsLogsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto integrationLogsRequestCreate = extractJsonModelBodyParam<IntegrationLogsRequestCreate>(bodyContent);




    int status_code = 500;
    std::shared_ptr<IntegrationLogsSuccessResponse> resultObject = std::make_shared<IntegrationLogsSuccessResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(integrationLogsRequestCreate);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The server could not understand the request due to invalid syntax." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<IntegrationLogsSuccessResponse>> IntegrationsApiIntegrationsLogsResource::handler_POST(
        std::shared_ptr<IntegrationLogsRequestCreate> const & integrationLogsRequestCreate)
{
    throw IntegrationsApiException(501, "Not implemented");
}


std::string IntegrationsApiIntegrationsLogsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
IntegrationsApiIntegrationsIdResource::IntegrationsApiIntegrationsIdResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/integrations/{id: .*}/");
	this->set_method_handler("GET",
		std::bind(&IntegrationsApiIntegrationsIdResource::handler_GET_internal, this,
			std::placeholders::_1));
}

IntegrationsApiIntegrationsIdResource::~IntegrationsApiIntegrationsIdResource()
{
}

std::pair<int, std::string> IntegrationsApiIntegrationsIdResource::handleIntegrationsApiException(const IntegrationsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsIdResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsIdResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void IntegrationsApiIntegrationsIdResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void IntegrationsApiIntegrationsIdResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void IntegrationsApiIntegrationsIdResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void IntegrationsApiIntegrationsIdResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string id = getPathParam_id(request);



    int status_code = 500;
    std::shared_ptr<IntegrationRecord> resultObject = std::make_shared<IntegrationRecord>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(id);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<IntegrationRecord>> IntegrationsApiIntegrationsIdResource::handler_GET(
        std::string const & id)
{
    throw IntegrationsApiException(501, "Not implemented");
}


std::string IntegrationsApiIntegrationsIdResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
IntegrationsApiIntegrationsResource::IntegrationsApiIntegrationsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/integrations/");
	this->set_method_handler("GET",
		std::bind(&IntegrationsApiIntegrationsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

IntegrationsApiIntegrationsResource::~IntegrationsApiIntegrationsResource()
{
}

std::pair<int, std::string> IntegrationsApiIntegrationsResource::handleIntegrationsApiException(const IntegrationsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> IntegrationsApiIntegrationsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void IntegrationsApiIntegrationsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void IntegrationsApiIntegrationsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void IntegrationsApiIntegrationsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void IntegrationsApiIntegrationsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Integrations_get_list_200_response> resultObject = std::make_shared<Integrations_get_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(bookmark, pageSize);
    }
    catch(const IntegrationsApiException& e) {
        std::tie(status_code, result) = handleIntegrationsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Integrations_get_list_200_response>> IntegrationsApiIntegrationsResource::handler_GET(
        std::string const & bookmark, int32_t const & pageSize)
{
    throw IntegrationsApiException(501, "Not implemented");
}


std::string IntegrationsApiIntegrationsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

IntegrationsApi::IntegrationsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

IntegrationsApi::~IntegrationsApi() {}

void IntegrationsApi::setIntegrationsApiIntegrationsCommerceExternal_business_idResource(std::shared_ptr<IntegrationsApiIntegrationsCommerceExternal_business_idResource> spIntegrationsApiIntegrationsCommerceExternal_business_idResource) {
    m_spIntegrationsApiIntegrationsCommerceExternal_business_idResource = spIntegrationsApiIntegrationsCommerceExternal_business_idResource;
    m_service->publish(m_spIntegrationsApiIntegrationsCommerceExternal_business_idResource);
}
void IntegrationsApi::setIntegrationsApiIntegrationsCommerceResource(std::shared_ptr<IntegrationsApiIntegrationsCommerceResource> spIntegrationsApiIntegrationsCommerceResource) {
    m_spIntegrationsApiIntegrationsCommerceResource = spIntegrationsApiIntegrationsCommerceResource;
    m_service->publish(m_spIntegrationsApiIntegrationsCommerceResource);
}
void IntegrationsApi::setIntegrationsApiIntegrationsLogsResource(std::shared_ptr<IntegrationsApiIntegrationsLogsResource> spIntegrationsApiIntegrationsLogsResource) {
    m_spIntegrationsApiIntegrationsLogsResource = spIntegrationsApiIntegrationsLogsResource;
    m_service->publish(m_spIntegrationsApiIntegrationsLogsResource);
}
void IntegrationsApi::setIntegrationsApiIntegrationsIdResource(std::shared_ptr<IntegrationsApiIntegrationsIdResource> spIntegrationsApiIntegrationsIdResource) {
    m_spIntegrationsApiIntegrationsIdResource = spIntegrationsApiIntegrationsIdResource;
    m_service->publish(m_spIntegrationsApiIntegrationsIdResource);
}
void IntegrationsApi::setIntegrationsApiIntegrationsResource(std::shared_ptr<IntegrationsApiIntegrationsResource> spIntegrationsApiIntegrationsResource) {
    m_spIntegrationsApiIntegrationsResource = spIntegrationsApiIntegrationsResource;
    m_service->publish(m_spIntegrationsApiIntegrationsResource);
}


void IntegrationsApi::publishDefaultResources() {
    if (!m_spIntegrationsApiIntegrationsCommerceExternal_business_idResource) {
        setIntegrationsApiIntegrationsCommerceExternal_business_idResource(std::make_shared<IntegrationsApiIntegrationsCommerceExternal_business_idResource>());
    }
    if (!m_spIntegrationsApiIntegrationsCommerceResource) {
        setIntegrationsApiIntegrationsCommerceResource(std::make_shared<IntegrationsApiIntegrationsCommerceResource>());
    }
    if (!m_spIntegrationsApiIntegrationsLogsResource) {
        setIntegrationsApiIntegrationsLogsResource(std::make_shared<IntegrationsApiIntegrationsLogsResource>());
    }
    if (!m_spIntegrationsApiIntegrationsIdResource) {
        setIntegrationsApiIntegrationsIdResource(std::make_shared<IntegrationsApiIntegrationsIdResource>());
    }
    if (!m_spIntegrationsApiIntegrationsResource) {
        setIntegrationsApiIntegrationsResource(std::make_shared<IntegrationsApiIntegrationsResource>());
    }
}

std::shared_ptr<restbed::Service> IntegrationsApi::service() {
    return m_service;
}


}
}
}
}

