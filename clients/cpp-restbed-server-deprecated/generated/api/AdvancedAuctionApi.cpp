/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "AdvancedAuctionApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

AdvancedAuctionApiException::AdvancedAuctionApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int AdvancedAuctionApiException::getStatus() const
{
    return m_status;
}
const char* AdvancedAuctionApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

AdvancedAuctionApiAdvanced_auctionItemsGetResource::AdvancedAuctionApiAdvanced_auctionItemsGetResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/advanced_auction/items/get/");
	this->set_method_handler("POST",
		std::bind(&AdvancedAuctionApiAdvanced_auctionItemsGetResource::handler_POST_internal, this,
			std::placeholders::_1));
}

AdvancedAuctionApiAdvanced_auctionItemsGetResource::~AdvancedAuctionApiAdvanced_auctionItemsGetResource()
{
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsGetResource::handleAdvancedAuctionApiException(const AdvancedAuctionApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsGetResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsGetResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdvancedAuctionApiAdvanced_auctionItemsGetResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdvancedAuctionApiAdvanced_auctionItemsGetResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdvancedAuctionApiAdvanced_auctionItemsGetResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdvancedAuctionApiAdvanced_auctionItemsGetResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto advancedAuctionItemsGetRequest = extractJsonModelBodyParam<AdvancedAuctionItemsGetRequest>(bodyContent);


    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<AdvancedAuctionItems> resultObject = std::make_shared<AdvancedAuctionItems>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(advancedAuctionItemsGetRequest, adAccountId);
    }
    catch(const AdvancedAuctionApiException& e) {
        std::tie(status_code, result) = handleAdvancedAuctionApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 500) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 500, result.empty() ? "The server encountered an unexpected condition that prevented it from fulfilling the request." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<AdvancedAuctionItems>> AdvancedAuctionApiAdvanced_auctionItemsGetResource::handler_POST(
        std::shared_ptr<AdvancedAuctionItemsGetRequest> const & advancedAuctionItemsGetRequest, std::string const & adAccountId)
{
    throw AdvancedAuctionApiException(501, "Not implemented");
}


std::string AdvancedAuctionApiAdvanced_auctionItemsGetResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::AdvancedAuctionApiAdvanced_auctionItemsSubmitResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/advanced_auction/items/submit/");
	this->set_method_handler("POST",
		std::bind(&AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handler_POST_internal, this,
			std::placeholders::_1));
}

AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::~AdvancedAuctionApiAdvanced_auctionItemsSubmitResource()
{
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handleAdvancedAuctionApiException(const AdvancedAuctionApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto advancedAuctionItemsSubmitRequest = extractJsonModelBodyParam<AdvancedAuctionItemsSubmitRequest>(bodyContent);


    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<AdvancedAuctionProcessedItems> resultObject = std::make_shared<AdvancedAuctionProcessedItems>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(advancedAuctionItemsSubmitRequest, adAccountId);
    }
    catch(const AdvancedAuctionApiException& e) {
        std::tie(status_code, result) = handleAdvancedAuctionApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 206) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 206, result.empty() ? "Successful" : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 500) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 500, result.empty() ? "The server encountered an unexpected condition that prevented it from fulfilling the request." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<AdvancedAuctionProcessedItems>> AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::handler_POST(
        std::shared_ptr<AdvancedAuctionItemsSubmitRequest> const & advancedAuctionItemsSubmitRequest, std::string const & adAccountId)
{
    throw AdvancedAuctionApiException(501, "Not implemented");
}


std::string AdvancedAuctionApiAdvanced_auctionItemsSubmitResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

AdvancedAuctionApi::AdvancedAuctionApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

AdvancedAuctionApi::~AdvancedAuctionApi() {}

void AdvancedAuctionApi::setAdvancedAuctionApiAdvanced_auctionItemsGetResource(std::shared_ptr<AdvancedAuctionApiAdvanced_auctionItemsGetResource> spAdvancedAuctionApiAdvanced_auctionItemsGetResource) {
    m_spAdvancedAuctionApiAdvanced_auctionItemsGetResource = spAdvancedAuctionApiAdvanced_auctionItemsGetResource;
    m_service->publish(m_spAdvancedAuctionApiAdvanced_auctionItemsGetResource);
}
void AdvancedAuctionApi::setAdvancedAuctionApiAdvanced_auctionItemsSubmitResource(std::shared_ptr<AdvancedAuctionApiAdvanced_auctionItemsSubmitResource> spAdvancedAuctionApiAdvanced_auctionItemsSubmitResource) {
    m_spAdvancedAuctionApiAdvanced_auctionItemsSubmitResource = spAdvancedAuctionApiAdvanced_auctionItemsSubmitResource;
    m_service->publish(m_spAdvancedAuctionApiAdvanced_auctionItemsSubmitResource);
}


void AdvancedAuctionApi::publishDefaultResources() {
    if (!m_spAdvancedAuctionApiAdvanced_auctionItemsGetResource) {
        setAdvancedAuctionApiAdvanced_auctionItemsGetResource(std::make_shared<AdvancedAuctionApiAdvanced_auctionItemsGetResource>());
    }
    if (!m_spAdvancedAuctionApiAdvanced_auctionItemsSubmitResource) {
        setAdvancedAuctionApiAdvanced_auctionItemsSubmitResource(std::make_shared<AdvancedAuctionApiAdvanced_auctionItemsSubmitResource>());
    }
}

std::shared_ptr<restbed::Service> AdvancedAuctionApi::service() {
    return m_service;
}


}
}
}
}

