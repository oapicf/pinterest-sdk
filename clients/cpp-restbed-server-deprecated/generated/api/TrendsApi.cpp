/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "TrendsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

TrendsApiException::TrendsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int TrendsApiException::getStatus() const
{
    return m_status;
}
const char* TrendsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

TrendsApiTrendsEditorial_articlesResource::TrendsApiTrendsEditorial_articlesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/editorial_articles/");
	this->set_method_handler("GET",
		std::bind(&TrendsApiTrendsEditorial_articlesResource::handler_GET_internal, this,
			std::placeholders::_1));
}

TrendsApiTrendsEditorial_articlesResource::~TrendsApiTrendsEditorial_articlesResource()
{
}

std::pair<int, std::string> TrendsApiTrendsEditorial_articlesResource::handleTrendsApiException(const TrendsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsApiTrendsEditorial_articlesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsApiTrendsEditorial_articlesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsApiTrendsEditorial_articlesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsApiTrendsEditorial_articlesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void TrendsApiTrendsEditorial_articlesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsApiTrendsEditorial_articlesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params


    int status_code = 500;
    std::vector<std::shared_ptr<TrendsEditorial>> resultObject = std::vector<std::shared_ptr<TrendsEditorial>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(region);
    }
    catch(const TrendsApiException& e) {
        std::tie(status_code, result) = handleTrendsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<TrendsEditorial>>> TrendsApiTrendsEditorial_articlesResource::handler_GET(
        std::shared_ptr<ProductCategoryRegion> const & region)
{
    throw TrendsApiException(501, "Not implemented");
}


std::string TrendsApiTrendsEditorial_articlesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
TrendsApiTrendsTopicsFeaturedResource::TrendsApiTrendsTopicsFeaturedResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/topics/featured/");
	this->set_method_handler("GET",
		std::bind(&TrendsApiTrendsTopicsFeaturedResource::handler_GET_internal, this,
			std::placeholders::_1));
}

TrendsApiTrendsTopicsFeaturedResource::~TrendsApiTrendsTopicsFeaturedResource()
{
}

std::pair<int, std::string> TrendsApiTrendsTopicsFeaturedResource::handleTrendsApiException(const TrendsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsApiTrendsTopicsFeaturedResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsApiTrendsTopicsFeaturedResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsApiTrendsTopicsFeaturedResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsApiTrendsTopicsFeaturedResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void TrendsApiTrendsTopicsFeaturedResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsApiTrendsTopicsFeaturedResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params


    int status_code = 500;
    std::vector<std::shared_ptr<FeaturedTrend>> resultObject = std::vector<std::shared_ptr<FeaturedTrend>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(region, interest);
    }
    catch(const TrendsApiException& e) {
        std::tie(status_code, result) = handleTrendsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<FeaturedTrend>>> TrendsApiTrendsTopicsFeaturedResource::handler_GET(
        std::shared_ptr<ProductCategoryRegion> const & region, std::shared_ptr<InterestsEnum> const & interest)
{
    throw TrendsApiException(501, "Not implemented");
}


std::string TrendsApiTrendsTopicsFeaturedResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
TrendsApiTrendsProduct_categoriesDetailsResource::TrendsApiTrendsProduct_categoriesDetailsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/product_categories/details/");
	this->set_method_handler("GET",
		std::bind(&TrendsApiTrendsProduct_categoriesDetailsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

TrendsApiTrendsProduct_categoriesDetailsResource::~TrendsApiTrendsProduct_categoriesDetailsResource()
{
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesDetailsResource::handleTrendsApiException(const TrendsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesDetailsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesDetailsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsApiTrendsProduct_categoriesDetailsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsApiTrendsProduct_categoriesDetailsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void TrendsApiTrendsProduct_categoriesDetailsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsApiTrendsProduct_categoriesDetailsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params


    int status_code = 500;
    std::vector<std::shared_ptr<ProductCategoryDetails>> resultObject = std::vector<std::shared_ptr<ProductCategoryDetails>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(productCategories, region, lookbackWindow, engagementType);
    }
    catch(const TrendsApiException& e) {
        std::tie(status_code, result) = handleTrendsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<ProductCategoryDetails>>> TrendsApiTrendsProduct_categoriesDetailsResource::handler_GET(
        std::vector<std::shared_ptr<ProductCategoryEnum>> const & productCategories, std::shared_ptr<ProductCategoryRegion> const & region, std::shared_ptr<ProductCategoryDetailLookbackWindow> const & lookbackWindow, std::shared_ptr<ProductCategoriesEngagementType> const & engagementType)
{
    throw TrendsApiException(501, "Not implemented");
}


std::string TrendsApiTrendsProduct_categoriesDetailsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
TrendsApiTrendsProduct_categoriesTrendingResource::TrendsApiTrendsProduct_categoriesTrendingResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/product_categories/trending/");
	this->set_method_handler("GET",
		std::bind(&TrendsApiTrendsProduct_categoriesTrendingResource::handler_GET_internal, this,
			std::placeholders::_1));
}

TrendsApiTrendsProduct_categoriesTrendingResource::~TrendsApiTrendsProduct_categoriesTrendingResource()
{
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesTrendingResource::handleTrendsApiException(const TrendsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesTrendingResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsApiTrendsProduct_categoriesTrendingResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsApiTrendsProduct_categoriesTrendingResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsApiTrendsProduct_categoriesTrendingResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void TrendsApiTrendsProduct_categoriesTrendingResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsApiTrendsProduct_categoriesTrendingResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params


    int status_code = 500;
    std::vector<std::shared_ptr<TrendingProductCategory>> resultObject = std::vector<std::shared_ptr<TrendingProductCategory>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(region, verticals, ages, genders, engagementType);
    }
    catch(const TrendsApiException& e) {
        std::tie(status_code, result) = handleTrendsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<TrendingProductCategory>>> TrendsApiTrendsProduct_categoriesTrendingResource::handler_GET(
        std::shared_ptr<ProductCategoryRegion> const & region, std::vector<std::shared_ptr<VerticalProductCategory>> const & verticals, std::vector<std::shared_ptr<AgeTrendsBucket>> const & ages, std::vector<std::shared_ptr<GenderBucket>> const & genders, std::shared_ptr<ProductCategoriesEngagementType> const & engagementType)
{
    throw TrendsApiException(501, "Not implemented");
}


std::string TrendsApiTrendsProduct_categoriesTrendingResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

TrendsApi::TrendsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

TrendsApi::~TrendsApi() {}

void TrendsApi::setTrendsApiTrendsEditorial_articlesResource(std::shared_ptr<TrendsApiTrendsEditorial_articlesResource> spTrendsApiTrendsEditorial_articlesResource) {
    m_spTrendsApiTrendsEditorial_articlesResource = spTrendsApiTrendsEditorial_articlesResource;
    m_service->publish(m_spTrendsApiTrendsEditorial_articlesResource);
}
void TrendsApi::setTrendsApiTrendsTopicsFeaturedResource(std::shared_ptr<TrendsApiTrendsTopicsFeaturedResource> spTrendsApiTrendsTopicsFeaturedResource) {
    m_spTrendsApiTrendsTopicsFeaturedResource = spTrendsApiTrendsTopicsFeaturedResource;
    m_service->publish(m_spTrendsApiTrendsTopicsFeaturedResource);
}
void TrendsApi::setTrendsApiTrendsProduct_categoriesDetailsResource(std::shared_ptr<TrendsApiTrendsProduct_categoriesDetailsResource> spTrendsApiTrendsProduct_categoriesDetailsResource) {
    m_spTrendsApiTrendsProduct_categoriesDetailsResource = spTrendsApiTrendsProduct_categoriesDetailsResource;
    m_service->publish(m_spTrendsApiTrendsProduct_categoriesDetailsResource);
}
void TrendsApi::setTrendsApiTrendsProduct_categoriesTrendingResource(std::shared_ptr<TrendsApiTrendsProduct_categoriesTrendingResource> spTrendsApiTrendsProduct_categoriesTrendingResource) {
    m_spTrendsApiTrendsProduct_categoriesTrendingResource = spTrendsApiTrendsProduct_categoriesTrendingResource;
    m_service->publish(m_spTrendsApiTrendsProduct_categoriesTrendingResource);
}


void TrendsApi::publishDefaultResources() {
    if (!m_spTrendsApiTrendsEditorial_articlesResource) {
        setTrendsApiTrendsEditorial_articlesResource(std::make_shared<TrendsApiTrendsEditorial_articlesResource>());
    }
    if (!m_spTrendsApiTrendsTopicsFeaturedResource) {
        setTrendsApiTrendsTopicsFeaturedResource(std::make_shared<TrendsApiTrendsTopicsFeaturedResource>());
    }
    if (!m_spTrendsApiTrendsProduct_categoriesDetailsResource) {
        setTrendsApiTrendsProduct_categoriesDetailsResource(std::make_shared<TrendsApiTrendsProduct_categoriesDetailsResource>());
    }
    if (!m_spTrendsApiTrendsProduct_categoriesTrendingResource) {
        setTrendsApiTrendsProduct_categoriesTrendingResource(std::make_shared<TrendsApiTrendsProduct_categoriesTrendingResource>());
    }
}

std::shared_ptr<restbed::Service> TrendsApi::service() {
    return m_service;
}


}
}
}
}

