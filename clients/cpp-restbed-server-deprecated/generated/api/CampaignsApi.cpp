/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CampaignsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

CampaignsApiException::CampaignsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CampaignsApiException::getStatus() const
{
    return m_status;
}
const char* CampaignsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/pins/analytics/");
	this->set_method_handler("GET",
		std::bind(&CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::~CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string campaignId = getQueryParam_campaignId(request);
    const std::string startDate = getQueryParam_startDate(request);
    const std::string endDate = getQueryParam_endDate(request);
    const double clickWindowDays = getQueryParam_clickWindowDays(request);
    const double engagementWindowDays = getQueryParam_engagementWindowDays(request);
    const double viewWindowDays = getQueryParam_viewWindowDays(request);
    const std::string conversionReportTime = getQueryParam_conversionReportTime(request);


    int status_code = 500;
    std::vector<std::shared_ptr<AdPinAnalytics>> resultObject = std::vector<std::shared_ptr<AdPinAnalytics>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(campaignId, pinIds, startDate, endDate, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<AdPinAnalytics>>> CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::handler_GET(
        std::string const & campaignId, std::vector<std::shared_ptr<std::string>> const & pinIds, std::string const & startDate, std::string const & endDate, std::vector<std::shared_ptr<ReportingColumnSync>> const & columns, std::shared_ptr<Granularity> const & granularity, std::string const & adAccountId, double const & clickWindowDays, double const & engagementWindowDays, double const & viewWindowDays, std::string const & conversionReportTime)
{
    throw CampaignsApiException(501, "Not implemented");
}


std::string CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/targeting_analytics/");
	this->set_method_handler("GET",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::~CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string startDate = getQueryParam_startDate(request);
    const std::string endDate = getQueryParam_endDate(request);
    const double clickWindowDays = getQueryParam_clickWindowDays(request);
    const double engagementWindowDays = getQueryParam_engagementWindowDays(request);
    const double viewWindowDays = getQueryParam_viewWindowDays(request);
    const std::string conversionReportTime = getQueryParam_conversionReportTime(request);


    int status_code = 500;
    std::shared_ptr<MetricsResponse> resultObject = std::make_shared<MetricsResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adAccountId, campaignIds, startDate, endDate, targetingTypes, columns, granularity, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, attributionTypes, reportingTimezone);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<MetricsResponse>> CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET(
        std::string const & adAccountId, std::vector<std::shared_ptr<std::string>> const & campaignIds, std::string const & startDate, std::string const & endDate, std::vector<std::shared_ptr<AdsAnalyticsCampaignTargetingType>> const & targetingTypes, std::vector<std::shared_ptr<ReportingColumnSync>> const & columns, std::shared_ptr<Granularity> const & granularity, double const & clickWindowDays, double const & engagementWindowDays, double const & viewWindowDays, std::string const & conversionReportTime, std::vector<std::shared_ptr<ConversionReportAttributionType>> const & attributionTypes, std::shared_ptr<ReportingTimeZone> const & reportingTimezone)
{
    throw CampaignsApiException(501, "Not implemented");
}


std::string CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/analytics/");
	this->set_method_handler("GET",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::~CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string startDate = getQueryParam_startDate(request);
    const std::string endDate = getQueryParam_endDate(request);
    const double clickWindowDays = getQueryParam_clickWindowDays(request);
    const double engagementWindowDays = getQueryParam_engagementWindowDays(request);
    const double viewWindowDays = getQueryParam_viewWindowDays(request);
    const std::string conversionReportTime = getQueryParam_conversionReportTime(request);
    const bool aggregateReportRows = getQueryParam_aggregateReportRows(request);


    int status_code = 500;
    std::vector<std::shared_ptr<CampaignsAnalyticsMetrics>> resultObject = std::vector<std::shared_ptr<CampaignsAnalyticsMetrics>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(startDate, endDate, campaignIds, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, aggregateReportRows, reportingTimezone);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<CampaignsAnalyticsMetrics>>> CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::handler_GET(
        std::string const & startDate, std::string const & endDate, std::vector<std::shared_ptr<std::string>> const & campaignIds, std::vector<std::shared_ptr<ReportingColumnSync>> const & columns, std::shared_ptr<Granularity> const & granularity, std::string const & adAccountId, double const & clickWindowDays, double const & engagementWindowDays, double const & viewWindowDays, std::string const & conversionReportTime, bool const & aggregateReportRows, std::shared_ptr<ReportingTimeZone> const & reportingTimezone)
{
    throw CampaignsApiException(501, "Not implemented");
}


std::string CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CampaignsApiAd_accountsAd_account_idCampaignsResource::CampaignsApiAd_accountsAd_account_idCampaignsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/");
	this->set_method_handler("POST",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idCampaignsResource::~CampaignsApiAd_accountsAd_account_idCampaignsResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idCampaignsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idCampaignsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto campaignCreateItem = extractJsonArrayBodyParam<CampaignCreateItem>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<CampaignBatchWriteResponseModel> resultObject = std::make_shared<CampaignBatchWriteResponseModel>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, campaignCreateItem);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Campaigns_list_200_response> resultObject = std::make_shared<Campaigns_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, bookmark, pageSize, order, campaignIds, entityStatuses);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto campaignBatchUpdateItem = extractJsonArrayBodyParam<CampaignBatchUpdateItem>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<CampaignBatchWriteResponseModel> resultObject = std::make_shared<CampaignBatchWriteResponseModel>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, campaignBatchUpdateItem);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<CampaignBatchWriteResponseModel>> CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_POST(
        std::string const & adAccountId, std::vector<std::shared_ptr<CampaignCreateItem>> const & campaignCreateItem)
{
    throw CampaignsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Campaigns_list_200_response>> CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_GET(
    std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize, std::shared_ptr<Pinterest_Lib_PaginationOrder> const & order, std::vector<std::shared_ptr<std::string>> const & campaignIds, std::vector<std::shared_ptr<EntityStatus>> const & entityStatuses)
{
    throw CampaignsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<CampaignBatchWriteResponseModel>> CampaignsApiAd_accountsAd_account_idCampaignsResource::handler_PATCH(
    std::string const & adAccountId, std::vector<std::shared_ptr<CampaignBatchUpdateItem>> const & campaignBatchUpdateItem)
{
    throw CampaignsApiException(501, "Not implemented");
}

std::string CampaignsApiAd_accountsAd_account_idCampaignsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/{campaign_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::~CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string campaignId = getPathParam_campaignId(request);
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<Campaign> resultObject = std::make_shared<Campaign>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(campaignId, adAccountId);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Campaign>> CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::handler_GET(
        std::string const & campaignId, std::string const & adAccountId)
{
    throw CampaignsApiException(501, "Not implemented");
}


std::string CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/delivery_estimates/");
	this->set_method_handler("POST",
		std::bind(&CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::~CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource()
{
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto campaignDeliveryEstimatesCampaign = extractJsonArrayBodyParam<CampaignDeliveryEstimatesCampaign>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<CampaignDeliveryEstimatesResponse> resultObject = std::make_shared<CampaignDeliveryEstimatesResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, campaignDeliveryEstimatesCampaign);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 503) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 503, result.empty() ? "The service is temporarily unavailable." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<CampaignDeliveryEstimatesResponse>> CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST(
        std::string const & adAccountId, std::vector<std::shared_ptr<CampaignDeliveryEstimatesCampaign>> const & campaignDeliveryEstimatesCampaign)
{
    throw CampaignsApiException(501, "Not implemented");
}


std::string CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

CampaignsApi::CampaignsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CampaignsApi::~CampaignsApi() {}

void CampaignsApi::setCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource> spCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource) {
    m_spCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource = spCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource> spCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource) {
    m_spCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource = spCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource> spCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource) {
    m_spCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource = spCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idCampaignsResource> spCampaignsApiAd_accountsAd_account_idCampaignsResource) {
    m_spCampaignsApiAd_accountsAd_account_idCampaignsResource = spCampaignsApiAd_accountsAd_account_idCampaignsResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idCampaignsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource> spCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource) {
    m_spCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource = spCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource(std::shared_ptr<CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource> spCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource) {
    m_spCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource = spCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource;
    m_service->publish(m_spCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource);
}


void CampaignsApi::publishDefaultResources() {
    if (!m_spCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource) {
        setCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource(std::make_shared<CampaignsApiAd_accountsAd_account_idPinsAnalyticsResource>());
    }
    if (!m_spCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource) {
        setCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource(std::make_shared<CampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource>());
    }
    if (!m_spCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource) {
        setCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource(std::make_shared<CampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource>());
    }
    if (!m_spCampaignsApiAd_accountsAd_account_idCampaignsResource) {
        setCampaignsApiAd_accountsAd_account_idCampaignsResource(std::make_shared<CampaignsApiAd_accountsAd_account_idCampaignsResource>());
    }
    if (!m_spCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource) {
        setCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource(std::make_shared<CampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource>());
    }
    if (!m_spCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource) {
        setCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource(std::make_shared<CampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource>());
    }
}

std::shared_ptr<restbed::Service> CampaignsApi::service() {
    return m_service;
}


}
}
}
}

