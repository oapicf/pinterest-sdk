/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "LeadAdsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

LeadAdsApiException::LeadAdsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int LeadAdsApiException::getStatus() const
{
    return m_status;
}
const char* LeadAdsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/leads/subscriptions/{subscription_id: .*}/");
	this->set_method_handler("DELETE",
		std::bind(&LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::~LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource()
{
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handleLeadAdsApiException(const LeadAdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);
    const std::string subscriptionId = getPathParam_subscriptionId(request);



    int status_code = 500;
    std::shared_ptr<LeadSubscription> resultObject = std::make_shared<LeadSubscription>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_DELETE(adAccountId, subscriptionId);
    }
    catch(const LeadAdsApiException& e) {
        std::tie(status_code, result) = handleLeadAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 204) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 204, result.empty() ? "Resource deleted successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    const std::string subscriptionId = getPathParam_subscriptionId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<LeadSubscription> resultObject = std::make_shared<LeadSubscription>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, subscriptionId);
    }
    catch(const LeadAdsApiException& e) {
        std::tie(status_code, result) = handleLeadAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<LeadSubscription>> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_DELETE(
        std::string const & adAccountId, std::string const & subscriptionId)
{
    throw LeadAdsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<LeadSubscription>> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::handler_GET(
    std::string const & adAccountId, std::string const & subscriptionId)
{
    throw LeadAdsApiException(501, "Not implemented");
}

std::string LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/leads/subscriptions/");
	this->set_method_handler("GET",
		std::bind(&LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("POST",
		std::bind(&LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_POST_internal, this,
			std::placeholders::_1));
}

LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::~LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource()
{
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handleLeadAdsApiException(const LeadAdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Ad_accounts_subscriptions_get_list_200_response> resultObject = std::make_shared<Ad_accounts_subscriptions_get_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adAccountId, bookmark, pageSize);
    }
    catch(const LeadAdsApiException& e) {
        std::tie(status_code, result) = handleLeadAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto leadSubscriptionPostParamsCreate = extractJsonModelBodyParam<LeadSubscriptionPostParamsCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<LeadSubscription> resultObject = std::make_shared<LeadSubscription>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_POST(adAccountId, leadSubscriptionPostParamsCreate);
    }
    catch(const LeadAdsApiException& e) {
        std::tie(status_code, result) = handleLeadAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<Ad_accounts_subscriptions_get_list_200_response>> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_GET(
        std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize)
{
    throw LeadAdsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<LeadSubscription>> LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::handler_POST(
    std::string const & adAccountId, std::shared_ptr<LeadSubscriptionPostParamsCreate> const & leadSubscriptionPostParamsCreate)
{
    throw LeadAdsApiException(501, "Not implemented");
}

std::string LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

LeadAdsApi::LeadAdsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

LeadAdsApi::~LeadAdsApi() {}

void LeadAdsApi::setLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource(std::shared_ptr<LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource> spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource) {
    m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource = spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource;
    m_service->publish(m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource);
}
void LeadAdsApi::setLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource(std::shared_ptr<LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource> spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource) {
    m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource = spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource;
    m_service->publish(m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource);
}


void LeadAdsApi::publishDefaultResources() {
    if (!m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource) {
        setLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource(std::make_shared<LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsSubscription_idResource>());
    }
    if (!m_spLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource) {
        setLeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource(std::make_shared<LeadAdsApiAd_accountsAd_account_idLeadsSubscriptionsResource>());
    }
}

std::shared_ptr<restbed::Service> LeadAdsApi::service() {
    return m_service;
}


}
}
}
}

