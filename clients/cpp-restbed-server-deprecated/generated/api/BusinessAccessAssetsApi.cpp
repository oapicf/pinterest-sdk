/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "BusinessAccessAssetsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

BusinessAccessAssetsApiException::BusinessAccessAssetsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int BusinessAccessAssetsApiException::getStatus() const
{
    return m_status;
}
const char* BusinessAccessAssetsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/asset_groups/");
	this->set_method_handler("POST",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("DELETE",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::~BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto assetGroupInputCreate = extractJsonModelBodyParam<AssetGroupInputCreate>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);



    int status_code = 500;
    std::shared_ptr<AssetGroupInput> resultObject = std::make_shared<AssetGroupInput>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(businessId, assetGroupInputCreate);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto businessId = std::make_shared<>(bodyContent);
    auto assetGroupDeletionDelete = extractJsonModelBodyParam<AssetGroupDeletionDelete>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<AssetGroupDeletion> resultObject = std::make_shared<AssetGroupDeletion>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_DELETE(businessId, assetGroupDeletionDelete);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto businessId = std::make_shared<>(bodyContent);
    auto assetGroupModificationReadOrUpdate = extractJsonModelBodyParam<AssetGroupModificationReadOrUpdate>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<AssetGroupModification> resultObject = std::make_shared<AssetGroupModification>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(businessId, assetGroupModificationReadOrUpdate);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<AssetGroupInput>> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_POST(
        std::string const & businessId, std::shared_ptr<AssetGroupInputCreate> const & assetGroupInputCreate)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<AssetGroupDeletion>> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_DELETE(
    std::string const & businessId, std::shared_ptr<AssetGroupDeletionDelete> const & assetGroupDeletionDelete)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<AssetGroupModification>> BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::handler_PATCH(
    std::string const & businessId, std::shared_ptr<AssetGroupModificationReadOrUpdate> const & assetGroupModificationReadOrUpdate)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::string BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/assets/{asset_id: .*}/members/");
	this->set_method_handler("GET",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::~BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);
    const std::string assetId = getPathParam_assetId(request);

    // Getting the query params
    const int32_t startIndex = getQueryParam_startIndex(request);
    const bool fetchSystemUsers = getQueryParam_fetchSystemUsers(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Business_asset_members_get_200_response> resultObject = std::make_shared<Business_asset_members_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(businessId, assetId, startIndex, fetchSystemUsers, bookmark, pageSize);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Business_asset_members_get_200_response>> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::handler_GET(
        std::string const & businessId, std::string const & assetId, int32_t const & startIndex, bool const & fetchSystemUsers, std::string const & bookmark, int32_t const & pageSize)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}


std::string BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/assets/{asset_id: .*}/partners/");
	this->set_method_handler("GET",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::~BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);
    const std::string assetId = getPathParam_assetId(request);

    // Getting the query params
    const int32_t startIndex = getQueryParam_startIndex(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Business_asset_members_get_200_response> resultObject = std::make_shared<Business_asset_members_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(businessId, assetId, startIndex, bookmark, pageSize);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Business_asset_members_get_200_response>> BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::handler_GET(
        std::string const & businessId, std::string const & assetId, int32_t const & startIndex, std::string const & bookmark, int32_t const & pageSize)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}


std::string BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/assets/");
	this->set_method_handler("GET",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::~BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);

    // Getting the query params
    const std::string childAssetId = getQueryParam_childAssetId(request);
    const std::string assetGroupId = getQueryParam_assetGroupId(request);
    const std::string assetType = getQueryParam_assetType(request);
    const int32_t startIndex = getQueryParam_startIndex(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Business_assets_get_200_response> resultObject = std::make_shared<Business_assets_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(businessId, permissions, childAssetId, assetGroupId, assetType, startIndex, bookmark, pageSize);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Business_assets_get_200_response>> BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::handler_GET(
        std::string const & businessId, std::vector<std::shared_ptr<PermissionsWithOwner>> const & permissions, std::string const & childAssetId, std::string const & assetGroupId, std::string const & assetType, int32_t const & startIndex, std::string const & bookmark, int32_t const & pageSize)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}


std::string BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/members/{member_id: .*}/assets/");
	this->set_method_handler("GET",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::~BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);
    const std::string memberId = getPathParam_memberId(request);

    // Getting the query params
    const std::string assetType = getQueryParam_assetType(request);
    const int32_t startIndex = getQueryParam_startIndex(request);
    const bool sortAscending = getQueryParam_sortAscending(request);
    const std::string searchValue = getQueryParam_searchValue(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<BusinessMemberAssetsGetResponse> resultObject = std::make_shared<BusinessMemberAssetsGetResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(businessId, memberId, assetType, startIndex, sortBy, sortAscending, searchBy, searchValue, assetPermissionType, adAccountStatuses, bookmark, pageSize);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<BusinessMemberAssetsGetResponse>> BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::handler_GET(
        std::string const & businessId, std::string const & memberId, std::string const & assetType, int32_t const & startIndex, std::shared_ptr<AssetSortBy> const & sortBy, bool const & sortAscending, std::shared_ptr<AssetSearchBy> const & searchBy, std::string const & searchValue, std::shared_ptr<AssetPermissionType> const & assetPermissionType, std::vector<std::shared_ptr<NonDraftEntityStatus>> const & adAccountStatuses, std::string const & bookmark, int32_t const & pageSize)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}


std::string BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/members/assets/access/");
	this->set_method_handler("DELETE",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::~BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto businessMembersAssetAccessDeleteBody = extractJsonModelBodyParam<BusinessMembersAssetAccessDeleteBody>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);



    int status_code = 500;
    std::shared_ptr<DeleteMemberAccessResultsResponseArray> resultObject = std::make_shared<DeleteMemberAccessResultsResponseArray>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_DELETE(businessId, businessMembersAssetAccessDeleteBody);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto businessId = std::make_shared<>(bodyContent);
    auto updateMemberAssetAccessBody = extractJsonModelBodyParam<UpdateMemberAssetAccessBody>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<UpdateMemberAssetsResultsResponseArray> resultObject = std::make_shared<UpdateMemberAssetsResultsResponseArray>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(businessId, updateMemberAssetAccessBody);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<DeleteMemberAccessResultsResponseArray>> BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_DELETE(
        std::string const & businessId, std::shared_ptr<BusinessMembersAssetAccessDeleteBody> const & businessMembersAssetAccessDeleteBody)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<UpdateMemberAssetsResultsResponseArray>> BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::handler_PATCH(
    std::string const & businessId, std::shared_ptr<UpdateMemberAssetAccessBody> const & updateMemberAssetAccessBody)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::string BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/partners/{partner_id: .*}/assets/");
	this->set_method_handler("GET",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::~BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);
    const std::string partnerId = getPathParam_partnerId(request);

    // Getting the query params
    const std::string partnerType = getQueryParam_partnerType(request);
    const std::string assetType = getQueryParam_assetType(request);
    const int32_t startIndex = getQueryParam_startIndex(request);
    const bool sortAscending = getQueryParam_sortAscending(request);
    const std::string searchValue = getQueryParam_searchValue(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Business_partner_asset_access_get_200_response> resultObject = std::make_shared<Business_partner_asset_access_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(businessId, partnerId, partnerType, assetType, startIndex, sortBy, sortAscending, searchBy, searchValue, bookmark, pageSize);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Business_partner_asset_access_get_200_response>> BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::handler_GET(
        std::string const & businessId, std::string const & partnerId, std::string const & partnerType, std::string const & assetType, int32_t const & startIndex, std::shared_ptr<AssetSortBy> const & sortBy, bool const & sortAscending, std::shared_ptr<AssetSearchBy> const & searchBy, std::string const & searchValue, std::string const & bookmark, int32_t const & pageSize)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}


std::string BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/businesses/{business_id: .*}/partners/assets/");
	this->set_method_handler("DELETE",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::~BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource()
{
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handleBusinessAccessAssetsApiException(const BusinessAccessAssetsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto deletePartnerAssetAccessBody = extractJsonModelBodyParam<DeletePartnerAssetAccessBody>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId(request);



    int status_code = 500;
    std::shared_ptr<DeletePartnerAssetAccessResultsResponseArray> resultObject = std::make_shared<DeletePartnerAssetAccessResultsResponseArray>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_DELETE(businessId, deletePartnerAssetAccessBody);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto businessId = std::make_shared<>(bodyContent);
    auto updatePartnerAssetAccessBody = extractJsonModelBodyParam<UpdatePartnerAssetAccessBody>(bodyContent);

    // Getting the path params
    const std::string businessId = getPathParam_businessId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<UpdatePartnerAssetsResultsResponseArray> resultObject = std::make_shared<UpdatePartnerAssetsResultsResponseArray>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(businessId, updatePartnerAssetAccessBody);
    }
    catch(const BusinessAccessAssetsApiException& e) {
        std::tie(status_code, result) = handleBusinessAccessAssetsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<DeletePartnerAssetAccessResultsResponseArray>> BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_DELETE(
        std::string const & businessId, std::shared_ptr<DeletePartnerAssetAccessBody> const & deletePartnerAssetAccessBody)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<UpdatePartnerAssetsResultsResponseArray>> BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::handler_PATCH(
    std::string const & businessId, std::shared_ptr<UpdatePartnerAssetAccessBody> const & updatePartnerAssetAccessBody)
{
    throw BusinessAccessAssetsApiException(501, "Not implemented");
}

std::string BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

BusinessAccessAssetsApi::BusinessAccessAssetsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

BusinessAccessAssetsApi::~BusinessAccessAssetsApi() {}

void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource> spBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource = spBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource> spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource = spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource> spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource = spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource> spBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource = spBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource> spBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource = spBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource> spBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource = spBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource> spBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource = spBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource);
}
void BusinessAccessAssetsApi::setBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource(std::shared_ptr<BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource> spBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource) {
    m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource = spBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource;
    m_service->publish(m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource);
}


void BusinessAccessAssetsApi::publishDefaultResources() {
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idAsset_groupsResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idMembersResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idAssetsAsset_idPartnersResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idAssetsResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idAssetsResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idMembersMember_idAssetsResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idMembersAssetsAccessResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idPartnersPartner_idAssetsResource>());
    }
    if (!m_spBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource) {
        setBusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource(std::make_shared<BusinessAccessAssetsApiBusinessesBusiness_idPartnersAssetsResource>());
    }
}

std::shared_ptr<restbed::Service> BusinessAccessAssetsApi::service() {
    return m_service;
}


}
}
}
}

