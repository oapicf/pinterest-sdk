/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CustomerListsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

CustomerListsApiException::CustomerListsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CustomerListsApiException::getStatus() const
{
    return m_status;
}
const char* CustomerListsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::CustomerListsApiAd_accountsAd_account_idCustomer_listsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/customer_lists/");
	this->set_method_handler("POST",
		std::bind(&CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::~CustomerListsApiAd_accountsAd_account_idCustomer_listsResource()
{
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handleCustomerListsApiException(const CustomerListsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto customerListCreate = extractJsonModelBodyParam<CustomerListCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<CustomerList> resultObject = std::make_shared<CustomerList>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, customerListCreate);
    }
    catch(const CustomerListsApiException& e) {
        std::tie(status_code, result) = handleCustomerListsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);

    const bool excludeNca = getQueryParam_excludeNca_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Customer_lists_list_200_response> resultObject = std::make_shared<Customer_lists_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, bookmark, pageSize, order, excludeNca);
    }
    catch(const CustomerListsApiException& e) {
        std::tie(status_code, result) = handleCustomerListsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<CustomerList>> CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_POST(
        std::string const & adAccountId, std::shared_ptr<CustomerListCreate> const & customerListCreate)
{
    throw CustomerListsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Customer_lists_list_200_response>> CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::handler_GET(
    std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize, std::shared_ptr<Pinterest_Lib_PaginationOrder> const & order, bool const & excludeNca)
{
    throw CustomerListsApiException(501, "Not implemented");
}

std::string CustomerListsApiAd_accountsAd_account_idCustomer_listsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/customer_lists/{customer_list_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::~CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource()
{
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handleCustomerListsApiException(const CustomerListsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);
    const std::string customerListId = getPathParam_customerListId(request);



    int status_code = 500;
    std::shared_ptr<CustomerList> resultObject = std::make_shared<CustomerList>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adAccountId, customerListId);
    }
    catch(const CustomerListsApiException& e) {
        std::tie(status_code, result) = handleCustomerListsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto customerListId = std::make_shared<>(bodyContent);
    auto customerListUpdateWithRequiredBody = extractJsonModelBodyParam<CustomerListUpdateWithRequiredBody>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    const std::string customerListId = getPathParam_customerListId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<CustomerList> resultObject = std::make_shared<CustomerList>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, customerListId, customerListUpdateWithRequiredBody);
    }
    catch(const CustomerListsApiException& e) {
        std::tie(status_code, result) = handleCustomerListsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<CustomerList>> CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_GET(
        std::string const & adAccountId, std::string const & customerListId)
{
    throw CustomerListsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<CustomerList>> CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::handler_PATCH(
    std::string const & adAccountId, std::string const & customerListId, std::shared_ptr<CustomerListUpdateWithRequiredBody> const & customerListUpdateWithRequiredBody)
{
    throw CustomerListsApiException(501, "Not implemented");
}

std::string CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

CustomerListsApi::CustomerListsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CustomerListsApi::~CustomerListsApi() {}

void CustomerListsApi::setCustomerListsApiAd_accountsAd_account_idCustomer_listsResource(std::shared_ptr<CustomerListsApiAd_accountsAd_account_idCustomer_listsResource> spCustomerListsApiAd_accountsAd_account_idCustomer_listsResource) {
    m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsResource = spCustomerListsApiAd_accountsAd_account_idCustomer_listsResource;
    m_service->publish(m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsResource);
}
void CustomerListsApi::setCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource(std::shared_ptr<CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource> spCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource) {
    m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource = spCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource;
    m_service->publish(m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource);
}


void CustomerListsApi::publishDefaultResources() {
    if (!m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsResource) {
        setCustomerListsApiAd_accountsAd_account_idCustomer_listsResource(std::make_shared<CustomerListsApiAd_accountsAd_account_idCustomer_listsResource>());
    }
    if (!m_spCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource) {
        setCustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource(std::make_shared<CustomerListsApiAd_accountsAd_account_idCustomer_listsCustomer_list_idResource>());
    }
}

std::shared_ptr<restbed::Service> CustomerListsApi::service() {
    return m_service;
}


}
}
}
}

