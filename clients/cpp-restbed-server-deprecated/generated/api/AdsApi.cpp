/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "AdsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

AdsApiException::AdsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int AdsApiException::getStatus() const
{
    return m_status;
}
const char* AdsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

AdsApiAd_accountsAd_account_idAd_previewsResource::AdsApiAd_accountsAd_account_idAd_previewsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/ad_previews/");
	this->set_method_handler("POST",
		std::bind(&AdsApiAd_accountsAd_account_idAd_previewsResource::handler_POST_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idAd_previewsResource::~AdsApiAd_accountsAd_account_idAd_previewsResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAd_previewsResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAd_previewsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAd_previewsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idAd_previewsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idAd_previewsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idAd_previewsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idAd_previewsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto adPreviewRequest = extractJsonModelBodyParam<AdPreviewRequest>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<AdPreviewURLResponse> resultObject = std::make_shared<AdPreviewURLResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, adPreviewRequest);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<AdPreviewURLResponse>> AdsApiAd_accountsAd_account_idAd_previewsResource::handler_POST(
        std::string const & adAccountId, std::shared_ptr<AdPreviewRequest> const & adPreviewRequest)
{
    throw AdsApiException(501, "Not implemented");
}


std::string AdsApiAd_accountsAd_account_idAd_previewsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/ads/targeting_analytics/");
	this->set_method_handler("GET",
		std::bind(&AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::~AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string startDate = getQueryParam_startDate(request);
    const std::string endDate = getQueryParam_endDate(request);
    const bool sortAscending = getQueryParam_sortAscending(request);


    int status_code = 500;
    std::shared_ptr<MetricsResponse> resultObject = std::make_shared<MetricsResponse>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adAccountId, adIds, startDate, endDate, targetingTypes, columns, granularity, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, attributionTypes, reportingTimezone, sortColumns, sortAscending);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<MetricsResponse>> AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::handler_GET(
        std::string const & adAccountId, std::vector<std::shared_ptr<std::string>> const & adIds, std::string const & startDate, std::string const & endDate, std::vector<std::shared_ptr<AdsAnalyticsAdTargetingType>> const & targetingTypes, std::vector<std::shared_ptr<ReportingColumnSync>> const & columns, std::shared_ptr<Granularity> const & granularity, std::shared_ptr<ConversionAttributionWindowDays> const & clickWindowDays, std::shared_ptr<ConversionAttributionWindowDays> const & engagementWindowDays, std::shared_ptr<ConversionAttributionWindowDays> const & viewWindowDays, std::shared_ptr<ConversionReportTimeType> const & conversionReportTime, std::vector<std::shared_ptr<ConversionReportAttributionType>> const & attributionTypes, std::shared_ptr<ReportingTimeZone> const & reportingTimezone, std::vector<std::string> const & sortColumns, bool const & sortAscending)
{
    throw AdsApiException(501, "Not implemented");
}


std::string AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdsApiAd_accountsAd_account_idAdsAnalyticsResource::AdsApiAd_accountsAd_account_idAdsAnalyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/ads/analytics/");
	this->set_method_handler("GET",
		std::bind(&AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idAdsAnalyticsResource::~AdsApiAd_accountsAd_account_idAdsAnalyticsResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idAdsAnalyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idAdsAnalyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idAdsAnalyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);

    // Getting the query params
    const std::string startDate = getQueryParam_startDate(request);
    const std::string endDate = getQueryParam_endDate(request);
    const double clickWindowDays = getQueryParam_clickWindowDays(request);
    const double engagementWindowDays = getQueryParam_engagementWindowDays(request);
    const double viewWindowDays = getQueryParam_viewWindowDays(request);
    const std::string conversionReportTime = getQueryParam_conversionReportTime(request);


    int status_code = 500;
    std::vector<std::shared_ptr<AdsAnalytics>> resultObject = std::vector<std::shared_ptr<AdsAnalytics>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(startDate, endDate, columns, granularity, adAccountId, pinIds, adIds, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, campaignIds, reportingTimezone);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<AdsAnalytics>>> AdsApiAd_accountsAd_account_idAdsAnalyticsResource::handler_GET(
        std::string const & startDate, std::string const & endDate, std::vector<std::shared_ptr<ReportingColumnSync>> const & columns, std::shared_ptr<Granularity> const & granularity, std::string const & adAccountId, std::vector<std::string> const & pinIds, std::vector<std::shared_ptr<std::string>> const & adIds, double const & clickWindowDays, double const & engagementWindowDays, double const & viewWindowDays, std::string const & conversionReportTime, std::vector<std::shared_ptr<std::string>> const & campaignIds, std::shared_ptr<ReportingTimeZone> const & reportingTimezone)
{
    throw AdsApiException(501, "Not implemented");
}


std::string AdsApiAd_accountsAd_account_idAdsAnalyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdsApiAd_accountsAd_account_idAdsResource::AdsApiAd_accountsAd_account_idAdsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/ads/");
	this->set_method_handler("POST",
		std::bind(&AdsApiAd_accountsAd_account_idAdsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&AdsApiAd_accountsAd_account_idAdsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&AdsApiAd_accountsAd_account_idAdsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idAdsResource::~AdsApiAd_accountsAd_account_idAdsResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idAdsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idAdsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idAdsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idAdsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto adCreate = extractJsonArrayBodyParam<AdCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<AdBatchWriteResponseModel> resultObject = std::make_shared<AdBatchWriteResponseModel>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, adCreate);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void AdsApiAd_accountsAd_account_idAdsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Ads_list_200_response> resultObject = std::make_shared<Ads_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, bookmark, pageSize, order, campaignIds, adGroupIds, adIds, entityStatuses);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void AdsApiAd_accountsAd_account_idAdsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto adAccountId = std::make_shared<>(bodyContent);
    auto adBatchUpdate = extractJsonArrayBodyParam<AdBatchUpdate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<AdBatchWriteResponseModel> resultObject = std::make_shared<AdBatchWriteResponseModel>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, adBatchUpdate);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<AdBatchWriteResponseModel>> AdsApiAd_accountsAd_account_idAdsResource::handler_POST(
        std::string const & adAccountId, std::vector<std::shared_ptr<AdCreate>> const & adCreate)
{
    throw AdsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Ads_list_200_response>> AdsApiAd_accountsAd_account_idAdsResource::handler_GET(
    std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize, std::shared_ptr<Pinterest_Lib_PaginationOrder> const & order, std::vector<std::shared_ptr<std::string>> const & campaignIds, std::vector<std::shared_ptr<std::string>> const & adGroupIds, std::vector<std::shared_ptr<std::string>> const & adIds, std::vector<std::shared_ptr<EntityStatus>> const & entityStatuses)
{
    throw AdsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<AdBatchWriteResponseModel>> AdsApiAd_accountsAd_account_idAdsResource::handler_PATCH(
    std::string const & adAccountId, std::vector<std::shared_ptr<AdBatchUpdate>> const & adBatchUpdate)
{
    throw AdsApiException(501, "Not implemented");
}

std::string AdsApiAd_accountsAd_account_idAdsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdsApiAd_accountsAd_account_idAdsAd_idResource::AdsApiAd_accountsAd_account_idAdsAd_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/ads/{ad_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&AdsApiAd_accountsAd_account_idAdsAd_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idAdsAd_idResource::~AdsApiAd_accountsAd_account_idAdsAd_idResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAd_idResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAd_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idAdsAd_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idAdsAd_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idAdsAd_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idAdsAd_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idAdsAd_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string adId = getPathParam_adId(request);
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::shared_ptr<Ad> resultObject = std::make_shared<Ad>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(adId, adAccountId);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Ad>> AdsApiAd_accountsAd_account_idAdsAd_idResource::handler_GET(
        std::string const & adId, std::string const & adAccountId)
{
    throw AdsApiException(501, "Not implemented");
}


std::string AdsApiAd_accountsAd_account_idAdsAd_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::AdsApiAd_accountsAd_account_idCampaign_ad_previewResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaign_ad_preview/");
	this->set_method_handler("POST",
		std::bind(&AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("DELETE",
		std::bind(&AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_GET_internal, this,
			std::placeholders::_1));
}

AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::~AdsApiAd_accountsAd_account_idCampaign_ad_previewResource()
{
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handleAdsApiException(const AdsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto campaignAdPreviewCreate = extractJsonArrayBodyParam<CampaignAdPreviewCreate>(bodyContent);

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId(request);



    int status_code = 500;
    std::vector<std::shared_ptr<Campaign_ad_preview_create_200_response_inner>> resultObject = std::vector<std::shared_ptr<Campaign_ad_preview_create_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(adAccountId, campaignAdPreviewCreate);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params

    int status_code = 500;
    std::vector<std::shared_ptr<Campaign_ad_preview_delete_200_response_inner>> resultObject = std::vector<std::shared_ptr<Campaign_ad_preview_delete_200_response_inner>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_DELETE(adGroupIds, adAccountId);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 204) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 204, result.empty() ? "Resource deleted successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string adAccountId = getPathParam_adAccountId_x_extension(request);

    // Getting the query params

    int status_code = 500;
    std::vector<std::shared_ptr<CampaignAdPreview>> resultObject = std::vector<std::shared_ptr<CampaignAdPreview>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(adGroupIds, adAccountId);
    }
    catch(const AdsApiException& e) {
        std::tie(status_code, result) = handleAdsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::vector<std::shared_ptr<Campaign_ad_preview_create_200_response_inner>>> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_POST(
        std::string const & adAccountId, std::vector<std::shared_ptr<CampaignAdPreviewCreate>> const & campaignAdPreviewCreate)
{
    throw AdsApiException(501, "Not implemented");
}

std::pair<int, std::vector<std::shared_ptr<Campaign_ad_preview_delete_200_response_inner>>> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_DELETE(
    std::vector<std::shared_ptr<std::string>> const & adGroupIds, std::string const & adAccountId)
{
    throw AdsApiException(501, "Not implemented");
}
std::pair<int, std::vector<std::shared_ptr<CampaignAdPreview>>> AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::handler_GET(
    std::vector<std::shared_ptr<std::string>> const & adGroupIds, std::string const & adAccountId)
{
    throw AdsApiException(501, "Not implemented");
}

std::string AdsApiAd_accountsAd_account_idCampaign_ad_previewResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

AdsApi::AdsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

AdsApi::~AdsApi() {}

void AdsApi::setAdsApiAd_accountsAd_account_idAd_previewsResource(std::shared_ptr<AdsApiAd_accountsAd_account_idAd_previewsResource> spAdsApiAd_accountsAd_account_idAd_previewsResource) {
    m_spAdsApiAd_accountsAd_account_idAd_previewsResource = spAdsApiAd_accountsAd_account_idAd_previewsResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idAd_previewsResource);
}
void AdsApi::setAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource(std::shared_ptr<AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource> spAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource) {
    m_spAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource = spAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource);
}
void AdsApi::setAdsApiAd_accountsAd_account_idAdsAnalyticsResource(std::shared_ptr<AdsApiAd_accountsAd_account_idAdsAnalyticsResource> spAdsApiAd_accountsAd_account_idAdsAnalyticsResource) {
    m_spAdsApiAd_accountsAd_account_idAdsAnalyticsResource = spAdsApiAd_accountsAd_account_idAdsAnalyticsResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idAdsAnalyticsResource);
}
void AdsApi::setAdsApiAd_accountsAd_account_idAdsResource(std::shared_ptr<AdsApiAd_accountsAd_account_idAdsResource> spAdsApiAd_accountsAd_account_idAdsResource) {
    m_spAdsApiAd_accountsAd_account_idAdsResource = spAdsApiAd_accountsAd_account_idAdsResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idAdsResource);
}
void AdsApi::setAdsApiAd_accountsAd_account_idAdsAd_idResource(std::shared_ptr<AdsApiAd_accountsAd_account_idAdsAd_idResource> spAdsApiAd_accountsAd_account_idAdsAd_idResource) {
    m_spAdsApiAd_accountsAd_account_idAdsAd_idResource = spAdsApiAd_accountsAd_account_idAdsAd_idResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idAdsAd_idResource);
}
void AdsApi::setAdsApiAd_accountsAd_account_idCampaign_ad_previewResource(std::shared_ptr<AdsApiAd_accountsAd_account_idCampaign_ad_previewResource> spAdsApiAd_accountsAd_account_idCampaign_ad_previewResource) {
    m_spAdsApiAd_accountsAd_account_idCampaign_ad_previewResource = spAdsApiAd_accountsAd_account_idCampaign_ad_previewResource;
    m_service->publish(m_spAdsApiAd_accountsAd_account_idCampaign_ad_previewResource);
}


void AdsApi::publishDefaultResources() {
    if (!m_spAdsApiAd_accountsAd_account_idAd_previewsResource) {
        setAdsApiAd_accountsAd_account_idAd_previewsResource(std::make_shared<AdsApiAd_accountsAd_account_idAd_previewsResource>());
    }
    if (!m_spAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource) {
        setAdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource(std::make_shared<AdsApiAd_accountsAd_account_idAdsTargeting_analyticsResource>());
    }
    if (!m_spAdsApiAd_accountsAd_account_idAdsAnalyticsResource) {
        setAdsApiAd_accountsAd_account_idAdsAnalyticsResource(std::make_shared<AdsApiAd_accountsAd_account_idAdsAnalyticsResource>());
    }
    if (!m_spAdsApiAd_accountsAd_account_idAdsResource) {
        setAdsApiAd_accountsAd_account_idAdsResource(std::make_shared<AdsApiAd_accountsAd_account_idAdsResource>());
    }
    if (!m_spAdsApiAd_accountsAd_account_idAdsAd_idResource) {
        setAdsApiAd_accountsAd_account_idAdsAd_idResource(std::make_shared<AdsApiAd_accountsAd_account_idAdsAd_idResource>());
    }
    if (!m_spAdsApiAd_accountsAd_account_idCampaign_ad_previewResource) {
        setAdsApiAd_accountsAd_account_idCampaign_ad_previewResource(std::make_shared<AdsApiAd_accountsAd_account_idCampaign_ad_previewResource>());
    }
}

std::shared_ptr<restbed::Service> AdsApi::service() {
    return m_service;
}


}
}
}
}

