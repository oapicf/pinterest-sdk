/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "ResourcesApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

ResourcesApiException::ResourcesApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int ResourcesApiException::getStatus() const
{
    return m_status;
}
const char* ResourcesApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

ResourcesApiResourcesAd_account_countriesResource::ResourcesApiResourcesAd_account_countriesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/ad_account_countries/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesAd_account_countriesResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesAd_account_countriesResource::~ResourcesApiResourcesAd_account_countriesResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesAd_account_countriesResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesAd_account_countriesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesAd_account_countriesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesAd_account_countriesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesAd_account_countriesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesAd_account_countriesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesAd_account_countriesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();




    int status_code = 500;
    std::shared_ptr<Ad_account_countries_get_200_response> resultObject = std::make_shared<Ad_account_countries_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET();
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Ad_account_countries_get_200_response>> ResourcesApiResourcesAd_account_countriesResource::handler_GET(
        )
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesAd_account_countriesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
ResourcesApiResourcesDelivery_metricsResource::ResourcesApiResourcesDelivery_metricsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/delivery_metrics/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesDelivery_metricsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesDelivery_metricsResource::~ResourcesApiResourcesDelivery_metricsResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesDelivery_metricsResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesDelivery_metricsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesDelivery_metricsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesDelivery_metricsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesDelivery_metricsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesDelivery_metricsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesDelivery_metricsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params


    int status_code = 500;
    std::shared_ptr<Delivery_metrics_get_200_response> resultObject = std::make_shared<Delivery_metrics_get_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(reportType);
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Delivery_metrics_get_200_response>> ResourcesApiResourcesDelivery_metricsResource::handler_GET(
        std::shared_ptr<ReportType> const & reportType)
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesDelivery_metricsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
ResourcesApiResourcesTargetingInterestsInterest_idResource::ResourcesApiResourcesTargetingInterestsInterest_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/targeting/interests/{interest_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesTargetingInterestsInterest_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesTargetingInterestsInterest_idResource::~ResourcesApiResourcesTargetingInterestsInterest_idResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesTargetingInterestsInterest_idResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesTargetingInterestsInterest_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesTargetingInterestsInterest_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesTargetingInterestsInterest_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesTargetingInterestsInterest_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesTargetingInterestsInterest_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesTargetingInterestsInterest_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string interestId = getPathParam_interestId(request);



    int status_code = 500;
    std::shared_ptr<SingleInterestTargetingOption> resultObject = std::make_shared<SingleInterestTargetingOption>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(interestId);
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<SingleInterestTargetingOption>> ResourcesApiResourcesTargetingInterestsInterest_idResource::handler_GET(
        std::string const & interestId)
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesTargetingInterestsInterest_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
ResourcesApiResourcesLead_form_questionsResource::ResourcesApiResourcesLead_form_questionsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/lead_form_questions/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesLead_form_questionsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesLead_form_questionsResource::~ResourcesApiResourcesLead_form_questionsResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesLead_form_questionsResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesLead_form_questionsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesLead_form_questionsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesLead_form_questionsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesLead_form_questionsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesLead_form_questionsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesLead_form_questionsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();




    int status_code = 500;
    std::string result = "";

    try {
         status_code =
             handler_GET();
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


int ResourcesApiResourcesLead_form_questionsResource::handler_GET(
        )
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesLead_form_questionsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
ResourcesApiResourcesMetrics_ready_stateResource::ResourcesApiResourcesMetrics_ready_stateResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/metrics_ready_state/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesMetrics_ready_stateResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesMetrics_ready_stateResource::~ResourcesApiResourcesMetrics_ready_stateResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesMetrics_ready_stateResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesMetrics_ready_stateResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesMetrics_ready_stateResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesMetrics_ready_stateResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesMetrics_ready_stateResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesMetrics_ready_stateResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesMetrics_ready_stateResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();


    // Getting the query params
    const std::string date = getQueryParam_date(request);


    int status_code = 500;
    std::shared_ptr<BookClosed> resultObject = std::make_shared<BookClosed>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(date);
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<BookClosed>> ResourcesApiResourcesMetrics_ready_stateResource::handler_GET(
        std::string const & date)
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesMetrics_ready_stateResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
ResourcesApiResourcesTargetingTargeting_typeResource::ResourcesApiResourcesTargetingTargeting_typeResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/resources/targeting/{targeting_type: .*}/");
	this->set_method_handler("GET",
		std::bind(&ResourcesApiResourcesTargetingTargeting_typeResource::handler_GET_internal, this,
			std::placeholders::_1));
}

ResourcesApiResourcesTargetingTargeting_typeResource::~ResourcesApiResourcesTargetingTargeting_typeResource()
{
}

std::pair<int, std::string> ResourcesApiResourcesTargetingTargeting_typeResource::handleResourcesApiException(const ResourcesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> ResourcesApiResourcesTargetingTargeting_typeResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> ResourcesApiResourcesTargetingTargeting_typeResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void ResourcesApiResourcesTargetingTargeting_typeResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void ResourcesApiResourcesTargetingTargeting_typeResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void ResourcesApiResourcesTargetingTargeting_typeResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void ResourcesApiResourcesTargetingTargeting_typeResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);
    const std::string clientId = getQueryParam_clientId(request);
    const std::string oauthSignature = getQueryParam_oauthSignature(request);
    const std::string timestamp = getQueryParam_timestamp(request);


    int status_code = 500;
    std::vector<std::shared_ptr<Object>> resultObject = std::vector<std::shared_ptr<Object>>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(targetingType, adAccountId, clientId, oauthSignature, timestamp);
    }
    catch(const ResourcesApiException& e) {
        std::tie(status_code, result) = handleResourcesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::vector<std::shared_ptr<Object>>> ResourcesApiResourcesTargetingTargeting_typeResource::handler_GET(
        std::shared_ptr<PublicTargetingType> const & targetingType, std::string const & adAccountId, std::string const & clientId, std::string const & oauthSignature, std::string const & timestamp)
{
    throw ResourcesApiException(501, "Not implemented");
}


std::string ResourcesApiResourcesTargetingTargeting_typeResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

ResourcesApi::ResourcesApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

ResourcesApi::~ResourcesApi() {}

void ResourcesApi::setResourcesApiResourcesAd_account_countriesResource(std::shared_ptr<ResourcesApiResourcesAd_account_countriesResource> spResourcesApiResourcesAd_account_countriesResource) {
    m_spResourcesApiResourcesAd_account_countriesResource = spResourcesApiResourcesAd_account_countriesResource;
    m_service->publish(m_spResourcesApiResourcesAd_account_countriesResource);
}
void ResourcesApi::setResourcesApiResourcesDelivery_metricsResource(std::shared_ptr<ResourcesApiResourcesDelivery_metricsResource> spResourcesApiResourcesDelivery_metricsResource) {
    m_spResourcesApiResourcesDelivery_metricsResource = spResourcesApiResourcesDelivery_metricsResource;
    m_service->publish(m_spResourcesApiResourcesDelivery_metricsResource);
}
void ResourcesApi::setResourcesApiResourcesTargetingInterestsInterest_idResource(std::shared_ptr<ResourcesApiResourcesTargetingInterestsInterest_idResource> spResourcesApiResourcesTargetingInterestsInterest_idResource) {
    m_spResourcesApiResourcesTargetingInterestsInterest_idResource = spResourcesApiResourcesTargetingInterestsInterest_idResource;
    m_service->publish(m_spResourcesApiResourcesTargetingInterestsInterest_idResource);
}
void ResourcesApi::setResourcesApiResourcesLead_form_questionsResource(std::shared_ptr<ResourcesApiResourcesLead_form_questionsResource> spResourcesApiResourcesLead_form_questionsResource) {
    m_spResourcesApiResourcesLead_form_questionsResource = spResourcesApiResourcesLead_form_questionsResource;
    m_service->publish(m_spResourcesApiResourcesLead_form_questionsResource);
}
void ResourcesApi::setResourcesApiResourcesMetrics_ready_stateResource(std::shared_ptr<ResourcesApiResourcesMetrics_ready_stateResource> spResourcesApiResourcesMetrics_ready_stateResource) {
    m_spResourcesApiResourcesMetrics_ready_stateResource = spResourcesApiResourcesMetrics_ready_stateResource;
    m_service->publish(m_spResourcesApiResourcesMetrics_ready_stateResource);
}
void ResourcesApi::setResourcesApiResourcesTargetingTargeting_typeResource(std::shared_ptr<ResourcesApiResourcesTargetingTargeting_typeResource> spResourcesApiResourcesTargetingTargeting_typeResource) {
    m_spResourcesApiResourcesTargetingTargeting_typeResource = spResourcesApiResourcesTargetingTargeting_typeResource;
    m_service->publish(m_spResourcesApiResourcesTargetingTargeting_typeResource);
}


void ResourcesApi::publishDefaultResources() {
    if (!m_spResourcesApiResourcesAd_account_countriesResource) {
        setResourcesApiResourcesAd_account_countriesResource(std::make_shared<ResourcesApiResourcesAd_account_countriesResource>());
    }
    if (!m_spResourcesApiResourcesDelivery_metricsResource) {
        setResourcesApiResourcesDelivery_metricsResource(std::make_shared<ResourcesApiResourcesDelivery_metricsResource>());
    }
    if (!m_spResourcesApiResourcesTargetingInterestsInterest_idResource) {
        setResourcesApiResourcesTargetingInterestsInterest_idResource(std::make_shared<ResourcesApiResourcesTargetingInterestsInterest_idResource>());
    }
    if (!m_spResourcesApiResourcesLead_form_questionsResource) {
        setResourcesApiResourcesLead_form_questionsResource(std::make_shared<ResourcesApiResourcesLead_form_questionsResource>());
    }
    if (!m_spResourcesApiResourcesMetrics_ready_stateResource) {
        setResourcesApiResourcesMetrics_ready_stateResource(std::make_shared<ResourcesApiResourcesMetrics_ready_stateResource>());
    }
    if (!m_spResourcesApiResourcesTargetingTargeting_typeResource) {
        setResourcesApiResourcesTargetingTargeting_typeResource(std::make_shared<ResourcesApiResourcesTargetingTargeting_typeResource>());
    }
}

std::shared_ptr<restbed::Service> ResourcesApi::service() {
    return m_service;
}


}
}
}
}

