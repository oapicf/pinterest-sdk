/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CatalogFeedsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

CatalogFeedsApiException::CatalogFeedsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CatalogFeedsApiException::getStatus() const
{
    return m_status;
}
const char* CatalogFeedsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/feeds/{feed_id: .*}/processing_results/");
	this->set_method_handler("GET",
		std::bind(&CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::~CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource()
{
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handleCatalogFeedsApiException(const CatalogFeedsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string feedId = getPathParam_feedId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Feed_processing_results_list_200_response> resultObject = std::make_shared<Feed_processing_results_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(feedId, adAccountId, bookmark, pageSize);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Feed_processing_results_list_200_response>> CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::handler_GET(
        std::string const & feedId, std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}


std::string CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogFeedsApiCatalogsFeedsResource::CatalogFeedsApiCatalogsFeedsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/feeds/");
	this->set_method_handler("POST",
		std::bind(&CatalogFeedsApiCatalogsFeedsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CatalogFeedsApiCatalogsFeedsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CatalogFeedsApiCatalogsFeedsResource::~CatalogFeedsApiCatalogsFeedsResource()
{
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsResource::handleCatalogFeedsApiException(const CatalogFeedsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogFeedsApiCatalogsFeedsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogFeedsApiCatalogsFeedsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogFeedsApiCatalogsFeedsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogFeedsApiCatalogsFeedsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto catalogsFeedCreateRequestSchema = extractJsonModelBodyParam<CatalogsFeedCreateRequestSchema>(bodyContent);


    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<CatalogsFeed> resultObject = std::make_shared<CatalogsFeed>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(catalogsFeedCreateRequestSchema, adAccountId);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "Resource create operation completed successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CatalogFeedsApiCatalogsFeedsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the query params
    const std::string catalogId = getQueryParam_catalogId_x_extension(request);

    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);

    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Feeds_list_200_response> resultObject = std::make_shared<Feeds_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(catalogId, adAccountId, bookmark, pageSize);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<CatalogsFeed>> CatalogFeedsApiCatalogsFeedsResource::handler_POST(
        std::shared_ptr<CatalogsFeedCreateRequestSchema> const & catalogsFeedCreateRequestSchema, std::string const & adAccountId)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Feeds_list_200_response>> CatalogFeedsApiCatalogsFeedsResource::handler_GET(
    std::string const & catalogId, std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}

std::string CatalogFeedsApiCatalogsFeedsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogFeedsApiCatalogsFeedsFeed_idResource::CatalogFeedsApiCatalogsFeedsFeed_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/feeds/{feed_id: .*}/");
	this->set_method_handler("DELETE",
		std::bind(&CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

CatalogFeedsApiCatalogsFeedsFeed_idResource::~CatalogFeedsApiCatalogsFeedsFeed_idResource()
{
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idResource::handleCatalogFeedsApiException(const CatalogFeedsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogFeedsApiCatalogsFeedsFeed_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogFeedsApiCatalogsFeedsFeed_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string feedId = getPathParam_feedId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<CatalogsFeed> resultObject = std::make_shared<CatalogsFeed>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_DELETE(feedId, adAccountId);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 204) {

        const constexpr auto contentType = "text/plain";
        returnResponse(session, 204, result.empty() ? "Resource deleted successfully." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the path params
    const std::string feedId = getPathParam_feedId_x_extension(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<CatalogsFeed> resultObject = std::make_shared<CatalogsFeed>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(feedId, adAccountId);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}
// x-extension
void CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // body params or form params here from the body content string
    auto feedId = std::make_shared<>(bodyContent);
    auto catalogsFeedUpdateRequestSchema = extractJsonModelBodyParam<CatalogsFeedUpdateRequestSchema>(bodyContent);
    auto adAccountId = std::make_shared<>(bodyContent);

    // Getting the path params
    const std::string feedId = getPathParam_feedId_x_extension(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId_x_extension(request);


    int status_code = 500;
    std::shared_ptr<CatalogsFeed> resultObject = std::make_shared<CatalogsFeed>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(feedId, catalogsFeedUpdateRequestSchema, adAccountId);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<CatalogsFeed>> CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_DELETE(
        std::string const & feedId, std::string const & adAccountId)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<CatalogsFeed>> CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_GET(
    std::string const & feedId, std::string const & adAccountId)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}
std::pair<int, std::shared_ptr<CatalogsFeed>> CatalogFeedsApiCatalogsFeedsFeed_idResource::handler_PATCH(
    std::string const & feedId, std::shared_ptr<CatalogsFeedUpdateRequestSchema> const & catalogsFeedUpdateRequestSchema, std::string const & adAccountId)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}

std::string CatalogFeedsApiCatalogsFeedsFeed_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::CatalogFeedsApiCatalogsFeedsFeed_idIngestResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/feeds/{feed_id: .*}/ingest/");
	this->set_method_handler("POST",
		std::bind(&CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::~CatalogFeedsApiCatalogsFeedsFeed_idIngestResource()
{
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handleCatalogFeedsApiException(const CatalogFeedsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string feedId = getPathParam_feedId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);


    int status_code = 500;
    std::shared_ptr<CatalogsFeedIngestion> resultObject = std::make_shared<CatalogsFeedIngestion>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(feedId, adAccountId);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<CatalogsFeedIngestion>> CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::handler_POST(
        std::string const & feedId, std::string const & adAccountId)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}


std::string CatalogFeedsApiCatalogsFeedsFeed_idIngestResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/processing_results/{processing_result_id: .*}/item_issues/");
	this->set_method_handler("GET",
		std::bind(&CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::~CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource()
{
}

std::pair<int, std::string> CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handleCatalogFeedsApiException(const CatalogFeedsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string processingResultId = getPathParam_processingResultId(request);

    // Getting the query params
    const std::string adAccountId = getQueryParam_adAccountId(request);
    const std::string bookmark = getQueryParam_bookmark(request);
    const int32_t pageSize = getQueryParam_pageSize(request);


    int status_code = 500;
    std::shared_ptr<Items_issues_list_200_response> resultObject = std::make_shared<Items_issues_list_200_response>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(processingResultId, itemNumbers, itemValidationIssue, adAccountId, bookmark, pageSize);
    }
    catch(const CatalogFeedsApiException& e) {
        std::tie(status_code, result) = handleCatalogFeedsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "The request has succeeded." : result, contentType);
        return;
    }
    if (status_code == 400) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 400, result.empty() ? "The request could not be understood by the server due to unexpected data." : result, contentType);
        return;
    }
    if (status_code == 401) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 401, result.empty() ? "Authentication is required and has either failed or not been provided." : result, contentType);
        return;
    }
    if (status_code == 403) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 403, result.empty() ? "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource." : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "The requested resource could not be found on this server." : result, contentType);
        return;
    }
    if (status_code == 429) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 429, result.empty() ? "The user has sent too many requests in a given amount of time and is being rate limited." : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "An unexpected error response." : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<Items_issues_list_200_response>> CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::handler_GET(
        std::string const & processingResultId, std::vector<int32_t> const & itemNumbers, std::shared_ptr<CatalogsItemValidationIssue> const & itemValidationIssue, std::string const & adAccountId, std::string const & bookmark, int32_t const & pageSize)
{
    throw CatalogFeedsApiException(501, "Not implemented");
}


std::string CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

CatalogFeedsApi::CatalogFeedsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CatalogFeedsApi::~CatalogFeedsApi() {}

void CatalogFeedsApi::setCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource(std::shared_ptr<CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource> spCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource) {
    m_spCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource = spCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource;
    m_service->publish(m_spCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource);
}
void CatalogFeedsApi::setCatalogFeedsApiCatalogsFeedsResource(std::shared_ptr<CatalogFeedsApiCatalogsFeedsResource> spCatalogFeedsApiCatalogsFeedsResource) {
    m_spCatalogFeedsApiCatalogsFeedsResource = spCatalogFeedsApiCatalogsFeedsResource;
    m_service->publish(m_spCatalogFeedsApiCatalogsFeedsResource);
}
void CatalogFeedsApi::setCatalogFeedsApiCatalogsFeedsFeed_idResource(std::shared_ptr<CatalogFeedsApiCatalogsFeedsFeed_idResource> spCatalogFeedsApiCatalogsFeedsFeed_idResource) {
    m_spCatalogFeedsApiCatalogsFeedsFeed_idResource = spCatalogFeedsApiCatalogsFeedsFeed_idResource;
    m_service->publish(m_spCatalogFeedsApiCatalogsFeedsFeed_idResource);
}
void CatalogFeedsApi::setCatalogFeedsApiCatalogsFeedsFeed_idIngestResource(std::shared_ptr<CatalogFeedsApiCatalogsFeedsFeed_idIngestResource> spCatalogFeedsApiCatalogsFeedsFeed_idIngestResource) {
    m_spCatalogFeedsApiCatalogsFeedsFeed_idIngestResource = spCatalogFeedsApiCatalogsFeedsFeed_idIngestResource;
    m_service->publish(m_spCatalogFeedsApiCatalogsFeedsFeed_idIngestResource);
}
void CatalogFeedsApi::setCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource(std::shared_ptr<CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource> spCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource) {
    m_spCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource = spCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource;
    m_service->publish(m_spCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource);
}


void CatalogFeedsApi::publishDefaultResources() {
    if (!m_spCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource) {
        setCatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource(std::make_shared<CatalogFeedsApiCatalogsFeedsFeed_idProcessing_resultsResource>());
    }
    if (!m_spCatalogFeedsApiCatalogsFeedsResource) {
        setCatalogFeedsApiCatalogsFeedsResource(std::make_shared<CatalogFeedsApiCatalogsFeedsResource>());
    }
    if (!m_spCatalogFeedsApiCatalogsFeedsFeed_idResource) {
        setCatalogFeedsApiCatalogsFeedsFeed_idResource(std::make_shared<CatalogFeedsApiCatalogsFeedsFeed_idResource>());
    }
    if (!m_spCatalogFeedsApiCatalogsFeedsFeed_idIngestResource) {
        setCatalogFeedsApiCatalogsFeedsFeed_idIngestResource(std::make_shared<CatalogFeedsApiCatalogsFeedsFeed_idIngestResource>());
    }
    if (!m_spCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource) {
        setCatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource(std::make_shared<CatalogFeedsApiCatalogsProcessing_resultsProcessing_result_idItem_issuesResource>());
    }
}

std::shared_ptr<restbed::Service> CatalogFeedsApi::service() {
    return m_service;
}


}
}
}
}

