/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.14.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import QuizPinQuestion from './QuizPinQuestion';
import QuizPinResult from './QuizPinResult';

/**
 * The QuizPinData model module.
 * @module model/QuizPinData
 * @version 1.1.1-pre.0
 */
class QuizPinData {
    /**
     * Constructs a new <code>QuizPinData</code>.
     * This field includes all quiz data including questions, options, and results.
     * @alias module:model/QuizPinData
     */
    constructor() { 
        
        QuizPinData.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>QuizPinData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QuizPinData} obj Optional instance to populate.
     * @return {module:model/QuizPinData} The populated <code>QuizPinData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QuizPinData();

            if (data.hasOwnProperty('questions')) {
                obj['questions'] = ApiClient.convertToType(data['questions'], [QuizPinQuestion]);
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [QuizPinResult]);
            }
            if (data.hasOwnProperty('tie_breaker_type')) {
                obj['tie_breaker_type'] = ApiClient.convertToType(data['tie_breaker_type'], 'String');
            }
            if (data.hasOwnProperty('tie_breaker_custom_result')) {
                obj['tie_breaker_custom_result'] = QuizPinResult.constructFromObject(data['tie_breaker_custom_result']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QuizPinData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QuizPinData</code>.
     */
    static validateJSON(data) {
        if (data['questions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['questions'])) {
                throw new Error("Expected the field `questions` to be an array in the JSON data but got " + data['questions']);
            }
            // validate the optional field `questions` (array)
            for (const item of data['questions']) {
                QuizPinQuestion.validateJSON(item);
            };
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                QuizPinResult.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['tie_breaker_type'] && !(typeof data['tie_breaker_type'] === 'string' || data['tie_breaker_type'] instanceof String)) {
            throw new Error("Expected the field `tie_breaker_type` to be a primitive type in the JSON string but got " + data['tie_breaker_type']);
        }
        // validate the optional field `tie_breaker_custom_result`
        if (data['tie_breaker_custom_result']) { // data not null
          QuizPinResult.validateJSON(data['tie_breaker_custom_result']);
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/QuizPinQuestion>} questions
 */
QuizPinData.prototype['questions'] = undefined;

/**
 * @member {Array.<module:model/QuizPinResult>} results
 */
QuizPinData.prototype['results'] = undefined;

/**
 * Quiz ad tie breaker type, default is RANDOM
 * @member {module:model/QuizPinData.TieBreakerTypeEnum} tie_breaker_type
 */
QuizPinData.prototype['tie_breaker_type'] = undefined;

/**
 * @member {module:model/QuizPinResult} tie_breaker_custom_result
 */
QuizPinData.prototype['tie_breaker_custom_result'] = undefined;





/**
 * Allowed values for the <code>tie_breaker_type</code> property.
 * @enum {String}
 * @readonly
 */
QuizPinData['TieBreakerTypeEnum'] = {

    /**
     * value: "RANDOM"
     * @const
     */
    "RANDOM": "RANDOM",

    /**
     * value: "CUSTOM"
     * @const
     */
    "CUSTOM": "CUSTOM"
};



export default QuizPinData;

