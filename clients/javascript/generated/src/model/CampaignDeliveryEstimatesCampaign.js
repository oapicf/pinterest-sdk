/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdGroupDeliveryEstimates from './AdGroupDeliveryEstimates';
import BudgetDurationType from './BudgetDurationType';
import DeliveryEstimateObjectiveType from './DeliveryEstimateObjectiveType';

/**
 * The CampaignDeliveryEstimatesCampaign model module.
 * @module model/CampaignDeliveryEstimatesCampaign
 * @version 2.0.1-pre.0
 */
class CampaignDeliveryEstimatesCampaign {
    /**
     * Constructs a new <code>CampaignDeliveryEstimatesCampaign</code>.
     * Campaign configuration for delivery estimates.
     * @alias module:model/CampaignDeliveryEstimatesCampaign
     * @param adGroups {Array.<module:model/AdGroupDeliveryEstimates>} 
     * @param budgetDurationType {module:model/BudgetDurationType} Duration type of the budget
     * @param objectiveType {module:model/DeliveryEstimateObjectiveType} 
     * @param startDate {String} Start date of the date range for an ad campaign, pattern YYYY-MM-DD.
     */
    constructor(adGroups, budgetDurationType, objectiveType, startDate) { 
        
        CampaignDeliveryEstimatesCampaign.initialize(this, adGroups, budgetDurationType, objectiveType, startDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, adGroups, budgetDurationType, objectiveType, startDate) { 
        obj['ad_groups'] = adGroups;
        obj['budget_duration_type'] = budgetDurationType;
        obj['objective_type'] = objectiveType;
        obj['start_date'] = startDate;
    }

    /**
     * Constructs a <code>CampaignDeliveryEstimatesCampaign</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CampaignDeliveryEstimatesCampaign} obj Optional instance to populate.
     * @return {module:model/CampaignDeliveryEstimatesCampaign} The populated <code>CampaignDeliveryEstimatesCampaign</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CampaignDeliveryEstimatesCampaign();

            if (data.hasOwnProperty('ad_groups')) {
                obj['ad_groups'] = ApiClient.convertToType(data['ad_groups'], [AdGroupDeliveryEstimates]);
            }
            if (data.hasOwnProperty('budget_duration_type')) {
                obj['budget_duration_type'] = ApiClient.convertToType(data['budget_duration_type'], BudgetDurationType);
            }
            if (data.hasOwnProperty('daily_spend_cap')) {
                obj['daily_spend_cap'] = ApiClient.convertToType(data['daily_spend_cap'], 'Number');
            }
            if (data.hasOwnProperty('end_date')) {
                obj['end_date'] = ApiClient.convertToType(data['end_date'], 'String');
            }
            if (data.hasOwnProperty('lifetime_spend_cap')) {
                obj['lifetime_spend_cap'] = ApiClient.convertToType(data['lifetime_spend_cap'], 'Number');
            }
            if (data.hasOwnProperty('objective_type')) {
                obj['objective_type'] = DeliveryEstimateObjectiveType.constructFromObject(data['objective_type']);
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CampaignDeliveryEstimatesCampaign</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CampaignDeliveryEstimatesCampaign</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CampaignDeliveryEstimatesCampaign.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['ad_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ad_groups'])) {
                throw new Error("Expected the field `ad_groups` to be an array in the JSON data but got " + data['ad_groups']);
            }
            // validate the optional field `ad_groups` (array)
            for (const item of data['ad_groups']) {
                AdGroupDeliveryEstimates.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['end_date'] && !(typeof data['end_date'] === 'string' || data['end_date'] instanceof String)) {
            throw new Error("Expected the field `end_date` to be a primitive type in the JSON string but got " + data['end_date']);
        }
        // ensure the json data is a string
        if (data['start_date'] && !(typeof data['start_date'] === 'string' || data['start_date'] instanceof String)) {
            throw new Error("Expected the field `start_date` to be a primitive type in the JSON string but got " + data['start_date']);
        }

        return true;
    }


}

CampaignDeliveryEstimatesCampaign.RequiredProperties = ["ad_groups", "budget_duration_type", "objective_type", "start_date"];

/**
 * @member {Array.<module:model/AdGroupDeliveryEstimates>} ad_groups
 */
CampaignDeliveryEstimatesCampaign.prototype['ad_groups'] = undefined;

/**
 * Duration type of the budget
 * @member {module:model/BudgetDurationType} budget_duration_type
 */
CampaignDeliveryEstimatesCampaign.prototype['budget_duration_type'] = undefined;

/**
 * Campaign daily spend cap. When the budget_duration_type is FIXED_DAILY or FLEXIBLE_DAILY, this field is required.
 * @member {Number} daily_spend_cap
 */
CampaignDeliveryEstimatesCampaign.prototype['daily_spend_cap'] = undefined;

/**
 * End date of the date range for an ad campaign, pattern YYYY-MM-DD. Required when budget_duration_type is LIFETIME.
 * @member {String} end_date
 */
CampaignDeliveryEstimatesCampaign.prototype['end_date'] = undefined;

/**
 * Campaign total spending cap. When the budget_duration_type is LIFETIME, this field is required. The end date field is also required for lifetime spending.
 * @member {Number} lifetime_spend_cap
 */
CampaignDeliveryEstimatesCampaign.prototype['lifetime_spend_cap'] = undefined;

/**
 * @member {module:model/DeliveryEstimateObjectiveType} objective_type
 */
CampaignDeliveryEstimatesCampaign.prototype['objective_type'] = undefined;

/**
 * Start date of the date range for an ad campaign, pattern YYYY-MM-DD.
 * @member {String} start_date
 */
CampaignDeliveryEstimatesCampaign.prototype['start_date'] = undefined;






export default CampaignDeliveryEstimatesCampaign;

