/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ItemIdStoreCodePair model module.
 * @module model/ItemIdStoreCodePair
 * @version 2.0.1-pre.0
 */
class ItemIdStoreCodePair {
    /**
     * Constructs a new <code>ItemIdStoreCodePair</code>.
     * A pair of item_id and store_code that uniquely identifies a local inventory item
     * @alias module:model/ItemIdStoreCodePair
     * @param itemId {String} Catalog item id in the merchant namespace
     * @param storeCode {String} Store code for the local inventory item
     */
    constructor(itemId, storeCode) { 
        
        ItemIdStoreCodePair.initialize(this, itemId, storeCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, itemId, storeCode) { 
        obj['item_id'] = itemId;
        obj['store_code'] = storeCode;
    }

    /**
     * Constructs a <code>ItemIdStoreCodePair</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ItemIdStoreCodePair} obj Optional instance to populate.
     * @return {module:model/ItemIdStoreCodePair} The populated <code>ItemIdStoreCodePair</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ItemIdStoreCodePair();

            if (data.hasOwnProperty('item_id')) {
                obj['item_id'] = ApiClient.convertToType(data['item_id'], 'String');
            }
            if (data.hasOwnProperty('store_code')) {
                obj['store_code'] = ApiClient.convertToType(data['store_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ItemIdStoreCodePair</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ItemIdStoreCodePair</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ItemIdStoreCodePair.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['item_id'] && !(typeof data['item_id'] === 'string' || data['item_id'] instanceof String)) {
            throw new Error("Expected the field `item_id` to be a primitive type in the JSON string but got " + data['item_id']);
        }
        // ensure the json data is a string
        if (data['store_code'] && !(typeof data['store_code'] === 'string' || data['store_code'] instanceof String)) {
            throw new Error("Expected the field `store_code` to be a primitive type in the JSON string but got " + data['store_code']);
        }

        return true;
    }


}

ItemIdStoreCodePair.RequiredProperties = ["item_id", "store_code"];

/**
 * Catalog item id in the merchant namespace
 * @member {String} item_id
 */
ItemIdStoreCodePair.prototype['item_id'] = undefined;

/**
 * Store code for the local inventory item
 * @member {String} store_code
 */
ItemIdStoreCodePair.prototype['store_code'] = undefined;






export default ItemIdStoreCodePair;

