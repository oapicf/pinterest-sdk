/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomerListRecordRow from './CustomerListRecordRow';
import UserListType from './UserListType';

/**
 * The CustomerListCreate model module.
 * @module model/CustomerListCreate
 * @version 2.0.1-pre.0
 */
class CustomerListCreate {
    /**
     * Constructs a new <code>CustomerListCreate</code>.
     * Resource create operation model.
     * @alias module:model/CustomerListCreate
     * @param name {String} Customer list name.
     */
    constructor(name) { 
        
        CustomerListCreate.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>CustomerListCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomerListCreate} obj Optional instance to populate.
     * @return {module:model/CustomerListCreate} The populated <code>CustomerListCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomerListCreate();

            if (data.hasOwnProperty('is_nca')) {
                obj['is_nca'] = ApiClient.convertToType(data['is_nca'], 'Boolean');
            }
            if (data.hasOwnProperty('list_type')) {
                obj['list_type'] = ApiClient.convertToType(data['list_type'], UserListType);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('records')) {
                obj['records'] = ApiClient.convertToType(data['records'], 'String');
            }
            if (data.hasOwnProperty('records_v2')) {
                obj['records_v2'] = ApiClient.convertToType(data['records_v2'], [CustomerListRecordRow]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomerListCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomerListCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CustomerListCreate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['records'] && !(typeof data['records'] === 'string' || data['records'] instanceof String)) {
            throw new Error("Expected the field `records` to be a primitive type in the JSON string but got " + data['records']);
        }
        if (data['records_v2']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['records_v2'])) {
                throw new Error("Expected the field `records_v2` to be an array in the JSON data but got " + data['records_v2']);
            }
            // validate the optional field `records_v2` (array)
            for (const item of data['records_v2']) {
                CustomerListRecordRow.validateJSON(item);
            };
        }

        return true;
    }


}

CustomerListCreate.RequiredProperties = ["name"];

/**
 * Whether the list was uploaded for new customer acquisition (expanded matching). Immutable after creation.
 * @member {Boolean} is_nca
 */
CustomerListCreate.prototype['is_nca'] = undefined;

/**
 * Type of customer list (e.g., EMAIL, IDFA, MAID).
 * @member {module:model/UserListType} list_type
 */
CustomerListCreate.prototype['list_type'] = undefined;

/**
 * Customer list name.
 * @member {String} name
 */
CustomerListCreate.prototype['name'] = undefined;

/**
 * Records list. Can be any combination of emails, MAIDs, or IDFAs. Emails must be lowercase and can be plain text or hashed using SHA1, SHA256, or MD5. MAIDs and IDFAs must be hashed with SHA1, SHA256, or MD5.
 * @member {String} records
 */
CustomerListCreate.prototype['records'] = undefined;

/**
 * Multi-field record format. Array of objects with optional email, maid, ip_address, user_agent, external_id, hashed_pinner_id, hashed_phone_number, and liveramp_envelope per row. Provide exactly one of records or records_v2.
 * @member {Array.<module:model/CustomerListRecordRow>} records_v2
 */
CustomerListCreate.prototype['records_v2'] = undefined;






export default CustomerListCreate;

