/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AdPreviewSourceImage model module.
 * @module model/AdPreviewSourceImage
 * @version 2.0.1-pre.0
 */
class AdPreviewSourceImage {
    /**
     * Constructs a new <code>AdPreviewSourceImage</code>.
     * Ad preview source from an image URL.
     * @alias module:model/AdPreviewSourceImage
     * @param imageUrl {String} Image URL.
     * @param title {String} Title displayed below ad.
     */
    constructor(imageUrl, title) { 
        
        AdPreviewSourceImage.initialize(this, imageUrl, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, imageUrl, title) { 
        obj['image_url'] = imageUrl;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>AdPreviewSourceImage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AdPreviewSourceImage} obj Optional instance to populate.
     * @return {module:model/AdPreviewSourceImage} The populated <code>AdPreviewSourceImage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AdPreviewSourceImage();

            if (data.hasOwnProperty('image_url')) {
                obj['image_url'] = ApiClient.convertToType(data['image_url'], 'String');
            }
            if (data.hasOwnProperty('promotion_id')) {
                obj['promotion_id'] = ApiClient.convertToType(data['promotion_id'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AdPreviewSourceImage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AdPreviewSourceImage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AdPreviewSourceImage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['image_url'] && !(typeof data['image_url'] === 'string' || data['image_url'] instanceof String)) {
            throw new Error("Expected the field `image_url` to be a primitive type in the JSON string but got " + data['image_url']);
        }
        // ensure the json data is a string
        if (data['promotion_id'] && !(typeof data['promotion_id'] === 'string' || data['promotion_id'] instanceof String)) {
            throw new Error("Expected the field `promotion_id` to be a primitive type in the JSON string but got " + data['promotion_id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

AdPreviewSourceImage.RequiredProperties = ["image_url", "title"];

/**
 * Image URL.
 * @member {String} image_url
 */
AdPreviewSourceImage.prototype['image_url'] = undefined;

/**
 * Promotion id for the ad to preview, optional and only applicable when creating ad preview for an existing promotion.
 * @member {String} promotion_id
 */
AdPreviewSourceImage.prototype['promotion_id'] = undefined;

/**
 * Title displayed below ad.
 * @member {String} title
 */
AdPreviewSourceImage.prototype['title'] = undefined;






export default AdPreviewSourceImage;

