/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Keyword from './Keyword';
import KeywordError from './KeywordError';

/**
 * The Keywords model module.
 * @module model/Keywords
 * @version 2.0.1-pre.0
 */
class Keywords {
    /**
     * Constructs a new <code>Keywords</code>.
     * @alias module:model/Keywords
     * @param errors {Array.<module:model/KeywordError>} Keyword error
     * @param keywords {Array.<module:model/Keyword>} Keywords
     */
    constructor(errors, keywords) { 
        
        Keywords.initialize(this, errors, keywords);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, errors, keywords) { 
        obj['errors'] = errors;
        obj['keywords'] = keywords;
    }

    /**
     * Constructs a <code>Keywords</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Keywords} obj Optional instance to populate.
     * @return {module:model/Keywords} The populated <code>Keywords</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Keywords();

            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [KeywordError]);
            }
            if (data.hasOwnProperty('keywords')) {
                obj['keywords'] = ApiClient.convertToType(data['keywords'], [Keyword]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Keywords</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Keywords</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Keywords.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                KeywordError.validateJSON(item);
            };
        }
        if (data['keywords']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['keywords'])) {
                throw new Error("Expected the field `keywords` to be an array in the JSON data but got " + data['keywords']);
            }
            // validate the optional field `keywords` (array)
            for (const item of data['keywords']) {
                Keyword.validateJSON(item);
            };
        }

        return true;
    }


}

Keywords.RequiredProperties = ["errors", "keywords"];

/**
 * Keyword error
 * @member {Array.<module:model/KeywordError>} errors
 */
Keywords.prototype['errors'] = undefined;

/**
 * Keywords
 * @member {Array.<module:model/Keyword>} keywords
 */
Keywords.prototype['keywords'] = undefined;






export default Keywords;

