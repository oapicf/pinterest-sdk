/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CampaignUpdateRequestAllOf2.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CampaignUpdateRequestAllOf2::CampaignUpdateRequestAllOf2(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CampaignUpdateRequestAllOf2::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CampaignUpdateRequestAllOf2::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CampaignUpdateRequestAllOf2::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CampaignUpdateRequestAllOf2::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CampaignUpdateRequestAllOf2");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CampaignUpdateRequestAllOf2::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Bid_optionsIsSet) {
            object["bid_options"] = JsonValueConverter<std::shared_ptr<CampaignBidOptionsUpdate>>::toJsonValue(getBidOptions());
        }
        if (m_Intended_promotion_typeIsSet) {
            object["intended_promotion_type"] = JsonValueConverter<std::shared_ptr<IntendedPromotionType>>::toJsonValue(getIntendedPromotionType());
        }
        if (m_Is_ltv_optimizedIsSet) {
            object["is_ltv_optimized"] = JsonValueConverter<bool>::toJsonValue(isIsLtvOptimized());
        }
        if (m_Is_performance_plusIsSet) {
            object["is_performance_plus"] = JsonValueConverter<bool>::toJsonValue(isIsPerformancePlus());
        }
        if (m_Is_top_of_searchIsSet) {
            object["is_top_of_search"] = JsonValueConverter<bool>::toJsonValue(isIsTopOfSearch());
        }
        if (m_Objective_typeIsSet) {
            object["objective_type"] = JsonValueConverter<std::shared_ptr<ObjectiveType>>::toJsonValue(getObjectiveType());
        }
    return object;
}

void CampaignUpdateRequestAllOf2::fromJsonObject_internal(boost::json::object const& object)
{
    m_Bid_optionsIsSet = false;
    m_Intended_promotion_typeIsSet = false;
    m_Is_ltv_optimizedIsSet = false;
    m_Is_performance_plusIsSet = false;
    m_Is_top_of_searchIsSet = false;
    m_Objective_typeIsSet = false;
    {
        const auto Bid_optionsIt = object.find("bid_options");
        if (Bid_optionsIt != object.end()) {
            setBidOptions(JsonValueConverter<std::shared_ptr<CampaignBidOptionsUpdate>>::fromJsonValue(Bid_optionsIt->value()));
        }
    }
    {
        const auto Intended_promotion_typeIt = object.find("intended_promotion_type");
        if (Intended_promotion_typeIt != object.end()) {
            setIntendedPromotionType(JsonValueConverter<std::shared_ptr<IntendedPromotionType>>::fromJsonValue(Intended_promotion_typeIt->value()));
        }
    }
    {
        const auto Is_ltv_optimizedIt = object.find("is_ltv_optimized");
        if (Is_ltv_optimizedIt != object.end()) {
            setIsLtvOptimized(JsonValueConverter<bool>::fromJsonValue(Is_ltv_optimizedIt->value()));
        }
    }
    {
        const auto Is_performance_plusIt = object.find("is_performance_plus");
        if (Is_performance_plusIt != object.end()) {
            setIsPerformancePlus(JsonValueConverter<bool>::fromJsonValue(Is_performance_plusIt->value()));
        }
    }
    {
        const auto Is_top_of_searchIt = object.find("is_top_of_search");
        if (Is_top_of_searchIt != object.end()) {
            setIsTopOfSearch(JsonValueConverter<bool>::fromJsonValue(Is_top_of_searchIt->value()));
        }
    }
    {
        const auto Objective_typeIt = object.find("objective_type");
        if (Objective_typeIt != object.end()) {
            setObjectiveType(JsonValueConverter<std::shared_ptr<ObjectiveType>>::fromJsonValue(Objective_typeIt->value()));
        }
    }
}

std::shared_ptr<CampaignBidOptionsUpdate> CampaignUpdateRequestAllOf2::getBidOptions() const
{
    return m_Bid_options;
}

void CampaignUpdateRequestAllOf2::setBidOptions(std::shared_ptr<CampaignBidOptionsUpdate> value)
{
    
    m_Bid_options = std::move(value);
    m_Bid_optionsIsSet = true;
}
std::shared_ptr<IntendedPromotionType> CampaignUpdateRequestAllOf2::getIntendedPromotionType() const
{
    return m_Intended_promotion_type;
}

void CampaignUpdateRequestAllOf2::setIntendedPromotionType(std::shared_ptr<IntendedPromotionType> value)
{
    
    m_Intended_promotion_type = std::move(value);
    m_Intended_promotion_typeIsSet = true;
}
bool CampaignUpdateRequestAllOf2::isIsLtvOptimized() const
{
    return m_Is_ltv_optimized;
}

void CampaignUpdateRequestAllOf2::setIsLtvOptimized(bool value)
{
    
    m_Is_ltv_optimized = std::move(value);
    m_Is_ltv_optimizedIsSet = true;
}
bool CampaignUpdateRequestAllOf2::isIsPerformancePlus() const
{
    return m_Is_performance_plus;
}

void CampaignUpdateRequestAllOf2::setIsPerformancePlus(bool value)
{
    
    m_Is_performance_plus = std::move(value);
    m_Is_performance_plusIsSet = true;
}
bool CampaignUpdateRequestAllOf2::isIsTopOfSearch() const
{
    return m_Is_top_of_search;
}

void CampaignUpdateRequestAllOf2::setIsTopOfSearch(bool value)
{
    
    m_Is_top_of_search = std::move(value);
    m_Is_top_of_searchIsSet = true;
}
std::shared_ptr<ObjectiveType> CampaignUpdateRequestAllOf2::getObjectiveType() const
{
    return m_Objective_type;
}

void CampaignUpdateRequestAllOf2::setObjectiveType(std::shared_ptr<ObjectiveType> value)
{
    
    m_Objective_type = std::move(value);
    m_Objective_typeIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CampaignUpdateRequestAllOf2>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CampaignUpdateRequestAllOf2>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CampaignUpdateRequestAllOf2>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CampaignUpdateRequestAllOf2>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

