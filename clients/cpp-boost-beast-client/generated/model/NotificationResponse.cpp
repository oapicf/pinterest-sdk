/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "NotificationResponse.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

NotificationResponse::NotificationResponse(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string NotificationResponse::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void NotificationResponse::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value NotificationResponse::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void NotificationResponse::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for NotificationResponse");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object NotificationResponse::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Error_msgIsSet) {
            object["error_msg"] = JsonValueConverter<std::string>::toJsonValue(getErrorMsg());
        }
        if (m_Received_atIsSet) {
            object["received_at"] = JsonValueConverter<int32_t>::toJsonValue(getReceivedAt());
        }
        if (m_SuccessIsSet) {
            object["success"] = JsonValueConverter<bool>::toJsonValue(isSuccess());
        }
    return object;
}

void NotificationResponse::fromJsonObject_internal(boost::json::object const& object)
{
    m_Error_msgIsSet = false;
    m_Received_atIsSet = false;
    m_SuccessIsSet = false;
    {
        const auto Error_msgIt = object.find("error_msg");
        if (Error_msgIt != object.end()) {
            setErrorMsg(JsonValueConverter<std::string>::fromJsonValue(Error_msgIt->value()));
        }
    }
    {
        const auto Received_atIt = object.find("received_at");
        if (Received_atIt != object.end()) {
            setReceivedAt(JsonValueConverter<int32_t>::fromJsonValue(Received_atIt->value()));
        }
    }
    {
        const auto SuccessIt = object.find("success");
        if (SuccessIt != object.end()) {
            setSuccess(JsonValueConverter<bool>::fromJsonValue(SuccessIt->value()));
        }
    }
}

std::string NotificationResponse::getErrorMsg() const
{
    return m_Error_msg;
}

void NotificationResponse::setErrorMsg(std::string value)
{
    
    m_Error_msg = std::move(value);
    m_Error_msgIsSet = true;
}
int32_t NotificationResponse::getReceivedAt() const
{
    return m_Received_at;
}

void NotificationResponse::setReceivedAt(int32_t value)
{
    
    m_Received_at = std::move(value);
    m_Received_atIsSet = true;
}
bool NotificationResponse::isSuccess() const
{
    return m_Success;
}

void NotificationResponse::setSuccess(bool value)
{
    
    m_Success = std::move(value);
    m_SuccessIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<NotificationResponse>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<NotificationResponse>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<NotificationResponse>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<NotificationResponse>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

