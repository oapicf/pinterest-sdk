/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "TrendingProductCategory.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

TrendingProductCategory::TrendingProductCategory(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string TrendingProductCategory::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void TrendingProductCategory::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value TrendingProductCategory::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void TrendingProductCategory::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for TrendingProductCategory");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object TrendingProductCategory::toJsonObject_internal() const
{
    boost::json::object object;
        object["engagement_type"] = JsonValueConverter<std::shared_ptr<ProductCategoriesEngagementType>>::toJsonValue(getEngagementType());
        object["pct_change_mom"] = JsonValueConverter<int32_t>::toJsonValue(getPctChangeMom());
        object["percent_relative_volume"] = JsonValueConverter<int32_t>::toJsonValue(getPercentRelativeVolume());
        object["pinterest_product_category_id"] = JsonValueConverter<int32_t>::toJsonValue(getPinterestProductCategoryId());
        object["product_category"] = JsonValueConverter<std::string>::toJsonValue(getProductCategory());
        if (m_VerticalsIsSet) {
            object["verticals"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getVerticals());
        }
    return object;
}

void TrendingProductCategory::fromJsonObject_internal(boost::json::object const& object)
{
    m_VerticalsIsSet = false;
    {
        const auto Engagement_typeIt = object.find("engagement_type");
        if (Engagement_typeIt != object.end()) {
            setEngagementType(JsonValueConverter<std::shared_ptr<ProductCategoriesEngagementType>>::fromJsonValue(Engagement_typeIt->value()));
        }
    }
    {
        const auto Pct_change_momIt = object.find("pct_change_mom");
        if (Pct_change_momIt != object.end()) {
            setPctChangeMom(JsonValueConverter<int32_t>::fromJsonValue(Pct_change_momIt->value()));
        }
    }
    {
        const auto Percent_relative_volumeIt = object.find("percent_relative_volume");
        if (Percent_relative_volumeIt != object.end()) {
            setPercentRelativeVolume(JsonValueConverter<int32_t>::fromJsonValue(Percent_relative_volumeIt->value()));
        }
    }
    {
        const auto Pinterest_product_category_idIt = object.find("pinterest_product_category_id");
        if (Pinterest_product_category_idIt != object.end()) {
            setPinterestProductCategoryId(JsonValueConverter<int32_t>::fromJsonValue(Pinterest_product_category_idIt->value()));
        }
    }
    {
        const auto Product_categoryIt = object.find("product_category");
        if (Product_categoryIt != object.end()) {
            setProductCategory(JsonValueConverter<std::string>::fromJsonValue(Product_categoryIt->value()));
        }
    }
    {
        const auto VerticalsIt = object.find("verticals");
        if (VerticalsIt != object.end()) {
            setVerticals(JsonValueConverter<std::vector<std::string>>::fromJsonValue(VerticalsIt->value()));
        }
    }
}

std::shared_ptr<ProductCategoriesEngagementType> TrendingProductCategory::getEngagementType() const
{
    return m_Engagement_type;
}

void TrendingProductCategory::setEngagementType(std::shared_ptr<ProductCategoriesEngagementType> value)
{
    
    m_Engagement_type = std::move(value);
}
int32_t TrendingProductCategory::getPctChangeMom() const
{
    return m_Pct_change_mom;
}

void TrendingProductCategory::setPctChangeMom(int32_t value)
{
    
    m_Pct_change_mom = std::move(value);
}
int32_t TrendingProductCategory::getPercentRelativeVolume() const
{
    return m_Percent_relative_volume;
}

void TrendingProductCategory::setPercentRelativeVolume(int32_t value)
{
    
    m_Percent_relative_volume = std::move(value);
}
int32_t TrendingProductCategory::getPinterestProductCategoryId() const
{
    return m_Pinterest_product_category_id;
}

void TrendingProductCategory::setPinterestProductCategoryId(int32_t value)
{
    
    m_Pinterest_product_category_id = std::move(value);
}
std::string TrendingProductCategory::getProductCategory() const
{
    return m_Product_category;
}

void TrendingProductCategory::setProductCategory(std::string value)
{
    
    m_Product_category = std::move(value);
}
std::vector<std::string> TrendingProductCategory::getVerticals() const
{
    return m_Verticals;
}

void TrendingProductCategory::setVerticals(std::vector<std::string> value)
{
    
    m_Verticals = std::move(value);
    m_VerticalsIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<TrendingProductCategory>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<TrendingProductCategory>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<TrendingProductCategory>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<TrendingProductCategory>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

