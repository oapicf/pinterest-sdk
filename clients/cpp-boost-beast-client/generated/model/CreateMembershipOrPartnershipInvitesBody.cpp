/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CreateMembershipOrPartnershipInvitesBody.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CreateMembershipOrPartnershipInvitesBody::CreateMembershipOrPartnershipInvitesBody(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CreateMembershipOrPartnershipInvitesBody::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CreateMembershipOrPartnershipInvitesBody::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CreateMembershipOrPartnershipInvitesBody::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CreateMembershipOrPartnershipInvitesBody::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CreateMembershipOrPartnershipInvitesBody");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CreateMembershipOrPartnershipInvitesBody::toJsonObject_internal() const
{
    boost::json::object object;
        object["business_role"] = JsonValueConverter<std::shared_ptr<BusinessRoleForInvite>>::toJsonValue(getBusinessRole());
        object["invite_type"] = JsonValueConverter<std::shared_ptr<InviteType>>::toJsonValue(getInviteType());
        if (m_MembersIsSet) {
            object["members"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getMembers());
        }
        if (m_PartnersIsSet) {
            object["partners"] = JsonValueConverter<std::vector<std::shared_ptr<std::string>>>::toJsonValue(getPartners());
        }
    return object;
}

void CreateMembershipOrPartnershipInvitesBody::fromJsonObject_internal(boost::json::object const& object)
{
    m_MembersIsSet = false;
    m_PartnersIsSet = false;
    {
        const auto Business_roleIt = object.find("business_role");
        if (Business_roleIt != object.end()) {
            setBusinessRole(JsonValueConverter<std::shared_ptr<BusinessRoleForInvite>>::fromJsonValue(Business_roleIt->value()));
        }
    }
    {
        const auto Invite_typeIt = object.find("invite_type");
        if (Invite_typeIt != object.end()) {
            setInviteType(JsonValueConverter<std::shared_ptr<InviteType>>::fromJsonValue(Invite_typeIt->value()));
        }
    }
    {
        const auto MembersIt = object.find("members");
        if (MembersIt != object.end()) {
            setMembers(JsonValueConverter<std::vector<std::string>>::fromJsonValue(MembersIt->value()));
        }
    }
    {
        const auto PartnersIt = object.find("partners");
        if (PartnersIt != object.end()) {
            setPartners(JsonValueConverter<std::vector<std::shared_ptr<std::string>>>::fromJsonValue(PartnersIt->value()));
        }
    }
}

std::shared_ptr<BusinessRoleForInvite> CreateMembershipOrPartnershipInvitesBody::getBusinessRole() const
{
    return m_Business_role;
}

void CreateMembershipOrPartnershipInvitesBody::setBusinessRole(std::shared_ptr<BusinessRoleForInvite> value)
{
    
    m_Business_role = std::move(value);
}
std::shared_ptr<InviteType> CreateMembershipOrPartnershipInvitesBody::getInviteType() const
{
    return m_Invite_type;
}

void CreateMembershipOrPartnershipInvitesBody::setInviteType(std::shared_ptr<InviteType> value)
{
    
    m_Invite_type = std::move(value);
}
std::vector<std::string> CreateMembershipOrPartnershipInvitesBody::getMembers() const
{
    return m_Members;
}

void CreateMembershipOrPartnershipInvitesBody::setMembers(std::vector<std::string> value)
{
    
    m_Members = std::move(value);
    m_MembersIsSet = true;
}
std::vector<std::shared_ptr<std::string>> CreateMembershipOrPartnershipInvitesBody::getPartners() const
{
    return m_Partners;
}

void CreateMembershipOrPartnershipInvitesBody::setPartners(std::vector<std::shared_ptr<std::string>> value)
{
    
    m_Partners = std::move(value);
    m_PartnersIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CreateMembershipOrPartnershipInvitesBody>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CreateMembershipOrPartnershipInvitesBody>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CreateMembershipOrPartnershipInvitesBody>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CreateMembershipOrPartnershipInvitesBody>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

