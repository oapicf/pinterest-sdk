/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "LinkedBusiness.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

LinkedBusiness::LinkedBusiness(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string LinkedBusiness::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void LinkedBusiness::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value LinkedBusiness::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void LinkedBusiness::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for LinkedBusiness");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object LinkedBusiness::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Image_large_urlIsSet) {
            object["image_large_url"] = JsonValueConverter<std::string>::toJsonValue(getImageLargeUrl());
        }
        if (m_Image_medium_urlIsSet) {
            object["image_medium_url"] = JsonValueConverter<std::string>::toJsonValue(getImageMediumUrl());
        }
        if (m_Image_small_urlIsSet) {
            object["image_small_url"] = JsonValueConverter<std::string>::toJsonValue(getImageSmallUrl());
        }
        if (m_Image_xlarge_urlIsSet) {
            object["image_xlarge_url"] = JsonValueConverter<std::string>::toJsonValue(getImageXlargeUrl());
        }
        if (m_UsernameIsSet) {
            object["username"] = JsonValueConverter<std::string>::toJsonValue(getUsername());
        }
    return object;
}

void LinkedBusiness::fromJsonObject_internal(boost::json::object const& object)
{
    m_Image_large_urlIsSet = false;
    m_Image_medium_urlIsSet = false;
    m_Image_small_urlIsSet = false;
    m_Image_xlarge_urlIsSet = false;
    m_UsernameIsSet = false;
    {
        const auto Image_large_urlIt = object.find("image_large_url");
        if (Image_large_urlIt != object.end()) {
            setImageLargeUrl(JsonValueConverter<std::string>::fromJsonValue(Image_large_urlIt->value()));
        }
    }
    {
        const auto Image_medium_urlIt = object.find("image_medium_url");
        if (Image_medium_urlIt != object.end()) {
            setImageMediumUrl(JsonValueConverter<std::string>::fromJsonValue(Image_medium_urlIt->value()));
        }
    }
    {
        const auto Image_small_urlIt = object.find("image_small_url");
        if (Image_small_urlIt != object.end()) {
            setImageSmallUrl(JsonValueConverter<std::string>::fromJsonValue(Image_small_urlIt->value()));
        }
    }
    {
        const auto Image_xlarge_urlIt = object.find("image_xlarge_url");
        if (Image_xlarge_urlIt != object.end()) {
            setImageXlargeUrl(JsonValueConverter<std::string>::fromJsonValue(Image_xlarge_urlIt->value()));
        }
    }
    {
        const auto UsernameIt = object.find("username");
        if (UsernameIt != object.end()) {
            setUsername(JsonValueConverter<std::string>::fromJsonValue(UsernameIt->value()));
        }
    }
}

std::string LinkedBusiness::getImageLargeUrl() const
{
    return m_Image_large_url;
}

void LinkedBusiness::setImageLargeUrl(std::string value)
{
    
    m_Image_large_url = std::move(value);
    m_Image_large_urlIsSet = true;
}
std::string LinkedBusiness::getImageMediumUrl() const
{
    return m_Image_medium_url;
}

void LinkedBusiness::setImageMediumUrl(std::string value)
{
    
    m_Image_medium_url = std::move(value);
    m_Image_medium_urlIsSet = true;
}
std::string LinkedBusiness::getImageSmallUrl() const
{
    return m_Image_small_url;
}

void LinkedBusiness::setImageSmallUrl(std::string value)
{
    
    m_Image_small_url = std::move(value);
    m_Image_small_urlIsSet = true;
}
std::string LinkedBusiness::getImageXlargeUrl() const
{
    return m_Image_xlarge_url;
}

void LinkedBusiness::setImageXlargeUrl(std::string value)
{
    
    m_Image_xlarge_url = std::move(value);
    m_Image_xlarge_urlIsSet = true;
}
std::string LinkedBusiness::getUsername() const
{
    return m_Username;
}

void LinkedBusiness::setUsername(std::string value)
{
    
    m_Username = std::move(value);
    m_UsernameIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<LinkedBusiness>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<LinkedBusiness>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<LinkedBusiness>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<LinkedBusiness>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

