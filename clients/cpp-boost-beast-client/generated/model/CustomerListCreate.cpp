/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CustomerListCreate.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CustomerListCreate::CustomerListCreate(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CustomerListCreate::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CustomerListCreate::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CustomerListCreate::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CustomerListCreate::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CustomerListCreate");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CustomerListCreate::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Is_ncaIsSet) {
            object["is_nca"] = JsonValueConverter<bool>::toJsonValue(isIsNca());
        }
        if (m_List_typeIsSet) {
            object["list_type"] = JsonValueConverter<std::shared_ptr<UserListType>>::toJsonValue(getListType());
        }
        object["name"] = JsonValueConverter<std::string>::toJsonValue(getName());
        if (m_RecordsIsSet) {
            object["records"] = JsonValueConverter<std::string>::toJsonValue(getRecords());
        }
        if (m_Records_v2IsSet) {
            object["records_v2"] = JsonValueConverter<std::vector<std::shared_ptr<CustomerListRecordRow>>>::toJsonValue(getRecordsV2());
        }
    return object;
}

void CustomerListCreate::fromJsonObject_internal(boost::json::object const& object)
{
    m_Is_ncaIsSet = false;
    m_List_typeIsSet = false;
    m_RecordsIsSet = false;
    m_Records_v2IsSet = false;
    {
        const auto Is_ncaIt = object.find("is_nca");
        if (Is_ncaIt != object.end()) {
            setIsNca(JsonValueConverter<bool>::fromJsonValue(Is_ncaIt->value()));
        }
    }
    {
        const auto List_typeIt = object.find("list_type");
        if (List_typeIt != object.end()) {
            setListType(JsonValueConverter<std::shared_ptr<UserListType>>::fromJsonValue(List_typeIt->value()));
        }
    }
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            setName(JsonValueConverter<std::string>::fromJsonValue(NameIt->value()));
        }
    }
    {
        const auto RecordsIt = object.find("records");
        if (RecordsIt != object.end()) {
            setRecords(JsonValueConverter<std::string>::fromJsonValue(RecordsIt->value()));
        }
    }
    {
        const auto Records_v2It = object.find("records_v2");
        if (Records_v2It != object.end()) {
            setRecordsV2(JsonValueConverter<std::vector<std::shared_ptr<CustomerListRecordRow>>>::fromJsonValue(Records_v2It->value()));
        }
    }
}

bool CustomerListCreate::isIsNca() const
{
    return m_Is_nca;
}

void CustomerListCreate::setIsNca(bool value)
{
    
    m_Is_nca = std::move(value);
    m_Is_ncaIsSet = true;
}
std::shared_ptr<UserListType> CustomerListCreate::getListType() const
{
    return m_List_type;
}

void CustomerListCreate::setListType(std::shared_ptr<UserListType> value)
{
    
    m_List_type = std::move(value);
    m_List_typeIsSet = true;
}
std::string CustomerListCreate::getName() const
{
    return m_Name;
}

void CustomerListCreate::setName(std::string value)
{
    
    m_Name = std::move(value);
}
std::string CustomerListCreate::getRecords() const
{
    return m_Records;
}

void CustomerListCreate::setRecords(std::string value)
{
    
    m_Records = std::move(value);
    m_RecordsIsSet = true;
}
std::vector<std::shared_ptr<CustomerListRecordRow>> CustomerListCreate::getRecordsV2() const
{
    return m_Records_v2;
}

void CustomerListCreate::setRecordsV2(std::vector<std::shared_ptr<CustomerListRecordRow>> value)
{
    
    m_Records_v2 = std::move(value);
    m_Records_v2IsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CustomerListCreate>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CustomerListCreate>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CustomerListCreate>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CustomerListCreate>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

