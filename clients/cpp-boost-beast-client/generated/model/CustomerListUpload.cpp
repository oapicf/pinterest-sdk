/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CustomerListUpload.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CustomerListUpload::CustomerListUpload(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CustomerListUpload::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CustomerListUpload::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CustomerListUpload::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CustomerListUpload::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CustomerListUpload");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CustomerListUpload::toJsonObject_internal() const
{
    boost::json::object object;
        object["ad_account_id"] = JsonValueConverter<std::string>::toJsonValue(getAdAccountId());
        object["creation_time"] = JsonValueConverter<int32_t>::toJsonValue(getCreationTime());
        object["customer_list_id"] = JsonValueConverter<std::string>::toJsonValue(getCustomerListId());
        if (m_Error_countsIsSet) {
            object["error_counts"] = JsonValueConverter<std::vector<std::shared_ptr<ErrorDetail>>>::toJsonValue(getErrorCounts());
        }
        object["id"] = JsonValueConverter<std::string>::toJsonValue(getId());
        object["operation"] = JsonValueConverter<std::shared_ptr<UserListOperationType>>::toJsonValue(getOperation());
        if (m_Record_countsIsSet) {
            object["record_counts"] = JsonValueConverter<std::shared_ptr<RecordCounts>>::toJsonValue(getRecordCounts());
        }
        object["state"] = JsonValueConverter<std::shared_ptr<WorkloadState>>::toJsonValue(getState());
        object["updated_time"] = JsonValueConverter<int32_t>::toJsonValue(getUpdatedTime());
    return object;
}

void CustomerListUpload::fromJsonObject_internal(boost::json::object const& object)
{
    m_Error_countsIsSet = false;
    m_Record_countsIsSet = false;
    {
        const auto Ad_account_idIt = object.find("ad_account_id");
        if (Ad_account_idIt != object.end()) {
            setAdAccountId(JsonValueConverter<std::string>::fromJsonValue(Ad_account_idIt->value()));
        }
    }
    {
        const auto Creation_timeIt = object.find("creation_time");
        if (Creation_timeIt != object.end()) {
            setCreationTime(JsonValueConverter<int32_t>::fromJsonValue(Creation_timeIt->value()));
        }
    }
    {
        const auto Customer_list_idIt = object.find("customer_list_id");
        if (Customer_list_idIt != object.end()) {
            setCustomerListId(JsonValueConverter<std::string>::fromJsonValue(Customer_list_idIt->value()));
        }
    }
    {
        const auto Error_countsIt = object.find("error_counts");
        if (Error_countsIt != object.end()) {
            setErrorCounts(JsonValueConverter<std::vector<std::shared_ptr<ErrorDetail>>>::fromJsonValue(Error_countsIt->value()));
        }
    }
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            setId(JsonValueConverter<std::string>::fromJsonValue(IdIt->value()));
        }
    }
    {
        const auto OperationIt = object.find("operation");
        if (OperationIt != object.end()) {
            setOperation(JsonValueConverter<std::shared_ptr<UserListOperationType>>::fromJsonValue(OperationIt->value()));
        }
    }
    {
        const auto Record_countsIt = object.find("record_counts");
        if (Record_countsIt != object.end()) {
            setRecordCounts(JsonValueConverter<std::shared_ptr<RecordCounts>>::fromJsonValue(Record_countsIt->value()));
        }
    }
    {
        const auto StateIt = object.find("state");
        if (StateIt != object.end()) {
            setState(JsonValueConverter<std::shared_ptr<WorkloadState>>::fromJsonValue(StateIt->value()));
        }
    }
    {
        const auto Updated_timeIt = object.find("updated_time");
        if (Updated_timeIt != object.end()) {
            setUpdatedTime(JsonValueConverter<int32_t>::fromJsonValue(Updated_timeIt->value()));
        }
    }
}

std::string CustomerListUpload::getAdAccountId() const
{
    return m_Ad_account_id;
}

void CustomerListUpload::setAdAccountId(std::string value)
{
    
    m_Ad_account_id = std::move(value);
}
int32_t CustomerListUpload::getCreationTime() const
{
    return m_Creation_time;
}

void CustomerListUpload::setCreationTime(int32_t value)
{
    
    m_Creation_time = std::move(value);
}
std::string CustomerListUpload::getCustomerListId() const
{
    return m_Customer_list_id;
}

void CustomerListUpload::setCustomerListId(std::string value)
{
    
    m_Customer_list_id = std::move(value);
}
std::vector<std::shared_ptr<ErrorDetail>> CustomerListUpload::getErrorCounts() const
{
    return m_Error_counts;
}

void CustomerListUpload::setErrorCounts(std::vector<std::shared_ptr<ErrorDetail>> value)
{
    
    m_Error_counts = std::move(value);
    m_Error_countsIsSet = true;
}
std::string CustomerListUpload::getId() const
{
    return m_Id;
}

void CustomerListUpload::setId(std::string value)
{
    
    m_Id = std::move(value);
}
std::shared_ptr<UserListOperationType> CustomerListUpload::getOperation() const
{
    return m_Operation;
}

void CustomerListUpload::setOperation(std::shared_ptr<UserListOperationType> value)
{
    
    m_Operation = std::move(value);
}
std::shared_ptr<RecordCounts> CustomerListUpload::getRecordCounts() const
{
    return m_Record_counts;
}

void CustomerListUpload::setRecordCounts(std::shared_ptr<RecordCounts> value)
{
    
    m_Record_counts = std::move(value);
    m_Record_countsIsSet = true;
}
std::shared_ptr<WorkloadState> CustomerListUpload::getState() const
{
    return m_State;
}

void CustomerListUpload::setState(std::shared_ptr<WorkloadState> value)
{
    
    m_State = std::move(value);
}
int32_t CustomerListUpload::getUpdatedTime() const
{
    return m_Updated_time;
}

void CustomerListUpload::setUpdatedTime(int32_t value)
{
    
    m_Updated_time = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CustomerListUpload>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CustomerListUpload>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CustomerListUpload>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CustomerListUpload>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

