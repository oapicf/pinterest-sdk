/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CatalogsRetailItemsBatch.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CatalogsRetailItemsBatch::CatalogsRetailItemsBatch(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CatalogsRetailItemsBatch::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CatalogsRetailItemsBatch::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CatalogsRetailItemsBatch::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CatalogsRetailItemsBatch::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CatalogsRetailItemsBatch");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CatalogsRetailItemsBatch::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Batch_idIsSet) {
            object["batch_id"] = JsonValueConverter<std::string>::toJsonValue(getBatchId());
        }
        object["catalog_type"] = JsonValueConverter<std::string>::toJsonValue(getCatalogType());
        if (m_Completed_timeIsSet) {
            object["completed_time"] = JsonValueConverter<std::string>::toJsonValue(getCompletedTime());
        }
        object["created_time"] = JsonValueConverter<std::string>::toJsonValue(getCreatedTime());
        if (m_ItemsIsSet) {
            object["items"] = JsonValueConverter<std::vector<std::shared_ptr<ItemProcessingRecord>>>::toJsonValue(getItems());
        }
        if (m_StatusIsSet) {
            object["status"] = JsonValueConverter<std::shared_ptr<BatchOperationStatus>>::toJsonValue(getStatus());
        }
    return object;
}

void CatalogsRetailItemsBatch::fromJsonObject_internal(boost::json::object const& object)
{
    m_Batch_idIsSet = false;
    m_Completed_timeIsSet = false;
    m_ItemsIsSet = false;
    m_StatusIsSet = false;
    {
        const auto Batch_idIt = object.find("batch_id");
        if (Batch_idIt != object.end()) {
            setBatchId(JsonValueConverter<std::string>::fromJsonValue(Batch_idIt->value()));
        }
    }
    {
        const auto Catalog_typeIt = object.find("catalog_type");
        if (Catalog_typeIt != object.end()) {
            setCatalogType(JsonValueConverter<std::string>::fromJsonValue(Catalog_typeIt->value()));
        }
    }
    {
        const auto Completed_timeIt = object.find("completed_time");
        if (Completed_timeIt != object.end()) {
            setCompletedTime(JsonValueConverter<std::string>::fromJsonValue(Completed_timeIt->value()));
        }
    }
    {
        const auto Created_timeIt = object.find("created_time");
        if (Created_timeIt != object.end()) {
            setCreatedTime(JsonValueConverter<std::string>::fromJsonValue(Created_timeIt->value()));
        }
    }
    {
        const auto ItemsIt = object.find("items");
        if (ItemsIt != object.end()) {
            setItems(JsonValueConverter<std::vector<std::shared_ptr<ItemProcessingRecord>>>::fromJsonValue(ItemsIt->value()));
        }
    }
    {
        const auto StatusIt = object.find("status");
        if (StatusIt != object.end()) {
            setStatus(JsonValueConverter<std::shared_ptr<BatchOperationStatus>>::fromJsonValue(StatusIt->value()));
        }
    }
}

std::string CatalogsRetailItemsBatch::getBatchId() const
{
    return m_Batch_id;
}

void CatalogsRetailItemsBatch::setBatchId(std::string value)
{
    
    m_Batch_id = std::move(value);
    m_Batch_idIsSet = true;
}
std::string CatalogsRetailItemsBatch::getCatalogType() const
{
    return m_Catalog_type;
}

void CatalogsRetailItemsBatch::setCatalogType(std::string value)
{
    static const std::array<std::string, 1> allowedValues = {
        "RETAIL"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Catalog_type = std::move(value);
}
std::string CatalogsRetailItemsBatch::getCompletedTime() const
{
    return m_Completed_time;
}

void CatalogsRetailItemsBatch::setCompletedTime(std::string value)
{
    
    m_Completed_time = std::move(value);
    m_Completed_timeIsSet = true;
}
std::string CatalogsRetailItemsBatch::getCreatedTime() const
{
    return m_Created_time;
}

void CatalogsRetailItemsBatch::setCreatedTime(std::string value)
{
    
    m_Created_time = std::move(value);
}
std::vector<std::shared_ptr<ItemProcessingRecord>> CatalogsRetailItemsBatch::getItems() const
{
    return m_Items;
}

void CatalogsRetailItemsBatch::setItems(std::vector<std::shared_ptr<ItemProcessingRecord>> value)
{
    
    m_Items = std::move(value);
    m_ItemsIsSet = true;
}
std::shared_ptr<BatchOperationStatus> CatalogsRetailItemsBatch::getStatus() const
{
    return m_Status;
}

void CatalogsRetailItemsBatch::setStatus(std::shared_ptr<BatchOperationStatus> value)
{
    
    m_Status = std::move(value);
    m_StatusIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CatalogsRetailItemsBatch>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CatalogsRetailItemsBatch>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CatalogsRetailItemsBatch>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CatalogsRetailItemsBatch>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

