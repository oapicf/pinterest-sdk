/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Account.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Account::Account(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Account::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Account::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Account::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Account::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Account");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Account::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_AboutIsSet) {
            object["about"] = JsonValueConverter<std::string>::toJsonValue(getAbout());
        }
        if (m_Account_typeIsSet) {
            object["account_type"] = JsonValueConverter<std::shared_ptr<UserAccountType>>::toJsonValue(getAccountType());
        }
        if (m_Board_countIsSet) {
            object["board_count"] = JsonValueConverter<int32_t>::toJsonValue(getBoardCount());
        }
        if (m_Business_nameIsSet) {
            object["business_name"] = JsonValueConverter<std::string>::toJsonValue(getBusinessName());
        }
        if (m_Follower_countIsSet) {
            object["follower_count"] = JsonValueConverter<int32_t>::toJsonValue(getFollowerCount());
        }
        if (m_Following_countIsSet) {
            object["following_count"] = JsonValueConverter<int32_t>::toJsonValue(getFollowingCount());
        }
        if (m_IdIsSet) {
            object["id"] = JsonValueConverter<std::string>::toJsonValue(getId());
        }
        if (m_Monthly_viewsIsSet) {
            object["monthly_views"] = JsonValueConverter<int32_t>::toJsonValue(getMonthlyViews());
        }
        if (m_Pin_countIsSet) {
            object["pin_count"] = JsonValueConverter<int32_t>::toJsonValue(getPinCount());
        }
        if (m_Profile_imageIsSet) {
            object["profile_image"] = JsonValueConverter<std::string>::toJsonValue(getProfileImage());
        }
        if (m_UsernameIsSet) {
            object["username"] = JsonValueConverter<std::string>::toJsonValue(getUsername());
        }
        if (m_Website_urlIsSet) {
            object["website_url"] = JsonValueConverter<std::string>::toJsonValue(getWebsiteUrl());
        }
    return object;
}

void Account::fromJsonObject_internal(boost::json::object const& object)
{
    m_AboutIsSet = false;
    m_Account_typeIsSet = false;
    m_Board_countIsSet = false;
    m_Business_nameIsSet = false;
    m_Follower_countIsSet = false;
    m_Following_countIsSet = false;
    m_IdIsSet = false;
    m_Monthly_viewsIsSet = false;
    m_Pin_countIsSet = false;
    m_Profile_imageIsSet = false;
    m_UsernameIsSet = false;
    m_Website_urlIsSet = false;
    {
        const auto AboutIt = object.find("about");
        if (AboutIt != object.end()) {
            setAbout(JsonValueConverter<std::string>::fromJsonValue(AboutIt->value()));
        }
    }
    {
        const auto Account_typeIt = object.find("account_type");
        if (Account_typeIt != object.end()) {
            setAccountType(JsonValueConverter<std::shared_ptr<UserAccountType>>::fromJsonValue(Account_typeIt->value()));
        }
    }
    {
        const auto Board_countIt = object.find("board_count");
        if (Board_countIt != object.end()) {
            setBoardCount(JsonValueConverter<int32_t>::fromJsonValue(Board_countIt->value()));
        }
    }
    {
        const auto Business_nameIt = object.find("business_name");
        if (Business_nameIt != object.end()) {
            setBusinessName(JsonValueConverter<std::string>::fromJsonValue(Business_nameIt->value()));
        }
    }
    {
        const auto Follower_countIt = object.find("follower_count");
        if (Follower_countIt != object.end()) {
            setFollowerCount(JsonValueConverter<int32_t>::fromJsonValue(Follower_countIt->value()));
        }
    }
    {
        const auto Following_countIt = object.find("following_count");
        if (Following_countIt != object.end()) {
            setFollowingCount(JsonValueConverter<int32_t>::fromJsonValue(Following_countIt->value()));
        }
    }
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            setId(JsonValueConverter<std::string>::fromJsonValue(IdIt->value()));
        }
    }
    {
        const auto Monthly_viewsIt = object.find("monthly_views");
        if (Monthly_viewsIt != object.end()) {
            setMonthlyViews(JsonValueConverter<int32_t>::fromJsonValue(Monthly_viewsIt->value()));
        }
    }
    {
        const auto Pin_countIt = object.find("pin_count");
        if (Pin_countIt != object.end()) {
            setPinCount(JsonValueConverter<int32_t>::fromJsonValue(Pin_countIt->value()));
        }
    }
    {
        const auto Profile_imageIt = object.find("profile_image");
        if (Profile_imageIt != object.end()) {
            setProfileImage(JsonValueConverter<std::string>::fromJsonValue(Profile_imageIt->value()));
        }
    }
    {
        const auto UsernameIt = object.find("username");
        if (UsernameIt != object.end()) {
            setUsername(JsonValueConverter<std::string>::fromJsonValue(UsernameIt->value()));
        }
    }
    {
        const auto Website_urlIt = object.find("website_url");
        if (Website_urlIt != object.end()) {
            setWebsiteUrl(JsonValueConverter<std::string>::fromJsonValue(Website_urlIt->value()));
        }
    }
}

std::string Account::getAbout() const
{
    return m_About;
}

void Account::setAbout(std::string value)
{
    
    m_About = std::move(value);
    m_AboutIsSet = true;
}
std::shared_ptr<UserAccountType> Account::getAccountType() const
{
    return m_Account_type;
}

void Account::setAccountType(std::shared_ptr<UserAccountType> value)
{
    
    m_Account_type = std::move(value);
    m_Account_typeIsSet = true;
}
int32_t Account::getBoardCount() const
{
    return m_Board_count;
}

void Account::setBoardCount(int32_t value)
{
    
    m_Board_count = std::move(value);
    m_Board_countIsSet = true;
}
std::string Account::getBusinessName() const
{
    return m_Business_name;
}

void Account::setBusinessName(std::string value)
{
    
    m_Business_name = std::move(value);
    m_Business_nameIsSet = true;
}
int32_t Account::getFollowerCount() const
{
    return m_Follower_count;
}

void Account::setFollowerCount(int32_t value)
{
    
    m_Follower_count = std::move(value);
    m_Follower_countIsSet = true;
}
int32_t Account::getFollowingCount() const
{
    return m_Following_count;
}

void Account::setFollowingCount(int32_t value)
{
    
    m_Following_count = std::move(value);
    m_Following_countIsSet = true;
}
std::string Account::getId() const
{
    return m_Id;
}

void Account::setId(std::string value)
{
    
    m_Id = std::move(value);
    m_IdIsSet = true;
}
int32_t Account::getMonthlyViews() const
{
    return m_Monthly_views;
}

void Account::setMonthlyViews(int32_t value)
{
    
    m_Monthly_views = std::move(value);
    m_Monthly_viewsIsSet = true;
}
int32_t Account::getPinCount() const
{
    return m_Pin_count;
}

void Account::setPinCount(int32_t value)
{
    
    m_Pin_count = std::move(value);
    m_Pin_countIsSet = true;
}
std::string Account::getProfileImage() const
{
    return m_Profile_image;
}

void Account::setProfileImage(std::string value)
{
    
    m_Profile_image = std::move(value);
    m_Profile_imageIsSet = true;
}
std::string Account::getUsername() const
{
    return m_Username;
}

void Account::setUsername(std::string value)
{
    
    m_Username = std::move(value);
    m_UsernameIsSet = true;
}
std::string Account::getWebsiteUrl() const
{
    return m_Website_url;
}

void Account::setWebsiteUrl(std::string value)
{
    
    m_Website_url = std::move(value);
    m_Website_urlIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Account>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<Account>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Account>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<Account>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

