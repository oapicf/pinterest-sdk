/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CatalogsRetailListProductsByCatalogBasedFilterRequest.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CatalogsRetailListProductsByCatalogBasedFilterRequest::CatalogsRetailListProductsByCatalogBasedFilterRequest(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CatalogsRetailListProductsByCatalogBasedFilterRequest::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CatalogsRetailListProductsByCatalogBasedFilterRequest::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CatalogsRetailListProductsByCatalogBasedFilterRequest");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CatalogsRetailListProductsByCatalogBasedFilterRequest::toJsonObject_internal() const
{
    boost::json::object object;
        object["catalog_id"] = JsonValueConverter<std::string>::toJsonValue(getCatalogId());
        object["catalog_type"] = JsonValueConverter<std::string>::toJsonValue(getCatalogType());
        object["country"] = JsonValueConverter<std::shared_ptr<Country>>::toJsonValue(getCountry());
        object["filters"] = JsonValueConverter<std::shared_ptr<CatalogsProductGroupFilters>>::toJsonValue(getFilters());
        object["locale"] = JsonValueConverter<std::shared_ptr<CatalogsLocale>>::toJsonValue(getLocale());
    return object;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto Catalog_idIt = object.find("catalog_id");
        if (Catalog_idIt != object.end()) {
            setCatalogId(JsonValueConverter<std::string>::fromJsonValue(Catalog_idIt->value()));
        }
    }
    {
        const auto Catalog_typeIt = object.find("catalog_type");
        if (Catalog_typeIt != object.end()) {
            setCatalogType(JsonValueConverter<std::string>::fromJsonValue(Catalog_typeIt->value()));
        }
    }
    {
        const auto CountryIt = object.find("country");
        if (CountryIt != object.end()) {
            setCountry(JsonValueConverter<std::shared_ptr<Country>>::fromJsonValue(CountryIt->value()));
        }
    }
    {
        const auto FiltersIt = object.find("filters");
        if (FiltersIt != object.end()) {
            setFilters(JsonValueConverter<std::shared_ptr<CatalogsProductGroupFilters>>::fromJsonValue(FiltersIt->value()));
        }
    }
    {
        const auto LocaleIt = object.find("locale");
        if (LocaleIt != object.end()) {
            setLocale(JsonValueConverter<std::shared_ptr<CatalogsLocale>>::fromJsonValue(LocaleIt->value()));
        }
    }
}

std::string CatalogsRetailListProductsByCatalogBasedFilterRequest::getCatalogId() const
{
    return m_Catalog_id;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::setCatalogId(std::string value)
{
    
    m_Catalog_id = std::move(value);
}
std::string CatalogsRetailListProductsByCatalogBasedFilterRequest::getCatalogType() const
{
    return m_Catalog_type;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::setCatalogType(std::string value)
{
    static const std::array<std::string, 1> allowedValues = {
        "RETAIL"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Catalog_type = std::move(value);
}
std::shared_ptr<Country> CatalogsRetailListProductsByCatalogBasedFilterRequest::getCountry() const
{
    return m_Country;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::setCountry(std::shared_ptr<Country> value)
{
    
    m_Country = std::move(value);
}
std::shared_ptr<CatalogsProductGroupFilters> CatalogsRetailListProductsByCatalogBasedFilterRequest::getFilters() const
{
    return m_Filters;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::setFilters(std::shared_ptr<CatalogsProductGroupFilters> value)
{
    
    m_Filters = std::move(value);
}
std::shared_ptr<CatalogsLocale> CatalogsRetailListProductsByCatalogBasedFilterRequest::getLocale() const
{
    return m_Locale;
}

void CatalogsRetailListProductsByCatalogBasedFilterRequest::setLocale(std::shared_ptr<CatalogsLocale> value)
{
    
    m_Locale = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CatalogsRetailListProductsByCatalogBasedFilterRequest>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CatalogsRetailListProductsByCatalogBasedFilterRequest>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CatalogsRetailListProductsByCatalogBasedFilterRequest>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CatalogsRetailListProductsByCatalogBasedFilterRequest>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

