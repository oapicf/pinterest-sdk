/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "EntityHistory.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

EntityHistory::EntityHistory(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string EntityHistory::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void EntityHistory::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value EntityHistory::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void EntityHistory::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for EntityHistory");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object EntityHistory::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Change_timestampIsSet) {
            object["change_timestamp"] = JsonValueConverter<int32_t>::toJsonValue(getChangeTimestamp());
        }
        if (m_Data_changesIsSet) {
            object["data_changes"] = JsonValueConverter<std::vector<std::shared_ptr<EntityDataChangeHistory>>>::toJsonValue(getDataChanges());
        }
        if (m_Entity_idIsSet) {
            object["entity_id"] = JsonValueConverter<std::string>::toJsonValue(getEntityId());
        }
        if (m_Entity_nameIsSet) {
            object["entity_name"] = JsonValueConverter<std::string>::toJsonValue(getEntityName());
        }
        if (m_LdapIsSet) {
            object["ldap"] = JsonValueConverter<std::string>::toJsonValue(getLdap());
        }
        if (m_OperationIsSet) {
            object["operation"] = JsonValueConverter<std::shared_ptr<ChangeHistoryOperationType>>::toJsonValue(getOperation());
        }
        if (m_User_idIsSet) {
            object["user_id"] = JsonValueConverter<std::string>::toJsonValue(getUserId());
        }
    return object;
}

void EntityHistory::fromJsonObject_internal(boost::json::object const& object)
{
    m_Change_timestampIsSet = false;
    m_Data_changesIsSet = false;
    m_Entity_idIsSet = false;
    m_Entity_nameIsSet = false;
    m_LdapIsSet = false;
    m_OperationIsSet = false;
    m_User_idIsSet = false;
    {
        const auto Change_timestampIt = object.find("change_timestamp");
        if (Change_timestampIt != object.end()) {
            setChangeTimestamp(JsonValueConverter<int32_t>::fromJsonValue(Change_timestampIt->value()));
        }
    }
    {
        const auto Data_changesIt = object.find("data_changes");
        if (Data_changesIt != object.end()) {
            setDataChanges(JsonValueConverter<std::vector<std::shared_ptr<EntityDataChangeHistory>>>::fromJsonValue(Data_changesIt->value()));
        }
    }
    {
        const auto Entity_idIt = object.find("entity_id");
        if (Entity_idIt != object.end()) {
            setEntityId(JsonValueConverter<std::string>::fromJsonValue(Entity_idIt->value()));
        }
    }
    {
        const auto Entity_nameIt = object.find("entity_name");
        if (Entity_nameIt != object.end()) {
            setEntityName(JsonValueConverter<std::string>::fromJsonValue(Entity_nameIt->value()));
        }
    }
    {
        const auto LdapIt = object.find("ldap");
        if (LdapIt != object.end()) {
            setLdap(JsonValueConverter<std::string>::fromJsonValue(LdapIt->value()));
        }
    }
    {
        const auto OperationIt = object.find("operation");
        if (OperationIt != object.end()) {
            setOperation(JsonValueConverter<std::shared_ptr<ChangeHistoryOperationType>>::fromJsonValue(OperationIt->value()));
        }
    }
    {
        const auto User_idIt = object.find("user_id");
        if (User_idIt != object.end()) {
            setUserId(JsonValueConverter<std::string>::fromJsonValue(User_idIt->value()));
        }
    }
}

int32_t EntityHistory::getChangeTimestamp() const
{
    return m_Change_timestamp;
}

void EntityHistory::setChangeTimestamp(int32_t value)
{
    
    m_Change_timestamp = std::move(value);
    m_Change_timestampIsSet = true;
}
std::vector<std::shared_ptr<EntityDataChangeHistory>> EntityHistory::getDataChanges() const
{
    return m_Data_changes;
}

void EntityHistory::setDataChanges(std::vector<std::shared_ptr<EntityDataChangeHistory>> value)
{
    
    m_Data_changes = std::move(value);
    m_Data_changesIsSet = true;
}
std::string EntityHistory::getEntityId() const
{
    return m_Entity_id;
}

void EntityHistory::setEntityId(std::string value)
{
    
    m_Entity_id = std::move(value);
    m_Entity_idIsSet = true;
}
std::string EntityHistory::getEntityName() const
{
    return m_Entity_name;
}

void EntityHistory::setEntityName(std::string value)
{
    
    m_Entity_name = std::move(value);
    m_Entity_nameIsSet = true;
}
std::string EntityHistory::getLdap() const
{
    return m_Ldap;
}

void EntityHistory::setLdap(std::string value)
{
    
    m_Ldap = std::move(value);
    m_LdapIsSet = true;
}
std::shared_ptr<ChangeHistoryOperationType> EntityHistory::getOperation() const
{
    return m_Operation;
}

void EntityHistory::setOperation(std::shared_ptr<ChangeHistoryOperationType> value)
{
    
    m_Operation = std::move(value);
    m_OperationIsSet = true;
}
std::string EntityHistory::getUserId() const
{
    return m_User_id;
}

void EntityHistory::setUserId(std::string value)
{
    
    m_User_id = std::move(value);
    m_User_idIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<EntityHistory>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<EntityHistory>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<EntityHistory>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<EntityHistory>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

