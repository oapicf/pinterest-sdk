/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "StoreMetadata.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

StoreMetadata::StoreMetadata(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string StoreMetadata::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void StoreMetadata::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value StoreMetadata::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void StoreMetadata::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for StoreMetadata");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object StoreMetadata::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_GeohashIsSet) {
            object["geohash"] = JsonValueConverter<std::string>::toJsonValue(getGeohash());
        }
        if (m_LatitudeIsSet) {
            object["latitude"] = JsonValueConverter<double>::toJsonValue(getLatitude());
        }
        if (m_LongitudeIsSet) {
            object["longitude"] = JsonValueConverter<double>::toJsonValue(getLongitude());
        }
        object["store_code"] = JsonValueConverter<std::string>::toJsonValue(getStoreCode());
        object["store_id"] = JsonValueConverter<std::string>::toJsonValue(getStoreId());
        if (m_Store_nameIsSet) {
            object["store_name"] = JsonValueConverter<std::string>::toJsonValue(getStoreName());
        }
    return object;
}

void StoreMetadata::fromJsonObject_internal(boost::json::object const& object)
{
    m_GeohashIsSet = false;
    m_LatitudeIsSet = false;
    m_LongitudeIsSet = false;
    m_Store_nameIsSet = false;
    {
        const auto GeohashIt = object.find("geohash");
        if (GeohashIt != object.end()) {
            setGeohash(JsonValueConverter<std::string>::fromJsonValue(GeohashIt->value()));
        }
    }
    {
        const auto LatitudeIt = object.find("latitude");
        if (LatitudeIt != object.end()) {
            setLatitude(JsonValueConverter<double>::fromJsonValue(LatitudeIt->value()));
        }
    }
    {
        const auto LongitudeIt = object.find("longitude");
        if (LongitudeIt != object.end()) {
            setLongitude(JsonValueConverter<double>::fromJsonValue(LongitudeIt->value()));
        }
    }
    {
        const auto Store_codeIt = object.find("store_code");
        if (Store_codeIt != object.end()) {
            setStoreCode(JsonValueConverter<std::string>::fromJsonValue(Store_codeIt->value()));
        }
    }
    {
        const auto Store_idIt = object.find("store_id");
        if (Store_idIt != object.end()) {
            setStoreId(JsonValueConverter<std::string>::fromJsonValue(Store_idIt->value()));
        }
    }
    {
        const auto Store_nameIt = object.find("store_name");
        if (Store_nameIt != object.end()) {
            setStoreName(JsonValueConverter<std::string>::fromJsonValue(Store_nameIt->value()));
        }
    }
}

std::string StoreMetadata::getGeohash() const
{
    return m_Geohash;
}

void StoreMetadata::setGeohash(std::string value)
{
    
    m_Geohash = std::move(value);
    m_GeohashIsSet = true;
}
double StoreMetadata::getLatitude() const
{
    return m_Latitude;
}

void StoreMetadata::setLatitude(double value)
{
    
    m_Latitude = std::move(value);
    m_LatitudeIsSet = true;
}
double StoreMetadata::getLongitude() const
{
    return m_Longitude;
}

void StoreMetadata::setLongitude(double value)
{
    
    m_Longitude = std::move(value);
    m_LongitudeIsSet = true;
}
std::string StoreMetadata::getStoreCode() const
{
    return m_Store_code;
}

void StoreMetadata::setStoreCode(std::string value)
{
    
    m_Store_code = std::move(value);
}
std::string StoreMetadata::getStoreId() const
{
    return m_Store_id;
}

void StoreMetadata::setStoreId(std::string value)
{
    
    m_Store_id = std::move(value);
}
std::string StoreMetadata::getStoreName() const
{
    return m_Store_name;
}

void StoreMetadata::setStoreName(std::string value)
{
    
    m_Store_name = std::move(value);
    m_Store_nameIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<StoreMetadata>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<StoreMetadata>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<StoreMetadata>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<StoreMetadata>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

