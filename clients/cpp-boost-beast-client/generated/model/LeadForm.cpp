/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "LeadForm.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

LeadForm::LeadForm(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string LeadForm::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void LeadForm::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value LeadForm::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void LeadForm::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for LeadForm");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object LeadForm::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Ad_account_idIsSet) {
            object["ad_account_id"] = JsonValueConverter<std::string>::toJsonValue(getAdAccountId());
        }
        object["completion_message"] = JsonValueConverter<std::string>::toJsonValue(getCompletionMessage());
        if (m_Created_timeIsSet) {
            object["created_time"] = JsonValueConverter<int32_t>::toJsonValue(getCreatedTime());
        }
        if (m_Disclosure_languageIsSet) {
            object["disclosure_language"] = JsonValueConverter<std::string>::toJsonValue(getDisclosureLanguage());
        }
        object["has_accepted_terms"] = JsonValueConverter<bool>::toJsonValue(isHasAcceptedTerms());
        object["id"] = JsonValueConverter<std::string>::toJsonValue(getId());
        object["name"] = JsonValueConverter<std::string>::toJsonValue(getName());
        if (m_Policy_linksIsSet) {
            object["policy_links"] = JsonValueConverter<std::vector<std::shared_ptr<LeadFormPolicyLink>>>::toJsonValue(getPolicyLinks());
        }
        object["privacy_policy_link"] = JsonValueConverter<std::string>::toJsonValue(getPrivacyPolicyLink());
        object["questions"] = JsonValueConverter<std::vector<std::shared_ptr<LeadFormQuestion>>>::toJsonValue(getQuestions());
        if (m_StatusIsSet) {
            object["status"] = JsonValueConverter<std::shared_ptr<LeadFormStatus>>::toJsonValue(getStatus());
        }
        if (m_Updated_timeIsSet) {
            object["updated_time"] = JsonValueConverter<int32_t>::toJsonValue(getUpdatedTime());
        }
    return object;
}

void LeadForm::fromJsonObject_internal(boost::json::object const& object)
{
    m_Ad_account_idIsSet = false;
    m_Created_timeIsSet = false;
    m_Disclosure_languageIsSet = false;
    m_Policy_linksIsSet = false;
    m_StatusIsSet = false;
    m_Updated_timeIsSet = false;
    {
        const auto Ad_account_idIt = object.find("ad_account_id");
        if (Ad_account_idIt != object.end()) {
            setAdAccountId(JsonValueConverter<std::string>::fromJsonValue(Ad_account_idIt->value()));
        }
    }
    {
        const auto Completion_messageIt = object.find("completion_message");
        if (Completion_messageIt != object.end()) {
            setCompletionMessage(JsonValueConverter<std::string>::fromJsonValue(Completion_messageIt->value()));
        }
    }
    {
        const auto Created_timeIt = object.find("created_time");
        if (Created_timeIt != object.end()) {
            setCreatedTime(JsonValueConverter<int32_t>::fromJsonValue(Created_timeIt->value()));
        }
    }
    {
        const auto Disclosure_languageIt = object.find("disclosure_language");
        if (Disclosure_languageIt != object.end()) {
            setDisclosureLanguage(JsonValueConverter<std::string>::fromJsonValue(Disclosure_languageIt->value()));
        }
    }
    {
        const auto Has_accepted_termsIt = object.find("has_accepted_terms");
        if (Has_accepted_termsIt != object.end()) {
            setHasAcceptedTerms(JsonValueConverter<bool>::fromJsonValue(Has_accepted_termsIt->value()));
        }
    }
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            setId(JsonValueConverter<std::string>::fromJsonValue(IdIt->value()));
        }
    }
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            setName(JsonValueConverter<std::string>::fromJsonValue(NameIt->value()));
        }
    }
    {
        const auto Policy_linksIt = object.find("policy_links");
        if (Policy_linksIt != object.end()) {
            setPolicyLinks(JsonValueConverter<std::vector<std::shared_ptr<LeadFormPolicyLink>>>::fromJsonValue(Policy_linksIt->value()));
        }
    }
    {
        const auto Privacy_policy_linkIt = object.find("privacy_policy_link");
        if (Privacy_policy_linkIt != object.end()) {
            setPrivacyPolicyLink(JsonValueConverter<std::string>::fromJsonValue(Privacy_policy_linkIt->value()));
        }
    }
    {
        const auto QuestionsIt = object.find("questions");
        if (QuestionsIt != object.end()) {
            setQuestions(JsonValueConverter<std::vector<std::shared_ptr<LeadFormQuestion>>>::fromJsonValue(QuestionsIt->value()));
        }
    }
    {
        const auto StatusIt = object.find("status");
        if (StatusIt != object.end()) {
            setStatus(JsonValueConverter<std::shared_ptr<LeadFormStatus>>::fromJsonValue(StatusIt->value()));
        }
    }
    {
        const auto Updated_timeIt = object.find("updated_time");
        if (Updated_timeIt != object.end()) {
            setUpdatedTime(JsonValueConverter<int32_t>::fromJsonValue(Updated_timeIt->value()));
        }
    }
}

std::string LeadForm::getAdAccountId() const
{
    return m_Ad_account_id;
}

void LeadForm::setAdAccountId(std::string value)
{
    
    m_Ad_account_id = std::move(value);
    m_Ad_account_idIsSet = true;
}
std::string LeadForm::getCompletionMessage() const
{
    return m_Completion_message;
}

void LeadForm::setCompletionMessage(std::string value)
{
    
    m_Completion_message = std::move(value);
}
int32_t LeadForm::getCreatedTime() const
{
    return m_Created_time;
}

void LeadForm::setCreatedTime(int32_t value)
{
    
    m_Created_time = std::move(value);
    m_Created_timeIsSet = true;
}
std::string LeadForm::getDisclosureLanguage() const
{
    return m_Disclosure_language;
}

void LeadForm::setDisclosureLanguage(std::string value)
{
    
    m_Disclosure_language = std::move(value);
    m_Disclosure_languageIsSet = true;
}
bool LeadForm::isHasAcceptedTerms() const
{
    return m_Has_accepted_terms;
}

void LeadForm::setHasAcceptedTerms(bool value)
{
    
    m_Has_accepted_terms = std::move(value);
}
std::string LeadForm::getId() const
{
    return m_Id;
}

void LeadForm::setId(std::string value)
{
    
    m_Id = std::move(value);
}
std::string LeadForm::getName() const
{
    return m_Name;
}

void LeadForm::setName(std::string value)
{
    
    m_Name = std::move(value);
}
std::vector<std::shared_ptr<LeadFormPolicyLink>> LeadForm::getPolicyLinks() const
{
    return m_Policy_links;
}

void LeadForm::setPolicyLinks(std::vector<std::shared_ptr<LeadFormPolicyLink>> value)
{
    
    m_Policy_links = std::move(value);
    m_Policy_linksIsSet = true;
}
std::string LeadForm::getPrivacyPolicyLink() const
{
    return m_Privacy_policy_link;
}

void LeadForm::setPrivacyPolicyLink(std::string value)
{
    
    m_Privacy_policy_link = std::move(value);
}
std::vector<std::shared_ptr<LeadFormQuestion>> LeadForm::getQuestions() const
{
    return m_Questions;
}

void LeadForm::setQuestions(std::vector<std::shared_ptr<LeadFormQuestion>> value)
{
    
    m_Questions = std::move(value);
}
std::shared_ptr<LeadFormStatus> LeadForm::getStatus() const
{
    return m_Status;
}

void LeadForm::setStatus(std::shared_ptr<LeadFormStatus> value)
{
    
    m_Status = std::move(value);
    m_StatusIsSet = true;
}
int32_t LeadForm::getUpdatedTime() const
{
    return m_Updated_time;
}

void LeadForm::setUpdatedTime(int32_t value)
{
    
    m_Updated_time = std::move(value);
    m_Updated_timeIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<LeadForm>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<LeadForm>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<LeadForm>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<LeadForm>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

