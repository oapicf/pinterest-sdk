/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ProductCategoryDetails.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

ProductCategoryDetails::ProductCategoryDetails(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string ProductCategoryDetails::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void ProductCategoryDetails::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value ProductCategoryDetails::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void ProductCategoryDetails::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for ProductCategoryDetails");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object ProductCategoryDetails::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_DemographicsIsSet) {
            object["demographics"] = JsonValueConverter<std::shared_ptr<ProductCategoriesDemographic>>::toJsonValue(getDemographics());
        }
        object["has_prediction"] = JsonValueConverter<bool>::toJsonValue(isHasPrediction());
        if (m_Metrics_highlightsIsSet) {
            object["metrics_highlights"] = JsonValueConverter<std::shared_ptr<ProductCategoriesMetricsHighlights>>::toJsonValue(getMetricsHighlights());
        }
        if (m_Predicted_time_seriesIsSet) {
            object["predicted_time_series"] = JsonValueConverter<std::map<std::string, double>>::toJsonValue(getPredictedTimeSeries());
        }
        object["product_category"] = JsonValueConverter<std::shared_ptr<ProductCategoryEnum>>::toJsonValue(getProductCategory());
        if (m_Related_searchesIsSet) {
            object["related_searches"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getRelatedSearches());
        }
        if (m_Time_seriesIsSet) {
            object["time_series"] = JsonValueConverter<std::map<std::string, double>>::toJsonValue(getTimeSeries());
        }
    return object;
}

void ProductCategoryDetails::fromJsonObject_internal(boost::json::object const& object)
{
    m_DemographicsIsSet = false;
    m_Metrics_highlightsIsSet = false;
    m_Predicted_time_seriesIsSet = false;
    m_Related_searchesIsSet = false;
    m_Time_seriesIsSet = false;
    {
        const auto DemographicsIt = object.find("demographics");
        if (DemographicsIt != object.end()) {
            setDemographics(JsonValueConverter<std::shared_ptr<ProductCategoriesDemographic>>::fromJsonValue(DemographicsIt->value()));
        }
    }
    {
        const auto Has_predictionIt = object.find("has_prediction");
        if (Has_predictionIt != object.end()) {
            setHasPrediction(JsonValueConverter<bool>::fromJsonValue(Has_predictionIt->value()));
        }
    }
    {
        const auto Metrics_highlightsIt = object.find("metrics_highlights");
        if (Metrics_highlightsIt != object.end()) {
            setMetricsHighlights(JsonValueConverter<std::shared_ptr<ProductCategoriesMetricsHighlights>>::fromJsonValue(Metrics_highlightsIt->value()));
        }
    }
    {
        const auto Predicted_time_seriesIt = object.find("predicted_time_series");
        if (Predicted_time_seriesIt != object.end()) {
            setPredictedTimeSeries(JsonValueConverter<std::map<std::string, double>>::fromJsonValue(Predicted_time_seriesIt->value()));
        }
    }
    {
        const auto Product_categoryIt = object.find("product_category");
        if (Product_categoryIt != object.end()) {
            setProductCategory(JsonValueConverter<std::shared_ptr<ProductCategoryEnum>>::fromJsonValue(Product_categoryIt->value()));
        }
    }
    {
        const auto Related_searchesIt = object.find("related_searches");
        if (Related_searchesIt != object.end()) {
            setRelatedSearches(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Related_searchesIt->value()));
        }
    }
    {
        const auto Time_seriesIt = object.find("time_series");
        if (Time_seriesIt != object.end()) {
            setTimeSeries(JsonValueConverter<std::map<std::string, double>>::fromJsonValue(Time_seriesIt->value()));
        }
    }
}

std::shared_ptr<ProductCategoriesDemographic> ProductCategoryDetails::getDemographics() const
{
    return m_Demographics;
}

void ProductCategoryDetails::setDemographics(std::shared_ptr<ProductCategoriesDemographic> value)
{
    
    m_Demographics = std::move(value);
    m_DemographicsIsSet = true;
}
bool ProductCategoryDetails::isHasPrediction() const
{
    return m_Has_prediction;
}

void ProductCategoryDetails::setHasPrediction(bool value)
{
    
    m_Has_prediction = std::move(value);
}
std::shared_ptr<ProductCategoriesMetricsHighlights> ProductCategoryDetails::getMetricsHighlights() const
{
    return m_Metrics_highlights;
}

void ProductCategoryDetails::setMetricsHighlights(std::shared_ptr<ProductCategoriesMetricsHighlights> value)
{
    
    m_Metrics_highlights = std::move(value);
    m_Metrics_highlightsIsSet = true;
}
std::map<std::string, double> ProductCategoryDetails::getPredictedTimeSeries() const
{
    return m_Predicted_time_series;
}

void ProductCategoryDetails::setPredictedTimeSeries(std::map<std::string, double> value)
{
    
    m_Predicted_time_series = std::move(value);
    m_Predicted_time_seriesIsSet = true;
}
std::shared_ptr<ProductCategoryEnum> ProductCategoryDetails::getProductCategory() const
{
    return m_Product_category;
}

void ProductCategoryDetails::setProductCategory(std::shared_ptr<ProductCategoryEnum> value)
{
    
    m_Product_category = std::move(value);
}
std::vector<std::string> ProductCategoryDetails::getRelatedSearches() const
{
    return m_Related_searches;
}

void ProductCategoryDetails::setRelatedSearches(std::vector<std::string> value)
{
    
    m_Related_searches = std::move(value);
    m_Related_searchesIsSet = true;
}
std::map<std::string, double> ProductCategoryDetails::getTimeSeries() const
{
    return m_Time_series;
}

void ProductCategoryDetails::setTimeSeries(std::map<std::string, double> value)
{
    
    m_Time_series = std::move(value);
    m_Time_seriesIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<ProductCategoryDetails>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<ProductCategoryDetails>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<ProductCategoryDetails>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<ProductCategoryDetails>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

