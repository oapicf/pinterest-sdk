/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "AdGroupAudienceSizingCreate.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

AdGroupAudienceSizingCreate::AdGroupAudienceSizingCreate(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string AdGroupAudienceSizingCreate::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void AdGroupAudienceSizingCreate::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value AdGroupAudienceSizingCreate::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void AdGroupAudienceSizingCreate::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for AdGroupAudienceSizingCreate");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object AdGroupAudienceSizingCreate::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Auto_targeting_enabledIsSet) {
            object["auto_targeting_enabled"] = JsonValueConverter<bool>::toJsonValue(isAutoTargetingEnabled());
        }
        if (m_Creative_typesIsSet) {
            object["creative_types"] = JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingCreativeTypes>>>::toJsonValue(getCreativeTypes());
        }
        if (m_KeywordsIsSet) {
            object["keywords"] = JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingKeyword>>>::toJsonValue(getKeywords());
        }
        if (m_Placement_groupIsSet) {
            object["placement_group"] = JsonValueConverter<std::shared_ptr<AdgroupPlacementGroupType>>::toJsonValue(getPlacementGroup());
        }
        if (m_Product_group_idsIsSet) {
            object["product_group_ids"] = JsonValueConverter<std::vector<std::shared_ptr<std::string>>>::toJsonValue(getProductGroupIds());
        }
        if (m_Targeting_specIsSet) {
            object["targeting_spec"] = JsonValueConverter<std::shared_ptr<TargetingSpecOptimal>>::toJsonValue(getTargetingSpec());
        }
    return object;
}

void AdGroupAudienceSizingCreate::fromJsonObject_internal(boost::json::object const& object)
{
    m_Auto_targeting_enabledIsSet = false;
    m_Creative_typesIsSet = false;
    m_KeywordsIsSet = false;
    m_Placement_groupIsSet = false;
    m_Product_group_idsIsSet = false;
    m_Targeting_specIsSet = false;
    {
        const auto Auto_targeting_enabledIt = object.find("auto_targeting_enabled");
        if (Auto_targeting_enabledIt != object.end()) {
            setAutoTargetingEnabled(JsonValueConverter<bool>::fromJsonValue(Auto_targeting_enabledIt->value()));
        }
    }
    {
        const auto Creative_typesIt = object.find("creative_types");
        if (Creative_typesIt != object.end()) {
            setCreativeTypes(JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingCreativeTypes>>>::fromJsonValue(Creative_typesIt->value()));
        }
    }
    {
        const auto KeywordsIt = object.find("keywords");
        if (KeywordsIt != object.end()) {
            setKeywords(JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingKeyword>>>::fromJsonValue(KeywordsIt->value()));
        }
    }
    {
        const auto Placement_groupIt = object.find("placement_group");
        if (Placement_groupIt != object.end()) {
            setPlacementGroup(JsonValueConverter<std::shared_ptr<AdgroupPlacementGroupType>>::fromJsonValue(Placement_groupIt->value()));
        }
    }
    {
        const auto Product_group_idsIt = object.find("product_group_ids");
        if (Product_group_idsIt != object.end()) {
            setProductGroupIds(JsonValueConverter<std::vector<std::shared_ptr<std::string>>>::fromJsonValue(Product_group_idsIt->value()));
        }
    }
    {
        const auto Targeting_specIt = object.find("targeting_spec");
        if (Targeting_specIt != object.end()) {
            setTargetingSpec(JsonValueConverter<std::shared_ptr<TargetingSpecOptimal>>::fromJsonValue(Targeting_specIt->value()));
        }
    }
}

bool AdGroupAudienceSizingCreate::isAutoTargetingEnabled() const
{
    return m_Auto_targeting_enabled;
}

void AdGroupAudienceSizingCreate::setAutoTargetingEnabled(bool value)
{
    
    m_Auto_targeting_enabled = std::move(value);
    m_Auto_targeting_enabledIsSet = true;
}
std::vector<std::shared_ptr<AdGroupAudienceSizingCreativeTypes>> AdGroupAudienceSizingCreate::getCreativeTypes() const
{
    return m_Creative_types;
}

void AdGroupAudienceSizingCreate::setCreativeTypes(std::vector<std::shared_ptr<AdGroupAudienceSizingCreativeTypes>> value)
{
    
    m_Creative_types = std::move(value);
    m_Creative_typesIsSet = true;
}
std::vector<std::shared_ptr<AdGroupAudienceSizingKeyword>> AdGroupAudienceSizingCreate::getKeywords() const
{
    return m_Keywords;
}

void AdGroupAudienceSizingCreate::setKeywords(std::vector<std::shared_ptr<AdGroupAudienceSizingKeyword>> value)
{
    
    m_Keywords = std::move(value);
    m_KeywordsIsSet = true;
}
std::shared_ptr<AdgroupPlacementGroupType> AdGroupAudienceSizingCreate::getPlacementGroup() const
{
    return m_Placement_group;
}

void AdGroupAudienceSizingCreate::setPlacementGroup(std::shared_ptr<AdgroupPlacementGroupType> value)
{
    
    m_Placement_group = std::move(value);
    m_Placement_groupIsSet = true;
}
std::vector<std::shared_ptr<std::string>> AdGroupAudienceSizingCreate::getProductGroupIds() const
{
    return m_Product_group_ids;
}

void AdGroupAudienceSizingCreate::setProductGroupIds(std::vector<std::shared_ptr<std::string>> value)
{
    
    m_Product_group_ids = std::move(value);
    m_Product_group_idsIsSet = true;
}
std::shared_ptr<TargetingSpecOptimal> AdGroupAudienceSizingCreate::getTargetingSpec() const
{
    return m_Targeting_spec;
}

void AdGroupAudienceSizingCreate::setTargetingSpec(std::shared_ptr<TargetingSpecOptimal> value)
{
    
    m_Targeting_spec = std::move(value);
    m_Targeting_specIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<AdGroupAudienceSizingCreate>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingCreate>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<AdGroupAudienceSizingCreate>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<AdGroupAudienceSizingCreate>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

