/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ScheduleCreateRequest.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

ScheduleCreateRequest::ScheduleCreateRequest(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string ScheduleCreateRequest::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void ScheduleCreateRequest::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value ScheduleCreateRequest::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void ScheduleCreateRequest::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for ScheduleCreateRequest");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object ScheduleCreateRequest::toJsonObject_internal() const
{
    boost::json::object object;
        object["entity_id"] = JsonValueConverter<std::string>::toJsonValue(getEntityId());
        object["entity_type"] = JsonValueConverter<std::string>::toJsonValue(getEntityType());
        object["delta_value"] = JsonValueConverter<std::shared_ptr<ScheduleCommonDeltaValue>>::toJsonValue(getDeltaValue());
        object["end_timestamp"] = JsonValueConverter<int32_t>::toJsonValue(getEndTimestamp());
        object["name"] = JsonValueConverter<std::string>::toJsonValue(getName());
        object["schedule_action"] = JsonValueConverter<std::shared_ptr<ScheduleAction>>::toJsonValue(getScheduleAction());
        object["schedule_status"] = JsonValueConverter<std::shared_ptr<ScheduleStatus>>::toJsonValue(getScheduleStatus());
        object["schedule_type"] = JsonValueConverter<std::shared_ptr<ScheduleType>>::toJsonValue(getScheduleType());
        object["start_timestamp"] = JsonValueConverter<int32_t>::toJsonValue(getStartTimestamp());
    return object;
}

void ScheduleCreateRequest::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto Entity_idIt = object.find("entity_id");
        if (Entity_idIt != object.end()) {
            setEntityId(JsonValueConverter<std::string>::fromJsonValue(Entity_idIt->value()));
        }
    }
    {
        const auto Entity_typeIt = object.find("entity_type");
        if (Entity_typeIt != object.end()) {
            setEntityType(JsonValueConverter<std::string>::fromJsonValue(Entity_typeIt->value()));
        }
    }
    {
        const auto Delta_valueIt = object.find("delta_value");
        if (Delta_valueIt != object.end()) {
            setDeltaValue(JsonValueConverter<std::shared_ptr<ScheduleCommonDeltaValue>>::fromJsonValue(Delta_valueIt->value()));
        }
    }
    {
        const auto End_timestampIt = object.find("end_timestamp");
        if (End_timestampIt != object.end()) {
            setEndTimestamp(JsonValueConverter<int32_t>::fromJsonValue(End_timestampIt->value()));
        }
    }
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            setName(JsonValueConverter<std::string>::fromJsonValue(NameIt->value()));
        }
    }
    {
        const auto Schedule_actionIt = object.find("schedule_action");
        if (Schedule_actionIt != object.end()) {
            setScheduleAction(JsonValueConverter<std::shared_ptr<ScheduleAction>>::fromJsonValue(Schedule_actionIt->value()));
        }
    }
    {
        const auto Schedule_statusIt = object.find("schedule_status");
        if (Schedule_statusIt != object.end()) {
            setScheduleStatus(JsonValueConverter<std::shared_ptr<ScheduleStatus>>::fromJsonValue(Schedule_statusIt->value()));
        }
    }
    {
        const auto Schedule_typeIt = object.find("schedule_type");
        if (Schedule_typeIt != object.end()) {
            setScheduleType(JsonValueConverter<std::shared_ptr<ScheduleType>>::fromJsonValue(Schedule_typeIt->value()));
        }
    }
    {
        const auto Start_timestampIt = object.find("start_timestamp");
        if (Start_timestampIt != object.end()) {
            setStartTimestamp(JsonValueConverter<int32_t>::fromJsonValue(Start_timestampIt->value()));
        }
    }
}

std::string ScheduleCreateRequest::getEntityId() const
{
    return m_Entity_id;
}

void ScheduleCreateRequest::setEntityId(std::string value)
{
    
    m_Entity_id = std::move(value);
}
std::string ScheduleCreateRequest::getEntityType() const
{
    return m_Entity_type;
}

void ScheduleCreateRequest::setEntityType(std::string value)
{
    
    m_Entity_type = std::move(value);
}
std::shared_ptr<ScheduleCommonDeltaValue> ScheduleCreateRequest::getDeltaValue() const
{
    return m_Delta_value;
}

void ScheduleCreateRequest::setDeltaValue(std::shared_ptr<ScheduleCommonDeltaValue> value)
{
    
    m_Delta_value = std::move(value);
}
int32_t ScheduleCreateRequest::getEndTimestamp() const
{
    return m_End_timestamp;
}

void ScheduleCreateRequest::setEndTimestamp(int32_t value)
{
    
    m_End_timestamp = std::move(value);
}
std::string ScheduleCreateRequest::getName() const
{
    return m_Name;
}

void ScheduleCreateRequest::setName(std::string value)
{
    
    m_Name = std::move(value);
}
std::shared_ptr<ScheduleAction> ScheduleCreateRequest::getScheduleAction() const
{
    return m_Schedule_action;
}

void ScheduleCreateRequest::setScheduleAction(std::shared_ptr<ScheduleAction> value)
{
    
    m_Schedule_action = std::move(value);
}
std::shared_ptr<ScheduleStatus> ScheduleCreateRequest::getScheduleStatus() const
{
    return m_Schedule_status;
}

void ScheduleCreateRequest::setScheduleStatus(std::shared_ptr<ScheduleStatus> value)
{
    
    m_Schedule_status = std::move(value);
}
std::shared_ptr<ScheduleType> ScheduleCreateRequest::getScheduleType() const
{
    return m_Schedule_type;
}

void ScheduleCreateRequest::setScheduleType(std::shared_ptr<ScheduleType> value)
{
    
    m_Schedule_type = std::move(value);
}
int32_t ScheduleCreateRequest::getStartTimestamp() const
{
    return m_Start_timestamp;
}

void ScheduleCreateRequest::setStartTimestamp(int32_t value)
{
    
    m_Start_timestamp = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<ScheduleCreateRequest>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<ScheduleCreateRequest>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<ScheduleCreateRequest>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<ScheduleCreateRequest>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

