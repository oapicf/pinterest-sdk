/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "VideoMetadataWithItemType.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

VideoMetadataWithItemType::VideoMetadataWithItemType(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string VideoMetadataWithItemType::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void VideoMetadataWithItemType::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value VideoMetadataWithItemType::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void VideoMetadataWithItemType::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for VideoMetadataWithItemType");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object VideoMetadataWithItemType::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Cover_image_urlIsSet) {
            object["cover_image_url"] = JsonValueConverter<std::string>::toJsonValue(getCoverImageUrl());
        }
        if (m_DurationIsSet) {
            object["duration"] = JsonValueConverter<double>::toJsonValue(getDuration());
        }
        if (m_HeightIsSet) {
            object["height"] = JsonValueConverter<int32_t>::toJsonValue(getHeight());
        }
        object["item_type"] = JsonValueConverter<std::string>::toJsonValue(getItemType());
        if (m_Video_urlIsSet) {
            object["video_url"] = JsonValueConverter<std::string>::toJsonValue(getVideoUrl());
        }
        if (m_Video_url_hlsIsSet) {
            object["video_url_hls"] = JsonValueConverter<std::string>::toJsonValue(getVideoUrlHls());
        }
        if (m_WidthIsSet) {
            object["width"] = JsonValueConverter<int32_t>::toJsonValue(getWidth());
        }
    return object;
}

void VideoMetadataWithItemType::fromJsonObject_internal(boost::json::object const& object)
{
    m_Cover_image_urlIsSet = false;
    m_DurationIsSet = false;
    m_HeightIsSet = false;
    m_Video_urlIsSet = false;
    m_Video_url_hlsIsSet = false;
    m_WidthIsSet = false;
    {
        const auto Cover_image_urlIt = object.find("cover_image_url");
        if (Cover_image_urlIt != object.end()) {
            setCoverImageUrl(JsonValueConverter<std::string>::fromJsonValue(Cover_image_urlIt->value()));
        }
    }
    {
        const auto DurationIt = object.find("duration");
        if (DurationIt != object.end()) {
            setDuration(JsonValueConverter<double>::fromJsonValue(DurationIt->value()));
        }
    }
    {
        const auto HeightIt = object.find("height");
        if (HeightIt != object.end()) {
            setHeight(JsonValueConverter<int32_t>::fromJsonValue(HeightIt->value()));
        }
    }
    {
        const auto Item_typeIt = object.find("item_type");
        if (Item_typeIt != object.end()) {
            setItemType(JsonValueConverter<std::string>::fromJsonValue(Item_typeIt->value()));
        }
    }
    {
        const auto Video_urlIt = object.find("video_url");
        if (Video_urlIt != object.end()) {
            setVideoUrl(JsonValueConverter<std::string>::fromJsonValue(Video_urlIt->value()));
        }
    }
    {
        const auto Video_url_hlsIt = object.find("video_url_hls");
        if (Video_url_hlsIt != object.end()) {
            setVideoUrlHls(JsonValueConverter<std::string>::fromJsonValue(Video_url_hlsIt->value()));
        }
    }
    {
        const auto WidthIt = object.find("width");
        if (WidthIt != object.end()) {
            setWidth(JsonValueConverter<int32_t>::fromJsonValue(WidthIt->value()));
        }
    }
}

std::string VideoMetadataWithItemType::getCoverImageUrl() const
{
    return m_Cover_image_url;
}

void VideoMetadataWithItemType::setCoverImageUrl(std::string value)
{
    
    m_Cover_image_url = std::move(value);
    m_Cover_image_urlIsSet = true;
}
double VideoMetadataWithItemType::getDuration() const
{
    return m_Duration;
}

void VideoMetadataWithItemType::setDuration(double value)
{
    
    m_Duration = std::move(value);
    m_DurationIsSet = true;
}
int32_t VideoMetadataWithItemType::getHeight() const
{
    return m_Height;
}

void VideoMetadataWithItemType::setHeight(int32_t value)
{
    
    m_Height = std::move(value);
    m_HeightIsSet = true;
}
std::string VideoMetadataWithItemType::getItemType() const
{
    return m_Item_type;
}

void VideoMetadataWithItemType::setItemType(std::string value)
{
    static const std::array<std::string, 1> allowedValues = {
        "video"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Item_type = std::move(value);
}
std::string VideoMetadataWithItemType::getVideoUrl() const
{
    return m_Video_url;
}

void VideoMetadataWithItemType::setVideoUrl(std::string value)
{
    
    m_Video_url = std::move(value);
    m_Video_urlIsSet = true;
}
std::string VideoMetadataWithItemType::getVideoUrlHls() const
{
    return m_Video_url_hls;
}

void VideoMetadataWithItemType::setVideoUrlHls(std::string value)
{
    
    m_Video_url_hls = std::move(value);
    m_Video_url_hlsIsSet = true;
}
int32_t VideoMetadataWithItemType::getWidth() const
{
    return m_Width;
}

void VideoMetadataWithItemType::setWidth(int32_t value)
{
    
    m_Width = std::move(value);
    m_WidthIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<VideoMetadataWithItemType>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<VideoMetadataWithItemType>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<VideoMetadataWithItemType>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<VideoMetadataWithItemType>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

