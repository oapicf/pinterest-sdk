/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "EventQualityScore.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

EventQualityScore::EventQualityScore(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string EventQualityScore::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void EventQualityScore::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value EventQualityScore::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void EventQualityScore::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for EventQualityScore");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object EventQualityScore::toJsonObject_internal() const
{
    boost::json::object object;
        object["ingestion_source"] = JsonValueConverter<std::shared_ptr<IngestionSourceOptions>>::toJsonValue(getIngestionSource());
        object["lookback_period"] = JsonValueConverter<std::shared_ptr<LookbackPeriodOptions>>::toJsonValue(getLookbackPeriod());
        object["overall_status"] = JsonValueConverter<std::shared_ptr<OverallStatusOptions>>::toJsonValue(getOverallStatus());
        object["quality_components"] = JsonValueConverter<std::shared_ptr<QualityComponents>>::toJsonValue(getQualityComponents());
        object["source_platform"] = JsonValueConverter<std::shared_ptr<SourcePlatformOptions>>::toJsonValue(getSourcePlatform());
    return object;
}

void EventQualityScore::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto Ingestion_sourceIt = object.find("ingestion_source");
        if (Ingestion_sourceIt != object.end()) {
            setIngestionSource(JsonValueConverter<std::shared_ptr<IngestionSourceOptions>>::fromJsonValue(Ingestion_sourceIt->value()));
        }
    }
    {
        const auto Lookback_periodIt = object.find("lookback_period");
        if (Lookback_periodIt != object.end()) {
            setLookbackPeriod(JsonValueConverter<std::shared_ptr<LookbackPeriodOptions>>::fromJsonValue(Lookback_periodIt->value()));
        }
    }
    {
        const auto Overall_statusIt = object.find("overall_status");
        if (Overall_statusIt != object.end()) {
            setOverallStatus(JsonValueConverter<std::shared_ptr<OverallStatusOptions>>::fromJsonValue(Overall_statusIt->value()));
        }
    }
    {
        const auto Quality_componentsIt = object.find("quality_components");
        if (Quality_componentsIt != object.end()) {
            setQualityComponents(JsonValueConverter<std::shared_ptr<QualityComponents>>::fromJsonValue(Quality_componentsIt->value()));
        }
    }
    {
        const auto Source_platformIt = object.find("source_platform");
        if (Source_platformIt != object.end()) {
            setSourcePlatform(JsonValueConverter<std::shared_ptr<SourcePlatformOptions>>::fromJsonValue(Source_platformIt->value()));
        }
    }
}

std::shared_ptr<IngestionSourceOptions> EventQualityScore::getIngestionSource() const
{
    return m_Ingestion_source;
}

void EventQualityScore::setIngestionSource(std::shared_ptr<IngestionSourceOptions> value)
{
    
    m_Ingestion_source = std::move(value);
}
std::shared_ptr<LookbackPeriodOptions> EventQualityScore::getLookbackPeriod() const
{
    return m_Lookback_period;
}

void EventQualityScore::setLookbackPeriod(std::shared_ptr<LookbackPeriodOptions> value)
{
    
    m_Lookback_period = std::move(value);
}
std::shared_ptr<OverallStatusOptions> EventQualityScore::getOverallStatus() const
{
    return m_Overall_status;
}

void EventQualityScore::setOverallStatus(std::shared_ptr<OverallStatusOptions> value)
{
    
    m_Overall_status = std::move(value);
}
std::shared_ptr<QualityComponents> EventQualityScore::getQualityComponents() const
{
    return m_Quality_components;
}

void EventQualityScore::setQualityComponents(std::shared_ptr<QualityComponents> value)
{
    
    m_Quality_components = std::move(value);
}
std::shared_ptr<SourcePlatformOptions> EventQualityScore::getSourcePlatform() const
{
    return m_Source_platform;
}

void EventQualityScore::setSourcePlatform(std::shared_ptr<SourcePlatformOptions> value)
{
    
    m_Source_platform = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<EventQualityScore>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<EventQualityScore>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<EventQualityScore>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<EventQualityScore>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

