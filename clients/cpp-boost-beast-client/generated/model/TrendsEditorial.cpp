/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "TrendsEditorial.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

TrendsEditorial::TrendsEditorial(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string TrendsEditorial::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void TrendsEditorial::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value TrendsEditorial::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void TrendsEditorial::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for TrendsEditorial");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object TrendsEditorial::toJsonObject_internal() const
{
    boost::json::object object;
        object["board_url"] = JsonValueConverter<std::string>::toJsonValue(getBoardUrl());
        object["description"] = JsonValueConverter<std::string>::toJsonValue(getDescription());
        object["interests"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getInterests());
        object["pins_url"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getPinsUrl());
        object["related_keywords"] = JsonValueConverter<std::vector<std::shared_ptr<KeywordInfo>>>::toJsonValue(getRelatedKeywords());
        object["title"] = JsonValueConverter<std::string>::toJsonValue(getTitle());
    return object;
}

void TrendsEditorial::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto Board_urlIt = object.find("board_url");
        if (Board_urlIt != object.end()) {
            setBoardUrl(JsonValueConverter<std::string>::fromJsonValue(Board_urlIt->value()));
        }
    }
    {
        const auto DescriptionIt = object.find("description");
        if (DescriptionIt != object.end()) {
            setDescription(JsonValueConverter<std::string>::fromJsonValue(DescriptionIt->value()));
        }
    }
    {
        const auto InterestsIt = object.find("interests");
        if (InterestsIt != object.end()) {
            setInterests(JsonValueConverter<std::vector<std::string>>::fromJsonValue(InterestsIt->value()));
        }
    }
    {
        const auto Pins_urlIt = object.find("pins_url");
        if (Pins_urlIt != object.end()) {
            setPinsUrl(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Pins_urlIt->value()));
        }
    }
    {
        const auto Related_keywordsIt = object.find("related_keywords");
        if (Related_keywordsIt != object.end()) {
            setRelatedKeywords(JsonValueConverter<std::vector<std::shared_ptr<KeywordInfo>>>::fromJsonValue(Related_keywordsIt->value()));
        }
    }
    {
        const auto TitleIt = object.find("title");
        if (TitleIt != object.end()) {
            setTitle(JsonValueConverter<std::string>::fromJsonValue(TitleIt->value()));
        }
    }
}

std::string TrendsEditorial::getBoardUrl() const
{
    return m_Board_url;
}

void TrendsEditorial::setBoardUrl(std::string value)
{
    
    m_Board_url = std::move(value);
}
std::string TrendsEditorial::getDescription() const
{
    return m_Description;
}

void TrendsEditorial::setDescription(std::string value)
{
    
    m_Description = std::move(value);
}
std::vector<std::string> TrendsEditorial::getInterests() const
{
    return m_Interests;
}

void TrendsEditorial::setInterests(std::vector<std::string> value)
{
    
    m_Interests = std::move(value);
}
std::vector<std::string> TrendsEditorial::getPinsUrl() const
{
    return m_Pins_url;
}

void TrendsEditorial::setPinsUrl(std::vector<std::string> value)
{
    
    m_Pins_url = std::move(value);
}
std::vector<std::shared_ptr<KeywordInfo>> TrendsEditorial::getRelatedKeywords() const
{
    return m_Related_keywords;
}

void TrendsEditorial::setRelatedKeywords(std::vector<std::shared_ptr<KeywordInfo>> value)
{
    
    m_Related_keywords = std::move(value);
}
std::string TrendsEditorial::getTitle() const
{
    return m_Title;
}

void TrendsEditorial::setTitle(std::string value)
{
    
    m_Title = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<TrendsEditorial>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<TrendsEditorial>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<TrendsEditorial>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<TrendsEditorial>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

