/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CampaignDeliveryEstimatesCampaign.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CampaignDeliveryEstimatesCampaign::CampaignDeliveryEstimatesCampaign(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CampaignDeliveryEstimatesCampaign::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CampaignDeliveryEstimatesCampaign::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CampaignDeliveryEstimatesCampaign::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CampaignDeliveryEstimatesCampaign::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CampaignDeliveryEstimatesCampaign");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CampaignDeliveryEstimatesCampaign::toJsonObject_internal() const
{
    boost::json::object object;
        object["ad_groups"] = JsonValueConverter<std::vector<std::shared_ptr<AdGroupDeliveryEstimates>>>::toJsonValue(getAdGroups());
        object["budget_duration_type"] = JsonValueConverter<std::shared_ptr<BudgetDurationType>>::toJsonValue(getBudgetDurationType());
        if (m_Daily_spend_capIsSet) {
            object["daily_spend_cap"] = JsonValueConverter<int32_t>::toJsonValue(getDailySpendCap());
        }
        if (m_End_dateIsSet) {
            object["end_date"] = JsonValueConverter<std::string>::toJsonValue(getEndDate());
        }
        if (m_Lifetime_spend_capIsSet) {
            object["lifetime_spend_cap"] = JsonValueConverter<int32_t>::toJsonValue(getLifetimeSpendCap());
        }
        object["objective_type"] = JsonValueConverter<std::shared_ptr<DeliveryEstimateObjectiveType>>::toJsonValue(getObjectiveType());
        object["start_date"] = JsonValueConverter<std::string>::toJsonValue(getStartDate());
    return object;
}

void CampaignDeliveryEstimatesCampaign::fromJsonObject_internal(boost::json::object const& object)
{
    m_Daily_spend_capIsSet = false;
    m_End_dateIsSet = false;
    m_Lifetime_spend_capIsSet = false;
    {
        const auto Ad_groupsIt = object.find("ad_groups");
        if (Ad_groupsIt != object.end()) {
            setAdGroups(JsonValueConverter<std::vector<std::shared_ptr<AdGroupDeliveryEstimates>>>::fromJsonValue(Ad_groupsIt->value()));
        }
    }
    {
        const auto Budget_duration_typeIt = object.find("budget_duration_type");
        if (Budget_duration_typeIt != object.end()) {
            setBudgetDurationType(JsonValueConverter<std::shared_ptr<BudgetDurationType>>::fromJsonValue(Budget_duration_typeIt->value()));
        }
    }
    {
        const auto Daily_spend_capIt = object.find("daily_spend_cap");
        if (Daily_spend_capIt != object.end()) {
            setDailySpendCap(JsonValueConverter<int32_t>::fromJsonValue(Daily_spend_capIt->value()));
        }
    }
    {
        const auto End_dateIt = object.find("end_date");
        if (End_dateIt != object.end()) {
            setEndDate(JsonValueConverter<std::string>::fromJsonValue(End_dateIt->value()));
        }
    }
    {
        const auto Lifetime_spend_capIt = object.find("lifetime_spend_cap");
        if (Lifetime_spend_capIt != object.end()) {
            setLifetimeSpendCap(JsonValueConverter<int32_t>::fromJsonValue(Lifetime_spend_capIt->value()));
        }
    }
    {
        const auto Objective_typeIt = object.find("objective_type");
        if (Objective_typeIt != object.end()) {
            setObjectiveType(JsonValueConverter<std::shared_ptr<DeliveryEstimateObjectiveType>>::fromJsonValue(Objective_typeIt->value()));
        }
    }
    {
        const auto Start_dateIt = object.find("start_date");
        if (Start_dateIt != object.end()) {
            setStartDate(JsonValueConverter<std::string>::fromJsonValue(Start_dateIt->value()));
        }
    }
}

std::vector<std::shared_ptr<AdGroupDeliveryEstimates>> CampaignDeliveryEstimatesCampaign::getAdGroups() const
{
    return m_Ad_groups;
}

void CampaignDeliveryEstimatesCampaign::setAdGroups(std::vector<std::shared_ptr<AdGroupDeliveryEstimates>> value)
{
    
    m_Ad_groups = std::move(value);
}
std::shared_ptr<BudgetDurationType> CampaignDeliveryEstimatesCampaign::getBudgetDurationType() const
{
    return m_Budget_duration_type;
}

void CampaignDeliveryEstimatesCampaign::setBudgetDurationType(std::shared_ptr<BudgetDurationType> value)
{
    
    m_Budget_duration_type = std::move(value);
}
int32_t CampaignDeliveryEstimatesCampaign::getDailySpendCap() const
{
    return m_Daily_spend_cap;
}

void CampaignDeliveryEstimatesCampaign::setDailySpendCap(int32_t value)
{
    
    m_Daily_spend_cap = std::move(value);
    m_Daily_spend_capIsSet = true;
}
std::string CampaignDeliveryEstimatesCampaign::getEndDate() const
{
    return m_End_date;
}

void CampaignDeliveryEstimatesCampaign::setEndDate(std::string value)
{
    
    m_End_date = std::move(value);
    m_End_dateIsSet = true;
}
int32_t CampaignDeliveryEstimatesCampaign::getLifetimeSpendCap() const
{
    return m_Lifetime_spend_cap;
}

void CampaignDeliveryEstimatesCampaign::setLifetimeSpendCap(int32_t value)
{
    
    m_Lifetime_spend_cap = std::move(value);
    m_Lifetime_spend_capIsSet = true;
}
std::shared_ptr<DeliveryEstimateObjectiveType> CampaignDeliveryEstimatesCampaign::getObjectiveType() const
{
    return m_Objective_type;
}

void CampaignDeliveryEstimatesCampaign::setObjectiveType(std::shared_ptr<DeliveryEstimateObjectiveType> value)
{
    
    m_Objective_type = std::move(value);
}
std::string CampaignDeliveryEstimatesCampaign::getStartDate() const
{
    return m_Start_date;
}

void CampaignDeliveryEstimatesCampaign::setStartDate(std::string value)
{
    
    m_Start_date = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CampaignDeliveryEstimatesCampaign>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CampaignDeliveryEstimatesCampaign>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CampaignDeliveryEstimatesCampaign>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CampaignDeliveryEstimatesCampaign>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

