/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "UserWebsiteVerification.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

UserWebsiteVerification::UserWebsiteVerification(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string UserWebsiteVerification::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void UserWebsiteVerification::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value UserWebsiteVerification::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void UserWebsiteVerification::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for UserWebsiteVerification");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object UserWebsiteVerification::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Dns_txt_recordIsSet) {
            object["dns_txt_record"] = JsonValueConverter<std::string>::toJsonValue(getDnsTxtRecord());
        }
        if (m_File_contentIsSet) {
            object["file_content"] = JsonValueConverter<std::string>::toJsonValue(getFileContent());
        }
        if (m_FilenameIsSet) {
            object["filename"] = JsonValueConverter<std::string>::toJsonValue(getFilename());
        }
        if (m_MetatagIsSet) {
            object["metatag"] = JsonValueConverter<std::string>::toJsonValue(getMetatag());
        }
        if (m_Verification_codeIsSet) {
            object["verification_code"] = JsonValueConverter<std::string>::toJsonValue(getVerificationCode());
        }
    return object;
}

void UserWebsiteVerification::fromJsonObject_internal(boost::json::object const& object)
{
    m_Dns_txt_recordIsSet = false;
    m_File_contentIsSet = false;
    m_FilenameIsSet = false;
    m_MetatagIsSet = false;
    m_Verification_codeIsSet = false;
    {
        const auto Dns_txt_recordIt = object.find("dns_txt_record");
        if (Dns_txt_recordIt != object.end()) {
            setDnsTxtRecord(JsonValueConverter<std::string>::fromJsonValue(Dns_txt_recordIt->value()));
        }
    }
    {
        const auto File_contentIt = object.find("file_content");
        if (File_contentIt != object.end()) {
            setFileContent(JsonValueConverter<std::string>::fromJsonValue(File_contentIt->value()));
        }
    }
    {
        const auto FilenameIt = object.find("filename");
        if (FilenameIt != object.end()) {
            setFilename(JsonValueConverter<std::string>::fromJsonValue(FilenameIt->value()));
        }
    }
    {
        const auto MetatagIt = object.find("metatag");
        if (MetatagIt != object.end()) {
            setMetatag(JsonValueConverter<std::string>::fromJsonValue(MetatagIt->value()));
        }
    }
    {
        const auto Verification_codeIt = object.find("verification_code");
        if (Verification_codeIt != object.end()) {
            setVerificationCode(JsonValueConverter<std::string>::fromJsonValue(Verification_codeIt->value()));
        }
    }
}

std::string UserWebsiteVerification::getDnsTxtRecord() const
{
    return m_Dns_txt_record;
}

void UserWebsiteVerification::setDnsTxtRecord(std::string value)
{
    
    m_Dns_txt_record = std::move(value);
    m_Dns_txt_recordIsSet = true;
}
std::string UserWebsiteVerification::getFileContent() const
{
    return m_File_content;
}

void UserWebsiteVerification::setFileContent(std::string value)
{
    
    m_File_content = std::move(value);
    m_File_contentIsSet = true;
}
std::string UserWebsiteVerification::getFilename() const
{
    return m_Filename;
}

void UserWebsiteVerification::setFilename(std::string value)
{
    
    m_Filename = std::move(value);
    m_FilenameIsSet = true;
}
std::string UserWebsiteVerification::getMetatag() const
{
    return m_Metatag;
}

void UserWebsiteVerification::setMetatag(std::string value)
{
    
    m_Metatag = std::move(value);
    m_MetatagIsSet = true;
}
std::string UserWebsiteVerification::getVerificationCode() const
{
    return m_Verification_code;
}

void UserWebsiteVerification::setVerificationCode(std::string value)
{
    
    m_Verification_code = std::move(value);
    m_Verification_codeIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<UserWebsiteVerification>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<UserWebsiteVerification>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<UserWebsiteVerification>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<UserWebsiteVerification>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

