/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BaseBusinessAssets.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BaseBusinessAssets::BaseBusinessAssets(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BaseBusinessAssets::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BaseBusinessAssets::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BaseBusinessAssets::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BaseBusinessAssets::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BaseBusinessAssets");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BaseBusinessAssets::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Asset_group_infoIsSet) {
            object["asset_group_info"] = JsonValueConverter<std::shared_ptr<AssetGroupBinding>>::toJsonValue(getAssetGroupInfo());
        }
        if (m_Asset_idIsSet) {
            object["asset_id"] = JsonValueConverter<std::string>::toJsonValue(getAssetId());
        }
        if (m_Asset_typeIsSet) {
            object["asset_type"] = JsonValueConverter<std::shared_ptr<AssetTypeResponse>>::toJsonValue(getAssetType());
        }
        if (m_PermissionsIsSet) {
            object["permissions"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getPermissions());
        }
    return object;
}

void BaseBusinessAssets::fromJsonObject_internal(boost::json::object const& object)
{
    m_Asset_group_infoIsSet = false;
    m_Asset_idIsSet = false;
    m_Asset_typeIsSet = false;
    m_PermissionsIsSet = false;
    {
        const auto Asset_group_infoIt = object.find("asset_group_info");
        if (Asset_group_infoIt != object.end()) {
            setAssetGroupInfo(JsonValueConverter<std::shared_ptr<AssetGroupBinding>>::fromJsonValue(Asset_group_infoIt->value()));
        }
    }
    {
        const auto Asset_idIt = object.find("asset_id");
        if (Asset_idIt != object.end()) {
            setAssetId(JsonValueConverter<std::string>::fromJsonValue(Asset_idIt->value()));
        }
    }
    {
        const auto Asset_typeIt = object.find("asset_type");
        if (Asset_typeIt != object.end()) {
            setAssetType(JsonValueConverter<std::shared_ptr<AssetTypeResponse>>::fromJsonValue(Asset_typeIt->value()));
        }
    }
    {
        const auto PermissionsIt = object.find("permissions");
        if (PermissionsIt != object.end()) {
            setPermissions(JsonValueConverter<std::vector<std::string>>::fromJsonValue(PermissionsIt->value()));
        }
    }
}

std::shared_ptr<AssetGroupBinding> BaseBusinessAssets::getAssetGroupInfo() const
{
    return m_Asset_group_info;
}

void BaseBusinessAssets::setAssetGroupInfo(std::shared_ptr<AssetGroupBinding> value)
{
    
    m_Asset_group_info = std::move(value);
    m_Asset_group_infoIsSet = true;
}
std::string BaseBusinessAssets::getAssetId() const
{
    return m_Asset_id;
}

void BaseBusinessAssets::setAssetId(std::string value)
{
    
    m_Asset_id = std::move(value);
    m_Asset_idIsSet = true;
}
std::shared_ptr<AssetTypeResponse> BaseBusinessAssets::getAssetType() const
{
    return m_Asset_type;
}

void BaseBusinessAssets::setAssetType(std::shared_ptr<AssetTypeResponse> value)
{
    
    m_Asset_type = std::move(value);
    m_Asset_typeIsSet = true;
}
std::vector<std::string> BaseBusinessAssets::getPermissions() const
{
    return m_Permissions;
}

void BaseBusinessAssets::setPermissions(std::vector<std::string> value)
{
    
    m_Permissions = std::move(value);
    m_PermissionsIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BaseBusinessAssets>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BaseBusinessAssets>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BaseBusinessAssets>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BaseBusinessAssets>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

