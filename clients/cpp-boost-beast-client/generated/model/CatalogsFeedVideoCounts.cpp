/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CatalogsFeedVideoCounts.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CatalogsFeedVideoCounts::CatalogsFeedVideoCounts(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CatalogsFeedVideoCounts::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CatalogsFeedVideoCounts::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CatalogsFeedVideoCounts::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CatalogsFeedVideoCounts::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CatalogsFeedVideoCounts");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CatalogsFeedVideoCounts::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Ingested_videosIsSet) {
            object["ingested_videos"] = JsonValueConverter<int32_t>::toJsonValue(getIngestedVideos());
        }
        if (m_Not_ingested_videosIsSet) {
            object["not_ingested_videos"] = JsonValueConverter<int32_t>::toJsonValue(getNotIngestedVideos());
        }
        if (m_Total_videosIsSet) {
            object["total_videos"] = JsonValueConverter<int32_t>::toJsonValue(getTotalVideos());
        }
    return object;
}

void CatalogsFeedVideoCounts::fromJsonObject_internal(boost::json::object const& object)
{
    m_Ingested_videosIsSet = false;
    m_Not_ingested_videosIsSet = false;
    m_Total_videosIsSet = false;
    {
        const auto Ingested_videosIt = object.find("ingested_videos");
        if (Ingested_videosIt != object.end()) {
            setIngestedVideos(JsonValueConverter<int32_t>::fromJsonValue(Ingested_videosIt->value()));
        }
    }
    {
        const auto Not_ingested_videosIt = object.find("not_ingested_videos");
        if (Not_ingested_videosIt != object.end()) {
            setNotIngestedVideos(JsonValueConverter<int32_t>::fromJsonValue(Not_ingested_videosIt->value()));
        }
    }
    {
        const auto Total_videosIt = object.find("total_videos");
        if (Total_videosIt != object.end()) {
            setTotalVideos(JsonValueConverter<int32_t>::fromJsonValue(Total_videosIt->value()));
        }
    }
}

int32_t CatalogsFeedVideoCounts::getIngestedVideos() const
{
    return m_Ingested_videos;
}

void CatalogsFeedVideoCounts::setIngestedVideos(int32_t value)
{
    
    m_Ingested_videos = std::move(value);
    m_Ingested_videosIsSet = true;
}
int32_t CatalogsFeedVideoCounts::getNotIngestedVideos() const
{
    return m_Not_ingested_videos;
}

void CatalogsFeedVideoCounts::setNotIngestedVideos(int32_t value)
{
    
    m_Not_ingested_videos = std::move(value);
    m_Not_ingested_videosIsSet = true;
}
int32_t CatalogsFeedVideoCounts::getTotalVideos() const
{
    return m_Total_videos;
}

void CatalogsFeedVideoCounts::setTotalVideos(int32_t value)
{
    
    m_Total_videos = std::move(value);
    m_Total_videosIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CatalogsFeedVideoCounts>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CatalogsFeedVideoCounts>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CatalogsFeedVideoCounts>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CatalogsFeedVideoCounts>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

