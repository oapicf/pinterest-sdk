/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "PinMediaSource.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

PinMediaSource::PinMediaSource(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string PinMediaSource::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void PinMediaSource::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value PinMediaSource::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void PinMediaSource::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for PinMediaSource");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object PinMediaSource::toJsonObject_internal() const
{
    boost::json::object object;
        object["content_type"] = JsonValueConverter<std::shared_ptr<ContentType>>::toJsonValue(getContentType());
        object["data"] = JsonValueConverter<std::string>::toJsonValue(getData());
        if (m_Is_standardIsSet) {
            object["is_standard"] = JsonValueConverter<bool>::toJsonValue(isIsStandard());
        }
        object["source_type"] = JsonValueConverter<std::string>::toJsonValue(getSourceType());
        object["url"] = JsonValueConverter<std::string>::toJsonValue(getUrl());
        if (m_Cover_image_content_typeIsSet) {
            object["cover_image_content_type"] = JsonValueConverter<std::shared_ptr<ContentType>>::toJsonValue(getCoverImageContentType());
        }
        if (m_Cover_image_dataIsSet) {
            object["cover_image_data"] = JsonValueConverter<std::string>::toJsonValue(getCoverImageData());
        }
        if (m_Cover_image_key_frame_timeIsSet) {
            object["cover_image_key_frame_time"] = JsonValueConverter<int32_t>::toJsonValue(getCoverImageKeyFrameTime());
        }
        if (m_Cover_image_urlIsSet) {
            object["cover_image_url"] = JsonValueConverter<std::string>::toJsonValue(getCoverImageUrl());
        }
        object["media_id"] = JsonValueConverter<std::string>::toJsonValue(getMediaId());
        if (m_IndexIsSet) {
            object["index"] = JsonValueConverter<int32_t>::toJsonValue(getIndex());
        }
        object["items"] = JsonValueConverter<std::vector<std::shared_ptr<PinMediaSourceImagesURLItem>>>::toJsonValue(getItems());
        if (m_Is_affiliate_linkIsSet) {
            object["is_affiliate_link"] = JsonValueConverter<bool>::toJsonValue(isIsAffiliateLink());
        }
    return object;
}

void PinMediaSource::fromJsonObject_internal(boost::json::object const& object)
{
    m_Is_standardIsSet = false;
    m_Cover_image_content_typeIsSet = false;
    m_Cover_image_dataIsSet = false;
    m_Cover_image_key_frame_timeIsSet = false;
    m_Cover_image_urlIsSet = false;
    m_IndexIsSet = false;
    m_Is_affiliate_linkIsSet = false;
    {
        const auto Content_typeIt = object.find("content_type");
        if (Content_typeIt != object.end()) {
            setContentType(JsonValueConverter<std::shared_ptr<ContentType>>::fromJsonValue(Content_typeIt->value()));
        }
    }
    {
        const auto DataIt = object.find("data");
        if (DataIt != object.end()) {
            setData(JsonValueConverter<std::string>::fromJsonValue(DataIt->value()));
        }
    }
    {
        const auto Is_standardIt = object.find("is_standard");
        if (Is_standardIt != object.end()) {
            setIsStandard(JsonValueConverter<bool>::fromJsonValue(Is_standardIt->value()));
        }
    }
    {
        const auto Source_typeIt = object.find("source_type");
        if (Source_typeIt != object.end()) {
            setSourceType(JsonValueConverter<std::string>::fromJsonValue(Source_typeIt->value()));
        }
    }
    {
        const auto UrlIt = object.find("url");
        if (UrlIt != object.end()) {
            setUrl(JsonValueConverter<std::string>::fromJsonValue(UrlIt->value()));
        }
    }
    {
        const auto Cover_image_content_typeIt = object.find("cover_image_content_type");
        if (Cover_image_content_typeIt != object.end()) {
            setCoverImageContentType(JsonValueConverter<std::shared_ptr<ContentType>>::fromJsonValue(Cover_image_content_typeIt->value()));
        }
    }
    {
        const auto Cover_image_dataIt = object.find("cover_image_data");
        if (Cover_image_dataIt != object.end()) {
            setCoverImageData(JsonValueConverter<std::string>::fromJsonValue(Cover_image_dataIt->value()));
        }
    }
    {
        const auto Cover_image_key_frame_timeIt = object.find("cover_image_key_frame_time");
        if (Cover_image_key_frame_timeIt != object.end()) {
            setCoverImageKeyFrameTime(JsonValueConverter<int32_t>::fromJsonValue(Cover_image_key_frame_timeIt->value()));
        }
    }
    {
        const auto Cover_image_urlIt = object.find("cover_image_url");
        if (Cover_image_urlIt != object.end()) {
            setCoverImageUrl(JsonValueConverter<std::string>::fromJsonValue(Cover_image_urlIt->value()));
        }
    }
    {
        const auto Media_idIt = object.find("media_id");
        if (Media_idIt != object.end()) {
            setMediaId(JsonValueConverter<std::string>::fromJsonValue(Media_idIt->value()));
        }
    }
    {
        const auto IndexIt = object.find("index");
        if (IndexIt != object.end()) {
            setIndex(JsonValueConverter<int32_t>::fromJsonValue(IndexIt->value()));
        }
    }
    {
        const auto ItemsIt = object.find("items");
        if (ItemsIt != object.end()) {
            setItems(JsonValueConverter<std::vector<std::shared_ptr<PinMediaSourceImagesURLItem>>>::fromJsonValue(ItemsIt->value()));
        }
    }
    {
        const auto Is_affiliate_linkIt = object.find("is_affiliate_link");
        if (Is_affiliate_linkIt != object.end()) {
            setIsAffiliateLink(JsonValueConverter<bool>::fromJsonValue(Is_affiliate_linkIt->value()));
        }
    }
}

std::shared_ptr<ContentType> PinMediaSource::getContentType() const
{
    return m_Content_type;
}

void PinMediaSource::setContentType(std::shared_ptr<ContentType> value)
{
    
    m_Content_type = std::move(value);
}
std::string PinMediaSource::getData() const
{
    return m_Data;
}

void PinMediaSource::setData(std::string value)
{
    
    m_Data = std::move(value);
}
bool PinMediaSource::isIsStandard() const
{
    return m_Is_standard;
}

void PinMediaSource::setIsStandard(bool value)
{
    
    m_Is_standard = std::move(value);
    m_Is_standardIsSet = true;
}
std::string PinMediaSource::getSourceType() const
{
    return m_Source_type;
}

void PinMediaSource::setSourceType(std::string value)
{
    static const std::array<std::string, 1> allowedValues = {
        "pin_url"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Source_type = std::move(value);
}
std::string PinMediaSource::getUrl() const
{
    return m_Url;
}

void PinMediaSource::setUrl(std::string value)
{
    
    m_Url = std::move(value);
}
std::shared_ptr<ContentType> PinMediaSource::getCoverImageContentType() const
{
    return m_Cover_image_content_type;
}

void PinMediaSource::setCoverImageContentType(std::shared_ptr<ContentType> value)
{
    
    m_Cover_image_content_type = std::move(value);
    m_Cover_image_content_typeIsSet = true;
}
std::string PinMediaSource::getCoverImageData() const
{
    return m_Cover_image_data;
}

void PinMediaSource::setCoverImageData(std::string value)
{
    
    m_Cover_image_data = std::move(value);
    m_Cover_image_dataIsSet = true;
}
int32_t PinMediaSource::getCoverImageKeyFrameTime() const
{
    return m_Cover_image_key_frame_time;
}

void PinMediaSource::setCoverImageKeyFrameTime(int32_t value)
{
    
    m_Cover_image_key_frame_time = std::move(value);
    m_Cover_image_key_frame_timeIsSet = true;
}
std::string PinMediaSource::getCoverImageUrl() const
{
    return m_Cover_image_url;
}

void PinMediaSource::setCoverImageUrl(std::string value)
{
    
    m_Cover_image_url = std::move(value);
    m_Cover_image_urlIsSet = true;
}
std::string PinMediaSource::getMediaId() const
{
    return m_Media_id;
}

void PinMediaSource::setMediaId(std::string value)
{
    
    m_Media_id = std::move(value);
}
int32_t PinMediaSource::getIndex() const
{
    return m_Index;
}

void PinMediaSource::setIndex(int32_t value)
{
    
    m_Index = std::move(value);
    m_IndexIsSet = true;
}
std::vector<std::shared_ptr<PinMediaSourceImagesURLItem>> PinMediaSource::getItems() const
{
    return m_Items;
}

void PinMediaSource::setItems(std::vector<std::shared_ptr<PinMediaSourceImagesURLItem>> value)
{
    
    m_Items = std::move(value);
}
bool PinMediaSource::isIsAffiliateLink() const
{
    return m_Is_affiliate_link;
}

void PinMediaSource::setIsAffiliateLink(bool value)
{
    
    m_Is_affiliate_link = std::move(value);
    m_Is_affiliate_linkIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<PinMediaSource>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<PinMediaSource>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<PinMediaSource>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<PinMediaSource>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

