/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CustomerListRecordRow.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CustomerListRecordRow::CustomerListRecordRow(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CustomerListRecordRow::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CustomerListRecordRow::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CustomerListRecordRow::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CustomerListRecordRow::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CustomerListRecordRow");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CustomerListRecordRow::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_EmailIsSet) {
            object["email"] = JsonValueConverter<std::string>::toJsonValue(getEmail());
        }
        if (m_External_idIsSet) {
            object["external_id"] = JsonValueConverter<std::string>::toJsonValue(getExternalId());
        }
        if (m_Hashed_phone_numberIsSet) {
            object["hashed_phone_number"] = JsonValueConverter<std::string>::toJsonValue(getHashedPhoneNumber());
        }
        if (m_Hashed_pinner_idIsSet) {
            object["hashed_pinner_id"] = JsonValueConverter<std::string>::toJsonValue(getHashedPinnerId());
        }
        if (m_Ip_addressIsSet) {
            object["ip_address"] = JsonValueConverter<std::string>::toJsonValue(getIpAddress());
        }
        if (m_Liveramp_envelopeIsSet) {
            object["liveramp_envelope"] = JsonValueConverter<std::string>::toJsonValue(getLiverampEnvelope());
        }
        if (m_MaidIsSet) {
            object["maid"] = JsonValueConverter<std::string>::toJsonValue(getMaid());
        }
        if (m_User_agentIsSet) {
            object["user_agent"] = JsonValueConverter<std::string>::toJsonValue(getUserAgent());
        }
    return object;
}

void CustomerListRecordRow::fromJsonObject_internal(boost::json::object const& object)
{
    m_EmailIsSet = false;
    m_External_idIsSet = false;
    m_Hashed_phone_numberIsSet = false;
    m_Hashed_pinner_idIsSet = false;
    m_Ip_addressIsSet = false;
    m_Liveramp_envelopeIsSet = false;
    m_MaidIsSet = false;
    m_User_agentIsSet = false;
    {
        const auto EmailIt = object.find("email");
        if (EmailIt != object.end()) {
            setEmail(JsonValueConverter<std::string>::fromJsonValue(EmailIt->value()));
        }
    }
    {
        const auto External_idIt = object.find("external_id");
        if (External_idIt != object.end()) {
            setExternalId(JsonValueConverter<std::string>::fromJsonValue(External_idIt->value()));
        }
    }
    {
        const auto Hashed_phone_numberIt = object.find("hashed_phone_number");
        if (Hashed_phone_numberIt != object.end()) {
            setHashedPhoneNumber(JsonValueConverter<std::string>::fromJsonValue(Hashed_phone_numberIt->value()));
        }
    }
    {
        const auto Hashed_pinner_idIt = object.find("hashed_pinner_id");
        if (Hashed_pinner_idIt != object.end()) {
            setHashedPinnerId(JsonValueConverter<std::string>::fromJsonValue(Hashed_pinner_idIt->value()));
        }
    }
    {
        const auto Ip_addressIt = object.find("ip_address");
        if (Ip_addressIt != object.end()) {
            setIpAddress(JsonValueConverter<std::string>::fromJsonValue(Ip_addressIt->value()));
        }
    }
    {
        const auto Liveramp_envelopeIt = object.find("liveramp_envelope");
        if (Liveramp_envelopeIt != object.end()) {
            setLiverampEnvelope(JsonValueConverter<std::string>::fromJsonValue(Liveramp_envelopeIt->value()));
        }
    }
    {
        const auto MaidIt = object.find("maid");
        if (MaidIt != object.end()) {
            setMaid(JsonValueConverter<std::string>::fromJsonValue(MaidIt->value()));
        }
    }
    {
        const auto User_agentIt = object.find("user_agent");
        if (User_agentIt != object.end()) {
            setUserAgent(JsonValueConverter<std::string>::fromJsonValue(User_agentIt->value()));
        }
    }
}

std::string CustomerListRecordRow::getEmail() const
{
    return m_Email;
}

void CustomerListRecordRow::setEmail(std::string value)
{
    
    m_Email = std::move(value);
    m_EmailIsSet = true;
}
std::string CustomerListRecordRow::getExternalId() const
{
    return m_External_id;
}

void CustomerListRecordRow::setExternalId(std::string value)
{
    
    m_External_id = std::move(value);
    m_External_idIsSet = true;
}
std::string CustomerListRecordRow::getHashedPhoneNumber() const
{
    return m_Hashed_phone_number;
}

void CustomerListRecordRow::setHashedPhoneNumber(std::string value)
{
    
    m_Hashed_phone_number = std::move(value);
    m_Hashed_phone_numberIsSet = true;
}
std::string CustomerListRecordRow::getHashedPinnerId() const
{
    return m_Hashed_pinner_id;
}

void CustomerListRecordRow::setHashedPinnerId(std::string value)
{
    
    m_Hashed_pinner_id = std::move(value);
    m_Hashed_pinner_idIsSet = true;
}
std::string CustomerListRecordRow::getIpAddress() const
{
    return m_Ip_address;
}

void CustomerListRecordRow::setIpAddress(std::string value)
{
    
    m_Ip_address = std::move(value);
    m_Ip_addressIsSet = true;
}
std::string CustomerListRecordRow::getLiverampEnvelope() const
{
    return m_Liveramp_envelope;
}

void CustomerListRecordRow::setLiverampEnvelope(std::string value)
{
    
    m_Liveramp_envelope = std::move(value);
    m_Liveramp_envelopeIsSet = true;
}
std::string CustomerListRecordRow::getMaid() const
{
    return m_Maid;
}

void CustomerListRecordRow::setMaid(std::string value)
{
    
    m_Maid = std::move(value);
    m_MaidIsSet = true;
}
std::string CustomerListRecordRow::getUserAgent() const
{
    return m_User_agent;
}

void CustomerListRecordRow::setUserAgent(std::string value)
{
    
    m_User_agent = std::move(value);
    m_User_agentIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CustomerListRecordRow>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CustomerListRecordRow>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CustomerListRecordRow>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CustomerListRecordRow>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

