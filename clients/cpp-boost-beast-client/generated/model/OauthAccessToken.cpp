/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "OauthAccessToken.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

OauthAccessToken::OauthAccessToken(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string OauthAccessToken::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void OauthAccessToken::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value OauthAccessToken::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void OauthAccessToken::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for OauthAccessToken");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object OauthAccessToken::toJsonObject_internal() const
{
    boost::json::object object;
        object["access_token"] = JsonValueConverter<std::string>::toJsonValue(getAccessToken());
        object["expires_in"] = JsonValueConverter<int32_t>::toJsonValue(getExpiresIn());
        if (m_Refresh_tokenIsSet) {
            object["refresh_token"] = JsonValueConverter<std::string>::toJsonValue(getRefreshToken());
        }
        if (m_Refresh_token_expires_atIsSet) {
            object["refresh_token_expires_at"] = JsonValueConverter<int32_t>::toJsonValue(getRefreshTokenExpiresAt());
        }
        if (m_Refresh_token_expires_inIsSet) {
            object["refresh_token_expires_in"] = JsonValueConverter<int32_t>::toJsonValue(getRefreshTokenExpiresIn());
        }
        if (m_Response_typeIsSet) {
            object["response_type"] = JsonValueConverter<std::shared_ptr<TokenGrantType>>::toJsonValue(getResponseType());
        }
        if (m_ScopeIsSet) {
            object["scope"] = JsonValueConverter<std::string>::toJsonValue(getScope());
        }
        object["token_type"] = JsonValueConverter<std::string>::toJsonValue(getTokenType());
    return object;
}

void OauthAccessToken::fromJsonObject_internal(boost::json::object const& object)
{
    m_Refresh_tokenIsSet = false;
    m_Refresh_token_expires_atIsSet = false;
    m_Refresh_token_expires_inIsSet = false;
    m_Response_typeIsSet = false;
    m_ScopeIsSet = false;
    {
        const auto Access_tokenIt = object.find("access_token");
        if (Access_tokenIt != object.end()) {
            setAccessToken(JsonValueConverter<std::string>::fromJsonValue(Access_tokenIt->value()));
        }
    }
    {
        const auto Expires_inIt = object.find("expires_in");
        if (Expires_inIt != object.end()) {
            setExpiresIn(JsonValueConverter<int32_t>::fromJsonValue(Expires_inIt->value()));
        }
    }
    {
        const auto Refresh_tokenIt = object.find("refresh_token");
        if (Refresh_tokenIt != object.end()) {
            setRefreshToken(JsonValueConverter<std::string>::fromJsonValue(Refresh_tokenIt->value()));
        }
    }
    {
        const auto Refresh_token_expires_atIt = object.find("refresh_token_expires_at");
        if (Refresh_token_expires_atIt != object.end()) {
            setRefreshTokenExpiresAt(JsonValueConverter<int32_t>::fromJsonValue(Refresh_token_expires_atIt->value()));
        }
    }
    {
        const auto Refresh_token_expires_inIt = object.find("refresh_token_expires_in");
        if (Refresh_token_expires_inIt != object.end()) {
            setRefreshTokenExpiresIn(JsonValueConverter<int32_t>::fromJsonValue(Refresh_token_expires_inIt->value()));
        }
    }
    {
        const auto Response_typeIt = object.find("response_type");
        if (Response_typeIt != object.end()) {
            setResponseType(JsonValueConverter<std::shared_ptr<TokenGrantType>>::fromJsonValue(Response_typeIt->value()));
        }
    }
    {
        const auto ScopeIt = object.find("scope");
        if (ScopeIt != object.end()) {
            setScope(JsonValueConverter<std::string>::fromJsonValue(ScopeIt->value()));
        }
    }
    {
        const auto Token_typeIt = object.find("token_type");
        if (Token_typeIt != object.end()) {
            setTokenType(JsonValueConverter<std::string>::fromJsonValue(Token_typeIt->value()));
        }
    }
}

std::string OauthAccessToken::getAccessToken() const
{
    return m_Access_token;
}

void OauthAccessToken::setAccessToken(std::string value)
{
    
    m_Access_token = std::move(value);
}
int32_t OauthAccessToken::getExpiresIn() const
{
    return m_Expires_in;
}

void OauthAccessToken::setExpiresIn(int32_t value)
{
    
    m_Expires_in = std::move(value);
}
std::string OauthAccessToken::getRefreshToken() const
{
    return m_Refresh_token;
}

void OauthAccessToken::setRefreshToken(std::string value)
{
    
    m_Refresh_token = std::move(value);
    m_Refresh_tokenIsSet = true;
}
int32_t OauthAccessToken::getRefreshTokenExpiresAt() const
{
    return m_Refresh_token_expires_at;
}

void OauthAccessToken::setRefreshTokenExpiresAt(int32_t value)
{
    
    m_Refresh_token_expires_at = std::move(value);
    m_Refresh_token_expires_atIsSet = true;
}
int32_t OauthAccessToken::getRefreshTokenExpiresIn() const
{
    return m_Refresh_token_expires_in;
}

void OauthAccessToken::setRefreshTokenExpiresIn(int32_t value)
{
    
    m_Refresh_token_expires_in = std::move(value);
    m_Refresh_token_expires_inIsSet = true;
}
std::shared_ptr<TokenGrantType> OauthAccessToken::getResponseType() const
{
    return m_Response_type;
}

void OauthAccessToken::setResponseType(std::shared_ptr<TokenGrantType> value)
{
    
    m_Response_type = std::move(value);
    m_Response_typeIsSet = true;
}
std::string OauthAccessToken::getScope() const
{
    return m_Scope;
}

void OauthAccessToken::setScope(std::string value)
{
    
    m_Scope = std::move(value);
    m_ScopeIsSet = true;
}
std::string OauthAccessToken::getTokenType() const
{
    return m_Token_type;
}

void OauthAccessToken::setTokenType(std::string value)
{
    
    m_Token_type = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<OauthAccessToken>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<OauthAccessToken>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<OauthAccessToken>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<OauthAccessToken>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

