/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CampaignBidOptions.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

CampaignBidOptions::CampaignBidOptions(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string CampaignBidOptions::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void CampaignBidOptions::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value CampaignBidOptions::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void CampaignBidOptions::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for CampaignBidOptions");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object CampaignBidOptions::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Age_bucket_multipliersIsSet) {
            object["age_bucket_multipliers"] = JsonValueConverter<std::shared_ptr<AgeBucketMultipliers>>::toJsonValue(getAgeBucketMultipliers());
        }
        if (m_App_type_multipliersIsSet) {
            object["app_type_multipliers"] = JsonValueConverter<std::shared_ptr<AppTypeMultipliers>>::toJsonValue(getAppTypeMultipliers());
        }
        if (m_Audience_multipliersIsSet) {
            object["audience_multipliers"] = JsonValueConverter<std::shared_ptr<CampaignAudienceMultipliers>>::toJsonValue(getAudienceMultipliers());
        }
        if (m_Freq_bid_multiplier_time_windowIsSet) {
            object["freq_bid_multiplier_time_window"] = JsonValueConverter<std::shared_ptr<FreqBidMultiplierTimeWindow>>::toJsonValue(getFreqBidMultiplierTimeWindow());
        }
        if (m_Frequency_multipliersIsSet) {
            object["frequency_multipliers"] = JsonValueConverter<std::shared_ptr<FrequencyMultipliers>>::toJsonValue(getFrequencyMultipliers());
        }
        if (m_Gender_multipliersIsSet) {
            object["gender_multipliers"] = JsonValueConverter<std::shared_ptr<GenderMultipliers>>::toJsonValue(getGenderMultipliers());
        }
        if (m_Placement_multipliersIsSet) {
            object["placement_multipliers"] = JsonValueConverter<std::shared_ptr<PlacementMultipliers>>::toJsonValue(getPlacementMultipliers());
        }
    return object;
}

void CampaignBidOptions::fromJsonObject_internal(boost::json::object const& object)
{
    m_Age_bucket_multipliersIsSet = false;
    m_App_type_multipliersIsSet = false;
    m_Audience_multipliersIsSet = false;
    m_Freq_bid_multiplier_time_windowIsSet = false;
    m_Frequency_multipliersIsSet = false;
    m_Gender_multipliersIsSet = false;
    m_Placement_multipliersIsSet = false;
    {
        const auto Age_bucket_multipliersIt = object.find("age_bucket_multipliers");
        if (Age_bucket_multipliersIt != object.end()) {
            setAgeBucketMultipliers(JsonValueConverter<std::shared_ptr<AgeBucketMultipliers>>::fromJsonValue(Age_bucket_multipliersIt->value()));
        }
    }
    {
        const auto App_type_multipliersIt = object.find("app_type_multipliers");
        if (App_type_multipliersIt != object.end()) {
            setAppTypeMultipliers(JsonValueConverter<std::shared_ptr<AppTypeMultipliers>>::fromJsonValue(App_type_multipliersIt->value()));
        }
    }
    {
        const auto Audience_multipliersIt = object.find("audience_multipliers");
        if (Audience_multipliersIt != object.end()) {
            setAudienceMultipliers(JsonValueConverter<std::shared_ptr<CampaignAudienceMultipliers>>::fromJsonValue(Audience_multipliersIt->value()));
        }
    }
    {
        const auto Freq_bid_multiplier_time_windowIt = object.find("freq_bid_multiplier_time_window");
        if (Freq_bid_multiplier_time_windowIt != object.end()) {
            setFreqBidMultiplierTimeWindow(JsonValueConverter<std::shared_ptr<FreqBidMultiplierTimeWindow>>::fromJsonValue(Freq_bid_multiplier_time_windowIt->value()));
        }
    }
    {
        const auto Frequency_multipliersIt = object.find("frequency_multipliers");
        if (Frequency_multipliersIt != object.end()) {
            setFrequencyMultipliers(JsonValueConverter<std::shared_ptr<FrequencyMultipliers>>::fromJsonValue(Frequency_multipliersIt->value()));
        }
    }
    {
        const auto Gender_multipliersIt = object.find("gender_multipliers");
        if (Gender_multipliersIt != object.end()) {
            setGenderMultipliers(JsonValueConverter<std::shared_ptr<GenderMultipliers>>::fromJsonValue(Gender_multipliersIt->value()));
        }
    }
    {
        const auto Placement_multipliersIt = object.find("placement_multipliers");
        if (Placement_multipliersIt != object.end()) {
            setPlacementMultipliers(JsonValueConverter<std::shared_ptr<PlacementMultipliers>>::fromJsonValue(Placement_multipliersIt->value()));
        }
    }
}

std::shared_ptr<AgeBucketMultipliers> CampaignBidOptions::getAgeBucketMultipliers() const
{
    return m_Age_bucket_multipliers;
}

void CampaignBidOptions::setAgeBucketMultipliers(std::shared_ptr<AgeBucketMultipliers> value)
{
    
    m_Age_bucket_multipliers = std::move(value);
    m_Age_bucket_multipliersIsSet = true;
}
std::shared_ptr<AppTypeMultipliers> CampaignBidOptions::getAppTypeMultipliers() const
{
    return m_App_type_multipliers;
}

void CampaignBidOptions::setAppTypeMultipliers(std::shared_ptr<AppTypeMultipliers> value)
{
    
    m_App_type_multipliers = std::move(value);
    m_App_type_multipliersIsSet = true;
}
std::shared_ptr<CampaignAudienceMultipliers> CampaignBidOptions::getAudienceMultipliers() const
{
    return m_Audience_multipliers;
}

void CampaignBidOptions::setAudienceMultipliers(std::shared_ptr<CampaignAudienceMultipliers> value)
{
    
    m_Audience_multipliers = std::move(value);
    m_Audience_multipliersIsSet = true;
}
std::shared_ptr<FreqBidMultiplierTimeWindow> CampaignBidOptions::getFreqBidMultiplierTimeWindow() const
{
    return m_Freq_bid_multiplier_time_window;
}

void CampaignBidOptions::setFreqBidMultiplierTimeWindow(std::shared_ptr<FreqBidMultiplierTimeWindow> value)
{
    
    m_Freq_bid_multiplier_time_window = std::move(value);
    m_Freq_bid_multiplier_time_windowIsSet = true;
}
std::shared_ptr<FrequencyMultipliers> CampaignBidOptions::getFrequencyMultipliers() const
{
    return m_Frequency_multipliers;
}

void CampaignBidOptions::setFrequencyMultipliers(std::shared_ptr<FrequencyMultipliers> value)
{
    
    m_Frequency_multipliers = std::move(value);
    m_Frequency_multipliersIsSet = true;
}
std::shared_ptr<GenderMultipliers> CampaignBidOptions::getGenderMultipliers() const
{
    return m_Gender_multipliers;
}

void CampaignBidOptions::setGenderMultipliers(std::shared_ptr<GenderMultipliers> value)
{
    
    m_Gender_multipliers = std::move(value);
    m_Gender_multipliersIsSet = true;
}
std::shared_ptr<PlacementMultipliers> CampaignBidOptions::getPlacementMultipliers() const
{
    return m_Placement_multipliers;
}

void CampaignBidOptions::setPlacementMultipliers(std::shared_ptr<PlacementMultipliers> value)
{
    
    m_Placement_multipliers = std::move(value);
    m_Placement_multipliersIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<CampaignBidOptions>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<CampaignBidOptions>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<CampaignBidOptions>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<CampaignBidOptions>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

