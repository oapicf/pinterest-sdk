/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BrandAccountUpdate.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BrandAccountUpdate::BrandAccountUpdate(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BrandAccountUpdate::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BrandAccountUpdate::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BrandAccountUpdate::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BrandAccountUpdate::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BrandAccountUpdate");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BrandAccountUpdate::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_AboutIsSet) {
            object["about"] = JsonValueConverter<std::string>::toJsonValue(getAbout());
        }
        if (m_CountryIsSet) {
            object["country"] = JsonValueConverter<std::shared_ptr<Country>>::toJsonValue(getCountry());
        }
        if (m_NameIsSet) {
            object["name"] = JsonValueConverter<std::string>::toJsonValue(getName());
        }
        if (m_Profile_imageIsSet) {
            object["profile_image"] = JsonValueConverter<std::shared_ptr<BrandAccountProfileImageUpdate>>::toJsonValue(getProfileImage());
        }
        if (m_UsernameIsSet) {
            object["username"] = JsonValueConverter<std::string>::toJsonValue(getUsername());
        }
        if (m_WebsiteIsSet) {
            object["website"] = JsonValueConverter<std::string>::toJsonValue(getWebsite());
        }
    return object;
}

void BrandAccountUpdate::fromJsonObject_internal(boost::json::object const& object)
{
    m_AboutIsSet = false;
    m_CountryIsSet = false;
    m_NameIsSet = false;
    m_Profile_imageIsSet = false;
    m_UsernameIsSet = false;
    m_WebsiteIsSet = false;
    {
        const auto AboutIt = object.find("about");
        if (AboutIt != object.end()) {
            setAbout(JsonValueConverter<std::string>::fromJsonValue(AboutIt->value()));
        }
    }
    {
        const auto CountryIt = object.find("country");
        if (CountryIt != object.end()) {
            setCountry(JsonValueConverter<std::shared_ptr<Country>>::fromJsonValue(CountryIt->value()));
        }
    }
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            setName(JsonValueConverter<std::string>::fromJsonValue(NameIt->value()));
        }
    }
    {
        const auto Profile_imageIt = object.find("profile_image");
        if (Profile_imageIt != object.end()) {
            setProfileImage(JsonValueConverter<std::shared_ptr<BrandAccountProfileImageUpdate>>::fromJsonValue(Profile_imageIt->value()));
        }
    }
    {
        const auto UsernameIt = object.find("username");
        if (UsernameIt != object.end()) {
            setUsername(JsonValueConverter<std::string>::fromJsonValue(UsernameIt->value()));
        }
    }
    {
        const auto WebsiteIt = object.find("website");
        if (WebsiteIt != object.end()) {
            setWebsite(JsonValueConverter<std::string>::fromJsonValue(WebsiteIt->value()));
        }
    }
}

std::string BrandAccountUpdate::getAbout() const
{
    return m_About;
}

void BrandAccountUpdate::setAbout(std::string value)
{
    
    m_About = std::move(value);
    m_AboutIsSet = true;
}
std::shared_ptr<Country> BrandAccountUpdate::getCountry() const
{
    return m_Country;
}

void BrandAccountUpdate::setCountry(std::shared_ptr<Country> value)
{
    
    m_Country = std::move(value);
    m_CountryIsSet = true;
}
std::string BrandAccountUpdate::getName() const
{
    return m_Name;
}

void BrandAccountUpdate::setName(std::string value)
{
    
    m_Name = std::move(value);
    m_NameIsSet = true;
}
std::shared_ptr<BrandAccountProfileImageUpdate> BrandAccountUpdate::getProfileImage() const
{
    return m_Profile_image;
}

void BrandAccountUpdate::setProfileImage(std::shared_ptr<BrandAccountProfileImageUpdate> value)
{
    
    m_Profile_image = std::move(value);
    m_Profile_imageIsSet = true;
}
std::string BrandAccountUpdate::getUsername() const
{
    return m_Username;
}

void BrandAccountUpdate::setUsername(std::string value)
{
    
    m_Username = std::move(value);
    m_UsernameIsSet = true;
}
std::string BrandAccountUpdate::getWebsite() const
{
    return m_Website;
}

void BrandAccountUpdate::setWebsite(std::string value)
{
    
    m_Website = std::move(value);
    m_WebsiteIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BrandAccountUpdate>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BrandAccountUpdate>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BrandAccountUpdate>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BrandAccountUpdate>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

