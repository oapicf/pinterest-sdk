/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BidOptionsAppTypeMultipliers.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BidOptionsAppTypeMultipliers::BidOptionsAppTypeMultipliers(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BidOptionsAppTypeMultipliers::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BidOptionsAppTypeMultipliers::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BidOptionsAppTypeMultipliers::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BidOptionsAppTypeMultipliers::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BidOptionsAppTypeMultipliers");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BidOptionsAppTypeMultipliers::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Android_mobileIsSet) {
            object["android_mobile"] = JsonValueConverter<double>::toJsonValue(getAndroidMobile());
        }
        if (m_Android_tabletIsSet) {
            object["android_tablet"] = JsonValueConverter<double>::toJsonValue(getAndroidTablet());
        }
        if (m_IpadIsSet) {
            object["ipad"] = JsonValueConverter<double>::toJsonValue(getIpad());
        }
        if (m_IphoneIsSet) {
            object["iphone"] = JsonValueConverter<double>::toJsonValue(getIphone());
        }
        if (m_WebIsSet) {
            object["web"] = JsonValueConverter<double>::toJsonValue(getWeb());
        }
        if (m_Web_mobileIsSet) {
            object["web_mobile"] = JsonValueConverter<double>::toJsonValue(getWebMobile());
        }
    return object;
}

void BidOptionsAppTypeMultipliers::fromJsonObject_internal(boost::json::object const& object)
{
    m_Android_mobileIsSet = false;
    m_Android_tabletIsSet = false;
    m_IpadIsSet = false;
    m_IphoneIsSet = false;
    m_WebIsSet = false;
    m_Web_mobileIsSet = false;
    {
        const auto Android_mobileIt = object.find("android_mobile");
        if (Android_mobileIt != object.end()) {
            setAndroidMobile(JsonValueConverter<double>::fromJsonValue(Android_mobileIt->value()));
        }
    }
    {
        const auto Android_tabletIt = object.find("android_tablet");
        if (Android_tabletIt != object.end()) {
            setAndroidTablet(JsonValueConverter<double>::fromJsonValue(Android_tabletIt->value()));
        }
    }
    {
        const auto IpadIt = object.find("ipad");
        if (IpadIt != object.end()) {
            setIpad(JsonValueConverter<double>::fromJsonValue(IpadIt->value()));
        }
    }
    {
        const auto IphoneIt = object.find("iphone");
        if (IphoneIt != object.end()) {
            setIphone(JsonValueConverter<double>::fromJsonValue(IphoneIt->value()));
        }
    }
    {
        const auto WebIt = object.find("web");
        if (WebIt != object.end()) {
            setWeb(JsonValueConverter<double>::fromJsonValue(WebIt->value()));
        }
    }
    {
        const auto Web_mobileIt = object.find("web_mobile");
        if (Web_mobileIt != object.end()) {
            setWebMobile(JsonValueConverter<double>::fromJsonValue(Web_mobileIt->value()));
        }
    }
}

double BidOptionsAppTypeMultipliers::getAndroidMobile() const
{
    return m_Android_mobile;
}

void BidOptionsAppTypeMultipliers::setAndroidMobile(double value)
{
    
    m_Android_mobile = std::move(value);
    m_Android_mobileIsSet = true;
}
double BidOptionsAppTypeMultipliers::getAndroidTablet() const
{
    return m_Android_tablet;
}

void BidOptionsAppTypeMultipliers::setAndroidTablet(double value)
{
    
    m_Android_tablet = std::move(value);
    m_Android_tabletIsSet = true;
}
double BidOptionsAppTypeMultipliers::getIpad() const
{
    return m_Ipad;
}

void BidOptionsAppTypeMultipliers::setIpad(double value)
{
    
    m_Ipad = std::move(value);
    m_IpadIsSet = true;
}
double BidOptionsAppTypeMultipliers::getIphone() const
{
    return m_Iphone;
}

void BidOptionsAppTypeMultipliers::setIphone(double value)
{
    
    m_Iphone = std::move(value);
    m_IphoneIsSet = true;
}
double BidOptionsAppTypeMultipliers::getWeb() const
{
    return m_Web;
}

void BidOptionsAppTypeMultipliers::setWeb(double value)
{
    
    m_Web = std::move(value);
    m_WebIsSet = true;
}
double BidOptionsAppTypeMultipliers::getWebMobile() const
{
    return m_Web_mobile;
}

void BidOptionsAppTypeMultipliers::setWebMobile(double value)
{
    
    m_Web_mobile = std::move(value);
    m_Web_mobileIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BidOptionsAppTypeMultipliers>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BidOptionsAppTypeMultipliers>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BidOptionsAppTypeMultipliers>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BidOptionsAppTypeMultipliers>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

