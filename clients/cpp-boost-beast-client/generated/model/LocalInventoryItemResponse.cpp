/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "LocalInventoryItemResponse.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

LocalInventoryItemResponse::LocalInventoryItemResponse(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string LocalInventoryItemResponse::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void LocalInventoryItemResponse::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value LocalInventoryItemResponse::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void LocalInventoryItemResponse::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for LocalInventoryItemResponse");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object LocalInventoryItemResponse::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Ad_linkIsSet) {
            object["ad_link"] = JsonValueConverter<std::string>::toJsonValue(getAdLink());
        }
        if (m_AvailabilityIsSet) {
            object["availability"] = JsonValueConverter<std::shared_ptr<ItemAvailability>>::toJsonValue(getAvailability());
        }
        object["created_at"] = JsonValueConverter<int64_t>::toJsonValue(getCreatedAt());
        object["item_id"] = JsonValueConverter<std::string>::toJsonValue(getItemId());
        object["last_updated_time"] = JsonValueConverter<int64_t>::toJsonValue(getLastUpdatedTime());
        if (m_PriceIsSet) {
            object["price"] = JsonValueConverter<std::string>::toJsonValue(getPrice());
        }
        if (m_Sale_priceIsSet) {
            object["sale_price"] = JsonValueConverter<std::string>::toJsonValue(getSalePrice());
        }
        object["store_metadata"] = JsonValueConverter<std::shared_ptr<StoreMetadata>>::toJsonValue(getStoreMetadata());
    return object;
}

void LocalInventoryItemResponse::fromJsonObject_internal(boost::json::object const& object)
{
    m_Ad_linkIsSet = false;
    m_AvailabilityIsSet = false;
    m_PriceIsSet = false;
    m_Sale_priceIsSet = false;
    {
        const auto Ad_linkIt = object.find("ad_link");
        if (Ad_linkIt != object.end()) {
            setAdLink(JsonValueConverter<std::string>::fromJsonValue(Ad_linkIt->value()));
        }
    }
    {
        const auto AvailabilityIt = object.find("availability");
        if (AvailabilityIt != object.end()) {
            setAvailability(JsonValueConverter<std::shared_ptr<ItemAvailability>>::fromJsonValue(AvailabilityIt->value()));
        }
    }
    {
        const auto Created_atIt = object.find("created_at");
        if (Created_atIt != object.end()) {
            setCreatedAt(JsonValueConverter<int64_t>::fromJsonValue(Created_atIt->value()));
        }
    }
    {
        const auto Item_idIt = object.find("item_id");
        if (Item_idIt != object.end()) {
            setItemId(JsonValueConverter<std::string>::fromJsonValue(Item_idIt->value()));
        }
    }
    {
        const auto Last_updated_timeIt = object.find("last_updated_time");
        if (Last_updated_timeIt != object.end()) {
            setLastUpdatedTime(JsonValueConverter<int64_t>::fromJsonValue(Last_updated_timeIt->value()));
        }
    }
    {
        const auto PriceIt = object.find("price");
        if (PriceIt != object.end()) {
            setPrice(JsonValueConverter<std::string>::fromJsonValue(PriceIt->value()));
        }
    }
    {
        const auto Sale_priceIt = object.find("sale_price");
        if (Sale_priceIt != object.end()) {
            setSalePrice(JsonValueConverter<std::string>::fromJsonValue(Sale_priceIt->value()));
        }
    }
    {
        const auto Store_metadataIt = object.find("store_metadata");
        if (Store_metadataIt != object.end()) {
            setStoreMetadata(JsonValueConverter<std::shared_ptr<StoreMetadata>>::fromJsonValue(Store_metadataIt->value()));
        }
    }
}

std::string LocalInventoryItemResponse::getAdLink() const
{
    return m_Ad_link;
}

void LocalInventoryItemResponse::setAdLink(std::string value)
{
    
    m_Ad_link = std::move(value);
    m_Ad_linkIsSet = true;
}
std::shared_ptr<ItemAvailability> LocalInventoryItemResponse::getAvailability() const
{
    return m_Availability;
}

void LocalInventoryItemResponse::setAvailability(std::shared_ptr<ItemAvailability> value)
{
    
    m_Availability = std::move(value);
    m_AvailabilityIsSet = true;
}
int64_t LocalInventoryItemResponse::getCreatedAt() const
{
    return m_Created_at;
}

void LocalInventoryItemResponse::setCreatedAt(int64_t value)
{
    
    m_Created_at = std::move(value);
}
std::string LocalInventoryItemResponse::getItemId() const
{
    return m_Item_id;
}

void LocalInventoryItemResponse::setItemId(std::string value)
{
    
    m_Item_id = std::move(value);
}
int64_t LocalInventoryItemResponse::getLastUpdatedTime() const
{
    return m_Last_updated_time;
}

void LocalInventoryItemResponse::setLastUpdatedTime(int64_t value)
{
    
    m_Last_updated_time = std::move(value);
}
std::string LocalInventoryItemResponse::getPrice() const
{
    return m_Price;
}

void LocalInventoryItemResponse::setPrice(std::string value)
{
    
    m_Price = std::move(value);
    m_PriceIsSet = true;
}
std::string LocalInventoryItemResponse::getSalePrice() const
{
    return m_Sale_price;
}

void LocalInventoryItemResponse::setSalePrice(std::string value)
{
    
    m_Sale_price = std::move(value);
    m_Sale_priceIsSet = true;
}
std::shared_ptr<StoreMetadata> LocalInventoryItemResponse::getStoreMetadata() const
{
    return m_Store_metadata;
}

void LocalInventoryItemResponse::setStoreMetadata(std::shared_ptr<StoreMetadata> value)
{
    
    m_Store_metadata = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<LocalInventoryItemResponse>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<LocalInventoryItemResponse>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<LocalInventoryItemResponse>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<LocalInventoryItemResponse>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

