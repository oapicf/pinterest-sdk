/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "TrendingTopic.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

TrendingTopic::TrendingTopic(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string TrendingTopic::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void TrendingTopic::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value TrendingTopic::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void TrendingTopic::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for TrendingTopic");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object TrendingTopic::toJsonObject_internal() const
{
    boost::json::object object;
        object["description"] = JsonValueConverter<std::string>::toJsonValue(getDescription());
        object["id"] = JsonValueConverter<std::string>::toJsonValue(getId());
        if (m_Percent_growth_momIsSet) {
            object["percent_growth_mom"] = JsonValueConverter<int32_t>::toJsonValue(getPercentGrowthMom());
        }
        object["pins"] = JsonValueConverter<std::vector<std::shared_ptr<TrendingPin>>>::toJsonValue(getPins());
        object["related_interests"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getRelatedInterests());
        object["related_searches"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getRelatedSearches());
        object["time_series"] = JsonValueConverter<std::map<std::string, double>>::toJsonValue(getTimeSeries());
        object["title"] = JsonValueConverter<std::string>::toJsonValue(getTitle());
    return object;
}

void TrendingTopic::fromJsonObject_internal(boost::json::object const& object)
{
    m_Percent_growth_momIsSet = false;
    {
        const auto DescriptionIt = object.find("description");
        if (DescriptionIt != object.end()) {
            setDescription(JsonValueConverter<std::string>::fromJsonValue(DescriptionIt->value()));
        }
    }
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            setId(JsonValueConverter<std::string>::fromJsonValue(IdIt->value()));
        }
    }
    {
        const auto Percent_growth_momIt = object.find("percent_growth_mom");
        if (Percent_growth_momIt != object.end()) {
            setPercentGrowthMom(JsonValueConverter<int32_t>::fromJsonValue(Percent_growth_momIt->value()));
        }
    }
    {
        const auto PinsIt = object.find("pins");
        if (PinsIt != object.end()) {
            setPins(JsonValueConverter<std::vector<std::shared_ptr<TrendingPin>>>::fromJsonValue(PinsIt->value()));
        }
    }
    {
        const auto Related_interestsIt = object.find("related_interests");
        if (Related_interestsIt != object.end()) {
            setRelatedInterests(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Related_interestsIt->value()));
        }
    }
    {
        const auto Related_searchesIt = object.find("related_searches");
        if (Related_searchesIt != object.end()) {
            setRelatedSearches(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Related_searchesIt->value()));
        }
    }
    {
        const auto Time_seriesIt = object.find("time_series");
        if (Time_seriesIt != object.end()) {
            setTimeSeries(JsonValueConverter<std::map<std::string, double>>::fromJsonValue(Time_seriesIt->value()));
        }
    }
    {
        const auto TitleIt = object.find("title");
        if (TitleIt != object.end()) {
            setTitle(JsonValueConverter<std::string>::fromJsonValue(TitleIt->value()));
        }
    }
}

std::string TrendingTopic::getDescription() const
{
    return m_Description;
}

void TrendingTopic::setDescription(std::string value)
{
    
    m_Description = std::move(value);
}
std::string TrendingTopic::getId() const
{
    return m_Id;
}

void TrendingTopic::setId(std::string value)
{
    
    m_Id = std::move(value);
}
int32_t TrendingTopic::getPercentGrowthMom() const
{
    return m_Percent_growth_mom;
}

void TrendingTopic::setPercentGrowthMom(int32_t value)
{
    
    m_Percent_growth_mom = std::move(value);
    m_Percent_growth_momIsSet = true;
}
std::vector<std::shared_ptr<TrendingPin>> TrendingTopic::getPins() const
{
    return m_Pins;
}

void TrendingTopic::setPins(std::vector<std::shared_ptr<TrendingPin>> value)
{
    
    m_Pins = std::move(value);
}
std::vector<std::string> TrendingTopic::getRelatedInterests() const
{
    return m_Related_interests;
}

void TrendingTopic::setRelatedInterests(std::vector<std::string> value)
{
    
    m_Related_interests = std::move(value);
}
std::vector<std::string> TrendingTopic::getRelatedSearches() const
{
    return m_Related_searches;
}

void TrendingTopic::setRelatedSearches(std::vector<std::string> value)
{
    
    m_Related_searches = std::move(value);
}
std::map<std::string, double> TrendingTopic::getTimeSeries() const
{
    return m_Time_series;
}

void TrendingTopic::setTimeSeries(std::map<std::string, double> value)
{
    
    m_Time_series = std::move(value);
}
std::string TrendingTopic::getTitle() const
{
    return m_Title;
}

void TrendingTopic::setTitle(std::string value)
{
    
    m_Title = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<TrendingTopic>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<TrendingTopic>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<TrendingTopic>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<TrendingTopic>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

