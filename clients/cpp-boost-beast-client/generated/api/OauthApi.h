/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * OauthApi.h
 *
 * 
 */

#ifndef BOOST_BEAST_OPENAPI_CLIENT_OauthApi_
#define BOOST_BEAST_OPENAPI_CLIENT_OauthApi_

#include <exception>
#include <map>
#include <memory>
#include <string>
#include <utility>
#include <vector>

#include <boost/beast/core.hpp>
#include <boost/optional.hpp>

#include "api/HttpClient.h"

#include "ConversionAccessToken.h"
#include "OauthAccessToken.h"
#include "Pinterest_Lib_Error.h"
#include "TokenGrantType.h"
#include "TokenTypeHint.h"
#include <string>

namespace org {
namespace openapitools {
namespace client {
namespace api {

using namespace org::openapitools::client::model;

///
/// Exception to flag problems in the api's
///
class OauthApiException: public std::exception
{
public:
    OauthApiException(boost::beast::http::status statusCode, std::string what);

    boost::beast::http::status getStatus() const;
    const char* what() const noexcept override;

private:
    boost::beast::http::status m_status;
    std::string m_what;
};


class OauthApi {

public:
    OauthApi(
        std::shared_ptr<HttpClient> client,
        const std::string& context = "/v5")
      : m_client(std::move(client)),
        m_context(context)
    {}

    virtual ~OauthApi() = default;

    /// <summary>
    /// Generate OAuth access token for conversion API
    /// </summary>
    /// <remarks>
    /// Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
    /// </remarks>
    virtual std::shared_ptr<ConversionAccessToken>
    oauth_conversionToken(
        );

    /// <summary>
    /// Generate OAuth access token
    /// </summary>
    /// <remarks>
    /// Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the &#x60;continuous_refresh&#x60; parameter to &#x60;true&#x60; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 
    /// </remarks>
    virtual std::shared_ptr<OauthAccessToken>
    oauth_token(
        const std::shared_ptr<TokenGrantType>& grantType, const std::string& code, const std::string& continuousRefresh, const std::string& redirectUri, const std::string& refreshToken, const std::string& scope);

    /// <summary>
    /// Revoke a token
    /// </summary>
    /// <remarks>
    /// Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
    /// </remarks>
    virtual void
    token_revoke(
        const std::string& token, const std::shared_ptr<TokenTypeHint>& tokenTypeHint);


protected:
    virtual std::string base64encode(const std::string& str);

    virtual void setPreferredMediaTypeHeader(
        std::map<std::string, std::string>& headers, const std::string& headerName, const std::vector<std::string>& contentTypes);

    virtual void handleStdException(
        const std::exception& e);
    virtual void handleUncaughtException();

    private:
    std::shared_ptr<HttpClient> m_client;
    std::string m_context;
};


}
}
}
}


#endif /* BOOST_BEAST_OPENAPI_CLIENT_OauthApi_ */
