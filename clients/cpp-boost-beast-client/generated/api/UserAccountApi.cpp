/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <sstream>
#include <stdexcept>
#include <string>
#include <type_traits>
#include <map>
#include <array>
#include <algorithm>
#include <utility>
#include <vector>

#include <boost/lexical_cast.hpp>
#include <boost/beast/http/status.hpp>
#include <boost/version.hpp>
#include <boost/beast/core/detail/base64.hpp>
#include <boost/algorithm/string.hpp>
#include <boost/json.hpp>


#include "UserAccountApi.h"


namespace org {
namespace openapitools {
namespace client {
namespace api {

using namespace org::openapitools::client::model;


namespace {
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 1> preferredTypes = {"json"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
                                      contentTypes.cend(),
                                      [preferredType](const std::string& str) {
                                            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    if (contentTypes.size() == 0) {
        return "application/json";
    }

    return contentTypes.at(0);
}

inline std::string normalizeMediaType(const std::string& contentType) {
    const auto parameterSeparator = contentType.find(';');
    std::string mediaType = contentType.substr(0, parameterSeparator);
    boost::algorithm::trim(mediaType);
    boost::algorithm::to_lower(mediaType);
    return mediaType;
}

inline bool isJsonContentType(const std::string& contentType) {
    const std::string mediaType = normalizeMediaType(contentType);
    return mediaType == "application/json"
        || (mediaType.size() > 5 && mediaType.compare(mediaType.size() - 5, 5, "+json") == 0);
}
}

struct FormParameter {
    FormParameter(std::string parameterName, std::string parameterValue, bool file)
        : name(std::move(parameterName)), value(std::move(parameterValue)), isFile(file) {
    }

    std::string name;
    std::string value;
    bool isFile;
};

inline std::string toFormParameterValue(const std::string& value) {
    return value;
}

inline std::string toFormParameterValue(bool value) {
    return value ? "true" : "false";
}

inline std::string toRawBodyValue(const std::string& value) {
    return value;
}

inline std::string toRawBodyValue(bool value) {
    return value ? "true" : "false";
}

template<typename T>
std::string toRawBodyValue(const T& value) {
    return boost::lexical_cast<std::string>(value);
}

template<typename T>
std::string toFormParameterValue(const T& value) {
    return boost::lexical_cast<std::string>(value);
}

template<typename T>
std::string toFormParameterValue(const std::vector<T>& values) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const auto& value : values) {
        serializedValues << separator << toFormParameterValue(value);
        separator = ",";
    }
    return serializedValues.str();
}

inline std::string percentEncodeFormValue(const std::string& value) {
    static const char hexDigits[] = "0123456789ABCDEF";
    std::string encodedValue;
    encodedValue.reserve(value.size());
    for (const unsigned char character : value) {
        if ((character >= 'a' && character <= 'z')
            || (character >= 'A' && character <= 'Z')
            || (character >= '0' && character <= '9')
            || character == '-' || character == '.' || character == '_'
            || character == '~') {
            encodedValue.push_back(static_cast<char>(character));
        } else if (character == ' ') {
            encodedValue.push_back('+');
        } else {
            encodedValue.push_back('%');
            encodedValue.push_back(hexDigits[(character >> 4) & 0x0F]);
            encodedValue.push_back(hexDigits[character & 0x0F]);
        }
    }
    return encodedValue;
}

inline std::string percentEncodeRfc3986Value(const std::string& unencodedValue) {
    static const char hexDigits[] = "0123456789ABCDEF";
    std::string encodedValue;
    encodedValue.reserve(unencodedValue.size());
    for (const unsigned char character : unencodedValue) {
        if ((character >= 'a' && character <= 'z')
            || (character >= 'A' && character <= 'Z')
            || (character >= '0' && character <= '9')
            || character == '-' || character == '.' || character == '_'
            || character == '~') {
            encodedValue.push_back(static_cast<char>(character));
        } else {
            encodedValue.push_back('%');
            encodedValue.push_back(hexDigits[(character >> 4) & 0x0F]);
            encodedValue.push_back(hexDigits[character & 0x0F]);
        }
    }
    return encodedValue;
}

inline std::string percentEncodePathValue(const std::string& unencodedValue) {
    return percentEncodeRfc3986Value(unencodedValue);
}

inline std::string percentEncodeQueryValue(const std::string& unencodedValue) {
    return percentEncodeRfc3986Value(unencodedValue);
}

template<typename T>
std::string serializePathParameterValue(const T& pathParameterValue) {
    return percentEncodePathValue(toFormParameterValue(pathParameterValue));
}

template<typename T>
std::string serializePathParameterValue(const std::vector<T>& pathParameterValues) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& pathParameterValue : pathParameterValues) {
        serializedValues << separator
                         << percentEncodePathValue(toFormParameterValue(pathParameterValue));
        separator = ",";
    }
    return serializedValues.str();
}

template<typename T>
void replacePathParameter(
    std::string& path,
    const std::string& parameterName,
    const T& parameterValue) {
    const std::string placeholder = "{" + parameterName + "}";
    const std::string serializedValue = serializePathParameterValue(parameterValue);
    std::string::size_type position = 0;
    while ((position = path.find(placeholder, position)) != std::string::npos) {
        path.replace(position, placeholder.size(), serializedValue);
        position += serializedValue.size();
    }
}

template<typename T>
std::string serializeQueryParameterValue(const T& queryParameterValue) {
    return percentEncodeQueryValue(toFormParameterValue(queryParameterValue));
}

template<typename T>
std::string serializeQueryParameterValue(
    const std::vector<T>& queryParameterValues,
    const std::string& collectionDelimiter) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& queryParameterValue : queryParameterValues) {
        serializedValues << separator
                         << percentEncodeQueryValue(toFormParameterValue(queryParameterValue));
        separator = collectionDelimiter.c_str();
    }
    return serializedValues.str();
}

template<typename T>
std::string serializeQueryParameterValue(
    const std::map<std::string, T>& queryParameterValues,
    const std::string& collectionDelimiter) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const auto& queryParameterValue : queryParameterValues) {
        serializedValues << separator
                         << percentEncodeQueryValue(queryParameterValue.first)
                         << collectionDelimiter
                         << percentEncodeQueryValue(toFormParameterValue(queryParameterValue.second));
        separator = collectionDelimiter.c_str();
    }
    return serializedValues.str();
}

inline void appendQueryParameter(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::string& parameterValue) {
    queryParameterStream << queryParameterSeparator
                         << percentEncodeQueryValue(parameterName) << '='
                         << parameterValue;
    queryParameterSeparator = "&";
}

template<typename T>
void appendMultiQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::vector<T>& queryParameterValues) {
    for (const T& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            parameterName,
            serializeQueryParameterValue(queryParameterValue));
    }
}

template<typename T>
void appendExplodedQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::map<std::string, T>& queryParameterValues) {
    for (const auto& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            queryParameterValue.first,
            serializeQueryParameterValue(queryParameterValue.second));
    }
}

template<typename T>
void appendDeepObjectQueryParameters(
    std::stringstream& queryParameterStream,
    const char*& queryParameterSeparator,
    const std::string& parameterName,
    const std::map<std::string, T>& queryParameterValues) {
    for (const auto& queryParameterValue : queryParameterValues) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            parameterName + "[" + queryParameterValue.first + "]",
            serializeQueryParameterValue(queryParameterValue.second));
    }
}

inline std::string serializeHeaderParameterValue(const std::string& headerParameterValue) {
    return headerParameterValue;
}

inline std::string serializeHeaderParameterValue(bool headerParameterValue) {
    return headerParameterValue ? "true" : "false";
}

template<typename T>
typename std::enable_if<std::is_arithmetic<T>::value, std::string>::type
serializeHeaderParameterValue(const T& headerParameterValue) {
    return boost::lexical_cast<std::string>(headerParameterValue);
}

template<typename T>
typename std::enable_if<!std::is_arithmetic<T>::value, std::string>::type
serializeHeaderParameterValue(const T&) {
    throw std::invalid_argument(
        "Header parameter serialization supports only primitive values and arrays of primitive values");
}

template<typename T>
std::string serializeHeaderParameterValue(const std::vector<T>& headerParameterValues) {
    std::stringstream serializedValues;
    const char* separator = "";
    for (const T& headerParameterValue : headerParameterValues) {
        serializedValues << separator << serializeHeaderParameterValue(headerParameterValue);
        separator = ",";
    }
    return serializedValues.str();
}

inline std::string serializeUrlEncodedFormData(const std::vector<FormParameter>& formParameters) {
    std::stringstream serializedFormData;
    const char* separator = "";
    for (const auto& formParameter : formParameters) {
        serializedFormData << separator
                           << percentEncodeFormValue(formParameter.name)
                           << '='
                           << percentEncodeFormValue(formParameter.value);
        separator = "&";
    }
    return serializedFormData.str();
}

inline std::string selectMultipartBoundary(const std::vector<FormParameter>& formParameters) {
    std::string boundary = "OpenAPIGeneratorBoundary";
    for (;;) {
        const auto collision = std::find_if(
            formParameters.cbegin(),
            formParameters.cend(),
            [&boundary](const FormParameter& formParameter) {
                return formParameter.value.find(boundary) != std::string::npos;
            });
        if (collision == formParameters.cend()) {
            return boundary;
        }
        boundary.push_back('X');
    }
}

inline std::string escapeMultipartParameter(const std::string& value) {
    std::string escapedValue;
    escapedValue.reserve(value.size());
    for (const char character : value) {
        if (character == '\\' || character == '"') {
            escapedValue.push_back('\\');
        }
        escapedValue.push_back(character);
    }
    return escapedValue;
}

inline std::string serializeMultipartFormData(
    const std::vector<FormParameter>& formParameters,
    const std::string& boundary) {
    std::stringstream serializedFormData;
    for (const auto& formParameter : formParameters) {
        serializedFormData << "--" << boundary << "\r\n"
                           << "Content-Disposition: form-data; name=\""
                           << escapeMultipartParameter(formParameter.name) << '"';
        if (formParameter.isFile) {
            serializedFormData << "; filename=\""
                               << escapeMultipartParameter(formParameter.name) << '"';
        }
        serializedFormData << "\r\n";
        if (formParameter.isFile) {
            serializedFormData << "Content-Type: application/octet-stream\r\n";
        }
        serializedFormData << "\r\n" << formParameter.value << "\r\n";
    }
    serializedFormData << "--" << boundary << "--\r\n";
    return serializedFormData.str();
}

namespace {

std::string base64encodeImpl(const std::string& str) {
#if BOOST_VERSION < 107100
    return boost::beast::detail::base64_encode(str);
#else
    std::string encoded;
    encoded.resize(boost::beast::detail::base64::encoded_size(str.size()));
    if (!str.empty()) {
        encoded.resize(boost::beast::detail::base64::encode(&encoded[0], str.data(), str.size()));
    }
    return encoded;
#endif
}

template<typename T>
boost::json::value toRequestJsonValue(const T& requestValue);

template<typename T>
boost::json::value toRequestJsonValue(const std::shared_ptr<T>& requestValue);

template<typename T>
boost::json::value toRequestJsonValue(const std::vector<T>& requestValues);

template<typename T>
boost::json::value toRequestJsonValue(const std::map<std::string, T>& requestValues);

template<typename T>
boost::json::value toRequestJsonValue(const T& requestValue) {
    return boost::json::value_from(requestValue);
}

template<typename T>
boost::json::value toRequestJsonValue(const std::shared_ptr<T>& requestValue) {
    return requestValue == nullptr ? boost::json::value(nullptr) : requestValue->toJsonValue();
}

template<typename T>
boost::json::value toRequestJsonValue(const std::vector<T>& requestValues) {
    boost::json::array requestArray;
    requestArray.reserve(requestValues.size());
    for (const auto& requestValue : requestValues) {
        requestArray.emplace_back(toRequestJsonValue(requestValue));
    }
    return requestArray;
}

template<typename T>
boost::json::value toRequestJsonValue(const std::map<std::string, T>& requestValues) {
    boost::json::object requestObject;
    for (const auto& requestEntry : requestValues) {
        requestObject.emplace(requestEntry.first, toRequestJsonValue(requestEntry.second));
    }
    return requestObject;
}

template<typename T>
struct ResponseJsonValueConverter {
    static T convert(const boost::json::value& responseValue) {
        return boost::json::value_to<T>(responseValue);
    }
};

template<>
struct ResponseJsonValueConverter<boost::json::value> {
    static boost::json::value convert(const boost::json::value& responseValue) {
        return responseValue;
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::shared_ptr<T>> {
    static std::shared_ptr<T> convert(const boost::json::value& responseValue) {
        return responseValue.is_null() ? nullptr : std::make_shared<T>(responseValue);
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::vector<T>> {
    static std::vector<T> convert(const boost::json::value& responseValue) {
        const auto& responseArray = responseValue.as_array();
        std::vector<T> convertedValues;
        convertedValues.reserve(responseArray.size());
        for (const auto& arrayValue : responseArray) {
            convertedValues.emplace_back(ResponseJsonValueConverter<T>::convert(arrayValue));
        }
        return convertedValues;
    }
};

template<typename T>
struct ResponseJsonValueConverter<std::map<std::string, T>> {
    static std::map<std::string, T> convert(const boost::json::value& responseValue) {
        const auto& responseObject = responseValue.as_object();
        std::map<std::string, T> convertedValues;
        for (const auto& objectEntry : responseObject) {
            convertedValues.emplace(
                objectEntry.key_c_str(),
                ResponseJsonValueConverter<T>::convert(objectEntry.value()));
        }
        return convertedValues;
    }
};

template<typename T>
struct ResponseBodyDeserializer {
    static void deserialize(
        T& convertedResponse,
        const std::string& responseBody,
        const std::string& responseContentType,
        bool tolerateEmptyBody) {
        if (!isJsonContentType(responseContentType)) {
            throw std::invalid_argument(
                "Content type '" + responseContentType + "' does not support structured response bodies");
        }
        if (tolerateEmptyBody && responseBody.empty()) {
            return;
        }
        convertedResponse = ResponseJsonValueConverter<T>::convert(boost::json::parse(responseBody));
    }
};

template<>
struct ResponseBodyDeserializer<std::string> {
    static void deserialize(
        std::string& convertedResponse,
        const std::string& responseBody,
        const std::string&,
        bool) {
        convertedResponse = responseBody;
    }
};
}

UserAccountApiException::UserAccountApiException(boost::beast::http::status statusCode, std::string what)
  : m_status(statusCode),
    m_what(what)
{
}

boost::beast::http::status UserAccountApiException::getStatus() const
{
    return m_status;
}

const char* UserAccountApiException::what() const noexcept
{
    return m_what.c_str();
}

std::shared_ptr<Boards_list_200_response>
UserAccountApi::boardsUserFollows_list(
    const boost::optional<std::string>& adAccountId, const boost::optional<bool>& explicitFollowing, const boost::optional<std::string>& bookmark, const boost::optional<int32_t>& pageSize) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/following/boards";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    if (explicitFollowing) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "explicit_following",
            serializeQueryParameterValue(*explicitFollowing));
    }
    if (bookmark) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "bookmark",
            serializeQueryParameterValue(*bookmark));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "page_size",
            serializeQueryParameterValue(*pageSize));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Boards_list_200_response> deserializedResponse = std::make_shared<Boards_list_200_response>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Boards_list_200_response>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<FollowUser>
UserAccountApi::followUser_update(
    const std::string& username, const std::shared_ptr<FollowUserCreate>& followUserCreate) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/following/{username}";
    std::map<std::string, std::string> headers;
    static const std::vector<std::string> contentTypes{ "application/json", };
    std::string requestContentType = selectPreferredContentType(contentTypes);
    headers["Content-Type"] = requestContentType;
    // Body params
    if (isJsonContentType(requestContentType)) {
        serializedRequestBody = boost::json::serialize(toRequestJsonValue(followUserCreate));
    } else {
        throw std::invalid_argument("Content type '" + requestContentType + "' does not support structured request bodies");
    }
    // path params
    replacePathParameter(path, "username", username);

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("POST",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<FollowUser> deserializedResponse = std::make_shared<FollowUser>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<FollowUser>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(201)) {
        ResponseBodyDeserializer<std::shared_ptr<FollowUser>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<Followers_list_200_response>
UserAccountApi::followers_list(
    const boost::optional<std::string>& bookmark, const boost::optional<int32_t>& pageSize) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/followers";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (bookmark) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "bookmark",
            serializeQueryParameterValue(*bookmark));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "page_size",
            serializeQueryParameterValue(*pageSize));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Followers_list_200_response> deserializedResponse = std::make_shared<Followers_list_200_response>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Followers_list_200_response>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::vector<std::shared_ptr<LinkedBusiness>>
UserAccountApi::linkedBusinessAccounts_get(
    ) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/businesses";
    std::map<std::string, std::string> headers;

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::vector<std::shared_ptr<LinkedBusiness>> deserializedResponse = std::vector<std::shared_ptr<LinkedBusiness>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::vector<std::shared_ptr<LinkedBusiness>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<UserWebsite>
UserAccountApi::unverifyWebsite_delete(
    const std::string& website) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/websites";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "website",
            serializeQueryParameterValue(website));
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("DELETE",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<UserWebsite> deserializedResponse = std::make_shared<UserWebsite>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<UserWebsite>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(204)) {
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<User_websites_get_200_response>
UserAccountApi::userWebsites_get(
    const boost::optional<std::string>& bookmark, const boost::optional<int32_t>& pageSize) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/websites";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (bookmark) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "bookmark",
            serializeQueryParameterValue(*bookmark));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "page_size",
            serializeQueryParameterValue(*pageSize));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<User_websites_get_200_response> deserializedResponse = std::make_shared<User_websites_get_200_response>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<User_websites_get_200_response>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<UserWebsite>
UserAccountApi::verifyWebsite_update(
    const std::shared_ptr<UserWebsiteCreate>& userWebsiteCreate, const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/websites";
    std::map<std::string, std::string> headers;
    static const std::vector<std::string> contentTypes{ "application/json", };
    std::string requestContentType = selectPreferredContentType(contentTypes);
    headers["Content-Type"] = requestContentType;
    // Body params
    if (isJsonContentType(requestContentType)) {
        serializedRequestBody = boost::json::serialize(toRequestJsonValue(userWebsiteCreate));
    } else {
        throw std::invalid_argument("Content type '" + requestContentType + "' does not support structured request bodies");
    }
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("POST",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<UserWebsite> deserializedResponse = std::make_shared<UserWebsite>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<UserWebsite>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(201)) {
        ResponseBodyDeserializer<std::shared_ptr<UserWebsite>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::map<std::string, std::shared_ptr<AnalyticsMetricsResponse>>
UserAccountApi::userAccount_analytics(
    const std::string& startDate, const std::string& endDate, const boost::optional<std::string>& fromClaimedContent, const boost::optional<std::string>& pinFormat, const boost::optional<std::string>& appTypes, const boost::optional<std::string>& contentType, const boost::optional<std::string>& source, const boost::optional<std::vector<std::shared_ptr<QuerymetrictypesItems>>>& metricTypes, const boost::optional<std::string>& splitField, const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/analytics";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "start_date",
            serializeQueryParameterValue(startDate));
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "end_date",
            serializeQueryParameterValue(endDate));
    if (fromClaimedContent) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "from_claimed_content",
            serializeQueryParameterValue(*fromClaimedContent));
    }
    if (pinFormat) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "pin_format",
            serializeQueryParameterValue(*pinFormat));
    }
    if (appTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "app_types",
            serializeQueryParameterValue(*appTypes));
    }
    if (contentType) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "content_type",
            serializeQueryParameterValue(*contentType));
    }
    if (source) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "source",
            serializeQueryParameterValue(*source));
    }
    if (metricTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "metric_types",
            serializeQueryParameterValue(
                *metricTypes,
                ","));
    }
    if (splitField) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "split_field",
            serializeQueryParameterValue(*splitField));
    }
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::map<std::string, std::shared_ptr<AnalyticsMetricsResponse>> deserializedResponse = std::map<std::string, std::shared_ptr<AnalyticsMetricsResponse>>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::map<std::string, std::shared_ptr<AnalyticsMetricsResponse>>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            true);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<TopPinsAnalyticsResponse>
UserAccountApi::userAccount_analytics_topPins(
    const std::string& startDate, const std::string& endDate, const std::shared_ptr<TopPinsSortBy>& sortBy, const boost::optional<std::string>& fromClaimedContent, const boost::optional<std::string>& pinFormat, const boost::optional<std::string>& appTypes, const boost::optional<std::string>& contentType, const boost::optional<std::string>& source, const boost::optional<std::vector<std::shared_ptr<QuerymetrictypesItems>>>& metricTypes, const boost::optional<int32_t>& numOfPins, const boost::optional<double>& createdInLastNDays, const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/analytics/top_pins";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "start_date",
            serializeQueryParameterValue(startDate));
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "end_date",
            serializeQueryParameterValue(endDate));
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "sort_by",
            serializeQueryParameterValue(sortBy));
    if (fromClaimedContent) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "from_claimed_content",
            serializeQueryParameterValue(*fromClaimedContent));
    }
    if (pinFormat) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "pin_format",
            serializeQueryParameterValue(*pinFormat));
    }
    if (appTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "app_types",
            serializeQueryParameterValue(*appTypes));
    }
    if (contentType) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "content_type",
            serializeQueryParameterValue(*contentType));
    }
    if (source) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "source",
            serializeQueryParameterValue(*source));
    }
    if (metricTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "metric_types",
            serializeQueryParameterValue(
                *metricTypes,
                ","));
    }
    if (numOfPins) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "num_of_pins",
            serializeQueryParameterValue(*numOfPins));
    }
    if (createdInLastNDays) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "created_in_last_n_days",
            serializeQueryParameterValue(*createdInLastNDays));
    }
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<TopPinsAnalyticsResponse> deserializedResponse = std::make_shared<TopPinsAnalyticsResponse>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<TopPinsAnalyticsResponse>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<TopVideoPinsAnalyticsResponse>
UserAccountApi::userAccount_analytics_topVideoPins(
    const std::string& startDate, const std::string& endDate, const std::shared_ptr<TopVideoPinsSortBy>& sortBy, const boost::optional<std::string>& fromClaimedContent, const boost::optional<std::string>& pinFormat, const boost::optional<std::string>& appTypes, const boost::optional<std::string>& contentType, const boost::optional<std::string>& source, const boost::optional<std::vector<std::shared_ptr<QueryvideopinmetrictypesItems>>>& metricTypes, const boost::optional<int32_t>& numOfPins, const boost::optional<double>& createdInLastNDays, const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/analytics/top_video_pins";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "start_date",
            serializeQueryParameterValue(startDate));
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "end_date",
            serializeQueryParameterValue(endDate));
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "sort_by",
            serializeQueryParameterValue(sortBy));
    if (fromClaimedContent) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "from_claimed_content",
            serializeQueryParameterValue(*fromClaimedContent));
    }
    if (pinFormat) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "pin_format",
            serializeQueryParameterValue(*pinFormat));
    }
    if (appTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "app_types",
            serializeQueryParameterValue(*appTypes));
    }
    if (contentType) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "content_type",
            serializeQueryParameterValue(*contentType));
    }
    if (source) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "source",
            serializeQueryParameterValue(*source));
    }
    if (metricTypes) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "metric_types",
            serializeQueryParameterValue(
                *metricTypes,
                ","));
    }
    if (numOfPins) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "num_of_pins",
            serializeQueryParameterValue(*numOfPins));
    }
    if (createdInLastNDays) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "created_in_last_n_days",
            serializeQueryParameterValue(*createdInLastNDays));
    }
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<TopVideoPinsAnalyticsResponse> deserializedResponse = std::make_shared<TopVideoPinsAnalyticsResponse>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<TopVideoPinsAnalyticsResponse>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<User_account_followed_interests_200_response>
UserAccountApi::userAccount_followedInterests(
    const std::string& username, const boost::optional<std::string>& bookmark, const boost::optional<int32_t>& pageSize) {
    std::string serializedRequestBody;
    std::string path = m_context + "/users/{username}/interests/follow";
    std::map<std::string, std::string> headers;
    // path params
    replacePathParameter(path, "username", username);
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (bookmark) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "bookmark",
            serializeQueryParameterValue(*bookmark));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "page_size",
            serializeQueryParameterValue(*pageSize));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<User_account_followed_interests_200_response> deserializedResponse = std::make_shared<User_account_followed_interests_200_response>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<User_account_followed_interests_200_response>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The server could not understand the request due to invalid syntax.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Access is unauthorized.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The server cannot find the requested resource.");
    }
    throw UserAccountApiException(statusCode, "Unexpected error");
}
std::shared_ptr<Account>
UserAccountApi::userAccount_get(
    const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Account> deserializedResponse = std::make_shared<Account>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Account>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<Followers_list_200_response>
UserAccountApi::userFollowing_get(
    const boost::optional<std::string>& adAccountId, const boost::optional<bool>& explicitFollowing, const boost::optional<std::shared_ptr<UserFollowingFeedType>>& feedType, const boost::optional<std::string>& bookmark, const boost::optional<int32_t>& pageSize) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/following";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    if (explicitFollowing) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "explicit_following",
            serializeQueryParameterValue(*explicitFollowing));
    }
    if (feedType) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "feed_type",
            serializeQueryParameterValue(*feedType));
    }
    if (bookmark) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "bookmark",
            serializeQueryParameterValue(*bookmark));
    }
    if (pageSize) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "page_size",
            serializeQueryParameterValue(*pageSize));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<Followers_list_200_response> deserializedResponse = std::make_shared<Followers_list_200_response>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<Followers_list_200_response>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}
std::shared_ptr<UserWebsiteVerification>
UserAccountApi::websiteVerification_get(
    const boost::optional<std::string>& adAccountId) {
    std::string serializedRequestBody;
    std::string path = m_context + "/user_account/websites/verification";
    std::map<std::string, std::string> headers;
    // query params
    std::stringstream queryParameterStream;
    const char* queryParameterSeparator = "?";
    if (adAccountId) {
        appendQueryParameter(
            queryParameterStream,
            queryParameterSeparator,
            "ad_account_id",
            serializeQueryParameterValue(*adAccountId));
    }
    path += queryParameterStream.str();

    std::string responseContentType = "application/json";
    static const std::vector<std::string> acceptTypes{ "application/json", };
    responseContentType = selectPreferredContentType(acceptTypes);
    headers["Accept"] = responseContentType;

    auto statusCode = boost::beast::http::status::unknown;
    std::string responseBody;
    try {
        std::tie(statusCode, responseBody) =
            m_client->execute("GET",
                              path,
                              serializedRequestBody,
                              headers);
    }
    catch(const std::exception& exception) {
        handleStdException(exception);
    }
    catch(...) {
        handleUncaughtException();
    }

    std::shared_ptr<UserWebsiteVerification> deserializedResponse = std::make_shared<UserWebsiteVerification>();
    if (statusCode == boost::beast::http::status(200)) {
        ResponseBodyDeserializer<std::shared_ptr<UserWebsiteVerification>>::deserialize(
            deserializedResponse,
            responseBody,
            responseContentType,
            false);
        return deserializedResponse;
    }
    if (statusCode == boost::beast::http::status(400)) {
        throw UserAccountApiException(statusCode, "The request could not be understood by the server due to unexpected data.");
    }
    if (statusCode == boost::beast::http::status(401)) {
        throw UserAccountApiException(statusCode, "Authentication is required and has either failed or not been provided.");
    }
    if (statusCode == boost::beast::http::status(403)) {
        throw UserAccountApiException(statusCode, "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.");
    }
    if (statusCode == boost::beast::http::status(404)) {
        throw UserAccountApiException(statusCode, "The requested resource could not be found on this server.");
    }
    if (statusCode == boost::beast::http::status(429)) {
        throw UserAccountApiException(statusCode, "The user has sent too many requests in a given amount of time and is being rate limited.");
    }
    throw UserAccountApiException(statusCode, "An unexpected error response.");
}


std::string UserAccountApi::base64encode(const std::string& str) {
    return base64encodeImpl(str);
}

void UserAccountApi::setPreferredMediaTypeHeader(std::map<std::string, std::string>& headers, const std::string& headerName, const std::vector<std::string>& contentTypes) {
    const std::string contentType = selectPreferredContentType(contentTypes);
    headers[headerName] = contentType;
}

void UserAccountApi::handleStdException(const std::exception&) {
    throw;
}

void UserAccountApi::handleUncaughtException() {
    throw;
}


}  // namespace org
}  // namespace openapitools
}  // namespace client
}  // namespace api
