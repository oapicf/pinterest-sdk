extends ApiBee
class_name UserAccountApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API UserAccountApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "UserAccountApi"


# Operation boardsUserFollows/list → GET /user_account/following/boards
# List following boards
#
# Get a list of the boards a user follows. The request returns a board summary object array.
func boards_user_follows/list(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	# explicitFollowing: bool = false   Eg: true
	# Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows.
	explicitFollowing = false,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "boards_user_follows/list.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "boards_user_follows/list.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "boards_user_follows/list.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "boards_user_follows/list.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/following/boards"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ad_account_id"] = adAccountId
	bzz_query["explicit_following"] = explicitFollowing
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BoardsList200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func boards_user_follows/list_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	# explicitFollowing: bool = false   Eg: true
	# Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows.
	explicitFollowing = false,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "boards_user_follows/list")
	bzz_callable.bind(
		adAccountId,
		explicitFollowing,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation followUser/update → POST /user_account/following/{username}
# Follow user
#
# **This endpoint is currently in beta and not available to all apps. [Learn more](/docs/getting-started/using-beta-and-restricted-features/).**  Use this request, as a signed-in user, to follow another user.
func follow_user/update(
	# username: String = ""   Eg: username_example
	# A valid username
	username: String,
	# followUserCreate: FollowUserCreate
	followUserCreate: FollowUserCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `username` constraints
	var bzz_username_regex := RegEx.new()
	bzz_username_regex.compile("/(?!^\\d+$)^.+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_username_regex.search(str(username)):
		var error := ApiError.new()
		error.identifier = "follow_user/update.param.validation.pattern"
		error.message = "Invalid value for `username`, must conform to the pattern `/(?!^\\d+$)^.+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/following/{username}".replace("{" + "username" + "}", _bzz_urlize_path_param(username))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "follow_user/update.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = followUserCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = FollowUser.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func follow_user/update_threaded(
	# username: String = ""   Eg: username_example
	# A valid username
	username: String,
	# followUserCreate: FollowUserCreate
	followUserCreate: FollowUserCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "follow_user/update")
	bzz_callable.bind(
		username,
		followUserCreate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation followers/list → GET /user_account/followers
# List followers
#
# Get a list of your followers.
func followers/list(
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "followers/list.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "followers/list.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/followers"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = FollowersList200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func followers/list_threaded(
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "followers/list")
	bzz_callable.bind(
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation linkedBusinessAccounts/get → GET /user_account/businesses
# List linked businesses
#
# Get a list of your linked business accounts.
func linked_business_accounts/get(
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/businesses"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LinkedBusiness.bzz_denormalize_multiple(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func linked_business_accounts/get_threaded(
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "linked_business_accounts/get")
	bzz_callable.bind(
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation unverifyWebsite/delete → DELETE /user_account/websites
# Unverify website
#
# Unverify a website verified by the signed-in user.
func unverify_website/delete(
	# website: String = ""   Eg: website_example
	# Website with path or domain only
	website: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/websites"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["website"] = website

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserWebsite.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func unverify_website/delete_threaded(
	# website: String = ""   Eg: website_example
	# Website with path or domain only
	website: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "unverify_website/delete")
	bzz_callable.bind(
		website,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userAccount/analytics → GET /user_account/analytics
# Get user account analytics
#
# Get analytics for the \"operation user_account\" - By default, the \"operation user_account\" is the token user_account.  Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\".
func user_account/analytics(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get data for, default is all.
	metricTypes = null,
	# splitField: String = "NO_SPLIT"   Eg: splitField_example
	# How to split the data into groups. Not including this param means data won't be split.
	splitField = "NO_SPLIT",
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "user_account/analytics.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "user_account/analytics.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/analytics"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["start_date"] = startDate
	bzz_query["end_date"] = endDate
	bzz_query["from_claimed_content"] = fromClaimedContent
	bzz_query["pin_format"] = pinFormat
	bzz_query["app_types"] = appTypes
	bzz_query["content_type"] = contentType
	bzz_query["source"] = source
	bzz_query["metric_types"] = metricTypes
	bzz_query["split_field"] = splitField
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_account/analytics_threaded(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get data for, default is all.
	metricTypes = null,
	# splitField: String = "NO_SPLIT"   Eg: splitField_example
	# How to split the data into groups. Not including this param means data won't be split.
	splitField = "NO_SPLIT",
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_account/analytics")
	bzz_callable.bind(
		startDate,
		endDate,
		fromClaimedContent,
		pinFormat,
		appTypes,
		contentType,
		source,
		metricTypes,
		splitField,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userAccount/analytics/topPins → GET /user_account/analytics/top_pins
# Get user account top pins analytics
#
# Gets analytics data about a user's top pins (limited to the top 50). - By default, the \"operation user_account\" is the token user_account.  Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\".
func user_account/analytics/top_pins(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# sortBy: TopPinsSortBy
	# Specify sorting order for metrics
	sortBy: TopPinsSortBy,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get data for, default is all.
	metricTypes = null,
	# numOfPins: int = 10   Eg: 56
	# Number of pins to include, default is 10. Max is 50.
	numOfPins = 10,
	# createdInLastNDays: float   Eg: 8.14
	# Get metrics for pins created in the last \"n\" days.
	createdInLastNDays = null,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `numOfPins` constraints
	if numOfPins > 50:
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_pins.param.validation.maximum"
		error.message = "Invalid value for `numOfPins`, must be smaller than or equal to 50."
		on_failure.call(error)
		return
	if numOfPins < 1:
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_pins.param.validation.minimum"
		error.message = "Invalid value for `numOfPins`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "user_account/analytics/top_pins.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_pins.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/analytics/top_pins"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["start_date"] = startDate
	bzz_query["end_date"] = endDate
	bzz_query["sort_by"] = sortBy
	bzz_query["from_claimed_content"] = fromClaimedContent
	bzz_query["pin_format"] = pinFormat
	bzz_query["app_types"] = appTypes
	bzz_query["content_type"] = contentType
	bzz_query["source"] = source
	bzz_query["metric_types"] = metricTypes
	bzz_query["num_of_pins"] = numOfPins
	bzz_query["created_in_last_n_days"] = createdInLastNDays
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = TopPinsAnalyticsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_account/analytics/top_pins_threaded(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# sortBy: TopPinsSortBy
	# Specify sorting order for metrics
	sortBy: TopPinsSortBy,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get data for, default is all.
	metricTypes = null,
	# numOfPins: int = 10   Eg: 56
	# Number of pins to include, default is 10. Max is 50.
	numOfPins = 10,
	# createdInLastNDays: float   Eg: 8.14
	# Get metrics for pins created in the last \"n\" days.
	createdInLastNDays = null,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_account/analytics/top_pins")
	bzz_callable.bind(
		startDate,
		endDate,
		sortBy,
		fromClaimedContent,
		pinFormat,
		appTypes,
		contentType,
		source,
		metricTypes,
		numOfPins,
		createdInLastNDays,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userAccount/analytics/topVideoPins → GET /user_account/analytics/top_video_pins
# Get user account top video pins analytics
#
# Gets analytics data about a user's top video pins (limited to the top 50). - By default, the \"operation user_account\" is the token user_account.  Optional: Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\".
func user_account/analytics/top_video_pins(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# sortBy: TopVideoPinsSortBy
	# Specify sorting order for video metrics
	sortBy: TopVideoPinsSortBy,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get video data for, default is all.
	metricTypes = null,
	# numOfPins: int = 10   Eg: 56
	# Number of pins to include, default is 10. Max is 50.
	numOfPins = 10,
	# createdInLastNDays: float   Eg: 8.14
	# Get metrics for pins created in the last \"n\" days.
	createdInLastNDays = null,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `numOfPins` constraints
	if numOfPins > 50:
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_video_pins.param.validation.maximum"
		error.message = "Invalid value for `numOfPins`, must be smaller than or equal to 50."
		on_failure.call(error)
		return
	if numOfPins < 1:
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_video_pins.param.validation.minimum"
		error.message = "Invalid value for `numOfPins`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "user_account/analytics/top_video_pins.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "user_account/analytics/top_video_pins.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/analytics/top_video_pins"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["start_date"] = startDate
	bzz_query["end_date"] = endDate
	bzz_query["sort_by"] = sortBy
	bzz_query["from_claimed_content"] = fromClaimedContent
	bzz_query["pin_format"] = pinFormat
	bzz_query["app_types"] = appTypes
	bzz_query["content_type"] = contentType
	bzz_query["source"] = source
	bzz_query["metric_types"] = metricTypes
	bzz_query["num_of_pins"] = numOfPins
	bzz_query["created_in_last_n_days"] = createdInLastNDays
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = TopVideoPinsAnalyticsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_account/analytics/top_video_pins_threaded(
	# startDate: String   Eg: 2013-10-20
	# Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
	startDate: String,
	# endDate: String   Eg: 2013-10-20
	# Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
	endDate: String,
	# sortBy: TopVideoPinsSortBy
	# Specify sorting order for video metrics
	sortBy: TopVideoPinsSortBy,
	# fromClaimedContent: String = "BOTH"   Eg: fromClaimedContent_example
	# Filter on Pins that match your claimed domain.
	fromClaimedContent = "BOTH",
	# pinFormat: String = "ALL"   Eg: pinFormat_example
	# Pin formats to get data for, default is all.
	pinFormat = "ALL",
	# appTypes: String = "ALL"   Eg: appTypes_example
	# Apps or devices to get data for, default is all.
	appTypes = "ALL",
	# contentType: String = "ALL"   Eg: contentType_example
	# Filter to paid or organic data. Default is all.
	contentType = "ALL",
	# source: String = "ALL"   Eg: source_example
	# Filter to activity from Pins created and saved by your, or activity created and saved by others from your claimed accounts
	source = "ALL",
	# metricTypes: Array
	# Metric types to get video data for, default is all.
	metricTypes = null,
	# numOfPins: int = 10   Eg: 56
	# Number of pins to include, default is 10. Max is 50.
	numOfPins = 10,
	# createdInLastNDays: float   Eg: 8.14
	# Get metrics for pins created in the last \"n\" days.
	createdInLastNDays = null,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_account/analytics/top_video_pins")
	bzz_callable.bind(
		startDate,
		endDate,
		sortBy,
		fromClaimedContent,
		pinFormat,
		appTypes,
		contentType,
		source,
		metricTypes,
		numOfPins,
		createdInLastNDays,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userAccount/followedInterests → GET /users/{username}/interests/follow
# List following interests
#
# Get a list of a user's following interests in one place.
func user_account/followed_interests(
	# username: String = ""   Eg: username_example
	# A valid username
	username: String,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `username` constraints
	var bzz_username_regex := RegEx.new()
	bzz_username_regex.compile("/(?!^\\d+$)^.+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_username_regex.search(str(username)):
		var error := ApiError.new()
		error.identifier = "user_account/followed_interests.param.validation.pattern"
		error.message = "Invalid value for `username`, must conform to the pattern `/(?!^\\d+$)^.+$/`."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "user_account/followed_interests.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "user_account/followed_interests.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/users/{username}/interests/follow".replace("{" + "username" + "}", _bzz_urlize_path_param(username))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserAccountFollowedInterests200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_account/followed_interests_threaded(
	# username: String = ""   Eg: username_example
	# A valid username
	username: String,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_account/followed_interests")
	bzz_callable.bind(
		username,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userAccount/get → GET /user_account
# Get user account
#
# Get account information for the \"operation user_account\" - By default, the \"operation user_account\" is the token user_account.  [Understanding Business Access]: https://developers.pinterest.com/docs/getting-started/using-business-access/ \"Understanding Business Access\" If using Business Access: Specify an ad_account_id to use the owner of that ad_account as the \"operation user_account\". See [Understanding Business Access] for more information.
func user_account/get(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "user_account/get.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "user_account/get.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = Account.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_account/get_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_account/get")
	bzz_callable.bind(
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userFollowing/get → GET /user_account/following
# List following
#
# Get a list of who a certain user follows.
func user_following/get(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	# explicitFollowing: bool = false   Eg: true
	# Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows.
	explicitFollowing = false,
	# feedType: UserFollowingFeedType
	# Thrift param specifying what type of followees will be kept. Default to include all followees.
	feedType = null,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "user_following/get.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "user_following/get.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "user_following/get.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "user_following/get.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/following"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ad_account_id"] = adAccountId
	bzz_query["explicit_following"] = explicitFollowing
	bzz_query["feed_type"] = feedType
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = FollowersList200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_following/get_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	# explicitFollowing: bool = false   Eg: true
	# Whether or not to include implicit user follows, which means followees with board follows. When explicit_following is True, it means we only want explicit user follows.
	explicitFollowing = false,
	# feedType: UserFollowingFeedType
	# Thrift param specifying what type of followees will be kept. Default to include all followees.
	feedType = null,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_following/get")
	bzz_callable.bind(
		adAccountId,
		explicitFollowing,
		feedType,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation userWebsites/get → GET /user_account/websites
# Get user websites
#
# Get user websites, claimed or not
func user_websites/get(
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "user_websites/get.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "user_websites/get.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/websites"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserWebsitesGet200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func user_websites/get_threaded(
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "user_websites/get")
	bzz_callable.bind(
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation verifyWebsite/update → POST /user_account/websites
# Verify website
#
# Verify a website as a signed-in user.
func verify_website/update(
	# userWebsiteCreate: UserWebsiteCreate
	userWebsiteCreate: UserWebsiteCreate,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "verify_website/update.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "verify_website/update.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/websites"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "verify_website/update.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null
	bzz_body = userWebsiteCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserWebsite.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func verify_website/update_threaded(
	# userWebsiteCreate: UserWebsiteCreate
	userWebsiteCreate: UserWebsiteCreate,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "verify_website/update")
	bzz_callable.bind(
		userWebsiteCreate,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation websiteVerification/get → GET /user_account/websites/verification
# Get user verification code for website claiming
#
# Get verification code for user to install on the website to claim it.
func website_verification/get(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "website_verification/get.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "website_verification/get.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/user_account/websites/verification"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["ad_account_id"] = adAccountId

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UserWebsiteVerification.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func website_verification/get_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId = "",
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "website_verification/get")
	bzz_callable.bind(
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


