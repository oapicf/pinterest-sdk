extends ApiBee
class_name BusinessAccessRelationshipsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API BusinessAccessRelationshipsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "BusinessAccessRelationshipsApi"


# Operation brandAccounts/create → POST /business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts
# Create a Brand Account
#
# Create a Brand Account that will be a child business of a business hierarchy. Request must contain name, username, and country.
func brand_accounts/create(
	# businessHierarchyId: String = ""   Eg: businessHierarchyId_example
	# business hierarchy node id
	businessHierarchyId: String,
	# brandAccountCreate: BrandAccountCreate
	brandAccountCreate: BrandAccountCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessHierarchyId` constraints
	if (businessHierarchyId is String) and businessHierarchyId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "brand_accounts/create.param.validation.max_length"
		error.message = "Invalid length for `businessHierarchyId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessHierarchyId is String) and businessHierarchyId.length() < 1:
		var error := ApiError.new()
		error.identifier = "brand_accounts/create.param.validation.min_length"
		error.message = "Invalid length for `businessHierarchyId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessHierarchyId_regex := RegEx.new()
	bzz_businessHierarchyId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessHierarchyId_regex.search(str(businessHierarchyId)):
		var error := ApiError.new()
		error.identifier = "brand_accounts/create.param.validation.pattern"
		error.message = "Invalid value for `businessHierarchyId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts".replace("{" + "business_hierarchy_id" + "}", _bzz_urlize_path_param(businessHierarchyId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "brand_accounts/create.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = brandAccountCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BrandAccount.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func brand_accounts/create_threaded(
	# businessHierarchyId: String = ""   Eg: businessHierarchyId_example
	# business hierarchy node id
	businessHierarchyId: String,
	# brandAccountCreate: BrandAccountCreate
	brandAccountCreate: BrandAccountCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "brand_accounts/create")
	bzz_callable.bind(
		businessHierarchyId,
		brandAccountCreate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation brandAccounts/update → PATCH /business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts/{brand_account_id}
# Update a Brand Account
#
# Update an existing Brand Account
func brand_accounts/update(
	# brandAccountId: String = ""   Eg: brandAccountId_example
	brandAccountId: String,
	# businessHierarchyId: String = ""   Eg: businessHierarchyId_example
	# business hierarchy node id
	businessHierarchyId: String,
	# brandAccountUpdate: BrandAccountUpdate
	brandAccountUpdate: BrandAccountUpdate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessHierarchyId` constraints
	if (businessHierarchyId is String) and businessHierarchyId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "brand_accounts/update.param.validation.max_length"
		error.message = "Invalid length for `businessHierarchyId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessHierarchyId is String) and businessHierarchyId.length() < 1:
		var error := ApiError.new()
		error.identifier = "brand_accounts/update.param.validation.min_length"
		error.message = "Invalid length for `businessHierarchyId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessHierarchyId_regex := RegEx.new()
	bzz_businessHierarchyId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessHierarchyId_regex.search(str(businessHierarchyId)):
		var error := ApiError.new()
		error.identifier = "brand_accounts/update.param.validation.pattern"
		error.message = "Invalid value for `businessHierarchyId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PATCH")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts/{brand_account_id}".replace("{" + "brand_account_id" + "}", _bzz_urlize_path_param(brandAccountId)).replace("{" + "business_hierarchy_id" + "}", _bzz_urlize_path_param(businessHierarchyId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "brand_accounts/update.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = brandAccountUpdate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = BrandAccount.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func brand_accounts/update_threaded(
	# brandAccountId: String = ""   Eg: brandAccountId_example
	brandAccountId: String,
	# businessHierarchyId: String = ""   Eg: businessHierarchyId_example
	# business hierarchy node id
	businessHierarchyId: String,
	# brandAccountUpdate: BrandAccountUpdate
	brandAccountUpdate: BrandAccountUpdate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "brand_accounts/update")
	bzz_callable.bind(
		brandAccountId,
		businessHierarchyId,
		brandAccountUpdate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteBusinessMembership → DELETE /businesses/{business_id}/members
# Terminate business memberships
#
# Terminate memberships between the specified members and your business.
func delete_business_membership(
	# businessId: String = ""   Eg: businessId_example
	# Business id
	businessId: String,
	# deleteBusinessMembershipBody: DeleteBusinessMembershipBody
	deleteBusinessMembershipBody: DeleteBusinessMembershipBody,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "delete_business_membership.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "delete_business_membership.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "delete_business_membership.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/members".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "delete_business_membership.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = deleteBusinessMembershipBody

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = DeleteBusinessMembership200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_business_membership_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Business id
	businessId: String,
	# deleteBusinessMembershipBody: DeleteBusinessMembershipBody
	deleteBusinessMembershipBody: DeleteBusinessMembershipBody,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_business_membership")
	bzz_callable.bind(
		businessId,
		deleteBusinessMembershipBody,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation deleteBusinessPartners → DELETE /businesses/{business_id}/partners
# Terminate business partnerships
#
# Terminate partnerships between the specified partners and your business. Note: You may only batch terminate partners of the same partner type.
func delete_business_partners(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# deleteBusinessPartnersDelete: DeleteBusinessPartnersDelete
	deleteBusinessPartnersDelete: DeleteBusinessPartnersDelete,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "delete_business_partners.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "delete_business_partners.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "delete_business_partners.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/partners".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "delete_business_partners.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = deleteBusinessPartnersDelete

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = DeleteBusinessPartners.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func delete_business_partners_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# deleteBusinessPartnersDelete: DeleteBusinessPartnersDelete
	deleteBusinessPartnersDelete: DeleteBusinessPartnersDelete,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "delete_business_partners")
	bzz_callable.bind(
		businessId,
		deleteBusinessPartnersDelete,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation get/businessEmployers → GET /businesses/employers
# List business employers for user
#
# Get all of the viewing user's business employers.
func get/business_employers(
	# assetsSummary: bool = true   Eg: true
	# Include assets summary in the response if this is true. Defaults to true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = true,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "get/business_employers.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "get/business_employers.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/employers"

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["assets_summary"] = assetsSummary
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = GetBusinessEmployers200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get/business_employers_threaded(
	# assetsSummary: bool = true   Eg: true
	# Include assets summary in the response if this is true. Defaults to true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = true,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get/business_employers")
	bzz_callable.bind(
		assetsSummary,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation get/businessMembers → GET /businesses/{business_id}/members
# Get business members
#
# Get all members of the specified business. The return response will include the member's business_role and assets they have access to if assets_summary=TRUE
func get/business_members(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# fetchSystemUsers: bool = false   Eg: true
	# Fetches system users if True. Fetches regular user employees if False.
	fetchSystemUsers = false,
	# assetsSummary: bool = false   Eg: true
	# Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = false,
	# businessRoles: Array
	# A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned.
	businessRoles = null,
	# memberIds: String = ""   Eg: memberIds_example
	# A list of business members ids separated by comma.
	memberIds = "",
	# startIndex: int = 0   Eg: 56
	# An index to start fetching the results from. Only the results starting from this index will be returned.
	startIndex = 0,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "get/business_members.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "get/business_members.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "get/business_members.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `memberIds` constraints
	if (memberIds is String) and memberIds.length() > 500:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "get/business_members.param.validation.max_length"
		error.message = "Invalid length for `memberIds`, must be smaller than or equal to 500."
		on_failure.call(error)
		return

	# Validate param `startIndex` constraints
	if startIndex < 0:
		var error := ApiError.new()
		error.identifier = "get/business_members.param.validation.minimum"
		error.message = "Invalid value for `startIndex`, must be greater than or equal to 0."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "get/business_members.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "get/business_members.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/members".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["fetch_system_users"] = fetchSystemUsers
	bzz_query["assets_summary"] = assetsSummary
	bzz_query["business_roles"] = businessRoles
	bzz_query["member_ids"] = memberIds
	bzz_query["start_index"] = startIndex
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = GetBusinessEmployers200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get/business_members_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# fetchSystemUsers: bool = false   Eg: true
	# Fetches system users if True. Fetches regular user employees if False.
	fetchSystemUsers = false,
	# assetsSummary: bool = false   Eg: true
	# Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = false,
	# businessRoles: Array
	# A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned.
	businessRoles = null,
	# memberIds: String = ""   Eg: memberIds_example
	# A list of business members ids separated by comma.
	memberIds = "",
	# startIndex: int = 0   Eg: 56
	# An index to start fetching the results from. Only the results starting from this index will be returned.
	startIndex = 0,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get/business_members")
	bzz_callable.bind(
		businessId,
		fetchSystemUsers,
		assetsSummary,
		businessRoles,
		memberIds,
		startIndex,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation get/businessPartners → GET /businesses/{business_id}/partners
# Get business partners
#
# Get all partners of the specified business.  If the assets_summary=TRUE and: - partner_type=INTERNAL, the business assets returned are your business assets the partner has access to. - partner_type=EXTERNAL, the business assets returned are your partner's business assets the partner has granted you   access to.
func get/business_partners(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# assetsSummary: bool = false   Eg: true
	# Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = false,
	# partnerType: PartnerType
	# Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets. If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
	partnerType = null,
	# partnerIds: String = ""   Eg: partnerIds_example
	# A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned.
	partnerIds = "",
	# startIndex: int = 0   Eg: 56
	# An index to start fetching the results from. Only the results starting from this index will be returned.
	startIndex = 0,
	# sortAscending: bool   Eg: true
	# Sort ascending.
	sortAscending = null,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "get/business_partners.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "get/business_partners.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "get/business_partners.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `partnerIds` constraints
	if (partnerIds is String) and partnerIds.length() > 500:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "get/business_partners.param.validation.max_length"
		error.message = "Invalid length for `partnerIds`, must be smaller than or equal to 500."
		on_failure.call(error)
		return

	# Validate param `startIndex` constraints
	if startIndex < 0:
		var error := ApiError.new()
		error.identifier = "get/business_partners.param.validation.minimum"
		error.message = "Invalid value for `startIndex`, must be greater than or equal to 0."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "get/business_partners.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "get/business_partners.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/partners".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["assets_summary"] = assetsSummary
	bzz_query["partner_type"] = partnerType
	bzz_query["partner_ids"] = partnerIds
	bzz_query["start_index"] = startIndex
	bzz_query["sort_ascending"] = sortAscending
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = GetBusinessEmployers200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func get/business_partners_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# assetsSummary: bool = false   Eg: true
	# Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
	assetsSummary = false,
	# partnerType: PartnerType
	# Specifies whether to fetch internal or external (shared) partners. If partner_type=INTERNAL, the asset being queried is for accesses the partner has to your business assets. If partner_type=EXTERNAL, the asset being queried is for the accesses you have to the partner's business asset.
	partnerType = null,
	# partnerIds: String = ""   Eg: partnerIds_example
	# A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned.
	partnerIds = "",
	# startIndex: int = 0   Eg: 56
	# An index to start fetching the results from. Only the results starting from this index will be returned.
	startIndex = 0,
	# sortAscending: bool   Eg: true
	# Sort ascending.
	sortAscending = null,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "get/business_partners")
	bzz_callable.bind(
		businessId,
		assetsSummary,
		partnerType,
		partnerIds,
		startIndex,
		sortAscending,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation systemUser/update → PATCH /businesses/{business_id}/system_users/{system_user_id}
# Update a system user information.
#
# Update a system user information such as name.
func system_user/update(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# systemUserId: String = ""   Eg: systemUserId_example
	# Unique identifier of a system user.
	systemUserId: String,
	# systemUserUpdateWithRequiredBody: SystemUserUpdateWithRequiredBody
	systemUserUpdateWithRequiredBody: SystemUserUpdateWithRequiredBody,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "system_user/update.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "system_user/update.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "system_user/update.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `systemUserId` constraints
	if (systemUserId is String) and systemUserId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "system_user/update.param.validation.max_length"
		error.message = "Invalid length for `systemUserId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (systemUserId is String) and systemUserId.length() < 1:
		var error := ApiError.new()
		error.identifier = "system_user/update.param.validation.min_length"
		error.message = "Invalid length for `systemUserId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_systemUserId_regex := RegEx.new()
	bzz_systemUserId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_systemUserId_regex.search(str(systemUserId)):
		var error := ApiError.new()
		error.identifier = "system_user/update.param.validation.pattern"
		error.message = "Invalid value for `systemUserId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PATCH")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/system_users/{system_user_id}".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId)).replace("{" + "system_user_id" + "}", _bzz_urlize_path_param(systemUserId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "system_user/update.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = systemUserUpdateWithRequiredBody

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func system_user/update_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Unique identifier of the requesting business.
	businessId: String,
	# systemUserId: String = ""   Eg: systemUserId_example
	# Unique identifier of a system user.
	systemUserId: String,
	# systemUserUpdateWithRequiredBody: SystemUserUpdateWithRequiredBody
	systemUserUpdateWithRequiredBody: SystemUserUpdateWithRequiredBody,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "system_user/update")
	bzz_callable.bind(
		businessId,
		systemUserId,
		systemUserUpdateWithRequiredBody,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation update/businessMemberships → PATCH /businesses/{business_id}/members
# Update member's business role
#
# Update a member's business role within the business.
func update/business_memberships(
	# businessId: String = ""   Eg: businessId_example
	# Business id
	businessId: String,
	# businessMembershipMember: Array
	businessMembershipMember: Array,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `businessId` constraints
	if (businessId is String) and businessId.length() > 20:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "update/business_memberships.param.validation.max_length"
		error.message = "Invalid length for `businessId`, must be smaller than or equal to 20."
		on_failure.call(error)
		return
	if (businessId is String) and businessId.length() < 1:
		var error := ApiError.new()
		error.identifier = "update/business_memberships.param.validation.min_length"
		error.message = "Invalid length for `businessId`, must be greater than or equal to 1."
		on_failure.call(error)
		return
	var bzz_businessId_regex := RegEx.new()
	bzz_businessId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_businessId_regex.search(str(businessId)):
		var error := ApiError.new()
		error.identifier = "update/business_memberships.param.validation.pattern"
		error.message = "Invalid value for `businessId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `businessMembershipMember` constraints
	if (businessMembershipMember is Array) and businessMembershipMember.size() < 1:
		var error := ApiError.new()
		error.identifier = "update/business_memberships.param.validation.min_items"
		error.message = "Invalid array size for `businessMembershipMember`, must hold at least 1 elements."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PATCH")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/businesses/{business_id}/members".replace("{" + "business_id" + "}", _bzz_urlize_path_param(businessId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "update/business_memberships.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = businessMembershipMember

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = UpdateBusinessMembershipsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func update/business_memberships_threaded(
	# businessId: String = ""   Eg: businessId_example
	# Business id
	businessId: String,
	# businessMembershipMember: Array
	businessMembershipMember: Array,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "update/business_memberships")
	bzz_callable.bind(
		businessId,
		businessMembershipMember,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


