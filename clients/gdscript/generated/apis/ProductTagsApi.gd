extends ApiBee
class_name ProductTagsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API ProductTagsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "ProductTagsApi"


# Operation productTags/bulkAdd → POST /pins/{pin_id}/product_tags
# Add product tags to pin
#
# Add product tags to a pin. Product tags allow you to tag product pins onto a hero pin. - Maximum 24 product tags can be added in a single request. - Duplicate pin_ids in the request will result in an error. - If any product tag fails eligibility check, the entire request fails (all-or-nothing). - If a product tag is already tagged on the pin, it is treated as a no-op success.
func product_tags/bulk_add(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	# productTagsBulkAddRequest: ProductTagsBulkAddRequest
	productTagsBulkAddRequest: ProductTagsBulkAddRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pinId` constraints
	var bzz_pinId_regex := RegEx.new()
	bzz_pinId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_pinId_regex.search(str(pinId)):
		var error := ApiError.new()
		error.identifier = "product_tags/bulk_add.param.validation.pattern"
		error.message = "Invalid value for `pinId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/pins/{pin_id}/product_tags".replace("{" + "pin_id" + "}", _bzz_urlize_path_param(pinId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "product_tags/bulk_add.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = productTagsBulkAddRequest

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ProductTagsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func product_tags/bulk_add_threaded(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	# productTagsBulkAddRequest: ProductTagsBulkAddRequest
	productTagsBulkAddRequest: ProductTagsBulkAddRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "product_tags/bulk_add")
	bzz_callable.bind(
		pinId,
		productTagsBulkAddRequest,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation productTags/bulkDelete → POST /pins/{pin_id}/product_tags/bulk-delete
# Delete product tags from pin
#
# Delete product tags from a pin. - If a product tag is not found on the pin, it is treated as a no-op success.
func product_tags/bulk_delete(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	# productTagsBulkDeleteRequest: ProductTagsBulkDeleteRequest
	productTagsBulkDeleteRequest: ProductTagsBulkDeleteRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pinId` constraints
	var bzz_pinId_regex := RegEx.new()
	bzz_pinId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_pinId_regex.search(str(pinId)):
		var error := ApiError.new()
		error.identifier = "product_tags/bulk_delete.param.validation.pattern"
		error.message = "Invalid value for `pinId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/pins/{pin_id}/product_tags/bulk-delete".replace("{" + "pin_id" + "}", _bzz_urlize_path_param(pinId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "product_tags/bulk_delete.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = productTagsBulkDeleteRequest

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func product_tags/bulk_delete_threaded(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	# productTagsBulkDeleteRequest: ProductTagsBulkDeleteRequest
	productTagsBulkDeleteRequest: ProductTagsBulkDeleteRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "product_tags/bulk_delete")
	bzz_callable.bind(
		pinId,
		productTagsBulkDeleteRequest,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation productTags/list → GET /pins/{pin_id}/product_tags
# Get product tags for pin
#
# Retrieve all product tags for a pin. - Returns an empty array if the pin has no product tags.
func product_tags/list(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `pinId` constraints
	var bzz_pinId_regex := RegEx.new()
	bzz_pinId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_pinId_regex.search(str(pinId)):
		var error := ApiError.new()
		error.identifier = "product_tags/list.param.validation.pattern"
		error.message = "Invalid value for `pinId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/pins/{pin_id}/product_tags".replace("{" + "pin_id" + "}", _bzz_urlize_path_param(pinId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ProductTagsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func product_tags/list_threaded(
	# pinId: String = ""   Eg: pinId_example
	# Unique identifier of the hero pin that will receive product tags.
	pinId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "product_tags/list")
	bzz_callable.bind(
		pinId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


