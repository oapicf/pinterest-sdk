extends ApiBee
class_name ConversionDeletionRequestsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API ConversionDeletionRequestsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "ConversionDeletionRequestsApi"


# Operation conversionDeletionRequest/create → POST /ad_accounts/{ad_account_id}/conversion_deletion_requests
# Create a conversion deletion request
#
# **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Create a request to delete conversion data for a list of user emails and/or EPIKs, limited to the specified `ad_account_id`. After 72 hours the request is processed and submitted to our deletion process. Then the deletion process ensures deletion within a 30 days period, once the request is submitted to the deletion process it cannot be canceled.
func conversion_deletion_request/create(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# conversionDeletionRequestCreate: ConversionDeletionRequestCreate
	conversionDeletionRequestCreate: ConversionDeletionRequestCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/create.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/create.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/conversion_deletion_requests".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/create.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = conversionDeletionRequestCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ConversionDeletionRequest.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func conversion_deletion_request/create_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# conversionDeletionRequestCreate: ConversionDeletionRequestCreate
	conversionDeletionRequestCreate: ConversionDeletionRequestCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "conversion_deletion_request/create")
	bzz_callable.bind(
		adAccountId,
		conversionDeletionRequestCreate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation conversionDeletionRequest/delete → DELETE /ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}
# Delete a conversion deletion request
#
# **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Delete a conversion deletion request from `ad_account_id` with `request_id`. This will cancel the request and prevent it from being processed. This can only be done if the request is in the `PENDING` status and before the 72 hours mark.
func conversion_deletion_request/delete(
	# requestId: String = ""   Eg: requestId_example
	# Unique identifier of the conversion deletion request
	requestId: String,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `requestId` constraints
	if (requestId is String) and requestId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/delete.param.validation.max_length"
		error.message = "Invalid length for `requestId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_requestId_regex := RegEx.new()
	bzz_requestId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_requestId_regex.search(str(requestId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/delete.param.validation.pattern"
		error.message = "Invalid value for `requestId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/delete.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/delete.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("DELETE")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}".replace("{" + "request_id" + "}", _bzz_urlize_path_param(requestId)).replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ConversionDeletionRequest.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func conversion_deletion_request/delete_threaded(
	# requestId: String = ""   Eg: requestId_example
	# Unique identifier of the conversion deletion request
	requestId: String,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "conversion_deletion_request/delete")
	bzz_callable.bind(
		requestId,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation conversionDeletionRequest/get → GET /ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}
# Get a single conversion deletion request
#
# **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Get a single conversion deletion request from `ad_account_id` with `request_id`.
func conversion_deletion_request/get(
	# requestId: String = ""   Eg: requestId_example
	# Unique identifier of the conversion deletion request
	requestId: String,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `requestId` constraints
	if (requestId is String) and requestId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/get.param.validation.max_length"
		error.message = "Invalid length for `requestId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_requestId_regex := RegEx.new()
	bzz_requestId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_requestId_regex.search(str(requestId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/get.param.validation.pattern"
		error.message = "Invalid value for `requestId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/get.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/get.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}".replace("{" + "request_id" + "}", _bzz_urlize_path_param(requestId)).replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ConversionDeletionRequest.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func conversion_deletion_request/get_threaded(
	# requestId: String = ""   Eg: requestId_example
	# Unique identifier of the conversion deletion request
	requestId: String,
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "conversion_deletion_request/get")
	bzz_callable.bind(
		requestId,
		adAccountId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation conversionDeletionRequest/list → GET /ad_accounts/{ad_account_id}/conversion_deletion_requests
# List conversion deletion requests
#
# **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Get a list of the conversion deletion requests for the specified `ad_account_id`.
func conversion_deletion_request/list(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	# order: PinterestLibPaginationOrder
	# The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
	order = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "conversion_deletion_request/list.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/list.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/list.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "conversion_deletion_request/list.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/conversion_deletion_requests".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize
	bzz_query["order"] = order

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = ConversionDeletionRequestList200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func conversion_deletion_request/list_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	# order: PinterestLibPaginationOrder
	# The order in which to sort the items returned: \"ASCENDING\" or \"DESCENDING\" by ID. Note that higher-value IDs are associated with more-recently added items.
	order = null,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "conversion_deletion_request/list")
	bzz_callable.bind(
		adAccountId,
		bookmark,
		pageSize,
		order,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


