extends ApiBee
class_name LabelsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API LabelsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "LabelsApi"


# Operation labels/apply → POST /ad_accounts/{ad_account_id}/labels/{label_id}/apply
# Apply label to entity
#
#   [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)    Apply a label to one or more campaigns.   Future releases may support labels for other [entities](/docs/key-concepts/pinterest-entities/) in addition to campaigns.   Currently, you can apply **brand** and **custom** labels. Future releases will provide more options.    **Note:** You can only apply one brand label to a campaign. You can apply up to 30 custom labels to a campaign.
func labels/apply(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# labelId: String = ""   Eg: labelId_example
	# Label ID.
	labelId: String,
	# labeledEntitiesCreate: LabeledEntitiesCreate
	labeledEntitiesCreate: LabeledEntitiesCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "labels/apply.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "labels/apply.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/labels/{label_id}/apply".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId)).replace("{" + "label_id" + "}", _bzz_urlize_path_param(labelId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "labels/apply.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = labeledEntitiesCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LabeledEntities.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func labels/apply_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# labelId: String = ""   Eg: labelId_example
	# Label ID.
	labelId: String,
	# labeledEntitiesCreate: LabeledEntitiesCreate
	labeledEntitiesCreate: LabeledEntitiesCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "labels/apply")
	bzz_callable.bind(
		adAccountId,
		labelId,
		labeledEntitiesCreate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation labels/create → POST /ad_accounts/{ad_account_id}/labels
# Create labels
#
# [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  Apply one or more labels to a campaign. Future releases may support labels for other [entities](/docs/key-concepts/pinterest-entities/). Currently, you can apply brand and custom labels. Future releases will provide more options.  **Note:** You can only apply one brand label to a campaign. You can apply 30 custom labels to a campaign.
func labels/create(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# labelCreateRequest: LabelCreateRequest
	labelCreateRequest: LabelCreateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "labels/create.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "labels/create.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/labels".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "labels/create.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = labelCreateRequest

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LabelsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func labels/create_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# labelCreateRequest: LabelCreateRequest
	labelCreateRequest: LabelCreateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "labels/create")
	bzz_callable.bind(
		adAccountId,
		labelCreateRequest,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation labels/list → GET /ad_accounts/{ad_account_id}/labels
# List labels
#
# [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  See a list of labels for assets that your account owns, and filter the list by different criteria. If no filter is provided, it will default to labels associated with the ad account id.
func labels/list(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# campaignIds: Array
	# List of Campaign Ids to use to filter the results.
	campaignIds = null,
	# labelIds: Array
	# List of Label Ids to use to filter the results.
	labelIds = null,
	# entityStatuses: Array = ["ACTIVE"]
	# Label entity status
	entityStatuses = ["ACTIVE"],
	# labelTypes: Array = ["BRAND","CUSTOM"]
	# Label type.
	labelTypes = ["BRAND","CUSTOM"],
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "labels/list.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `campaignIds` constraints
	if (campaignIds is Array) and campaignIds.size() > 250:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.max_items"
		error.message = "Invalid array size for `campaignIds`, must hold at most 250 elements."
		on_failure.call(error)
		return
	if (campaignIds is Array) and campaignIds.size() < 1:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.min_items"
		error.message = "Invalid array size for `campaignIds`, must hold at least 1 elements."
		on_failure.call(error)
		return

	# Validate param `labelIds` constraints
	if (labelIds is Array) and labelIds.size() > 250:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.max_items"
		error.message = "Invalid array size for `labelIds`, must hold at most 250 elements."
		on_failure.call(error)
		return
	if (labelIds is Array) and labelIds.size() < 1:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.min_items"
		error.message = "Invalid array size for `labelIds`, must hold at least 1 elements."
		on_failure.call(error)
		return

	# Validate param `pageSize` constraints
	if pageSize > 250:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.maximum"
		error.message = "Invalid value for `pageSize`, must be smaller than or equal to 250."
		on_failure.call(error)
		return
	if pageSize < 1:
		var error := ApiError.new()
		error.identifier = "labels/list.param.validation.minimum"
		error.message = "Invalid value for `pageSize`, must be greater than or equal to 1."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/labels".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()
	bzz_query["campaign_ids"] = campaignIds
	bzz_query["label_ids"] = labelIds
	bzz_query["entity_statuses"] = entityStatuses
	bzz_query["label_types"] = labelTypes
	bzz_query["bookmark"] = bookmark
	bzz_query["page_size"] = pageSize

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LabelsList200Response.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func labels/list_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# campaignIds: Array
	# List of Campaign Ids to use to filter the results.
	campaignIds = null,
	# labelIds: Array
	# List of Label Ids to use to filter the results.
	labelIds = null,
	# entityStatuses: Array = ["ACTIVE"]
	# Label entity status
	entityStatuses = ["ACTIVE"],
	# labelTypes: Array = ["BRAND","CUSTOM"]
	# Label type.
	labelTypes = ["BRAND","CUSTOM"],
	# bookmark: String = ""   Eg: bookmark_example
	# Cursor used to fetch the next page of items
	bookmark = "",
	# pageSize: int = 25   Eg: 56
	# Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
	pageSize = 25,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "labels/list")
	bzz_callable.bind(
		adAccountId,
		campaignIds,
		labelIds,
		entityStatuses,
		labelTypes,
		bookmark,
		pageSize,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation labels/remove → POST /ad_accounts/{ad_account_id}/labels/{label_id}/remove
# Remove label from entities
#
#   [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)    Remove a label from one or more entities.
func labels/remove(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# labelId: String = ""   Eg: labelId_example
	# Label ID.
	labelId: String,
	# labeledEntitiesCreate: LabeledEntitiesCreate
	labeledEntitiesCreate: LabeledEntitiesCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "labels/remove.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "labels/remove.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/labels/{label_id}/remove".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId)).replace("{" + "label_id" + "}", _bzz_urlize_path_param(labelId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "labels/remove.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = labeledEntitiesCreate

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LabeledEntities.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func labels/remove_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# labelId: String = ""   Eg: labelId_example
	# Label ID.
	labelId: String,
	# labeledEntitiesCreate: LabeledEntitiesCreate
	labeledEntitiesCreate: LabeledEntitiesCreate,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "labels/remove")
	bzz_callable.bind(
		adAccountId,
		labelId,
		labeledEntitiesCreate,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation labels/update → PATCH /ad_accounts/{ad_account_id}/labels
# Update labels
#
# [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  Change the properties of one or more labels.
func labels/update(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# labelUpdateRequest: LabelUpdateRequest
	labelUpdateRequest: LabelUpdateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "labels/update.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "labels/update.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("PATCH")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/labels".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "labels/update.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = labelUpdateRequest

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = LabelsResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func labels/update_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	# Unique identifier of an ad account.
	adAccountId: String,
	# labelUpdateRequest: LabelUpdateRequest
	labelUpdateRequest: LabelUpdateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "labels/update")
	bzz_callable.bind(
		adAccountId,
		labelUpdateRequest,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


