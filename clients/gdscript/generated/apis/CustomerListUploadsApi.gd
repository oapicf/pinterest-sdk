extends ApiBee
class_name CustomerListUploadsApi

# THIS FILE WAS AUTOMATICALLY GENERATED by the OpenAPI Generator project.
# For more information on how to customize templates, see:
# https://openapi-generator.tech
# https://github.com/OpenAPITools/openapi-generator/tree/master/modules/openapi-generator/src/main/resources/gdscript
# The OpenAPI Generator Community, © Public Domain, 2022

# API CustomerListUploadsApi
# Instantiate this object and use it to make requests to the API.

func _bzz_get_api_name() -> String:
	return "CustomerListUploadsApi"


# Operation customerListUploads/create → POST /ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads
# Create customer list upload
#
# Create a customer list upload request for multipart S3 upload.  Note: Each part must be at least 5mb; however the last part can be any size greater than 0. Clients with smaller files can request a single part count. This minimal part size restriction is defined by the AWS S3 API.  **Please review the [update customer list endpoint](/docs/api/v5/customer_lists-update/) documentation for additional information.**
func customer_list_uploads/create(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadCreateRequest: CustomerListUploadCreateRequest
	customerListUploadCreateRequest: CustomerListUploadCreateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/create.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/create.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `customerListId` constraints
	if (customerListId is String) and customerListId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/create.param.validation.max_length"
		error.message = "Invalid length for `customerListId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_customerListId_regex := RegEx.new()
	bzz_customerListId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_customerListId_regex.search(str(customerListId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/create.param.validation.pattern"
		error.message = "Invalid value for `customerListId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId)).replace("{" + "customer_list_id" + "}", _bzz_urlize_path_param(customerListId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_consumable_by_server := ['application/json']
	var bzz_found_producible_mime := false
	for bzz_mime in BZZ_PRODUCIBLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_consumable_by_server:
			bzz_headers["Content-Type"] = bzz_mime
			bzz_found_producible_mime = true
			break
	if not bzz_found_producible_mime:
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/create.headers.content_type"
		error.message = "That endpoint only accepts %s as content type(s) and none are supported by this client."
		on_failure.call(error)
		return
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null
	bzz_body = customerListUploadCreateRequest

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CustomerListUploadCreateResponse.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func customer_list_uploads/create_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadCreateRequest: CustomerListUploadCreateRequest
	customerListUploadCreateRequest: CustomerListUploadCreateRequest,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "customer_list_uploads/create")
	bzz_callable.bind(
		adAccountId,
		customerListId,
		customerListUploadCreateRequest,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation customerListUploads/get → GET /ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}
# Get customer list upload
#
# Get the metadata for a given upload by its ID.
func customer_list_uploads/get(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadId: String = ""   Eg: customerListUploadId_example
	# Customer List Upload ID.
	customerListUploadId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/get.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/get.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `customerListId` constraints
	if (customerListId is String) and customerListId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/get.param.validation.max_length"
		error.message = "Invalid length for `customerListId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_customerListId_regex := RegEx.new()
	bzz_customerListId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_customerListId_regex.search(str(customerListId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/get.param.validation.pattern"
		error.message = "Invalid value for `customerListId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `customerListUploadId` constraints
	var bzz_customerListUploadId_regex := RegEx.new()
	bzz_customerListUploadId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_customerListUploadId_regex.search(str(customerListUploadId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/get.param.validation.pattern"
		error.message = "Invalid value for `customerListUploadId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("GET")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId)).replace("{" + "customer_list_id" + "}", _bzz_urlize_path_param(customerListId)).replace("{" + "customer_list_upload_id" + "}", _bzz_urlize_path_param(customerListUploadId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CustomerListUpload.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func customer_list_uploads/get_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadId: String = ""   Eg: customerListUploadId_example
	# Customer List Upload ID.
	customerListUploadId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "customer_list_uploads/get")
	bzz_callable.bind(
		adAccountId,
		customerListId,
		customerListUploadId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


# Operation customerListUploads/run → POST /ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}/run
# Run customer list upload
#
# Begin processing a customer list upload.
func customer_list_uploads/run(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadId: String = ""   Eg: customerListUploadId_example
	# Customer List Upload ID.
	customerListUploadId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
):

	# Validate param `adAccountId` constraints
	if (adAccountId is String) and adAccountId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/run.param.validation.max_length"
		error.message = "Invalid length for `adAccountId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_adAccountId_regex := RegEx.new()
	bzz_adAccountId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_adAccountId_regex.search(str(adAccountId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/run.param.validation.pattern"
		error.message = "Invalid value for `adAccountId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `customerListId` constraints
	if (customerListId is String) and customerListId.length() > 18:
		var error := ApiError.new()
		#error.internal_code = ERR_INVALID_PARAMETER
		error.identifier = "customer_list_uploads/run.param.validation.max_length"
		error.message = "Invalid length for `customerListId`, must be smaller than or equal to 18."
		on_failure.call(error)
		return
	var bzz_customerListId_regex := RegEx.new()
	bzz_customerListId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_customerListId_regex.search(str(customerListId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/run.param.validation.pattern"
		error.message = "Invalid value for `customerListId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Validate param `customerListUploadId` constraints
	var bzz_customerListUploadId_regex := RegEx.new()
	bzz_customerListUploadId_regex.compile("/^\\d+$/".trim_prefix('/').trim_suffix('/'))
	if not bzz_customerListUploadId_regex.search(str(customerListUploadId)):
		var error := ApiError.new()
		error.identifier = "customer_list_uploads/run.param.validation.pattern"
		error.message = "Invalid value for `customerListUploadId`, must conform to the pattern `/^\\d+$/`."
		on_failure.call(error)
		return

	# Convert the String HTTP method to a Constant Godot understands
	var bzz_method := self._bzz_convert_http_method("POST")

	# Compute the URL path to the API resource
	var bzz_path := "/v5/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}/run".replace("{" + "ad_account_id" + "}", _bzz_urlize_path_param(adAccountId)).replace("{" + "customer_list_id" + "}", _bzz_urlize_path_param(customerListId)).replace("{" + "customer_list_upload_id" + "}", _bzz_urlize_path_param(customerListUploadId))

	# Collect the headers
	var bzz_headers := Dictionary()
	var bzz_mimes_produced_by_server := ['application/json']
	for bzz_mime in BZZ_CONSUMABLE_CONTENT_TYPES:
		if bzz_mime in bzz_mimes_produced_by_server:
			bzz_headers["Accept"] = bzz_mime
			break

	# Collect the query parameters
	# Note: we do not support multiple values for a single param (for now), nor arrays
	var bzz_query := Dictionary()

	var bzz_body = null

	self._bzz_request(
		bzz_method, bzz_path, bzz_headers, bzz_query, bzz_body,
		func(bzz_response):
			bzz_response.data = CustomerListUpload.bzz_denormalize_single(bzz_response.data)
			on_success.call(bzz_response)
			,
		func(bzz_error):
			on_failure.call(bzz_error)
			,  # ざわ‥
	)


func customer_list_uploads/run_threaded(
	# adAccountId: String = ""   Eg: adAccountId_example
	adAccountId: String,
	# customerListId: String = ""   Eg: customerListId_example
	# Customer list ID.
	customerListId: String,
	# customerListUploadId: String = ""   Eg: customerListUploadId_example
	# Customer List Upload ID.
	customerListUploadId: String,
	on_success: Callable = Callable(),  # func(response: ApiResponse)
	on_failure: Callable = Callable(),  # func(error: ApiError)
) -> Thread:
	var bzz_thread := Thread.new()
	var bzz_callable := Callable(self, "customer_list_uploads/run")
	bzz_callable.bind(
		adAccountId,
		customerListId,
		customerListUploadId,
		on_success,
		on_failure,
	)
	bzz_thread.start(bzz_callable)
	return bzz_thread


