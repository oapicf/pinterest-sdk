// tslint:disable
/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import { BaseAPI, throwIfNullOrUndefined } from '../runtime';
import type { OperationOpts, HttpHeaders } from '../runtime';
import type {
    ConversionAccessToken,
    OauthAccessToken,
    PinterestLibError,
    TokenGrantType,
    TokenTypeHint,
} from '../models';

export interface OauthTokenRequest {
    grantType: TokenGrantType;
    code?: string;
    continuousRefresh?: string;
    redirectUri?: string;
    refreshToken?: string;
    scope?: string;
}

export interface TokenRevokeRequest {
    token: string;
    tokenTypeHint?: TokenTypeHint;
}

/**
 * no description
 */
export class OauthApi extends BaseAPI {

    /**
     * Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
     * Generate OAuth access token for conversion API
     */
    oauthConversionToken(): Observable<ConversionAccessToken>
    oauthConversionToken(opts?: OperationOpts): Observable<AjaxResponse<ConversionAccessToken>>
    oauthConversionToken(opts?: OperationOpts): Observable<ConversionAccessToken | AjaxResponse<ConversionAccessToken>> {
        const headers: HttpHeaders = {
            // oauth required
            ...(this.configuration.accessToken != null
                ? { Authorization: typeof this.configuration.accessToken === 'function'
                    ? this.configuration.accessToken('pinterest_oauth2', ['ads:write'])
                    : this.configuration.accessToken }
                : undefined
            ),
        };

        return this.request<ConversionAccessToken>({
            url: '/oauth/conversion_token',
            method: 'POST',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the `continuous_refresh` parameter to `true` to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 
     * Generate OAuth access token
     */
    oauthToken({ grantType, code, continuousRefresh, redirectUri, refreshToken, scope }: OauthTokenRequest): Observable<OauthAccessToken>
    oauthToken({ grantType, code, continuousRefresh, redirectUri, refreshToken, scope }: OauthTokenRequest, opts?: OperationOpts): Observable<AjaxResponse<OauthAccessToken>>
    oauthToken({ grantType, code, continuousRefresh, redirectUri, refreshToken, scope }: OauthTokenRequest, opts?: OperationOpts): Observable<OauthAccessToken | AjaxResponse<OauthAccessToken>> {
        throwIfNullOrUndefined(grantType, 'grantType', 'oauthToken');

        const headers: HttpHeaders = {
            ...(this.configuration.username != null && this.configuration.password != null ? { Authorization: `Basic ${btoa(this.configuration.username + ':' + this.configuration.password)}` } : undefined),
        };

        const formData = new FormData();
        if (code !== undefined) { formData.append('code', code as any); }
        if (continuousRefresh !== undefined) { formData.append('continuous_refresh', continuousRefresh as any); }
        if (grantType !== undefined) { formData.append('grant_type', grantType as any); }
        if (redirectUri !== undefined) { formData.append('redirect_uri', redirectUri as any); }
        if (refreshToken !== undefined) { formData.append('refresh_token', refreshToken as any); }
        if (scope !== undefined) { formData.append('scope', scope as any); }

        return this.request<OauthAccessToken>({
            url: '/oauth/token',
            method: 'POST',
            headers,
            body: formData,
        }, opts?.responseOpts);
    };

    /**
     * Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
     * Revoke a token
     */
    tokenRevoke({ token, tokenTypeHint }: TokenRevokeRequest): Observable<void>
    tokenRevoke({ token, tokenTypeHint }: TokenRevokeRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    tokenRevoke({ token, tokenTypeHint }: TokenRevokeRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(token, 'token', 'tokenRevoke');

        const headers: HttpHeaders = {
            ...(this.configuration.username != null && this.configuration.password != null ? { Authorization: `Basic ${btoa(this.configuration.username + ':' + this.configuration.password)}` } : undefined),
        };

        const formData = new FormData();
        if (token !== undefined) { formData.append('token', token as any); }
        if (tokenTypeHint !== undefined) { formData.append('token_type_hint', tokenTypeHint as any); }

        return this.request<void>({
            url: '/oauth/token/revoke',
            method: 'POST',
            headers,
            body: formData,
        }, opts?.responseOpts);
    };

}
