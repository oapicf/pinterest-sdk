#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.12.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_audience
import ../models/model_audience_create_custom_request
import ../models/model_audience_create_request
import ../models/model_audience_update_request
import ../models/model_error
import ../models/model_audiences_list_200_response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      when name(stripGenericParams(T.typedesc).typedesc) == name(Table):
        (some(json.to(parseJson(response.body), T.typedesc)), response)
      else:
        (some(marshal.to[T](response.body)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc audiencesCreate*(httpClient: HttpClient, adAccountId: string, audienceCreateRequest: AudienceCreateRequest): (Option[Audience], Response) =
  ## Create audience
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/audiences", $(%audienceCreateRequest))
  constructResult[Audience](response)


proc audiencesCreateCustom*(httpClient: HttpClient, adAccountId: string, audienceCreateCustomRequest: AudienceCreateCustomRequest): (Option[Audience], Response) =
  ## Create custom audience
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/audiences/custom", $(%audienceCreateCustomRequest))
  constructResult[Audience](response)


proc audiencesGet*(httpClient: HttpClient, adAccountId: string, audienceId: string): (Option[Audience], Response) =
  ## Get audience

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audiences/{audience_id}")
  constructResult[Audience](response)


proc audiencesList*(httpClient: HttpClient, adAccountId: string, bookmark: string, order: string, pageSize: int, ownershipType: string): (Option[audiences_list_200_response], Response) =
  ## List audiences
  let query_for_api_call = encodeQuery([
    ("bookmark", $bookmark), # Cursor used to fetch the next page of items
    ("order", $order), # The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. For received audiences, it is sorted by sharing event time. Note that higher-value IDs are associated with more-recently added items.
    ("page_size", $pageSize), # Maximum number of items to include in a single page of the response. See documentation on <a href='/docs/getting-started/pagination/'>Pagination</a> for more information.
    ("ownership_type", $ownershipType), # <strong>This feature is currently in beta and not available to all apps.</strong> Filter audiences by ownership type.
  ])

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audiences" & "?" & query_for_api_call)
  constructResult[audiences_list_200_response](response)


proc audiencesUpdate*(httpClient: HttpClient, adAccountId: string, audienceId: string, audienceUpdateRequest: AudienceUpdateRequest): (Option[Audience], Response) =
  ## Update audience
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/audiences/{audience_id}", $(%audienceUpdateRequest))
  constructResult[Audience](response)

