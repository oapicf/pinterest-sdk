#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_ad_accounts_audience
import ../models/model_ad_accounts_audience_create
import ../models/model_ad_accounts_audience_update
import ../models/model_audience_ownership_type
import ../models/model_pinterest/lib/error
import ../models/model_pinterest/lib/pagination_order
import ../models/model_audiences_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc audiencesCreate*(httpClient: HttpClient, adAccountId: string, adAccountsAudienceCreate: AdAccountsAudienceCreate): (Option[AdAccountsAudience], Response) =
  ## Create audience
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/audiences", $(%adAccountsAudienceCreate))
  constructResult[AdAccountsAudience](response)


proc audiencesGet*(httpClient: HttpClient, audienceId: string, adAccountId: string): (Option[AdAccountsAudience], Response) =
  ## Get audience

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audiences/{audience_id}")
  constructResult[AdAccountsAudience](response)


proc audiencesList*(httpClient: HttpClient, adAccountId: string, bookmark: string, pageSize: int, order: PinterestLibPaginationOrder, ownershipType: AudienceOwnershipType, excludeNca: bool): (Option[audiences_list_200_response], Response) =
  ## List audiences
  var query_params_list: seq[(string, string)] = @[]
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  if $ownershipType != "":
    query_params_list.add(("ownership_type", $ownershipType))
  if $excludeNca != "":
    query_params_list.add(("exclude_nca", $excludeNca))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audiences" & "?" & url_encoded_query_params)
  constructResult[audiences_list_200_response](response)


proc audiencesUpdate*(httpClient: HttpClient, audienceId: string, adAccountId: string, adAccountsAudienceUpdate: AdAccountsAudienceUpdate): (Option[AdAccountsAudience], Response) =
  ## Update audience
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/audiences/{audience_id}", $(%adAccountsAudienceUpdate))
  constructResult[AdAccountsAudience](response)

