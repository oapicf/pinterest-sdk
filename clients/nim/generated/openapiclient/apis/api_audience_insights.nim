#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.12.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_audience_definition_response
import ../models/model_audience_insight_type
import ../models/model_audience_insights_response
import ../models/model_error

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      when name(stripGenericParams(T.typedesc).typedesc) == name(Table):
        (some(json.to(parseJson(response.body), T.typedesc)), response)
      else:
        (some(marshal.to[T](response.body)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc audienceInsightsGet*(httpClient: HttpClient, adAccountId: string, audienceInsightType: AudienceInsightType): (Option[AudienceInsightsResponse], Response) =
  ## Get audience insights
  let query_for_api_call = encodeQuery([
    ("audience_insight_type", $audienceInsightType), # Type of audience insights.
  ])

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audience_insights" & "?" & query_for_api_call)
  constructResult[AudienceInsightsResponse](response)


proc audienceInsightsScopeAndTypeGet*(httpClient: HttpClient, adAccountId: string): (Option[AudienceDefinitionResponse], Response) =
  ## Get audience insights scope and type

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/insights/audiences")
  constructResult[AudienceDefinitionResponse](response)

