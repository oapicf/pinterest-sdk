#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_audience_insight_type
import ../models/model_audience_insights
import ../models/model_pinterest/lib/error
import ../models/model_audience_insights_scope_and_type_get200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc audienceInsightsGet*(httpClient: HttpClient, adAccountId: string, audienceInsightType: AudienceInsightType): (Option[AudienceInsights], Response) =
  ## Get audience insights
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("audience_insight_type", $audienceInsightType))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/audience_insights" & "?" & url_encoded_query_params)
  constructResult[AudienceInsights](response)


proc audienceInsightsScopeAndTypeGet*(httpClient: HttpClient, adAccountId: string): (Option[audience_insights_scope_and_type_get_200_response], Response) =
  ## Get audience insights scope and type

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/insights/audiences")
  constructResult[audience_insights_scope_and_type_get_200_response](response)

