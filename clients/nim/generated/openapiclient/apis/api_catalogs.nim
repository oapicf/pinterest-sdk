#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_catalog
import ../models/model_catalog_create
import ../models/model_catalogs_available_filter_values
import ../models/model_catalogs_locale
import ../models/model_country
import ../models/model_pinterest/lib/error
import ../models/model_catalogs_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc catalogsAvailableFilterValues*(httpClient: HttpClient, catalogId: string, feedId: string, country: Country, language: CatalogsLocale, adAccountId: string): (Option[CatalogsAvailableFilterValues], Response) =
  ## List available filter values
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("catalog_id", $catalogId))
  if $feedId != "":
    query_params_list.add(("feed_id", $feedId))
  if $country != "":
    query_params_list.add(("country", $country))
  if $language != "":
    query_params_list.add(("language", $language))
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/catalogs/available_filter_values" & "?" & url_encoded_query_params)
  constructResult[CatalogsAvailableFilterValues](response)


proc catalogsCreate*(httpClient: HttpClient, catalogCreate: CatalogCreate, adAccountId: string): (Option[Catalog], Response) =
  ## Create catalog
  httpClient.headers["Content-Type"] = "application/json"
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.post(basepath & "/catalogs" & "?" & url_encoded_query_params, $(%catalogCreate))
  constructResult[Catalog](response)


proc catalogsList*(httpClient: HttpClient, adAccountId: string, bookmark: string, pageSize: int): (Option[catalogs_list_200_response], Response) =
  ## List catalogs
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/catalogs" & "?" & url_encoded_query_params)
  constructResult[catalogs_list_200_response](response)

