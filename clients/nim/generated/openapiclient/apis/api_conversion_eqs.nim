#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_event_quality_score
import ../models/model_ingestion_source_options
import ../models/model_lookback_period_options
import ../models/model_pinterest/lib/error
import ../models/model_source_platform_options

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc conversionEqsList*(httpClient: HttpClient, lookbackPeriod: LookbackPeriodOptions, adAccountId: string, sourcePlatform: SourcePlatformOptions, ingestionSource: IngestionSourceOptions): (Option[seq[EventQualityScore]], Response) =
  ## Get event quality score (EQS)
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("lookback_period", $lookbackPeriod))
  if $sourcePlatform != "":
    query_params_list.add(("source_platform", $sourcePlatform))
  if $ingestionSource != "":
    query_params_list.add(("ingestion_source", $ingestionSource))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/conversion_eqs" & "?" & url_encoded_query_params)
  constructResult[seq[EventQualityScore]](response)

