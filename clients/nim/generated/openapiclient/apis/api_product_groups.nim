#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.12.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_error
import ../models/model_ad_accounts_catalogs_product_groups_list_200_response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      when name(stripGenericParams(T.typedesc).typedesc) == name(Table):
        (some(json.to(parseJson(response.body), T.typedesc)), response)
      else:
        (some(marshal.to[T](response.body)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc adAccountsCatalogsProductGroupsList*(httpClient: HttpClient, adAccountId: string, feedProfileId: string): (Option[ad_accounts_catalogs_product_groups_list_200_response], Response) {.deprecated.} =
  ## Get catalog product groups
  let query_for_api_call = encodeQuery([
    ("feed_profile_id", $feedProfileId), # The feed profile id whose catalog product groups we want to return.
  ])

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/product_groups/catalogs" & "?" & query_for_api_call)
  constructResult[ad_accounts_catalogs_product_groups_list_200_response](response)

