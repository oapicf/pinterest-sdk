#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_catalogs_items_batch
import ../models/model_catalogs_items_batch_post_request
import ../models/model_catalogs_items_request
import ../models/model_pinterest/lib/error
import ../models/model_items_post200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc itemsBatchGet*(httpClient: HttpClient, batchId: string, adAccountId: string): (Option[CatalogsItemsBatch], Response) =
  ## Get item batch status
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/catalogs/items/batch/{batch_id}" & "?" & url_encoded_query_params)
  constructResult[CatalogsItemsBatch](response)


proc itemsBatchPost*(httpClient: HttpClient, catalogsItemsBatchPostRequest: CatalogsItemsBatchPostRequest, adAccountId: string): (Option[CatalogsItemsBatch], Response) =
  ## Operate on item batch
  httpClient.headers["Content-Type"] = "application/json"
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.post(basepath & "/catalogs/items/batch" & "?" & url_encoded_query_params, $(%catalogsItemsBatchPostRequest))
  constructResult[CatalogsItemsBatch](response)


proc itemsPost*(httpClient: HttpClient, catalogsItemsRequest: CatalogsItemsRequest, adAccountId: string): (Option[items_post_200_response], Response) =
  ## Get catalogs items (POST)
  httpClient.headers["Content-Type"] = "application/json"
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.post(basepath & "/catalogs/items" & "?" & url_encoded_query_params, $(%catalogsItemsRequest))
  constructResult[items_post_200_response](response)

