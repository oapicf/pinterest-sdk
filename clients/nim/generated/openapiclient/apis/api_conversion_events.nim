#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_conversion_events
import ../models/model_conversion_events_create
import ../models/model_detailed_error
import ../models/model_pinterest/lib/error

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc eventsCreate*(httpClient: HttpClient, adAccountId: string, conversionEventsCreate: ConversionEventsCreate, test: bool): (Option[ConversionEvents], Response) =
  ## Send conversions
  httpClient.headers["Content-Type"] = "application/json"
  var query_params_list: seq[(string, string)] = @[]
  if $test != "":
    query_params_list.add(("test", $test))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/events" & "?" & url_encoded_query_params, $(%conversionEventsCreate))
  constructResult[ConversionEvents](response)

