#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_book_closed
import ../models/model_pinterest/lib/error
import ../models/model_public_targeting_type
import ../models/model_report_type
import ../models/model_single_interest_targeting_option
import ../models/model_ad_account_countries_get200response
import ../models/model_delivery_metrics_get200response
import ../models/model_object

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc adAccountCountriesGet*(httpClient: HttpClient): (Option[ad_account_countries_get_200_response], Response) =
  ## Get ad accounts countries

  let response = httpClient.get(basepath & "/resources/ad_account_countries")
  constructResult[ad_account_countries_get_200_response](response)


proc deliveryMetricsGet*(httpClient: HttpClient, reportType: ReportType): (Option[delivery_metrics_get_200_response], Response) =
  ## Get available metrics' definitions
  var query_params_list: seq[(string, string)] = @[]
  if $reportType != "":
    query_params_list.add(("report_type", $reportType))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/resources/delivery_metrics" & "?" & url_encoded_query_params)
  constructResult[delivery_metrics_get_200_response](response)


proc interestTargetingOptionsGet*(httpClient: HttpClient, interestId: string): (Option[SingleInterestTargetingOption], Response) =
  ## Get interest details

  let response = httpClient.get(basepath & fmt"/resources/targeting/interests/{interest_id}")
  constructResult[SingleInterestTargetingOption](response)


proc leadFormQuestionsGet*(httpClient: HttpClient): Response =
  ## Get lead form questions
  httpClient.get(basepath & "/resources/lead_form_questions")



proc metricsReadyStateGet*(httpClient: HttpClient, date: string): (Option[BookClosed], Response) =
  ## Get metrics ready state
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("date", $date))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/resources/metrics_ready_state" & "?" & url_encoded_query_params)
  constructResult[BookClosed](response)


proc targetingOptionsGet*(httpClient: HttpClient, targetingType: PublicTargetingType, adAccountId: string, clientId: string, oauthSignature: string, timestamp: string): (Option[seq[JsonNode]], Response) =
  ## Get targeting options
  var query_params_list: seq[(string, string)] = @[]
  if $adAccountId != "":
    query_params_list.add(("ad_account_id", $adAccountId))
  if $clientId != "":
    query_params_list.add(("client_id", $clientId))
  if $oauthSignature != "":
    query_params_list.add(("oauth_signature", $oauthSignature))
  if $timestamp != "":
    query_params_list.add(("timestamp", $timestamp))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/resources/targeting/{targeting_type}" & "?" & url_encoded_query_params)
  constructResult[seq[JsonNode]](response)

