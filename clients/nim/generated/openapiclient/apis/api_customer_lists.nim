#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_customer_list
import ../models/model_customer_list_request
import ../models/model_customer_list_update_request
import ../models/model_error
import ../models/model_customer_lists_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc customerListsCreate*(httpClient: HttpClient, adAccountId: string, customerListRequest: CustomerListRequest): (Option[CustomerList], Response) =
  ## Create customer lists
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists", $(%customerListRequest))
  constructResult[CustomerList](response)


proc customerListsGet*(httpClient: HttpClient, adAccountId: string, customerListId: string): (Option[CustomerList], Response) =
  ## Get customer list

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}")
  constructResult[CustomerList](response)


proc customerListsList*(httpClient: HttpClient, adAccountId: string, pageSize: int, order: string, bookmark: string): (Option[customer_lists_list_200_response], Response) =
  ## Get customer lists
  var query_params_list: seq[(string, string)] = @[]
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists" & "?" & url_encoded_query_params)
  constructResult[customer_lists_list_200_response](response)


proc customerListsUpdate*(httpClient: HttpClient, adAccountId: string, customerListId: string, customerListUpdateRequest: CustomerListUpdateRequest): (Option[CustomerList], Response) =
  ## Update customer list
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}", $(%customerListUpdateRequest))
  constructResult[CustomerList](response)

