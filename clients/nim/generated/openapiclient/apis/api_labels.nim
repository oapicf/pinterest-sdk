#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_label_create_request
import ../models/model_label_update_request
import ../models/model_labeled_entities
import ../models/model_labeled_entities_create
import ../models/model_labels_response
import ../models/model_pinterest/lib/error
import ../models/model_query_label_entity_statuses_items
import ../models/model_query_label_types_items
import ../models/model_labels_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc labelsApply*(httpClient: HttpClient, adAccountId: string, labelId: string, labeledEntitiesCreate: LabeledEntitiesCreate): (Option[LabeledEntities], Response) =
  ## Apply label to entity
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/labels/{label_id}/apply", $(%labeledEntitiesCreate))
  constructResult[LabeledEntities](response)


proc labelsCreate*(httpClient: HttpClient, adAccountId: string, labelCreateRequest: LabelCreateRequest): (Option[LabelsResponse], Response) =
  ## Create labels
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/labels", $(%labelCreateRequest))
  constructResult[LabelsResponse](response)


proc labelsList*(httpClient: HttpClient, adAccountId: string, campaignIds: seq[string], labelIds: seq[string], entityStatuses: seq[QueryLabelEntityStatusesItems], labelTypes: seq[QueryLabelTypesItems], bookmark: string, pageSize: int): (Option[labels_list_200_response], Response) =
  ## List labels
  var query_params_list: seq[(string, string)] = @[]
  if campaignIds.len > 0:
    query_params_list.add(("campaign_ids", $campaignIds.join(",")))
  if labelIds.len > 0:
    query_params_list.add(("label_ids", $labelIds.join(",")))
  if entityStatuses.len > 0:
    query_params_list.add(("entity_statuses", $entityStatuses.join(",")))
  if labelTypes.len > 0:
    query_params_list.add(("label_types", $labelTypes.join(",")))
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/labels" & "?" & url_encoded_query_params)
  constructResult[labels_list_200_response](response)


proc labelsRemove*(httpClient: HttpClient, adAccountId: string, labelId: string, labeledEntitiesCreate: LabeledEntitiesCreate): (Option[LabeledEntities], Response) =
  ## Remove label from entities
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/labels/{label_id}/remove", $(%labeledEntitiesCreate))
  constructResult[LabeledEntities](response)


proc labelsUpdate*(httpClient: HttpClient, adAccountId: string, labelUpdateRequest: LabelUpdateRequest): (Option[LabelsResponse], Response) =
  ## Update labels
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/labels", $(%labelUpdateRequest))
  constructResult[LabelsResponse](response)

