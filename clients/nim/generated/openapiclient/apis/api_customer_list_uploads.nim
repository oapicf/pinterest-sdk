#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_customer_list_upload
import ../models/model_customer_list_upload_create_request
import ../models/model_customer_list_upload_create_response
import ../models/model_pinterest/lib/error

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc customerListUploadsCreate*(httpClient: HttpClient, adAccountId: string, customerListId: string, customerListUploadCreateRequest: CustomerListUploadCreateRequest): (Option[CustomerListUploadCreateResponse], Response) =
  ## Create customer list upload
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads", $(%customerListUploadCreateRequest))
  constructResult[CustomerListUploadCreateResponse](response)


proc customerListUploadsGet*(httpClient: HttpClient, adAccountId: string, customerListId: string, customerListUploadId: string): (Option[CustomerListUpload], Response) =
  ## Get customer list upload

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}")
  constructResult[CustomerListUpload](response)


proc customerListUploadsRun*(httpClient: HttpClient, adAccountId: string, customerListId: string, customerListUploadId: string): (Option[CustomerListUpload], Response) =
  ## Run customer list upload

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/customer_lists/{customer_list_id}/uploads/{customer_list_upload_id}/run")
  constructResult[CustomerListUpload](response)

