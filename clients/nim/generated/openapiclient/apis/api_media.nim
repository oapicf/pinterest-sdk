#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_error
import ../models/model_media_upload
import ../models/model_media_upload_details
import ../models/model_media_upload_request
import ../models/model_media_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc mediaCreate*(httpClient: HttpClient, mediaUploadRequest: MediaUploadRequest): (Option[MediaUpload], Response) =
  ## Register media upload
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/media", $(%mediaUploadRequest))
  constructResult[MediaUpload](response)


proc mediaGet*(httpClient: HttpClient, mediaId: string): (Option[MediaUploadDetails], Response) =
  ## Get media upload details

  let response = httpClient.get(basepath & fmt"/media/{media_id}")
  constructResult[MediaUploadDetails](response)


proc mediaList*(httpClient: HttpClient, bookmark: string, pageSize: int): (Option[media_list_200_response], Response) =
  ## List media uploads
  var query_params_list: seq[(string, string)] = @[]
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/media" & "?" & url_encoded_query_params)
  constructResult[media_list_200_response](response)

