#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_age_trends_bucket
import ../models/model_featured_trend
import ../models/model_gender_bucket
import ../models/model_interests_enum
import ../models/model_pinterest/lib/error
import ../models/model_product_categories_engagement_type
import ../models/model_product_category_detail_lookback_window
import ../models/model_product_category_details
import ../models/model_product_category_enum
import ../models/model_product_category_region
import ../models/model_trending_product_category
import ../models/model_trends_editorial
import ../models/model_vertical_product_category

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc trendsEditorialArticlesList*(httpClient: HttpClient, region: ProductCategoryRegion): (Option[seq[TrendsEditorial]], Response) =
  ## Returns editorial articles for a given region
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("region", $region))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/trends/editorial_articles" & "?" & url_encoded_query_params)
  constructResult[seq[TrendsEditorial]](response)


proc trendsFeaturedTopicsList*(httpClient: HttpClient, region: ProductCategoryRegion, interest: InterestsEnum): (Option[seq[FeaturedTrend]], Response) =
  ## Get featured topics
  var query_params_list: seq[(string, string)] = @[]
  if $interest != "":
    query_params_list.add(("interest", $interest))
  query_params_list.add(("region", $region))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/trends/topics/featured" & "?" & url_encoded_query_params)
  constructResult[seq[FeaturedTrend]](response)


proc trendsProductCategoriesDetailsList*(httpClient: HttpClient, productCategories: seq[ProductCategoryEnum], region: ProductCategoryRegion, lookbackWindow: ProductCategoryDetailLookbackWindow, engagementType: ProductCategoriesEngagementType): (Option[seq[ProductCategoryDetails]], Response) =
  ## Get product category details
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("product_categories", $productCategories.join(",")))
  query_params_list.add(("region", $region))
  if $lookbackWindow != "":
    query_params_list.add(("lookback_window", $lookbackWindow))
  if $engagementType != "":
    query_params_list.add(("engagement_type", $engagementType))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/trends/product_categories/details" & "?" & url_encoded_query_params)
  constructResult[seq[ProductCategoryDetails]](response)


proc trendsProductCategoriesTrendingList*(httpClient: HttpClient, region: ProductCategoryRegion, verticals: seq[VerticalProductCategory], ages: seq[AgeTrendsBucket], genders: seq[GenderBucket], engagementType: ProductCategoriesEngagementType): (Option[seq[TrendingProductCategory]], Response) =
  ## Get a list of growing Shopping Product Categories
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("region", $region))
  if verticals.len > 0:
    query_params_list.add(("verticals", $verticals.join(",")))
  if ages.len > 0:
    query_params_list.add(("ages", $ages.join(",")))
  if genders.len > 0:
    query_params_list.add(("genders", $genders.join(",")))
  if $engagementType != "":
    query_params_list.add(("engagement_type", $engagementType))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/trends/product_categories/trending" & "?" & url_encoded_query_params)
  constructResult[seq[TrendingProductCategory]](response)

