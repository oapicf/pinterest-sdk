#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_conversion_deletion_request
import ../models/model_conversion_deletion_request_create
import ../models/model_pinterest/lib/error
import ../models/model_pinterest/lib/pagination_order
import ../models/model_conversion_deletion_request_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc conversionDeletionRequestCreate*(httpClient: HttpClient, adAccountId: string, conversionDeletionRequestCreate: ConversionDeletionRequestCreate): (Option[ConversionDeletionRequest], Response) =
  ## Create a conversion deletion request
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/conversion_deletion_requests", $(%conversionDeletionRequestCreate))
  constructResult[ConversionDeletionRequest](response)


proc conversionDeletionRequestDelete*(httpClient: HttpClient, requestId: string, adAccountId: string): (Option[ConversionDeletionRequest], Response) =
  ## Delete a conversion deletion request

  let response = httpClient.delete(basepath & fmt"/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}")
  constructResult[ConversionDeletionRequest](response)


proc conversionDeletionRequestGet*(httpClient: HttpClient, requestId: string, adAccountId: string): (Option[ConversionDeletionRequest], Response) =
  ## Get a single conversion deletion request

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}")
  constructResult[ConversionDeletionRequest](response)


proc conversionDeletionRequestList*(httpClient: HttpClient, adAccountId: string, bookmark: string, pageSize: int, order: PinterestLibPaginationOrder): (Option[conversion_deletion_request_list_200_response], Response) =
  ## List conversion deletion requests
  var query_params_list: seq[(string, string)] = @[]
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/conversion_deletion_requests" & "?" & url_encoded_query_params)
  constructResult[conversion_deletion_request_list_200_response](response)

