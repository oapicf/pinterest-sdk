#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_conversion_access_token
import ../models/model_oauth_access_token
import ../models/model_pinterest/lib/error
import ../models/model_token_grant_type
import ../models/model_token_type_hint

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc oauthConversionToken*(httpClient: HttpClient): (Option[ConversionAccessToken], Response) =
  ## Generate OAuth access token for conversion API

  let response = httpClient.post(basepath & "/oauth/conversion_token")
  constructResult[ConversionAccessToken](response)


proc oauthToken*(httpClient: HttpClient, grantType: TokenGrantType, code: string, continuousRefresh: string, redirectUri: string, refreshToken: string, scope: string): (Option[OauthAccessToken], Response) =
  ## Generate OAuth access token
  httpClient.headers["Content-Type"] = "application/x-www-form-urlencoded"
  let form_data = encodeQuery([
    ("code", $code), # 
    ("continuous_refresh", $continuousRefresh), #   If your app was created before **September 25, 2025**, set to `true` to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token.
    ("grant_type", $grantType), # 
    ("redirect_uri", $redirectUri), # 
    ("refresh_token", $refreshToken), # 
    ("scope", $scope), # 
  ])

  let response = httpClient.post(basepath & "/oauth/token", $form_data)
  constructResult[OauthAccessToken](response)


proc tokenRevoke*(httpClient: HttpClient, token: string, tokenTypeHint: TokenTypeHint): Response =
  ## Revoke a token
  httpClient.headers["Content-Type"] = "application/x-www-form-urlencoded"
  let form_data = encodeQuery([
    ("token", $token), # The token to revoke.
    ("token_type_hint", $tokenTypeHint), # The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information.
  ])
  httpClient.post(basepath & "/oauth/token/revoke", $form_data)


