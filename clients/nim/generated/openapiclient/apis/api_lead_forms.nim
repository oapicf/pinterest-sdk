#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_lead_form
import ../models/model_lead_form_batch_update
import ../models/model_lead_form_create
import ../models/model_lead_form_test
import ../models/model_lead_form_test_create
import ../models/model_pinterest/lib/error
import ../models/model_pinterest/lib/pagination_order
import ../models/model_lead_forms_create200response
import ../models/model_lead_forms_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc leadFormGet*(httpClient: HttpClient, leadFormId: string, adAccountId: string): (Option[LeadForm], Response) =
  ## Get lead form by id

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}")
  constructResult[LeadForm](response)


proc leadFormTestCreate*(httpClient: HttpClient, adAccountId: string, leadFormId: string, leadFormTestCreate: LeadFormTestCreate): (Option[LeadFormTest], Response) =
  ## Create lead form test data
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}/test", $(%leadFormTestCreate))
  constructResult[LeadFormTest](response)


proc leadFormsCreate*(httpClient: HttpClient, adAccountId: string, leadFormCreate: seq[LeadFormCreate]): (Option[lead_forms_create_200_response], Response) =
  ## Create lead forms
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms", $(%leadFormCreate))
  constructResult[lead_forms_create_200_response](response)


proc leadFormsList*(httpClient: HttpClient, adAccountId: string, bookmark: string, pageSize: int, order: PinterestLibPaginationOrder): (Option[lead_forms_list_200_response], Response) =
  ## List lead forms
  var query_params_list: seq[(string, string)] = @[]
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms" & "?" & url_encoded_query_params)
  constructResult[lead_forms_list_200_response](response)


proc leadFormsUpdate*(httpClient: HttpClient, adAccountId: string, leadFormBatchUpdate: seq[LeadFormBatchUpdate]): (Option[lead_forms_create_200_response], Response) =
  ## Update lead forms
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms", $(%leadFormBatchUpdate))
  constructResult[lead_forms_create_200_response](response)

