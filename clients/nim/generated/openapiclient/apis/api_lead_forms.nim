#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_error
import ../models/model_lead_form_array_response
import ../models/model_lead_form_create_request
import ../models/model_lead_form_response
import ../models/model_lead_form_test_request
import ../models/model_lead_form_test_response
import ../models/model_lead_form_update_request
import ../models/model_lead_forms_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc leadFormGet*(httpClient: HttpClient, adAccountId: string, leadFormId: string): (Option[LeadFormResponse], Response) =
  ## Get lead form by id

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}")
  constructResult[LeadFormResponse](response)


proc leadFormTestCreate*(httpClient: HttpClient, adAccountId: string, leadFormId: string, leadFormTestRequest: LeadFormTestRequest): (Option[LeadFormTestResponse], Response) =
  ## Create lead form test data
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms/{lead_form_id}/test", $(%leadFormTestRequest))
  constructResult[LeadFormTestResponse](response)


proc leadFormsCreate*(httpClient: HttpClient, adAccountId: string, leadFormCreateRequest: seq[LeadFormCreateRequest]): (Option[LeadFormArrayResponse], Response) =
  ## Create lead forms
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms", $(%leadFormCreateRequest))
  constructResult[LeadFormArrayResponse](response)


proc leadFormsList*(httpClient: HttpClient, adAccountId: string, pageSize: int, order: string, bookmark: string): (Option[lead_forms_list_200_response], Response) =
  ## List lead forms
  var query_params_list: seq[(string, string)] = @[]
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms" & "?" & url_encoded_query_params)
  constructResult[lead_forms_list_200_response](response)


proc leadFormsUpdate*(httpClient: HttpClient, adAccountId: string, leadFormUpdateRequest: seq[LeadFormUpdateRequest]): (Option[LeadFormArrayResponse], Response) =
  ## Update lead forms
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/lead_forms", $(%leadFormUpdateRequest))
  constructResult[LeadFormArrayResponse](response)

