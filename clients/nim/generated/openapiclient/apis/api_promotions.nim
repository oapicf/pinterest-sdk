#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_error
import ../models/model_promotion_create_request
import ../models/model_promotion_response
import ../models/model_promotion_update_request
import ../models/model_promotions_response
import ../models/model_promotions_list200response

const basepath = "https://api.pinterest.com/v5"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc promotionsCreate*(httpClient: HttpClient, adAccountId: string, promotionCreateRequest: seq[PromotionCreateRequest]): (Option[PromotionsResponse], Response) =
  ## Create promotions
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/ad_accounts/{ad_account_id}/promotions", $(%promotionCreateRequest))
  constructResult[PromotionsResponse](response)


proc promotionsDelete*(httpClient: HttpClient, adAccountId: string, promotionId: string): Response =
  ## Delete promotion by id
  httpClient.delete(basepath & fmt"/ad_accounts/{ad_account_id}/promotions/{promotion_id}")



proc promotionsGet*(httpClient: HttpClient, adAccountId: string, promotionId: string): (Option[PromotionResponse], Response) =
  ## Get promotion by id

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/promotions/{promotion_id}")
  constructResult[PromotionResponse](response)


proc promotionsList*(httpClient: HttpClient, adAccountId: string, pageSize: int, order: string, bookmark: string): (Option[promotions_list_200_response], Response) =
  ## Get promotions
  var query_params_list: seq[(string, string)] = @[]
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $order != "":
    query_params_list.add(("order", $order))
  if $bookmark != "":
    query_params_list.add(("bookmark", $bookmark))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & fmt"/ad_accounts/{ad_account_id}/promotions" & "?" & url_encoded_query_params)
  constructResult[promotions_list_200_response](response)


proc promotionsUpdate*(httpClient: HttpClient, adAccountId: string, promotionUpdateRequest: seq[PromotionUpdateRequest]): (Option[PromotionsResponse], Response) =
  ## Update promotions
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/ad_accounts/{ad_account_id}/promotions", $(%promotionUpdateRequest))
  constructResult[PromotionsResponse](response)

