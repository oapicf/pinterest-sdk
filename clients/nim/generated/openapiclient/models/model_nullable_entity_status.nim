#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type NullableEntityStatus* {.pure.} = enum
  ACTIVE
  PAUSED
  ARCHIVED
  DRAFT
  DELETEDDRAFT

func `%`*(v: NullableEntityStatus): JsonNode =
  result = case v:
    of NullableEntityStatus.ACTIVE: %"ACTIVE"
    of NullableEntityStatus.PAUSED: %"PAUSED"
    of NullableEntityStatus.ARCHIVED: %"ARCHIVED"
    of NullableEntityStatus.DRAFT: %"DRAFT"
    of NullableEntityStatus.DELETEDDRAFT: %"DELETED_DRAFT"

func `$`*(v: NullableEntityStatus): string =
  result = case v:
    of NullableEntityStatus.ACTIVE: $("ACTIVE")
    of NullableEntityStatus.PAUSED: $("PAUSED")
    of NullableEntityStatus.ARCHIVED: $("ARCHIVED")
    of NullableEntityStatus.DRAFT: $("DRAFT")
    of NullableEntityStatus.DELETEDDRAFT: $("DELETED_DRAFT")

proc to*(node: JsonNode, T: typedesc[NullableEntityStatus]): NullableEntityStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum NullableEntityStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("ACTIVE"):
    return NullableEntityStatus.ACTIVE
  of $("PAUSED"):
    return NullableEntityStatus.PAUSED
  of $("ARCHIVED"):
    return NullableEntityStatus.ARCHIVED
  of $("DRAFT"):
    return NullableEntityStatus.DRAFT
  of $("DELETED_DRAFT"):
    return NullableEntityStatus.DELETEDDRAFT
  else:
    raise newException(ValueError, "Invalid enum value for NullableEntityStatus: " & strVal)

