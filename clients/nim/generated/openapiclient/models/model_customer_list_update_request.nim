#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_user_list_operation_type

type CustomerListUpdateRequest* = object
  ## 
  operationType*: UserListOperationType
  records*: string ## Records list. Can be any combination of emails, MAIDs, or IDFAs. Emails must be lowercase and can be plain text or hashed using SHA1, SHA256, or MD5. MAIDs and IDFAs must be hashed with SHA1, SHA256, or MD5.


# Custom JSON deserialization for CustomerListUpdateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerListUpdateRequest]): CustomerListUpdateRequest =
  result = CustomerListUpdateRequest()
  if node.kind == JObject:
    if node.hasKey("operation_type"):
      result.operationType = to(node["operation_type"], UserListOperationType)
    if node.hasKey("records"):
      result.records = to(node["records"], string)

# Custom JSON serialization for CustomerListUpdateRequest with custom field names
proc `%`*(obj: CustomerListUpdateRequest): JsonNode =
  result = newJObject()
  result["operation_type"] = %obj.operationType
  result["records"] = %obj.records

