#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_label
import model_label_error

type LabelsResponse* = object
  ## 
  errors*: Option[seq[LabelError]] ## Labels that were not successfully applied.
  labels*: Option[seq[Label]]


# Custom JSON deserialization for LabelsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[LabelsResponse]): LabelsResponse =
  result = LabelsResponse()
  if node.kind == JObject:
    if node.hasKey("errors") and node["errors"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["errors"]
      if arrayNode.kind == JArray:
        var arr: seq[LabelError] = @[]
        for item in arrayNode.items:
          arr.add(to(item, LabelError))
        result.errors = some(arr)
    if node.hasKey("labels") and node["labels"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["labels"]
      if arrayNode.kind == JArray:
        var arr: seq[Label] = @[]
        for item in arrayNode.items:
          arr.add(to(item, Label))
        result.labels = some(arr)

# Custom JSON serialization for LabelsResponse with custom field names
proc `%`*(obj: LabelsResponse): JsonNode =
  result = newJObject()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  if obj.labels.isSome():
    result["labels"] = %obj.labels.get()

