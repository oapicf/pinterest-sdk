#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BidOptionsPlacementMultipliers* = object
  ## This represents a mapping from placement to a bid price change value.  Values represent the change in bid price and can be positive or negative. For percentage changes, a value of 500.0 means a 5% change. For value changes, the amount represents the direct bid adjustment. All placement multipliers must be set at the same time. If a multiplier is not provided it is assumed to be 0 (no bid adjustment). The change must still remain between the range of 0 and 10.
  browse*: Option[float]
  relatedPins*: Option[float]
  search*: Option[float]


# Custom JSON deserialization for BidOptionsPlacementMultipliers with custom field names
proc to*(node: JsonNode, T: typedesc[BidOptionsPlacementMultipliers]): BidOptionsPlacementMultipliers =
  result = BidOptionsPlacementMultipliers()
  if node.kind == JObject:
    if node.hasKey("browse") and node["browse"].kind != JNull:
      result.browse = some(to(node["browse"], typeof(result.browse.get())))
    if node.hasKey("related_pins") and node["related_pins"].kind != JNull:
      result.relatedPins = some(to(node["related_pins"], typeof(result.relatedPins.get())))
    if node.hasKey("search") and node["search"].kind != JNull:
      result.search = some(to(node["search"], typeof(result.search.get())))

# Custom JSON serialization for BidOptionsPlacementMultipliers with custom field names
proc `%`*(obj: BidOptionsPlacementMultipliers): JsonNode =
  result = newJObject()
  if obj.browse.isSome():
    result["browse"] = %obj.browse.get()
  if obj.relatedPins.isSome():
    result["related_pins"] = %obj.relatedPins.get()
  if obj.search.isSome():
    result["search"] = %obj.search.get()

