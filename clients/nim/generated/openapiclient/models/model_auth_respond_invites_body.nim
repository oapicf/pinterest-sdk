#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_auth_respond_invites_body_item

type AuthRespondInvitesBody* = object
  ## An object with a list of all the invites the user would like to respond to and the action to take.
  invites*: seq[AuthRespondInvitesBodyItem]


# Custom JSON deserialization for AuthRespondInvitesBody with custom field names
proc to*(node: JsonNode, T: typedesc[AuthRespondInvitesBody]): AuthRespondInvitesBody =
  result = AuthRespondInvitesBody()
  if node.kind == JObject:
    if node.hasKey("invites"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["invites"]
      if arrayNode.kind == JArray:
        result.invites = @[]
        for item in arrayNode.items:
          result.invites.add(to(item, AuthRespondInvitesBodyItem))

# Custom JSON serialization for AuthRespondInvitesBody with custom field names
proc `%`*(obj: AuthRespondInvitesBody): JsonNode =
  result = newJObject()
  result["invites"] = %obj.invites

