#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_delivery_estimates_derived_metrics
import model_campaign_planning_ad_group_audience_size
import model_campaign_planning_conversion_rate
import model_campaign_planning_curve_estimate
import model_campaign_planning_response_error

type BulkCampaignDeliveryEstimatesItem* = object
  ## Delivery estimate result for a single campaign within a bulk request.
  adgroupAudienceSizes*: Option[seq[CampaignPlanningAdGroupAudienceSize]] ## Range audience sizes for each ad group, in the same order as the ad groups in the request.
  conversionRate*: Option[float] ## Conversion rate estimate. Used to translate impressions into conversions for oCPM ads, adapted based on the input conversion event and attribution windows.
  conversionRates*: Option[seq[CampaignPlanningConversionRate]] ## Conversion rate estimates. Used to translate impressions into conversions for oCPM ads based on different conversion event and attribution windows settings.
  curves*: Option[seq[CampaignPlanningCurveEstimate]] ## Estimated curves. Each curve will pertain to a single estimation type.
  derivedMetrics*: Option[CampaignDeliveryEstimatesDerivedMetrics]
  errors*: Option[seq[CampaignPlanningResponseError]] ## Errors encountered during estimation for this campaign.
  estimateId*: Option[string] ## UUID used to track delivery estimates when they are generated as part of a saved campaign.
  maxPotentialSpend*: Option[int] ## Maximum potential spend estimate.


# Custom JSON deserialization for BulkCampaignDeliveryEstimatesItem with custom field names
proc to*(node: JsonNode, T: typedesc[BulkCampaignDeliveryEstimatesItem]): BulkCampaignDeliveryEstimatesItem =
  result = BulkCampaignDeliveryEstimatesItem()
  if node.kind == JObject:
    if node.hasKey("adgroup_audience_sizes") and node["adgroup_audience_sizes"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["adgroup_audience_sizes"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningAdGroupAudienceSize] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningAdGroupAudienceSize))
        result.adgroupAudienceSizes = some(arr)
    if node.hasKey("conversion_rate") and node["conversion_rate"].kind != JNull:
      result.conversionRate = some(to(node["conversion_rate"], typeof(result.conversionRate.get())))
    if node.hasKey("conversion_rates") and node["conversion_rates"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["conversion_rates"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningConversionRate] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningConversionRate))
        result.conversionRates = some(arr)
    if node.hasKey("curves") and node["curves"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["curves"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningCurveEstimate] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningCurveEstimate))
        result.curves = some(arr)
    if node.hasKey("derived_metrics") and node["derived_metrics"].kind != JNull:
      result.derivedMetrics = some(to(node["derived_metrics"], typeof(result.derivedMetrics.get())))
    if node.hasKey("errors") and node["errors"].kind != JNull:
      result.errors = some(to(node["errors"], typeof(result.errors.get())))
    if node.hasKey("estimate_id") and node["estimate_id"].kind != JNull:
      result.estimateId = some(to(node["estimate_id"], typeof(result.estimateId.get())))
    if node.hasKey("max_potential_spend") and node["max_potential_spend"].kind != JNull:
      result.maxPotentialSpend = some(to(node["max_potential_spend"], typeof(result.maxPotentialSpend.get())))

# Custom JSON serialization for BulkCampaignDeliveryEstimatesItem with custom field names
proc `%`*(obj: BulkCampaignDeliveryEstimatesItem): JsonNode =
  result = newJObject()
  if obj.adgroupAudienceSizes.isSome():
    result["adgroup_audience_sizes"] = %obj.adgroupAudienceSizes.get()
  if obj.conversionRate.isSome():
    result["conversion_rate"] = %obj.conversionRate.get()
  if obj.conversionRates.isSome():
    result["conversion_rates"] = %obj.conversionRates.get()
  if obj.curves.isSome():
    result["curves"] = %obj.curves.get()
  if obj.derivedMetrics.isSome():
    result["derived_metrics"] = %obj.derivedMetrics.get()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  if obj.estimateId.isSome():
    result["estimate_id"] = %obj.estimateId.get()
  if obj.maxPotentialSpend.isSome():
    result["max_potential_spend"] = %obj.maxPotentialSpend.get()

