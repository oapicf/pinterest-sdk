#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_billing_invoice_document_type
import model_billing_invoice_status
import model_currency

type BillingInvoice* = object
  ## A billing invoice in the advertiser account.
  adAccountId*: Option[string] ## The ID of the ad account this invoice belongs to
  adAccountName*: Option[string] ## The name of the ad account this invoice belongs to
  amountBilledMicroCurrency*: Option[int] ## The amount billed in this invoice. Denoted in micro currency
  amountDiscountMicroCurrency*: Option[int] ## The discount in this invoice. Denoted in micro currency
  amountNetMicroCurrency*: Option[int] ## The net amount in this invoice. Denoted in micro currency
  amountTaxMicroCurrency*: Option[int] ## The tax in this invoice. Denoted in micro currency
  billToCountry*: Option[string] ## The country of the bill to address
  billingPeriodEndDate*: Option[string] ## The end date of the billing period. Format: YYYY-MM-DD
  billingPeriodStartDate*: Option[string] ## The start date of the billing period. Format: YYYY-MM-DD
  currency*: Option[Currency]
  documentType*: Option[BillingInvoiceDocumentType] ## The type of the document
  id*: Option[string] ## Unique identifier for the billing invoice
  invoiceDueDate*: Option[string] ## The date the invoice is due. Format: YYYY-MM-DD
  paymentTerms*: Option[string] ## The payment terms of the invoice
  status*: Option[BillingInvoiceStatus] ## The status of the invoice


# Custom JSON deserialization for BillingInvoice with custom field names
proc to*(node: JsonNode, T: typedesc[BillingInvoice]): BillingInvoice =
  result = BillingInvoice()
  if node.kind == JObject:
    if node.hasKey("ad_account_id") and node["ad_account_id"].kind != JNull:
      result.adAccountId = some(to(node["ad_account_id"], typeof(result.adAccountId.get())))
    if node.hasKey("ad_account_name") and node["ad_account_name"].kind != JNull:
      result.adAccountName = some(to(node["ad_account_name"], typeof(result.adAccountName.get())))
    if node.hasKey("amount_billed_micro_currency") and node["amount_billed_micro_currency"].kind != JNull:
      result.amountBilledMicroCurrency = some(to(node["amount_billed_micro_currency"], typeof(result.amountBilledMicroCurrency.get())))
    if node.hasKey("amount_discount_micro_currency") and node["amount_discount_micro_currency"].kind != JNull:
      result.amountDiscountMicroCurrency = some(to(node["amount_discount_micro_currency"], typeof(result.amountDiscountMicroCurrency.get())))
    if node.hasKey("amount_net_micro_currency") and node["amount_net_micro_currency"].kind != JNull:
      result.amountNetMicroCurrency = some(to(node["amount_net_micro_currency"], typeof(result.amountNetMicroCurrency.get())))
    if node.hasKey("amount_tax_micro_currency") and node["amount_tax_micro_currency"].kind != JNull:
      result.amountTaxMicroCurrency = some(to(node["amount_tax_micro_currency"], typeof(result.amountTaxMicroCurrency.get())))
    if node.hasKey("bill_to_country") and node["bill_to_country"].kind != JNull:
      result.billToCountry = some(to(node["bill_to_country"], typeof(result.billToCountry.get())))
    if node.hasKey("billing_period_end_date") and node["billing_period_end_date"].kind != JNull:
      result.billingPeriodEndDate = some(to(node["billing_period_end_date"], typeof(result.billingPeriodEndDate.get())))
    if node.hasKey("billing_period_start_date") and node["billing_period_start_date"].kind != JNull:
      result.billingPeriodStartDate = some(to(node["billing_period_start_date"], typeof(result.billingPeriodStartDate.get())))
    if node.hasKey("currency") and node["currency"].kind != JNull:
      result.currency = some(to(node["currency"], typeof(result.currency.get())))
    if node.hasKey("document_type") and node["document_type"].kind != JNull:
      result.documentType = some(to(node["document_type"], typeof(result.documentType.get())))
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("invoice_due_date") and node["invoice_due_date"].kind != JNull:
      result.invoiceDueDate = some(to(node["invoice_due_date"], typeof(result.invoiceDueDate.get())))
    if node.hasKey("payment_terms") and node["payment_terms"].kind != JNull:
      result.paymentTerms = some(to(node["payment_terms"], typeof(result.paymentTerms.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))

# Custom JSON serialization for BillingInvoice with custom field names
proc `%`*(obj: BillingInvoice): JsonNode =
  result = newJObject()
  if obj.adAccountId.isSome():
    result["ad_account_id"] = %obj.adAccountId.get()
  if obj.adAccountName.isSome():
    result["ad_account_name"] = %obj.adAccountName.get()
  if obj.amountBilledMicroCurrency.isSome():
    result["amount_billed_micro_currency"] = %obj.amountBilledMicroCurrency.get()
  if obj.amountDiscountMicroCurrency.isSome():
    result["amount_discount_micro_currency"] = %obj.amountDiscountMicroCurrency.get()
  if obj.amountNetMicroCurrency.isSome():
    result["amount_net_micro_currency"] = %obj.amountNetMicroCurrency.get()
  if obj.amountTaxMicroCurrency.isSome():
    result["amount_tax_micro_currency"] = %obj.amountTaxMicroCurrency.get()
  if obj.billToCountry.isSome():
    result["bill_to_country"] = %obj.billToCountry.get()
  if obj.billingPeriodEndDate.isSome():
    result["billing_period_end_date"] = %obj.billingPeriodEndDate.get()
  if obj.billingPeriodStartDate.isSome():
    result["billing_period_start_date"] = %obj.billingPeriodStartDate.get()
  if obj.currency.isSome():
    result["currency"] = %obj.currency.get()
  if obj.documentType.isSome():
    result["document_type"] = %obj.documentType.get()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.invoiceDueDate.isSome():
    result["invoice_due_date"] = %obj.invoiceDueDate.get()
  if obj.paymentTerms.isSome():
    result["payment_terms"] = %obj.paymentTerms.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()

