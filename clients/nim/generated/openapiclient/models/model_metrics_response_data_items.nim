#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type MetricsResponseDataItems* = object
  ## 
  metrics*: JsonNode ## Metrics object containing the requested metric columns and their values. The keys are metric names (e.g., AD_GROUP_ID, DATE, SPEND_IN_DOLLAR) and values are the metric values (strings or numbers).
  targetingType*: string ## The targeting type for this data item (e.g., KEYWORD, APPTYPE, GENDER)
  targetingValue*: string ## The targeting value for this data item (e.g., 'christmas decor ideas', 'iphone', 'female')


# Custom JSON deserialization for MetricsResponseDataItems with custom field names
proc to*(node: JsonNode, T: typedesc[MetricsResponseDataItems]): MetricsResponseDataItems =
  result = MetricsResponseDataItems()
  if node.kind == JObject:
    if node.hasKey("metrics"):
      result.metrics = to(node["metrics"], JsonNode)
    if node.hasKey("targeting_type"):
      result.targetingType = to(node["targeting_type"], string)
    if node.hasKey("targeting_value"):
      result.targetingValue = to(node["targeting_value"], string)

# Custom JSON serialization for MetricsResponseDataItems with custom field names
proc `%`*(obj: MetricsResponseDataItems): JsonNode =
  result = newJObject()
  result["metrics"] = %obj.metrics
  result["targeting_type"] = %obj.targetingType
  result["targeting_value"] = %obj.targetingValue

