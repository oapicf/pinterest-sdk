#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_trends_gender

type TrendsGenderDistribution* = object
  ## This represents a mapping from gender to distribution of search volume for a keyword. The sum of all values in this object should approximately be 1.
  genderDistribution*: Option[TrendsGender]


# Custom JSON deserialization for TrendsGenderDistribution with custom field names
proc to*(node: JsonNode, T: typedesc[TrendsGenderDistribution]): TrendsGenderDistribution =
  result = TrendsGenderDistribution()
  if node.kind == JObject:
    if node.hasKey("gender_distribution") and node["gender_distribution"].kind != JNull:
      result.genderDistribution = some(to(node["gender_distribution"], typeof(result.genderDistribution.get())))

# Custom JSON serialization for TrendsGenderDistribution with custom field names
proc `%`*(obj: TrendsGenderDistribution): JsonNode =
  result = newJObject()
  if obj.genderDistribution.isSome():
    result["gender_distribution"] = %obj.genderDistribution.get()

