#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type AttributionActionType* {.pure.} = enum
  View
  Click

func `%`*(v: AttributionActionType): JsonNode =
  result = case v:
    of AttributionActionType.View: %"view"
    of AttributionActionType.Click: %"click"

func `$`*(v: AttributionActionType): string =
  result = case v:
    of AttributionActionType.View: $("view")
    of AttributionActionType.Click: $("click")

proc to*(node: JsonNode, T: typedesc[AttributionActionType]): AttributionActionType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum AttributionActionType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("view"):
    return AttributionActionType.View
  of $("click"):
    return AttributionActionType.Click
  else:
    raise newException(ValueError, "Invalid enum value for AttributionActionType: " & strVal)

