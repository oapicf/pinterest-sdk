#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_conversion_api_response_events_items

type ConversionEvents* = object
  ## Conversion events.
  events*: seq[ConversionApiResponseEventsItems] ## Specific messages for each event received. The order will match the order in which the events were received in the request.
  numEventsProcessed*: int ## Number of events that were successfully processed from the events.
  numEventsReceived*: int ## Total number of events received in the request.


# Custom JSON deserialization for ConversionEvents with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionEvents]): ConversionEvents =
  result = ConversionEvents()
  if node.kind == JObject:
    if node.hasKey("events"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["events"]
      if arrayNode.kind == JArray:
        result.events = @[]
        for item in arrayNode.items:
          result.events.add(to(item, ConversionApiResponseEventsItems))
    if node.hasKey("num_events_processed"):
      result.numEventsProcessed = to(node["num_events_processed"], int)
    if node.hasKey("num_events_received"):
      result.numEventsReceived = to(node["num_events_received"], int)

# Custom JSON serialization for ConversionEvents with custom field names
proc `%`*(obj: ConversionEvents): JsonNode =
  result = newJObject()
  result["events"] = %obj.events
  result["num_events_processed"] = %obj.numEventsProcessed
  result["num_events_received"] = %obj.numEventsReceived

