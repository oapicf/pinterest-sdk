#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UserWebsite* = object
  ## 
  status*: string ## Status of the verification process
  verifiedAt*: string ## UTC timestamp when the verification happened - sometimes missing
  website*: Option[string] ## Website with path or domain only


# Custom JSON deserialization for UserWebsite with custom field names
proc to*(node: JsonNode, T: typedesc[UserWebsite]): UserWebsite =
  result = UserWebsite()
  if node.kind == JObject:
    if node.hasKey("status"):
      result.status = to(node["status"], string)
    if node.hasKey("verified_at"):
      result.verifiedAt = to(node["verified_at"], string)
    if node.hasKey("website") and node["website"].kind != JNull:
      result.website = some(to(node["website"], typeof(result.website.get())))

# Custom JSON serialization for UserWebsite with custom field names
proc `%`*(obj: UserWebsite): JsonNode =
  result = newJObject()
  result["status"] = %obj.status
  result["verified_at"] = %obj.verifiedAt
  if obj.website.isSome():
    result["website"] = %obj.website.get()

