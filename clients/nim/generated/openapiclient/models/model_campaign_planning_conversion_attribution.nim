#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_planning_conversion_attribution_window_days

type CampaignPlanningConversionAttribution* = object
  ## Attribution windows for a conversion event.
  clickWindowDays*: Option[CampaignPlanningConversionAttributionWindowDays] ## Number of days to use as the conversion attribution window for a pin click action.
  engagementWindowDays*: Option[CampaignPlanningConversionAttributionWindowDays] ## Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes.
  viewWindowDays*: Option[CampaignPlanningConversionAttributionWindowDays] ## Number of days to use as the conversion attribution window for a view action.


# Custom JSON deserialization for CampaignPlanningConversionAttribution with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignPlanningConversionAttribution]): CampaignPlanningConversionAttribution =
  result = CampaignPlanningConversionAttribution()
  if node.kind == JObject:
    if node.hasKey("click_window_days") and node["click_window_days"].kind != JNull:
      result.clickWindowDays = some(to(node["click_window_days"], typeof(result.clickWindowDays.get())))
    if node.hasKey("engagement_window_days") and node["engagement_window_days"].kind != JNull:
      result.engagementWindowDays = some(to(node["engagement_window_days"], typeof(result.engagementWindowDays.get())))
    if node.hasKey("view_window_days") and node["view_window_days"].kind != JNull:
      result.viewWindowDays = some(to(node["view_window_days"], typeof(result.viewWindowDays.get())))

# Custom JSON serialization for CampaignPlanningConversionAttribution with custom field names
proc `%`*(obj: CampaignPlanningConversionAttribution): JsonNode =
  result = newJObject()
  if obj.clickWindowDays.isSome():
    result["click_window_days"] = %obj.clickWindowDays.get()
  if obj.engagementWindowDays.isSome():
    result["engagement_window_days"] = %obj.engagementWindowDays.get()
  if obj.viewWindowDays.isSome():
    result["view_window_days"] = %obj.viewWindowDays.get()

