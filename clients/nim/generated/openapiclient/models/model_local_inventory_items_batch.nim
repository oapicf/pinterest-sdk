#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_supplemental_item_batch_operation_status
import model_supplemental_operation_result

type LocalInventoryItemsBatch* = object
  ## Unified model for local inventory items batch operation
  batchId*: string ## Id of the batch operation
  completedTime*: string ## Date and time (UTC) of the batch completion: YYYY-MM-DD'T'hh:mm:ss
  createdTime*: string ## Date and time (UTC) of the batch creation: YYYY-MM-DD'T'hh:mm:ss
  operationResults*: seq[SupplementalOperationResult] ## Array of operation results
  status*: SupplementalItemBatchOperationStatus ## Status of the batch: PROCESSING, COMPLETED, FAILED


# Custom JSON deserialization for LocalInventoryItemsBatch with custom field names
proc to*(node: JsonNode, T: typedesc[LocalInventoryItemsBatch]): LocalInventoryItemsBatch =
  result = LocalInventoryItemsBatch()
  if node.kind == JObject:
    if node.hasKey("batch_id"):
      result.batchId = to(node["batch_id"], string)
    if node.hasKey("completed_time"):
      result.completedTime = to(node["completed_time"], string)
    if node.hasKey("created_time"):
      result.createdTime = to(node["created_time"], string)
    if node.hasKey("operation_results"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["operation_results"]
      if arrayNode.kind == JArray:
        result.operationResults = @[]
        for item in arrayNode.items:
          result.operationResults.add(to(item, SupplementalOperationResult))
    if node.hasKey("status"):
      result.status = to(node["status"], SupplementalItemBatchOperationStatus)

# Custom JSON serialization for LocalInventoryItemsBatch with custom field names
proc `%`*(obj: LocalInventoryItemsBatch): JsonNode =
  result = newJObject()
  result["batch_id"] = %obj.batchId
  result["completed_time"] = %obj.completedTime
  result["created_time"] = %obj.createdTime
  result["operation_results"] = %obj.operationResults
  result["status"] = %obj.status

