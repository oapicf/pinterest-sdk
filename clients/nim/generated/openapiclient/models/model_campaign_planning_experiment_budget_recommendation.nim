#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_planning_budget_recommendation_point

type CampaignPlanningExperimentBudgetRecommendation* = object
  ## Experimental budget recommendation for a single experiment version.
  budgetRecommendation*: Option[int] ## Recommended budget for this experiment version.
  lifetimeDaysRecommendation*: Option[int] ## Recommended lifetime days for this experiment.
  pointEstimations*: Option[seq[CampaignPlanningBudgetRecommendationPoint]] ## Point estimations for this experiment version.
  versionId*: Option[string] ## Version identifier for the experiment.


# Custom JSON deserialization for CampaignPlanningExperimentBudgetRecommendation with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignPlanningExperimentBudgetRecommendation]): CampaignPlanningExperimentBudgetRecommendation =
  result = CampaignPlanningExperimentBudgetRecommendation()
  if node.kind == JObject:
    if node.hasKey("budget_recommendation") and node["budget_recommendation"].kind != JNull:
      result.budgetRecommendation = some(to(node["budget_recommendation"], typeof(result.budgetRecommendation.get())))
    if node.hasKey("lifetime_days_recommendation") and node["lifetime_days_recommendation"].kind != JNull:
      result.lifetimeDaysRecommendation = some(to(node["lifetime_days_recommendation"], typeof(result.lifetimeDaysRecommendation.get())))
    if node.hasKey("point_estimations") and node["point_estimations"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["point_estimations"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningBudgetRecommendationPoint] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningBudgetRecommendationPoint))
        result.pointEstimations = some(arr)
    if node.hasKey("version_id") and node["version_id"].kind != JNull:
      result.versionId = some(to(node["version_id"], typeof(result.versionId.get())))

# Custom JSON serialization for CampaignPlanningExperimentBudgetRecommendation with custom field names
proc `%`*(obj: CampaignPlanningExperimentBudgetRecommendation): JsonNode =
  result = newJObject()
  if obj.budgetRecommendation.isSome():
    result["budget_recommendation"] = %obj.budgetRecommendation.get()
  if obj.lifetimeDaysRecommendation.isSome():
    result["lifetime_days_recommendation"] = %obj.lifetimeDaysRecommendation.get()
  if obj.pointEstimations.isSome():
    result["point_estimations"] = %obj.pointEstimations.get()
  if obj.versionId.isSome():
    result["version_id"] = %obj.versionId.get()

