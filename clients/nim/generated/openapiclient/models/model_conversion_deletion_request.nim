#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_conversion_deletion_request_status

type ConversionDeletionRequest* = object
  ## Conversion deletion request
  createdTime*: string ## Timestamp when the conversion deletion request was succesfully created.
  processedTime*: string ## Timestamp when the conversion deletion request was processed.
  requestId*: string ## Unique identifier of the conversion deletion request
  status*: ConversionDeletionRequestStatus ## Represents the status of the conversion deletion request. \"PENDING\" or \"SUBMITTED\". \"PENDING\" meaning the Advertiser can cancel the request. The advertiser has a 7 day time window after the conversion deletion request was submitted to cancel it. \"SUBMITTED\" meaning that the Data Deletion process has begun and can no longer be canceled.


# Custom JSON deserialization for ConversionDeletionRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionDeletionRequest]): ConversionDeletionRequest =
  result = ConversionDeletionRequest()
  if node.kind == JObject:
    if node.hasKey("created_time"):
      result.createdTime = to(node["created_time"], string)
    if node.hasKey("processed_time"):
      result.processedTime = to(node["processed_time"], string)
    if node.hasKey("request_id"):
      result.requestId = to(node["request_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], ConversionDeletionRequestStatus)

# Custom JSON serialization for ConversionDeletionRequest with custom field names
proc `%`*(obj: ConversionDeletionRequest): JsonNode =
  result = newJObject()
  result["created_time"] = %obj.createdTime
  result["processed_time"] = %obj.processedTime
  result["request_id"] = %obj.requestId
  result["status"] = %obj.status

