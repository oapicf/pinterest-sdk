#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LabelType* {.pure.} = enum
  BRAND
  CUSTOM

func `%`*(v: LabelType): JsonNode =
  result = case v:
    of LabelType.BRAND: %"BRAND"
    of LabelType.CUSTOM: %"CUSTOM"

func `$`*(v: LabelType): string =
  result = case v:
    of LabelType.BRAND: $("BRAND")
    of LabelType.CUSTOM: $("CUSTOM")

proc to*(node: JsonNode, T: typedesc[LabelType]): LabelType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum LabelType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("BRAND"):
    return LabelType.BRAND
  of $("CUSTOM"):
    return LabelType.CUSTOM
  else:
    raise newException(ValueError, "Invalid enum value for LabelType: " & strVal)

