#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ScheduleStatus* {.pure.} = enum
  DRAFT
  CREATED
  SCHEDULED
  ACTIVE
  COMPLETED
  FAILED
  CANCELED

func `%`*(v: ScheduleStatus): JsonNode =
  result = case v:
    of ScheduleStatus.DRAFT: %"DRAFT"
    of ScheduleStatus.CREATED: %"CREATED"
    of ScheduleStatus.SCHEDULED: %"SCHEDULED"
    of ScheduleStatus.ACTIVE: %"ACTIVE"
    of ScheduleStatus.COMPLETED: %"COMPLETED"
    of ScheduleStatus.FAILED: %"FAILED"
    of ScheduleStatus.CANCELED: %"CANCELED"

func `$`*(v: ScheduleStatus): string =
  result = case v:
    of ScheduleStatus.DRAFT: $("DRAFT")
    of ScheduleStatus.CREATED: $("CREATED")
    of ScheduleStatus.SCHEDULED: $("SCHEDULED")
    of ScheduleStatus.ACTIVE: $("ACTIVE")
    of ScheduleStatus.COMPLETED: $("COMPLETED")
    of ScheduleStatus.FAILED: $("FAILED")
    of ScheduleStatus.CANCELED: $("CANCELED")

proc to*(node: JsonNode, T: typedesc[ScheduleStatus]): ScheduleStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ScheduleStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("DRAFT"):
    return ScheduleStatus.DRAFT
  of $("CREATED"):
    return ScheduleStatus.CREATED
  of $("SCHEDULED"):
    return ScheduleStatus.SCHEDULED
  of $("ACTIVE"):
    return ScheduleStatus.ACTIVE
  of $("COMPLETED"):
    return ScheduleStatus.COMPLETED
  of $("FAILED"):
    return ScheduleStatus.FAILED
  of $("CANCELED"):
    return ScheduleStatus.CANCELED
  else:
    raise newException(ValueError, "Invalid enum value for ScheduleStatus: " & strVal)

