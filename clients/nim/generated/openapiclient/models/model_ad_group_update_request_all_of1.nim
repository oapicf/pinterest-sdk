#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_targeting_spec_operations

type AdGroupUpdateRequestAllOf1* = object
  ## 
  bidMultiplier*: Option[float] ## <a href=\"/docs/getting-started/using-beta-and-restricted-features/\" target=\"blank>Open beta</a> Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Make sure the `bid_strategy` type for your ad group is set to `AUTOMATIC_BID`. - Not currently supported for <a href=\"/docs/api-features/pinterest-performance-plus-setup/\" target=\"blank\">Pinterest Performance+ campaigns</a>.
  id*: string ## Ad group ID.
  targetingSpecOperations*: Option[seq[TargetingSpecOperations]] ## <div>Targeting spec operations define modifications to apply to the targeting spec.</div> <br /> <div><strong>NOTE:</strong> The <code>targeting_spec</code> and <code>targeting_spec_operations</code> cannot be sent at the same time.</div> <br /> <div>The supported operations are:</div> <ul> <li><code>SET</code>: sets the field with the given values. If value is set to <code>null</code>, the field will be removed.</li> <li><code>ADD</code>: adds the given values to the field.</li> <li><code>REMOVE</code>: removes the given values from the field.</li> </ul> <div>Note the following:</div> <ul> <li>Same items are not added and removed at the same time.</li> <li>For a given field, only <code>ADD</code>/<code>REMOVE</code> or <code>SET</code> operations are allowed, not a mix of them.</li> <li>Only one SET operation is allowed for a given field.</li> <li>The <code>AGE_BUCKET</code>, <code>MAXIMUM_AGE</code>, <code>MINIMUM_AGE</code> and <code>SHOPPING_RETARGETING</code> fields only support the <code>SET</code> operation.</li> </ul>


# Custom JSON deserialization for AdGroupUpdateRequestAllOf1 with custom field names
proc to*(node: JsonNode, T: typedesc[AdGroupUpdateRequestAllOf1]): AdGroupUpdateRequestAllOf1 =
  result = AdGroupUpdateRequestAllOf1()
  if node.kind == JObject:
    if node.hasKey("bid_multiplier") and node["bid_multiplier"].kind != JNull:
      result.bidMultiplier = some(to(node["bid_multiplier"], typeof(result.bidMultiplier.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("targeting_spec_operations") and node["targeting_spec_operations"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["targeting_spec_operations"]
      if arrayNode.kind == JArray:
        var arr: seq[TargetingSpecOperations] = @[]
        for item in arrayNode.items:
          arr.add(to(item, TargetingSpecOperations))
        result.targetingSpecOperations = some(arr)

# Custom JSON serialization for AdGroupUpdateRequestAllOf1 with custom field names
proc `%`*(obj: AdGroupUpdateRequestAllOf1): JsonNode =
  result = newJObject()
  if obj.bidMultiplier.isSome():
    result["bid_multiplier"] = %obj.bidMultiplier.get()
  result["id"] = %obj.id
  if obj.targetingSpecOperations.isSome():
    result["targeting_spec_operations"] = %obj.targetingSpecOperations.get()

