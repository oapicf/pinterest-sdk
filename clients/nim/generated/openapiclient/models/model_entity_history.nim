#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_change_history_operation_type
import model_entity_data_change_history

type EntityHistory* = object
  ## 
  changeTimestamp*: Option[int] ## A Unix timestamp representing the time of the change in seconds
  dataChanges*: Option[seq[EntityDataChangeHistory]] ## Properties associated with a particular change to an entity's data
  entityId*: Option[string] ## The id of the entity that was changed
  entityName*: Option[string] ## The name of the entity that was changed
  ldap*: Option[string] ## The LDAP of the user who initiated the change; only pertinent for changes initiated by internal users
  operation*: Option[ChangeHistoryOperationType] ## The type of operation that caused the change
  userId*: Option[string] ## The id of the user who initiated the change


# Custom JSON deserialization for EntityHistory with custom field names
proc to*(node: JsonNode, T: typedesc[EntityHistory]): EntityHistory =
  result = EntityHistory()
  if node.kind == JObject:
    if node.hasKey("change_timestamp") and node["change_timestamp"].kind != JNull:
      result.changeTimestamp = some(to(node["change_timestamp"], typeof(result.changeTimestamp.get())))
    if node.hasKey("data_changes") and node["data_changes"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data_changes"]
      if arrayNode.kind == JArray:
        var arr: seq[EntityDataChangeHistory] = @[]
        for item in arrayNode.items:
          arr.add(to(item, EntityDataChangeHistory))
        result.dataChanges = some(arr)
    if node.hasKey("entity_id") and node["entity_id"].kind != JNull:
      result.entityId = some(to(node["entity_id"], typeof(result.entityId.get())))
    if node.hasKey("entity_name") and node["entity_name"].kind != JNull:
      result.entityName = some(to(node["entity_name"], typeof(result.entityName.get())))
    if node.hasKey("ldap") and node["ldap"].kind != JNull:
      result.ldap = some(to(node["ldap"], typeof(result.ldap.get())))
    if node.hasKey("operation") and node["operation"].kind != JNull:
      result.operation = some(to(node["operation"], typeof(result.operation.get())))
    if node.hasKey("user_id") and node["user_id"].kind != JNull:
      result.userId = some(to(node["user_id"], typeof(result.userId.get())))

# Custom JSON serialization for EntityHistory with custom field names
proc `%`*(obj: EntityHistory): JsonNode =
  result = newJObject()
  if obj.changeTimestamp.isSome():
    result["change_timestamp"] = %obj.changeTimestamp.get()
  if obj.dataChanges.isSome():
    result["data_changes"] = %obj.dataChanges.get()
  if obj.entityId.isSome():
    result["entity_id"] = %obj.entityId.get()
  if obj.entityName.isSome():
    result["entity_name"] = %obj.entityName.get()
  if obj.ldap.isSome():
    result["ldap"] = %obj.ldap.get()
  if obj.operation.isSome():
    result["operation"] = %obj.operation.get()
  if obj.userId.isSome():
    result["user_id"] = %obj.userId.get()

