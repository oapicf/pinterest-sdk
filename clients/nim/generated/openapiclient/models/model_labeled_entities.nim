#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_entity_label
import model_entity_label_error

type LabeledEntities* = object
  ## 
  entitiesLabels*: seq[EntityLabel]
  errors*: seq[EntityLabelError] ## Labels that were not successfully applied.


# Custom JSON deserialization for LabeledEntities with custom field names
proc to*(node: JsonNode, T: typedesc[LabeledEntities]): LabeledEntities =
  result = LabeledEntities()
  if node.kind == JObject:
    if node.hasKey("entities_labels"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["entities_labels"]
      if arrayNode.kind == JArray:
        result.entitiesLabels = @[]
        for item in arrayNode.items:
          result.entitiesLabels.add(to(item, EntityLabel))
    if node.hasKey("errors"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["errors"]
      if arrayNode.kind == JArray:
        result.errors = @[]
        for item in arrayNode.items:
          result.errors.add(to(item, EntityLabelError))

# Custom JSON serialization for LabeledEntities with custom field names
proc `%`*(obj: LabeledEntities): JsonNode =
  result = newJObject()
  result["entities_labels"] = %obj.entitiesLabels
  result["errors"] = %obj.errors

