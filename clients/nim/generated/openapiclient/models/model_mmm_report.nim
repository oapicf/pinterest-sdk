#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_bulk_reporting_job_status

type MMMReport* = object
  ## 
  message*: string
  reportStatus*: BulkReportingJobStatus
  size*: float
  status*: string
  token*: string
  url*: string


# Custom JSON deserialization for MMMReport with custom field names
proc to*(node: JsonNode, T: typedesc[MMMReport]): MMMReport =
  result = MMMReport()
  if node.kind == JObject:
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("report_status"):
      result.reportStatus = to(node["report_status"], BulkReportingJobStatus)
    if node.hasKey("size"):
      result.size = to(node["size"], float)
    if node.hasKey("status"):
      result.status = to(node["status"], string)
    if node.hasKey("token"):
      result.token = to(node["token"], string)
    if node.hasKey("url"):
      result.url = to(node["url"], string)

# Custom JSON serialization for MMMReport with custom field names
proc `%`*(obj: MMMReport): JsonNode =
  result = newJObject()
  result["message"] = %obj.message
  result["report_status"] = %obj.reportStatus
  result["size"] = %obj.size
  result["status"] = %obj.status
  result["token"] = %obj.token
  result["url"] = %obj.url

