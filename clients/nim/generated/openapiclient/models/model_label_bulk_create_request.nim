#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_label_create_item

type LabelBulkCreateRequest* = object
  ## 
  labels*: seq[LabelCreateItem] ## Labels that you are applying to the campaign.
  parentId*: string ## Unique identifier of the asset you are labelling. Currently, you can only label campaigns.


# Custom JSON deserialization for LabelBulkCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[LabelBulkCreateRequest]): LabelBulkCreateRequest =
  result = LabelBulkCreateRequest()
  if node.kind == JObject:
    if node.hasKey("labels"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["labels"]
      if arrayNode.kind == JArray:
        result.labels = @[]
        for item in arrayNode.items:
          result.labels.add(to(item, LabelCreateItem))
    if node.hasKey("parent_id"):
      result.parentId = to(node["parent_id"], string)

# Custom JSON serialization for LabelBulkCreateRequest with custom field names
proc `%`*(obj: LabelBulkCreateRequest): JsonNode =
  result = newJObject()
  result["labels"] = %obj.labels
  result["parent_id"] = %obj.parentId

