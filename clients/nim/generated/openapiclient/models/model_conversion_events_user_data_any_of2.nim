#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ConversionEventsUserDataAnyOf2* = object
  ## 
  em*: Option[seq[string]] ## Sha256 hashes of lowercase version of user's email addresses. Used for matching. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  hashedMaids*: Option[seq[string]] ## Sha256 hashes of user's \"Google Advertising IDs\" (GAIDs) or \"Apple's Identifier for Advertisers\" (IDFAs). Used for matching. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  clientIpAddress*: string ## The user's IP address, which can be either in IPv4 or IPv6 format. Used for matching. We highly recommend this for all events. It may improve reporting performance such as ROAS/CPA.
  clientUserAgent*: string ## The user agent string of the user's web browser. We highly recommend this for all events. It may improve reporting performance such as ROAS/CPA.


# Custom JSON deserialization for ConversionEventsUserDataAnyOf2 with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionEventsUserDataAnyOf2]): ConversionEventsUserDataAnyOf2 =
  result = ConversionEventsUserDataAnyOf2()
  if node.kind == JObject:
    if node.hasKey("em") and node["em"].kind != JNull:
      result.em = some(to(node["em"], typeof(result.em.get())))
    if node.hasKey("hashed_maids") and node["hashed_maids"].kind != JNull:
      result.hashedMaids = some(to(node["hashed_maids"], typeof(result.hashedMaids.get())))
    if node.hasKey("client_ip_address"):
      result.clientIpAddress = to(node["client_ip_address"], string)
    if node.hasKey("client_user_agent"):
      result.clientUserAgent = to(node["client_user_agent"], string)

# Custom JSON serialization for ConversionEventsUserDataAnyOf2 with custom field names
proc `%`*(obj: ConversionEventsUserDataAnyOf2): JsonNode =
  result = newJObject()
  if obj.em.isSome():
    result["em"] = %obj.em.get()
  if obj.hashedMaids.isSome():
    result["hashed_maids"] = %obj.hashedMaids.get()
  result["client_ip_address"] = %obj.clientIpAddress
  result["client_user_agent"] = %obj.clientUserAgent

