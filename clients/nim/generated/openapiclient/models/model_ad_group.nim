#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_action_type
import model_ad_group_tracking_urls
import model_adgroup_tracking_features
import model_any_type
import model_bid_strategy_type
import model_budget_type
import model_conversion_learning_mode_type
import model_entity_status
import model_nullable_optimization_goal_metadata
import model_pacing_delivery_type
import model_performance_plus_campaign_settings
import model_placement_group_type
import model_placement_traffic_type
import model_promotion_application_level
import model_summary_status
import model_targeting_spec_optimal

type AdGroup* = object
  ## 
  adAccountId*: string ## Advertiser ID.
  bidInMicroCurrency*: Option[int] ## Bid price in micro currency. This field is **REQUIRED** for the following campaign objective_type/billable_event combinations: AWARENESS/IMPRESSION, CONSIDERATION/CLICKTHROUGH, CATALOG_SALES/CLICKTHROUGH, VIDEO_VIEW/VIDEO_V_50_MRC.
  bidStrategyType*: Option[BidStrategyType]
  billableEvent*: ActionType
  budgetInMicroCurrency*: Option[int] ## Budget in micro currency. This field is **REQUIRED** for non-CBO (campaign budget optimization) campaigns.  A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. A CBO campaign is limited to 70 or less ad groups.
  campaignId*: string ## Campaign ID of the ad group.
  conversionLearningModeType*: ConversionLearningModeType ## oCPM learn mode
  createdTime*: int ## Ad group creation time. Unix timestamp in seconds.
  customerSegmentId*: Option[string] ## Customer segment ID applied to the ad group. We currently only support 1 customer segment per ad group. To use customer segments, do not set the `targeting_template_ids` field as well as the `AUDIENCE_INCLUDE` field of the `targeting_spec`. To clear the customer segment ID, set this field to '0'.
  dcaAssets*: JsonNode ## [DCA] The Dynamic creative assets to use for DCA. Dynamic Creative Assembly (DCA) accepts basic creative assets of an ad (image, video, title, call to action, logo etc). Then it automatically generates optimized ad combinations based on these assets.
  endTime*: Option[int] ## Timestamp in Unix format for scheduling when ads in the ad group stop appearing. If not specified, ads run indefinitely unless you update the ad group by changing their status to `paused`. Cannot occur after `end_time` for parent campaign (if specified). Learn about [scheduling ads](https://help.pinterest.com/en/business/article/ads-manager-schedule-ads).  For certain organizations ([Closed beta](https://help.pinterest.com/en/business/article/beta-features)): Supported for campaigns with Campaign Budget Optimization (CBO).  For all organizations: Supported for campaigns without CBO.
  extFeatures*: Option[AdgroupTrackingFeatures]
  feedProfileId*: Option[string] ## Feed Profile ID associated to the adgroup.
  id*: string ## Ad group ID.
  isCreativeOptimization*: Option[bool] ## Enable creative optimization for the ad group, default value is FALSE. When enabled, you allow Pinterest to automatically turn your product Pins into ads in different formats (collections and shopping) and deliver those ads to users at scale.
  isLocalInventory*: Option[bool] ## Indicates whether the ad group should use the local inventory.
  lifetimeFrequencyCap*: Option[int] ## Set a limit to the number of times a promoted pin from this campaign can be impressed by a pinner within the past rolling 30 days. Only available for CPM (cost per mille (1000 impressions))  ad groups. A CPM ad group has an IMPRESSION [billable_event](https://help.pinterest.com/en/business/article/billable-events) value. This field **REQUIRES** the `end_time` field.
  localInventoryRadiusInMiles*: Option[float] ## The targeting radius of the local inventory ads in miles.
  name*: string ## Ad group name.
  optimizationGoalMetadata*: Option[NullableOptimizationGoalMetadata] ## Optimization goals for objective-based performance campaigns. **REQUIRED** when campaign's `objective_type` is set to `\"WEB_CONVERSION\"`.
  performancePlusCampaignSettings*: Option[PerformancePlusCampaignSettings] ## Pinterest Performance+ campaign settings.
  placementGroup*: Option[PlacementGroupType] ## [Placement group](https://help.pinterest.com/en/business/article/placement-groups).
  placementTrafficType*: Option[PlacementTrafficType] ## A targeting option that enables advertisers to choose whether to run ads in fullscreen feed, two column feed, or both
  promotionApplicationLevel*: Option[PromotionApplicationLevel] ## Specify if the promotion is applied at ad group or item level
  promotionId*: Option[string] ## Promotion ID. To clear this field, set to null.
  promotionIds*: Option[seq[string]] ## Promotion IDs list. To clear this field, set to an empty array [].
  startTime*: Option[int] ## Timestamp in Unix format for scheduling when ads in the ad group start to appear. If not specified, ads appear during parent campaign's `start_time`. Cannot precede `start_time` for parent campaign (if specified). Learn about [scheduling ads](/docs/api-features/managing-ads/#step-2-create-an-ad-group). For certain organizations ([Closed beta](/docs/getting-started/using-beta-and-restricted-features/)): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO.
  status*: Option[EntityStatus] ## Ad group/entity status.
  summaryStatus*: SummaryStatus
  targetingSpec*: Option[TargetingSpecOptimal]
  targetingTemplateIds*: Option[seq[string]] ## Targeting template IDs applied to the ad group. We currently only support 1 targeting template per ad group. To use targeting templates, do not set any other targeting fields: targeting_spec, tracking_urls, auto_targeting_enabled, placement_group. To clear all targeting template IDs, set this field to ['0'].
  trackingUrls*: Option[AdGroupTrackingURLs]
  `type`*: string ## Always \"adgroup\".
  updatedTime*: int ## Ad group last update time. Unix timestamp in seconds.
  autoTargetingEnabled*: Option[bool] ## Enable auto-targeting for ad group. Default value is True. Also known as [\"Performance+ targeting\"](https://help.pinterest.com/en/business/article/performance-plus-targeting).
  bidMultiplier*: Option[float] ## [Open beta](https://help.pinterest.com/en/business/article/beta-features) Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Not currently supported for [Performance+ campaigns](https://help.pinterest.com/en/business/article/performance-plus-campaigns).
  budgetType*: Option[BudgetType]
  pacingDeliveryType*: Option[PacingDeliveryType]


# Custom JSON deserialization for AdGroup with custom field names
proc to*(node: JsonNode, T: typedesc[AdGroup]): AdGroup =
  result = AdGroup()
  if node.kind == JObject:
    if node.hasKey("ad_account_id"):
      result.adAccountId = to(node["ad_account_id"], string)
    if node.hasKey("bid_in_micro_currency") and node["bid_in_micro_currency"].kind != JNull:
      result.bidInMicroCurrency = some(to(node["bid_in_micro_currency"], typeof(result.bidInMicroCurrency.get())))
    if node.hasKey("bid_strategy_type") and node["bid_strategy_type"].kind != JNull:
      result.bidStrategyType = some(to(node["bid_strategy_type"], typeof(result.bidStrategyType.get())))
    if node.hasKey("billable_event"):
      result.billableEvent = to(node["billable_event"], ActionType)
    if node.hasKey("budget_in_micro_currency") and node["budget_in_micro_currency"].kind != JNull:
      result.budgetInMicroCurrency = some(to(node["budget_in_micro_currency"], typeof(result.budgetInMicroCurrency.get())))
    if node.hasKey("campaign_id"):
      result.campaignId = to(node["campaign_id"], string)
    if node.hasKey("conversion_learning_mode_type"):
      result.conversionLearningModeType = to(node["conversion_learning_mode_type"], ConversionLearningModeType)
    if node.hasKey("created_time"):
      result.createdTime = to(node["created_time"], int)
    if node.hasKey("customer_segment_id") and node["customer_segment_id"].kind != JNull:
      result.customerSegmentId = some(to(node["customer_segment_id"], typeof(result.customerSegmentId.get())))
    if node.hasKey("dca_assets"):
      result.dcaAssets = to(node["dca_assets"], JsonNode)
    if node.hasKey("end_time") and node["end_time"].kind != JNull:
      result.endTime = some(to(node["end_time"], typeof(result.endTime.get())))
    if node.hasKey("ext_features") and node["ext_features"].kind != JNull:
      result.extFeatures = some(to(node["ext_features"], typeof(result.extFeatures.get())))
    if node.hasKey("feed_profile_id") and node["feed_profile_id"].kind != JNull:
      result.feedProfileId = some(to(node["feed_profile_id"], typeof(result.feedProfileId.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("is_creative_optimization") and node["is_creative_optimization"].kind != JNull:
      result.isCreativeOptimization = some(to(node["is_creative_optimization"], typeof(result.isCreativeOptimization.get())))
    if node.hasKey("is_local_inventory") and node["is_local_inventory"].kind != JNull:
      result.isLocalInventory = some(to(node["is_local_inventory"], typeof(result.isLocalInventory.get())))
    if node.hasKey("lifetime_frequency_cap") and node["lifetime_frequency_cap"].kind != JNull:
      result.lifetimeFrequencyCap = some(to(node["lifetime_frequency_cap"], typeof(result.lifetimeFrequencyCap.get())))
    if node.hasKey("local_inventory_radius_in_miles") and node["local_inventory_radius_in_miles"].kind != JNull:
      result.localInventoryRadiusInMiles = some(to(node["local_inventory_radius_in_miles"], typeof(result.localInventoryRadiusInMiles.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("optimization_goal_metadata") and node["optimization_goal_metadata"].kind != JNull:
      result.optimizationGoalMetadata = some(to(node["optimization_goal_metadata"], typeof(result.optimizationGoalMetadata.get())))
    if node.hasKey("performance_plus_campaign_settings") and node["performance_plus_campaign_settings"].kind != JNull:
      result.performancePlusCampaignSettings = some(to(node["performance_plus_campaign_settings"], typeof(result.performancePlusCampaignSettings.get())))
    if node.hasKey("placement_group") and node["placement_group"].kind != JNull:
      result.placementGroup = some(to(node["placement_group"], typeof(result.placementGroup.get())))
    if node.hasKey("placement_traffic_type") and node["placement_traffic_type"].kind != JNull:
      result.placementTrafficType = some(to(node["placement_traffic_type"], typeof(result.placementTrafficType.get())))
    if node.hasKey("promotion_application_level") and node["promotion_application_level"].kind != JNull:
      result.promotionApplicationLevel = some(to(node["promotion_application_level"], typeof(result.promotionApplicationLevel.get())))
    if node.hasKey("promotion_id") and node["promotion_id"].kind != JNull:
      result.promotionId = some(to(node["promotion_id"], typeof(result.promotionId.get())))
    if node.hasKey("promotion_ids") and node["promotion_ids"].kind != JNull:
      result.promotionIds = some(to(node["promotion_ids"], typeof(result.promotionIds.get())))
    if node.hasKey("start_time") and node["start_time"].kind != JNull:
      result.startTime = some(to(node["start_time"], typeof(result.startTime.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("summary_status"):
      result.summaryStatus = to(node["summary_status"], SummaryStatus)
    if node.hasKey("targeting_spec") and node["targeting_spec"].kind != JNull:
      result.targetingSpec = some(to(node["targeting_spec"], typeof(result.targetingSpec.get())))
    if node.hasKey("targeting_template_ids") and node["targeting_template_ids"].kind != JNull:
      result.targetingTemplateIds = some(to(node["targeting_template_ids"], typeof(result.targetingTemplateIds.get())))
    if node.hasKey("tracking_urls") and node["tracking_urls"].kind != JNull:
      result.trackingUrls = some(to(node["tracking_urls"], typeof(result.trackingUrls.get())))
    if node.hasKey("type"):
      result.`type` = to(node["type"], string)
    if node.hasKey("updated_time"):
      result.updatedTime = to(node["updated_time"], int)
    if node.hasKey("auto_targeting_enabled") and node["auto_targeting_enabled"].kind != JNull:
      result.autoTargetingEnabled = some(to(node["auto_targeting_enabled"], typeof(result.autoTargetingEnabled.get())))
    if node.hasKey("bid_multiplier") and node["bid_multiplier"].kind != JNull:
      result.bidMultiplier = some(to(node["bid_multiplier"], typeof(result.bidMultiplier.get())))
    if node.hasKey("budget_type") and node["budget_type"].kind != JNull:
      result.budgetType = some(to(node["budget_type"], typeof(result.budgetType.get())))
    if node.hasKey("pacing_delivery_type") and node["pacing_delivery_type"].kind != JNull:
      result.pacingDeliveryType = some(to(node["pacing_delivery_type"], typeof(result.pacingDeliveryType.get())))

# Custom JSON serialization for AdGroup with custom field names
proc `%`*(obj: AdGroup): JsonNode =
  result = newJObject()
  result["ad_account_id"] = %obj.adAccountId
  if obj.bidInMicroCurrency.isSome():
    result["bid_in_micro_currency"] = %obj.bidInMicroCurrency.get()
  if obj.bidStrategyType.isSome():
    result["bid_strategy_type"] = %obj.bidStrategyType.get()
  result["billable_event"] = %obj.billableEvent
  if obj.budgetInMicroCurrency.isSome():
    result["budget_in_micro_currency"] = %obj.budgetInMicroCurrency.get()
  result["campaign_id"] = %obj.campaignId
  result["conversion_learning_mode_type"] = %obj.conversionLearningModeType
  result["created_time"] = %obj.createdTime
  if obj.customerSegmentId.isSome():
    result["customer_segment_id"] = %obj.customerSegmentId.get()
  result["dca_assets"] = %obj.dcaAssets
  if obj.endTime.isSome():
    result["end_time"] = %obj.endTime.get()
  if obj.extFeatures.isSome():
    result["ext_features"] = %obj.extFeatures.get()
  if obj.feedProfileId.isSome():
    result["feed_profile_id"] = %obj.feedProfileId.get()
  result["id"] = %obj.id
  if obj.isCreativeOptimization.isSome():
    result["is_creative_optimization"] = %obj.isCreativeOptimization.get()
  if obj.isLocalInventory.isSome():
    result["is_local_inventory"] = %obj.isLocalInventory.get()
  if obj.lifetimeFrequencyCap.isSome():
    result["lifetime_frequency_cap"] = %obj.lifetimeFrequencyCap.get()
  if obj.localInventoryRadiusInMiles.isSome():
    result["local_inventory_radius_in_miles"] = %obj.localInventoryRadiusInMiles.get()
  result["name"] = %obj.name
  if obj.optimizationGoalMetadata.isSome():
    result["optimization_goal_metadata"] = %obj.optimizationGoalMetadata.get()
  if obj.performancePlusCampaignSettings.isSome():
    result["performance_plus_campaign_settings"] = %obj.performancePlusCampaignSettings.get()
  if obj.placementGroup.isSome():
    result["placement_group"] = %obj.placementGroup.get()
  if obj.placementTrafficType.isSome():
    result["placement_traffic_type"] = %obj.placementTrafficType.get()
  if obj.promotionApplicationLevel.isSome():
    result["promotion_application_level"] = %obj.promotionApplicationLevel.get()
  if obj.promotionId.isSome():
    result["promotion_id"] = %obj.promotionId.get()
  if obj.promotionIds.isSome():
    result["promotion_ids"] = %obj.promotionIds.get()
  if obj.startTime.isSome():
    result["start_time"] = %obj.startTime.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  result["summary_status"] = %obj.summaryStatus
  if obj.targetingSpec.isSome():
    result["targeting_spec"] = %obj.targetingSpec.get()
  if obj.targetingTemplateIds.isSome():
    result["targeting_template_ids"] = %obj.targetingTemplateIds.get()
  if obj.trackingUrls.isSome():
    result["tracking_urls"] = %obj.trackingUrls.get()
  result["type"] = %obj.`type`
  result["updated_time"] = %obj.updatedTime
  if obj.autoTargetingEnabled.isSome():
    result["auto_targeting_enabled"] = %obj.autoTargetingEnabled.get()
  if obj.bidMultiplier.isSome():
    result["bid_multiplier"] = %obj.bidMultiplier.get()
  if obj.budgetType.isSome():
    result["budget_type"] = %obj.budgetType.get()
  if obj.pacingDeliveryType.isSome():
    result["pacing_delivery_type"] = %obj.pacingDeliveryType.get()

