#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_get_business_assets_response

type BusinessAssetsGet200response* = object
  ## 
  items*: seq[GetBusinessAssetsResponse] ## List of assets the requesting business has access to.
  bookmark*: Option[string]


# Custom JSON deserialization for BusinessAssetsGet200response with custom field names
proc to*(node: JsonNode, T: typedesc[BusinessAssetsGet200response]): BusinessAssetsGet200response =
  result = BusinessAssetsGet200response()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, GetBusinessAssetsResponse))
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))

# Custom JSON serialization for BusinessAssetsGet200response with custom field names
proc `%`*(obj: BusinessAssetsGet200response): JsonNode =
  result = newJObject()
  result["items"] = %obj.items
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()

