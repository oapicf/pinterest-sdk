#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_update_partner_asset_access_item

type UpdatePartnerAssetAccessBody* = object
  ## An object with a list of partner asset accesses to assign or update.
  accesses*: seq[UpdatePartnerAssetAccessItem] ## List of partner asset accesses to assign or update.


# Custom JSON deserialization for UpdatePartnerAssetAccessBody with custom field names
proc to*(node: JsonNode, T: typedesc[UpdatePartnerAssetAccessBody]): UpdatePartnerAssetAccessBody =
  result = UpdatePartnerAssetAccessBody()
  if node.kind == JObject:
    if node.hasKey("accesses"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["accesses"]
      if arrayNode.kind == JArray:
        result.accesses = @[]
        for item in arrayNode.items:
          result.accesses.add(to(item, UpdatePartnerAssetAccessItem))

# Custom JSON serialization for UpdatePartnerAssetAccessBody with custom field names
proc `%`*(obj: UpdatePartnerAssetAccessBody): JsonNode =
  result = newJObject()
  result["accesses"] = %obj.accesses

