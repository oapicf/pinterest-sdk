#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_content_type

type PinMediaSourceImagesBase64Item* = object
  ## 
  contentType*: ContentType
  data*: string
  description*: Option[string]
  link*: Option[string]
  title*: Option[string]


# Custom JSON deserialization for PinMediaSourceImagesBase64Item with custom field names
proc to*(node: JsonNode, T: typedesc[PinMediaSourceImagesBase64Item]): PinMediaSourceImagesBase64Item =
  result = PinMediaSourceImagesBase64Item()
  if node.kind == JObject:
    if node.hasKey("content_type"):
      result.contentType = to(node["content_type"], ContentType)
    if node.hasKey("data"):
      result.data = to(node["data"], string)
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("link") and node["link"].kind != JNull:
      result.link = some(to(node["link"], typeof(result.link.get())))
    if node.hasKey("title") and node["title"].kind != JNull:
      result.title = some(to(node["title"], typeof(result.title.get())))

# Custom JSON serialization for PinMediaSourceImagesBase64Item with custom field names
proc `%`*(obj: PinMediaSourceImagesBase64Item): JsonNode =
  result = newJObject()
  result["content_type"] = %obj.contentType
  result["data"] = %obj.data
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.link.isSome():
    result["link"] = %obj.link.get()
  if obj.title.isSome():
    result["title"] = %obj.title.get()

