#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_conversion_attribution_window_days
import model_conversion_product_reporting_column
import model_conversion_report_time_type
import model_objective_type

type ConversionProductAttributionType* {.pure.} = enum
  DEFAULT
  BRANDATTRIBUTION

type ConversionProductBreakdown* {.pure.} = enum
  PRODUCTBRAND
  PRODUCTCATEGORY
  PRODUCTBRANDANDCATEGORY
  PRODUCTSKU
  PRODUCTSKUGROUP

type Granularity* {.pure.} = enum
  WEEK
  MONTH
  TOTAL

type Level* {.pure.} = enum
  ADVERTISER
  CAMPAIGN
  ADGROUP

type ConversionProductReportRequest* = object
  ## Request for a brand, category, SKU report
  adGroupIds*: Option[seq[string]] ## List of ad group ids. <br>Only support ad_group_ids field when level of the report is AD_GROUP.
  campaignIds*: Option[seq[string]] ## List of campaign ids. <br>Only support campaign_ids field when level of the report is CAMPAIGN.
  campaignObjectiveTypes*: Option[seq[ObjectiveType]] ## List of values for filtering. Default is ['CONSIDERATION','AWARENESS','WEB_CONVERSION','VIDEO_COMPLETION'].
  clickWindowDays*: Option[ConversionAttributionWindowDays] ## Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
  columns*: seq[ConversionProductReportingColumn] ## Metric and entity columns
  conversionProductAttributionType*: Option[ConversionProductAttributionType]
  conversionProductBreakdown*: Option[ConversionProductBreakdown]
  conversionReportTime*: Option[ConversionReportTimeType] ## The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
  endDate*: string ## Metric report end date (UTC). Format: YYYY-MM-DD.<br> A max of 1 year is allowed between the start and end date for reports.
  granularity*: Granularity ## TOTAL - metrics are aggregated over the specified date range.<br> WEEK - metrics are broken down weekly.<br> MONTH - metrics are broken down monthly.
  level*: Level ## Level of the report
  productSkuIds*: Option[seq[string]] ## List of SKU ids. <br>Only support product_sku_ids field when report breakdown type is PRODUCT_SKU_GROUP.
  reportName*: string ## Name of the conversion product report.
  startDate*: string ## Metric report start date (UTC). Format: YYYY-MM-DD.<br> Start date must be after 2024-03-16. 7 day minimum time window for report is required.
  viewWindowDays*: Option[ConversionAttributionWindowDays] ## Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` day.

func `%`*(v: ConversionProductAttributionType): JsonNode =
  result = case v:
    of ConversionProductAttributionType.DEFAULT: %"DEFAULT"
    of ConversionProductAttributionType.BRANDATTRIBUTION: %"BRAND_ATTRIBUTION"
func `$`*(v: ConversionProductAttributionType): string =
  result = case v:
    of ConversionProductAttributionType.DEFAULT: $("DEFAULT")
    of ConversionProductAttributionType.BRANDATTRIBUTION: $("BRAND_ATTRIBUTION")

proc to*(node: JsonNode, T: typedesc[ConversionProductAttributionType]): ConversionProductAttributionType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ConversionProductAttributionType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("DEFAULT"):
    return ConversionProductAttributionType.DEFAULT
  of $("BRAND_ATTRIBUTION"):
    return ConversionProductAttributionType.BRANDATTRIBUTION
  else:
    raise newException(ValueError, "Invalid enum value for ConversionProductAttributionType: " & strVal)

func `%`*(v: ConversionProductBreakdown): JsonNode =
  result = case v:
    of ConversionProductBreakdown.PRODUCTBRAND: %"PRODUCT_BRAND"
    of ConversionProductBreakdown.PRODUCTCATEGORY: %"PRODUCT_CATEGORY"
    of ConversionProductBreakdown.PRODUCTBRANDANDCATEGORY: %"PRODUCT_BRAND_AND_CATEGORY"
    of ConversionProductBreakdown.PRODUCTSKU: %"PRODUCT_SKU"
    of ConversionProductBreakdown.PRODUCTSKUGROUP: %"PRODUCT_SKU_GROUP"
func `$`*(v: ConversionProductBreakdown): string =
  result = case v:
    of ConversionProductBreakdown.PRODUCTBRAND: $("PRODUCT_BRAND")
    of ConversionProductBreakdown.PRODUCTCATEGORY: $("PRODUCT_CATEGORY")
    of ConversionProductBreakdown.PRODUCTBRANDANDCATEGORY: $("PRODUCT_BRAND_AND_CATEGORY")
    of ConversionProductBreakdown.PRODUCTSKU: $("PRODUCT_SKU")
    of ConversionProductBreakdown.PRODUCTSKUGROUP: $("PRODUCT_SKU_GROUP")

proc to*(node: JsonNode, T: typedesc[ConversionProductBreakdown]): ConversionProductBreakdown =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ConversionProductBreakdown, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("PRODUCT_BRAND"):
    return ConversionProductBreakdown.PRODUCTBRAND
  of $("PRODUCT_CATEGORY"):
    return ConversionProductBreakdown.PRODUCTCATEGORY
  of $("PRODUCT_BRAND_AND_CATEGORY"):
    return ConversionProductBreakdown.PRODUCTBRANDANDCATEGORY
  of $("PRODUCT_SKU"):
    return ConversionProductBreakdown.PRODUCTSKU
  of $("PRODUCT_SKU_GROUP"):
    return ConversionProductBreakdown.PRODUCTSKUGROUP
  else:
    raise newException(ValueError, "Invalid enum value for ConversionProductBreakdown: " & strVal)

func `%`*(v: Granularity): JsonNode =
  result = case v:
    of Granularity.WEEK: %"WEEK"
    of Granularity.MONTH: %"MONTH"
    of Granularity.TOTAL: %"TOTAL"
func `$`*(v: Granularity): string =
  result = case v:
    of Granularity.WEEK: $("WEEK")
    of Granularity.MONTH: $("MONTH")
    of Granularity.TOTAL: $("TOTAL")

proc to*(node: JsonNode, T: typedesc[Granularity]): Granularity =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Granularity, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("WEEK"):
    return Granularity.WEEK
  of $("MONTH"):
    return Granularity.MONTH
  of $("TOTAL"):
    return Granularity.TOTAL
  else:
    raise newException(ValueError, "Invalid enum value for Granularity: " & strVal)

func `%`*(v: Level): JsonNode =
  result = case v:
    of Level.ADVERTISER: %"ADVERTISER"
    of Level.CAMPAIGN: %"CAMPAIGN"
    of Level.ADGROUP: %"AD_GROUP"
func `$`*(v: Level): string =
  result = case v:
    of Level.ADVERTISER: $("ADVERTISER")
    of Level.CAMPAIGN: $("CAMPAIGN")
    of Level.ADGROUP: $("AD_GROUP")

proc to*(node: JsonNode, T: typedesc[Level]): Level =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Level, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("ADVERTISER"):
    return Level.ADVERTISER
  of $("CAMPAIGN"):
    return Level.CAMPAIGN
  of $("AD_GROUP"):
    return Level.ADGROUP
  else:
    raise newException(ValueError, "Invalid enum value for Level: " & strVal)


# Custom JSON deserialization for ConversionProductReportRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionProductReportRequest]): ConversionProductReportRequest =
  result = ConversionProductReportRequest()
  if node.kind == JObject:
    if node.hasKey("ad_group_ids") and node["ad_group_ids"].kind != JNull:
      result.adGroupIds = some(to(node["ad_group_ids"], typeof(result.adGroupIds.get())))
    if node.hasKey("campaign_ids") and node["campaign_ids"].kind != JNull:
      result.campaignIds = some(to(node["campaign_ids"], typeof(result.campaignIds.get())))
    if node.hasKey("campaign_objective_types") and node["campaign_objective_types"].kind != JNull:
      result.campaignObjectiveTypes = some(to(node["campaign_objective_types"], typeof(result.campaignObjectiveTypes.get())))
    if node.hasKey("click_window_days") and node["click_window_days"].kind != JNull:
      result.clickWindowDays = some(to(node["click_window_days"], typeof(result.clickWindowDays.get())))
    if node.hasKey("columns"):
      result.columns = to(node["columns"], seq[ConversionProductReportingColumn])
    if node.hasKey("conversion_product_attribution_type") and node["conversion_product_attribution_type"].kind != JNull:
      result.conversionProductAttributionType = some(to(node["conversion_product_attribution_type"], ConversionProductAttributionType))
    if node.hasKey("conversion_product_breakdown") and node["conversion_product_breakdown"].kind != JNull:
      result.conversionProductBreakdown = some(to(node["conversion_product_breakdown"], ConversionProductBreakdown))
    if node.hasKey("conversion_report_time") and node["conversion_report_time"].kind != JNull:
      result.conversionReportTime = some(to(node["conversion_report_time"], typeof(result.conversionReportTime.get())))
    if node.hasKey("end_date"):
      result.endDate = to(node["end_date"], string)
    if node.hasKey("granularity"):
      result.granularity = to(node["granularity"], Granularity)
    if node.hasKey("level"):
      result.level = to(node["level"], Level)
    if node.hasKey("product_sku_ids") and node["product_sku_ids"].kind != JNull:
      result.productSkuIds = some(to(node["product_sku_ids"], typeof(result.productSkuIds.get())))
    if node.hasKey("report_name"):
      result.reportName = to(node["report_name"], string)
    if node.hasKey("start_date"):
      result.startDate = to(node["start_date"], string)
    if node.hasKey("view_window_days") and node["view_window_days"].kind != JNull:
      result.viewWindowDays = some(to(node["view_window_days"], typeof(result.viewWindowDays.get())))

# Custom JSON serialization for ConversionProductReportRequest with custom field names
proc `%`*(obj: ConversionProductReportRequest): JsonNode =
  result = newJObject()
  if obj.adGroupIds.isSome():
    result["ad_group_ids"] = %obj.adGroupIds.get()
  if obj.campaignIds.isSome():
    result["campaign_ids"] = %obj.campaignIds.get()
  if obj.campaignObjectiveTypes.isSome():
    result["campaign_objective_types"] = %obj.campaignObjectiveTypes.get()
  if obj.clickWindowDays.isSome():
    result["click_window_days"] = %obj.clickWindowDays.get()
  result["columns"] = %obj.columns
  if obj.conversionProductAttributionType.isSome():
    result["conversion_product_attribution_type"] = %obj.conversionProductAttributionType.get()
  if obj.conversionProductBreakdown.isSome():
    result["conversion_product_breakdown"] = %obj.conversionProductBreakdown.get()
  if obj.conversionReportTime.isSome():
    result["conversion_report_time"] = %obj.conversionReportTime.get()
  result["end_date"] = %obj.endDate
  result["granularity"] = %obj.granularity
  result["level"] = %obj.level
  if obj.productSkuIds.isSome():
    result["product_sku_ids"] = %obj.productSkuIds.get()
  result["report_name"] = %obj.reportName
  result["start_date"] = %obj.startDate
  if obj.viewWindowDays.isSome():
    result["view_window_days"] = %obj.viewWindowDays.get()

