#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BoardPrivacyFilter* {.pure.} = enum
  ALL
  PUBLIC
  PROTECTED
  SECRET
  PUBLICANDSECRET

func `%`*(v: BoardPrivacyFilter): JsonNode =
  result = case v:
    of BoardPrivacyFilter.ALL: %"ALL"
    of BoardPrivacyFilter.PUBLIC: %"PUBLIC"
    of BoardPrivacyFilter.PROTECTED: %"PROTECTED"
    of BoardPrivacyFilter.SECRET: %"SECRET"
    of BoardPrivacyFilter.PUBLICANDSECRET: %"PUBLIC_AND_SECRET"

func `$`*(v: BoardPrivacyFilter): string =
  result = case v:
    of BoardPrivacyFilter.ALL: $("ALL")
    of BoardPrivacyFilter.PUBLIC: $("PUBLIC")
    of BoardPrivacyFilter.PROTECTED: $("PROTECTED")
    of BoardPrivacyFilter.SECRET: $("SECRET")
    of BoardPrivacyFilter.PUBLICANDSECRET: $("PUBLIC_AND_SECRET")

proc to*(node: JsonNode, T: typedesc[BoardPrivacyFilter]): BoardPrivacyFilter =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum BoardPrivacyFilter, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("ALL"):
    return BoardPrivacyFilter.ALL
  of $("PUBLIC"):
    return BoardPrivacyFilter.PUBLIC
  of $("PROTECTED"):
    return BoardPrivacyFilter.PROTECTED
  of $("SECRET"):
    return BoardPrivacyFilter.SECRET
  of $("PUBLIC_AND_SECRET"):
    return BoardPrivacyFilter.PUBLICANDSECRET
  else:
    raise newException(ValueError, "Invalid enum value for BoardPrivacyFilter: " & strVal)

