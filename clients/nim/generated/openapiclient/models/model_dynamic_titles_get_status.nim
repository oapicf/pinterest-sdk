#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DynamicTitlesGetStatus* = object
  ## 
  generatedCount*: Option[int] ## The count of generated titles.
  isReady*: Option[bool] ## Whether dynamic titles have been generated and are ready to be reviewed for the ad group.
  reviewedCount*: Option[int] ## The count of advertiser reviewed titles.


# Custom JSON deserialization for DynamicTitlesGetStatus with custom field names
proc to*(node: JsonNode, T: typedesc[DynamicTitlesGetStatus]): DynamicTitlesGetStatus =
  result = DynamicTitlesGetStatus()
  if node.kind == JObject:
    if node.hasKey("generated_count") and node["generated_count"].kind != JNull:
      result.generatedCount = some(to(node["generated_count"], typeof(result.generatedCount.get())))
    if node.hasKey("is_ready") and node["is_ready"].kind != JNull:
      result.isReady = some(to(node["is_ready"], typeof(result.isReady.get())))
    if node.hasKey("reviewed_count") and node["reviewed_count"].kind != JNull:
      result.reviewedCount = some(to(node["reviewed_count"], typeof(result.reviewedCount.get())))

# Custom JSON serialization for DynamicTitlesGetStatus with custom field names
proc `%`*(obj: DynamicTitlesGetStatus): JsonNode =
  result = newJObject()
  if obj.generatedCount.isSome():
    result["generated_count"] = %obj.generatedCount.get()
  if obj.isReady.isSome():
    result["is_ready"] = %obj.isReady.get()
  if obj.reviewedCount.isSome():
    result["reviewed_count"] = %obj.reviewedCount.get()

