#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_entity_label_status
import model_label_parent_type

type EntityLabel* = object
  ## 
  entityId*: string ## Entity ID to apply label to.
  entityType*: Option[LabelParentType]
  labelId*: string ## Label ID.
  status*: Option[EntityLabelStatus]


# Custom JSON deserialization for EntityLabel with custom field names
proc to*(node: JsonNode, T: typedesc[EntityLabel]): EntityLabel =
  result = EntityLabel()
  if node.kind == JObject:
    if node.hasKey("entity_id"):
      result.entityId = to(node["entity_id"], string)
    if node.hasKey("entity_type") and node["entity_type"].kind != JNull:
      result.entityType = some(to(node["entity_type"], typeof(result.entityType.get())))
    if node.hasKey("label_id"):
      result.labelId = to(node["label_id"], string)
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))

# Custom JSON serialization for EntityLabel with custom field names
proc `%`*(obj: EntityLabel): JsonNode =
  result = newJObject()
  result["entity_id"] = %obj.entityId
  if obj.entityType.isSome():
    result["entity_type"] = %obj.entityType.get()
  result["label_id"] = %obj.labelId
  if obj.status.isSome():
    result["status"] = %obj.status.get()

