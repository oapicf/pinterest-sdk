#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_bid_options_update
import model_conversion_objective_type
import model_entity_status
import model_intended_promotion_type
import model_mobile_app_platform
import model_performance_plus_campaign_settings
import model_tracking_urls

type CampaignBatchUpdateItem* = object
  ## Single campaign update item with update-specific bid options.
  adAccountId*: Option[string] ## Setting this field does nothing. The ad account ID gets set from the path parameter.
  appId*: Option[string] ## [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  The store ID of the mobile app for `APP_INSTALL` campaigns.
  appPlatform*: Option[MobileAppPlatform] ## [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  Platform of the mobile app for `APP_INSTALL` campaigns.
  bidOptions*: Option[CampaignBidOptionsUpdate] ## Object describing an update to the campaign level bid multipliers.
  dailySpendCap*: Option[int] ## Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset.
  defaultAdGroupBudgetInMicroCurrency*: Option[int] ## Setting this field does nothing.
  endTime*: Option[int] ## Note: To clear this field on update, set the end time to -1. Timestamp in Unix format for scheduling when ads in the campaign stop appearing. Must occur after any end times for child ad groups. If `end_time` is not specified for the campaign, ads run indefinitely unless you update the campaign, changing their status to `paused`. Learn about [scheduling campaigns](/docs/api-features/managing-campaigns/#campaign-scheduling). Different end times can be set for the campaign's child ad groups, but they cannot occur after an `end_time` specified for the campaign. - If your campaign has a child ad group with an end time specified, and if you update that campaign with an `end_time` that is earlier than that of the ad group, the campaign `end_time` will supersede the ad group `end_time`, and the request will not return an error. - In this scenario, if you call [List campaigns](/docs/api/v5/campaigns-list) or [List ad groups](/docs/api/v5/ad_groups-list), the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence.
  id*: string ## Campaign ID, must be associated with the ad account ID provided in the path.
  intendedPromotionType*: Option[IntendedPromotionType]
  isAutomatedCampaign*: Option[bool] ## Note: This field is immutable unless the campaign is in draft status. Specifies whether the campaign was created in the automated campaign flow. When using Automated Campaigns, a daily spend cap must be set, and Pinterest Performance+ may not be used. The following objective types are supported: - CONSIDERATION - WEB\\_CONVERSION - SALES - LEADS - CATALOG\\_SALES
  isCampaignBudgetOptimization*: Option[bool] ## Note: This field is immutable unless the campaign is in draft status. Determines if a campaign automatically generates ad-group level budgets given a campaign budget to maximize campaign outcome. When using campaign budgets with a lifetime spend cap, the end time must be provided.
  isFlexibleDailyBudgets*: Option[bool] ## Determine if a campaign has setup for flexible daily budgets, also known as \"Pinterest Performance+ budgets\". Flexible daily budgets are only supported for campaign budget optimization with a daily spend cap, and not a lifetime spend cap.
  isLtvOptimized*: Option[bool] ## Specifies whether the campaign is optimized for Lifetime Value (LTV). Only available for eligible advertisers.
  isPerformancePlus*: Option[bool] ## Note: This field is immutable unless the campaign is in draft status. You can only use this feature with the following objectives: - CONSIDERATION - WEB\\_CONVERSION - SALES - LEADS - CATALOG\\_SALES - APP\\_INSTALL Enable Pinterest Performance+ for your campaign. To learn more, see [Pinterest Performance+ Setup](https://developers.pinterest.com/docs/api-features/pinterest-performance-plus-setup/)
  isTopOfSearch*: Option[bool] ## Note: This field is immutable unless the campaign is in draft status.  [Restricted](/docs/getting-started/using-beta-and-restricted-features/)  Have your ads and content appear at the top of search result lists in Pinterest.  You can only use this feature with the following objectives:  - CONSIDERATION  - WEB\\_CONVERSION  - SALES  - LEADS  - CATALOG\\_SALES  You cannot use this feature with Pinterest Performance+ or Premiere Spotlight enabled.
  lifetimeSpendCap*: Option[int] ## Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset.
  name*: Option[string] ## Campaign name - 255 chars max.
  objectiveType*: Option[ConversionObjectiveType]
  orderLineId*: Option[string] ## Note: To clear this field on update, set the order line ID to 0. Order line ID that appears on the invoice. The order line must not be expired.
  performancePlusCampaignSettings*: Option[PerformancePlusCampaignSettings] ## Pinterest Performance+ campaign settings.
  startTime*: Option[int] ## Timestamp in Unix format for scheduling when ads in the campaign start to appear. Must precede any start times set for child ad groups. Defaults to current time if no time is specified. Learn about [scheduling campaigns](/docs/api-features/managing-campaigns/#campaign-scheduling). Different start times can be set for the campaign's child ad groups, but they cannot occur before a `start_time` specified for the campaign. - If your campaign has a child ad group with a start time specified, and if you update that campaign with a `start_time` that is later than that of the ad group, the campaign `start_time` will supersede the ad group `start_time`, and the request will not return an error. - In this scenario, if you call [List campaigns](/docs/api/v5/campaigns-list) or [List ad groups](/docs/api/v5/ad_groups-list), the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence.
  status*: Option[EntityStatus]
  trackingUrls*: Option[TrackingUrls]


# Custom JSON deserialization for CampaignBatchUpdateItem with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignBatchUpdateItem]): CampaignBatchUpdateItem =
  result = CampaignBatchUpdateItem()
  if node.kind == JObject:
    if node.hasKey("ad_account_id") and node["ad_account_id"].kind != JNull:
      result.adAccountId = some(to(node["ad_account_id"], typeof(result.adAccountId.get())))
    if node.hasKey("app_id") and node["app_id"].kind != JNull:
      result.appId = some(to(node["app_id"], typeof(result.appId.get())))
    if node.hasKey("app_platform") and node["app_platform"].kind != JNull:
      result.appPlatform = some(to(node["app_platform"], typeof(result.appPlatform.get())))
    if node.hasKey("bid_options") and node["bid_options"].kind != JNull:
      result.bidOptions = some(to(node["bid_options"], typeof(result.bidOptions.get())))
    if node.hasKey("daily_spend_cap") and node["daily_spend_cap"].kind != JNull:
      result.dailySpendCap = some(to(node["daily_spend_cap"], typeof(result.dailySpendCap.get())))
    if node.hasKey("default_ad_group_budget_in_micro_currency") and node["default_ad_group_budget_in_micro_currency"].kind != JNull:
      result.defaultAdGroupBudgetInMicroCurrency = some(to(node["default_ad_group_budget_in_micro_currency"], typeof(result.defaultAdGroupBudgetInMicroCurrency.get())))
    if node.hasKey("end_time") and node["end_time"].kind != JNull:
      result.endTime = some(to(node["end_time"], typeof(result.endTime.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("intended_promotion_type") and node["intended_promotion_type"].kind != JNull:
      result.intendedPromotionType = some(to(node["intended_promotion_type"], typeof(result.intendedPromotionType.get())))
    if node.hasKey("is_automated_campaign") and node["is_automated_campaign"].kind != JNull:
      result.isAutomatedCampaign = some(to(node["is_automated_campaign"], typeof(result.isAutomatedCampaign.get())))
    if node.hasKey("is_campaign_budget_optimization") and node["is_campaign_budget_optimization"].kind != JNull:
      result.isCampaignBudgetOptimization = some(to(node["is_campaign_budget_optimization"], typeof(result.isCampaignBudgetOptimization.get())))
    if node.hasKey("is_flexible_daily_budgets") and node["is_flexible_daily_budgets"].kind != JNull:
      result.isFlexibleDailyBudgets = some(to(node["is_flexible_daily_budgets"], typeof(result.isFlexibleDailyBudgets.get())))
    if node.hasKey("is_ltv_optimized") and node["is_ltv_optimized"].kind != JNull:
      result.isLtvOptimized = some(to(node["is_ltv_optimized"], typeof(result.isLtvOptimized.get())))
    if node.hasKey("is_performance_plus") and node["is_performance_plus"].kind != JNull:
      result.isPerformancePlus = some(to(node["is_performance_plus"], typeof(result.isPerformancePlus.get())))
    if node.hasKey("is_top_of_search") and node["is_top_of_search"].kind != JNull:
      result.isTopOfSearch = some(to(node["is_top_of_search"], typeof(result.isTopOfSearch.get())))
    if node.hasKey("lifetime_spend_cap") and node["lifetime_spend_cap"].kind != JNull:
      result.lifetimeSpendCap = some(to(node["lifetime_spend_cap"], typeof(result.lifetimeSpendCap.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("objective_type") and node["objective_type"].kind != JNull:
      result.objectiveType = some(to(node["objective_type"], typeof(result.objectiveType.get())))
    if node.hasKey("order_line_id") and node["order_line_id"].kind != JNull:
      result.orderLineId = some(to(node["order_line_id"], typeof(result.orderLineId.get())))
    if node.hasKey("performance_plus_campaign_settings") and node["performance_plus_campaign_settings"].kind != JNull:
      result.performancePlusCampaignSettings = some(to(node["performance_plus_campaign_settings"], typeof(result.performancePlusCampaignSettings.get())))
    if node.hasKey("start_time") and node["start_time"].kind != JNull:
      result.startTime = some(to(node["start_time"], typeof(result.startTime.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("tracking_urls") and node["tracking_urls"].kind != JNull:
      result.trackingUrls = some(to(node["tracking_urls"], typeof(result.trackingUrls.get())))

# Custom JSON serialization for CampaignBatchUpdateItem with custom field names
proc `%`*(obj: CampaignBatchUpdateItem): JsonNode =
  result = newJObject()
  if obj.adAccountId.isSome():
    result["ad_account_id"] = %obj.adAccountId.get()
  if obj.appId.isSome():
    result["app_id"] = %obj.appId.get()
  if obj.appPlatform.isSome():
    result["app_platform"] = %obj.appPlatform.get()
  if obj.bidOptions.isSome():
    result["bid_options"] = %obj.bidOptions.get()
  if obj.dailySpendCap.isSome():
    result["daily_spend_cap"] = %obj.dailySpendCap.get()
  if obj.defaultAdGroupBudgetInMicroCurrency.isSome():
    result["default_ad_group_budget_in_micro_currency"] = %obj.defaultAdGroupBudgetInMicroCurrency.get()
  if obj.endTime.isSome():
    result["end_time"] = %obj.endTime.get()
  result["id"] = %obj.id
  if obj.intendedPromotionType.isSome():
    result["intended_promotion_type"] = %obj.intendedPromotionType.get()
  if obj.isAutomatedCampaign.isSome():
    result["is_automated_campaign"] = %obj.isAutomatedCampaign.get()
  if obj.isCampaignBudgetOptimization.isSome():
    result["is_campaign_budget_optimization"] = %obj.isCampaignBudgetOptimization.get()
  if obj.isFlexibleDailyBudgets.isSome():
    result["is_flexible_daily_budgets"] = %obj.isFlexibleDailyBudgets.get()
  if obj.isLtvOptimized.isSome():
    result["is_ltv_optimized"] = %obj.isLtvOptimized.get()
  if obj.isPerformancePlus.isSome():
    result["is_performance_plus"] = %obj.isPerformancePlus.get()
  if obj.isTopOfSearch.isSome():
    result["is_top_of_search"] = %obj.isTopOfSearch.get()
  if obj.lifetimeSpendCap.isSome():
    result["lifetime_spend_cap"] = %obj.lifetimeSpendCap.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.objectiveType.isSome():
    result["objective_type"] = %obj.objectiveType.get()
  if obj.orderLineId.isSome():
    result["order_line_id"] = %obj.orderLineId.get()
  if obj.performancePlusCampaignSettings.isSome():
    result["performance_plus_campaign_settings"] = %obj.performancePlusCampaignSettings.get()
  if obj.startTime.isSome():
    result["start_time"] = %obj.startTime.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.trackingUrls.isSome():
    result["tracking_urls"] = %obj.trackingUrls.get()

