#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_order_line

type OrderLinesList200response* = object
  ## 
  items*: seq[OrderLine]
  bookmark*: Option[string]


# Custom JSON deserialization for OrderLinesList200response with custom field names
proc to*(node: JsonNode, T: typedesc[OrderLinesList200response]): OrderLinesList200response =
  result = OrderLinesList200response()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, OrderLine))
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))

# Custom JSON serialization for OrderLinesList200response with custom field names
proc `%`*(obj: OrderLinesList200response): JsonNode =
  result = newJObject()
  result["items"] = %obj.items
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()

