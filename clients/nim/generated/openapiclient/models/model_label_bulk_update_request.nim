#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_label_status_bulk_update

type LabelBulkUpdateRequest* = object
  ## 
  id*: string ## Label ID.
  parentId*: string ## Unique identifier of the asset you are labelling. Currently, you can only label campaigns.
  status*: LabelStatusBulkUpdate


# Custom JSON deserialization for LabelBulkUpdateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[LabelBulkUpdateRequest]): LabelBulkUpdateRequest =
  result = LabelBulkUpdateRequest()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("parent_id"):
      result.parentId = to(node["parent_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], LabelStatusBulkUpdate)

# Custom JSON serialization for LabelBulkUpdateRequest with custom field names
proc `%`*(obj: LabelBulkUpdateRequest): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["parent_id"] = %obj.parentId
  result["status"] = %obj.status

