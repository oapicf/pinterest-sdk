#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PublicTargetingType* {.pure.} = enum
  APPTYPE
  GENDER
  LOCALE
  AGEBUCKET
  LOCATION
  GEO
  INTEREST
  KEYWORD
  AUDIENCEINCLUDE
  AUDIENCEEXCLUDE

func `%`*(v: PublicTargetingType): JsonNode =
  result = case v:
    of PublicTargetingType.APPTYPE: %"APPTYPE"
    of PublicTargetingType.GENDER: %"GENDER"
    of PublicTargetingType.LOCALE: %"LOCALE"
    of PublicTargetingType.AGEBUCKET: %"AGE_BUCKET"
    of PublicTargetingType.LOCATION: %"LOCATION"
    of PublicTargetingType.GEO: %"GEO"
    of PublicTargetingType.INTEREST: %"INTEREST"
    of PublicTargetingType.KEYWORD: %"KEYWORD"
    of PublicTargetingType.AUDIENCEINCLUDE: %"AUDIENCE_INCLUDE"
    of PublicTargetingType.AUDIENCEEXCLUDE: %"AUDIENCE_EXCLUDE"

func `$`*(v: PublicTargetingType): string =
  result = case v:
    of PublicTargetingType.APPTYPE: $("APPTYPE")
    of PublicTargetingType.GENDER: $("GENDER")
    of PublicTargetingType.LOCALE: $("LOCALE")
    of PublicTargetingType.AGEBUCKET: $("AGE_BUCKET")
    of PublicTargetingType.LOCATION: $("LOCATION")
    of PublicTargetingType.GEO: $("GEO")
    of PublicTargetingType.INTEREST: $("INTEREST")
    of PublicTargetingType.KEYWORD: $("KEYWORD")
    of PublicTargetingType.AUDIENCEINCLUDE: $("AUDIENCE_INCLUDE")
    of PublicTargetingType.AUDIENCEEXCLUDE: $("AUDIENCE_EXCLUDE")

proc to*(node: JsonNode, T: typedesc[PublicTargetingType]): PublicTargetingType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum PublicTargetingType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("APPTYPE"):
    return PublicTargetingType.APPTYPE
  of $("GENDER"):
    return PublicTargetingType.GENDER
  of $("LOCALE"):
    return PublicTargetingType.LOCALE
  of $("AGE_BUCKET"):
    return PublicTargetingType.AGEBUCKET
  of $("LOCATION"):
    return PublicTargetingType.LOCATION
  of $("GEO"):
    return PublicTargetingType.GEO
  of $("INTEREST"):
    return PublicTargetingType.INTEREST
  of $("KEYWORD"):
    return PublicTargetingType.KEYWORD
  of $("AUDIENCE_INCLUDE"):
    return PublicTargetingType.AUDIENCEINCLUDE
  of $("AUDIENCE_EXCLUDE"):
    return PublicTargetingType.AUDIENCEEXCLUDE
  else:
    raise newException(ValueError, "Invalid enum value for PublicTargetingType: " & strVal)

