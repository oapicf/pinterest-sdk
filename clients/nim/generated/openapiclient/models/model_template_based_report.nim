#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_bulk_reporting_job_status

type TemplateBasedReport* = object
  ## 
  message*: Option[string]
  reportStatus*: BulkReportingJobStatus
  templateId*: string ## Unique identifier of a template.
  token*: Option[string]


# Custom JSON deserialization for TemplateBasedReport with custom field names
proc to*(node: JsonNode, T: typedesc[TemplateBasedReport]): TemplateBasedReport =
  result = TemplateBasedReport()
  if node.kind == JObject:
    if node.hasKey("message") and node["message"].kind != JNull:
      result.message = some(to(node["message"], typeof(result.message.get())))
    if node.hasKey("report_status"):
      result.reportStatus = to(node["report_status"], BulkReportingJobStatus)
    if node.hasKey("template_id"):
      result.templateId = to(node["template_id"], string)
    if node.hasKey("token") and node["token"].kind != JNull:
      result.token = some(to(node["token"], typeof(result.token.get())))

# Custom JSON serialization for TemplateBasedReport with custom field names
proc `%`*(obj: TemplateBasedReport): JsonNode =
  result = newJObject()
  if obj.message.isSome():
    result["message"] = %obj.message.get()
  result["report_status"] = %obj.reportStatus
  result["template_id"] = %obj.templateId
  if obj.token.isSome():
    result["token"] = %obj.token.get()

