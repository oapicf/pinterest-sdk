#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_hotel_product_group_filters

type CatalogType* {.pure.} = enum
  HOTEL

type CatalogsHotelProductGroupCreateRequest* = object
  ## Request object for creating a hotel product group.
  catalogType*: CatalogType
  name*: string
  description*: Option[string]
  filters*: CatalogsHotelProductGroupFilters
  catalogId*: string ## Catalog id pertaining to the hotel product group.

func `%`*(v: CatalogType): JsonNode =
  result = case v:
    of CatalogType.HOTEL: %"HOTEL"
func `$`*(v: CatalogType): string =
  result = case v:
    of CatalogType.HOTEL: $("HOTEL")

proc to*(node: JsonNode, T: typedesc[CatalogType]): CatalogType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CatalogType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("HOTEL"):
    return CatalogType.HOTEL
  else:
    raise newException(ValueError, "Invalid enum value for CatalogType: " & strVal)


# Custom JSON deserialization for CatalogsHotelProductGroupCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsHotelProductGroupCreateRequest]): CatalogsHotelProductGroupCreateRequest =
  result = CatalogsHotelProductGroupCreateRequest()
  if node.kind == JObject:
    if node.hasKey("catalog_type"):
      result.catalogType = to(node["catalog_type"], CatalogType)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("filters"):
      result.filters = to(node["filters"], CatalogsHotelProductGroupFilters)
    if node.hasKey("catalog_id"):
      result.catalogId = to(node["catalog_id"], string)

# Custom JSON serialization for CatalogsHotelProductGroupCreateRequest with custom field names
proc `%`*(obj: CatalogsHotelProductGroupCreateRequest): JsonNode =
  result = newJObject()
  result["catalog_type"] = %obj.catalogType
  result["name"] = %obj.name
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  result["filters"] = %obj.filters
  result["catalog_id"] = %obj.catalogId

