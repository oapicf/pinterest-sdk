#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_customer_list_record_row
import model_user_list_type

type CustomerListCreate* = object
  ## Resource create operation model.
  isNca*: Option[bool] ## Whether the list was uploaded for new customer acquisition (expanded matching). Immutable after creation.
  listType*: Option[UserListType] ## Type of customer list (e.g., EMAIL, IDFA, MAID).
  name*: string ## Customer list name.
  records*: Option[string] ## Records list. Can be any combination of emails, MAIDs, or IDFAs. Emails must be lowercase and can be plain text or hashed using SHA1, SHA256, or MD5. MAIDs and IDFAs must be hashed with SHA1, SHA256, or MD5.
  recordsV2*: Option[seq[CustomerListRecordRow]] ## Multi-field record format. Array of objects with optional email, maid, ip_address, user_agent, external_id, hashed_pinner_id, hashed_phone_number, and liveramp_envelope per row. Provide exactly one of records or records_v2.


# Custom JSON deserialization for CustomerListCreate with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerListCreate]): CustomerListCreate =
  result = CustomerListCreate()
  if node.kind == JObject:
    if node.hasKey("is_nca") and node["is_nca"].kind != JNull:
      result.isNca = some(to(node["is_nca"], typeof(result.isNca.get())))
    if node.hasKey("list_type") and node["list_type"].kind != JNull:
      result.listType = some(to(node["list_type"], typeof(result.listType.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("records") and node["records"].kind != JNull:
      result.records = some(to(node["records"], typeof(result.records.get())))
    if node.hasKey("records_v2") and node["records_v2"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["records_v2"]
      if arrayNode.kind == JArray:
        var arr: seq[CustomerListRecordRow] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CustomerListRecordRow))
        result.recordsV2 = some(arr)

# Custom JSON serialization for CustomerListCreate with custom field names
proc `%`*(obj: CustomerListCreate): JsonNode =
  result = newJObject()
  if obj.isNca.isSome():
    result["is_nca"] = %obj.isNca.get()
  if obj.listType.isSome():
    result["list_type"] = %obj.listType.get()
  result["name"] = %obj.name
  if obj.records.isSome():
    result["records"] = %obj.records.get()
  if obj.recordsV2.isSome():
    result["records_v2"] = %obj.recordsV2.get()

