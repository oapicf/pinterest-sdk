#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TrackingUrls* = object
  ## Third-party tracking URLs. Up to three tracking URLs - with a max length of 2,000 - are supported for each event type. Tracking URLs set at the ad group or ad level can override those set at the campaign level. For more information, see <a href=\"https://help.pinterest.com/en/business/article/third-party-and-dynamic-tracking\" target=\"_blank\">Third-party and dynamic tracking</a>.
  audienceVerification*: Option[seq[string]]
  buyableButton*: Option[seq[string]]
  click*: Option[seq[string]]
  engagement*: Option[seq[string]]
  impression*: Option[seq[string]]


# Custom JSON deserialization for TrackingUrls with custom field names
proc to*(node: JsonNode, T: typedesc[TrackingUrls]): TrackingUrls =
  result = TrackingUrls()
  if node.kind == JObject:
    if node.hasKey("audience_verification") and node["audience_verification"].kind != JNull:
      result.audienceVerification = some(to(node["audience_verification"], typeof(result.audienceVerification.get())))
    if node.hasKey("buyable_button") and node["buyable_button"].kind != JNull:
      result.buyableButton = some(to(node["buyable_button"], typeof(result.buyableButton.get())))
    if node.hasKey("click") and node["click"].kind != JNull:
      result.click = some(to(node["click"], typeof(result.click.get())))
    if node.hasKey("engagement") and node["engagement"].kind != JNull:
      result.engagement = some(to(node["engagement"], typeof(result.engagement.get())))
    if node.hasKey("impression") and node["impression"].kind != JNull:
      result.impression = some(to(node["impression"], typeof(result.impression.get())))

# Custom JSON serialization for TrackingUrls with custom field names
proc `%`*(obj: TrackingUrls): JsonNode =
  result = newJObject()
  if obj.audienceVerification.isSome():
    result["audience_verification"] = %obj.audienceVerification.get()
  if obj.buyableButton.isSome():
    result["buyable_button"] = %obj.buyableButton.get()
  if obj.click.isSome():
    result["click"] = %obj.click.get()
  if obj.engagement.isSome():
    result["engagement"] = %obj.engagement.get()
  if obj.impression.isSome():
    result["impression"] = %obj.impression.get()

