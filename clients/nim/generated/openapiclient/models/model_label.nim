#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_nullable_label_status
import model_nullable_label_type

type Label* = object
  ## 
  id*: string ## Label ID.
  labelType*: Option[NullableLabelType]
  status*: Option[NullableLabelStatus]
  value*: string ## Label name. 100-character limit.


# Custom JSON deserialization for Label with custom field names
proc to*(node: JsonNode, T: typedesc[Label]): Label =
  result = Label()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("label_type") and node["label_type"].kind != JNull:
      result.labelType = some(to(node["label_type"], typeof(result.labelType.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("value"):
      result.value = to(node["value"], string)

# Custom JSON serialization for Label with custom field names
proc `%`*(obj: Label): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  if obj.labelType.isSome():
    result["label_type"] = %obj.labelType.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  result["value"] = %obj.value

