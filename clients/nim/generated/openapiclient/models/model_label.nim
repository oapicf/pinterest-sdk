#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_label_status
import model_label_type

type ParentType* {.pure.} = enum
  CAMPAIGN

type Label* = object
  ## 
  id*: Option[string] ## Label ID.
  labelType*: Option[LabelType]
  parentId*: Option[string] ## Label parent entity ID.
  parentType*: Option[ParentType] ## Label parent entity type.
  status*: Option[LabelStatus]
  value*: Option[string] ## Label name.

func `%`*(v: ParentType): JsonNode =
  result = case v:
    of ParentType.CAMPAIGN: %"CAMPAIGN"
func `$`*(v: ParentType): string =
  result = case v:
    of ParentType.CAMPAIGN: $("CAMPAIGN")

proc to*(node: JsonNode, T: typedesc[ParentType]): ParentType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ParentType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("CAMPAIGN"):
    return ParentType.CAMPAIGN
  else:
    raise newException(ValueError, "Invalid enum value for ParentType: " & strVal)


# Custom JSON deserialization for Label with custom field names
proc to*(node: JsonNode, T: typedesc[Label]): Label =
  result = Label()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("label_type") and node["label_type"].kind != JNull:
      result.labelType = some(to(node["label_type"], typeof(result.labelType.get())))
    if node.hasKey("parent_id") and node["parent_id"].kind != JNull:
      result.parentId = some(to(node["parent_id"], typeof(result.parentId.get())))
    if node.hasKey("parent_type") and node["parent_type"].kind != JNull:
      result.parentType = some(to(node["parent_type"], ParentType))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("value") and node["value"].kind != JNull:
      result.value = some(to(node["value"], typeof(result.value.get())))

# Custom JSON serialization for Label with custom field names
proc `%`*(obj: Label): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.labelType.isSome():
    result["label_type"] = %obj.labelType.get()
  if obj.parentId.isSome():
    result["parent_id"] = %obj.parentId.get()
  if obj.parentType.isSome():
    result["parent_type"] = %obj.parentType.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.value.isSome():
    result["value"] = %obj.value.get()

