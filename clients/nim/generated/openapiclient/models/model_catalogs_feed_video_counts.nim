#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CatalogsFeedVideoCounts* = object
  ## Counts of total, ingested, and not ingested videos in the feed file. The counts may not appear early in the process.
  ingestedVideos*: Option[int] ## The number of videos successfully ingested from the feed file.
  notIngestedVideos*: Option[int] ## The number of videos that were not ingested from the feed file.
  totalVideos*: Option[int] ## The number of videos in the feed file.


# Custom JSON deserialization for CatalogsFeedVideoCounts with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsFeedVideoCounts]): CatalogsFeedVideoCounts =
  result = CatalogsFeedVideoCounts()
  if node.kind == JObject:
    if node.hasKey("ingested_videos") and node["ingested_videos"].kind != JNull:
      result.ingestedVideos = some(to(node["ingested_videos"], typeof(result.ingestedVideos.get())))
    if node.hasKey("not_ingested_videos") and node["not_ingested_videos"].kind != JNull:
      result.notIngestedVideos = some(to(node["not_ingested_videos"], typeof(result.notIngestedVideos.get())))
    if node.hasKey("total_videos") and node["total_videos"].kind != JNull:
      result.totalVideos = some(to(node["total_videos"], typeof(result.totalVideos.get())))

# Custom JSON serialization for CatalogsFeedVideoCounts with custom field names
proc `%`*(obj: CatalogsFeedVideoCounts): JsonNode =
  result = newJObject()
  if obj.ingestedVideos.isSome():
    result["ingested_videos"] = %obj.ingestedVideos.get()
  if obj.notIngestedVideos.isSome():
    result["not_ingested_videos"] = %obj.notIngestedVideos.get()
  if obj.totalVideos.isSome():
    result["total_videos"] = %obj.totalVideos.get()

