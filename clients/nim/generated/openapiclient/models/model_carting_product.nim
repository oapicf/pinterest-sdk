#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_carting_retailer

type CartingProduct* = object
  ## 
  cartingProductId*: string ## the internal Pinterest carting product id (different from industry product id)
  displayPreferredRetailersOnly*: Option[bool] ## Whether to display only the preferred retailers for the carting product
  displayProductPrice*: Option[bool] ## Whether to display the price for the carting product
  preferredRetailers*: Option[seq[CartingRetailer]] ## A sorted array of preferred retailers for the carting product
  randomizePreferredRetailers*: Option[bool] ## Whether to randomize preferred retailers for the carting product


# Custom JSON deserialization for CartingProduct with custom field names
proc to*(node: JsonNode, T: typedesc[CartingProduct]): CartingProduct =
  result = CartingProduct()
  if node.kind == JObject:
    if node.hasKey("carting_product_id"):
      result.cartingProductId = to(node["carting_product_id"], string)
    if node.hasKey("display_preferred_retailers_only") and node["display_preferred_retailers_only"].kind != JNull:
      result.displayPreferredRetailersOnly = some(to(node["display_preferred_retailers_only"], typeof(result.displayPreferredRetailersOnly.get())))
    if node.hasKey("display_product_price") and node["display_product_price"].kind != JNull:
      result.displayProductPrice = some(to(node["display_product_price"], typeof(result.displayProductPrice.get())))
    if node.hasKey("preferred_retailers") and node["preferred_retailers"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["preferred_retailers"]
      if arrayNode.kind == JArray:
        var arr: seq[CartingRetailer] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CartingRetailer))
        result.preferredRetailers = some(arr)
    if node.hasKey("randomize_preferred_retailers") and node["randomize_preferred_retailers"].kind != JNull:
      result.randomizePreferredRetailers = some(to(node["randomize_preferred_retailers"], typeof(result.randomizePreferredRetailers.get())))

# Custom JSON serialization for CartingProduct with custom field names
proc `%`*(obj: CartingProduct): JsonNode =
  result = newJObject()
  result["carting_product_id"] = %obj.cartingProductId
  if obj.displayPreferredRetailersOnly.isSome():
    result["display_preferred_retailers_only"] = %obj.displayPreferredRetailersOnly.get()
  if obj.displayProductPrice.isSome():
    result["display_product_price"] = %obj.displayProductPrice.get()
  if obj.preferredRetailers.isSome():
    result["preferred_retailers"] = %obj.preferredRetailers.get()
  if obj.randomizePreferredRetailers.isSome():
    result["randomize_preferred_retailers"] = %obj.randomizePreferredRetailers.get()

