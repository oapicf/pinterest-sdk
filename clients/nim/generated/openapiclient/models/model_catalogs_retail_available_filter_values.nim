#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_retail_filter_values_map

type CatalogType* {.pure.} = enum
  RETAIL

type CatalogsRetailAvailableFilterValues* = object
  ## 
  catalogType*: CatalogType
  filterValues*: CatalogsRetailFilterValuesMap

func `%`*(v: CatalogType): JsonNode =
  result = case v:
    of CatalogType.RETAIL: %"RETAIL"
func `$`*(v: CatalogType): string =
  result = case v:
    of CatalogType.RETAIL: $("RETAIL")

proc to*(node: JsonNode, T: typedesc[CatalogType]): CatalogType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CatalogType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("RETAIL"):
    return CatalogType.RETAIL
  else:
    raise newException(ValueError, "Invalid enum value for CatalogType: " & strVal)


# Custom JSON deserialization for CatalogsRetailAvailableFilterValues with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsRetailAvailableFilterValues]): CatalogsRetailAvailableFilterValues =
  result = CatalogsRetailAvailableFilterValues()
  if node.kind == JObject:
    if node.hasKey("catalog_type"):
      result.catalogType = to(node["catalog_type"], CatalogType)
    if node.hasKey("filter_values"):
      result.filterValues = to(node["filter_values"], CatalogsRetailFilterValuesMap)

# Custom JSON serialization for CatalogsRetailAvailableFilterValues with custom field names
proc `%`*(obj: CatalogsRetailAvailableFilterValues): JsonNode =
  result = newJObject()
  result["catalog_type"] = %obj.catalogType
  result["filter_values"] = %obj.filterValues

