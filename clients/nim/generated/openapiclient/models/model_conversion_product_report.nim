#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_bulk_reporting_job_status

type ConversionProductReport* = object
  ## 
  message*: string ## Message returned from the create report request
  reportStatus*: BulkReportingJobStatus ## Async report status
  size*: float ## Size of the report in bytes
  token*: string ## Token returned from the create report request
  url*: string ## URL of the report


# Custom JSON deserialization for ConversionProductReport with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionProductReport]): ConversionProductReport =
  result = ConversionProductReport()
  if node.kind == JObject:
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("report_status"):
      result.reportStatus = to(node["report_status"], BulkReportingJobStatus)
    if node.hasKey("size"):
      result.size = to(node["size"], float)
    if node.hasKey("token"):
      result.token = to(node["token"], string)
    if node.hasKey("url"):
      result.url = to(node["url"], string)

# Custom JSON serialization for ConversionProductReport with custom field names
proc `%`*(obj: ConversionProductReport): JsonNode =
  result = newJObject()
  result["message"] = %obj.message
  result["report_status"] = %obj.reportStatus
  result["size"] = %obj.size
  result["token"] = %obj.token
  result["url"] = %obj.url

