#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_order_line_paid_type
import model_order_line_status

type OrderLine* = object
  ## 
  adAccountId*: Option[string] ## Ad account ID.
  budget*: Option[float] ## Order line budget in micro currency.
  endTime*: Option[float] ## End time. Unix timestamp.
  id*: Option[string] ## Order line ID.
  name*: Option[string] ## Order line name.
  paidBudget*: Option[float] ## Order line paid budget in micro currency.
  paidType*: Option[OrderLinePaidType] ## Order line paid type.
  purchaseOrderId*: Option[string] ## Purchase order ID.
  startTime*: Option[float] ## Start time. Unix timestamp.
  status*: Option[OrderLineStatus] ## Order line status.
  `type`*: Option[string] ## Always \"orderline\".
  campaignIds*: seq[string] ## Associated List of campaign IDs.


# Custom JSON deserialization for OrderLine with custom field names
proc to*(node: JsonNode, T: typedesc[OrderLine]): OrderLine =
  result = OrderLine()
  if node.kind == JObject:
    if node.hasKey("ad_account_id") and node["ad_account_id"].kind != JNull:
      result.adAccountId = some(to(node["ad_account_id"], typeof(result.adAccountId.get())))
    if node.hasKey("budget") and node["budget"].kind != JNull:
      result.budget = some(to(node["budget"], typeof(result.budget.get())))
    if node.hasKey("end_time") and node["end_time"].kind != JNull:
      result.endTime = some(to(node["end_time"], typeof(result.endTime.get())))
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("paid_budget") and node["paid_budget"].kind != JNull:
      result.paidBudget = some(to(node["paid_budget"], typeof(result.paidBudget.get())))
    if node.hasKey("paid_type") and node["paid_type"].kind != JNull:
      result.paidType = some(to(node["paid_type"], typeof(result.paidType.get())))
    if node.hasKey("purchase_order_id") and node["purchase_order_id"].kind != JNull:
      result.purchaseOrderId = some(to(node["purchase_order_id"], typeof(result.purchaseOrderId.get())))
    if node.hasKey("start_time") and node["start_time"].kind != JNull:
      result.startTime = some(to(node["start_time"], typeof(result.startTime.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], typeof(result.`type`.get())))
    if node.hasKey("campaign_ids"):
      result.campaignIds = to(node["campaign_ids"], seq[string])

# Custom JSON serialization for OrderLine with custom field names
proc `%`*(obj: OrderLine): JsonNode =
  result = newJObject()
  if obj.adAccountId.isSome():
    result["ad_account_id"] = %obj.adAccountId.get()
  if obj.budget.isSome():
    result["budget"] = %obj.budget.get()
  if obj.endTime.isSome():
    result["end_time"] = %obj.endTime.get()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.paidBudget.isSome():
    result["paid_budget"] = %obj.paidBudget.get()
  if obj.paidType.isSome():
    result["paid_type"] = %obj.paidType.get()
  if obj.purchaseOrderId.isSome():
    result["purchase_order_id"] = %obj.purchaseOrderId.get()
  if obj.startTime.isSome():
    result["start_time"] = %obj.startTime.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  result["campaign_ids"] = %obj.campaignIds

