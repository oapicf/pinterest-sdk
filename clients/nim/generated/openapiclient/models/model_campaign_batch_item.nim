#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_batch_response_data
import model_exception

type CampaignBatchItem* = object
  ## Item in a batch campaign response.
  data*: Option[CampaignBatchResponseData] ## Campaign data on success.
  exceptions*: Option[seq[Exception]] ## Exceptions on failure.


# Custom JSON deserialization for CampaignBatchItem with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignBatchItem]): CampaignBatchItem =
  result = CampaignBatchItem()
  if node.kind == JObject:
    if node.hasKey("data") and node["data"].kind != JNull:
      result.data = some(to(node["data"], typeof(result.data.get())))
    if node.hasKey("exceptions") and node["exceptions"].kind != JNull:
      result.exceptions = some(to(node["exceptions"], typeof(result.exceptions.get())))

# Custom JSON serialization for CampaignBatchItem with custom field names
proc `%`*(obj: CampaignBatchItem): JsonNode =
  result = newJObject()
  if obj.data.isSome():
    result["data"] = %obj.data.get()
  if obj.exceptions.isSome():
    result["exceptions"] = %obj.exceptions.get()

