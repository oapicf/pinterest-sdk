#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_media_upload_parameters
import model_media_upload_type

type MediaUpload* = object
  ## Media upload that has been registered but not uploaded/processed yet.
  mediaId*: string ## Unique identifier for this media upload. Used to track status and for attaching during Pin creation.
  mediaType*: MediaUploadType
  uploadParameters*: MediaUploadParameters ## The list of parameter key/value pairs you will need to send with your POST request to upload your media file.
  uploadUrl*: string ## The URL where you will POST your media file.


# Custom JSON deserialization for MediaUpload with custom field names
proc to*(node: JsonNode, T: typedesc[MediaUpload]): MediaUpload =
  result = MediaUpload()
  if node.kind == JObject:
    if node.hasKey("media_id"):
      result.mediaId = to(node["media_id"], string)
    if node.hasKey("media_type"):
      result.mediaType = to(node["media_type"], MediaUploadType)
    if node.hasKey("upload_parameters"):
      result.uploadParameters = to(node["upload_parameters"], MediaUploadParameters)
    if node.hasKey("upload_url"):
      result.uploadUrl = to(node["upload_url"], string)

# Custom JSON serialization for MediaUpload with custom field names
proc `%`*(obj: MediaUpload): JsonNode =
  result = newJObject()
  result["media_id"] = %obj.mediaId
  result["media_type"] = %obj.mediaType
  result["upload_parameters"] = %obj.uploadParameters
  result["upload_url"] = %obj.uploadUrl

