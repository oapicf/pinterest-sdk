#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_targeting_template_status

type CustomerSegment* = object
  ## 
  adAccountId*: string ## The ID of the ad account that this customer segment belongs to.
  audienceIds*: seq[string] ## Audience IDs included in the customer segment.
  createdTime*: int ## Customer segment created time. Unix timestamp in seconds.
  id*: string ## Customer segment ID.
  name*: string ## Customer segment name.
  status*: TargetingTemplateStatus ## Indicates whether the customer segment is active or deleted.
  updatedTime*: int ## Customer segment updated time. Unix timestamp in seconds.


# Custom JSON deserialization for CustomerSegment with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerSegment]): CustomerSegment =
  result = CustomerSegment()
  if node.kind == JObject:
    if node.hasKey("ad_account_id"):
      result.adAccountId = to(node["ad_account_id"], string)
    if node.hasKey("audience_ids"):
      result.audienceIds = to(node["audience_ids"], seq[string])
    if node.hasKey("created_time"):
      result.createdTime = to(node["created_time"], int)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], TargetingTemplateStatus)
    if node.hasKey("updated_time"):
      result.updatedTime = to(node["updated_time"], int)

# Custom JSON serialization for CustomerSegment with custom field names
proc `%`*(obj: CustomerSegment): JsonNode =
  result = newJObject()
  result["ad_account_id"] = %obj.adAccountId
  result["audience_ids"] = %obj.audienceIds
  result["created_time"] = %obj.createdTime
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["status"] = %obj.status
  result["updated_time"] = %obj.updatedTime

