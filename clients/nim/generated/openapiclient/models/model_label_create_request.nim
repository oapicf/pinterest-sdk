#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_label_create_item

type LabelCreateRequest* = object
  ## 
  labels*: seq[LabelCreateItem] ## Labels that you are applying to the campaign.


# Custom JSON deserialization for LabelCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[LabelCreateRequest]): LabelCreateRequest =
  result = LabelCreateRequest()
  if node.kind == JObject:
    if node.hasKey("labels"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["labels"]
      if arrayNode.kind == JArray:
        result.labels = @[]
        for item in arrayNode.items:
          result.labels.add(to(item, LabelCreateItem))

# Custom JSON serialization for LabelCreateRequest with custom field names
proc `%`*(obj: LabelCreateRequest): JsonNode =
  result = newJObject()
  result["labels"] = %obj.labels

