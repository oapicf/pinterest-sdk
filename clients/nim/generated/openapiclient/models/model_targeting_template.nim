#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_placement_group_type
import model_targeting_spec_optimal
import model_targeting_template_audience_sizing
import model_targeting_template_keyword
import model_targeting_template_status
import model_tracking_urls

type TargetingTemplate* = object
  ## 
  adAccountId*: string ## The ID of the advertiser that this targeting template belongs to.
  autoTargetingEnabled*: Option[bool] ## Enable auto-targeting for ad group. Also known as [\"expanded targeting\"](https://help.pinterest.com/en/business/article/expanded-targeting).
  createdTime*: int ## Targeting template created time. Unix timestamp in seconds.
  id*: string ## Targeting template ID.
  keywords*: Option[seq[TargetingTemplateKeyword]]
  name*: string ## targeting template name
  placementGroup*: Option[PlacementGroupType]
  sizing*: TargetingTemplateAudienceSizing
  status*: TargetingTemplateStatus ## Indicate targeting template is active or Deleted
  targetingAttributes*: TargetingSpecOptimal ## targeting profile attributes
  trackingUrls*: Option[TrackingUrls]
  updatedTime*: int ## Targeting template updated time.Unix timestamp in seconds.
  valid*: bool ## Inform if the targeting template is valid (ex. would be false if has revoked audience)


# Custom JSON deserialization for TargetingTemplate with custom field names
proc to*(node: JsonNode, T: typedesc[TargetingTemplate]): TargetingTemplate =
  result = TargetingTemplate()
  if node.kind == JObject:
    if node.hasKey("ad_account_id"):
      result.adAccountId = to(node["ad_account_id"], string)
    if node.hasKey("auto_targeting_enabled") and node["auto_targeting_enabled"].kind != JNull:
      result.autoTargetingEnabled = some(to(node["auto_targeting_enabled"], typeof(result.autoTargetingEnabled.get())))
    if node.hasKey("created_time"):
      result.createdTime = to(node["created_time"], int)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("keywords") and node["keywords"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["keywords"]
      if arrayNode.kind == JArray:
        var arr: seq[TargetingTemplateKeyword] = @[]
        for item in arrayNode.items:
          arr.add(to(item, TargetingTemplateKeyword))
        result.keywords = some(arr)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("placement_group") and node["placement_group"].kind != JNull:
      result.placementGroup = some(to(node["placement_group"], typeof(result.placementGroup.get())))
    if node.hasKey("sizing"):
      result.sizing = to(node["sizing"], TargetingTemplateAudienceSizing)
    if node.hasKey("status"):
      result.status = to(node["status"], TargetingTemplateStatus)
    if node.hasKey("targeting_attributes"):
      result.targetingAttributes = to(node["targeting_attributes"], TargetingSpecOptimal)
    if node.hasKey("tracking_urls") and node["tracking_urls"].kind != JNull:
      result.trackingUrls = some(to(node["tracking_urls"], typeof(result.trackingUrls.get())))
    if node.hasKey("updated_time"):
      result.updatedTime = to(node["updated_time"], int)
    if node.hasKey("valid"):
      result.valid = to(node["valid"], bool)

# Custom JSON serialization for TargetingTemplate with custom field names
proc `%`*(obj: TargetingTemplate): JsonNode =
  result = newJObject()
  result["ad_account_id"] = %obj.adAccountId
  if obj.autoTargetingEnabled.isSome():
    result["auto_targeting_enabled"] = %obj.autoTargetingEnabled.get()
  result["created_time"] = %obj.createdTime
  result["id"] = %obj.id
  if obj.keywords.isSome():
    result["keywords"] = %obj.keywords.get()
  result["name"] = %obj.name
  if obj.placementGroup.isSome():
    result["placement_group"] = %obj.placementGroup.get()
  result["sizing"] = %obj.sizing
  result["status"] = %obj.status
  result["targeting_attributes"] = %obj.targetingAttributes
  if obj.trackingUrls.isSome():
    result["tracking_urls"] = %obj.trackingUrls.get()
  result["updated_time"] = %obj.updatedTime
  result["valid"] = %obj.valid

