#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type AmazonConnectRequest* = object
  ## Request containing OTP and Amazon storefront info called by Amazon
  amazonStorefrontId*: Option[string] ## The Amazon storefront id
  amazonStorefrontName*: string ## The Amazon storefront name
  amazonStorefrontUrl*: string ## The Amazon storefront url
  amazonUserId*: Option[string] ## The Amazon user id
  isAmazonAccountLinked*: bool ## The Amazon account linking status
  oneTimePasscode*: Option[string] ## The one time passcode for Pinterest-initiated linking requests
  pinterestUserId*: Option[string] ## The Pinterest user id for Amazon-initiated linking requests


# Custom JSON deserialization for AmazonConnectRequest with custom field names
proc to*(node: JsonNode, T: typedesc[AmazonConnectRequest]): AmazonConnectRequest =
  result = AmazonConnectRequest()
  if node.kind == JObject:
    if node.hasKey("amazon_storefront_id") and node["amazon_storefront_id"].kind != JNull:
      result.amazonStorefrontId = some(to(node["amazon_storefront_id"], typeof(result.amazonStorefrontId.get())))
    if node.hasKey("amazon_storefront_name"):
      result.amazonStorefrontName = to(node["amazon_storefront_name"], string)
    if node.hasKey("amazon_storefront_url"):
      result.amazonStorefrontUrl = to(node["amazon_storefront_url"], string)
    if node.hasKey("amazon_user_id") and node["amazon_user_id"].kind != JNull:
      result.amazonUserId = some(to(node["amazon_user_id"], typeof(result.amazonUserId.get())))
    if node.hasKey("is_amazon_account_linked"):
      result.isAmazonAccountLinked = to(node["is_amazon_account_linked"], bool)
    if node.hasKey("one_time_passcode") and node["one_time_passcode"].kind != JNull:
      result.oneTimePasscode = some(to(node["one_time_passcode"], typeof(result.oneTimePasscode.get())))
    if node.hasKey("pinterest_user_id") and node["pinterest_user_id"].kind != JNull:
      result.pinterestUserId = some(to(node["pinterest_user_id"], typeof(result.pinterestUserId.get())))

# Custom JSON serialization for AmazonConnectRequest with custom field names
proc `%`*(obj: AmazonConnectRequest): JsonNode =
  result = newJObject()
  if obj.amazonStorefrontId.isSome():
    result["amazon_storefront_id"] = %obj.amazonStorefrontId.get()
  result["amazon_storefront_name"] = %obj.amazonStorefrontName
  result["amazon_storefront_url"] = %obj.amazonStorefrontUrl
  if obj.amazonUserId.isSome():
    result["amazon_user_id"] = %obj.amazonUserId.get()
  result["is_amazon_account_linked"] = %obj.isAmazonAccountLinked
  if obj.oneTimePasscode.isSome():
    result["one_time_passcode"] = %obj.oneTimePasscode.get()
  if obj.pinterestUserId.isSome():
    result["pinterest_user_id"] = %obj.pinterestUserId.get()

