#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type MsotEventName* {.pure.} = enum
  AddToCart
  Checkout
  Lead
  Signup

func `%`*(v: MsotEventName): JsonNode =
  result = case v:
    of MsotEventName.AddToCart: %"add_to_cart"
    of MsotEventName.Checkout: %"checkout"
    of MsotEventName.Lead: %"lead"
    of MsotEventName.Signup: %"signup"

func `$`*(v: MsotEventName): string =
  result = case v:
    of MsotEventName.AddToCart: $("add_to_cart")
    of MsotEventName.Checkout: $("checkout")
    of MsotEventName.Lead: $("lead")
    of MsotEventName.Signup: $("signup")

proc to*(node: JsonNode, T: typedesc[MsotEventName]): MsotEventName =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum MsotEventName, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("add_to_cart"):
    return MsotEventName.AddToCart
  of $("checkout"):
    return MsotEventName.Checkout
  of $("lead"):
    return MsotEventName.Lead
  of $("signup"):
    return MsotEventName.Signup
  else:
    raise newException(ValueError, "Invalid enum value for MsotEventName: " & strVal)

