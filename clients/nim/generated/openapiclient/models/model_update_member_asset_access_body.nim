#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_update_member_asset_access_item

type UpdateMemberAssetAccessBody* = object
  ## An object with a list of all the new member asset accesses.
  accesses*: seq[UpdateMemberAssetAccessItem] ## List of member asset accesses to assign or update.


# Custom JSON deserialization for UpdateMemberAssetAccessBody with custom field names
proc to*(node: JsonNode, T: typedesc[UpdateMemberAssetAccessBody]): UpdateMemberAssetAccessBody =
  result = UpdateMemberAssetAccessBody()
  if node.kind == JObject:
    if node.hasKey("accesses"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["accesses"]
      if arrayNode.kind == JArray:
        result.accesses = @[]
        for item in arrayNode.items:
          result.accesses.add(to(item, UpdateMemberAssetAccessItem))

# Custom JSON serialization for UpdateMemberAssetAccessBody with custom field names
proc `%`*(obj: UpdateMemberAssetAccessBody): JsonNode =
  result = newJObject()
  result["accesses"] = %obj.accesses

