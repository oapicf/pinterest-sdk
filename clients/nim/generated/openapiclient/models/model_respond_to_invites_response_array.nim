#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_respond_to_invite_result_item

type RespondToInvitesResponseArray* = object
  ## 
  items*: Option[seq[RespondToInviteResultItem]] ## List of invite/request accept/decline status. If there is an error, an exception object will be returned. If the invite/request was successfully accepted/declined, an invite object will be returned.


# Custom JSON deserialization for RespondToInvitesResponseArray with custom field names
proc to*(node: JsonNode, T: typedesc[RespondToInvitesResponseArray]): RespondToInvitesResponseArray =
  result = RespondToInvitesResponseArray()
  if node.kind == JObject:
    if node.hasKey("items") and node["items"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        var arr: seq[RespondToInviteResultItem] = @[]
        for item in arrayNode.items:
          arr.add(to(item, RespondToInviteResultItem))
        result.items = some(arr)

# Custom JSON serialization for RespondToInvitesResponseArray with custom field names
proc `%`*(obj: RespondToInvitesResponseArray): JsonNode =
  result = newJObject()
  if obj.items.isSome():
    result["items"] = %obj.items.get()

