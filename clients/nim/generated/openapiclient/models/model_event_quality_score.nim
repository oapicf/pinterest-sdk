#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ingestion_source_options
import model_lookback_period_options
import model_overall_status_options
import model_quality_components
import model_source_platform_options

type EventQualityScore* = object
  ## Schema for GET Conversion EQS response.
  ingestionSource*: IngestionSourceOptions
  lookbackPeriod*: LookbackPeriodOptions
  overallStatus*: OverallStatusOptions
  qualityComponents*: QualityComponents
  sourcePlatform*: SourcePlatformOptions


# Custom JSON deserialization for EventQualityScore with custom field names
proc to*(node: JsonNode, T: typedesc[EventQualityScore]): EventQualityScore =
  result = EventQualityScore()
  if node.kind == JObject:
    if node.hasKey("ingestion_source"):
      result.ingestionSource = to(node["ingestion_source"], IngestionSourceOptions)
    if node.hasKey("lookback_period"):
      result.lookbackPeriod = to(node["lookback_period"], LookbackPeriodOptions)
    if node.hasKey("overall_status"):
      result.overallStatus = to(node["overall_status"], OverallStatusOptions)
    if node.hasKey("quality_components"):
      result.qualityComponents = to(node["quality_components"], QualityComponents)
    if node.hasKey("source_platform"):
      result.sourcePlatform = to(node["source_platform"], SourcePlatformOptions)

# Custom JSON serialization for EventQualityScore with custom field names
proc `%`*(obj: EventQualityScore): JsonNode =
  result = newJObject()
  result["ingestion_source"] = %obj.ingestionSource
  result["lookback_period"] = %obj.lookbackPeriod
  result["overall_status"] = %obj.overallStatus
  result["quality_components"] = %obj.qualityComponents
  result["source_platform"] = %obj.sourcePlatform

