#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_interest

type UserAccountFollowedInterests200response* = object
  ## 
  items*: seq[Interest]
  bookmark*: Option[string]


# Custom JSON deserialization for UserAccountFollowedInterests200response with custom field names
proc to*(node: JsonNode, T: typedesc[UserAccountFollowedInterests200response]): UserAccountFollowedInterests200response =
  result = UserAccountFollowedInterests200response()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, Interest))
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))

# Custom JSON serialization for UserAccountFollowedInterests200response with custom field names
proc `%`*(obj: UserAccountFollowedInterests200response): JsonNode =
  result = newJObject()
  result["items"] = %obj.items
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()

