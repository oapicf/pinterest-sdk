#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_planning_budget_recommendation_point
import model_campaign_planning_experiment_budget_recommendation

type CampaignPlanningBudgetRecommendation* = object
  ## Budget recommendation response containing recommended budget, estimated days, and point estimations.
  budgetRecommendation*: Option[int] ## The recommended budget amount.
  experimentCampaignBudgetRecommendation*: Option[seq[CampaignPlanningExperimentBudgetRecommendation]] ## List of experimental budget recommendations.
  lifetimeDaysRecommendation*: Option[int] ## Recommended number of days for the campaign lifetime.
  pointEstimations*: Option[seq[CampaignPlanningBudgetRecommendationPoint]] ## List of point estimations for different budget scenarios.


# Custom JSON deserialization for CampaignPlanningBudgetRecommendation with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignPlanningBudgetRecommendation]): CampaignPlanningBudgetRecommendation =
  result = CampaignPlanningBudgetRecommendation()
  if node.kind == JObject:
    if node.hasKey("budget_recommendation") and node["budget_recommendation"].kind != JNull:
      result.budgetRecommendation = some(to(node["budget_recommendation"], typeof(result.budgetRecommendation.get())))
    if node.hasKey("experiment_campaign_budget_recommendation") and node["experiment_campaign_budget_recommendation"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["experiment_campaign_budget_recommendation"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningExperimentBudgetRecommendation] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningExperimentBudgetRecommendation))
        result.experimentCampaignBudgetRecommendation = some(arr)
    if node.hasKey("lifetime_days_recommendation") and node["lifetime_days_recommendation"].kind != JNull:
      result.lifetimeDaysRecommendation = some(to(node["lifetime_days_recommendation"], typeof(result.lifetimeDaysRecommendation.get())))
    if node.hasKey("point_estimations") and node["point_estimations"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["point_estimations"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningBudgetRecommendationPoint] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningBudgetRecommendationPoint))
        result.pointEstimations = some(arr)

# Custom JSON serialization for CampaignPlanningBudgetRecommendation with custom field names
proc `%`*(obj: CampaignPlanningBudgetRecommendation): JsonNode =
  result = newJObject()
  if obj.budgetRecommendation.isSome():
    result["budget_recommendation"] = %obj.budgetRecommendation.get()
  if obj.experimentCampaignBudgetRecommendation.isSome():
    result["experiment_campaign_budget_recommendation"] = %obj.experimentCampaignBudgetRecommendation.get()
  if obj.lifetimeDaysRecommendation.isSome():
    result["lifetime_days_recommendation"] = %obj.lifetimeDaysRecommendation.get()
  if obj.pointEstimations.isSome():
    result["point_estimations"] = %obj.pointEstimations.get()

