#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_delete_partner_asset_access_item

type DeletePartnerAssetAccessBody* = object
  ## An object with a list of partner asset accesses to delete.
  accesses*: seq[DeletePartnerAssetAccessItem] ## List of partner asset accesses to delete.


# Custom JSON deserialization for DeletePartnerAssetAccessBody with custom field names
proc to*(node: JsonNode, T: typedesc[DeletePartnerAssetAccessBody]): DeletePartnerAssetAccessBody =
  result = DeletePartnerAssetAccessBody()
  if node.kind == JObject:
    if node.hasKey("accesses"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["accesses"]
      if arrayNode.kind == JArray:
        result.accesses = @[]
        for item in arrayNode.items:
          result.accesses.add(to(item, DeletePartnerAssetAccessItem))

# Custom JSON serialization for DeletePartnerAssetAccessBody with custom field names
proc `%`*(obj: DeletePartnerAssetAccessBody): JsonNode =
  result = newJObject()
  result["accesses"] = %obj.accesses

