#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_board

type BoardsList200response* = object
  ## 
  items*: seq[Board] ## Boards
  bookmark*: Option[string]


# Custom JSON deserialization for BoardsList200response with custom field names
proc to*(node: JsonNode, T: typedesc[BoardsList200response]): BoardsList200response =
  result = BoardsList200response()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, Board))
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))

# Custom JSON serialization for BoardsList200response with custom field names
proc `%`*(obj: BoardsList200response): JsonNode =
  result = newJObject()
  result["items"] = %obj.items
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()

