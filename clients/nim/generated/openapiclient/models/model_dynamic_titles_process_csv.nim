#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_dynamic_titles_process_csv_error

type DynamicTitlesProcessCSV* = object
  ## 
  errors*: seq[DynamicTitlesProcessCSVError] ## List of validation errors. Empty on success.
  status*: string ## Processing status. Present on success.


# Custom JSON deserialization for DynamicTitlesProcessCSV with custom field names
proc to*(node: JsonNode, T: typedesc[DynamicTitlesProcessCSV]): DynamicTitlesProcessCSV =
  result = DynamicTitlesProcessCSV()
  if node.kind == JObject:
    if node.hasKey("errors"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["errors"]
      if arrayNode.kind == JArray:
        result.errors = @[]
        for item in arrayNode.items:
          result.errors.add(to(item, DynamicTitlesProcessCSVError))
    if node.hasKey("status"):
      result.status = to(node["status"], string)

# Custom JSON serialization for DynamicTitlesProcessCSV with custom field names
proc `%`*(obj: DynamicTitlesProcessCSV): JsonNode =
  result = newJObject()
  result["errors"] = %obj.errors
  result["status"] = %obj.status

