#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_bulk_campaign_delivery_estimates_item

type BulkCampaignDeliveryEstimatesResponse* = object
  ## Bulk campaign delivery estimates response.
  data*: seq[BulkCampaignDeliveryEstimatesItem] ## Per-campaign delivery estimate results, in the same order as the request.


# Custom JSON deserialization for BulkCampaignDeliveryEstimatesResponse with custom field names
proc to*(node: JsonNode, T: typedesc[BulkCampaignDeliveryEstimatesResponse]): BulkCampaignDeliveryEstimatesResponse =
  result = BulkCampaignDeliveryEstimatesResponse()
  if node.kind == JObject:
    if node.hasKey("data"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        result.data = @[]
        for item in arrayNode.items:
          result.data.add(to(item, BulkCampaignDeliveryEstimatesItem))

# Custom JSON serialization for BulkCampaignDeliveryEstimatesResponse with custom field names
proc `%`*(obj: BulkCampaignDeliveryEstimatesResponse): JsonNode =
  result = newJObject()
  result["data"] = %obj.data

