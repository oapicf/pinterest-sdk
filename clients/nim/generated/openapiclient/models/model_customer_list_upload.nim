#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_error_detail
import model_record_counts
import model_user_list_operation_type
import model_workload_state

type CustomerListUpload* = object
  ## 
  adAccountId*: string ## Advertiser ID.
  creationTime*: int ## Customer List Upload creation_time. Epoch (seconds).
  customerListId*: string ## ID of the customer list associated with this upload.
  errorCounts*: Option[seq[ErrorDetail]] ## Error counts by error code
  id*: string ## Customer List Upload ID.
  operation*: UserListOperationType
  recordCounts*: Option[RecordCounts] ## Record processing counts
  state*: WorkloadState
  updatedTime*: int ## Customer List Upload updated_time. Epoch (seconds).


# Custom JSON deserialization for CustomerListUpload with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerListUpload]): CustomerListUpload =
  result = CustomerListUpload()
  if node.kind == JObject:
    if node.hasKey("ad_account_id"):
      result.adAccountId = to(node["ad_account_id"], string)
    if node.hasKey("creation_time"):
      result.creationTime = to(node["creation_time"], int)
    if node.hasKey("customer_list_id"):
      result.customerListId = to(node["customer_list_id"], string)
    if node.hasKey("error_counts") and node["error_counts"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["error_counts"]
      if arrayNode.kind == JArray:
        var arr: seq[ErrorDetail] = @[]
        for item in arrayNode.items:
          arr.add(to(item, ErrorDetail))
        result.errorCounts = some(arr)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], UserListOperationType)
    if node.hasKey("record_counts") and node["record_counts"].kind != JNull:
      result.recordCounts = some(to(node["record_counts"], typeof(result.recordCounts.get())))
    if node.hasKey("state"):
      result.state = to(node["state"], WorkloadState)
    if node.hasKey("updated_time"):
      result.updatedTime = to(node["updated_time"], int)

# Custom JSON serialization for CustomerListUpload with custom field names
proc `%`*(obj: CustomerListUpload): JsonNode =
  result = newJObject()
  result["ad_account_id"] = %obj.adAccountId
  result["creation_time"] = %obj.creationTime
  result["customer_list_id"] = %obj.customerListId
  if obj.errorCounts.isSome():
    result["error_counts"] = %obj.errorCounts.get()
  result["id"] = %obj.id
  result["operation"] = %obj.operation
  if obj.recordCounts.isSome():
    result["record_counts"] = %obj.recordCounts.get()
  result["state"] = %obj.state
  result["updated_time"] = %obj.updatedTime

