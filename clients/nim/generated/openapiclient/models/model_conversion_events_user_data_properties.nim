#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ConversionEventsUserDataProperties* = object
  ## 
  clickId*: Option[string] ## The unique identifier stored in _epik cookie on your domain or &epik= query parameter in the URL. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA.
  clientIpAddress*: Option[string] ## The user's IP address, which can be either in IPv4 or IPv6 format. Used for matching. We highly recommend this for all events. It may improve reporting performance such as ROAS/CPA.
  clientUserAgent*: Option[string] ## The user agent string of the user's web browser. We highly recommend this for all events. It may improve reporting performance such as ROAS/CPA.
  country*: Option[seq[string]] ## Sha256 hashes of two-character ISO-3166 country code indicating the user's country, in lowercase. The string should be in the UTF-8 format.
  ct*: Option[seq[string]] ## Sha256 hashes of user's city, in lowercase, and without spaces or punctuation. User residency city (mostly billing). The string should be in the UTF-8 format.
  db*: Option[seq[string]] ## Sha256 hashes of user's date of birthday, given as year, month, and day. The string should be in the UTF-8 format.
  em*: Option[seq[string]] ## Sha256 hashes of lowercase version of user's email addresses. Used for matching. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  externalId*: Option[seq[string]] ## Sha256 hashes of the unique id from the advertiser that identifies a user in their space, e.g. user id, loyalty id, etc. We highly recommend this on all events. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  fn*: Option[seq[string]] ## Sha256 hashes of user's first name, in lowercase. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  ge*: Option[seq[string]] ## Sha256 hashes of user's gender, in lowercase. Either \"f\" or \"m\" or \"n\" for non-binary gender. The string should be in the UTF-8 format.
  hashedMaids*: Option[seq[string]] ## Sha256 hashes of user's \"Google Advertising IDs\" (GAIDs) or \"Apple's Identifier for Advertisers\" (IDFAs). Used for matching. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  ln*: Option[seq[string]] ## Sha256 hashes of user's last name, in lowercase. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  partnerId*: Option[string] ## A unique identifier of visitors' information defined by third party partners. e.g RampID
  ph*: Option[seq[string]] ## Sha256 hashes of user's phone numbers, only digits with country code, area code, and number. Remove any symbols, letters, spaces and leading zeros. We highly recommend this on checkout events at least. It may improve reporting performance such as ROAS/CPA. The string should be in the UTF-8 format.
  st*: Option[seq[string]] ## Sha256 hashes of user's state, given as a two-letter code in lowercase. User residency state (mostly billing). The string should be in the UTF-8 format.
  zp*: Option[seq[string]] ## Sha256 hashes of user's zipcode, only digits. User residency zipcode (mostly billing). The string should be in the UTF-8 format.


# Custom JSON deserialization for ConversionEventsUserDataProperties with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionEventsUserDataProperties]): ConversionEventsUserDataProperties =
  result = ConversionEventsUserDataProperties()
  if node.kind == JObject:
    if node.hasKey("click_id") and node["click_id"].kind != JNull:
      result.clickId = some(to(node["click_id"], typeof(result.clickId.get())))
    if node.hasKey("client_ip_address") and node["client_ip_address"].kind != JNull:
      result.clientIpAddress = some(to(node["client_ip_address"], typeof(result.clientIpAddress.get())))
    if node.hasKey("client_user_agent") and node["client_user_agent"].kind != JNull:
      result.clientUserAgent = some(to(node["client_user_agent"], typeof(result.clientUserAgent.get())))
    if node.hasKey("country") and node["country"].kind != JNull:
      result.country = some(to(node["country"], typeof(result.country.get())))
    if node.hasKey("ct") and node["ct"].kind != JNull:
      result.ct = some(to(node["ct"], typeof(result.ct.get())))
    if node.hasKey("db") and node["db"].kind != JNull:
      result.db = some(to(node["db"], typeof(result.db.get())))
    if node.hasKey("em") and node["em"].kind != JNull:
      result.em = some(to(node["em"], typeof(result.em.get())))
    if node.hasKey("external_id") and node["external_id"].kind != JNull:
      result.externalId = some(to(node["external_id"], typeof(result.externalId.get())))
    if node.hasKey("fn") and node["fn"].kind != JNull:
      result.fn = some(to(node["fn"], typeof(result.fn.get())))
    if node.hasKey("ge") and node["ge"].kind != JNull:
      result.ge = some(to(node["ge"], typeof(result.ge.get())))
    if node.hasKey("hashed_maids") and node["hashed_maids"].kind != JNull:
      result.hashedMaids = some(to(node["hashed_maids"], typeof(result.hashedMaids.get())))
    if node.hasKey("ln") and node["ln"].kind != JNull:
      result.ln = some(to(node["ln"], typeof(result.ln.get())))
    if node.hasKey("partner_id") and node["partner_id"].kind != JNull:
      result.partnerId = some(to(node["partner_id"], typeof(result.partnerId.get())))
    if node.hasKey("ph") and node["ph"].kind != JNull:
      result.ph = some(to(node["ph"], typeof(result.ph.get())))
    if node.hasKey("st") and node["st"].kind != JNull:
      result.st = some(to(node["st"], typeof(result.st.get())))
    if node.hasKey("zp") and node["zp"].kind != JNull:
      result.zp = some(to(node["zp"], typeof(result.zp.get())))

# Custom JSON serialization for ConversionEventsUserDataProperties with custom field names
proc `%`*(obj: ConversionEventsUserDataProperties): JsonNode =
  result = newJObject()
  if obj.clickId.isSome():
    result["click_id"] = %obj.clickId.get()
  if obj.clientIpAddress.isSome():
    result["client_ip_address"] = %obj.clientIpAddress.get()
  if obj.clientUserAgent.isSome():
    result["client_user_agent"] = %obj.clientUserAgent.get()
  if obj.country.isSome():
    result["country"] = %obj.country.get()
  if obj.ct.isSome():
    result["ct"] = %obj.ct.get()
  if obj.db.isSome():
    result["db"] = %obj.db.get()
  if obj.em.isSome():
    result["em"] = %obj.em.get()
  if obj.externalId.isSome():
    result["external_id"] = %obj.externalId.get()
  if obj.fn.isSome():
    result["fn"] = %obj.fn.get()
  if obj.ge.isSome():
    result["ge"] = %obj.ge.get()
  if obj.hashedMaids.isSome():
    result["hashed_maids"] = %obj.hashedMaids.get()
  if obj.ln.isSome():
    result["ln"] = %obj.ln.get()
  if obj.partnerId.isSome():
    result["partner_id"] = %obj.partnerId.get()
  if obj.ph.isSome():
    result["ph"] = %obj.ph.get()
  if obj.st.isSome():
    result["st"] = %obj.st.get()
  if obj.zp.isSome():
    result["zp"] = %obj.zp.get()

