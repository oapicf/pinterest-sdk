#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_item_attributes
import model_pin

type CatalogType* {.pure.} = enum
  RETAIL

type ItemResponseKind* {.pure.} = enum
  RetailItem

type CatalogsRetailItemResponse* = object
  ## Object describing a retail item record
  attributes*: Option[ItemAttributes]
  catalogType*: CatalogType
  itemId*: Option[string] ## The catalog retail item id in the merchant namespace
  itemResponseKind*: ItemResponseKind ## Discriminator literal identifying this leaf inside an `ItemResponse` payload.
  pins*: Option[seq[Pin]] ## The pins mapped to the item

func `%`*(v: CatalogType): JsonNode =
  result = case v:
    of CatalogType.RETAIL: %"RETAIL"
func `$`*(v: CatalogType): string =
  result = case v:
    of CatalogType.RETAIL: $("RETAIL")

proc to*(node: JsonNode, T: typedesc[CatalogType]): CatalogType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CatalogType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("RETAIL"):
    return CatalogType.RETAIL
  else:
    raise newException(ValueError, "Invalid enum value for CatalogType: " & strVal)

func `%`*(v: ItemResponseKind): JsonNode =
  result = case v:
    of ItemResponseKind.RetailItem: %"retail_item"
func `$`*(v: ItemResponseKind): string =
  result = case v:
    of ItemResponseKind.RetailItem: $("retail_item")

proc to*(node: JsonNode, T: typedesc[ItemResponseKind]): ItemResponseKind =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ItemResponseKind, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("retail_item"):
    return ItemResponseKind.RetailItem
  else:
    raise newException(ValueError, "Invalid enum value for ItemResponseKind: " & strVal)


# Custom JSON deserialization for CatalogsRetailItemResponse with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsRetailItemResponse]): CatalogsRetailItemResponse =
  result = CatalogsRetailItemResponse()
  if node.kind == JObject:
    if node.hasKey("attributes") and node["attributes"].kind != JNull:
      result.attributes = some(to(node["attributes"], typeof(result.attributes.get())))
    if node.hasKey("catalog_type"):
      result.catalogType = to(node["catalog_type"], CatalogType)
    if node.hasKey("item_id") and node["item_id"].kind != JNull:
      result.itemId = some(to(node["item_id"], typeof(result.itemId.get())))
    if node.hasKey("item_response_kind"):
      result.itemResponseKind = to(node["item_response_kind"], ItemResponseKind)
    if node.hasKey("pins") and node["pins"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["pins"]
      if arrayNode.kind == JArray:
        var arr: seq[Pin] = @[]
        for item in arrayNode.items:
          arr.add(to(item, Pin))
        result.pins = some(arr)

# Custom JSON serialization for CatalogsRetailItemResponse with custom field names
proc `%`*(obj: CatalogsRetailItemResponse): JsonNode =
  result = newJObject()
  if obj.attributes.isSome():
    result["attributes"] = %obj.attributes.get()
  result["catalog_type"] = %obj.catalogType
  if obj.itemId.isSome():
    result["item_id"] = %obj.itemId.get()
  result["item_response_kind"] = %obj.itemResponseKind
  if obj.pins.isSome():
    result["pins"] = %obj.pins.get()

