#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ContentType* {.pure.} = enum
  ImageJpeg
  ImagePng

type PinMediaSourceImagesBase64ItemsInner* = object
  ## 
  title*: Option[string]
  description*: Option[string]
  link*: Option[string] ## Destination link for the image.
  contentType*: ContentType
  data*: string ## Image to upload as base64 string.

func `%`*(v: ContentType): JsonNode =
  result = case v:
    of ContentType.ImageJpeg: %"image/jpeg"
    of ContentType.ImagePng: %"image/png"
func `$`*(v: ContentType): string =
  result = case v:
    of ContentType.ImageJpeg: $("image/jpeg")
    of ContentType.ImagePng: $("image/png")

proc to*(node: JsonNode, T: typedesc[ContentType]): ContentType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ContentType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image/jpeg"):
    return ContentType.ImageJpeg
  of $("image/png"):
    return ContentType.ImagePng
  else:
    raise newException(ValueError, "Invalid enum value for ContentType: " & strVal)


# Custom JSON deserialization for PinMediaSourceImagesBase64ItemsInner with custom field names
proc to*(node: JsonNode, T: typedesc[PinMediaSourceImagesBase64ItemsInner]): PinMediaSourceImagesBase64ItemsInner =
  result = PinMediaSourceImagesBase64ItemsInner()
  if node.kind == JObject:
    if node.hasKey("title") and node["title"].kind != JNull:
      result.title = some(to(node["title"], typeof(result.title.get())))
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("link") and node["link"].kind != JNull:
      result.link = some(to(node["link"], typeof(result.link.get())))
    if node.hasKey("content_type"):
      result.contentType = to(node["content_type"], ContentType)
    if node.hasKey("data"):
      result.data = to(node["data"], string)

# Custom JSON serialization for PinMediaSourceImagesBase64ItemsInner with custom field names
proc `%`*(obj: PinMediaSourceImagesBase64ItemsInner): JsonNode =
  result = newJObject()
  if obj.title.isSome():
    result["title"] = %obj.title.get()
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.link.isSome():
    result["link"] = %obj.link.get()
  result["content_type"] = %obj.contentType
  result["data"] = %obj.data

