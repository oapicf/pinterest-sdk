#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ConversionEventAppInfo* = object
  ## Object containing information about the application where event occurred.
  appId*: Option[string] ## App ID in Google Play Store, AppStore or other stores.
  appName*: Option[string] ## Name of the app. Primarily used for Mobile Apps.
  appPackageName*: Option[string] ## App package name
  appStore*: Option[string] ## The name of the app distributor or store from which the app was installed. Some options: Samsung Galaxy Store, Google Play Store, Amazon Store, Apple App Store, F-Droid, Aptoide, Obtanium, Huawei AppGallery, Xiaomi Mi GetApps
  appVersion*: Option[string] ## App version. Primarily used for mobile apps
  installTime*: Option[int] ## App install time. Unix timestamp in seconds
  userAgent*: Option[string] ## User Agent request header. Primarily used for Web events
  windowHeight*: Option[int] ## Inner height of the window or viewport.
  windowWidth*: Option[int] ## Inner width of the window or viewport.


# Custom JSON deserialization for ConversionEventAppInfo with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionEventAppInfo]): ConversionEventAppInfo =
  result = ConversionEventAppInfo()
  if node.kind == JObject:
    if node.hasKey("app_id") and node["app_id"].kind != JNull:
      result.appId = some(to(node["app_id"], typeof(result.appId.get())))
    if node.hasKey("app_name") and node["app_name"].kind != JNull:
      result.appName = some(to(node["app_name"], typeof(result.appName.get())))
    if node.hasKey("app_package_name") and node["app_package_name"].kind != JNull:
      result.appPackageName = some(to(node["app_package_name"], typeof(result.appPackageName.get())))
    if node.hasKey("app_store") and node["app_store"].kind != JNull:
      result.appStore = some(to(node["app_store"], typeof(result.appStore.get())))
    if node.hasKey("app_version") and node["app_version"].kind != JNull:
      result.appVersion = some(to(node["app_version"], typeof(result.appVersion.get())))
    if node.hasKey("install_time") and node["install_time"].kind != JNull:
      result.installTime = some(to(node["install_time"], typeof(result.installTime.get())))
    if node.hasKey("user_agent") and node["user_agent"].kind != JNull:
      result.userAgent = some(to(node["user_agent"], typeof(result.userAgent.get())))
    if node.hasKey("window_height") and node["window_height"].kind != JNull:
      result.windowHeight = some(to(node["window_height"], typeof(result.windowHeight.get())))
    if node.hasKey("window_width") and node["window_width"].kind != JNull:
      result.windowWidth = some(to(node["window_width"], typeof(result.windowWidth.get())))

# Custom JSON serialization for ConversionEventAppInfo with custom field names
proc `%`*(obj: ConversionEventAppInfo): JsonNode =
  result = newJObject()
  if obj.appId.isSome():
    result["app_id"] = %obj.appId.get()
  if obj.appName.isSome():
    result["app_name"] = %obj.appName.get()
  if obj.appPackageName.isSome():
    result["app_package_name"] = %obj.appPackageName.get()
  if obj.appStore.isSome():
    result["app_store"] = %obj.appStore.get()
  if obj.appVersion.isSome():
    result["app_version"] = %obj.appVersion.get()
  if obj.installTime.isSome():
    result["install_time"] = %obj.installTime.get()
  if obj.userAgent.isSome():
    result["user_agent"] = %obj.userAgent.get()
  if obj.windowHeight.isSome():
    result["window_height"] = %obj.windowHeight.get()
  if obj.windowWidth.isSome():
    result["window_width"] = %obj.windowWidth.get()

