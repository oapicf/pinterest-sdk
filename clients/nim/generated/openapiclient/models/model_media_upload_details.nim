#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_media_upload_status
import model_media_upload_type

type MediaUploadDetails* = object
  ## Media upload details
  mediaId*: string
  mediaType*: MediaUploadType
  status*: MediaUploadStatus


# Custom JSON deserialization for MediaUploadDetails with custom field names
proc to*(node: JsonNode, T: typedesc[MediaUploadDetails]): MediaUploadDetails =
  result = MediaUploadDetails()
  if node.kind == JObject:
    if node.hasKey("media_id"):
      result.mediaId = to(node["media_id"], string)
    if node.hasKey("media_type"):
      result.mediaType = to(node["media_type"], MediaUploadType)
    if node.hasKey("status"):
      result.status = to(node["status"], MediaUploadStatus)

# Custom JSON serialization for MediaUploadDetails with custom field names
proc `%`*(obj: MediaUploadDetails): JsonNode =
  result = newJObject()
  result["media_id"] = %obj.mediaId
  result["media_type"] = %obj.mediaType
  result["status"] = %obj.status

