#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ProductCondition* {.pure.} = enum
  NEW
  USED
  REFURBISHED

func `%`*(v: ProductCondition): JsonNode =
  result = case v:
    of ProductCondition.NEW: %"NEW"
    of ProductCondition.USED: %"USED"
    of ProductCondition.REFURBISHED: %"REFURBISHED"

func `$`*(v: ProductCondition): string =
  result = case v:
    of ProductCondition.NEW: $("NEW")
    of ProductCondition.USED: $("USED")
    of ProductCondition.REFURBISHED: $("REFURBISHED")

proc to*(node: JsonNode, T: typedesc[ProductCondition]): ProductCondition =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ProductCondition, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("NEW"):
    return ProductCondition.NEW
  of $("USED"):
    return ProductCondition.USED
  of $("REFURBISHED"):
    return ProductCondition.REFURBISHED
  else:
    raise newException(ValueError, "Invalid enum value for ProductCondition: " & strVal)

