#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_asset_id_permissions
import model_total_count_by_entity_status

type BusinessMemberAssetsGetResponse* = object
  ## Paginated response for business member assets with total count metadata.
  bookmark*: Option[string]
  items*: seq[AssetIdPermissions]
  totalDataCount*: int ## Total number of assets matching the query
  totalDataCountByStatus*: Option[TotalCountByEntityStatus] ## Breakdown of asset counts by entity status (ad accounts only)


# Custom JSON deserialization for BusinessMemberAssetsGetResponse with custom field names
proc to*(node: JsonNode, T: typedesc[BusinessMemberAssetsGetResponse]): BusinessMemberAssetsGetResponse =
  result = BusinessMemberAssetsGetResponse()
  if node.kind == JObject:
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, AssetIdPermissions))
    if node.hasKey("total_data_count"):
      result.totalDataCount = to(node["total_data_count"], int)
    if node.hasKey("total_data_count_by_status") and node["total_data_count_by_status"].kind != JNull:
      result.totalDataCountByStatus = some(to(node["total_data_count_by_status"], typeof(result.totalDataCountByStatus.get())))

# Custom JSON serialization for BusinessMemberAssetsGetResponse with custom field names
proc `%`*(obj: BusinessMemberAssetsGetResponse): JsonNode =
  result = newJObject()
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()
  result["items"] = %obj.items
  result["total_data_count"] = %obj.totalDataCount
  if obj.totalDataCountByStatus.isSome():
    result["total_data_count_by_status"] = %obj.totalDataCountByStatus.get()

