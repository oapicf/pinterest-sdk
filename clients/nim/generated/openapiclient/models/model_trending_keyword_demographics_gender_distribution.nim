#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type GenderDistribution* {.pure.} = enum
  Male
  Female
  Unspecified

type TrendingKeywordDemographicsGenderDistribution* = object
  ## This represents a mapping from gender to distribution of search volume for a keyword. The sum of all values in this object should approximately be 1.
  genderDistribution*: Option[GenderDistribution]

func `%`*(v: GenderDistribution): JsonNode =
  result = case v:
    of GenderDistribution.Male: %"male"
    of GenderDistribution.Female: %"female"
    of GenderDistribution.Unspecified: %"unspecified"
func `$`*(v: GenderDistribution): string =
  result = case v:
    of GenderDistribution.Male: $("male")
    of GenderDistribution.Female: $("female")
    of GenderDistribution.Unspecified: $("unspecified")

proc to*(node: JsonNode, T: typedesc[GenderDistribution]): GenderDistribution =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum GenderDistribution, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("male"):
    return GenderDistribution.Male
  of $("female"):
    return GenderDistribution.Female
  of $("unspecified"):
    return GenderDistribution.Unspecified
  else:
    raise newException(ValueError, "Invalid enum value for GenderDistribution: " & strVal)


# Custom JSON deserialization for TrendingKeywordDemographicsGenderDistribution with custom field names
proc to*(node: JsonNode, T: typedesc[TrendingKeywordDemographicsGenderDistribution]): TrendingKeywordDemographicsGenderDistribution =
  result = TrendingKeywordDemographicsGenderDistribution()
  if node.kind == JObject:
    if node.hasKey("gender_distribution") and node["gender_distribution"].kind != JNull:
      result.genderDistribution = some(to(node["gender_distribution"], GenderDistribution))

# Custom JSON serialization for TrendingKeywordDemographicsGenderDistribution with custom field names
proc `%`*(obj: TrendingKeywordDemographicsGenderDistribution): JsonNode =
  result = newJObject()
  if obj.genderDistribution.isSome():
    result["gender_distribution"] = %obj.genderDistribution.get()

