#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BoardPrivacy* {.pure.} = enum
  PUBLIC
  PROTECTED
  SECRET

func `%`*(v: BoardPrivacy): JsonNode =
  result = case v:
    of BoardPrivacy.PUBLIC: %"PUBLIC"
    of BoardPrivacy.PROTECTED: %"PROTECTED"
    of BoardPrivacy.SECRET: %"SECRET"

func `$`*(v: BoardPrivacy): string =
  result = case v:
    of BoardPrivacy.PUBLIC: $("PUBLIC")
    of BoardPrivacy.PROTECTED: $("PROTECTED")
    of BoardPrivacy.SECRET: $("SECRET")

proc to*(node: JsonNode, T: typedesc[BoardPrivacy]): BoardPrivacy =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum BoardPrivacy, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("PUBLIC"):
    return BoardPrivacy.PUBLIC
  of $("PROTECTED"):
    return BoardPrivacy.PROTECTED
  of $("SECRET"):
    return BoardPrivacy.SECRET
  else:
    raise newException(ValueError, "Invalid enum value for BoardPrivacy: " & strVal)

