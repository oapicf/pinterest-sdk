#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_media_upload_status
import model_media_upload_type

type Media* = object
  ## 
  mediaId*: string ## Unique identifier for this media upload. Used to track status and for attaching during Pin creation.
  mediaType*: MediaUploadType
  status*: MediaUploadStatus


# Custom JSON deserialization for Media with custom field names
proc to*(node: JsonNode, T: typedesc[Media]): Media =
  result = Media()
  if node.kind == JObject:
    if node.hasKey("media_id"):
      result.mediaId = to(node["media_id"], string)
    if node.hasKey("media_type"):
      result.mediaType = to(node["media_type"], MediaUploadType)
    if node.hasKey("status"):
      result.status = to(node["status"], MediaUploadStatus)

# Custom JSON serialization for Media with custom field names
proc `%`*(obj: Media): JsonNode =
  result = newJObject()
  result["media_id"] = %obj.mediaId
  result["media_type"] = %obj.mediaType
  result["status"] = %obj.status

