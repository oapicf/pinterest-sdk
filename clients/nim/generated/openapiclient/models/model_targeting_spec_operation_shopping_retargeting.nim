#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_targeting_spec_shopping_retargeting

type Field* {.pure.} = enum
  SHOPPINGRETARGETING

type Operation* {.pure.} = enum
  SET

type TargetingSpecOperationShoppingRetargeting* = object
  ## 
  field*: Field
  operation*: Operation
  values*: seq[TargetingSpecShoppingRetargeting]

func `%`*(v: Field): JsonNode =
  result = case v:
    of Field.SHOPPINGRETARGETING: %"SHOPPING_RETARGETING"
func `$`*(v: Field): string =
  result = case v:
    of Field.SHOPPINGRETARGETING: $("SHOPPING_RETARGETING")

proc to*(node: JsonNode, T: typedesc[Field]): Field =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Field, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("SHOPPING_RETARGETING"):
    return Field.SHOPPINGRETARGETING
  else:
    raise newException(ValueError, "Invalid enum value for Field: " & strVal)

func `%`*(v: Operation): JsonNode =
  result = case v:
    of Operation.SET: %"SET"
func `$`*(v: Operation): string =
  result = case v:
    of Operation.SET: $("SET")

proc to*(node: JsonNode, T: typedesc[Operation]): Operation =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Operation, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("SET"):
    return Operation.SET
  else:
    raise newException(ValueError, "Invalid enum value for Operation: " & strVal)


# Custom JSON deserialization for TargetingSpecOperationShoppingRetargeting with custom field names
proc to*(node: JsonNode, T: typedesc[TargetingSpecOperationShoppingRetargeting]): TargetingSpecOperationShoppingRetargeting =
  result = TargetingSpecOperationShoppingRetargeting()
  if node.kind == JObject:
    if node.hasKey("field"):
      result.field = to(node["field"], Field)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], Operation)
    if node.hasKey("values"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["values"]
      if arrayNode.kind == JArray:
        result.values = @[]
        for item in arrayNode.items:
          result.values.add(to(item, TargetingSpecShoppingRetargeting))

# Custom JSON serialization for TargetingSpecOperationShoppingRetargeting with custom field names
proc `%`*(obj: TargetingSpecOperationShoppingRetargeting): JsonNode =
  result = newJObject()
  result["field"] = %obj.field
  result["operation"] = %obj.operation
  result["values"] = %obj.values

