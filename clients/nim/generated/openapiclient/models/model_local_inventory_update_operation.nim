#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_retail_local_inventory_item_attributes_optional

type Operation* {.pure.} = enum
  UPDATE

type LocalInventoryUpdateOperation* = object
  ## Update operation for local inventory item
  attributes*: RetailLocalInventoryItemAttributesOptional
  itemId*: string ## Catalog item id in the merchant namespace
  operation*: Operation
  storeCode*: string ## Store code for the local inventory item

func `%`*(v: Operation): JsonNode =
  result = case v:
    of Operation.UPDATE: %"UPDATE"
func `$`*(v: Operation): string =
  result = case v:
    of Operation.UPDATE: $("UPDATE")

proc to*(node: JsonNode, T: typedesc[Operation]): Operation =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Operation, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("UPDATE"):
    return Operation.UPDATE
  else:
    raise newException(ValueError, "Invalid enum value for Operation: " & strVal)


# Custom JSON deserialization for LocalInventoryUpdateOperation with custom field names
proc to*(node: JsonNode, T: typedesc[LocalInventoryUpdateOperation]): LocalInventoryUpdateOperation =
  result = LocalInventoryUpdateOperation()
  if node.kind == JObject:
    if node.hasKey("attributes"):
      result.attributes = to(node["attributes"], RetailLocalInventoryItemAttributesOptional)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], Operation)
    if node.hasKey("store_code"):
      result.storeCode = to(node["store_code"], string)

# Custom JSON serialization for LocalInventoryUpdateOperation with custom field names
proc `%`*(obj: LocalInventoryUpdateOperation): JsonNode =
  result = newJObject()
  result["attributes"] = %obj.attributes
  result["item_id"] = %obj.itemId
  result["operation"] = %obj.operation
  result["store_code"] = %obj.storeCode

