#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_asset_group_binding
import model_asset_group_update_error

type AssetGroupModification* = object
  ## 
  exceptions*: seq[AssetGroupUpdateError] ## A list of errors associated with the asset groups. Will be returned if there is an error.
  updatedAssetGroups*: seq[AssetGroupBinding] ## A list of successfully edited asset groups.


# Custom JSON deserialization for AssetGroupModification with custom field names
proc to*(node: JsonNode, T: typedesc[AssetGroupModification]): AssetGroupModification =
  result = AssetGroupModification()
  if node.kind == JObject:
    if node.hasKey("exceptions"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["exceptions"]
      if arrayNode.kind == JArray:
        result.exceptions = @[]
        for item in arrayNode.items:
          result.exceptions.add(to(item, AssetGroupUpdateError))
    if node.hasKey("updated_asset_groups"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["updated_asset_groups"]
      if arrayNode.kind == JArray:
        result.updatedAssetGroups = @[]
        for item in arrayNode.items:
          result.updatedAssetGroups.add(to(item, AssetGroupBinding))

# Custom JSON serialization for AssetGroupModification with custom field names
proc `%`*(obj: AssetGroupModification): JsonNode =
  result = newJObject()
  result["exceptions"] = %obj.exceptions
  result["updated_asset_groups"] = %obj.updatedAssetGroups

