#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_country
import model_image_base64

type BrandAccountsCreateRequest* = object
  ## 
  name*: string ## Brand Account name
  username*: string ## Brand Account username
  country*: Country
  about*: Option[string] ## Brand Account about information
  website*: Option[string] ## Brand Account website
  profileImage*: Option[Image_Base64]


# Custom JSON deserialization for BrandAccountsCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[BrandAccountsCreateRequest]): BrandAccountsCreateRequest =
  result = BrandAccountsCreateRequest()
  if node.kind == JObject:
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("username"):
      result.username = to(node["username"], string)
    if node.hasKey("country"):
      result.country = to(node["country"], Country)
    if node.hasKey("about") and node["about"].kind != JNull:
      result.about = some(to(node["about"], typeof(result.about.get())))
    if node.hasKey("website") and node["website"].kind != JNull:
      result.website = some(to(node["website"], typeof(result.website.get())))
    if node.hasKey("profile_image") and node["profile_image"].kind != JNull:
      result.profileImage = some(to(node["profile_image"], typeof(result.profileImage.get())))

# Custom JSON serialization for BrandAccountsCreateRequest with custom field names
proc `%`*(obj: BrandAccountsCreateRequest): JsonNode =
  result = newJObject()
  result["name"] = %obj.name
  result["username"] = %obj.username
  result["country"] = %obj.country
  if obj.about.isSome():
    result["about"] = %obj.about.get()
  if obj.website.isSome():
    result["website"] = %obj.website.get()
  if obj.profileImage.isSome():
    result["profile_image"] = %obj.profileImage.get()

