#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_product_group_promotion_response_item

type ProductGroupPromotions* = object
  ## Wrapper model for ProductGroupPromotion.
  items*: seq[ProductGroupPromotionResponseItem] ## Response-only: created or updated product group promotions, or exceptions.


# Custom JSON deserialization for ProductGroupPromotions with custom field names
proc to*(node: JsonNode, T: typedesc[ProductGroupPromotions]): ProductGroupPromotions =
  result = ProductGroupPromotions()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, ProductGroupPromotionResponseItem))

# Custom JSON serialization for ProductGroupPromotions with custom field names
proc `%`*(obj: ProductGroupPromotions): JsonNode =
  result = newJObject()
  result["items"] = %obj.items

