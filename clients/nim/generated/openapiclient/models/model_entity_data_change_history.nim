#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_change_history_data_type

type EntityDataChangeHistory* = object
  ## 
  changedFieldId*: Option[string] ## A string identifier representing the changed field on the entity
  changedFieldName*: Option[string] ## The human readable name of the changed field on the entity
  dataType*: Option[ChangeHistoryDataType] ## Specifies the type of the field's data values
  newDataValue*: Option[string] ## A string representation of the value of the changed field, after the change
  oldDataValue*: Option[string] ## A string representation of the value of the changed field, before the change


# Custom JSON deserialization for EntityDataChangeHistory with custom field names
proc to*(node: JsonNode, T: typedesc[EntityDataChangeHistory]): EntityDataChangeHistory =
  result = EntityDataChangeHistory()
  if node.kind == JObject:
    if node.hasKey("changed_field_id") and node["changed_field_id"].kind != JNull:
      result.changedFieldId = some(to(node["changed_field_id"], typeof(result.changedFieldId.get())))
    if node.hasKey("changed_field_name") and node["changed_field_name"].kind != JNull:
      result.changedFieldName = some(to(node["changed_field_name"], typeof(result.changedFieldName.get())))
    if node.hasKey("data_type") and node["data_type"].kind != JNull:
      result.dataType = some(to(node["data_type"], typeof(result.dataType.get())))
    if node.hasKey("new_data_value") and node["new_data_value"].kind != JNull:
      result.newDataValue = some(to(node["new_data_value"], typeof(result.newDataValue.get())))
    if node.hasKey("old_data_value") and node["old_data_value"].kind != JNull:
      result.oldDataValue = some(to(node["old_data_value"], typeof(result.oldDataValue.get())))

# Custom JSON serialization for EntityDataChangeHistory with custom field names
proc `%`*(obj: EntityDataChangeHistory): JsonNode =
  result = newJObject()
  if obj.changedFieldId.isSome():
    result["changed_field_id"] = %obj.changedFieldId.get()
  if obj.changedFieldName.isSome():
    result["changed_field_name"] = %obj.changedFieldName.get()
  if obj.dataType.isSome():
    result["data_type"] = %obj.dataType.get()
  if obj.newDataValue.isSome():
    result["new_data_value"] = %obj.newDataValue.get()
  if obj.oldDataValue.isSome():
    result["old_data_value"] = %obj.oldDataValue.get()

