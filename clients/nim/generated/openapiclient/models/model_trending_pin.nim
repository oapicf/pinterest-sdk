#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TrendingPin* = object
  ## Pin image data for trending topics
  color*: string ## Dominant color of the pin image in hex format
  height*: int ## Height of the pin image in pixels
  id*: string ## Unique identifier for the pin
  src*: string ## URL of the pin image
  verticalOffset*: Option[float64] ## The vertical offset of the pin image as a percentage from 0 to 100, where 0 is the top of the image and 100 is the bottom.
  width*: int ## Width of the pin image in pixels


# Custom JSON deserialization for TrendingPin with custom field names
proc to*(node: JsonNode, T: typedesc[TrendingPin]): TrendingPin =
  result = TrendingPin()
  if node.kind == JObject:
    if node.hasKey("color"):
      result.color = to(node["color"], string)
    if node.hasKey("height"):
      result.height = to(node["height"], int)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("src"):
      result.src = to(node["src"], string)
    if node.hasKey("vertical_offset") and node["vertical_offset"].kind != JNull:
      result.verticalOffset = some(to(node["vertical_offset"], typeof(result.verticalOffset.get())))
    if node.hasKey("width"):
      result.width = to(node["width"], int)

# Custom JSON serialization for TrendingPin with custom field names
proc `%`*(obj: TrendingPin): JsonNode =
  result = newJObject()
  result["color"] = %obj.color
  result["height"] = %obj.height
  result["id"] = %obj.id
  result["src"] = %obj.src
  if obj.verticalOffset.isSome():
    result["vertical_offset"] = %obj.verticalOffset.get()
  result["width"] = %obj.width

