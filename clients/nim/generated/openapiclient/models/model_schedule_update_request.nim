#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_schedule_action
import model_schedule_common_delta_value
import model_schedule_status
import model_schedule_type

type ScheduleUpdateRequest* = object
  ## 
  entityId*: Option[string]
  entityType*: Option[string] ## Entity type
  id*: string ## Schedule ID.
  deltaValue*: Option[ScheduleCommonDeltaValue]
  endTimestamp*: Option[int] ## Schedule end time. Unix timestamp in seconds.
  name*: Option[string]
  scheduleAction*: Option[ScheduleAction]
  scheduleStatus*: Option[ScheduleStatus]
  scheduleType*: Option[ScheduleType]
  startTimestamp*: Option[int] ## Schedule start time. Unix timestamp in seconds.


# Custom JSON deserialization for ScheduleUpdateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ScheduleUpdateRequest]): ScheduleUpdateRequest =
  result = ScheduleUpdateRequest()
  if node.kind == JObject:
    if node.hasKey("entity_id") and node["entity_id"].kind != JNull:
      result.entityId = some(to(node["entity_id"], typeof(result.entityId.get())))
    if node.hasKey("entity_type") and node["entity_type"].kind != JNull:
      result.entityType = some(to(node["entity_type"], typeof(result.entityType.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("delta_value") and node["delta_value"].kind != JNull:
      result.deltaValue = some(to(node["delta_value"], typeof(result.deltaValue.get())))
    if node.hasKey("end_timestamp") and node["end_timestamp"].kind != JNull:
      result.endTimestamp = some(to(node["end_timestamp"], typeof(result.endTimestamp.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("schedule_action") and node["schedule_action"].kind != JNull:
      result.scheduleAction = some(to(node["schedule_action"], typeof(result.scheduleAction.get())))
    if node.hasKey("schedule_status") and node["schedule_status"].kind != JNull:
      result.scheduleStatus = some(to(node["schedule_status"], typeof(result.scheduleStatus.get())))
    if node.hasKey("schedule_type") and node["schedule_type"].kind != JNull:
      result.scheduleType = some(to(node["schedule_type"], typeof(result.scheduleType.get())))
    if node.hasKey("start_timestamp") and node["start_timestamp"].kind != JNull:
      result.startTimestamp = some(to(node["start_timestamp"], typeof(result.startTimestamp.get())))

# Custom JSON serialization for ScheduleUpdateRequest with custom field names
proc `%`*(obj: ScheduleUpdateRequest): JsonNode =
  result = newJObject()
  if obj.entityId.isSome():
    result["entity_id"] = %obj.entityId.get()
  if obj.entityType.isSome():
    result["entity_type"] = %obj.entityType.get()
  result["id"] = %obj.id
  if obj.deltaValue.isSome():
    result["delta_value"] = %obj.deltaValue.get()
  if obj.endTimestamp.isSome():
    result["end_timestamp"] = %obj.endTimestamp.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.scheduleAction.isSome():
    result["schedule_action"] = %obj.scheduleAction.get()
  if obj.scheduleStatus.isSome():
    result["schedule_status"] = %obj.scheduleStatus.get()
  if obj.scheduleType.isSome():
    result["schedule_type"] = %obj.scheduleType.get()
  if obj.startTimestamp.isSome():
    result["start_timestamp"] = %obj.startTimestamp.get()

