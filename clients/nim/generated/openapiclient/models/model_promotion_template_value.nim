#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_currency

type PromotionTemplateValue* = object
  ## 
  amount*: Option[float] ## Numeric value.
  currencyCode*: Option[Currency]
  customText*: Option[string] ## Custom text.
  percent*: Option[float] ## Percent value.


# Custom JSON deserialization for PromotionTemplateValue with custom field names
proc to*(node: JsonNode, T: typedesc[PromotionTemplateValue]): PromotionTemplateValue =
  result = PromotionTemplateValue()
  if node.kind == JObject:
    if node.hasKey("amount") and node["amount"].kind != JNull:
      result.amount = some(to(node["amount"], typeof(result.amount.get())))
    if node.hasKey("currency_code") and node["currency_code"].kind != JNull:
      result.currencyCode = some(to(node["currency_code"], typeof(result.currencyCode.get())))
    if node.hasKey("custom_text") and node["custom_text"].kind != JNull:
      result.customText = some(to(node["custom_text"], typeof(result.customText.get())))
    if node.hasKey("percent") and node["percent"].kind != JNull:
      result.percent = some(to(node["percent"], typeof(result.percent.get())))

# Custom JSON serialization for PromotionTemplateValue with custom field names
proc `%`*(obj: PromotionTemplateValue): JsonNode =
  result = newJObject()
  if obj.amount.isSome():
    result["amount"] = %obj.amount.get()
  if obj.currencyCode.isSome():
    result["currency_code"] = %obj.currencyCode.get()
  if obj.customText.isSome():
    result["custom_text"] = %obj.customText.get()
  if obj.percent.isSome():
    result["percent"] = %obj.percent.get()

