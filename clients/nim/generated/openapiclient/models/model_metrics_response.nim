#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_metrics_response_data_items

type MetricsResponse* = object
  ## 
  data*: Option[seq[MetricsResponseDataItems]]


# Custom JSON deserialization for MetricsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[MetricsResponse]): MetricsResponse =
  result = MetricsResponse()
  if node.kind == JObject:
    if node.hasKey("data") and node["data"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["data"]
      if arrayNode.kind == JArray:
        var arr: seq[MetricsResponseDataItems] = @[]
        for item in arrayNode.items:
          arr.add(to(item, MetricsResponseDataItems))
        result.data = some(arr)

# Custom JSON serialization for MetricsResponse with custom field names
proc `%`*(obj: MetricsResponse): JsonNode =
  result = newJObject()
  if obj.data.isSome():
    result["data"] = %obj.data.get()

