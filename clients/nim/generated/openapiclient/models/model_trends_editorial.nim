#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_keyword_info

type TrendsEditorial* = object
  ## Information of the published editorial article.
  boardUrl*: string ## URL of the editorial board
  description*: string ## Description of the editorial article
  interests*: seq[string] ## List of interests related to the editorial article
  pinsUrl*: seq[string] ## URL of the pins related to the editorial article
  relatedKeywords*: seq[KeywordInfo] ## List of keywords related to the editorial article
  title*: string ## Title of the editorial article


# Custom JSON deserialization for TrendsEditorial with custom field names
proc to*(node: JsonNode, T: typedesc[TrendsEditorial]): TrendsEditorial =
  result = TrendsEditorial()
  if node.kind == JObject:
    if node.hasKey("board_url"):
      result.boardUrl = to(node["board_url"], string)
    if node.hasKey("description"):
      result.description = to(node["description"], string)
    if node.hasKey("interests"):
      result.interests = to(node["interests"], seq[string])
    if node.hasKey("pins_url"):
      result.pinsUrl = to(node["pins_url"], seq[string])
    if node.hasKey("related_keywords"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["related_keywords"]
      if arrayNode.kind == JArray:
        result.relatedKeywords = @[]
        for item in arrayNode.items:
          result.relatedKeywords.add(to(item, KeywordInfo))
    if node.hasKey("title"):
      result.title = to(node["title"], string)

# Custom JSON serialization for TrendsEditorial with custom field names
proc `%`*(obj: TrendsEditorial): JsonNode =
  result = newJObject()
  result["board_url"] = %obj.boardUrl
  result["description"] = %obj.description
  result["interests"] = %obj.interests
  result["pins_url"] = %obj.pinsUrl
  result["related_keywords"] = %obj.relatedKeywords
  result["title"] = %obj.title

