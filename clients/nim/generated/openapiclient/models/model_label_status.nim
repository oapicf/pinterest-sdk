#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LabelStatus* {.pure.} = enum
  ACTIVE
  ARCHIVED

func `%`*(v: LabelStatus): JsonNode =
  result = case v:
    of LabelStatus.ACTIVE: %"ACTIVE"
    of LabelStatus.ARCHIVED: %"ARCHIVED"

func `$`*(v: LabelStatus): string =
  result = case v:
    of LabelStatus.ACTIVE: $("ACTIVE")
    of LabelStatus.ARCHIVED: $("ARCHIVED")

proc to*(node: JsonNode, T: typedesc[LabelStatus]): LabelStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum LabelStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("ACTIVE"):
    return LabelStatus.ACTIVE
  of $("ARCHIVED"):
    return LabelStatus.ARCHIVED
  else:
    raise newException(ValueError, "Invalid enum value for LabelStatus: " & strVal)

