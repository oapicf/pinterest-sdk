#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_role

type BusinessToBusinessSharedAudience* = object
  ## 
  audienceId*: string ## Unique identifier of an audience
  permissions*: seq[Role] ## Permissions granted to the recipients.
  recipientBusinessIds*: seq[string] ## Business IDs to share with or revoke from (request) / that received the audience (response).


# Custom JSON deserialization for BusinessToBusinessSharedAudience with custom field names
proc to*(node: JsonNode, T: typedesc[BusinessToBusinessSharedAudience]): BusinessToBusinessSharedAudience =
  result = BusinessToBusinessSharedAudience()
  if node.kind == JObject:
    if node.hasKey("audience_id"):
      result.audienceId = to(node["audience_id"], string)
    if node.hasKey("permissions"):
      result.permissions = to(node["permissions"], seq[Role])
    if node.hasKey("recipient_business_ids"):
      result.recipientBusinessIds = to(node["recipient_business_ids"], seq[string])

# Custom JSON serialization for BusinessToBusinessSharedAudience with custom field names
proc `%`*(obj: BusinessToBusinessSharedAudience): JsonNode =
  result = newJObject()
  result["audience_id"] = %obj.audienceId
  result["permissions"] = %obj.permissions
  result["recipient_business_ids"] = %obj.recipientBusinessIds

