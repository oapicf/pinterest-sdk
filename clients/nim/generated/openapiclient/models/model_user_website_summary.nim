#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UserWebsiteSummary* = object
  ## 
  website*: Option[string] ## Website with path or domain only
  status*: Option[string] ## Status of the verification process
  verifiedAt*: Option[string] ## UTC timestamp when the verification happened - sometimes missing


# Custom JSON deserialization for UserWebsiteSummary with custom field names
proc to*(node: JsonNode, T: typedesc[UserWebsiteSummary]): UserWebsiteSummary =
  result = UserWebsiteSummary()
  if node.kind == JObject:
    if node.hasKey("website") and node["website"].kind != JNull:
      result.website = some(to(node["website"], typeof(result.website.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("verified_at") and node["verified_at"].kind != JNull:
      result.verifiedAt = some(to(node["verified_at"], typeof(result.verifiedAt.get())))

# Custom JSON serialization for UserWebsiteSummary with custom field names
proc `%`*(obj: UserWebsiteSummary): JsonNode =
  result = newJObject()
  if obj.website.isSome():
    result["website"] = %obj.website.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.verifiedAt.isSome():
    result["verified_at"] = %obj.verifiedAt.get()

