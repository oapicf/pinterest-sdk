#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_schedule_action
import model_schedule_common_delta_value
import model_schedule_status
import model_schedule_type

type ScheduleCreateRequest* = object
  ## 
  entityId*: string
  entityType*: string ## Entity type
  deltaValue*: ScheduleCommonDeltaValue
  endTimestamp*: int ## Schedule end time. Unix timestamp in seconds.
  name*: string
  scheduleAction*: ScheduleAction
  scheduleStatus*: ScheduleStatus
  scheduleType*: ScheduleType
  startTimestamp*: int ## Schedule start time. Unix timestamp in seconds.


# Custom JSON deserialization for ScheduleCreateRequest with custom field names
proc to*(node: JsonNode, T: typedesc[ScheduleCreateRequest]): ScheduleCreateRequest =
  result = ScheduleCreateRequest()
  if node.kind == JObject:
    if node.hasKey("entity_id"):
      result.entityId = to(node["entity_id"], string)
    if node.hasKey("entity_type"):
      result.entityType = to(node["entity_type"], string)
    if node.hasKey("delta_value"):
      result.deltaValue = to(node["delta_value"], ScheduleCommonDeltaValue)
    if node.hasKey("end_timestamp"):
      result.endTimestamp = to(node["end_timestamp"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("schedule_action"):
      result.scheduleAction = to(node["schedule_action"], ScheduleAction)
    if node.hasKey("schedule_status"):
      result.scheduleStatus = to(node["schedule_status"], ScheduleStatus)
    if node.hasKey("schedule_type"):
      result.scheduleType = to(node["schedule_type"], ScheduleType)
    if node.hasKey("start_timestamp"):
      result.startTimestamp = to(node["start_timestamp"], int)

# Custom JSON serialization for ScheduleCreateRequest with custom field names
proc `%`*(obj: ScheduleCreateRequest): JsonNode =
  result = newJObject()
  result["entity_id"] = %obj.entityId
  result["entity_type"] = %obj.entityType
  result["delta_value"] = %obj.deltaValue
  result["end_timestamp"] = %obj.endTimestamp
  result["name"] = %obj.name
  result["schedule_action"] = %obj.scheduleAction
  result["schedule_status"] = %obj.scheduleStatus
  result["schedule_type"] = %obj.scheduleType
  result["start_timestamp"] = %obj.startTimestamp

