#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_order_line
import model_order_line_mutation_error

type OrderLineMutationResult* = object
  ## 
  errors*: Option[seq[OrderLineMutationError]] ## Error list if update(s) fail.
  orderLine*: Option[seq[OrderLine]] ## Order Line object array.


# Custom JSON deserialization for OrderLineMutationResult with custom field names
proc to*(node: JsonNode, T: typedesc[OrderLineMutationResult]): OrderLineMutationResult =
  result = OrderLineMutationResult()
  if node.kind == JObject:
    if node.hasKey("errors") and node["errors"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["errors"]
      if arrayNode.kind == JArray:
        var arr: seq[OrderLineMutationError] = @[]
        for item in arrayNode.items:
          arr.add(to(item, OrderLineMutationError))
        result.errors = some(arr)
    if node.hasKey("order_line") and node["order_line"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["order_line"]
      if arrayNode.kind == JArray:
        var arr: seq[OrderLine] = @[]
        for item in arrayNode.items:
          arr.add(to(item, OrderLine))
        result.orderLine = some(arr)

# Custom JSON serialization for OrderLineMutationResult with custom field names
proc `%`*(obj: OrderLineMutationResult): JsonNode =
  result = newJObject()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  if obj.orderLine.isSome():
    result["order_line"] = %obj.orderLine.get()

