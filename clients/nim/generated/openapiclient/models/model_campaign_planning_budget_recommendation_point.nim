#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_planning_estimation_type
import model_campaign_planning_point_estimate

type CampaignPlanningBudgetRecommendationPoint* = object
  ## A point estimation containing the estimate data and estimation type for a budget recommendation.
  estimationType*: Option[CampaignPlanningEstimationType] ## Estimation type for this point.
  pointEstimate*: Option[CampaignPlanningPointEstimate] ## Point estimate data.


# Custom JSON deserialization for CampaignPlanningBudgetRecommendationPoint with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignPlanningBudgetRecommendationPoint]): CampaignPlanningBudgetRecommendationPoint =
  result = CampaignPlanningBudgetRecommendationPoint()
  if node.kind == JObject:
    if node.hasKey("estimation_type") and node["estimation_type"].kind != JNull:
      result.estimationType = some(to(node["estimation_type"], typeof(result.estimationType.get())))
    if node.hasKey("point_estimate") and node["point_estimate"].kind != JNull:
      result.pointEstimate = some(to(node["point_estimate"], typeof(result.pointEstimate.get())))

# Custom JSON serialization for CampaignPlanningBudgetRecommendationPoint with custom field names
proc `%`*(obj: CampaignPlanningBudgetRecommendationPoint): JsonNode =
  result = newJObject()
  if obj.estimationType.isSome():
    result["estimation_type"] = %obj.estimationType.get()
  if obj.pointEstimate.isSome():
    result["point_estimate"] = %obj.pointEstimate.get()

