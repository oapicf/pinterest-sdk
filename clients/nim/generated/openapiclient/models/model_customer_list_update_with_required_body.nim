#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_customer_list_record_row
import model_user_list_operation_type

type CustomerListUpdateWithRequiredBody* = object
  ## Resource create or update operation model with required body fields (no OptionalProperties).
  operationType*: UserListOperationType ## Customer list update operation type (add or remove). Only valid in update request body.
  records*: Option[string] ## Records list. Can be any combination of emails, MAIDs, or IDFAs. Emails must be lowercase and can be plain text or hashed using SHA1, SHA256, or MD5. MAIDs and IDFAs must be hashed with SHA1, SHA256, or MD5.
  recordsV2*: Option[seq[CustomerListRecordRow]] ## Multi-field record format. Array of objects with optional email, maid, ip_address, user_agent, external_id, hashed_pinner_id, hashed_phone_number, and liveramp_envelope per row. Provide exactly one of records or records_v2.


# Custom JSON deserialization for CustomerListUpdateWithRequiredBody with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerListUpdateWithRequiredBody]): CustomerListUpdateWithRequiredBody =
  result = CustomerListUpdateWithRequiredBody()
  if node.kind == JObject:
    if node.hasKey("operation_type"):
      result.operationType = to(node["operation_type"], UserListOperationType)
    if node.hasKey("records") and node["records"].kind != JNull:
      result.records = some(to(node["records"], typeof(result.records.get())))
    if node.hasKey("records_v2") and node["records_v2"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["records_v2"]
      if arrayNode.kind == JArray:
        var arr: seq[CustomerListRecordRow] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CustomerListRecordRow))
        result.recordsV2 = some(arr)

# Custom JSON serialization for CustomerListUpdateWithRequiredBody with custom field names
proc `%`*(obj: CustomerListUpdateWithRequiredBody): JsonNode =
  result = newJObject()
  result["operation_type"] = %obj.operationType
  if obj.records.isSome():
    result["records"] = %obj.records.get()
  if obj.recordsV2.isSome():
    result["records_v2"] = %obj.recordsV2.get()

