#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_supplemental_item_processing_status
import model_supplemental_item_validation_event

type SupplementalType* {.pure.} = enum
  LOCALINVENTORY

type LocalInventoryOperationResult* = object
  ## Result model for local inventory operation
  errors*: Option[seq[SupplementalItemValidationEvent]] ## Array with the validation errors for the item processing record. A non empty errors list causes the item processing to fail.
  itemId*: string ## Catalog item id in the merchant namespace
  status*: SupplementalItemProcessingStatus ## Status of the item processing record
  storeCode*: string ## Store code for the local inventory item
  supplementalType*: SupplementalType
  warnings*: Option[seq[SupplementalItemValidationEvent]] ## Array with the validation warnings for the item processing record

func `%`*(v: SupplementalType): JsonNode =
  result = case v:
    of SupplementalType.LOCALINVENTORY: %"LOCAL_INVENTORY"
func `$`*(v: SupplementalType): string =
  result = case v:
    of SupplementalType.LOCALINVENTORY: $("LOCAL_INVENTORY")

proc to*(node: JsonNode, T: typedesc[SupplementalType]): SupplementalType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SupplementalType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("LOCAL_INVENTORY"):
    return SupplementalType.LOCALINVENTORY
  else:
    raise newException(ValueError, "Invalid enum value for SupplementalType: " & strVal)


# Custom JSON deserialization for LocalInventoryOperationResult with custom field names
proc to*(node: JsonNode, T: typedesc[LocalInventoryOperationResult]): LocalInventoryOperationResult =
  result = LocalInventoryOperationResult()
  if node.kind == JObject:
    if node.hasKey("errors") and node["errors"].kind != JNull:
      result.errors = some(to(node["errors"], typeof(result.errors.get())))
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], SupplementalItemProcessingStatus)
    if node.hasKey("store_code"):
      result.storeCode = to(node["store_code"], string)
    if node.hasKey("supplemental_type"):
      result.supplementalType = to(node["supplemental_type"], SupplementalType)
    if node.hasKey("warnings") and node["warnings"].kind != JNull:
      result.warnings = some(to(node["warnings"], typeof(result.warnings.get())))

# Custom JSON serialization for LocalInventoryOperationResult with custom field names
proc `%`*(obj: LocalInventoryOperationResult): JsonNode =
  result = newJObject()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  result["item_id"] = %obj.itemId
  result["status"] = %obj.status
  result["store_code"] = %obj.storeCode
  result["supplemental_type"] = %obj.supplementalType
  if obj.warnings.isSome():
    result["warnings"] = %obj.warnings.get()

