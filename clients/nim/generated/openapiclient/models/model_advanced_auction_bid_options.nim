#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_app_type_multipliers
import model_placement_multipliers

type AdvancedAuctionBidOptions* = object
  ## Object describing a retail catalog item's bid options (bid price and bid multipliers).
  bidInMicroCurrency*: Option[int64] ## Bid price in micro currency. A value of 0 will stop distribution for this item in `MAX_BID` ad groups in `CATALOG_SALES` campaigns. A value of `null` will fallback to the ad group's `bid_in_micro_currency`.
  appTypeMultipliers*: Option[AppTypeMultipliers]
  placementMultipliers*: Option[PlacementMultipliers]


# Custom JSON deserialization for AdvancedAuctionBidOptions with custom field names
proc to*(node: JsonNode, T: typedesc[AdvancedAuctionBidOptions]): AdvancedAuctionBidOptions =
  result = AdvancedAuctionBidOptions()
  if node.kind == JObject:
    if node.hasKey("bid_in_micro_currency") and node["bid_in_micro_currency"].kind != JNull:
      result.bidInMicroCurrency = some(to(node["bid_in_micro_currency"], typeof(result.bidInMicroCurrency.get())))
    if node.hasKey("app_type_multipliers") and node["app_type_multipliers"].kind != JNull:
      result.appTypeMultipliers = some(to(node["app_type_multipliers"], typeof(result.appTypeMultipliers.get())))
    if node.hasKey("placement_multipliers") and node["placement_multipliers"].kind != JNull:
      result.placementMultipliers = some(to(node["placement_multipliers"], typeof(result.placementMultipliers.get())))

# Custom JSON serialization for AdvancedAuctionBidOptions with custom field names
proc `%`*(obj: AdvancedAuctionBidOptions): JsonNode =
  result = newJObject()
  if obj.bidInMicroCurrency.isSome():
    result["bid_in_micro_currency"] = %obj.bidInMicroCurrency.get()
  if obj.appTypeMultipliers.isSome():
    result["app_type_multipliers"] = %obj.appTypeMultipliers.get()
  if obj.placementMultipliers.isSome():
    result["placement_multipliers"] = %obj.placementMultipliers.get()

