#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_targeting_spec_age_bucket
import model_targeting_spec_app_type
import model_targeting_spec_gender
import model_targeting_spec_shopping_retargeting

type TARGETINGSTRATEGY* {.pure.} = enum
  CHOOSEYOUROWN
  FINDNEWCUSTOMERS
  RECONNECTWITHUSERS

type TargetingSpec* = object
  ## Ad group targeting specification defining the ad group target audience. For example, `{\"APPTYPE\":[\"iphone\"], \"GENDER\":[\"male\"], \"LOCALE\":[\"en-US\"], \"LOCATION\":[\"501\"], \"MINIMUM_AGE\":\"18\", \"MAXIMUM_AGE\":\"65+\"}`
  AGE_BUCKET*: Option[seq[TargetingSpecAgeBucket]] ## **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted.
  APPTYPE*: Option[seq[TargetingSpecAppType]] ## Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted.
  AUDIENCE_EXCLUDE*: Option[seq[string]] ## Excluded customer list IDs. Used to drive new customer acquisition goals. For example: [\"2542620905475\"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**.
  AUDIENCE_INCLUDE*: Option[seq[string]] ## Targeted customer list IDs. For example: [\"2542620905473\"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**.
  GENDER*: Option[seq[TargetingSpecGender]] ## Targeted genders. Values: [\"unknown\",\"male\",\"female\"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**.
  GEO*: Option[seq[string]] ## Location region codes, e.g., \"BE-VOV\" (East Flanders, Belgium) For complete list, <a href=\"https://help.pinterest.com/sub/helpcenter/partner/pinterest_location_targeting_codes.xlsx\" target=\"_blank\">click here</a> or postal codes, e.g., \"US-94107\". Use either region codes or postal codes but not both. At least one of LOCATION or GEO must be specified. If the GEO field is missing, then only LOCATION values will be targeted (see LOCATION field below).
  INTEREST*: Option[seq[string]] ## Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**.
  LOCALE*: Option[seq[string]] ## 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted.
  LOCATION*: Option[seq[string]] ## 22 ISO Alpha 2 two letter country codes or US Nielsen DMA (Designated Market Area) codes (location region codes) (e.g., [\"US\", \"807\"]). For complete list, <a href=\"https://help.pinterest.com/sub/helpcenter/partner/pinterest_location_targeting_codes.xlsx\" target=\"_blank\">click here</a>. Location-Country and Location-Metro codes apply. At least one of LOCATION or GEO must be specified. If the LOCATION field is missing, then only GEO values will be targeted (see GEO field above).
  MAXIMUM_AGE*: Option[string] ## Maximum age to target (inclusive). Values: \"18\", \"19\", ..., \"65\", \"65+\". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted.
  MINIMUM_AGE*: Option[string] ## Minimum age to target (inclusive). Values: \"18\", \"19\", ..., \"65\". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted.
  SHOPPING_RETARGETING*: Option[seq[TargetingSpecShoppingRetargeting]] ## Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting
  TARGETING_STRATEGY*: Option[TARGETINGSTRATEGY] ## 

func `%`*(v: TARGETINGSTRATEGY): JsonNode =
  result = case v:
    of TARGETINGSTRATEGY.CHOOSEYOUROWN: %"CHOOSE_YOUR_OWN"
    of TARGETINGSTRATEGY.FINDNEWCUSTOMERS: %"FIND_NEW_CUSTOMERS"
    of TARGETINGSTRATEGY.RECONNECTWITHUSERS: %"RECONNECT_WITH_USERS"
func `$`*(v: TARGETINGSTRATEGY): string =
  result = case v:
    of TARGETINGSTRATEGY.CHOOSEYOUROWN: $("CHOOSE_YOUR_OWN")
    of TARGETINGSTRATEGY.FINDNEWCUSTOMERS: $("FIND_NEW_CUSTOMERS")
    of TARGETINGSTRATEGY.RECONNECTWITHUSERS: $("RECONNECT_WITH_USERS")

proc to*(node: JsonNode, T: typedesc[TARGETINGSTRATEGY]): TARGETINGSTRATEGY =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum TARGETINGSTRATEGY, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("CHOOSE_YOUR_OWN"):
    return TARGETINGSTRATEGY.CHOOSEYOUROWN
  of $("FIND_NEW_CUSTOMERS"):
    return TARGETINGSTRATEGY.FINDNEWCUSTOMERS
  of $("RECONNECT_WITH_USERS"):
    return TARGETINGSTRATEGY.RECONNECTWITHUSERS
  else:
    raise newException(ValueError, "Invalid enum value for TARGETINGSTRATEGY: " & strVal)


# Custom JSON deserialization for TargetingSpec with custom field names
proc to*(node: JsonNode, T: typedesc[TargetingSpec]): TargetingSpec =
  result = TargetingSpec()
  if node.kind == JObject:
    if node.hasKey("AGE_BUCKET") and node["AGE_BUCKET"].kind != JNull:
      result.AGE_BUCKET = some(to(node["AGE_BUCKET"], typeof(result.AGE_BUCKET.get())))
    if node.hasKey("APPTYPE") and node["APPTYPE"].kind != JNull:
      result.APPTYPE = some(to(node["APPTYPE"], typeof(result.APPTYPE.get())))
    if node.hasKey("AUDIENCE_EXCLUDE") and node["AUDIENCE_EXCLUDE"].kind != JNull:
      result.AUDIENCE_EXCLUDE = some(to(node["AUDIENCE_EXCLUDE"], typeof(result.AUDIENCE_EXCLUDE.get())))
    if node.hasKey("AUDIENCE_INCLUDE") and node["AUDIENCE_INCLUDE"].kind != JNull:
      result.AUDIENCE_INCLUDE = some(to(node["AUDIENCE_INCLUDE"], typeof(result.AUDIENCE_INCLUDE.get())))
    if node.hasKey("GENDER") and node["GENDER"].kind != JNull:
      result.GENDER = some(to(node["GENDER"], typeof(result.GENDER.get())))
    if node.hasKey("GEO") and node["GEO"].kind != JNull:
      result.GEO = some(to(node["GEO"], typeof(result.GEO.get())))
    if node.hasKey("INTEREST") and node["INTEREST"].kind != JNull:
      result.INTEREST = some(to(node["INTEREST"], typeof(result.INTEREST.get())))
    if node.hasKey("LOCALE") and node["LOCALE"].kind != JNull:
      result.LOCALE = some(to(node["LOCALE"], typeof(result.LOCALE.get())))
    if node.hasKey("LOCATION") and node["LOCATION"].kind != JNull:
      result.LOCATION = some(to(node["LOCATION"], typeof(result.LOCATION.get())))
    if node.hasKey("MAXIMUM_AGE") and node["MAXIMUM_AGE"].kind != JNull:
      result.MAXIMUM_AGE = some(to(node["MAXIMUM_AGE"], typeof(result.MAXIMUM_AGE.get())))
    if node.hasKey("MINIMUM_AGE") and node["MINIMUM_AGE"].kind != JNull:
      result.MINIMUM_AGE = some(to(node["MINIMUM_AGE"], typeof(result.MINIMUM_AGE.get())))
    if node.hasKey("SHOPPING_RETARGETING") and node["SHOPPING_RETARGETING"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["SHOPPING_RETARGETING"]
      if arrayNode.kind == JArray:
        var arr: seq[TargetingSpecShoppingRetargeting] = @[]
        for item in arrayNode.items:
          arr.add(to(item, TargetingSpecShoppingRetargeting))
        result.SHOPPING_RETARGETING = some(arr)
    if node.hasKey("TARGETING_STRATEGY") and node["TARGETING_STRATEGY"].kind != JNull:
      result.TARGETING_STRATEGY = some(to(node["TARGETING_STRATEGY"], TARGETINGSTRATEGY))

# Custom JSON serialization for TargetingSpec with custom field names
proc `%`*(obj: TargetingSpec): JsonNode =
  result = newJObject()
  if obj.AGE_BUCKET.isSome():
    result["AGE_BUCKET"] = %obj.AGE_BUCKET.get()
  if obj.APPTYPE.isSome():
    result["APPTYPE"] = %obj.APPTYPE.get()
  if obj.AUDIENCE_EXCLUDE.isSome():
    result["AUDIENCE_EXCLUDE"] = %obj.AUDIENCE_EXCLUDE.get()
  if obj.AUDIENCE_INCLUDE.isSome():
    result["AUDIENCE_INCLUDE"] = %obj.AUDIENCE_INCLUDE.get()
  if obj.GENDER.isSome():
    result["GENDER"] = %obj.GENDER.get()
  if obj.GEO.isSome():
    result["GEO"] = %obj.GEO.get()
  if obj.INTEREST.isSome():
    result["INTEREST"] = %obj.INTEREST.get()
  if obj.LOCALE.isSome():
    result["LOCALE"] = %obj.LOCALE.get()
  if obj.LOCATION.isSome():
    result["LOCATION"] = %obj.LOCATION.get()
  if obj.MAXIMUM_AGE.isSome():
    result["MAXIMUM_AGE"] = %obj.MAXIMUM_AGE.get()
  if obj.MINIMUM_AGE.isSome():
    result["MINIMUM_AGE"] = %obj.MINIMUM_AGE.get()
  if obj.SHOPPING_RETARGETING.isSome():
    result["SHOPPING_RETARGETING"] = %obj.SHOPPING_RETARGETING.get()
  if obj.TARGETING_STRATEGY.isSome():
    result["TARGETING_STRATEGY"] = %obj.TARGETING_STRATEGY.get()

