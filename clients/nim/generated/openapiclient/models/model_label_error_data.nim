#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_nullable_label_status
import model_nullable_label_type

type LabelErrorData* = object
  ## 
  id*: Option[string] ## Label ID.
  labelType*: Option[NullableLabelType]
  status*: Option[NullableLabelStatus]
  value*: Option[string] ## Label name. 100-character limit.


# Custom JSON deserialization for LabelErrorData with custom field names
proc to*(node: JsonNode, T: typedesc[LabelErrorData]): LabelErrorData =
  result = LabelErrorData()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("label_type") and node["label_type"].kind != JNull:
      result.labelType = some(to(node["label_type"], typeof(result.labelType.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("value") and node["value"].kind != JNull:
      result.value = some(to(node["value"], typeof(result.value.get())))

# Custom JSON serialization for LabelErrorData with custom field names
proc `%`*(obj: LabelErrorData): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.labelType.isSome():
    result["label_type"] = %obj.labelType.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.value.isSome():
    result["value"] = %obj.value.get()

