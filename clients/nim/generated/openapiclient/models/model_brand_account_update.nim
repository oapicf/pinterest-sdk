#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_brand_account_profile_image_update
import model_country

type BrandAccountUpdate* = object
  ## Resource create or update operation model.
  about*: Option[string] ## Brand Account about information
  country*: Option[Country]
  name*: Option[string] ## Brand Account name
  profileImage*: Option[BrandAccountProfileImageUpdate]
  username*: Option[string] ## Brand Account username
  website*: Option[string] ## Brand Account website


# Custom JSON deserialization for BrandAccountUpdate with custom field names
proc to*(node: JsonNode, T: typedesc[BrandAccountUpdate]): BrandAccountUpdate =
  result = BrandAccountUpdate()
  if node.kind == JObject:
    if node.hasKey("about") and node["about"].kind != JNull:
      result.about = some(to(node["about"], typeof(result.about.get())))
    if node.hasKey("country") and node["country"].kind != JNull:
      result.country = some(to(node["country"], typeof(result.country.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("profile_image") and node["profile_image"].kind != JNull:
      result.profileImage = some(to(node["profile_image"], typeof(result.profileImage.get())))
    if node.hasKey("username") and node["username"].kind != JNull:
      result.username = some(to(node["username"], typeof(result.username.get())))
    if node.hasKey("website") and node["website"].kind != JNull:
      result.website = some(to(node["website"], typeof(result.website.get())))

# Custom JSON serialization for BrandAccountUpdate with custom field names
proc `%`*(obj: BrandAccountUpdate): JsonNode =
  result = newJObject()
  if obj.about.isSome():
    result["about"] = %obj.about.get()
  if obj.country.isSome():
    result["country"] = %obj.country.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.profileImage.isSome():
    result["profile_image"] = %obj.profileImage.get()
  if obj.username.isSome():
    result["username"] = %obj.username.get()
  if obj.website.isSome():
    result["website"] = %obj.website.get()

