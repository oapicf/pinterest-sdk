#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TrendsGender* {.pure.} = enum
  Male
  Female
  Unspecified

func `%`*(v: TrendsGender): JsonNode =
  result = case v:
    of TrendsGender.Male: %"male"
    of TrendsGender.Female: %"female"
    of TrendsGender.Unspecified: %"unspecified"

func `$`*(v: TrendsGender): string =
  result = case v:
    of TrendsGender.Male: $("male")
    of TrendsGender.Female: $("female")
    of TrendsGender.Unspecified: $("unspecified")

proc to*(node: JsonNode, T: typedesc[TrendsGender]): TrendsGender =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum TrendsGender, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("male"):
    return TrendsGender.Male
  of $("female"):
    return TrendsGender.Female
  of $("unspecified"):
    return TrendsGender.Unspecified
  else:
    raise newException(ValueError, "Invalid enum value for TrendsGender: " & strVal)

