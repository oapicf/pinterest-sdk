#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_hotel_product_group_filters

type CatalogType* {.pure.} = enum
  HOTEL

type CatalogsHotelListProductsByCatalogBasedFilterRequest* = object
  ## Request object to list products for a given hotel catalog_id and product group filter.
  catalogId*: string ## Catalog ID pertaining to the product group.
  catalogType*: CatalogType
  filters*: CatalogsHotelProductGroupFilters

func `%`*(v: CatalogType): JsonNode =
  result = case v:
    of CatalogType.HOTEL: %"HOTEL"
func `$`*(v: CatalogType): string =
  result = case v:
    of CatalogType.HOTEL: $("HOTEL")

proc to*(node: JsonNode, T: typedesc[CatalogType]): CatalogType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CatalogType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("HOTEL"):
    return CatalogType.HOTEL
  else:
    raise newException(ValueError, "Invalid enum value for CatalogType: " & strVal)


# Custom JSON deserialization for CatalogsHotelListProductsByCatalogBasedFilterRequest with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsHotelListProductsByCatalogBasedFilterRequest]): CatalogsHotelListProductsByCatalogBasedFilterRequest =
  result = CatalogsHotelListProductsByCatalogBasedFilterRequest()
  if node.kind == JObject:
    if node.hasKey("catalog_id"):
      result.catalogId = to(node["catalog_id"], string)
    if node.hasKey("catalog_type"):
      result.catalogType = to(node["catalog_type"], CatalogType)
    if node.hasKey("filters"):
      result.filters = to(node["filters"], CatalogsHotelProductGroupFilters)

# Custom JSON serialization for CatalogsHotelListProductsByCatalogBasedFilterRequest with custom field names
proc `%`*(obj: CatalogsHotelListProductsByCatalogBasedFilterRequest): JsonNode =
  result = newJObject()
  result["catalog_id"] = %obj.catalogId
  result["catalog_type"] = %obj.catalogType
  result["filters"] = %obj.filters

