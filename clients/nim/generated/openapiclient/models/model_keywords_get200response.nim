#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_keyword

type KeywordsGet200response* = object
  ## 
  items*: seq[Keyword]
  bookmark*: Option[string]


# Custom JSON deserialization for KeywordsGet200response with custom field names
proc to*(node: JsonNode, T: typedesc[KeywordsGet200response]): KeywordsGet200response =
  result = KeywordsGet200response()
  if node.kind == JObject:
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, Keyword))
    if node.hasKey("bookmark") and node["bookmark"].kind != JNull:
      result.bookmark = some(to(node["bookmark"], typeof(result.bookmark.get())))

# Custom JSON serialization for KeywordsGet200response with custom field names
proc `%`*(obj: KeywordsGet200response): JsonNode =
  result = newJObject()
  result["items"] = %obj.items
  if obj.bookmark.isSome():
    result["bookmark"] = %obj.bookmark.get()

