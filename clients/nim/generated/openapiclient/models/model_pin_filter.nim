#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PinFilter* {.pure.} = enum
  ExcludeNative
  ExcludeRepins
  HasBeenPromoted

func `%`*(v: PinFilter): JsonNode =
  result = case v:
    of PinFilter.ExcludeNative: %"exclude_native"
    of PinFilter.ExcludeRepins: %"exclude_repins"
    of PinFilter.HasBeenPromoted: %"has_been_promoted"

func `$`*(v: PinFilter): string =
  result = case v:
    of PinFilter.ExcludeNative: $("exclude_native")
    of PinFilter.ExcludeRepins: $("exclude_repins")
    of PinFilter.HasBeenPromoted: $("has_been_promoted")

proc to*(node: JsonNode, T: typedesc[PinFilter]): PinFilter =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum PinFilter, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("exclude_native"):
    return PinFilter.ExcludeNative
  of $("exclude_repins"):
    return PinFilter.ExcludeRepins
  of $("has_been_promoted"):
    return PinFilter.HasBeenPromoted
  else:
    raise newException(ValueError, "Invalid enum value for PinFilter: " & strVal)

