#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_item_availability
import model_store_metadata

type LocalInventoryItemResponse* = object
  ## Local inventory item response
  adLink*: Option[string] ## Ad link for the item
  availability*: Option[ItemAvailability] ## Availability status of the item
  createdAt*: int64 ## The millisecond timestamp when the local inventory item was created
  itemId*: string ## The catalog item id in the merchant namespace
  lastUpdatedTime*: int64 ## The millisecond timestamp when the local inventory item was lastly modified by the merchant.
  price*: Option[string] ## The price of the product. It supports the following formats, \"49.99 USD\", \"49.99USD\" and \"49.99\". If the currency is not included, we default to US dollars.
  salePrice*: Option[string] ## The sale price of the product. It supports the following formats, \"24.99 USD\", \"24.99USD\" and \"24.99\". If the currency is not included, we default to US dollars.
  storeMetadata*: StoreMetadata ## Store metadata for this local inventory item


# Custom JSON deserialization for LocalInventoryItemResponse with custom field names
proc to*(node: JsonNode, T: typedesc[LocalInventoryItemResponse]): LocalInventoryItemResponse =
  result = LocalInventoryItemResponse()
  if node.kind == JObject:
    if node.hasKey("ad_link") and node["ad_link"].kind != JNull:
      result.adLink = some(to(node["ad_link"], typeof(result.adLink.get())))
    if node.hasKey("availability") and node["availability"].kind != JNull:
      result.availability = some(to(node["availability"], typeof(result.availability.get())))
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], int64)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("last_updated_time"):
      result.lastUpdatedTime = to(node["last_updated_time"], int64)
    if node.hasKey("price") and node["price"].kind != JNull:
      result.price = some(to(node["price"], typeof(result.price.get())))
    if node.hasKey("sale_price") and node["sale_price"].kind != JNull:
      result.salePrice = some(to(node["sale_price"], typeof(result.salePrice.get())))
    if node.hasKey("store_metadata"):
      result.storeMetadata = to(node["store_metadata"], StoreMetadata)

# Custom JSON serialization for LocalInventoryItemResponse with custom field names
proc `%`*(obj: LocalInventoryItemResponse): JsonNode =
  result = newJObject()
  if obj.adLink.isSome():
    result["ad_link"] = %obj.adLink.get()
  if obj.availability.isSome():
    result["availability"] = %obj.availability.get()
  result["created_at"] = %obj.createdAt
  result["item_id"] = %obj.itemId
  result["last_updated_time"] = %obj.lastUpdatedTime
  if obj.price.isSome():
    result["price"] = %obj.price.get()
  if obj.salePrice.isSome():
    result["sale_price"] = %obj.salePrice.get()
  result["store_metadata"] = %obj.storeMetadata

