#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ConversionEventsDataItemsCustomDataContentsItems* = object
  ## 
  id*: Option[string] ## The id of a product. We recommend using this if you are a merchant for AddToCart and Checkouts. For detail, please check [here](https://help.pinterest.com/en/business/article/before-you-get-started-with-catalogs) (Install the Pinterest tag section).
  itemBrand*: Option[string] ## The brand of a product.
  itemBrandId*: Option[string] ## The brand ID of a product.
  itemCategory*: Option[string] ## The category of a product.
  itemName*: Option[string] ## The name of a product.
  itemPrice*: Option[string] ## The price of a product. Accepted as a string in the request; it will be parsed into a double. This is the original item value before any discount. We recommend using this if you are a merchant for PageVisit, AddToCart and Checkouts. For detail, please check [here](https://help.pinterest.com/en/business/article/before-you-get-started-with-catalogs) (Install the Pinterest tag section).
  quantity*: Option[int64] ## The amount of a product. We recommend using this if you are a merchant for AddToCart and Checkouts. For detail, please check [here](https://help.pinterest.com/en/business/article/before-you-get-started-with-catalogs) (Install the Pinterest tag section).


# Custom JSON deserialization for ConversionEventsDataItemsCustomDataContentsItems with custom field names
proc to*(node: JsonNode, T: typedesc[ConversionEventsDataItemsCustomDataContentsItems]): ConversionEventsDataItemsCustomDataContentsItems =
  result = ConversionEventsDataItemsCustomDataContentsItems()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("item_brand") and node["item_brand"].kind != JNull:
      result.itemBrand = some(to(node["item_brand"], typeof(result.itemBrand.get())))
    if node.hasKey("item_brand_id") and node["item_brand_id"].kind != JNull:
      result.itemBrandId = some(to(node["item_brand_id"], typeof(result.itemBrandId.get())))
    if node.hasKey("item_category") and node["item_category"].kind != JNull:
      result.itemCategory = some(to(node["item_category"], typeof(result.itemCategory.get())))
    if node.hasKey("item_name") and node["item_name"].kind != JNull:
      result.itemName = some(to(node["item_name"], typeof(result.itemName.get())))
    if node.hasKey("item_price") and node["item_price"].kind != JNull:
      result.itemPrice = some(to(node["item_price"], typeof(result.itemPrice.get())))
    if node.hasKey("quantity") and node["quantity"].kind != JNull:
      result.quantity = some(to(node["quantity"], typeof(result.quantity.get())))

# Custom JSON serialization for ConversionEventsDataItemsCustomDataContentsItems with custom field names
proc `%`*(obj: ConversionEventsDataItemsCustomDataContentsItems): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.itemBrand.isSome():
    result["item_brand"] = %obj.itemBrand.get()
  if obj.itemBrandId.isSome():
    result["item_brand_id"] = %obj.itemBrandId.get()
  if obj.itemCategory.isSome():
    result["item_category"] = %obj.itemCategory.get()
  if obj.itemName.isSome():
    result["item_name"] = %obj.itemName.get()
  if obj.itemPrice.isSome():
    result["item_price"] = %obj.itemPrice.get()
  if obj.quantity.isSome():
    result["quantity"] = %obj.quantity.get()

