#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.14.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_creative_assets_attributes

type Operation* {.pure.} = enum
  CREATE

type CatalogsCreateCreativeAssetsItem* = object
  ## A creative assets item to be created.
  creativeAssetsId*: string ## The catalog creative assets id in the merchant namespace
  operation*: Operation
  attributes*: CatalogsCreativeAssetsAttributes

func `%`*(v: Operation): JsonNode =
  result = case v:
    of Operation.CREATE: %"CREATE"
func `$`*(v: Operation): string =
  result = case v:
    of Operation.CREATE: $("CREATE")

proc to*(node: JsonNode, T: typedesc[Operation]): Operation =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Operation, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("CREATE"):
    return Operation.CREATE
  else:
    raise newException(ValueError, "Invalid enum value for Operation: " & strVal)


# Custom JSON deserialization for CatalogsCreateCreativeAssetsItem with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsCreateCreativeAssetsItem]): CatalogsCreateCreativeAssetsItem =
  result = CatalogsCreateCreativeAssetsItem()
  if node.kind == JObject:
    if node.hasKey("creative_assets_id"):
      result.creativeAssetsId = to(node["creative_assets_id"], string)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], Operation)
    if node.hasKey("attributes"):
      result.attributes = to(node["attributes"], CatalogsCreativeAssetsAttributes)

# Custom JSON serialization for CatalogsCreateCreativeAssetsItem with custom field names
proc `%`*(obj: CatalogsCreateCreativeAssetsItem): JsonNode =
  result = newJObject()
  result["creative_assets_id"] = %obj.creativeAssetsId
  result["operation"] = %obj.operation
  result["attributes"] = %obj.attributes

