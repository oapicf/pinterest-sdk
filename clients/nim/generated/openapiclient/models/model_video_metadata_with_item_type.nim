#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ItemType* {.pure.} = enum
  Video

type VideoMetadataWithItemType* = object
  ## 
  coverImageUrl*: Option[string]
  duration*: Option[float] ## Duration (in miliseconds). Field maybe null after creation due to video processing time.
  height*: Option[int] ## Height (in pixels). Field maybe null after creation due to video processing time.
  itemType*: ItemType ## Discriminator literal identifying this as video metadata inside a `PinMediaMetadata` payload.
  videoUrl*: Option[string] ## Video url (720p).  **Note:** This field is limited and not available to all apps.
  videoUrlHls*: Option[string] ## Video url (HLS).  **Note:** This field is limited and not available to all apps.
  width*: Option[int] ## Width (in pixels). Field maybe null after creation due to video processing time.

func `%`*(v: ItemType): JsonNode =
  result = case v:
    of ItemType.Video: %"video"
func `$`*(v: ItemType): string =
  result = case v:
    of ItemType.Video: $("video")

proc to*(node: JsonNode, T: typedesc[ItemType]): ItemType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ItemType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("video"):
    return ItemType.Video
  else:
    raise newException(ValueError, "Invalid enum value for ItemType: " & strVal)


# Custom JSON deserialization for VideoMetadataWithItemType with custom field names
proc to*(node: JsonNode, T: typedesc[VideoMetadataWithItemType]): VideoMetadataWithItemType =
  result = VideoMetadataWithItemType()
  if node.kind == JObject:
    if node.hasKey("cover_image_url") and node["cover_image_url"].kind != JNull:
      result.coverImageUrl = some(to(node["cover_image_url"], typeof(result.coverImageUrl.get())))
    if node.hasKey("duration") and node["duration"].kind != JNull:
      result.duration = some(to(node["duration"], typeof(result.duration.get())))
    if node.hasKey("height") and node["height"].kind != JNull:
      result.height = some(to(node["height"], typeof(result.height.get())))
    if node.hasKey("item_type"):
      result.itemType = to(node["item_type"], ItemType)
    if node.hasKey("video_url") and node["video_url"].kind != JNull:
      result.videoUrl = some(to(node["video_url"], typeof(result.videoUrl.get())))
    if node.hasKey("video_url_hls") and node["video_url_hls"].kind != JNull:
      result.videoUrlHls = some(to(node["video_url_hls"], typeof(result.videoUrlHls.get())))
    if node.hasKey("width") and node["width"].kind != JNull:
      result.width = some(to(node["width"], typeof(result.width.get())))

# Custom JSON serialization for VideoMetadataWithItemType with custom field names
proc `%`*(obj: VideoMetadataWithItemType): JsonNode =
  result = newJObject()
  if obj.coverImageUrl.isSome():
    result["cover_image_url"] = %obj.coverImageUrl.get()
  if obj.duration.isSome():
    result["duration"] = %obj.duration.get()
  if obj.height.isSome():
    result["height"] = %obj.height.get()
  result["item_type"] = %obj.itemType
  if obj.videoUrl.isSome():
    result["video_url"] = %obj.videoUrl.get()
  if obj.videoUrlHls.isSome():
    result["video_url_hls"] = %obj.videoUrlHls.get()
  if obj.width.isSome():
    result["width"] = %obj.width.get()

