#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type StoreMetadata* = object
  ## Store metadata for a specific store location
  geohash*: Option[string] ## Geohash of the store location
  latitude*: Option[float64] ## Geographic latitude coordinate of the store
  longitude*: Option[float64] ## Geographic longitude coordinate of the store
  storeCode*: string ## Merchant provided store code
  storeId*: string ## Internal store code
  storeName*: Option[string] ## Store name


# Custom JSON deserialization for StoreMetadata with custom field names
proc to*(node: JsonNode, T: typedesc[StoreMetadata]): StoreMetadata =
  result = StoreMetadata()
  if node.kind == JObject:
    if node.hasKey("geohash") and node["geohash"].kind != JNull:
      result.geohash = some(to(node["geohash"], typeof(result.geohash.get())))
    if node.hasKey("latitude") and node["latitude"].kind != JNull:
      result.latitude = some(to(node["latitude"], typeof(result.latitude.get())))
    if node.hasKey("longitude") and node["longitude"].kind != JNull:
      result.longitude = some(to(node["longitude"], typeof(result.longitude.get())))
    if node.hasKey("store_code"):
      result.storeCode = to(node["store_code"], string)
    if node.hasKey("store_id"):
      result.storeId = to(node["store_id"], string)
    if node.hasKey("store_name") and node["store_name"].kind != JNull:
      result.storeName = some(to(node["store_name"], typeof(result.storeName.get())))

# Custom JSON serialization for StoreMetadata with custom field names
proc `%`*(obj: StoreMetadata): JsonNode =
  result = newJObject()
  if obj.geohash.isSome():
    result["geohash"] = %obj.geohash.get()
  if obj.latitude.isSome():
    result["latitude"] = %obj.latitude.get()
  if obj.longitude.isSome():
    result["longitude"] = %obj.longitude.get()
  result["store_code"] = %obj.storeCode
  result["store_id"] = %obj.storeId
  if obj.storeName.isSome():
    result["store_name"] = %obj.storeName.get()

