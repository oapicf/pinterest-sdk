#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_hotel_report_stats_parameters_report

type CatalogType* {.pure.} = enum
  HOTEL

type CatalogsHotelReportStatsParameters* = object
  ## Parameters for hotel report
  catalogType*: CatalogType
  report*: CatalogsHotelReportStatsParameters_report

func `%`*(v: CatalogType): JsonNode =
  result = case v:
    of CatalogType.HOTEL: %"HOTEL"
func `$`*(v: CatalogType): string =
  result = case v:
    of CatalogType.HOTEL: $("HOTEL")

proc to*(node: JsonNode, T: typedesc[CatalogType]): CatalogType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CatalogType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("HOTEL"):
    return CatalogType.HOTEL
  else:
    raise newException(ValueError, "Invalid enum value for CatalogType: " & strVal)


# Custom JSON deserialization for CatalogsHotelReportStatsParameters with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsHotelReportStatsParameters]): CatalogsHotelReportStatsParameters =
  result = CatalogsHotelReportStatsParameters()
  if node.kind == JObject:
    if node.hasKey("catalog_type"):
      result.catalogType = to(node["catalog_type"], CatalogType)
    if node.hasKey("report"):
      result.report = to(node["report"], CatalogsHotelReportStatsParameters_report)

# Custom JSON serialization for CatalogsHotelReportStatsParameters with custom field names
proc `%`*(obj: CatalogsHotelReportStatsParameters): JsonNode =
  result = newJObject()
  result["catalog_type"] = %obj.catalogType
  result["report"] = %obj.report

