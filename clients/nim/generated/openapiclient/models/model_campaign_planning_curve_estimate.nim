#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_campaign_planning_estimation_type
import model_campaign_planning_point_estimate

type CampaignPlanningCurveEstimate* = object
  ## 
  estimationType*: Option[CampaignPlanningEstimationType] ## Estimation type for campaign planning estimated curve
  points*: Option[seq[CampaignPlanningPointEstimate]] ## The estimation points that make up the estimated curve.


# Custom JSON deserialization for CampaignPlanningCurveEstimate with custom field names
proc to*(node: JsonNode, T: typedesc[CampaignPlanningCurveEstimate]): CampaignPlanningCurveEstimate =
  result = CampaignPlanningCurveEstimate()
  if node.kind == JObject:
    if node.hasKey("estimation_type") and node["estimation_type"].kind != JNull:
      result.estimationType = some(to(node["estimation_type"], typeof(result.estimationType.get())))
    if node.hasKey("points") and node["points"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["points"]
      if arrayNode.kind == JArray:
        var arr: seq[CampaignPlanningPointEstimate] = @[]
        for item in arrayNode.items:
          arr.add(to(item, CampaignPlanningPointEstimate))
        result.points = some(arr)

# Custom JSON serialization for CampaignPlanningCurveEstimate with custom field names
proc `%`*(obj: CampaignPlanningCurveEstimate): JsonNode =
  result = newJObject()
  if obj.estimationType.isSome():
    result["estimation_type"] = %obj.estimationType.get()
  if obj.points.isSome():
    result["points"] = %obj.points.get()

