#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_board_privacy

type BoardCreate* = object
  ## Resource create operation model.
  description*: Option[string]
  isAdsOnly*: Option[bool] ## If set to `true`, the board will be ad-only and can store ad-only Pins.
  name*: string ##      Name of the board.      **Note:** If you create an ad-only board by setting `is_ads_only`     to `true`, the board name automatically becomes \"Ad-only Pins\".
  privacy*: Option[BoardPrivacy] ##     Privacy setting for a board. Learn more about [secret](https://help.pinterest.com/en/article/secret-boards)     boards and [protected](https://help.pinterest.com/en/business/article/protected-boards) boards.      **Note:** If you create an ad-only board by setting `is_ads_only`     to `true`, the `privacy` settng automatically becomes `PROTECTED`. 


# Custom JSON deserialization for BoardCreate with custom field names
proc to*(node: JsonNode, T: typedesc[BoardCreate]): BoardCreate =
  result = BoardCreate()
  if node.kind == JObject:
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("is_ads_only") and node["is_ads_only"].kind != JNull:
      result.isAdsOnly = some(to(node["is_ads_only"], typeof(result.isAdsOnly.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("privacy") and node["privacy"].kind != JNull:
      result.privacy = some(to(node["privacy"], typeof(result.privacy.get())))

# Custom JSON serialization for BoardCreate with custom field names
proc `%`*(obj: BoardCreate): JsonNode =
  result = newJObject()
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.isAdsOnly.isSome():
    result["is_ads_only"] = %obj.isAdsOnly.get()
  result["name"] = %obj.name
  if obj.privacy.isSome():
    result["privacy"] = %obj.privacy.get()

