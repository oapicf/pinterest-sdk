#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DynamicTitlesUploadURL* = object
  ## 
  existingFilename*: Option[string] ## If present, a reviewed CSV was previously submitted for this ad group. Uploading a new file will replace it. Absent when no prior submission exists.
  requestId*: string ## Unique identifier for this upload session. Must be passed to the process endpoint.
  uploadUrl*: string ## Pre-signed S3 PUT URL to upload the reviewed CSV file.


# Custom JSON deserialization for DynamicTitlesUploadURL with custom field names
proc to*(node: JsonNode, T: typedesc[DynamicTitlesUploadURL]): DynamicTitlesUploadURL =
  result = DynamicTitlesUploadURL()
  if node.kind == JObject:
    if node.hasKey("existing_filename") and node["existing_filename"].kind != JNull:
      result.existingFilename = some(to(node["existing_filename"], typeof(result.existingFilename.get())))
    if node.hasKey("request_id"):
      result.requestId = to(node["request_id"], string)
    if node.hasKey("upload_url"):
      result.uploadUrl = to(node["upload_url"], string)

# Custom JSON serialization for DynamicTitlesUploadURL with custom field names
proc `%`*(obj: DynamicTitlesUploadURL): JsonNode =
  result = newJObject()
  if obj.existingFilename.isSome():
    result["existing_filename"] = %obj.existingFilename.get()
  result["request_id"] = %obj.requestId
  result["upload_url"] = %obj.uploadUrl

