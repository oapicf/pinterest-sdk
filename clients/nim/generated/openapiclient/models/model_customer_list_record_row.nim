#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CustomerListRecordRow* = object
  ## A single row in a multi-field customer list (v2 format).
  email*: Option[string] ## Email address (plain or hashed with SHA1, SHA256, or MD5).
  externalId*: Option[string] ## External ID identifier (not hashed).
  hashedPhoneNumber*: Option[string] ## Hashed phone number (hashed with SHA1, SHA256, or MD5).
  hashedPinnerId*: Option[string] ## Hashed pinner ID (hashed with SHA1, SHA256, or MD5).
  ipAddress*: Option[string] ## IP address (not hashed).
  liverampEnvelope*: Option[string] ## LiveRamp envelope identifier (Base64-encoded, not hashed).
  maid*: Option[string] ## Mobile advertising ID (plain or hashed with SHA1, SHA256, or MD5).
  userAgent*: Option[string] ## User agent string (not hashed).


# Custom JSON deserialization for CustomerListRecordRow with custom field names
proc to*(node: JsonNode, T: typedesc[CustomerListRecordRow]): CustomerListRecordRow =
  result = CustomerListRecordRow()
  if node.kind == JObject:
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("external_id") and node["external_id"].kind != JNull:
      result.externalId = some(to(node["external_id"], typeof(result.externalId.get())))
    if node.hasKey("hashed_phone_number") and node["hashed_phone_number"].kind != JNull:
      result.hashedPhoneNumber = some(to(node["hashed_phone_number"], typeof(result.hashedPhoneNumber.get())))
    if node.hasKey("hashed_pinner_id") and node["hashed_pinner_id"].kind != JNull:
      result.hashedPinnerId = some(to(node["hashed_pinner_id"], typeof(result.hashedPinnerId.get())))
    if node.hasKey("ip_address") and node["ip_address"].kind != JNull:
      result.ipAddress = some(to(node["ip_address"], typeof(result.ipAddress.get())))
    if node.hasKey("liveramp_envelope") and node["liveramp_envelope"].kind != JNull:
      result.liverampEnvelope = some(to(node["liveramp_envelope"], typeof(result.liverampEnvelope.get())))
    if node.hasKey("maid") and node["maid"].kind != JNull:
      result.maid = some(to(node["maid"], typeof(result.maid.get())))
    if node.hasKey("user_agent") and node["user_agent"].kind != JNull:
      result.userAgent = some(to(node["user_agent"], typeof(result.userAgent.get())))

# Custom JSON serialization for CustomerListRecordRow with custom field names
proc `%`*(obj: CustomerListRecordRow): JsonNode =
  result = newJObject()
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  if obj.externalId.isSome():
    result["external_id"] = %obj.externalId.get()
  if obj.hashedPhoneNumber.isSome():
    result["hashed_phone_number"] = %obj.hashedPhoneNumber.get()
  if obj.hashedPinnerId.isSome():
    result["hashed_pinner_id"] = %obj.hashedPinnerId.get()
  if obj.ipAddress.isSome():
    result["ip_address"] = %obj.ipAddress.get()
  if obj.liverampEnvelope.isSome():
    result["liveramp_envelope"] = %obj.liverampEnvelope.get()
  if obj.maid.isSome():
    result["maid"] = %obj.maid.get()
  if obj.userAgent.isSome():
    result["user_agent"] = %obj.userAgent.get()

