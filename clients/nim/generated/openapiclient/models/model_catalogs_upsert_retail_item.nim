#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_item_attributes_request

type Operation* {.pure.} = enum
  UPSERT

type CatalogsUpsertRetailItem* = object
  ## An item to be upserted
  attributes*: ItemAttributesRequest
  itemId*: string ## The catalog item id in the merchant namespace
  operation*: Operation

func `%`*(v: Operation): JsonNode =
  result = case v:
    of Operation.UPSERT: %"UPSERT"
func `$`*(v: Operation): string =
  result = case v:
    of Operation.UPSERT: $("UPSERT")

proc to*(node: JsonNode, T: typedesc[Operation]): Operation =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Operation, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("UPSERT"):
    return Operation.UPSERT
  else:
    raise newException(ValueError, "Invalid enum value for Operation: " & strVal)


# Custom JSON deserialization for CatalogsUpsertRetailItem with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsUpsertRetailItem]): CatalogsUpsertRetailItem =
  result = CatalogsUpsertRetailItem()
  if node.kind == JObject:
    if node.hasKey("attributes"):
      result.attributes = to(node["attributes"], ItemAttributesRequest)
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], Operation)

# Custom JSON serialization for CatalogsUpsertRetailItem with custom field names
proc `%`*(obj: CatalogsUpsertRetailItem): JsonNode =
  result = newJObject()
  result["attributes"] = %obj.attributes
  result["item_id"] = %obj.itemId
  result["operation"] = %obj.operation

