#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BidOptionsAppTypeMultipliers* = object
  ## This represents a mapping from app type targeting criteria to a bid price change value.  Values represent the change in bid price and can be positive or negative. For percentage changes, a value of 500.0 means a 5% change. For value changes, the amount represents the direct bid adjustment. All app type multipliers must be set at the same time. If a multiplier is not provided it is assumed to be 0 (no bid adjustment). The change must still remain between the range of 0 and 10.
  androidMobile*: Option[float]
  androidTablet*: Option[float]
  ipad*: Option[float]
  iphone*: Option[float]
  web*: Option[float]
  webMobile*: Option[float]


# Custom JSON deserialization for BidOptionsAppTypeMultipliers with custom field names
proc to*(node: JsonNode, T: typedesc[BidOptionsAppTypeMultipliers]): BidOptionsAppTypeMultipliers =
  result = BidOptionsAppTypeMultipliers()
  if node.kind == JObject:
    if node.hasKey("android_mobile") and node["android_mobile"].kind != JNull:
      result.androidMobile = some(to(node["android_mobile"], typeof(result.androidMobile.get())))
    if node.hasKey("android_tablet") and node["android_tablet"].kind != JNull:
      result.androidTablet = some(to(node["android_tablet"], typeof(result.androidTablet.get())))
    if node.hasKey("ipad") and node["ipad"].kind != JNull:
      result.ipad = some(to(node["ipad"], typeof(result.ipad.get())))
    if node.hasKey("iphone") and node["iphone"].kind != JNull:
      result.iphone = some(to(node["iphone"], typeof(result.iphone.get())))
    if node.hasKey("web") and node["web"].kind != JNull:
      result.web = some(to(node["web"], typeof(result.web.get())))
    if node.hasKey("web_mobile") and node["web_mobile"].kind != JNull:
      result.webMobile = some(to(node["web_mobile"], typeof(result.webMobile.get())))

# Custom JSON serialization for BidOptionsAppTypeMultipliers with custom field names
proc `%`*(obj: BidOptionsAppTypeMultipliers): JsonNode =
  result = newJObject()
  if obj.androidMobile.isSome():
    result["android_mobile"] = %obj.androidMobile.get()
  if obj.androidTablet.isSome():
    result["android_tablet"] = %obj.androidTablet.get()
  if obj.ipad.isSome():
    result["ipad"] = %obj.ipad.get()
  if obj.iphone.isSome():
    result["iphone"] = %obj.iphone.get()
  if obj.web.isSome():
    result["web"] = %obj.web.get()
  if obj.webMobile.isSome():
    result["web_mobile"] = %obj.webMobile.get()

