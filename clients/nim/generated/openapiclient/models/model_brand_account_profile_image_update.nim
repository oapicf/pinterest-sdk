#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ContentType* {.pure.} = enum
  ImageJpeg
  ImagePng

type BrandAccountProfileImageUpdate* = object
  ## Base64-encoded image media source
  contentType*: Option[ContentType]
  data*: Option[string]

func `%`*(v: ContentType): JsonNode =
  result = case v:
    of ContentType.ImageJpeg: %"image/jpeg"
    of ContentType.ImagePng: %"image/png"
func `$`*(v: ContentType): string =
  result = case v:
    of ContentType.ImageJpeg: $("image/jpeg")
    of ContentType.ImagePng: $("image/png")

proc to*(node: JsonNode, T: typedesc[ContentType]): ContentType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ContentType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("image/jpeg"):
    return ContentType.ImageJpeg
  of $("image/png"):
    return ContentType.ImagePng
  else:
    raise newException(ValueError, "Invalid enum value for ContentType: " & strVal)


# Custom JSON deserialization for BrandAccountProfileImageUpdate with custom field names
proc to*(node: JsonNode, T: typedesc[BrandAccountProfileImageUpdate]): BrandAccountProfileImageUpdate =
  result = BrandAccountProfileImageUpdate()
  if node.kind == JObject:
    if node.hasKey("content_type") and node["content_type"].kind != JNull:
      result.contentType = some(to(node["content_type"], ContentType))
    if node.hasKey("data") and node["data"].kind != JNull:
      result.data = some(to(node["data"], typeof(result.data.get())))

# Custom JSON serialization for BrandAccountProfileImageUpdate with custom field names
proc `%`*(obj: BrandAccountProfileImageUpdate): JsonNode =
  result = newJObject()
  if obj.contentType.isSome():
    result["content_type"] = %obj.contentType.get()
  if obj.data.isSome():
    result["data"] = %obj.data.get()

