#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ad
import model_pinterest/lib/error

type AdBatchItem* = object
  ## 
  data*: Option[Ad]
  exceptions*: Option[Pinterest.Lib.Error]


# Custom JSON deserialization for AdBatchItem with custom field names
proc to*(node: JsonNode, T: typedesc[AdBatchItem]): AdBatchItem =
  result = AdBatchItem()
  if node.kind == JObject:
    if node.hasKey("data") and node["data"].kind != JNull:
      result.data = some(to(node["data"], typeof(result.data.get())))
    if node.hasKey("exceptions") and node["exceptions"].kind != JNull:
      result.exceptions = some(to(node["exceptions"], typeof(result.exceptions.get())))

# Custom JSON serialization for AdBatchItem with custom field names
proc `%`*(obj: AdBatchItem): JsonNode =
  result = newJObject()
  if obj.data.isSome():
    result["data"] = %obj.data.get()
  if obj.exceptions.isSome():
    result["exceptions"] = %obj.exceptions.get()

