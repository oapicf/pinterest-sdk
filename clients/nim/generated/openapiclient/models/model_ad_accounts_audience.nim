#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ad_accounts_audience_rule
import model_audience_status
import model_audience_type

type AdAccountsAudience* = object
  ## 
  adAccountId*: Option[string] ## Ad account ID.
  audienceType*: Option[AudienceType] ## [Audience types](/docs/reference/glossary/#Audience Types): ACTALIKE, ENGAGEMENT, CUSTOMER_LIST and VISITOR
  createdByCompanyName*: string ## The company that created this audience.
  createdTimestamp*: int ## Creation time. Unix timestamp in seconds.
  description*: Option[string] ## Audience description.
  id*: string ## Audience ID.
  isNca*: bool ## Whether the audience derives from a new customer acquisition (expanded matching) customer list. Read-only.
  name*: Option[string] ## Audience name.
  rule*: Option[AdAccountsAudienceRule]
  size*: int ## Audience size.
  status*: AudienceStatus ## Audience status. READY, INITIALIZING, TOO_SMALL - Each audience list needs to have at least 100 people with Pinterest accounts before you can start using it.
  `type`*: string ## Always \"audience\".
  updatedTimestamp*: int ## Last update time. Unix timestamp in seconds.


# Custom JSON deserialization for AdAccountsAudience with custom field names
proc to*(node: JsonNode, T: typedesc[AdAccountsAudience]): AdAccountsAudience =
  result = AdAccountsAudience()
  if node.kind == JObject:
    if node.hasKey("ad_account_id") and node["ad_account_id"].kind != JNull:
      result.adAccountId = some(to(node["ad_account_id"], typeof(result.adAccountId.get())))
    if node.hasKey("audience_type") and node["audience_type"].kind != JNull:
      result.audienceType = some(to(node["audience_type"], typeof(result.audienceType.get())))
    if node.hasKey("created_by_company_name"):
      result.createdByCompanyName = to(node["created_by_company_name"], string)
    if node.hasKey("created_timestamp"):
      result.createdTimestamp = to(node["created_timestamp"], int)
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("is_nca"):
      result.isNca = to(node["is_nca"], bool)
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("rule") and node["rule"].kind != JNull:
      result.rule = some(to(node["rule"], typeof(result.rule.get())))
    if node.hasKey("size"):
      result.size = to(node["size"], int)
    if node.hasKey("status"):
      result.status = to(node["status"], AudienceStatus)
    if node.hasKey("type"):
      result.`type` = to(node["type"], string)
    if node.hasKey("updated_timestamp"):
      result.updatedTimestamp = to(node["updated_timestamp"], int)

# Custom JSON serialization for AdAccountsAudience with custom field names
proc `%`*(obj: AdAccountsAudience): JsonNode =
  result = newJObject()
  if obj.adAccountId.isSome():
    result["ad_account_id"] = %obj.adAccountId.get()
  if obj.audienceType.isSome():
    result["audience_type"] = %obj.audienceType.get()
  result["created_by_company_name"] = %obj.createdByCompanyName
  result["created_timestamp"] = %obj.createdTimestamp
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  result["id"] = %obj.id
  result["is_nca"] = %obj.isNca
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.rule.isSome():
    result["rule"] = %obj.rule.get()
  result["size"] = %obj.size
  result["status"] = %obj.status
  result["type"] = %obj.`type`
  result["updated_timestamp"] = %obj.updatedTimestamp

