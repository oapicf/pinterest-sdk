#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_catalogs_hotel_attributes

type Operation* {.pure.} = enum
  CREATE

type CatalogsCreateHotelItem* = object
  ## A hotel item to be created.
  attributes*: CatalogsHotelAttributes
  hotelId*: string ## The catalog hotel id in the merchant namespace
  operation*: Operation

func `%`*(v: Operation): JsonNode =
  result = case v:
    of Operation.CREATE: %"CREATE"
func `$`*(v: Operation): string =
  result = case v:
    of Operation.CREATE: $("CREATE")

proc to*(node: JsonNode, T: typedesc[Operation]): Operation =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Operation, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("CREATE"):
    return Operation.CREATE
  else:
    raise newException(ValueError, "Invalid enum value for Operation: " & strVal)


# Custom JSON deserialization for CatalogsCreateHotelItem with custom field names
proc to*(node: JsonNode, T: typedesc[CatalogsCreateHotelItem]): CatalogsCreateHotelItem =
  result = CatalogsCreateHotelItem()
  if node.kind == JObject:
    if node.hasKey("attributes"):
      result.attributes = to(node["attributes"], CatalogsHotelAttributes)
    if node.hasKey("hotel_id"):
      result.hotelId = to(node["hotel_id"], string)
    if node.hasKey("operation"):
      result.operation = to(node["operation"], Operation)

# Custom JSON serialization for CatalogsCreateHotelItem with custom field names
proc `%`*(obj: CatalogsCreateHotelItem): JsonNode =
  result = newJObject()
  result["attributes"] = %obj.attributes
  result["hotel_id"] = %obj.hotelId
  result["operation"] = %obj.operation

