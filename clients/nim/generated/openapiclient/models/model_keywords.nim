#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_keyword
import model_keyword_error

type Keywords* = object
  ## 
  errors*: seq[KeywordError] ## Keyword error
  keywords*: seq[Keyword] ## Keywords


# Custom JSON deserialization for Keywords with custom field names
proc to*(node: JsonNode, T: typedesc[Keywords]): Keywords =
  result = Keywords()
  if node.kind == JObject:
    if node.hasKey("errors"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["errors"]
      if arrayNode.kind == JArray:
        result.errors = @[]
        for item in arrayNode.items:
          result.errors.add(to(item, KeywordError))
    if node.hasKey("keywords"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["keywords"]
      if arrayNode.kind == JArray:
        result.keywords = @[]
        for item in arrayNode.items:
          result.keywords.add(to(item, Keyword))

# Custom JSON serialization for Keywords with custom field names
proc `%`*(obj: Keywords): JsonNode =
  result = newJObject()
  result["errors"] = %obj.errors
  result["keywords"] = %obj.keywords

