#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.23.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_product_categories_engagement_type
import model_vertical_product_category

type TrendingProductCategory* = object
  ## Trending shopping product category
  engagementType*: ProductCategoriesEngagementType ## Engagement type
  pctChangeMom*: int ## Month-over-month change percentage
  percentRelativeVolume*: int ## Relative volume percentage
  productCategory*: string ## Product Category Name
  verticals*: Option[seq[VerticalProductCategory]] ## Vertical name associated with the product category


# Custom JSON deserialization for TrendingProductCategory with custom field names
proc to*(node: JsonNode, T: typedesc[TrendingProductCategory]): TrendingProductCategory =
  result = TrendingProductCategory()
  if node.kind == JObject:
    if node.hasKey("engagement_type"):
      result.engagementType = to(node["engagement_type"], ProductCategoriesEngagementType)
    if node.hasKey("pct_change_mom"):
      result.pctChangeMom = to(node["pct_change_mom"], int)
    if node.hasKey("percent_relative_volume"):
      result.percentRelativeVolume = to(node["percent_relative_volume"], int)
    if node.hasKey("product_category"):
      result.productCategory = to(node["product_category"], string)
    if node.hasKey("verticals") and node["verticals"].kind != JNull:
      result.verticals = some(to(node["verticals"], typeof(result.verticals.get())))

# Custom JSON serialization for TrendingProductCategory with custom field names
proc `%`*(obj: TrendingProductCategory): JsonNode =
  result = newJObject()
  result["engagement_type"] = %obj.engagementType
  result["pct_change_mom"] = %obj.pctChangeMom
  result["percent_relative_volume"] = %obj.percentRelativeVolume
  result["product_category"] = %obj.productCategory
  if obj.verticals.isSome():
    result["verticals"] = %obj.verticals.get()

