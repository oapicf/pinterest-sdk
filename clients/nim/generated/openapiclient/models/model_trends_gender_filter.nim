#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TrendsGenderFilter* {.pure.} = enum
  Male
  Female
  Unknown

func `%`*(v: TrendsGenderFilter): JsonNode =
  result = case v:
    of TrendsGenderFilter.Male: %"male"
    of TrendsGenderFilter.Female: %"female"
    of TrendsGenderFilter.Unknown: %"unknown"

func `$`*(v: TrendsGenderFilter): string =
  result = case v:
    of TrendsGenderFilter.Male: $("male")
    of TrendsGenderFilter.Female: $("female")
    of TrendsGenderFilter.Unknown: $("unknown")

proc to*(node: JsonNode, T: typedesc[TrendsGenderFilter]): TrendsGenderFilter =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum TrendsGenderFilter, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("male"):
    return TrendsGenderFilter.Male
  of $("female"):
    return TrendsGenderFilter.Female
  of $("unknown"):
    return TrendsGenderFilter.Unknown
  else:
    raise newException(ValueError, "Invalid enum value for TrendsGenderFilter: " & strVal)

