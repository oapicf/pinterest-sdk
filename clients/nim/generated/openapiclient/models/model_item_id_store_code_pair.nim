#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ItemIdStoreCodePair* = object
  ## A pair of item_id and store_code that uniquely identifies a local inventory item
  itemId*: string ## Catalog item id in the merchant namespace
  storeCode*: string ## Store code for the local inventory item


# Custom JSON deserialization for ItemIdStoreCodePair with custom field names
proc to*(node: JsonNode, T: typedesc[ItemIdStoreCodePair]): ItemIdStoreCodePair =
  result = ItemIdStoreCodePair()
  if node.kind == JObject:
    if node.hasKey("item_id"):
      result.itemId = to(node["item_id"], string)
    if node.hasKey("store_code"):
      result.storeCode = to(node["store_code"], string)

# Custom JSON serialization for ItemIdStoreCodePair with custom field names
proc `%`*(obj: ItemIdStoreCodePair): JsonNode =
  result = newJObject()
  result["item_id"] = %obj.itemId
  result["store_code"] = %obj.storeCode

