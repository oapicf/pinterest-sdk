#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type WorkloadState* {.pure.} = enum
  NOTSTARTED
  RUNNING
  PAUSED
  SUCCEEDED
  FAILED

func `%`*(v: WorkloadState): JsonNode =
  result = case v:
    of WorkloadState.NOTSTARTED: %"NOT_STARTED"
    of WorkloadState.RUNNING: %"RUNNING"
    of WorkloadState.PAUSED: %"PAUSED"
    of WorkloadState.SUCCEEDED: %"SUCCEEDED"
    of WorkloadState.FAILED: %"FAILED"

func `$`*(v: WorkloadState): string =
  result = case v:
    of WorkloadState.NOTSTARTED: $("NOT_STARTED")
    of WorkloadState.RUNNING: $("RUNNING")
    of WorkloadState.PAUSED: $("PAUSED")
    of WorkloadState.SUCCEEDED: $("SUCCEEDED")
    of WorkloadState.FAILED: $("FAILED")

proc to*(node: JsonNode, T: typedesc[WorkloadState]): WorkloadState =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum WorkloadState, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("NOT_STARTED"):
    return WorkloadState.NOTSTARTED
  of $("RUNNING"):
    return WorkloadState.RUNNING
  of $("PAUSED"):
    return WorkloadState.PAUSED
  of $("SUCCEEDED"):
    return WorkloadState.SUCCEEDED
  of $("FAILED"):
    return WorkloadState.FAILED
  else:
    raise newException(ValueError, "Invalid enum value for WorkloadState: " & strVal)

