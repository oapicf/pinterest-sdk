#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type AdPreviewSourceImage* = object
  ## Ad preview source from an image URL.
  imageUrl*: string ## Image URL.
  promotionId*: Option[string] ## Promotion id for the ad to preview, optional and only applicable when creating ad preview for an existing promotion.
  title*: string ## Title displayed below ad.


# Custom JSON deserialization for AdPreviewSourceImage with custom field names
proc to*(node: JsonNode, T: typedesc[AdPreviewSourceImage]): AdPreviewSourceImage =
  result = AdPreviewSourceImage()
  if node.kind == JObject:
    if node.hasKey("image_url"):
      result.imageUrl = to(node["image_url"], string)
    if node.hasKey("promotion_id") and node["promotion_id"].kind != JNull:
      result.promotionId = some(to(node["promotion_id"], typeof(result.promotionId.get())))
    if node.hasKey("title"):
      result.title = to(node["title"], string)

# Custom JSON serialization for AdPreviewSourceImage with custom field names
proc `%`*(obj: AdPreviewSourceImage): JsonNode =
  result = newJObject()
  result["image_url"] = %obj.imageUrl
  if obj.promotionId.isSome():
    result["promotion_id"] = %obj.promotionId.get()
  result["title"] = %obj.title

