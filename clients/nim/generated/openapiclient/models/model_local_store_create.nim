#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_country

type LocalStoreCreate* = object
  ## Resource create operation model.
  addressPrimary*: Option[string] ## Primary address line of the store.
  addressSecondary*: Option[string] ## Secondary address line of the store.
  city*: Option[string] ## City where the store is located.
  country*: Country ## Country code where the store is located.
  latitude*: Option[float] ## Geographic latitude coordinate of the store.
  longitude*: Option[float] ## Geographic longitude coordinate of the store.
  name*: string ## The name of the local store.
  postalCode*: Option[string] ## Postal or ZIP code of the store.
  region*: Option[string] ## State or region code where the store is located.
  storeCode*: string ## Merchant provided code for the local store. Unique within the merchant's catalog.


# Custom JSON deserialization for LocalStoreCreate with custom field names
proc to*(node: JsonNode, T: typedesc[LocalStoreCreate]): LocalStoreCreate =
  result = LocalStoreCreate()
  if node.kind == JObject:
    if node.hasKey("address_primary") and node["address_primary"].kind != JNull:
      result.addressPrimary = some(to(node["address_primary"], typeof(result.addressPrimary.get())))
    if node.hasKey("address_secondary") and node["address_secondary"].kind != JNull:
      result.addressSecondary = some(to(node["address_secondary"], typeof(result.addressSecondary.get())))
    if node.hasKey("city") and node["city"].kind != JNull:
      result.city = some(to(node["city"], typeof(result.city.get())))
    if node.hasKey("country"):
      result.country = to(node["country"], Country)
    if node.hasKey("latitude") and node["latitude"].kind != JNull:
      result.latitude = some(to(node["latitude"], typeof(result.latitude.get())))
    if node.hasKey("longitude") and node["longitude"].kind != JNull:
      result.longitude = some(to(node["longitude"], typeof(result.longitude.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("postal_code") and node["postal_code"].kind != JNull:
      result.postalCode = some(to(node["postal_code"], typeof(result.postalCode.get())))
    if node.hasKey("region") and node["region"].kind != JNull:
      result.region = some(to(node["region"], typeof(result.region.get())))
    if node.hasKey("store_code"):
      result.storeCode = to(node["store_code"], string)

# Custom JSON serialization for LocalStoreCreate with custom field names
proc `%`*(obj: LocalStoreCreate): JsonNode =
  result = newJObject()
  if obj.addressPrimary.isSome():
    result["address_primary"] = %obj.addressPrimary.get()
  if obj.addressSecondary.isSome():
    result["address_secondary"] = %obj.addressSecondary.get()
  if obj.city.isSome():
    result["city"] = %obj.city.get()
  result["country"] = %obj.country
  if obj.latitude.isSome():
    result["latitude"] = %obj.latitude.get()
  if obj.longitude.isSome():
    result["longitude"] = %obj.longitude.get()
  result["name"] = %obj.name
  if obj.postalCode.isSome():
    result["postal_code"] = %obj.postalCode.get()
  if obj.region.isSome():
    result["region"] = %obj.region.get()
  result["store_code"] = %obj.storeCode

