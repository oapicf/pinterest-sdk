#
# Pinterest REST API
# 
# Pinterest's REST API
# The version of the OpenAPI document: 5.28.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PerformancePlusCampaignSettings* = object
  ## Pinterest Performance+ campaign settings.
  boostProspectingAdGroupBid*: Option[bool] ## Whether to boost prospecting ad group bid.
  pinnerListExclusions*: Option[seq[string]] ## List of campaign-level exclusion pinner list IDs.


# Custom JSON deserialization for PerformancePlusCampaignSettings with custom field names
proc to*(node: JsonNode, T: typedesc[PerformancePlusCampaignSettings]): PerformancePlusCampaignSettings =
  result = PerformancePlusCampaignSettings()
  if node.kind == JObject:
    if node.hasKey("boost_prospecting_ad_group_bid") and node["boost_prospecting_ad_group_bid"].kind != JNull:
      result.boostProspectingAdGroupBid = some(to(node["boost_prospecting_ad_group_bid"], typeof(result.boostProspectingAdGroupBid.get())))
    if node.hasKey("pinner_list_exclusions") and node["pinner_list_exclusions"].kind != JNull:
      result.pinnerListExclusions = some(to(node["pinner_list_exclusions"], typeof(result.pinnerListExclusions.get())))

# Custom JSON serialization for PerformancePlusCampaignSettings with custom field names
proc `%`*(obj: PerformancePlusCampaignSettings): JsonNode =
  result = newJObject()
  if obj.boostProspectingAdGroupBid.isSome():
    result["boost_prospecting_ad_group_bid"] = %obj.boostProspectingAdGroupBid.get()
  if obj.pinnerListExclusions.isSome():
    result["pinner_list_exclusions"] = %obj.pinnerListExclusions.get()

