/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.Error
import org.openapitools.server.model.LabelCreateRequest
import org.openapitools.server.model.LabelUpdateRequest
import org.openapitools.server.model.LabeledEntities
import org.openapitools.server.model.LabeledEntitiesCreate
import org.openapitools.server.model.LabelsList200Response
import org.openapitools.server.model.LabelsResponse
import org.openapitools.server.model.QueryLabelEntityStatusesItems
import org.openapitools.server.model.QueryLabelTypesItems

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class LabelsApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "LabelsApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val labelsApplyOperation = (apiOperation[LabeledEntities]("labelsApply")
    summary "Apply label to entity"
    parameters(pathParam[String]("adAccountId").description(""), pathParam[String]("labelId").description(""), bodyParam[LabeledEntitiesCreate]("labeledEntitiesCreate").description(""))
  )

  post("/ad_accounts/:ad_account_id/labels/:label_id/apply", operation(labelsApplyOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    val labelId = params.getOrElse("labelId", halt(400))
    //println("labelId: " + labelId)
    //println("labeledEntitiesCreate: " + labeledEntitiesCreate)
  }

  

  val labelsCreateOperation = (apiOperation[LabelsResponse]("labelsCreate")
    summary "Create labels"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[LabelCreateRequest]("labelCreateRequest").description(""))
  )

  post("/ad_accounts/:ad_account_id/labels", operation(labelsCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("labelCreateRequest: " + labelCreateRequest)
  }

  

  val labelsListOperation = (apiOperation[LabelsList200Response]("labelsList")
    summary "List labels"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[List[String]]("campaignIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("labelIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[QueryLabelEntityStatusesItems]]("entityStatuses").description("").optional.defaultValue(["ACTIVE"]), queryParam[List[QueryLabelTypesItems]]("labelTypes").description("").optional.defaultValue(["BRAND","CUSTOM"]), queryParam[String]("bookmark").description("").optional, queryParam[Int]("pageSize").description("").optional.defaultValue(25))
  )

  get("/ad_accounts/:ad_account_id/labels", operation(labelsListOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
        val labelIdsString = params.getAs[String]("labelIds")
    val labelIds = if("multi" == "default" || "multi" == "csv") {
      labelIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("labelIds: " + labelIds)
        val entityStatusesString = params.getAs[String]("entityStatuses")
    val entityStatuses = if("multi" == "default" || "multi" == "csv") {
      entityStatusesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("entityStatuses: " + entityStatuses)
        val labelTypesString = params.getAs[String]("labelTypes")
    val labelTypes = if("multi" == "default" || "multi" == "csv") {
      labelTypesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("labelTypes: " + labelTypes)
            val bookmark = params.getAs[String]("bookmark")

    //println("bookmark: " + bookmark)
            val pageSize = params.getAs[Int]("pageSize")

    //println("pageSize: " + pageSize)
  }

  

  val labelsRemoveOperation = (apiOperation[LabeledEntities]("labelsRemove")
    summary "Remove label from entities"
    parameters(pathParam[String]("adAccountId").description(""), pathParam[String]("labelId").description(""), bodyParam[LabeledEntitiesCreate]("labeledEntitiesCreate").description(""))
  )

  post("/ad_accounts/:ad_account_id/labels/:label_id/remove", operation(labelsRemoveOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    val labelId = params.getOrElse("labelId", halt(400))
    //println("labelId: " + labelId)
    //println("labeledEntitiesCreate: " + labeledEntitiesCreate)
  }

  

  val labelsUpdateOperation = (apiOperation[LabelsResponse]("labelsUpdate")
    summary "Update labels"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[LabelUpdateRequest]("labelUpdateRequest").description(""))
  )

  patch("/ad_accounts/:ad_account_id/labels", operation(labelsUpdateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("labelUpdateRequest: " + labelUpdateRequest)
  }

}
