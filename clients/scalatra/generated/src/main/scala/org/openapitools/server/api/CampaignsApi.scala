/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.AdPinAnalytics
import org.openapitools.server.model.AdsAnalyticsCampaignTargetingType
import org.openapitools.server.model.Campaign
import org.openapitools.server.model.CampaignBatchUpdateItem
import org.openapitools.server.model.CampaignBatchWriteResponseModel
import org.openapitools.server.model.CampaignCreateItem
import org.openapitools.server.model.CampaignDeliveryEstimatesCampaign
import org.openapitools.server.model.CampaignDeliveryEstimatesResponse
import org.openapitools.server.model.CampaignsAnalyticsMetrics
import org.openapitools.server.model.CampaignsList200Response
import org.openapitools.server.model.ConversionReportAttributionType
import org.openapitools.server.model.EntityStatus
import org.openapitools.server.model.Error
import org.openapitools.server.model.Granularity
import java.time.LocalDate
import org.openapitools.server.model.MetricsResponse
import org.openapitools.server.model.PaginationOrder
import org.openapitools.server.model.ReportingColumnSync
import org.openapitools.server.model.ReportingTimeZone

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class CampaignsApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "CampaignsApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val adPinsAnalyticsOperation = (apiOperation[List[AdPinAnalytics]]("adPinsAnalytics")
    summary "Get pins analytics"
    parameters(queryParam[String]("campaignId").description(""), queryParam[List[String]]("pinIds").description("").defaultValue(List.empty[String] ), queryParam[LocalDate]("startDate").description(""), queryParam[LocalDate]("endDate").description(""), queryParam[List[ReportingColumnSync]]("columns").description("").defaultValue(List.empty[ReportingColumnSync] ), queryParam[Granularity]("granularity").description(""), pathParam[String]("adAccountId").description(""), queryParam[Double]("clickWindowDays").description("").optional.defaultValue(30), queryParam[Double]("engagementWindowDays").description("").optional.defaultValue(30), queryParam[Double]("viewWindowDays").description("").optional.defaultValue(1), queryParam[String]("conversionReportTime").description("").optional.defaultValue(TIME_OF_AD_ACTION))
  )

  get("/ad_accounts/:ad_account_id/pins/analytics", operation(adPinsAnalyticsOperation)) {
            val campaignId = params.getAs[String]("campaignId")

    //println("campaignId: " + campaignId)
        val pinIdsString = params.getAs[String]("pinIds")
    val pinIds = if("multi" == "default" || "multi" == "csv") {
      pinIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("pinIds: " + pinIds)
            val startDate = params.getAs[LocalDate]("startDate")

    //println("startDate: " + startDate)
            val endDate = params.getAs[LocalDate]("endDate")

    //println("endDate: " + endDate)
        val columnsString = params.getAs[String]("columns")
    val columns = if("csv" == "default" || "csv" == "csv") {
      columnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("columns: " + columns)
            val granularity = params.getAs[Granularity]("granularity")

    //println("granularity: " + granularity)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
            val clickWindowDays = params.getAs[Double]("clickWindowDays")

    //println("clickWindowDays: " + clickWindowDays)
            val engagementWindowDays = params.getAs[Double]("engagementWindowDays")

    //println("engagementWindowDays: " + engagementWindowDays)
            val viewWindowDays = params.getAs[Double]("viewWindowDays")

    //println("viewWindowDays: " + viewWindowDays)
            val conversionReportTime = params.getAs[String]("conversionReportTime")

    //println("conversionReportTime: " + conversionReportTime)
  }

  

  val campaignTargetingAnalyticsGetOperation = (apiOperation[MetricsResponse]("campaignTargetingAnalyticsGet")
    summary "Get targeting analytics for campaigns"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[List[String]]("campaignIds").description("").defaultValue(List.empty[String] ), queryParam[LocalDate]("startDate").description(""), queryParam[LocalDate]("endDate").description(""), queryParam[List[AdsAnalyticsCampaignTargetingType]]("targetingTypes").description("").defaultValue(List.empty[AdsAnalyticsCampaignTargetingType] ), queryParam[List[ReportingColumnSync]]("columns").description("").defaultValue(List.empty[ReportingColumnSync] ), queryParam[Granularity]("granularity").description(""), queryParam[Double]("clickWindowDays").description("").optional.defaultValue(30), queryParam[Double]("engagementWindowDays").description("").optional.defaultValue(30), queryParam[Double]("viewWindowDays").description("").optional.defaultValue(1), queryParam[String]("conversionReportTime").description("").optional.defaultValue(TIME_OF_AD_ACTION), queryParam[List[ConversionReportAttributionType]]("attributionTypes").description("").optional.defaultValue(List.empty[ConversionReportAttributionType] ), queryParam[ReportingTimeZone]("reportingTimezone").description("").optional)
  )

  get("/ad_accounts/:ad_account_id/campaigns/targeting_analytics", operation(campaignTargetingAnalyticsGetOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
            val startDate = params.getAs[LocalDate]("startDate")

    //println("startDate: " + startDate)
            val endDate = params.getAs[LocalDate]("endDate")

    //println("endDate: " + endDate)
        val targetingTypesString = params.getAs[String]("targetingTypes")
    val targetingTypes = if("csv" == "default" || "csv" == "csv") {
      targetingTypesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("targetingTypes: " + targetingTypes)
        val columnsString = params.getAs[String]("columns")
    val columns = if("csv" == "default" || "csv" == "csv") {
      columnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("columns: " + columns)
            val granularity = params.getAs[Granularity]("granularity")

    //println("granularity: " + granularity)
            val clickWindowDays = params.getAs[Double]("clickWindowDays")

    //println("clickWindowDays: " + clickWindowDays)
            val engagementWindowDays = params.getAs[Double]("engagementWindowDays")

    //println("engagementWindowDays: " + engagementWindowDays)
            val viewWindowDays = params.getAs[Double]("viewWindowDays")

    //println("viewWindowDays: " + viewWindowDays)
            val conversionReportTime = params.getAs[String]("conversionReportTime")

    //println("conversionReportTime: " + conversionReportTime)
        val attributionTypesString = params.getAs[String]("attributionTypes")
    val attributionTypes = if("csv" == "default" || "csv" == "csv") {
      attributionTypesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("attributionTypes: " + attributionTypes)
            val reportingTimezone = params.getAs[ReportingTimeZone]("reportingTimezone")

    //println("reportingTimezone: " + reportingTimezone)
  }

  

  val campaignsAnalyticsOperation = (apiOperation[List[CampaignsAnalyticsMetrics]]("campaignsAnalytics")
    summary "Get campaign analytics"
    parameters(queryParam[LocalDate]("startDate").description(""), queryParam[LocalDate]("endDate").description(""), queryParam[List[String]]("campaignIds").description("").defaultValue(List.empty[String] ), queryParam[List[ReportingColumnSync]]("columns").description("").defaultValue(List.empty[ReportingColumnSync] ), queryParam[Granularity]("granularity").description(""), pathParam[String]("adAccountId").description(""), queryParam[Double]("clickWindowDays").description("").optional.defaultValue(30), queryParam[Double]("engagementWindowDays").description("").optional.defaultValue(30), queryParam[Double]("viewWindowDays").description("").optional.defaultValue(1), queryParam[String]("conversionReportTime").description("").optional.defaultValue(TIME_OF_AD_ACTION), queryParam[Boolean]("aggregateReportRows").description("").optional.defaultValue(false), queryParam[ReportingTimeZone]("reportingTimezone").description("").optional)
  )

  get("/ad_accounts/:ad_account_id/campaigns/analytics", operation(campaignsAnalyticsOperation)) {
            val startDate = params.getAs[LocalDate]("startDate")

    //println("startDate: " + startDate)
            val endDate = params.getAs[LocalDate]("endDate")

    //println("endDate: " + endDate)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
        val columnsString = params.getAs[String]("columns")
    val columns = if("csv" == "default" || "csv" == "csv") {
      columnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("columns: " + columns)
            val granularity = params.getAs[Granularity]("granularity")

    //println("granularity: " + granularity)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
            val clickWindowDays = params.getAs[Double]("clickWindowDays")

    //println("clickWindowDays: " + clickWindowDays)
            val engagementWindowDays = params.getAs[Double]("engagementWindowDays")

    //println("engagementWindowDays: " + engagementWindowDays)
            val viewWindowDays = params.getAs[Double]("viewWindowDays")

    //println("viewWindowDays: " + viewWindowDays)
            val conversionReportTime = params.getAs[String]("conversionReportTime")

    //println("conversionReportTime: " + conversionReportTime)
            val aggregateReportRows = params.getAs[Boolean]("aggregateReportRows")

    //println("aggregateReportRows: " + aggregateReportRows)
            val reportingTimezone = params.getAs[ReportingTimeZone]("reportingTimezone")

    //println("reportingTimezone: " + reportingTimezone)
  }

  

  val campaignsCreateOperation = (apiOperation[CampaignBatchWriteResponseModel]("campaignsCreate")
    summary "Create campaigns"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[CampaignCreateItem]]("campaignCreateItem").description(""))
  )

  post("/ad_accounts/:ad_account_id/campaigns", operation(campaignsCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("campaignCreateItem: " + campaignCreateItem)
  }

  

  val campaignsGetOperation = (apiOperation[Campaign]("campaignsGet")
    summary "Get campaign"
    parameters(pathParam[String]("campaignId").description(""), pathParam[String]("adAccountId").description(""))
  )

  get("/ad_accounts/:ad_account_id/campaigns/:campaign_id", operation(campaignsGetOperation)) {
    val campaignId = params.getOrElse("campaignId", halt(400))
    //println("campaignId: " + campaignId)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
  }

  

  val campaignsListOperation = (apiOperation[CampaignsList200Response]("campaignsList")
    summary "List campaigns"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[String]("bookmark").description("").optional, queryParam[Int]("pageSize").description("").optional.defaultValue(25), queryParam[PaginationOrder]("order").description("").optional, queryParam[List[String]]("campaignIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[EntityStatus]]("entityStatuses").description("").optional.defaultValue(["ACTIVE","PAUSED"]))
  )

  get("/ad_accounts/:ad_account_id/campaigns", operation(campaignsListOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
            val bookmark = params.getAs[String]("bookmark")

    //println("bookmark: " + bookmark)
            val pageSize = params.getAs[Int]("pageSize")

    //println("pageSize: " + pageSize)
            val order = params.getAs[PaginationOrder]("order")

    //println("order: " + order)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
        val entityStatusesString = params.getAs[String]("entityStatuses")
    val entityStatuses = if("multi" == "default" || "multi" == "csv") {
      entityStatusesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("entityStatuses: " + entityStatuses)
  }

  

  val campaignsUpdateOperation = (apiOperation[CampaignBatchWriteResponseModel]("campaignsUpdate")
    summary "Update campaigns"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[CampaignBatchUpdateItem]]("campaignBatchUpdateItem").description(""))
  )

  patch("/ad_accounts/:ad_account_id/campaigns", operation(campaignsUpdateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("campaignBatchUpdateItem: " + campaignBatchUpdateItem)
  }

  

  val getCampaignDeliveryEstimatesOperation = (apiOperation[CampaignDeliveryEstimatesResponse]("getCampaignDeliveryEstimates")
    summary "Get campaign delivery estimates"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[CampaignDeliveryEstimatesCampaign]]("campaignDeliveryEstimatesCampaign").description(""))
  )

  post("/ad_accounts/:ad_account_id/campaigns/delivery_estimates", operation(getCampaignDeliveryEstimatesOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("campaignDeliveryEstimatesCampaign: " + campaignDeliveryEstimatesCampaign)
  }

}
