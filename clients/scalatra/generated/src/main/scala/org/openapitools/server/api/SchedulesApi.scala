/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.Error
import org.openapitools.server.model.PaginationOrder
import org.openapitools.server.model.Schedule
import org.openapitools.server.model.ScheduleBatchUpdate
import org.openapitools.server.model.ScheduleCreate
import org.openapitools.server.model.ScheduleStatus
import org.openapitools.server.model.ScheduleType
import org.openapitools.server.model.SchedulesCreate200ResponseInner
import org.openapitools.server.model.SchedulesList200Response

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class SchedulesApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "SchedulesApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val schedulesCreateOperation = (apiOperation[List[SchedulesCreate200ResponseInner]]("schedulesCreate")
    summary "Create schedules"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[ScheduleCreate]]("scheduleCreate").description(""))
  )

  post("/ad_accounts/:ad_account_id/schedules", operation(schedulesCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("scheduleCreate: " + scheduleCreate)
  }

  

  val schedulesListOperation = (apiOperation[SchedulesList200Response]("schedulesList")
    summary "Get Schedules"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[List[String]]("entityIds").description("").defaultValue(List.empty[String] ), queryParam[String]("bookmark").description("").optional, queryParam[Int]("pageSize").description("").optional.defaultValue(25), queryParam[PaginationOrder]("order").description("").optional, queryParam[List[ScheduleStatus]]("scheduleStatuses").description("").optional.defaultValue(List.empty[ScheduleStatus] ), queryParam[ScheduleType]("scheduleType").description("").optional)
  )

  get("/ad_accounts/:ad_account_id/schedules", operation(schedulesListOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
        val entityIdsString = params.getAs[String]("entityIds")
    val entityIds = if("multi" == "default" || "multi" == "csv") {
      entityIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("entityIds: " + entityIds)
            val bookmark = params.getAs[String]("bookmark")

    //println("bookmark: " + bookmark)
            val pageSize = params.getAs[Int]("pageSize")

    //println("pageSize: " + pageSize)
            val order = params.getAs[PaginationOrder]("order")

    //println("order: " + order)
        val scheduleStatusesString = params.getAs[String]("scheduleStatuses")
    val scheduleStatuses = if("multi" == "default" || "multi" == "csv") {
      scheduleStatusesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("scheduleStatuses: " + scheduleStatuses)
            val scheduleType = params.getAs[ScheduleType]("scheduleType")

    //println("scheduleType: " + scheduleType)
  }

  

  val schedulesUpdateOperation = (apiOperation[List[SchedulesCreate200ResponseInner]]("schedulesUpdate")
    summary "Update schedules"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[ScheduleBatchUpdate]]("scheduleBatchUpdate").description(""))
  )

  patch("/ad_accounts/:ad_account_id/schedules", operation(schedulesUpdateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("scheduleBatchUpdate: " + scheduleBatchUpdate)
  }

}
