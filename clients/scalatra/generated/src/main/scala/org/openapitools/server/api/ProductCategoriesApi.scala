/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.AgeTrendsBucket
import org.openapitools.server.model.Error
import org.openapitools.server.model.FeaturedTrend
import org.openapitools.server.model.GenderBucket
import org.openapitools.server.model.InterestsEnum
import org.openapitools.server.model.ProductCategoriesEngagementType
import org.openapitools.server.model.ProductCategoryDetailLookbackWindow
import org.openapitools.server.model.ProductCategoryDetails
import org.openapitools.server.model.ProductCategoryEnum
import org.openapitools.server.model.ProductCategoryRegion
import org.openapitools.server.model.TrendingProductCategory
import org.openapitools.server.model.VerticalProductCategory

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class ProductCategoriesApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "ProductCategoriesApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val trendsFeaturedTopicsListOperation = (apiOperation[List[FeaturedTrend]]("trendsFeaturedTopicsList")
    summary "Get featured topics"
    parameters(queryParam[ProductCategoryRegion]("region").description(""), queryParam[InterestsEnum]("interest").description("").optional)
  )

  get("/trends/topics/featured", operation(trendsFeaturedTopicsListOperation)) {
            val region = params.getAs[ProductCategoryRegion]("region")

    //println("region: " + region)
            val interest = params.getAs[InterestsEnum]("interest")

    //println("interest: " + interest)
  }

  

  val trendsProductCategoriesDetailsListOperation = (apiOperation[List[ProductCategoryDetails]]("trendsProductCategoriesDetailsList")
    summary "Get product category details"
    parameters(queryParam[List[ProductCategoryEnum]]("productCategories").description("").defaultValue(List.empty[ProductCategoryEnum] ), queryParam[ProductCategoryRegion]("region").description(""), queryParam[ProductCategoryDetailLookbackWindow]("lookbackWindow").description("").optional, queryParam[ProductCategoriesEngagementType]("engagementType").description("").optional)
  )

  get("/trends/product_categories/details", operation(trendsProductCategoriesDetailsListOperation)) {
        val productCategoriesString = params.getAs[String]("productCategories")
    val productCategories = if("multi" == "default" || "multi" == "csv") {
      productCategoriesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("productCategories: " + productCategories)
            val region = params.getAs[ProductCategoryRegion]("region")

    //println("region: " + region)
            val lookbackWindow = params.getAs[ProductCategoryDetailLookbackWindow]("lookbackWindow")

    //println("lookbackWindow: " + lookbackWindow)
            val engagementType = params.getAs[ProductCategoriesEngagementType]("engagementType")

    //println("engagementType: " + engagementType)
  }

  

  val trendsProductCategoriesTrendingListOperation = (apiOperation[List[TrendingProductCategory]]("trendsProductCategoriesTrendingList")
    summary "Get a list of growing Shopping Product Categories"
    parameters(queryParam[ProductCategoryRegion]("region").description(""), queryParam[List[VerticalProductCategory]]("verticals").description("").optional.defaultValue(List.empty[VerticalProductCategory] ), queryParam[List[AgeTrendsBucket]]("ages").description("").optional.defaultValue(List.empty[AgeTrendsBucket] ), queryParam[List[GenderBucket]]("genders").description("").optional.defaultValue(List.empty[GenderBucket] ), queryParam[ProductCategoriesEngagementType]("engagementType").description("").optional)
  )

  get("/trends/product_categories/trending", operation(trendsProductCategoriesTrendingListOperation)) {
            val region = params.getAs[ProductCategoryRegion]("region")

    //println("region: " + region)
        val verticalsString = params.getAs[String]("verticals")
    val verticals = if("multi" == "default" || "multi" == "csv") {
      verticalsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("verticals: " + verticals)
        val agesString = params.getAs[String]("ages")
    val ages = if("multi" == "default" || "multi" == "csv") {
      agesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("ages: " + ages)
        val gendersString = params.getAs[String]("genders")
    val genders = if("multi" == "default" || "multi" == "csv") {
      gendersString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("genders: " + genders)
            val engagementType = params.getAs[ProductCategoriesEngagementType]("engagementType")

    //println("engagementType: " + engagementType)
  }

}
