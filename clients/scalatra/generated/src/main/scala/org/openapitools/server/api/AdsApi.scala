/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 */


package org.openapitools.server.api

import org.openapitools.server.model.Ad
import org.openapitools.server.model.AdBatchUpdate
import org.openapitools.server.model.AdBatchWriteResponseModel
import org.openapitools.server.model.AdCreate
import org.openapitools.server.model.AdPreviewRequest
import org.openapitools.server.model.AdPreviewURLResponse
import org.openapitools.server.model.AdsAnalytics
import org.openapitools.server.model.AdsAnalyticsAdTargetingType
import org.openapitools.server.model.AdsList200Response
import org.openapitools.server.model.CampaignAdPreview
import org.openapitools.server.model.CampaignAdPreviewCreate
import org.openapitools.server.model.CampaignAdPreviewCreate200ResponseInner
import org.openapitools.server.model.CampaignAdPreviewDelete200ResponseInner
import org.openapitools.server.model.ConversionAttributionWindowDays
import org.openapitools.server.model.ConversionReportAttributionType
import org.openapitools.server.model.ConversionReportTimeType
import org.openapitools.server.model.EntityStatus
import org.openapitools.server.model.Error
import org.openapitools.server.model.Granularity
import java.time.LocalDate
import org.openapitools.server.model.MetricsResponse
import org.openapitools.server.model.PaginationOrder
import org.openapitools.server.model.ReportingColumnSync
import org.openapitools.server.model.ReportingTimeZone

import java.io.File

import org.scalatra.ScalatraServlet
import org.scalatra.swagger._
import org.json4s._
import org.json4s.JsonDSL._
import org.scalatra.json.{ JValueResult, JacksonJsonSupport }
import org.scalatra.servlet.{ FileUploadSupport, MultipartConfig, SizeConstraintExceededException }

import scala.collection.JavaConverters._

class AdsApi(implicit val swagger: Swagger) extends ScalatraServlet
  with FileUploadSupport
  with JacksonJsonSupport
  with SwaggerSupport {
  protected implicit val jsonFormats: Formats = DefaultFormats

  protected val applicationDescription: String = "AdsApi"

  before() {
    contentType = formats("json")
    response.headers += ("Access-Control-Allow-Origin" -> "*")
  }
  

  val adPreviewsCreateOperation = (apiOperation[AdPreviewURLResponse]("adPreviewsCreate")
    summary "Create ad preview with pin or image"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[AdPreviewRequest]("adPreviewRequest").description(""))
  )

  post("/ad_accounts/:ad_account_id/ad_previews", operation(adPreviewsCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("adPreviewRequest: " + adPreviewRequest)
  }

  

  val adTargetingAnalyticsGetOperation = (apiOperation[MetricsResponse]("adTargetingAnalyticsGet")
    summary "Get targeting analytics for ads"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[List[String]]("adIds").description("").defaultValue(List.empty[String] ), queryParam[LocalDate]("startDate").description(""), queryParam[LocalDate]("endDate").description(""), queryParam[List[AdsAnalyticsAdTargetingType]]("targetingTypes").description("").defaultValue(List.empty[AdsAnalyticsAdTargetingType] ), queryParam[List[ReportingColumnSync]]("columns").description("").defaultValue(List.empty[ReportingColumnSync] ), queryParam[Granularity]("granularity").description(""), queryParam[ConversionAttributionWindowDays]("clickWindowDays").description("").optional, queryParam[ConversionAttributionWindowDays]("engagementWindowDays").description("").optional, queryParam[ConversionAttributionWindowDays]("viewWindowDays").description("").optional, queryParam[ConversionReportTimeType]("conversionReportTime").description("").optional, queryParam[List[ConversionReportAttributionType]]("attributionTypes").description("").optional.defaultValue(List.empty[ConversionReportAttributionType] ), queryParam[ReportingTimeZone]("reportingTimezone").description("").optional, queryParam[List[String]]("sortColumns").description("").optional.defaultValue(List.empty[String] ), queryParam[Boolean]("sortAscending").description("").optional)
  )

  get("/ad_accounts/:ad_account_id/ads/targeting_analytics", operation(adTargetingAnalyticsGetOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
        val adIdsString = params.getAs[String]("adIds")
    val adIds = if("multi" == "default" || "multi" == "csv") {
      adIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adIds: " + adIds)
            val startDate = params.getAs[LocalDate]("startDate")

    //println("startDate: " + startDate)
            val endDate = params.getAs[LocalDate]("endDate")

    //println("endDate: " + endDate)
        val targetingTypesString = params.getAs[String]("targetingTypes")
    val targetingTypes = if("csv" == "default" || "csv" == "csv") {
      targetingTypesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("targetingTypes: " + targetingTypes)
        val columnsString = params.getAs[String]("columns")
    val columns = if("csv" == "default" || "csv" == "csv") {
      columnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("columns: " + columns)
            val granularity = params.getAs[Granularity]("granularity")

    //println("granularity: " + granularity)
            val clickWindowDays = params.getAs[ConversionAttributionWindowDays]("clickWindowDays")

    //println("clickWindowDays: " + clickWindowDays)
            val engagementWindowDays = params.getAs[ConversionAttributionWindowDays]("engagementWindowDays")

    //println("engagementWindowDays: " + engagementWindowDays)
            val viewWindowDays = params.getAs[ConversionAttributionWindowDays]("viewWindowDays")

    //println("viewWindowDays: " + viewWindowDays)
            val conversionReportTime = params.getAs[ConversionReportTimeType]("conversionReportTime")

    //println("conversionReportTime: " + conversionReportTime)
        val attributionTypesString = params.getAs[String]("attributionTypes")
    val attributionTypes = if("csv" == "default" || "csv" == "csv") {
      attributionTypesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("attributionTypes: " + attributionTypes)
            val reportingTimezone = params.getAs[ReportingTimeZone]("reportingTimezone")

    //println("reportingTimezone: " + reportingTimezone)
        val sortColumnsString = params.getAs[String]("sortColumns")
    val sortColumns = if("multi" == "default" || "multi" == "csv") {
      sortColumnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("sortColumns: " + sortColumns)
            val sortAscending = params.getAs[Boolean]("sortAscending")

    //println("sortAscending: " + sortAscending)
  }

  

  val adsAnalyticsOperation = (apiOperation[List[AdsAnalytics]]("adsAnalytics")
    summary "Get ad analytics"
    parameters(queryParam[LocalDate]("startDate").description(""), queryParam[LocalDate]("endDate").description(""), queryParam[List[ReportingColumnSync]]("columns").description("").defaultValue(List.empty[ReportingColumnSync] ), queryParam[Granularity]("granularity").description(""), pathParam[String]("adAccountId").description(""), queryParam[List[String]]("pinIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("adIds").description("").optional.defaultValue(List.empty[String] ), queryParam[Double]("clickWindowDays").description("").optional.defaultValue(30), queryParam[Double]("engagementWindowDays").description("").optional.defaultValue(30), queryParam[Double]("viewWindowDays").description("").optional.defaultValue(1), queryParam[String]("conversionReportTime").description("").optional.defaultValue(TIME_OF_AD_ACTION), queryParam[List[String]]("campaignIds").description("").optional.defaultValue(List.empty[String] ), queryParam[ReportingTimeZone]("reportingTimezone").description("").optional)
  )

  get("/ad_accounts/:ad_account_id/ads/analytics", operation(adsAnalyticsOperation)) {
            val startDate = params.getAs[LocalDate]("startDate")

    //println("startDate: " + startDate)
            val endDate = params.getAs[LocalDate]("endDate")

    //println("endDate: " + endDate)
        val columnsString = params.getAs[String]("columns")
    val columns = if("csv" == "default" || "csv" == "csv") {
      columnsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("columns: " + columns)
            val granularity = params.getAs[Granularity]("granularity")

    //println("granularity: " + granularity)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
        val pinIdsString = params.getAs[String]("pinIds")
    val pinIds = if("multi" == "default" || "multi" == "csv") {
      pinIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("pinIds: " + pinIds)
        val adIdsString = params.getAs[String]("adIds")
    val adIds = if("multi" == "default" || "multi" == "csv") {
      adIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adIds: " + adIds)
            val clickWindowDays = params.getAs[Double]("clickWindowDays")

    //println("clickWindowDays: " + clickWindowDays)
            val engagementWindowDays = params.getAs[Double]("engagementWindowDays")

    //println("engagementWindowDays: " + engagementWindowDays)
            val viewWindowDays = params.getAs[Double]("viewWindowDays")

    //println("viewWindowDays: " + viewWindowDays)
            val conversionReportTime = params.getAs[String]("conversionReportTime")

    //println("conversionReportTime: " + conversionReportTime)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
            val reportingTimezone = params.getAs[ReportingTimeZone]("reportingTimezone")

    //println("reportingTimezone: " + reportingTimezone)
  }

  

  val adsCreateOperation = (apiOperation[AdBatchWriteResponseModel]("adsCreate")
    summary "Create ads"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[AdCreate]]("adCreate").description(""))
  )

  post("/ad_accounts/:ad_account_id/ads", operation(adsCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("adCreate: " + adCreate)
  }

  

  val adsGetOperation = (apiOperation[Ad]("adsGet")
    summary "Get ad"
    parameters(pathParam[String]("adId").description(""), pathParam[String]("adAccountId").description(""))
  )

  get("/ad_accounts/:ad_account_id/ads/:ad_id", operation(adsGetOperation)) {
    val adId = params.getOrElse("adId", halt(400))
    //println("adId: " + adId)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
  }

  

  val adsListOperation = (apiOperation[AdsList200Response]("adsList")
    summary "List ads"
    parameters(pathParam[String]("adAccountId").description(""), queryParam[String]("bookmark").description("").optional, queryParam[Int]("pageSize").description("").optional.defaultValue(25), queryParam[PaginationOrder]("order").description("").optional, queryParam[List[String]]("campaignIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("adGroupIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[String]]("adIds").description("").optional.defaultValue(List.empty[String] ), queryParam[List[EntityStatus]]("entityStatuses").description("").optional.defaultValue(["ACTIVE","PAUSED"]))
  )

  get("/ad_accounts/:ad_account_id/ads", operation(adsListOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
            val bookmark = params.getAs[String]("bookmark")

    //println("bookmark: " + bookmark)
            val pageSize = params.getAs[Int]("pageSize")

    //println("pageSize: " + pageSize)
            val order = params.getAs[PaginationOrder]("order")

    //println("order: " + order)
        val campaignIdsString = params.getAs[String]("campaignIds")
    val campaignIds = if("multi" == "default" || "multi" == "csv") {
      campaignIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("campaignIds: " + campaignIds)
        val adGroupIdsString = params.getAs[String]("adGroupIds")
    val adGroupIds = if("multi" == "default" || "multi" == "csv") {
      adGroupIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adGroupIds: " + adGroupIds)
        val adIdsString = params.getAs[String]("adIds")
    val adIds = if("multi" == "default" || "multi" == "csv") {
      adIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adIds: " + adIds)
        val entityStatusesString = params.getAs[String]("entityStatuses")
    val entityStatuses = if("multi" == "default" || "multi" == "csv") {
      entityStatusesString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("entityStatuses: " + entityStatuses)
  }

  

  val adsUpdateOperation = (apiOperation[AdBatchWriteResponseModel]("adsUpdate")
    summary "Update ads"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[AdBatchUpdate]]("adBatchUpdate").description(""))
  )

  patch("/ad_accounts/:ad_account_id/ads", operation(adsUpdateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("adBatchUpdate: " + adBatchUpdate)
  }

  

  val campaignAdPreviewCreateOperation = (apiOperation[List[CampaignAdPreviewCreate200ResponseInner]]("campaignAdPreviewCreate")
    summary "Create ad preview records for one or more ad groups"
    parameters(pathParam[String]("adAccountId").description(""), bodyParam[List[CampaignAdPreviewCreate]]("campaignAdPreviewCreate").description(""))
  )

  post("/ad_accounts/:ad_account_id/campaign_ad_preview", operation(campaignAdPreviewCreateOperation)) {
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
    //println("campaignAdPreviewCreate: " + campaignAdPreviewCreate)
  }

  

  val campaignAdPreviewDeleteOperation = (apiOperation[List[CampaignAdPreviewDelete200ResponseInner]]("campaignAdPreviewDelete")
    summary "Delete ad preview records for one or more ad groups"
    parameters(queryParam[List[String]]("adGroupIds").description("").defaultValue(List.empty[String] ), pathParam[String]("adAccountId").description(""))
  )

  delete("/ad_accounts/:ad_account_id/campaign_ad_preview", operation(campaignAdPreviewDeleteOperation)) {
        val adGroupIdsString = params.getAs[String]("adGroupIds")
    val adGroupIds = if("multi" == "default" || "multi" == "csv") {
      adGroupIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adGroupIds: " + adGroupIds)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
  }

  

  val campaignAdPreviewReadOperation = (apiOperation[List[CampaignAdPreview]]("campaignAdPreviewRead")
    summary "Fetch ad preview records for one or more ad groups"
    parameters(queryParam[List[String]]("adGroupIds").description("").defaultValue(List.empty[String] ), pathParam[String]("adAccountId").description(""))
  )

  get("/ad_accounts/:ad_account_id/campaign_ad_preview", operation(campaignAdPreviewReadOperation)) {
        val adGroupIdsString = params.getAs[String]("adGroupIds")
    val adGroupIds = if("multi" == "default" || "multi" == "csv") {
      adGroupIdsString match {
        case Some(str) => str.split(",").toSeq
        case None => Seq()
      }
    } else
      Seq()

    //println("adGroupIds: " + adGroupIds)
    val adAccountId = params.getOrElse("adAccountId", halt(400))
    //println("adAccountId: " + adAccountId)
  }

}
