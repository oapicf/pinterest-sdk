// tslint:disable
/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    ConversionAccessTokenResponse,
    ConversionAccessTokenResponseFromJSON,
    ConversionAccessTokenResponseToJSON,
    OauthAccessTokenResponse,
    OauthAccessTokenResponseFromJSON,
    OauthAccessTokenResponseToJSON,
} from '../models';

export interface OauthTokenRequest {
    grantType: OauthTokenGrantTypeEnum;
}

export interface TokenRevokeRequest {
    token: string;
    tokenTypeHint?: TokenRevokeTokenTypeHintEnum;
}


/**
 * Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
 * Generate OAuth access token for conversion API
 */
function oauthConversionTokenRaw<T>( requestConfig: runtime.TypedQueryConfig<T, ConversionAccessTokenResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['oauth', ["ads:write"]];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth/conversion_token`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ConversionAccessTokenResponseFromJSON(body), text);
    }

    return config;
}

/**
* Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
* Generate OAuth access token for conversion API
*/
export function oauthConversionToken<T>( requestConfig?: runtime.TypedQueryConfig<T, ConversionAccessTokenResponse>): QueryConfig<T> {
    return oauthConversionTokenRaw( requestConfig);
}

/**
 * Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for <a href=\'/docs/getting-started/set-up-authentication-and-authorization/\' target=\'blank\'>requesting and refreshing tokens</a>.  <strong>Note:</strong> If your app was created <strong>before September 25, 2025</strong>, make sure to set the <code>continuous_refresh</code> parameter to <code>true</code> to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use <a href=\'/docs/developer-tools/token-debugger/\' target=\'blank\'>Token Debugger</a> to validate and inspect your access token.
 * Generate OAuth access token
 */
function oauthTokenRaw<T>(requestParameters: OauthTokenRequest, requestConfig: runtime.TypedQueryConfig<T, OauthAccessTokenResponse> = {}): QueryConfig<T> {
    if (requestParameters.grantType === null || requestParameters.grantType === undefined) {
        throw new runtime.RequiredError('grantType','Required parameter requestParameters.grantType was null or undefined when calling oauthToken.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['basic'];
    const formData = new FormData();
    if (requestParameters.grantType !== undefined) {
        formData.append('grant_type', requestParameters.grantType as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth/token`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(OauthAccessTokenResponseFromJSON(body), text);
    }

    return config;
}

/**
* Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for <a href=\'/docs/getting-started/set-up-authentication-and-authorization/\' target=\'blank\'>requesting and refreshing tokens</a>.  <strong>Note:</strong> If your app was created <strong>before September 25, 2025</strong>, make sure to set the <code>continuous_refresh</code> parameter to <code>true</code> to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use <a href=\'/docs/developer-tools/token-debugger/\' target=\'blank\'>Token Debugger</a> to validate and inspect your access token.
* Generate OAuth access token
*/
export function oauthToken<T>(requestParameters: OauthTokenRequest, requestConfig?: runtime.TypedQueryConfig<T, OauthAccessTokenResponse>): QueryConfig<T> {
    return oauthTokenRaw(requestParameters, requestConfig);
}

/**
 * Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
 * Revoke a token
 */
function tokenRevokeRaw<T>(requestParameters: TokenRevokeRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.token === null || requestParameters.token === undefined) {
        throw new runtime.RequiredError('token','Required parameter requestParameters.token was null or undefined when calling tokenRevoke.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['basic'];
    const formData = new FormData();
    if (requestParameters.token !== undefined) {
        formData.append('token', requestParameters.token as any);
    }

    if (requestParameters.tokenTypeHint !== undefined) {
        formData.append('token_type_hint', requestParameters.tokenTypeHint as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth/token/revoke`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
* Revoke a token
*/
export function tokenRevoke<T>(requestParameters: TokenRevokeRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return tokenRevokeRaw(requestParameters, requestConfig);
}


/**
    * @export
    * @enum {string}
    */
export enum OauthTokenGrantTypeEnum {
    AuthorizationCode = 'authorization_code',
    RefreshToken = 'refresh_token',
    ClientCredentials = 'client_credentials'
}
/**
    * @export
    * @enum {string}
    */
export enum TokenRevokeTokenTypeHintEnum {
    AccessToken = 'access_token',
    RefreshToken = 'refresh_token'
}
