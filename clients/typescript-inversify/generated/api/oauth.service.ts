/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Observable } from 'rxjs/Observable';

import { map } from 'rxjs/operators';
import IHttpClient from '../IHttpClient';
import { inject, injectable } from 'inversify';
import { IAPIConfiguration } from '../IAPIConfiguration';
import { Headers } from '../Headers';
import HttpResponse from '../HttpResponse';

import { ConversionAccessToken } from '../model/conversionAccessToken';
import { OauthAccessToken } from '../model/oauthAccessToken';
import { PinterestLibError } from '../model/pinterestLibError';
import { TokenGrantType } from '../model/tokenGrantType';
import { TokenTypeHint } from '../model/tokenTypeHint';

import { COLLECTION_FORMATS }  from '../variables';



@injectable()
export class OauthService {
    private basePath: string = 'https://api.pinterest.com/v5';

    constructor(@inject('IApiHttpClient') private httpClient: IHttpClient,
        @inject('IAPIConfiguration') private APIConfiguration: IAPIConfiguration ) {
        if(this.APIConfiguration.basePath)
            this.basePath = this.APIConfiguration.basePath;
    }

    /**
     * Generate OAuth access token for conversion API
     * Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
     
     */
    public oauthConversionToken(observe?: 'body', headers?: Headers): Observable<ConversionAccessToken>;
    public oauthConversionToken(observe?: 'response', headers?: Headers): Observable<HttpResponse<ConversionAccessToken>>;
    public oauthConversionToken(observe: any = 'body', headers: Headers = {}): Observable<any> {
        // authentication (pinterest_oauth2) required
        if (this.APIConfiguration.accessToken) {
            let accessToken = typeof this.APIConfiguration.accessToken === 'function'
                ? this.APIConfiguration.accessToken()
                : this.APIConfiguration.accessToken;
            headers['Authorization'] = 'Bearer ' + accessToken;
        }
        headers['Accept'] = 'application/json';

        const response: Observable<HttpResponse<ConversionAccessToken>> = this.httpClient.post(`${this.basePath}/oauth/conversion_token`, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <ConversionAccessToken>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Generate OAuth access token
     * Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the &#x60;continuous_refresh&#x60; parameter to &#x60;true&#x60; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 
     * @param grantType 
     * @param code 
     * @param continuousRefresh   If your app was created before **September 25, 2025**, set to &#x60;true&#x60; to generate a [continuous refresh token](/docs/getting-started/set-up-authentication-and-authorization/#exchange-the-default-refresh-token-for-a-continuous-refresh-token), which has a 60-day expiration window. We no longer support the legacy refresh token, which has a 365-day expiration window.    If your app was created on or after **September 25, 2025**, ignore this parameter. You automatically receive a continuous refresh token when you request an access token.
     * @param redirectUri 
     * @param refreshToken 
     * @param scope 
     
     */
    public oauthToken(grantType: TokenGrantType, code?: string, continuousRefresh?: string, redirectUri?: string, refreshToken?: string, scope?: string, observe?: 'body', headers?: Headers): Observable<OauthAccessToken>;
    public oauthToken(grantType: TokenGrantType, code?: string, continuousRefresh?: string, redirectUri?: string, refreshToken?: string, scope?: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<OauthAccessToken>>;
    public oauthToken(grantType: TokenGrantType, code?: string, continuousRefresh?: string, redirectUri?: string, refreshToken?: string, scope?: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (grantType === null || grantType === undefined){
            throw new Error('Required parameter grantType was null or undefined when calling oauthToken.');
        }

        // authentication (basic) required
        if (this.APIConfiguration.username || this.APIConfiguration.password) {
            headers['Authorization'] = btoa(this.APIConfiguration.username + ':' + this.APIConfiguration.password);
        }
        headers['Accept'] = 'application/json';

        let formData: FormData = new FormData();
        headers['Content-Type'] = 'application/x-www-form-urlencoded;charset=UTF-8';
        if (code !== undefined) {
            formData.append('code', <any>code);
        }
        if (continuousRefresh !== undefined) {
            formData.append('continuous_refresh', <any>continuousRefresh);
        }
        if (grantType !== undefined) {
            formData.append('grant_type', <any>grantType);
        }
        if (redirectUri !== undefined) {
            formData.append('redirect_uri', <any>redirectUri);
        }
        if (refreshToken !== undefined) {
            formData.append('refresh_token', <any>refreshToken);
        }
        if (scope !== undefined) {
            formData.append('scope', <any>scope);
        }

        const response: Observable<HttpResponse<OauthAccessToken>> = this.httpClient.post(`${this.basePath}/oauth/token`, formData, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <OauthAccessToken>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Revoke a token
     * Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
     * @param token The token to revoke.
     * @param tokenTypeHint The type of the token to revoke. Please refer to [our developer guide for more information](https://developers.pinterest.com/docs/getting-started/set-up-authentication-and-authorization/) for more information.
     
     */
    public tokenRevoke(token: string, tokenTypeHint?: TokenTypeHint, observe?: 'body', headers?: Headers): Observable<any>;
    public tokenRevoke(token: string, tokenTypeHint?: TokenTypeHint, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public tokenRevoke(token: string, tokenTypeHint?: TokenTypeHint, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (token === null || token === undefined){
            throw new Error('Required parameter token was null or undefined when calling tokenRevoke.');
        }

        // authentication (basic) required
        if (this.APIConfiguration.username || this.APIConfiguration.password) {
            headers['Authorization'] = btoa(this.APIConfiguration.username + ':' + this.APIConfiguration.password);
        }
        headers['Accept'] = 'application/json';

        let formData: FormData = new FormData();
        headers['Content-Type'] = 'application/x-www-form-urlencoded;charset=UTF-8';
        if (token !== undefined) {
            formData.append('token', <any>token);
        }
        if (tokenTypeHint !== undefined) {
            formData.append('token_type_hint', <any>tokenTypeHint);
        }

        const response: Observable<HttpResponse<any>> = this.httpClient.post(`${this.basePath}/oauth/token/revoke`, formData, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }

}
