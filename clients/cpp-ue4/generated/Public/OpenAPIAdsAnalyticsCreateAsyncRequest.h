/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIAdAdsAnalyticsAsyncTargetingTypes.h"
#include "OpenAPIAdGroupSummaryStatus.h"
#include "OpenAPIAdsAnalyticsMetricsFilter.h"
#include "OpenAPICampaignObjectiveType.h"
#include "OpenAPICampaignSummaryStatus.h"
#include "OpenAPIConversionAttributionWindowDays.h"
#include "OpenAPIConversionReportAttributionType.h"
#include "OpenAPIConversionReportTimeType.h"
#include "OpenAPICustomConversionEventMetrics.h"
#include "OpenAPIDataOutputFormat.h"
#include "OpenAPIGranularity.h"
#include "OpenAPIMetricsReportingLevel.h"
#include "OpenAPIPinPromotionSummaryStatus.h"
#include "OpenAPIPrimarySort.h"
#include "OpenAPIProductGroupSummaryStatus.h"
#include "OpenAPIReportingColumnAsync.h"
#include "OpenAPIReportingTimeZone.h"

namespace OpenAPI
{

/*
 * OpenAPIAdsAnalyticsCreateAsyncRequest
 *
 * 
 */
class OPENAPI_API OpenAPIAdsAnalyticsCreateAsyncRequest : public Model
{
public:
    virtual ~OpenAPIAdsAnalyticsCreateAsyncRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* List of ad group ids */
	TOptional<TArray<FString>> AdGroupIds;
	/* List of values for filtering */
	TOptional<TArray<OpenAPIAdGroupSummaryStatus>> AdGroupStatuses;
	/* List of ad ids. This parameter is not supported for Product Item level reports. */
	TOptional<TArray<FString>> AdIds;
	/* List of values for filtering. This parameter is not supported for Product Item level reports. */
	TOptional<TArray<OpenAPIPinPromotionSummaryStatus>> AdStatuses;
	/* List of attribution types for the conversion report. */
	TOptional<TArray<OpenAPIConversionReportAttributionType>> AttributionTypes;
	/* Campaign brand label for filtering. */
	TOptional<FString> CampaignBrandLabel;
	/* Campaign custom label for filtering. */
	TOptional<FString> CampaignCustomLabel;
	/* List of campaign ids */
	TOptional<TArray<FString>> CampaignIds;
	/* List of values for filtering. [\"WEB_SESSIONS\"] is in BETA. */
	TOptional<TArray<OpenAPICampaignObjectiveType>> CampaignObjectiveTypes;
	/* List of status values for filtering */
	TOptional<TArray<OpenAPICampaignSummaryStatus>> CampaignStatuses;
	/* Number of days to use as the conversion attribution window for a pin click action. */
	TOptional<OpenAPIConversionAttributionWindowDays> ClickWindowDays;
	/* Metric and entity columns. Pin promotion and ad related columns are not supported for Product Item level reports. */
	TOptional<TArray<OpenAPIReportingColumnAsync>> Columns;
	/* Determines if the targeting types included in the request should be consolidated into a single breakdown. */
	TOptional<bool> CombineTargetingTypes;
	/* Date dimension for conversion metrics. */
	TOptional<OpenAPIConversionReportTimeType> ConversionReportTime;
	/* List of advertiser-defined custom conversion event metrics to include in the report */
	TOptional<TArray<OpenAPICustomConversionEventMetrics>> CustomConversionEventMetrics;
	/* Metric report end date (UTC). Format: YYYY-MM-DD */
	FString EndDate;
	/* Which hour of the end date to stop the report (inclusive). Only allowed for hourly reports. */
	TOptional<int32> EndHour;
	/* Number of days to use as the conversion attribution window for an engagement action. */
	TOptional<OpenAPIConversionAttributionWindowDays> EngagementWindowDays;
	/*   TOTAL - metrics are aggregated over the specified date range.   DAY - metrics are broken down daily.   HOUR - metrics are broken down hourly.   WEEKLY - metrics are broken down weekly.   MONTHLY - metrics are broken down monthly. */
	OpenAPIGranularity Granularity;
	/* Level of the report */
	TOptional<OpenAPIMetricsReportingLevel> Level;
	/* List of metrics filters */
	TOptional<TArray<OpenAPIAdsAnalyticsMetricsFilter>> MetricsFilters;
	TOptional<OpenAPIPrimarySort> PrimarySort;
	/* List of product group ids */
	TOptional<TArray<FString>> ProductGroupIds;
	/* List of values for filtering */
	TOptional<TArray<OpenAPIProductGroupSummaryStatus>> ProductGroupStatuses;
	/* List of product item ids */
	TOptional<TArray<FString>> ProductItemIds;
	TOptional<OpenAPIDataOutputFormat> ReportFormat;
	/* Specify the timezone to be applied for the reporting. */
	TOptional<OpenAPIReportingTimeZone> ReportingTimezone;
	/* Metric report start date (UTC). Format: YYYY-MM-DD */
	FString StartDate;
	/* Which hour of the start date to begin the report. Only allowed for hourly reports. */
	TOptional<int32> StartHour;
	/* List of targeting types. Requires `level` to be a value ending in `_TARGETING`. */
	TOptional<TArray<OpenAPIAdAdsAnalyticsAsyncTargetingTypes>> TargetingTypes;
	/* Number of days to use as the conversion attribution window for a view action. */
	TOptional<OpenAPIConversionAttributionWindowDays> ViewWindowDays;
};

}
