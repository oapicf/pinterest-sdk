/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIAdCollectionsHeaderType.h"
#include "OpenAPIAdDisapprovalReasons.h"
#include "OpenAPIAdReviewStatus.h"
#include "OpenAPICartingProduct.h"
#include "OpenAPICreativeType.h"
#include "OpenAPICustomizableCTAType.h"
#include "OpenAPIDisclosureType.h"
#include "OpenAPIEntityStatus.h"
#include "OpenAPIGridClickType.h"
#include "OpenAPIPinPromotionSummaryStatus.h"
#include "OpenAPIQuizPinData.h"
#include "OpenAPITrackingUrls.h"

namespace OpenAPI
{

/*
 * OpenAPIAd
 *
 * 
 */
class OPENAPI_API OpenAPIAd : public Model
{
public:
    virtual ~OpenAPIAd() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* The ID of the advertiser that this ad belongs to. */
	FString AdAccountId;
	/* ID of the ad group that contains the ad. */
	FString AdGroupId;
	/* Deep link URL for Android devices. */
	TOptional<FString> AndroidDeepLink;
	/* ID of the ad campaign that contains this ad. */
	FString CampaignId;
	/* Comma-separated deep links for the carousel pin on Android. */
	TOptional<TArray<FString>> CarouselAndroidDeepLinks;
	/* Comma-separated destination URLs for the carousel pin to promote. */
	TOptional<TArray<FString>> CarouselDestinationUrls;
	/* Comma-separated deep links for the carousel pin on iOS. */
	TOptional<TArray<FString>> CarouselIosDeepLinks;
	/* The vendor platform type of the carting/WTB ad. */
	TOptional<int32> CartingPlatformType;
	/* Array of carting/WTB products for the ad. */
	TOptional<TArray<OpenAPICartingProduct>> CartingProducts;
	/* Tracking url for the ad clicks. */
	TOptional<FString> ClickTrackingUrl;
	/* Destination URL template for all items within a collections drawer. */
	TOptional<FString> CollectionItemsDestinationUrlTemplate;
	TOptional<OpenAPIAdCollectionsHeaderType> CollectionsHeaderType;
	/* Pin creation time. Unix timestamp in seconds. */
	TOptional<int32> CreatedTime;
	OpenAPICreativeType CreativeType;
	TOptional<OpenAPICustomizableCTAType> CustomizableCtaType;
	/* Destination URL. */
	TOptional<FString> DestinationUrl;
	TOptional<OpenAPIDisclosureType> DisclosureType;
	/* URL for a page that provides disclosures about a pharmaceutical product, such as potential side effects. Make sure the URL takes the user directly to the disclosure content and the referenced site is secure. */
	TOptional<FString> DisclosureUrl;
	TOptional<OpenAPIGridClickType> GridClickType;
	/* The ID of this ad. */
	FString Id;
	/* Deep link URL for iOS devices. */
	TOptional<FString> IosDeepLink;
	/* Is the ad a carting/WTB ad? */
	TOptional<bool> IsCarting;
	/* Whether the advertiser has accepted the terms and conditions for collage ad. */
	TOptional<bool> IsCollageAcceptedTerms;
	/* Whether the collage ad has a single destination url override. */
	TOptional<bool> IsCollageSingleDestination;
	/* Is original pin deleted? */
	TOptional<bool> IsPinDeleted;
	/* Is pin repinnable? */
	TOptional<bool> IsRemovable;
	/* Lead form ID for lead ad generation. */
	TOptional<FString> LeadFormId;
	/* Name of the ad - 255 chars max. */
	TOptional<FString> Name;
	/* Pin ID. This field may only be updated for draft ads. */
	FString PinId;
	/* Before creating a quiz ad, you must create an organic Pin using POST/Create Pin for each result in the quiz. Quiz ads cannot be saved by a Pinner. Quiz ad results can be saved. */
	TOptional<OpenAPIQuizPinData> QuizPinData;
	/* Enum reason why the pin was rejected. Returned if `review_status` is \"REJECTED\". */
	TArray<OpenAPIAdDisapprovalReasons> RejectedReasons;
	/* Text reason why the pin was rejected. Returned if `review_status` is \"REJECTED\". */
	TArray<FString> RejectionLabels;
	/* Ad review status */
	OpenAPIAdReviewStatus ReviewStatus;
	TOptional<OpenAPIEntityStatus> Status;
	/* Ad summary status */
	OpenAPIPinPromotionSummaryStatus SummaryStatus;
	TOptional<OpenAPITrackingUrls> TrackingUrls;
	/* Always \"ad\". */
	FString Type;
	/* Last update time. Unix timestamp in seconds. */
	TOptional<int32> UpdatedTime;
	/* Tracking URL for ad impressions. */
	TOptional<FString> ViewTrackingUrl;
};

}
