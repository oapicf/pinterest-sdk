/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIConversionEventAppInfo.h"
#include "OpenAPIConversionEventDeviceInfo.h"
#include "OpenAPIConversionEventsDataItemsCustomData.h"
#include "OpenAPIConversionEventsUserDataProperties.h"

namespace OpenAPI
{

/*
 * OpenAPIConversionEventsDataItems
 *
 * 
 */
class OPENAPI_API OpenAPIConversionEventsDataItems : public Model
{
public:
    virtual ~OpenAPIConversionEventsDataItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* The source indicating where the conversion event occurred. - `app_android` - `app_ios` - `web` - `offline` */
	FString ActionSource;
	/* The app store app ID. */
	TOptional<FString> AppId;
	TOptional<OpenAPIConversionEventAppInfo> AppInfo;
	/* Name of the app. */
	TOptional<FString> AppName;
	/* Version of the app. */
	TOptional<FString> AppVersion;
	TOptional<OpenAPIConversionEventsDataItemsCustomData> CustomData;
	/* Brand of the user device. */
	TOptional<FString> DeviceBrand;
	/* User device's mobile carrier. */
	TOptional<FString> DeviceCarrier;
	TOptional<OpenAPIConversionEventDeviceInfo> DeviceInfo;
	/* Model of the user device. */
	TOptional<FString> DeviceModel;
	/* Type of the user device. */
	TOptional<FString> DeviceType;
	/* A unique id string that identifies this event and can be used for deduping between events ingested via both the conversion API and Pinterest tracking. Without this, event's data is likely to be double counted and will cause report metric inflation. Third-party vendors make sure this field is updated on both Pinterest tag and Conversions API side before rolling out template for Conversions API. */
	FString EventId;
	/* The type of the user event. Please use the right event_name; otherwise the event will not be accepted and show up correctly in reports.  - `add_payment_info` - `add_to_cart` - `add_to_wishlist` - `app_install` - `app_open` - `checkout` - `contact` - `custom` - `customize_product` - `find_location` - `initiate_checkout` - `lead` - `page_visit` - `schedule` - `search` - `signup` - `start_trial` - `submit_application` - `subscribe` - `view_category` - `view_content` - `watch_video` */
	FString EventName;
	/* URL of the web conversion event. */
	TOptional<FString> EventSourceUrl;
	/* The time when the event happened. Unix timestamp in seconds. */
	int64 EventTime = 0;
	/* Two-character ISO-639-1 language code indicating the user's language. */
	TOptional<FString> Language;
	/* When action_source is web or offline, it defines whether the user has opted out of tracking for web conversion events. While when action_source is app_android or app_ios, it defines whether the user has enabled Limit Ad Tracking on their iOS device, or opted out of Ads Personalization on their Android device. */
	TOptional<bool> OptOut;
	/* Version of the device operating system. */
	TOptional<FString> OsVersion;
	/* The third party partner name responsible to send the event to Conversions API on behalf of the advertiser. The naming convention is \"ss-partnername\" lowercase. E.g 'ss-shopify' */
	TOptional<FString> PartnerName;
	/* Object containing customer information data. Note, It is required at least one of 1) em, 2) hashed_maids or 3) pair client_ip_address + client_user_agent. */
	OpenAPIConversionEventsUserDataProperties UserData;
	/* Whether the event occurred when the user device was connected to wifi. */
	TOptional<bool> Wifi;
};

}
