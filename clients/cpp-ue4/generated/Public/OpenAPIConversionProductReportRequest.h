/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"
#include "OpenAPIConversionAttributionWindowDays.h"
#include "OpenAPIConversionProductReportingColumn.h"
#include "OpenAPIConversionReportTimeType.h"
#include "OpenAPIObjectiveType.h"

namespace OpenAPI
{

/*
 * OpenAPIConversionProductReportRequest
 *
 * Request for a brand, category, SKU report
 */
class OPENAPI_API OpenAPIConversionProductReportRequest : public Model
{
public:
    virtual ~OpenAPIConversionProductReportRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* List of ad group ids. <br>Only support ad_group_ids field when level of the report is AD_GROUP. */
	TOptional<TArray<FString>> AdGroupIds;
	/* List of campaign ids. <br>Only support campaign_ids field when level of the report is CAMPAIGN. */
	TOptional<TArray<FString>> CampaignIds;
	/* List of values for filtering. Default is ['CONSIDERATION','AWARENESS','WEB_CONVERSION','VIDEO_COMPLETION']. */
	TOptional<TArray<OpenAPIObjectiveType>> CampaignObjectiveTypes;
	/* Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. */
	TOptional<OpenAPIConversionAttributionWindowDays> ClickWindowDays;
	/* Metric and entity columns */
	TArray<OpenAPIConversionProductReportingColumn> Columns;
	enum class ConversionProductAttributionTypeEnum
	{
		_Default,
		BrandAttribution,
  	};

	static FString EnumToString(const ConversionProductAttributionTypeEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, ConversionProductAttributionTypeEnum& EnumValue);
	TOptional<ConversionProductAttributionTypeEnum> ConversionProductAttributionType;
	enum class ConversionProductBreakdownEnum
	{
		ProductBrand,
		ProductCategory,
		ProductBrandAndCategory,
		ProductSku,
		ProductSkuGroup,
  	};

	static FString EnumToString(const ConversionProductBreakdownEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, ConversionProductBreakdownEnum& EnumValue);
	TOptional<ConversionProductBreakdownEnum> ConversionProductBreakdown;
	/* The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. */
	TOptional<OpenAPIConversionReportTimeType> ConversionReportTime;
	/* Metric report end date (UTC). Format: YYYY-MM-DD.<br> A max of 1 year is allowed between the start and end date for reports. */
	FString EndDate;
	enum class GranularityEnum
	{
		Week,
		Month,
		Total,
  	};

	static FString EnumToString(const GranularityEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, GranularityEnum& EnumValue);
	/* TOTAL - metrics are aggregated over the specified date range.<br> WEEK - metrics are broken down weekly.<br> MONTH - metrics are broken down monthly. */
	GranularityEnum Granularity;
	enum class LevelEnum
	{
		Advertiser,
		Campaign,
		AdGroup,
  	};

	static FString EnumToString(const LevelEnum& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, LevelEnum& EnumValue);
	/* Level of the report */
	LevelEnum Level;
	/* List of SKU ids. <br>Only support product_sku_ids field when report breakdown type is PRODUCT_SKU_GROUP. */
	TOptional<TArray<FString>> ProductSkuIds;
	/* Name of the conversion product report. */
	FString ReportName;
	/* Metric report start date (UTC). Format: YYYY-MM-DD.<br> Start date must be after 2024-03-16. 7 day minimum time window for report is required. */
	FString StartDate;
	/* Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` day. */
	TOptional<OpenAPIConversionAttributionWindowDays> ViewWindowDays;
};

}
