/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"

namespace OpenAPI
{

/*
 * OpenAPICampaignDeliveryEstimatesDerivedMetrics
 *
 * Derived metrics for an ad campaign.
 */
class OPENAPI_API OpenAPICampaignDeliveryEstimatesDerivedMetrics : public Model
{
public:
    virtual ~OpenAPICampaignDeliveryEstimatesDerivedMetrics() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Estimated cost per click. */
	TOptional<float> Cpc;
	/* Lower estimate of the cost per click. */
	TOptional<float> CpcLower;
	/* Upper estimate of the cost per click. */
	TOptional<float> CpcUpper;
	/* Estimated cost per thousand impressions. */
	TOptional<float> Cpm;
	/* Lower estimate of cost per thousand impressions. */
	TOptional<float> CpmLower;
	/* Upper estimate of cost per thousand impressions. */
	TOptional<float> CpmUpper;
	/* Estimated lifetime frequency. */
	TOptional<float> LifetimeFrequency;
	/* Lower estimate of lifetime frequency. */
	TOptional<float> LifetimeFrequencyLower;
	/* Upper estimate of lifetime frequency. */
	TOptional<float> LifetimeFrequencyUpper;
	/* Estimated lifetime impressions. */
	TOptional<float> LifetimeImpression;
	/* Lower estimate of lifetime impressions. */
	TOptional<float> LifetimeImpressionLower;
	/* Upper estimate of lifetime impressions. */
	TOptional<float> LifetimeImpressionUpper;
	/* Estimated lifetime reach. */
	TOptional<float> LifetimeReach;
	/* Lower estimate of lifetime reach. */
	TOptional<float> LifetimeReachLower;
	/* Upper estimate of lifetime reach. */
	TOptional<float> LifetimeReachUpper;
	/* Estimated weekly clicks. */
	TOptional<float> WeeklyClick;
	/* Lower estimate of weekly clicks. */
	TOptional<float> WeeklyClickLower;
	/* Upper estimate of weekly clicks. */
	TOptional<float> WeeklyClickUpper;
	/* Estimated weekly frequency. */
	TOptional<float> WeeklyFrequency;
	/* Lower estimate of weekly frequency. */
	TOptional<float> WeeklyFrequencyLower;
	/* Upper estimate of weekly frequency. */
	TOptional<float> WeeklyFrequencyUpper;
	/* Estimated weekly impressions. */
	TOptional<float> WeeklyImpression;
	/* Lower estimate of weekly impressions. */
	TOptional<float> WeeklyImpressionLower;
	/* Upper estimate of weekly impressions. */
	TOptional<float> WeeklyImpressionUpper;
	/* Estimated weekly reach. */
	TOptional<float> WeeklyReach;
	/* Lower estimate of weekly reach. */
	TOptional<float> WeeklyReachLower;
	/* Upper estimate of weekly reach. */
	TOptional<float> WeeklyReachUpper;
};

}
