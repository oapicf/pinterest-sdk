/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ReportingTimeZone,
  BidFloor,
  DynamicTitlesDownloadCSV,
  DynamicTitlesProcessCSVCreate,
  AdGroup,
  Granularity,
  AdGroupAudienceSizingCreate,
  PinterestLibPaginationOrder,
  AdGroupAudienceSizing,
  AdGroupsAnalyticsMetrics,
  MetricsResponse,
  AdGroupsCreate200Response,
  DynamicTitlesGetStatus,
  DynamicTitlesUploadURL,
  AdGroupsList200Response,
  DynamicTitlesProcessCSV,
  BidFloorCreate,
} from './models';

/**
 * adGroupsAnalytics - parameters interface
 */
export interface IAdGroupsAnalyticsParams {
  startDate: string;
  endDate: string;
  adGroupIds: Array<string>;
  columns: Array<ReportingColumnSync>;
  granularity: Granularity;
  adAccountId: string;
  clickWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  engagementWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  viewWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  conversionReportTime?: 'TIME_OF_AD_ACTION' | 'TIME_OF_CONVERSION';
  aggregateReportRows?: boolean;
  reportingTimezone?: ReportingTimeZone;
}

/**
 * adGroupsAudienceSizing - parameters interface
 */
export interface IAdGroupsAudienceSizingParams {
  adAccountId: string;
  adGroupAudienceSizingCreate: AdGroupAudienceSizingCreate;
}

/**
 * adGroupsBidFloorGet - parameters interface
 */
export interface IAdGroupsBidFloorGetParams {
  adAccountId: string;
  bidFloorCreate: BidFloorCreate;
}

/**
 * adGroupsCreate - parameters interface
 */
export interface IAdGroupsCreateParams {
  adAccountId: string;
  adGroupCreateCreate: Array<AdGroupCreateCreate>;
}

/**
 * adGroupsDynamicTitlesDownloadCsv - parameters interface
 */
export interface IAdGroupsDynamicTitlesDownloadCsvParams {
  adAccountId: string;
  adGroupId: string;
}

/**
 * adGroupsDynamicTitlesGetStatus - parameters interface
 */
export interface IAdGroupsDynamicTitlesGetStatusParams {
  adAccountId: string;
  adGroupId: string;
}

/**
 * adGroupsDynamicTitlesGetUploadUrl - parameters interface
 */
export interface IAdGroupsDynamicTitlesGetUploadUrlParams {
  adAccountId: string;
  adGroupId: string;
}

/**
 * adGroupsDynamicTitlesProcessCsv - parameters interface
 */
export interface IAdGroupsDynamicTitlesProcessCsvParams {
  adAccountId: string;
  adGroupId: string;
  dynamicTitlesProcessCSVCreate: DynamicTitlesProcessCSVCreate;
}

/**
 * adGroupsGet - parameters interface
 */
export interface IAdGroupsGetParams {
  adGroupId: string;
  adAccountId: string;
}

/**
 * adGroupsList - parameters interface
 */
export interface IAdGroupsListParams {
  adAccountId: string;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
  campaignIds?: Array<string>;
  adGroupIds?: Array<string>;
  entityStatuses?: Array<EntityStatus>;
  translateInterestsToNames?: boolean;
}

/**
 * adGroupsTargetingAnalyticsGet - parameters interface
 */
export interface IAdGroupsTargetingAnalyticsGetParams {
  adAccountId: string;
  adGroupIds: Array<string>;
  startDate: string;
  endDate: string;
  targetingTypes: Array<AdsAnalyticsAdGroupTargetingType>;
  columns: Array<ReportingColumnSync>;
  granularity: Granularity;
  clickWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  engagementWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  viewWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  conversionReportTime?: 'TIME_OF_AD_ACTION' | 'TIME_OF_CONVERSION';
  attributionTypes?: Array<ConversionReportAttributionType>;
  reportingTimezone?: ReportingTimeZone;
  sortColumns?: Array<string>;
  sortAscending?: boolean;
}

/**
 * adGroupsUpdate - parameters interface
 */
export interface IAdGroupsUpdateParams {
  adAccountId: string;
  adGroupUpdateBatchUpdate: Array<AdGroupUpdateBatchUpdate>;
}

/**
 * getAdGroupsByPromotionIdsList - parameters interface
 */
export interface IGetAdGroupsByPromotionIdsListParams {
  adAccountId: string;
  promotionIds: Array<string>;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
}

/**
 * AdGroupsApi - API class
 */
@autoinject()
export class AdGroupsApi extends Api {

  /**
   * Creates a new AdGroupsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Get ad group analytics
   * Get analytics for the specified ad groups in the specified &#x60;ad_account_id&#x60;, filtered by the specified options.  - The token\&#39;s user_account must either be the Owner of the specified ad account, or have one of the necessary roles granted to them via [Business Access](https://help.pinterest.com/en/business/article/share-and-manage-access-to-your-ad-accounts): Admin, Analyst, Campaign Manager. - If granularity is not HOUR, you can pull data from up to 90 days before the current date in UTC time, with a maximum time range of 90 days. - If granularity is HOUR, you can pull data from up to 8 days before the current date in UTC time, with a maximum time range of 3 days.
   * @param params.startDate Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
   * @param params.endDate Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
   * @param params.adGroupIds List of Ad group Ids to use to filter the results.
   * @param params.columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile\&#39;s currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it\&#39;s microdollars. Otherwise, it\&#39;s in microunits of the advertiser\&#39;s currency.  For example, if the advertiser\&#39;s currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
   * @param params.granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.clickWindowDays Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days.
   * @param params.engagementWindowDays Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**.
   * @param params.viewWindowDays Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;1&#x60; day.
   * @param params.conversionReportTime The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
   * @param params.aggregateReportRows Determines if report rows should be aggregated across all requested entities. This feature is currently in BETA and is not available to all users.
   * @param params.reportingTimezone Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
   */
  async adGroupsAnalytics(params: IAdGroupsAnalyticsParams): Promise<Array<AdGroupsAnalyticsMetrics>> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsAnalytics', params, 'startDate');
    this.ensureParamIsSet('adGroupsAnalytics', params, 'endDate');
    this.ensureParamIsSet('adGroupsAnalytics', params, 'adGroupIds');
    this.ensureParamIsSet('adGroupsAnalytics', params, 'columns');
    this.ensureParamIsSet('adGroupsAnalytics', params, 'granularity');
    this.ensureParamIsSet('adGroupsAnalytics', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/analytics`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'start_date': params['startDate'],
        'end_date': params['endDate'],
        'ad_group_ids': params['adGroupIds'],
        'columns': params['columns'],
        'granularity': params['granularity'],
        'click_window_days': params['clickWindowDays'],
        'engagement_window_days': params['engagementWindowDays'],
        'view_window_days': params['viewWindowDays'],
        'conversion_report_time': params['conversionReportTime'],
        'aggregate_report_rows': params['aggregateReportRows'],
        'reporting_timezone': params['reportingTimezone'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get audience sizing
   * Get potential audience size for an ad group with given targeting criteria. Potential audience size estimates the number of people you may be able to reach per month with your campaign. It is based on historical advertising data and the targeting criteria you select. It does not guarantee results or take into account factors such as bid, budget, schedule, seasonality or product experiments.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupAudienceSizingCreate 
   */
  async adGroupsAudienceSizing(params: IAdGroupsAudienceSizingParams): Promise<AdGroupAudienceSizing> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsAudienceSizing', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsAudienceSizing', params, 'adGroupAudienceSizingCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/audience_sizing`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adGroupAudienceSizingCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get bid floors
   * List bid floors for your campaign configuration. Bid floors are given in microcurrency values based on the currency in the bid floor specification.  Microcurrency is used to track very small transactions, based on the currency set in the advertiser\&#39;s profile.  A microcurrency unit is 10^(-6) of the standard unit of currency selected in the advertiser\&#39;s profile.  **Equivalency equations**, using dollars as an example currency:  * $1 &#x3D; 1,000,000 microdollars * 1 microdollar &#x3D; $0.000001  **To convert between currency and microcurrency**, using dollars as an example currency:  * To convert dollars to microdollars, mutiply dollars by 1,000,000 * To convert microdollars to dollars, divide microdollars by 1,000,000  For more on bid floors see [Set your bid](https://help.pinterest.com/en/business/article/set-your-bid).
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bidFloorCreate 
   */
  async adGroupsBidFloorGet(params: IAdGroupsBidFloorGetParams): Promise<BidFloor> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsBidFloorGet', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsBidFloorGet', params, 'bidFloorCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/bid_floor`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['bidFloorCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create ad groups
   * Create multiple new ad groups. All ads in a given ad group will have the same budget, bid, run dates, targeting, and placement (search, browse, other).  For more information, [click here](https://help.pinterest.com/en/business/article/campaign-structure).  **Notes:** - &#x60;bid_in_micro_currency&#x60; and &#x60;budget_in_micro_currency&#x60; should be expressed in microcurrency amounts based on the currency field set in the advertiser\&#39;s profile.  Microcurrency is used to track very small transactions, based on the currency set in the advertiser\&#39;s profile. A microcurrency unit is 10^(-6) of the standard unit of currency selected in the advertiser\&#39;s profile.  **Equivalency equations**, using dollars as an example currency: - $1 &#x3D; 1,000,000 microdollars - 1 microdollar &#x3D; $0.000001  **To convert between currency and microcurrency**, using dollars as an example currency: - To convert dollars to microdollars, multiply dollars by 1,000,000 - To convert microdollars to dollars, divide microdollars by 1,000,000  - Ad groups belong to ad campaigns. Some types of campaigns (e.g. budget optimization) have limits on the number of ad groups they can hold. If you exceed those limits, you will get an error message. - Certain organizations with [closed beta](/docs/getting-started/using-beta-and-restricted-features/) access can set &#x60;start_time&#x60; and &#x60;end_time&#x60; at the ad group level for campaigns with Campaign Budget Optimization (CBO) objectives: &#x60;TRAFFIC&#x60;, &#x60;AWARENESS&#x60;, &#x60;WEB_CONVERSIONS&#x60;, and &#x60;CATALOG_SALES&#x60;. All other organizations can set these scheduling parameters for non-CBO campaigns only. - If the parent ad campaign has start and end times set, ad group start and end times must occur within the parent campaign schedule.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupCreateCreate 
   */
  async adGroupsCreate(params: IAdGroupsCreateParams): Promise<AdGroupsCreate200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsCreate', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsCreate', params, 'adGroupCreateCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adGroupCreateCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get dynamic titles CSV download URL
   * Get a presigned S3 download URL for the dynamic titles review CSV. Returns 400 if titles have not been generated yet.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupId Ad group ID.
   */
  async adGroupsDynamicTitlesDownloadCsv(params: IAdGroupsDynamicTitlesDownloadCsvParams): Promise<DynamicTitlesDownloadCSV> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsDynamicTitlesDownloadCsv', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsDynamicTitlesDownloadCsv', params, 'adGroupId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/csv`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'ad_group_id'}}`, encodeURIComponent(`${params['adGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get dynamic titles status
   * Get dynamic titles generation status for an ad group, including whether titles are ready for review and counts of generated and reviewed titles.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupId Ad group ID.
   */
  async adGroupsDynamicTitlesGetStatus(params: IAdGroupsDynamicTitlesGetStatusParams): Promise<DynamicTitlesGetStatus> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsDynamicTitlesGetStatus', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsDynamicTitlesGetStatus', params, 'adGroupId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/status`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'ad_group_id'}}`, encodeURIComponent(`${params['adGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get dynamic titles upload URL
   * Get a presigned S3 upload URL for the dynamic titles review CSV and a request_id for submission.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupId Ad group ID.
   */
  async adGroupsDynamicTitlesGetUploadUrl(params: IAdGroupsDynamicTitlesGetUploadUrlParams): Promise<DynamicTitlesUploadURL> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsDynamicTitlesGetUploadUrl', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsDynamicTitlesGetUploadUrl', params, 'adGroupId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/uploads`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'ad_group_id'}}`, encodeURIComponent(`${params['adGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Process dynamic titles CSV
   * Validate and process the uploaded dynamic titles review CSV. Returns validation errors if the CSV is invalid.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupId Ad group ID.
   * @param params.dynamicTitlesProcessCSVCreate 
   */
  async adGroupsDynamicTitlesProcessCsv(params: IAdGroupsDynamicTitlesProcessCsvParams): Promise<DynamicTitlesProcessCSV> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsDynamicTitlesProcessCsv', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsDynamicTitlesProcessCsv', params, 'adGroupId');
    this.ensureParamIsSet('adGroupsDynamicTitlesProcessCsv', params, 'dynamicTitlesProcessCSVCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'ad_group_id'}}`, encodeURIComponent(`${params['adGroupId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['dynamicTitlesProcessCSVCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get ad group
   * Get a specific ad group given the ad group ID.
   * @param params.adGroupId Ad group ID.
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async adGroupsGet(params: IAdGroupsGetParams): Promise<AdGroup> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsGet', params, 'adGroupId');
    this.ensureParamIsSet('adGroupsGet', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}`
      .replace(`{${'ad_group_id'}}`, encodeURIComponent(`${params['adGroupId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List ad groups
   * List ad groups based on provided campaign IDs or ad group IDs.(campaign_ids or ad_group_ids). **Note:** Provide only campaign_id or ad_group_id. Do not provide both.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.campaignIds List of Campaign Ids to use to filter the results.
   * @param params.adGroupIds List of Ad group Ids to retrieve keywords from. This feature is currently in BETA and is not available to all users.
   * @param params.entityStatuses Entity status
   * @param params.translateInterestsToNames Return interests as text names (if value is true) rather than topic IDs.
   */
  async adGroupsList(params: IAdGroupsListParams): Promise<AdGroupsList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'campaign_ids': params['campaignIds'],
        'ad_group_ids': params['adGroupIds'],
        'entity_statuses': params['entityStatuses'],
        'translate_interests_to_names': params['translateInterestsToNames'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get targeting analytics for ad groups
   * Get targeting analytics for one or more ad groups. For the requested ad group(s) and metrics, the response will include the requested metric information (e.g. SPEND_IN_DOLLAR) for the requested target type (e.g. \&quot;age_bucket\&quot;) for applicable values (e.g. \&quot;45-49\&quot;).  - The token\&#39;s user_account must either be the Owner of the specified ad account, or have one of the necessary roles granted to them via [Business Access](https://help.pinterest.com/en/business/article/share-and-manage-access-to-your-ad-accounts): Admin, Analyst, Campaign Manager. - If granularity is not HOUR, you can pull data from up to 90 days before the current date in UTC time, with a maximum time range of 90 days. - If granularity is HOUR, you can pull data from up to 8 days before the current date in UTC time, with a maximum time range of 3 days.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupIds List of Ad group Ids to use to filter the results.
   * @param params.startDate Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
   * @param params.endDate Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
   * @param params.targetingTypes Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\&quot;AGE_BUCKET_AND_GENDER\&quot;, \&quot;CREATIVE_ENHANCEMENTS\&quot;] are in BETA and not yet available to all users.
   * @param params.columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile\&#39;s currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it\&#39;s microdollars. Otherwise, it\&#39;s in microunits of the advertiser\&#39;s currency.  For example, if the advertiser\&#39;s currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
   * @param params.granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
   * @param params.clickWindowDays Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days.
   * @param params.engagementWindowDays Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**.
   * @param params.viewWindowDays Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;1&#x60; day.
   * @param params.conversionReportTime The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
   * @param params.attributionTypes List of types of attribution for the conversion report
   * @param params.reportingTimezone Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
   * @param params.sortColumns Sort Columns.
   * @param params.sortAscending Sort ascending.
   */
  async adGroupsTargetingAnalyticsGet(params: IAdGroupsTargetingAnalyticsGetParams): Promise<MetricsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'adGroupIds');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'startDate');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'endDate');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'targetingTypes');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'columns');
    this.ensureParamIsSet('adGroupsTargetingAnalyticsGet', params, 'granularity');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups/targeting_analytics`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'ad_group_ids': params['adGroupIds'],
        'start_date': params['startDate'],
        'end_date': params['endDate'],
        'targeting_types': params['targetingTypes'],
        'columns': params['columns'],
        'granularity': params['granularity'],
        'click_window_days': params['clickWindowDays'],
        'engagement_window_days': params['engagementWindowDays'],
        'view_window_days': params['viewWindowDays'],
        'conversion_report_time': params['conversionReportTime'],
        'attribution_types': params['attributionTypes'],
        'reporting_timezone': params['reportingTimezone'],
        'sort_columns': params['sortColumns'],
        'sort_ascending': params['sortAscending'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update ad groups
   * Update multiple existing ad groups.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adGroupUpdateBatchUpdate 
   */
  async adGroupsUpdate(params: IAdGroupsUpdateParams): Promise<AdGroupsCreate200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('adGroupsUpdate', params, 'adAccountId');
    this.ensureParamIsSet('adGroupsUpdate', params, 'adGroupUpdateBatchUpdate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_groups`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adGroupUpdateBatchUpdate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List of ad groups using promotions IDs.
   *   Get a list of ad groups that are associated with those promotion ids
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.promotionIds List of Promotion IDs to use to filter the results.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   */
  async getAdGroupsByPromotionIdsList(params: IGetAdGroupsByPromotionIdsListParams): Promise<AdGroupsList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('getAdGroupsByPromotionIdsList', params, 'adAccountId');
    this.ensureParamIsSet('getAdGroupsByPromotionIdsList', params, 'promotionIds');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotion_applied_entities`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'promotion_ids': params['promotionIds'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

