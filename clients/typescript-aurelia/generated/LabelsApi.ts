/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  LabelsResponse,
  LabeledEntitiesCreate,
  LabelCreateRequest,
  LabelsList200Response,
  LabelUpdateRequest,
  LabeledEntities,
} from './models';

/**
 * labelsApply - parameters interface
 */
export interface ILabelsApplyParams {
  adAccountId: string;
  labelId: string;
  labeledEntitiesCreate: LabeledEntitiesCreate;
}

/**
 * labelsCreate - parameters interface
 */
export interface ILabelsCreateParams {
  adAccountId: string;
  labelCreateRequest: LabelCreateRequest;
}

/**
 * labelsList - parameters interface
 */
export interface ILabelsListParams {
  adAccountId: string;
  campaignIds?: Array<string>;
  labelIds?: Array<string>;
  entityStatuses?: Array<QueryLabelEntityStatusesItems>;
  labelTypes?: Array<QueryLabelTypesItems>;
  bookmark?: string;
  pageSize?: number;
}

/**
 * labelsRemove - parameters interface
 */
export interface ILabelsRemoveParams {
  adAccountId: string;
  labelId: string;
  labeledEntitiesCreate: LabeledEntitiesCreate;
}

/**
 * labelsUpdate - parameters interface
 */
export interface ILabelsUpdateParams {
  adAccountId: string;
  labelUpdateRequest: LabelUpdateRequest;
}

/**
 * LabelsApi - API class
 */
@autoinject()
export class LabelsApi extends Api {

  /**
   * Creates a new LabelsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Apply label to entity
   *   [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)    Apply a label to one or more campaigns.   Future releases may support labels for other [entities](/docs/key-concepts/pinterest-entities/) in addition to campaigns.   Currently, you can apply **brand** and **custom** labels. Future releases will provide more options.    **Note:** You can only apply one brand label to a campaign. You can apply up to 30 custom labels to a campaign.
   * @param params.adAccountId 
   * @param params.labelId Label ID.
   * @param params.labeledEntitiesCreate 
   */
  async labelsApply(params: ILabelsApplyParams): Promise<LabeledEntities> {
    // Verify required parameters are set
    this.ensureParamIsSet('labelsApply', params, 'adAccountId');
    this.ensureParamIsSet('labelsApply', params, 'labelId');
    this.ensureParamIsSet('labelsApply', params, 'labeledEntitiesCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/labels/{label_id}/apply`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'label_id'}}`, encodeURIComponent(`${params['labelId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['labeledEntitiesCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create labels
   * [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  Apply one or more labels to a campaign. Future releases may support labels for other [entities](/docs/key-concepts/pinterest-entities/). Currently, you can apply brand and custom labels. Future releases will provide more options.  **Note:** You can only apply one brand label to a campaign. You can apply 30 custom labels to a campaign.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.labelCreateRequest 
   */
  async labelsCreate(params: ILabelsCreateParams): Promise<LabelsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('labelsCreate', params, 'adAccountId');
    this.ensureParamIsSet('labelsCreate', params, 'labelCreateRequest');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/labels`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['labelCreateRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List labels
   * [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  See a list of labels for assets that your account owns, and filter the list by different criteria. If no filter is provided, it will default to labels associated with the ad account id.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.campaignIds List of Campaign Ids to use to filter the results.
   * @param params.labelIds List of Label Ids to use to filter the results.
   * @param params.entityStatuses Label entity status
   * @param params.labelTypes Label type.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   */
  async labelsList(params: ILabelsListParams): Promise<LabelsList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('labelsList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/labels`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'campaign_ids': params['campaignIds'],
        'label_ids': params['labelIds'],
        'entity_statuses': params['entityStatuses'],
        'label_types': params['labelTypes'],
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Remove label from entities
   *   [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)    Remove a label from one or more entities.
   * @param params.adAccountId 
   * @param params.labelId Label ID.
   * @param params.labeledEntitiesCreate 
   */
  async labelsRemove(params: ILabelsRemoveParams): Promise<LabeledEntities> {
    // Verify required parameters are set
    this.ensureParamIsSet('labelsRemove', params, 'adAccountId');
    this.ensureParamIsSet('labelsRemove', params, 'labelId');
    this.ensureParamIsSet('labelsRemove', params, 'labeledEntitiesCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/labels/{label_id}/remove`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'label_id'}}`, encodeURIComponent(`${params['labelId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['labeledEntitiesCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update labels
   * [Closed beta](/docs/getting-started/using-beta-and-restricted-features/)  Change the properties of one or more labels.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.labelUpdateRequest 
   */
  async labelsUpdate(params: ILabelsUpdateParams): Promise<LabelsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('labelsUpdate', params, 'adAccountId');
    this.ensureParamIsSet('labelsUpdate', params, 'labelUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/labels`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['labelUpdateRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

