/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ProductTagsBulkAddRequest,
  ProductTagsBulkDeleteRequest,
  ProductTagsResponse,
} from './models';

/**
 * productTagsBulkAdd - parameters interface
 */
export interface IProductTagsBulkAddParams {
  pinId: string;
  productTagsBulkAddRequest: ProductTagsBulkAddRequest;
}

/**
 * productTagsBulkDelete - parameters interface
 */
export interface IProductTagsBulkDeleteParams {
  pinId: string;
  productTagsBulkDeleteRequest: ProductTagsBulkDeleteRequest;
}

/**
 * productTagsList - parameters interface
 */
export interface IProductTagsListParams {
  pinId: string;
}

/**
 * ProductTagsApi - API class
 */
@autoinject()
export class ProductTagsApi extends Api {

  /**
   * Creates a new ProductTagsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Add product tags to pin
   * Add product tags to a pin. Product tags allow you to tag product pins onto a hero pin. - Maximum 24 product tags can be added in a single request. - Duplicate pin_ids in the request will result in an error. - If any product tag fails eligibility check, the entire request fails (all-or-nothing). - If a product tag is already tagged on the pin, it is treated as a no-op success.
   * @param params.pinId Unique identifier of the hero pin that will receive product tags.
   * @param params.productTagsBulkAddRequest 
   */
  async productTagsBulkAdd(params: IProductTagsBulkAddParams): Promise<ProductTagsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('productTagsBulkAdd', params, 'pinId');
    this.ensureParamIsSet('productTagsBulkAdd', params, 'productTagsBulkAddRequest');

    // Create URL to call
    const url = `${this.basePath}/pins/{pin_id}/product_tags`
      .replace(`{${'pin_id'}}`, encodeURIComponent(`${params['pinId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['productTagsBulkAddRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete product tags from pin
   * Delete product tags from a pin. - If a product tag is not found on the pin, it is treated as a no-op success.
   * @param params.pinId Unique identifier of the hero pin that will receive product tags.
   * @param params.productTagsBulkDeleteRequest 
   */
  async productTagsBulkDelete(params: IProductTagsBulkDeleteParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('productTagsBulkDelete', params, 'pinId');
    this.ensureParamIsSet('productTagsBulkDelete', params, 'productTagsBulkDeleteRequest');

    // Create URL to call
    const url = `${this.basePath}/pins/{pin_id}/product_tags/bulk-delete`
      .replace(`{${'pin_id'}}`, encodeURIComponent(`${params['pinId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['productTagsBulkDeleteRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get product tags for pin
   * Retrieve all product tags for a pin. - Returns an empty array if the pin has no product tags.
   * @param params.pinId Unique identifier of the hero pin that will receive product tags.
   */
  async productTagsList(params: IProductTagsListParams): Promise<ProductTagsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('productTagsList', params, 'pinId');

    // Create URL to call
    const url = `${this.basePath}/pins/{pin_id}/product_tags`
      .replace(`{${'pin_id'}}`, encodeURIComponent(`${params['pinId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

