/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  AudienceOwnershipType,
  AdAccountsAudience,
  PinterestLibPaginationOrder,
  AdAccountsAudienceUpdate,
  AdAccountsAudienceCreate,
  AudiencesList200Response,
} from './models';

/**
 * audiencesCreate - parameters interface
 */
export interface IAudiencesCreateParams {
  adAccountId: string;
  adAccountsAudienceCreate: AdAccountsAudienceCreate;
}

/**
 * audiencesGet - parameters interface
 */
export interface IAudiencesGetParams {
  audienceId: string;
  adAccountId: string;
}

/**
 * audiencesList - parameters interface
 */
export interface IAudiencesListParams {
  adAccountId: string;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
  ownershipType?: AudienceOwnershipType;
  excludeNca?: boolean;
}

/**
 * audiencesUpdate - parameters interface
 */
export interface IAudiencesUpdateParams {
  audienceId: string;
  adAccountId: string;
  adAccountsAudienceUpdate: AdAccountsAudienceUpdate;
}

/**
 * AudiencesApi - API class
 */
@autoinject()
export class AudiencesApi extends Api {

  /**
   * Creates a new AudiencesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create audience
   * Create a new audience for the ad account.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adAccountsAudienceCreate 
   */
  async audiencesCreate(params: IAudiencesCreateParams): Promise<AdAccountsAudience> {
    // Verify required parameters are set
    this.ensureParamIsSet('audiencesCreate', params, 'adAccountId');
    this.ensureParamIsSet('audiencesCreate', params, 'adAccountsAudienceCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/audiences`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adAccountsAudienceCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get audience
   * Get a specific audience given the audience ID.
   * @param params.audienceId Audience ID.
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async audiencesGet(params: IAudiencesGetParams): Promise<AdAccountsAudience> {
    // Verify required parameters are set
    this.ensureParamIsSet('audiencesGet', params, 'audienceId');
    this.ensureParamIsSet('audiencesGet', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/audiences/{audience_id}`
      .replace(`{${'audience_id'}}`, encodeURIComponent(`${params['audienceId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List audiences
   * Get list of audiences for the ad account.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.ownershipType 
   * @param params.excludeNca When true, excludes audiences derived from new customer acquisition (expanded matching) customer lists from the result. Defaults to false (include all).
   */
  async audiencesList(params: IAudiencesListParams): Promise<AudiencesList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('audiencesList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/audiences`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'ownership_type': params['ownershipType'],
        'exclude_nca': params['excludeNca'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update audience
   * Update an existing audience for the ad account.
   * @param params.audienceId Audience ID.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adAccountsAudienceUpdate 
   */
  async audiencesUpdate(params: IAudiencesUpdateParams): Promise<AdAccountsAudience> {
    // Verify required parameters are set
    this.ensureParamIsSet('audiencesUpdate', params, 'audienceId');
    this.ensureParamIsSet('audiencesUpdate', params, 'adAccountId');
    this.ensureParamIsSet('audiencesUpdate', params, 'adAccountsAudienceUpdate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/audiences/{audience_id}`
      .replace(`{${'audience_id'}}`, encodeURIComponent(`${params['audienceId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adAccountsAudienceUpdate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

