/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  CustomerSegmentCreate,
  CustomerSegment,
  CustomerSegmentList200Response,
  PinterestLibPaginationOrder,
  CustomerSegmentUpdateRequestUpdateWithRequiredBody,
} from './models';

/**
 * customerSegmentCreate - parameters interface
 */
export interface ICustomerSegmentCreateParams {
  adAccountId: string;
  customerSegmentCreate: CustomerSegmentCreate;
}

/**
 * customerSegmentList - parameters interface
 */
export interface ICustomerSegmentListParams {
  adAccountId: string;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
  includeSizing?: boolean;
  searchQuery?: string;
}

/**
 * customerSegmentUpdate - parameters interface
 */
export interface ICustomerSegmentUpdateParams {
  adAccountId: string;
  customerSegmentUpdateRequestUpdateWithRequiredBody: CustomerSegmentUpdateRequestUpdateWithRequiredBody;
}

/**
 * CustomerSegmentApi - API class
 */
@autoinject()
export class CustomerSegmentApi extends Api {

  /**
   * Creates a new CustomerSegmentApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create customer segments
   * Customer segments allow advertisers to define existing customers for Pinterest Performance+ campaigns. Customer segments are made up of audience lists.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.customerSegmentCreate 
   */
  async customerSegmentCreate(params: ICustomerSegmentCreateParams): Promise<CustomerSegment> {
    // Verify required parameters are set
    this.ensureParamIsSet('customerSegmentCreate', params, 'adAccountId');
    this.ensureParamIsSet('customerSegmentCreate', params, 'customerSegmentCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/customer_segments`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['customerSegmentCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List customer segments
   * Get a list of the customer segments in the specified &#x60;ad_account_id&#x60;.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.includeSizing Include audience sizing in result or not
   * @param params.searchQuery Search query. Can contain pin description keywords or comma-separated pin IDs.
   */
  async customerSegmentList(params: ICustomerSegmentListParams): Promise<CustomerSegmentList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('customerSegmentList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/customer_segments`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'include_sizing': params['includeSizing'],
        'search_query': params['searchQuery'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update customer segments
   * Update the customer segment given advertiser ID and customer segment ID
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.customerSegmentUpdateRequestUpdateWithRequiredBody 
   */
  async customerSegmentUpdate(params: ICustomerSegmentUpdateParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('customerSegmentUpdate', params, 'adAccountId');
    this.ensureParamIsSet('customerSegmentUpdate', params, 'customerSegmentUpdateRequestUpdateWithRequiredBody');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/customer_segments`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['customerSegmentUpdateRequestUpdateWithRequiredBody'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

