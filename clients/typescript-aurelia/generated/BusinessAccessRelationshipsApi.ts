/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  GetBusinessEmployers200Response,
  PartnerType,
  BrandAccountCreate,
  DeleteBusinessPartnersDelete,
  DeleteBusinessPartners,
  UpdateBusinessMembershipsResponse,
  BrandAccount,
  SystemUserUpdateWithRequiredBody,
  DeleteBusinessMembership200Response,
  DeleteBusinessMembershipBody,
  BrandAccountUpdate,
} from './models';

/**
 * brandAccountsCreate - parameters interface
 */
export interface IBrandAccountsCreateParams {
  businessHierarchyId: string;
  brandAccountCreate: BrandAccountCreate;
}

/**
 * brandAccountsUpdate - parameters interface
 */
export interface IBrandAccountsUpdateParams {
  brandAccountId: string;
  businessHierarchyId: string;
  brandAccountUpdate: BrandAccountUpdate;
}

/**
 * deleteBusinessMembership - parameters interface
 */
export interface IDeleteBusinessMembershipParams {
  businessId: string;
  deleteBusinessMembershipBody: DeleteBusinessMembershipBody;
}

/**
 * deleteBusinessPartners - parameters interface
 */
export interface IDeleteBusinessPartnersParams {
  businessId: string;
  deleteBusinessPartnersDelete: DeleteBusinessPartnersDelete;
}

/**
 * getBusinessEmployers - parameters interface
 */
export interface IGetBusinessEmployersParams {
  assetsSummary?: boolean;
  bookmark?: string;
  pageSize?: number;
}

/**
 * getBusinessMembers - parameters interface
 */
export interface IGetBusinessMembersParams {
  businessId: string;
  fetchSystemUsers?: boolean;
  assetsSummary?: boolean;
  businessRoles?: Array<MemberBusinessRole>;
  memberIds?: string;
  startIndex?: number;
  bookmark?: string;
  pageSize?: number;
}

/**
 * getBusinessPartners - parameters interface
 */
export interface IGetBusinessPartnersParams {
  businessId: string;
  assetsSummary?: boolean;
  partnerType?: PartnerType;
  partnerIds?: string;
  startIndex?: number;
  sortAscending?: boolean;
  bookmark?: string;
  pageSize?: number;
}

/**
 * systemUserUpdate - parameters interface
 */
export interface ISystemUserUpdateParams {
  businessId: string;
  systemUserId: string;
  systemUserUpdateWithRequiredBody: SystemUserUpdateWithRequiredBody;
}

/**
 * updateBusinessMemberships - parameters interface
 */
export interface IUpdateBusinessMembershipsParams {
  businessId: string;
  businessMembershipMember: Array<BusinessMembershipMember>;
}

/**
 * BusinessAccessRelationshipsApi - API class
 */
@autoinject()
export class BusinessAccessRelationshipsApi extends Api {

  /**
   * Creates a new BusinessAccessRelationshipsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create a Brand Account
   * Create a Brand Account that will be a child business of a business hierarchy. Request must contain name, username, and country.
   * @param params.businessHierarchyId business hierarchy node id
   * @param params.brandAccountCreate 
   */
  async brandAccountsCreate(params: IBrandAccountsCreateParams): Promise<BrandAccount> {
    // Verify required parameters are set
    this.ensureParamIsSet('brandAccountsCreate', params, 'businessHierarchyId');
    this.ensureParamIsSet('brandAccountsCreate', params, 'brandAccountCreate');

    // Create URL to call
    const url = `${this.basePath}/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts`
      .replace(`{${'business_hierarchy_id'}}`, encodeURIComponent(`${params['businessHierarchyId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['brandAccountCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update a Brand Account
   * Update an existing Brand Account
   * @param params.brandAccountId 
   * @param params.businessHierarchyId business hierarchy node id
   * @param params.brandAccountUpdate 
   */
  async brandAccountsUpdate(params: IBrandAccountsUpdateParams): Promise<BrandAccount> {
    // Verify required parameters are set
    this.ensureParamIsSet('brandAccountsUpdate', params, 'brandAccountId');
    this.ensureParamIsSet('brandAccountsUpdate', params, 'businessHierarchyId');
    this.ensureParamIsSet('brandAccountsUpdate', params, 'brandAccountUpdate');

    // Create URL to call
    const url = `${this.basePath}/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts/{brand_account_id}`
      .replace(`{${'brand_account_id'}}`, encodeURIComponent(`${params['brandAccountId']}`))
      .replace(`{${'business_hierarchy_id'}}`, encodeURIComponent(`${params['businessHierarchyId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['brandAccountUpdate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Terminate business memberships
   * Terminate memberships between the specified members and your business.
   * @param params.businessId Business id
   * @param params.deleteBusinessMembershipBody 
   */
  async deleteBusinessMembership(params: IDeleteBusinessMembershipParams): Promise<DeleteBusinessMembership200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteBusinessMembership', params, 'businessId');
    this.ensureParamIsSet('deleteBusinessMembership', params, 'deleteBusinessMembershipBody');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/members`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['deleteBusinessMembershipBody'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Terminate business partnerships
   * Terminate partnerships between the specified partners and your business. Note: You may only batch terminate partners of the same partner type.
   * @param params.businessId Unique identifier of the requesting business.
   * @param params.deleteBusinessPartnersDelete 
   */
  async deleteBusinessPartners(params: IDeleteBusinessPartnersParams): Promise<DeleteBusinessPartners> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteBusinessPartners', params, 'businessId');
    this.ensureParamIsSet('deleteBusinessPartners', params, 'deleteBusinessPartnersDelete');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/partners`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['deleteBusinessPartnersDelete'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List business employers for user
   * Get all of the viewing user\&#39;s business employers.
   * @param params.assetsSummary Include assets summary in the response if this is true. Defaults to true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   */
  async getBusinessEmployers(params: IGetBusinessEmployersParams): Promise<GetBusinessEmployers200Response> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/businesses/employers`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'assets_summary': params['assetsSummary'],
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get business members
   * Get all members of the specified business. The return response will include the member\&#39;s business_role and assets they have access to if assets_summary&#x3D;TRUE
   * @param params.businessId Unique identifier of the requesting business.
   * @param params.fetchSystemUsers Fetches system users if True. Fetches regular user employees if False.
   * @param params.assetsSummary Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
   * @param params.businessRoles A list of business roles to filter the members by. Only members whose roles are in the specified roles will be returned.
   * @param params.memberIds A list of business members ids separated by comma.
   * @param params.startIndex An index to start fetching the results from. Only the results starting from this index will be returned.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   */
  async getBusinessMembers(params: IGetBusinessMembersParams): Promise<GetBusinessEmployers200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('getBusinessMembers', params, 'businessId');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/members`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'fetch_system_users': params['fetchSystemUsers'],
        'assets_summary': params['assetsSummary'],
        'business_roles': params['businessRoles'],
        'member_ids': params['memberIds'],
        'start_index': params['startIndex'],
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get business partners
   * Get all partners of the specified business.  If the assets_summary&#x3D;TRUE and: - partner_type&#x3D;INTERNAL, the business assets returned are your business assets the partner has access to. - partner_type&#x3D;EXTERNAL, the business assets returned are your partner\&#39;s business assets the partner has granted you   access to.
   * @param params.businessId Unique identifier of the requesting business.
   * @param params.assetsSummary Include assets summary in the response if this is true.  The assets summary returns a dictionary representing a summary of the assets for the business user ID, with information like the ad accounts and profiles the user has permissions for and what those permissions are
   * @param params.partnerType Specifies whether to fetch internal or external (shared) partners. If partner_type&#x3D;INTERNAL, the asset being queried is for accesses the partner has to your business assets. If partner_type&#x3D;EXTERNAL, the asset being queried is for the accesses you have to the partner\&#39;s business asset.
   * @param params.partnerIds A list of business partner ids separated by commas used to filter the results. Only partners with the specified ids will be returned.
   * @param params.startIndex An index to start fetching the results from. Only the results starting from this index will be returned.
   * @param params.sortAscending Sort ascending.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   */
  async getBusinessPartners(params: IGetBusinessPartnersParams): Promise<GetBusinessEmployers200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('getBusinessPartners', params, 'businessId');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/partners`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'assets_summary': params['assetsSummary'],
        'partner_type': params['partnerType'],
        'partner_ids': params['partnerIds'],
        'start_index': params['startIndex'],
        'sort_ascending': params['sortAscending'],
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update a system user information.
   * Update a system user information such as name.
   * @param params.businessId Unique identifier of the requesting business.
   * @param params.systemUserId Unique identifier of a system user.
   * @param params.systemUserUpdateWithRequiredBody 
   */
  async systemUserUpdate(params: ISystemUserUpdateParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('systemUserUpdate', params, 'businessId');
    this.ensureParamIsSet('systemUserUpdate', params, 'systemUserId');
    this.ensureParamIsSet('systemUserUpdate', params, 'systemUserUpdateWithRequiredBody');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/system_users/{system_user_id}`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`))
      .replace(`{${'system_user_id'}}`, encodeURIComponent(`${params['systemUserId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['systemUserUpdateWithRequiredBody'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update member\&#39;s business role
   * Update a member\&#39;s business role within the business.
   * @param params.businessId Business id
   * @param params.businessMembershipMember 
   */
  async updateBusinessMemberships(params: IUpdateBusinessMembershipsParams): Promise<UpdateBusinessMembershipsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateBusinessMemberships', params, 'businessId');
    this.ensureParamIsSet('updateBusinessMemberships', params, 'businessMembershipMember');

    // Create URL to call
    const url = `${this.basePath}/businesses/{business_id}/members`
      .replace(`{${'business_id'}}`, encodeURIComponent(`${params['businessId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['businessMembershipMember'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

