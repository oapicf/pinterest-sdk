/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  LeadsExportsCreate,
  LeadsExports,
  LeadsExportResponseData,
} from './models';

/**
 * leadsExportCreate - parameters interface
 */
export interface ILeadsExportCreateParams {
  adAccountId: string;
  leadsExportsCreate: LeadsExportsCreate;
}

/**
 * leadsExportGet - parameters interface
 */
export interface ILeadsExportGetParams {
  adAccountId: string;
  leadsExportId: string;
}

/**
 * LeadsExportApi - API class
 */
@autoinject()
export class LeadsExportApi extends Api {

  /**
   * Creates a new LeadsExportApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create a request to export leads collected from a lead ad
   * **This feature is currently in beta and not available to all apps. If you\&#39;re interested in joining the beta, please reach out to your Pinterest account manager.**  Create an export of leads collected from a lead ad. This returns a &#x60;leads_export_id&#x60; token that you can use to download the export when it is ready.  Note: Lead ad data will be available up to 30 days after the lead has been submitted.  For more, see [Lead ads](https://help.pinterest.com/en/business/article/lead-ads).
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.leadsExportsCreate 
   */
  async leadsExportCreate(params: ILeadsExportCreateParams): Promise<LeadsExports> {
    // Verify required parameters are set
    this.ensureParamIsSet('leadsExportCreate', params, 'adAccountId');
    this.ensureParamIsSet('leadsExportCreate', params, 'leadsExportsCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/leads_export`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['leadsExportsCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get the lead export from the lead export create call
   * **This feature is currently in beta and not available to all apps. If you\&#39;re interested in joining the beta, please reach out to your Pinterest account manager.**  Get the export of leads collected from a lead ad. This returns a URL to a list of lead export given a lead_export_id token returned from the create a lead export call. You can use the URL to download the report.  Note: Lead ad data will be available up to 30 days after the lead has been submitted.  For more, see [Lead ads](https://help.pinterest.com/en/business/article/lead-ads).
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.leadsExportId lead_export_id token returned from the create a lead export endpoint
   */
  async leadsExportGet(params: ILeadsExportGetParams): Promise<LeadsExportResponseData> {
    // Verify required parameters are set
    this.ensureParamIsSet('leadsExportGet', params, 'adAccountId');
    this.ensureParamIsSet('leadsExportGet', params, 'leadsExportId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/leads_export/{leads_export_id}`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'leads_export_id'}}`, encodeURIComponent(`${params['leadsExportId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

