/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PromotionsList200Response,
  PromotionsResponse,
  PromotionResponse,
} from './models';

/**
 * promotionsCreate - parameters interface
 */
export interface IPromotionsCreateParams {
  adAccountId: string;
  promotionCreateRequest: Array<PromotionCreateRequest>;
}

/**
 * promotionsDelete - parameters interface
 */
export interface IPromotionsDeleteParams {
  adAccountId: string;
  promotionId: string;
}

/**
 * promotionsGet - parameters interface
 */
export interface IPromotionsGetParams {
  adAccountId: string;
  promotionId: string;
}

/**
 * promotionsList - parameters interface
 */
export interface IPromotionsListParams {
  adAccountId: string;
  pageSize?: number;
  order?: 'ASCENDING' | 'DESCENDING';
  bookmark?: string;
}

/**
 * promotionsUpdate - parameters interface
 */
export interface IPromotionsUpdateParams {
  adAccountId: string;
  promotionUpdateRequest: Array<PromotionUpdateRequest>;
}

/**
 * PromotionsApi - API class
 */
@autoinject()
export class PromotionsApi extends Api {

  /**
   * Creates a new PromotionsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create promotions
   * Create multiple new promotions.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.promotionCreateRequest List of promotions to create, size limit [1, 30].
   */
  async promotionsCreate(params: IPromotionsCreateParams): Promise<PromotionsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('promotionsCreate', params, 'adAccountId');
    this.ensureParamIsSet('promotionsCreate', params, 'promotionCreateRequest');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotions`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['promotionCreateRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete promotion by id
   * Delete a promotion within Pinterest.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.promotionId Unique identifier of a promotion
   */
  async promotionsDelete(params: IPromotionsDeleteParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('promotionsDelete', params, 'adAccountId');
    this.ensureParamIsSet('promotionsDelete', params, 'promotionId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotions/{promotion_id}`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'promotion_id'}}`, encodeURIComponent(`${params['promotionId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get promotion by id
   * Get a promotion by its Pinterest-specific id. It must be associated with the provided ad account id.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.promotionId Unique identifier of a promotion
   */
  async promotionsGet(params: IPromotionsGetParams): Promise<PromotionResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('promotionsGet', params, 'adAccountId');
    this.ensureParamIsSet('promotionsGet', params, 'promotionId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotions/{promotion_id}`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`))
      .replace(`{${'promotion_id'}}`, encodeURIComponent(`${params['promotionId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get promotions
   * Gets all promotions associated with an ad account ID that can be applied to an ad group. Can be either internally-saved promotions or external promotions imported from a commerce integration.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.pageSize Maximum number of items to include in a single page of the response. See documentation on &lt;a href&#x3D;\&#39;/docs/reference/pagination/\&#39;&gt;Pagination&lt;/a&gt; for more information.
   * @param params.order The order in which to sort the items returned: “ASCENDING” or “DESCENDING” by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.bookmark Cursor used to fetch the next page of items
   */
  async promotionsList(params: IPromotionsListParams): Promise<PromotionsList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('promotionsList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotions`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'page_size': params['pageSize'],
        'order': params['order'],
        'bookmark': params['bookmark'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update promotions
   * Update multiple promotions.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.promotionUpdateRequest List of promotions to create, size limit [1, 30].
   */
  async promotionsUpdate(params: IPromotionsUpdateParams): Promise<PromotionsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('promotionsUpdate', params, 'adAccountId');
    this.ensureParamIsSet('promotionsUpdate', params, 'promotionUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/promotions`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['promotionUpdateRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

