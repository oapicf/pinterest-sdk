/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ConversionDeletionRequestCreate,
  ConversionDeletionRequestList200Response,
  ConversionDeletionRequest,
  PinterestLibPaginationOrder,
} from './models';

/**
 * conversionDeletionRequestCreate - parameters interface
 */
export interface IConversionDeletionRequestCreateParams {
  adAccountId: string;
  conversionDeletionRequestCreate: ConversionDeletionRequestCreate;
}

/**
 * conversionDeletionRequestDelete - parameters interface
 */
export interface IConversionDeletionRequestDeleteParams {
  requestId: string;
  adAccountId: string;
}

/**
 * conversionDeletionRequestGet - parameters interface
 */
export interface IConversionDeletionRequestGetParams {
  requestId: string;
  adAccountId: string;
}

/**
 * conversionDeletionRequestList - parameters interface
 */
export interface IConversionDeletionRequestListParams {
  adAccountId: string;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
}

/**
 * ConversionDeletionRequestsApi - API class
 */
@autoinject()
export class ConversionDeletionRequestsApi extends Api {

  /**
   * Creates a new ConversionDeletionRequestsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create a conversion deletion request
   * **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Create a request to delete conversion data for a list of user emails and/or EPIKs, limited to the specified &#x60;ad_account_id&#x60;. After 72 hours the request is processed and submitted to our deletion process. Then the deletion process ensures deletion within a 30 days period, once the request is submitted to the deletion process it cannot be canceled.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.conversionDeletionRequestCreate 
   */
  async conversionDeletionRequestCreate(params: IConversionDeletionRequestCreateParams): Promise<ConversionDeletionRequest> {
    // Verify required parameters are set
    this.ensureParamIsSet('conversionDeletionRequestCreate', params, 'adAccountId');
    this.ensureParamIsSet('conversionDeletionRequestCreate', params, 'conversionDeletionRequestCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/conversion_deletion_requests`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['conversionDeletionRequestCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete a conversion deletion request
   * **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Delete a conversion deletion request from &#x60;ad_account_id&#x60; with &#x60;request_id&#x60;. This will cancel the request and prevent it from being processed. This can only be done if the request is in the &#x60;PENDING&#x60; status and before the 72 hours mark.
   * @param params.requestId Unique identifier of the conversion deletion request
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async conversionDeletionRequestDelete(params: IConversionDeletionRequestDeleteParams): Promise<ConversionDeletionRequest> {
    // Verify required parameters are set
    this.ensureParamIsSet('conversionDeletionRequestDelete', params, 'requestId');
    this.ensureParamIsSet('conversionDeletionRequestDelete', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}`
      .replace(`{${'request_id'}}`, encodeURIComponent(`${params['requestId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get a single conversion deletion request
   * **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Get a single conversion deletion request from &#x60;ad_account_id&#x60; with &#x60;request_id&#x60;.
   * @param params.requestId Unique identifier of the conversion deletion request
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async conversionDeletionRequestGet(params: IConversionDeletionRequestGetParams): Promise<ConversionDeletionRequest> {
    // Verify required parameters are set
    this.ensureParamIsSet('conversionDeletionRequestGet', params, 'requestId');
    this.ensureParamIsSet('conversionDeletionRequestGet', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/conversion_deletion_requests/{request_id}`
      .replace(`{${'request_id'}}`, encodeURIComponent(`${params['requestId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List conversion deletion requests
   * **This endpoint is currently in beta and not available to all apps.** [Learn more](/docs/key-concepts/using-beta-and-restricted-features/). Get a list of the conversion deletion requests for the specified &#x60;ad_account_id&#x60;.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   */
  async conversionDeletionRequestList(params: IConversionDeletionRequestListParams): Promise<ConversionDeletionRequestList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('conversionDeletionRequestList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/conversion_deletion_requests`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

