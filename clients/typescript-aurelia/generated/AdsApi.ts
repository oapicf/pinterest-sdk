/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ReportingTimeZone,
  AdsAnalytics,
  ConversionReportTimeType,
  Ad,
  CampaignAdPreviewDelete200ResponseInner,
  Granularity,
  AdPreviewURLResponse,
  CampaignAdPreview,
  ConversionAttributionWindowDays,
  AdPreviewRequest,
  PinterestLibPaginationOrder,
  MetricsResponse,
  CampaignAdPreviewCreate200ResponseInner,
  AdBatchWriteResponseModel,
  AdsList200Response,
} from './models';

/**
 * adPreviewsCreate - parameters interface
 */
export interface IAdPreviewsCreateParams {
  adAccountId: string;
  adPreviewRequest: AdPreviewRequest;
}

/**
 * adTargetingAnalyticsGet - parameters interface
 */
export interface IAdTargetingAnalyticsGetParams {
  adAccountId: string;
  adIds: Array<string>;
  startDate: string;
  endDate: string;
  targetingTypes: Array<AdsAnalyticsAdTargetingType>;
  columns: Array<ReportingColumnSync>;
  granularity: Granularity;
  clickWindowDays?: ConversionAttributionWindowDays;
  engagementWindowDays?: ConversionAttributionWindowDays;
  viewWindowDays?: ConversionAttributionWindowDays;
  conversionReportTime?: ConversionReportTimeType;
  attributionTypes?: Array<ConversionReportAttributionType>;
  reportingTimezone?: ReportingTimeZone;
  sortColumns?: Array<string>;
  sortAscending?: boolean;
}

/**
 * adsAnalytics - parameters interface
 */
export interface IAdsAnalyticsParams {
  startDate: string;
  endDate: string;
  columns: Array<ReportingColumnSync>;
  granularity: Granularity;
  adAccountId: string;
  pinIds?: Array<string>;
  adIds?: Array<string>;
  clickWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  engagementWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  viewWindowDays?: 0 | 1 | 7 | 14 | 30 | 60;
  conversionReportTime?: 'TIME_OF_AD_ACTION' | 'TIME_OF_CONVERSION';
  campaignIds?: Array<string>;
  reportingTimezone?: ReportingTimeZone;
}

/**
 * adsCreate - parameters interface
 */
export interface IAdsCreateParams {
  adAccountId: string;
  adCreate: Array<AdCreate>;
}

/**
 * adsGet - parameters interface
 */
export interface IAdsGetParams {
  adId: string;
  adAccountId: string;
}

/**
 * adsList - parameters interface
 */
export interface IAdsListParams {
  adAccountId: string;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
  campaignIds?: Array<string>;
  adGroupIds?: Array<string>;
  adIds?: Array<string>;
  entityStatuses?: Array<EntityStatus>;
}

/**
 * adsUpdate - parameters interface
 */
export interface IAdsUpdateParams {
  adAccountId: string;
  adBatchUpdate: Array<AdBatchUpdate>;
}

/**
 * campaignAdPreviewCreate - parameters interface
 */
export interface ICampaignAdPreviewCreateParams {
  adAccountId: string;
  campaignAdPreviewCreate: Array<CampaignAdPreviewCreate>;
}

/**
 * campaignAdPreviewDelete - parameters interface
 */
export interface ICampaignAdPreviewDeleteParams {
  adGroupIds: Array<string>;
  adAccountId: string;
}

/**
 * campaignAdPreviewRead - parameters interface
 */
export interface ICampaignAdPreviewReadParams {
  adGroupIds: Array<string>;
  adAccountId: string;
}

/**
 * AdsApi - API class
 */
@autoinject()
export class AdsApi extends Api {

  /**
   * Creates a new AdsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create ad preview with pin or image
   * Create an ad preview given an ad account ID and either an existing organic pin ID or the URL for an image to be used to create the Pin and the ad.  If you are creating a preview from an existing Pin, that Pin must be promotable: that is, it must have a clickthrough link and meet other requirements. (See [Ads Overview](https://help.pinterest.com/en/business/article/promoted-pins-overview).)  You can view the returned preview URL on a webpage or iframe for 7 days, after which the URL expires. Collection ads are not currently supported ad preview.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adPreviewRequest 
   */
  async adPreviewsCreate(params: IAdPreviewsCreateParams): Promise<AdPreviewURLResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('adPreviewsCreate', params, 'adAccountId');
    this.ensureParamIsSet('adPreviewsCreate', params, 'adPreviewRequest');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ad_previews`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adPreviewRequest'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get targeting analytics for ads
   * Get targeting analytics for one or more ads. For the requested ad(s) and metrics, the response will include the requested metric information (e.g. SPEND_IN_DOLLAR) for the requested target type (e.g. \&quot;age_bucket\&quot;) for applicable values (e.g. \&quot;45-49\&quot;).  * The token\&#39;s user_account must either be the Owner of the specified ad account, or have one of the necessary roles granted to them via [Business Access](https://help.pinterest.com/en/business/article/share-and-manage-access-to-your-ad-accounts): Admin, Analyst, Campaign Manager. * If granularity is not HOUR, you can pull data from up to 90 days before the current date in UTC time, with a maximum time range of 90 days. * If granularity is HOUR, you can pull data from up to 8 days before the current date in UTC time, with a maximum time range of 3 days.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adIds List of Ad Ids to use to filter the results.
   * @param params.startDate Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
   * @param params.endDate Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
   * @param params.targetingTypes Targeting type breakdowns for the report. The reporting per targeting type is independent from each other. [\&quot;AGE_BUCKET_AND_GENDER\&quot;] is in BETA and not yet available to all users.
   * @param params.columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile\&#39;s currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it\&#39;s microdollars. Otherwise, it\&#39;s in microunits of the advertiser\&#39;s currency.  For example, if the advertiser\&#39;s currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
   * @param params.granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
   * @param params.clickWindowDays Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days.
   * @param params.engagementWindowDays Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days.  **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**.
   * @param params.viewWindowDays Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;1&#x60; day.
   * @param params.conversionReportTime The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
   * @param params.attributionTypes List of types of attribution for the conversion report
   * @param params.reportingTimezone Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
   * @param params.sortColumns Sort Columns.
   * @param params.sortAscending Sort ascending.
   */
  async adTargetingAnalyticsGet(params: IAdTargetingAnalyticsGetParams): Promise<MetricsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'adAccountId');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'adIds');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'startDate');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'endDate');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'targetingTypes');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'columns');
    this.ensureParamIsSet('adTargetingAnalyticsGet', params, 'granularity');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads/targeting_analytics`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'ad_ids': params['adIds'],
        'start_date': params['startDate'],
        'end_date': params['endDate'],
        'targeting_types': params['targetingTypes'],
        'columns': params['columns'],
        'granularity': params['granularity'],
        'click_window_days': params['clickWindowDays'],
        'engagement_window_days': params['engagementWindowDays'],
        'view_window_days': params['viewWindowDays'],
        'conversion_report_time': params['conversionReportTime'],
        'attribution_types': params['attributionTypes'],
        'reporting_timezone': params['reportingTimezone'],
        'sort_columns': params['sortColumns'],
        'sort_ascending': params['sortAscending'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get ad analytics
   *     Get analytics for the specified ads in the specified &#x60;ad_account_id&#x60;, filtered by the specified options.     - The token\&#39;s user_account must either be the Owner of the specified ad account, or have one of the necessary roles granted to them via [Business Access](https://help.pinterest.com/en/business/article/share-and-manage-access-to-your-ad-accounts): Admin, Analyst, Campaign Manager.     - The request must contain either ad_ids or both campaign_ids and pin_ids.     - If granularity is not HOUR, you can pull data from up to 90 days before the current date in UTC time, with a maximum time range of 90 days.     - If granularity is HOUR, you can pull data from up to 8 days before the current date in UTC time, with a maximum time range of 3 days.
   * @param params.startDate Metric report start date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days back from today.
   * @param params.endDate Metric report end date (UTC). Format: YYYY-MM-DD. Cannot be more than 90 days past start_date.
   * @param params.columns Columns to retrieve, encoded as a comma-separated string. **NOTE**: Any metrics defined as MICRO_DOLLARS returns a value based on the advertiser profile\&#39;s currency field. For USD, ($1/1,000,000, or $0.000001 - one one-ten-thousandth of a cent). it\&#39;s microdollars. Otherwise, it\&#39;s in microunits of the advertiser\&#39;s currency.  For example, if the advertiser\&#39;s currency is GBP (British pound sterling), all MICRO_DOLLARS fields will be in GBP microunits (1/1,000,000 British pound).  If a column has no value, it may not be returned.
   * @param params.granularity   TOTAL - metrics are aggregated over the specified date range.    DAY - metrics are broken down daily.    HOUR - metrics are broken down hourly.    WEEK - metrics are broken down weekly.    MONTH - metrics are broken down monthly
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.pinIds List of Pin IDs.
   * @param params.adIds List of Ad Ids to use to filter the results.
   * @param params.clickWindowDays Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days.
   * @param params.engagementWindowDays Number of days to use as the conversion attribution window for an engagement action. Engagements include saves, closeups, link clicks, and carousel card swipes. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;30&#x60; days. **Note:** This parameter no longer returns new data. However, you can still access historic data through **Sept 30, 2027**.
   * @param params.viewWindowDays Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to &#x60;1&#x60; day.
   * @param params.conversionReportTime The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
   * @param params.campaignIds List of Campaign Ids to use to filter the results.
   * @param params.reportingTimezone Specify the timezone to be applied for the reporting. This feature is currently in BETA and is not available to all users.
   */
  async adsAnalytics(params: IAdsAnalyticsParams): Promise<Array<AdsAnalytics>> {
    // Verify required parameters are set
    this.ensureParamIsSet('adsAnalytics', params, 'startDate');
    this.ensureParamIsSet('adsAnalytics', params, 'endDate');
    this.ensureParamIsSet('adsAnalytics', params, 'columns');
    this.ensureParamIsSet('adsAnalytics', params, 'granularity');
    this.ensureParamIsSet('adsAnalytics', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads/analytics`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'pin_ids': params['pinIds'],
        'start_date': params['startDate'],
        'end_date': params['endDate'],
        'ad_ids': params['adIds'],
        'columns': params['columns'],
        'granularity': params['granularity'],
        'click_window_days': params['clickWindowDays'],
        'engagement_window_days': params['engagementWindowDays'],
        'view_window_days': params['viewWindowDays'],
        'conversion_report_time': params['conversionReportTime'],
        'campaign_ids': params['campaignIds'],
        'reporting_timezone': params['reportingTimezone'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create ads
   * Create multiple new ads. Request must contain ad_group_id, creative_type, and the source Pin pin_id.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adCreate 
   */
  async adsCreate(params: IAdsCreateParams): Promise<AdBatchWriteResponseModel> {
    // Verify required parameters are set
    this.ensureParamIsSet('adsCreate', params, 'adAccountId');
    this.ensureParamIsSet('adsCreate', params, 'adCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get ad
   * Get a specific ad given the ad ID. If your pin is rejected, rejected_reasons will contain additional information from the Ad Review process. For more information about our policies and rejection reasons see the [Pinterest advertising standards](https://www.pinterest.com/_/_/policy/advertising-guidelines/).
   * @param params.adId The ID of this ad.
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async adsGet(params: IAdsGetParams): Promise<Ad> {
    // Verify required parameters are set
    this.ensureParamIsSet('adsGet', params, 'adId');
    this.ensureParamIsSet('adsGet', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads/{ad_id}`
      .replace(`{${'ad_id'}}`, encodeURIComponent(`${params['adId']}`))
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List ads
   * List ads that meet the filters provided:     - Listed campaign ids or ad group ids or ad ids     - Listed entity statuses  If no filter is provided, all ads in the ad account are returned.  **Note:** Provide only &#x60;campaign_id&#x60; or &#x60;ad_group_id&#x60; or &#x60;ad_id&#x60;. Do not provide more than one type.  Review status is provided for each ad; if &#x60;review_status&#x60; is &#x60;REJECTED&#x60;, the &#x60;rejected_reasons&#x60; field will contain additional information.  For more, see [Pinterest advertising standards](https://policy.pinterest.com/en/advertising-guidelines).
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.campaignIds List of Campaign Ids to use to filter the results.
   * @param params.adGroupIds List of Ad group Ids to retrieve keywords from. This feature is currently in BETA and is not available to all users.
   * @param params.adIds List of Ad Ids to use to filter the results.
   * @param params.entityStatuses Entity status
   */
  async adsList(params: IAdsListParams): Promise<AdsList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('adsList', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'campaign_ids': params['campaignIds'],
        'ad_group_ids': params['adGroupIds'],
        'ad_ids': params['adIds'],
        'entity_statuses': params['entityStatuses'],
      })

      // Authentication 'pinterest_oauth2' required
      // Authentication 'client_credentials' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update ads
   * Update multiple existing ads
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.adBatchUpdate 
   */
  async adsUpdate(params: IAdsUpdateParams): Promise<AdBatchWriteResponseModel> {
    // Verify required parameters are set
    this.ensureParamIsSet('adsUpdate', params, 'adAccountId');
    this.ensureParamIsSet('adsUpdate', params, 'adBatchUpdate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/ads`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['adBatchUpdate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create ad preview records for one or more ad groups
   * Create ad preview records for one or more ad groups that can be shared. Each ad group is processed independently; individual failures do not block other previews.
   * @param params.adAccountId Unique identifier of an ad account.
   * @param params.campaignAdPreviewCreate 
   */
  async campaignAdPreviewCreate(params: ICampaignAdPreviewCreateParams): Promise<Array<CampaignAdPreviewCreate200ResponseInner>> {
    // Verify required parameters are set
    this.ensureParamIsSet('campaignAdPreviewCreate', params, 'adAccountId');
    this.ensureParamIsSet('campaignAdPreviewCreate', params, 'campaignAdPreviewCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/campaign_ad_preview`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['campaignAdPreviewCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete ad preview records for one or more ad groups
   * Delete ad preview records for one or more ad groups. All ad groups are validated before deleting any records.
   * @param params.adGroupIds List of Ad group Ids to use to filter the results.
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async campaignAdPreviewDelete(params: ICampaignAdPreviewDeleteParams): Promise<Array<CampaignAdPreviewDelete200ResponseInner>> {
    // Verify required parameters are set
    this.ensureParamIsSet('campaignAdPreviewDelete', params, 'adGroupIds');
    this.ensureParamIsSet('campaignAdPreviewDelete', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/campaign_ad_preview`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'ad_group_ids': params['adGroupIds'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Fetch ad preview records for one or more ad groups
   * Fetch ad preview records for one or more ad groups. Returns all active previews associated with the provided ad group IDs.
   * @param params.adGroupIds List of Ad group Ids to use to filter the results.
   * @param params.adAccountId Unique identifier of an ad account.
   */
  async campaignAdPreviewRead(params: ICampaignAdPreviewReadParams): Promise<Array<CampaignAdPreview>> {
    // Verify required parameters are set
    this.ensureParamIsSet('campaignAdPreviewRead', params, 'adGroupIds');
    this.ensureParamIsSet('campaignAdPreviewRead', params, 'adAccountId');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/campaign_ad_preview`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'ad_group_ids': params['adGroupIds'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

