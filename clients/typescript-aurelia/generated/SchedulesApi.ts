/**
 * Pinterest REST API
 * Pinterest\'s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ScheduleType,
  PinterestLibPaginationOrder,
  SchedulesCreate200ResponseInner,
  SchedulesList200Response,
} from './models';

/**
 * schedulesCreate - parameters interface
 */
export interface ISchedulesCreateParams {
  adAccountId: string;
  scheduleCreate: Array<ScheduleCreate>;
}

/**
 * schedulesList - parameters interface
 */
export interface ISchedulesListParams {
  adAccountId: string;
  entityIds: Array<string>;
  bookmark?: string;
  pageSize?: number;
  order?: PinterestLibPaginationOrder;
  scheduleStatuses?: Array<ScheduleStatus>;
  scheduleType?: ScheduleType;
}

/**
 * schedulesUpdate - parameters interface
 */
export interface ISchedulesUpdateParams {
  adAccountId: string;
  scheduleBatchUpdate: Array<ScheduleBatchUpdate>;
}

/**
 * SchedulesApi - API class
 */
@autoinject()
export class SchedulesApi extends Api {

  /**
   * Creates a new SchedulesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create schedules
   * Batch create schedules
   * @param params.adAccountId 
   * @param params.scheduleCreate 
   */
  async schedulesCreate(params: ISchedulesCreateParams): Promise<Array<SchedulesCreate200ResponseInner>> {
    // Verify required parameters are set
    this.ensureParamIsSet('schedulesCreate', params, 'adAccountId');
    this.ensureParamIsSet('schedulesCreate', params, 'scheduleCreate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/schedules`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['scheduleCreate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Schedules
   * Get schedules for a specific advertiser
   * @param params.adAccountId 
   * @param params.entityIds List of Entity IDs, must be associated with the Ad Accound ID provided in the path.
   * @param params.bookmark Cursor used to fetch the next page of items
   * @param params.pageSize Maximum number of items to include in a single page. See documentation on [Pagination](/docs/reference/pagination/) for more information.
   * @param params.order The order in which to sort the items returned: \&quot;ASCENDING\&quot; or \&quot;DESCENDING\&quot; by ID. Note that higher-value IDs are associated with more-recently added items.
   * @param params.scheduleStatuses Filter schedules by status (one or more)
   * @param params.scheduleType Filter schedules by a type
   */
  async schedulesList(params: ISchedulesListParams): Promise<SchedulesList200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('schedulesList', params, 'adAccountId');
    this.ensureParamIsSet('schedulesList', params, 'entityIds');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/schedules`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'bookmark': params['bookmark'],
        'page_size': params['pageSize'],
        'order': params['order'],
        'schedule_statuses': params['scheduleStatuses'],
        'schedule_type': params['scheduleType'],
        'entity_ids': params['entityIds'],
      })

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update schedules
   * Update one or more schedules
   * @param params.adAccountId 
   * @param params.scheduleBatchUpdate 
   */
  async schedulesUpdate(params: ISchedulesUpdateParams): Promise<Array<SchedulesCreate200ResponseInner>> {
    // Verify required parameters are set
    this.ensureParamIsSet('schedulesUpdate', params, 'adAccountId');
    this.ensureParamIsSet('schedulesUpdate', params, 'scheduleBatchUpdate');

    // Create URL to call
    const url = `${this.basePath}/ad_accounts/{ad_account_id}/schedules`
      .replace(`{${'ad_account_id'}}`, encodeURIComponent(`${params['adAccountId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['scheduleBatchUpdate'] || {}))

      // Authentication 'pinterest_oauth2' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

