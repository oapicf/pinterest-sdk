/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import java.time.OffsetDateTime
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LocalStoreData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LocalStoreData(
/* Primary address line of the store. */
  addressPrimary: String = "" ,
/* Secondary address line of the store. */
  addressSecondary: String = "" ,
/* City where the store is located. */
  city: String = "" ,
/* Country code where the store is located. */
  country: Country,
/* Creation timestamp */
  createdAt: OffsetDateTime,
/* The ID of the local store. */
  id: String,
/* Geographic latitude coordinate of the store. */
  latitude: Float = 0 ,
/* Geographic longitude coordinate of the store. */
  longitude: Float = 0 ,
/* The name of the local store. */
  name: String,
/* Postal or ZIP code of the store. */
  postalCode: String = "" ,
/* State or region code where the store is located. */
  region: String = "" ,
/* Merchant provided code for the local store. Unique within the merchant's catalog. */
  storeCode: String,
/* Last update timestamp */
  updatedAt: OffsetDateTime
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== addressPrimary validation ==================
        
        
        
        

        // ================== addressSecondary validation ==================
        
        
        
        

        // ================== city validation ==================
        
        
        
        

        // ================== country validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (country == null) {
            _allValidationErrors += ValidationError(path :+ LocalStore.Fields.country, "country is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== createdAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (createdAt == null) {
            _allValidationErrors += ValidationError(path :+ LocalStore.Fields.createdAt, "createdAt is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ LocalStore.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== latitude validation ==================
        
        
        
        

        // ================== longitude validation ==================
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== postalCode validation ==================
        
        
        
        

        // ================== region validation ==================
        
        
        
        

        // ================== storeCode validation ==================
        
        
        
        

        // ================== updatedAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (updatedAt == null) {
            _allValidationErrors += ValidationError(path :+ LocalStore.Fields.updatedAt, "updatedAt is a required field and cannot be null")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LocalStore] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LocalStore = {
    LocalStore(
        addressPrimary = Option(addressPrimary) /* 1 */,
        addressSecondary = Option(addressSecondary) /* 1 */,
        city = Option(city) /* 1 */,
        country = country /* 2 */,
        createdAt = createdAt /* 2 */,
        id = id /* 2 */,
        latitude = Option(latitude) /* 1 */,
        longitude = Option(longitude) /* 1 */,
        name = name /* 2 */,
        postalCode = Option(postalCode) /* 1 */,
        region = Option(region) /* 1 */,
        storeCode = storeCode /* 2 */,
        updatedAt = updatedAt /* 2 */
    
    )
  }
}

object LocalStoreData {

  def validated(d8a : LocalStoreData, failFast : Boolean) : scala.util.Try[LocalStore] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LocalStoreData = try {
        val data = read[LocalStoreData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LocalStoreData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LocalStoreData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LocalStoreData] = try {
        read[List[LocalStoreData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LocalStore]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LocalStore]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LocalStoreData] = try {
        read[Map[String, LocalStoreData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LocalStore]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LocalStore]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

