/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LeadsExportsCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LeadsExportsCreateData(
/* ID for the ad collecting leads. */
  adId: String,
/* Export leads collected on and before end date (UTC). Format: YYYY-MM-DD. */
  endDate: String,
/* Export leads collected on and after start date (UTC). Format: YYYY-MM-DD. */
  startDate: String
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adId == null || !regex.r.matches(adId) then
              _allValidationErrors += ValidationError(path :+ LeadsExportsCreate.Fields.adId, s"value '$adId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== endDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if endDate == null || !regex.r.matches(endDate) then
              _allValidationErrors += ValidationError(path :+ LeadsExportsCreate.Fields.endDate, s"value '$endDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== startDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if startDate == null || !regex.r.matches(startDate) then
              _allValidationErrors += ValidationError(path :+ LeadsExportsCreate.Fields.startDate, s"value '$startDate' doesn't match pattern $regex")
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LeadsExportsCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LeadsExportsCreate = {
    LeadsExportsCreate(
        adId = adId /* 2 */,
        endDate = endDate /* 2 */,
        startDate = startDate /* 2 */
    
    )
  }
}

object LeadsExportsCreateData {

  def validated(d8a : LeadsExportsCreateData, failFast : Boolean) : scala.util.Try[LeadsExportsCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LeadsExportsCreateData = try {
        val data = read[LeadsExportsCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LeadsExportsCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LeadsExportsCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LeadsExportsCreateData] = try {
        read[List[LeadsExportsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LeadsExportsCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LeadsExportsCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LeadsExportsCreateData] = try {
        read[Map[String, LeadsExportsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LeadsExportsCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LeadsExportsCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

