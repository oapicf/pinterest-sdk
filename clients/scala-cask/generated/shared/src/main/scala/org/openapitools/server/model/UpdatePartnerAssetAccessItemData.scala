/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** UpdatePartnerAssetAccessItemData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdatePartnerAssetAccessItemData(
/* Unique identifier of the business asset. */
  assetId: String,
/* Unique identifier of a business partner to update asset access to. */
  partnerId: String,
/* A non-empty array of permissions to assign to the partner. */
  permissions: Seq[Permissions]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== assetId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if assetId == null || !regex.r.matches(assetId) then
              _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.assetId, s"value '$assetId' doesn't match pattern $regex")
        }
        // validate max length 25
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 25) {
               _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.assetId, s"length $len is longer than the max length 25")
            }
        }
        
        
        
        

        // ================== partnerId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if partnerId == null || !regex.r.matches(partnerId) then
              _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.partnerId, s"value '$partnerId' doesn't match pattern $regex")
        }
        // validate max length 25
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if partnerId == null then 0 else partnerId.length
            if (len < 25) {
               _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.partnerId, s"length $len is longer than the max length 25")
            }
        }
        
        
        
        

        // ================== permissions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (permissions == null) {
            _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.permissions, "permissions is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if permissions == null then 0 else permissions.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.permissions, s"permissions has $len, which is less than the min items 1")
            }
        }
        // validate min items 50
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if permissions == null then 0 else permissions.size
            if (len > 50) {
               _allValidationErrors += ValidationError(path :+ UpdatePartnerAssetAccessItem.Fields.permissions, s"permissions has $len, which is greater than the max items 50")
            }
        }
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[UpdatePartnerAssetAccessItem] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdatePartnerAssetAccessItem = {
    UpdatePartnerAssetAccessItem(
        assetId = assetId /* 2 */,
        partnerId = partnerId /* 2 */,
        permissions = permissions.map(_.asModel) /* 5 */
    
    )
  }
}

object UpdatePartnerAssetAccessItemData {

  def validated(d8a : UpdatePartnerAssetAccessItemData, failFast : Boolean) : scala.util.Try[UpdatePartnerAssetAccessItem] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : UpdatePartnerAssetAccessItemData = try {
        val data = read[UpdatePartnerAssetAccessItemData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating UpdatePartnerAssetAccessItemData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : UpdatePartnerAssetAccessItemData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[UpdatePartnerAssetAccessItemData] = try {
        read[List[UpdatePartnerAssetAccessItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdatePartnerAssetAccessItem]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdatePartnerAssetAccessItem]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdatePartnerAssetAccessItemData] = try {
        read[Map[String, UpdatePartnerAssetAccessItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdatePartnerAssetAccessItem]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdatePartnerAssetAccessItem]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

