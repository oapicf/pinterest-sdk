/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** IntegrationLogClientErrorData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class IntegrationLogClientErrorData(
/* Original cause of the error. */
  cause: String = "" ,
/* Column number in the line of the file that raised the error. */
  columnNumber: Int = 0 ,
/* Filename where the error happened. */
  fileName: String = "" ,
/* Line number where the error happened. */
  lineNumber: Int = 0 ,
/* Human-readable description of the error. */
  message: String = "" ,
/* More detail about the message. */
  messageDetail: String = "" ,
/* Filename where the error happened. */
  name: String = "" ,
/* Integer that specifies the error code. */
  number: Int = 0 ,
/* Stack trace of where the error happened. */
  stackTrace: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== cause validation ==================
        // validate max length 512
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if cause == null then 0 else cause.length
            if (len < 512) {
               _allValidationErrors += ValidationError(path :+ IntegrationLogClientError.Fields.cause, s"length $len is longer than the max length 512")
            }
        }
        
        
        
        

        // ================== columnNumber validation ==================
        
        
        
        

        // ================== fileName validation ==================
        // validate max length 256
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if fileName == null then 0 else fileName.length
            if (len < 256) {
               _allValidationErrors += ValidationError(path :+ IntegrationLogClientError.Fields.fileName, s"length $len is longer than the max length 256")
            }
        }
        
        
        
        

        // ================== lineNumber validation ==================
        
        
        
        

        // ================== message validation ==================
        // validate max length 8192
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if message == null then 0 else message.length
            if (len < 8192) {
               _allValidationErrors += ValidationError(path :+ IntegrationLogClientError.Fields.message, s"length $len is longer than the max length 8192")
            }
        }
        
        
        
        

        // ================== messageDetail validation ==================
        // validate max length 1024
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if messageDetail == null then 0 else messageDetail.length
            if (len < 1024) {
               _allValidationErrors += ValidationError(path :+ IntegrationLogClientError.Fields.messageDetail, s"length $len is longer than the max length 1024")
            }
        }
        
        
        
        

        // ================== name validation ==================
        // validate max length 256
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 256) {
               _allValidationErrors += ValidationError(path :+ IntegrationLogClientError.Fields.name, s"length $len is longer than the max length 256")
            }
        }
        
        
        
        

        // ================== number validation ==================
        
        
        
        

        // ================== stackTrace validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[IntegrationLogClientError] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : IntegrationLogClientError = {
    IntegrationLogClientError(
        cause = Option(cause) /* 1 */,
        columnNumber = Option(columnNumber) /* 1 */,
        fileName = Option(fileName) /* 1 */,
        lineNumber = Option(lineNumber) /* 1 */,
        message = Option(message) /* 1 */,
        messageDetail = Option(messageDetail) /* 1 */,
        name = Option(name) /* 1 */,
        number = Option(number) /* 1 */,
        stackTrace = Option(stackTrace) /* 1 */
    
    )
  }
}

object IntegrationLogClientErrorData {

  def validated(d8a : IntegrationLogClientErrorData, failFast : Boolean) : scala.util.Try[IntegrationLogClientError] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : IntegrationLogClientErrorData = try {
        val data = read[IntegrationLogClientErrorData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating IntegrationLogClientErrorData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : IntegrationLogClientErrorData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[IntegrationLogClientErrorData] = try {
        read[List[IntegrationLogClientErrorData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[IntegrationLogClientError]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[IntegrationLogClientError]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, IntegrationLogClientErrorData] = try {
        read[Map[String, IntegrationLogClientErrorData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, IntegrationLogClientError]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, IntegrationLogClientError]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

