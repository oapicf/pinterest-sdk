/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** InviteResponseData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class InviteResponseData(
  assetsSummary: InviteAssetsSummaryData = null ,
/* The access level a user would be granted on the business if the invite/request is accepted. This can be EMPLOYEE, BIZ_ADMIN, or PARTNER. */
  businessRoles: Seq[String] = Nil ,
/* Metadata for the business that created the invite/request. */
  createdByBusiness: BusinessAccessUserSummaryData = null ,
/* Metadata for the user that created the invite/request. */
  createdByUser: BusinessAccessUserSummaryData = null ,
/* The time the invite/request was created. Returned in milliseconds. */
  createdTime: Int = 0 ,
/* Unique identifier of the invite/request. */
  id: String = "" ,
  inviteData: InviteDataResponseData = null ,
/* Indicates whether the invite/request was received. */
  isReceivedInvite: Boolean = false ,
/* Metadata for the member/partner that was sent the invite/request. */
  user: BusinessAccessUserSummaryData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== assetsSummary validation ==================
        
        
        
        
        // validating assetsSummary
        if (_allValidationErrors.isEmpty || !failFast) {
            if assetsSummary != null then _allValidationErrors ++= assetsSummary.validationErrors(path :+ InviteResponse.Fields.assetsSummary, failFast)
        }

        // ================== businessRoles validation ==================
        
        
        
        

        // ================== createdByBusiness validation ==================
        
        
        
        
        // validating createdByBusiness
        if (_allValidationErrors.isEmpty || !failFast) {
            if createdByBusiness != null then _allValidationErrors ++= createdByBusiness.validationErrors(path :+ InviteResponse.Fields.createdByBusiness, failFast)
        }

        // ================== createdByUser validation ==================
        
        
        
        
        // validating createdByUser
        if (_allValidationErrors.isEmpty || !failFast) {
            if createdByUser != null then _allValidationErrors ++= createdByUser.validationErrors(path :+ InviteResponse.Fields.createdByUser, failFast)
        }

        // ================== createdTime validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ InviteResponse.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== inviteData validation ==================
        
        
        
        
        // validating inviteData
        if (_allValidationErrors.isEmpty || !failFast) {
            if inviteData != null then _allValidationErrors ++= inviteData.validationErrors(path :+ InviteResponse.Fields.inviteData, failFast)
        }

        // ================== isReceivedInvite validation ==================
        
        
        
        

        // ================== user validation ==================
        
        
        
        
        // validating user
        if (_allValidationErrors.isEmpty || !failFast) {
            if user != null then _allValidationErrors ++= user.validationErrors(path :+ InviteResponse.Fields.user, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[InviteResponse] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : InviteResponse = {
    InviteResponse(
        assetsSummary = Option(assetsSummary).map(_.asModel) /* 4 */,
        businessRoles = businessRoles /* 2 */,
        createdByBusiness = Option(createdByBusiness).map(_.asModel) /* 4 */,
        createdByUser = Option(createdByUser).map(_.asModel) /* 4 */,
        createdTime = Option(createdTime) /* 1 */,
        id = Option(id) /* 1 */,
        inviteData = Option(inviteData).map(_.asModel) /* 4 */,
        isReceivedInvite = Option(isReceivedInvite) /* 1 */,
        user = Option(user).map(_.asModel) /* 4 */
    
    )
  }
}

object InviteResponseData {

  def validated(d8a : InviteResponseData, failFast : Boolean) : scala.util.Try[InviteResponse] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : InviteResponseData = try {
        val data = read[InviteResponseData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating InviteResponseData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : InviteResponseData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[InviteResponseData] = try {
        read[List[InviteResponseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[InviteResponse]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[InviteResponse]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, InviteResponseData] = try {
        read[Map[String, InviteResponseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, InviteResponse]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, InviteResponse]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

