/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AdvancedAuctionItemsSubmitRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AdvancedAuctionItemsSubmitRequestData(
/* Catalog id pertaining to all items */
  catalogId: String,
/* Array of item bid option operations */
  items: Seq[AdvancedAuctionItemsSubmitRecordData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== catalogId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if catalogId == null || !regex.r.matches(catalogId) then
              _allValidationErrors += ValidationError(path :+ AdvancedAuctionItemsSubmitRequest.Fields.catalogId, s"value '$catalogId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== items validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (items == null) {
            _allValidationErrors += ValidationError(path :+ AdvancedAuctionItemsSubmitRequest.Fields.items, "items is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if items == null then 0 else items.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdvancedAuctionItemsSubmitRequest.Fields.items, s"items has $len, which is less than the min items 1")
            }
        }
        // validate min items 10000
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if items == null then 0 else items.size
            if (len > 10000) {
               _allValidationErrors += ValidationError(path :+ AdvancedAuctionItemsSubmitRequest.Fields.items, s"items has $len, which is greater than the max items 10000")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (items != null) {
                items.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AdvancedAuctionItemsSubmitRequest.Fields.items :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AdvancedAuctionItemsSubmitRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AdvancedAuctionItemsSubmitRequest = {
    AdvancedAuctionItemsSubmitRequest(
        catalogId = catalogId /* 2 */,
        items = items.map(_.asModel) /* 5 */
    
    )
  }
}

object AdvancedAuctionItemsSubmitRequestData {

  def validated(d8a : AdvancedAuctionItemsSubmitRequestData, failFast : Boolean) : scala.util.Try[AdvancedAuctionItemsSubmitRequest] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AdvancedAuctionItemsSubmitRequestData = try {
        val data = read[AdvancedAuctionItemsSubmitRequestData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AdvancedAuctionItemsSubmitRequestData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AdvancedAuctionItemsSubmitRequestData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AdvancedAuctionItemsSubmitRequestData] = try {
        read[List[AdvancedAuctionItemsSubmitRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AdvancedAuctionItemsSubmitRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AdvancedAuctionItemsSubmitRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AdvancedAuctionItemsSubmitRequestData] = try {
        read[Map[String, AdvancedAuctionItemsSubmitRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AdvancedAuctionItemsSubmitRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AdvancedAuctionItemsSubmitRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

