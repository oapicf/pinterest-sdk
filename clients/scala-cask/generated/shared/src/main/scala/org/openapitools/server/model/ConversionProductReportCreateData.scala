/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ConversionProductReportCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ConversionProductReportCreateData(
/*   List of ad group ids.   Only support ad_group_ids field when level of the report is AD_GROUP. */
  adGroupIds: Seq[String] = Nil ,
/*   List of campaign ids.   Only support campaign_ids field when level of the report is CAMPAIGN. */
  campaignIds: Seq[String] = Nil ,
/* List of values for filtering. Default is ['CONSIDERATION','AWARENESS','WEB_CONVERSION','VIDEO_COMPLETION']. */
  campaignObjectiveTypes: Seq[ConversionObjectiveType] = Nil ,
/* Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days. */
  clickWindowDays: ConversionAttributionWindowDays = 30 ,
/* Metric and entity columns */
  columns: Seq[ConversionProductReportingColumn],
/*   Required attribution type of the B/C/S report.   When the attribution type is BRAND_ATTRIBUTION, start_date for the report must be after 2025-04-01. */
  conversionProductAttributionType: ConversionProductAttributionType = DEFAULT ,
/* Report breakdown type. This is used to specify the breakdown of the report by brand, category, or SKU. */
  conversionProductBreakdown: ConversionProductReportBreakdownType = PRODUCT_BRAND ,
/* The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event. */
  conversionReportTime: ConversionReportTimeType = TIME_OF_AD_ACTION ,
/*   Metric report end date (UTC). Format: YYYY-MM-DD.   A max of 1 year is allowed between the start and end date for reports. */
  endDate: String,
/* Report granularity for time-based metric aggregation */
  granularity: ConversionProductReportGranularity,
/* Level of the report */
  level: ConversionProductReportLevel,
/*   List of SKU ids.   Only support product_sku_ids field when report breakdown type is PRODUCT_SKU_GROUP. */
  productSkuIds: Seq[String] = Nil ,
/* Name of the conversion product report */
  reportName: String,
/*   Metric report start date (UTC). Format: YYYY-MM-DD.   Start date must be after 2024-03-16. 7 day minimum time window for report is required. */
  startDate: String,
/* Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `1` day. */
  viewWindowDays: ConversionAttributionWindowDays = 30 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adGroupIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupIds == null then 0 else adGroupIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.adGroupIds, s"adGroupIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupIds == null then 0 else adGroupIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.adGroupIds, s"adGroupIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== campaignIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.campaignIds, s"campaignIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.campaignIds, s"campaignIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== campaignObjectiveTypes validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignObjectiveTypes == null then 0 else campaignObjectiveTypes.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.campaignObjectiveTypes, s"campaignObjectiveTypes has $len, which is less than the min items 1")
            }
        }
        // validate min items 7
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignObjectiveTypes == null then 0 else campaignObjectiveTypes.size
            if (len > 7) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.campaignObjectiveTypes, s"campaignObjectiveTypes has $len, which is greater than the max items 7")
            }
        }
        
        
        

        // ================== clickWindowDays validation ==================
        
        
        
        

        // ================== columns validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (columns == null) {
            _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.columns, "columns is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== conversionProductAttributionType validation ==================
        
        
        
        

        // ================== conversionProductBreakdown validation ==================
        
        
        
        

        // ================== conversionReportTime validation ==================
        
        
        
        

        // ================== endDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if endDate == null || !regex.r.matches(endDate) then
              _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.endDate, s"value '$endDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== granularity validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (granularity == null) {
            _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.granularity, "granularity is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== level validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (level == null) {
            _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.level, "level is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== productSkuIds validation ==================
        
        // validate min items 2
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productSkuIds == null then 0 else productSkuIds.size
            if (len < 2) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.productSkuIds, s"productSkuIds has $len, which is less than the min items 2")
            }
        }
        // validate min items 30000
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productSkuIds == null then 0 else productSkuIds.size
            if (len > 30000) {
               _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.productSkuIds, s"productSkuIds has $len, which is greater than the max items 30000")
            }
        }
        
        
        

        // ================== reportName validation ==================
        
        
        
        

        // ================== startDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if startDate == null || !regex.r.matches(startDate) then
              _allValidationErrors += ValidationError(path :+ ConversionProductReportCreate.Fields.startDate, s"value '$startDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== viewWindowDays validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ConversionProductReportCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ConversionProductReportCreate = {
    ConversionProductReportCreate(
        adGroupIds = adGroupIds /* 2 */,
        campaignIds = campaignIds /* 2 */,
        campaignObjectiveTypes = campaignObjectiveTypes.map(_.asModel) /* 5 */,
        clickWindowDays = Option(clickWindowDays) /* 1 */,
        columns = columns.map(_.asModel) /* 5 */,
        conversionProductAttributionType = Option(conversionProductAttributionType) /* 1 */,
        conversionProductBreakdown = Option(conversionProductBreakdown) /* 1 */,
        conversionReportTime = Option(conversionReportTime) /* 1 */,
        endDate = endDate /* 2 */,
        granularity = granularity /* 2 */,
        level = level /* 2 */,
        productSkuIds = productSkuIds /* 2 */,
        reportName = reportName /* 2 */,
        startDate = startDate /* 2 */,
        viewWindowDays = Option(viewWindowDays) /* 1 */
    
    )
  }
}

object ConversionProductReportCreateData {

  def validated(d8a : ConversionProductReportCreateData, failFast : Boolean) : scala.util.Try[ConversionProductReportCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ConversionProductReportCreateData = try {
        val data = read[ConversionProductReportCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ConversionProductReportCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ConversionProductReportCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ConversionProductReportCreateData] = try {
        read[List[ConversionProductReportCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ConversionProductReportCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ConversionProductReportCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ConversionProductReportCreateData] = try {
        read[Map[String, ConversionProductReportCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ConversionProductReportCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ConversionProductReportCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

