/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using model.mustache
package org.openapitools.server.model

import scala.util.control.NonFatal

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        
case class LeadForm(
        /* The Ad Account ID that this lead form belongs to. */
    adAccountId: Option[String] = None ,
        /* A message for people who complete the form to let them know what happens next. */
    completionMessage: String,
        /* Lead form creation time. Unix timestamp in seconds. */
    createdTime: Option[Int] = None ,
        /* Additional disclosure language to be included in the lead form. */
    disclosureLanguage: Option[String] = None ,
        /* Whether the advertiser has accepted Pinterest's terms of service for creating a lead ad.  By sending us TRUE for this parameter, you agree that (i) you will use any personal information received in compliance with the privacy policy you share with Pinterest, and (ii) you will comply with Pinterest's [Lead Ad Terms](https://policy.pinterest.com/en/lead-ad-terms). As a reminder, all advertising on Pinterest is subject to the [Pinterest Advertising Services Agreement](https://business.pinterest.com/en/pinterest-advertising-services-agreement/) or an equivalent agreement as set forth on an IO */
    hasAcceptedTerms: Boolean,
        /* The ID of this lead form */
    id: String,
        /* Internal name of the lead form. */
    name: String,
        /* List of additional policy links to be displayed on the lead form. */
    policyLinks: Seq[LeadFormPolicyLink] = Nil ,
        /* A link to the advertiser's privacy policy. This will be included in the lead form's disclosure language. */
    privacyPolicyLink: String,
        /* List of questions to be displayed on the lead form. */
    questions: Seq[LeadFormQuestion],
    status: Option[LeadFormStatus] = None ,
        /* Last update time. Unix timestamp in seconds. */
    updatedTime: Option[Int] = None 


) {

def asJsonString: String = asData.asJsonString
def asJson: ujson.Value = asData.asJson

def asData : LeadFormData = {
LeadFormData(
    adAccountId = adAccountId.getOrElse("") /*  1 */,
    completionMessage = completionMessage /* 2 */,
    createdTime = createdTime.getOrElse(0) /*  1 */,
    disclosureLanguage = disclosureLanguage.getOrElse("") /*  1 */,
    hasAcceptedTerms = hasAcceptedTerms /* 2 */,
    id = id /* 2 */,
    name = name /* 2 */,
    policyLinks = policyLinks.map(_.asData) /* 6 */,
    privacyPolicyLink = privacyPolicyLink /* 2 */,
    questions = questions.map(_.asData) /* 6 */,
    status = status.getOrElse(null) /*  1 */,
    updatedTime = updatedTime.getOrElse(0) /*  1 */

)
}
}

object LeadForm {
given RW[LeadForm] = summon[RW[ujson.Value]].bimap[LeadForm](_.asJson, json => read[LeadFormData](json).asModel)

enum Fields(val fieldName : String) extends Field(fieldName) {
    case adAccountId extends Fields("adAccountId")
    case completionMessage extends Fields("completionMessage")
    case createdTime extends Fields("createdTime")
    case disclosureLanguage extends Fields("disclosureLanguage")
    case hasAcceptedTerms extends Fields("hasAcceptedTerms")
    case id extends Fields("id")
    case name extends Fields("name")
    case policyLinks extends Fields("policyLinks")
    case privacyPolicyLink extends Fields("privacyPolicyLink")
    case questions extends Fields("questions")
    case status extends Fields("status")
    case updatedTime extends Fields("updatedTime")
}


}


