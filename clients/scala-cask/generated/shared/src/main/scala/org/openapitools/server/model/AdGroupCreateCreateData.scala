/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.math.BigDecimal
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AdGroupCreateCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AdGroupCreateCreateData(
/* Enable auto-targeting for ad group. Default value is True. Also known as [\"Performance+ targeting\"](https://help.pinterest.com/en/business/article/performance-plus-targeting). */
  autoTargetingEnabled: Boolean = false ,
/* Bid price in micro currency. This field is **REQUIRED** for the following campaign objective_type/billable_event combinations: AWARENESS/IMPRESSION, CONSIDERATION/CLICKTHROUGH, CATALOG_SALES/CLICKTHROUGH, VIDEO_VIEW/VIDEO_V_50_MRC. */
  bidInMicroCurrency: Int = 0 ,
/* [Open beta](https://help.pinterest.com/en/business/article/beta-features) Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Not currently supported for [Performance+ campaigns](https://help.pinterest.com/en/business/article/performance-plus-campaigns). */
  bidMultiplier: BigDecimal = 0 ,
  bidStrategyType: BidStrategyType = null ,
  billableEvent: ActionType,
/* Budget in micro currency. This field is **REQUIRED** for non-CBO (campaign budget optimization) campaigns.  A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. A CBO campaign is limited to 70 or less ad groups. */
  budgetInMicroCurrency: Int = 0 ,
  budgetType: BudgetType = DAILY ,
/* Campaign ID of the ad group. */
  campaignId: String,
/* Customer segment ID applied to the ad group. We currently only support 1 customer segment per ad group. To use customer segments, do not set the `targeting_template_ids` field as well as the `AUDIENCE_INCLUDE` field of the `targeting_spec`. To clear the customer segment ID, set this field to '0'. */
  customerSegmentId: String = "" ,
/* Timestamp in Unix format for scheduling when ads in the ad group stop appearing. If not specified, ads run indefinitely unless you update the ad group by changing their status to `paused`. Cannot occur after `end_time` for parent campaign (if specified). Learn about [scheduling ads](https://help.pinterest.com/en/business/article/ads-manager-schedule-ads).  For certain organizations ([Closed beta](https://help.pinterest.com/en/business/article/beta-features)): Supported for campaigns with Campaign Budget Optimization (CBO).  For all organizations: Supported for campaigns without CBO. */
  endTime: Int = 0 ,
  extFeatures: AdgroupTrackingFeaturesData = null ,
/* Feed Profile ID associated to the adgroup. */
  feedProfileId: String = "" ,
/* Enable creative optimization for the ad group, default value is FALSE. When enabled, you allow Pinterest to automatically turn your product Pins into ads in different formats (collections and shopping) and deliver those ads to users at scale. */
  isCreativeOptimization: Boolean = false ,
/* Indicates whether the ad group should use the local inventory. */
  isLocalInventory: Boolean = false ,
/* Set a limit to the number of times a promoted pin from this campaign can be impressed by a pinner within the past rolling 30 days. Only available for CPM (cost per mille (1000 impressions))  ad groups. A CPM ad group has an IMPRESSION [billable_event](https://help.pinterest.com/en/business/article/billable-events) value. This field **REQUIRES** the `end_time` field. */
  lifetimeFrequencyCap: Int = 0 ,
/* The targeting radius of the local inventory ads in miles. */
  localInventoryRadiusInMiles: BigDecimal = 0 ,
/* Ad group name. */
  name: String,
/* Optimization goals for objective-based performance campaigns. **REQUIRED** when campaign's `objective_type` is set to `\"WEB_CONVERSION\"`. */
  optimizationGoalMetadata: NullableOptimizationGoalMetadataData = null ,
  pacingDeliveryType: PacingDeliveryType = STANDARD ,
/* Pinterest Performance+ campaign settings. */
  performancePlusCampaignSettings: PerformancePlusCampaignSettingsData = null ,
/* [Placement group](https://help.pinterest.com/en/business/article/placement-groups). */
  placementGroup: PlacementGroupType = null ,
/* A targeting option that enables advertisers to choose whether to run ads in fullscreen feed, two column feed, or both */
  placementTrafficType: PlacementTrafficType = null ,
/* Specify if the promotion is applied at ad group or item level */
  promotionApplicationLevel: PromotionApplicationLevel = null ,
/* Promotion ID. To clear this field, set to null. */
  promotionId: String = "" ,
/* Promotion IDs list. To clear this field, set to an empty array []. */
  promotionIds: Seq[String] = Nil ,
/* Timestamp in Unix format for scheduling when ads in the ad group start to appear. If not specified, ads appear during parent campaign's `start_time`. Cannot precede `start_time` for parent campaign (if specified). Learn about [scheduling ads](/docs/api-features/managing-ads/#step-2-create-an-ad-group). For certain organizations ([Closed beta](/docs/getting-started/using-beta-and-restricted-features/)): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO. */
  startTime: Int = 0 ,
/* Ad group/entity status. */
  status: EntityStatus = null ,
  targetingSpec: TargetingSpecOptimalData = null ,
/* Targeting template IDs applied to the ad group. We currently only support 1 targeting template per ad group. To use targeting templates, do not set any other targeting fields: targeting_spec, tracking_urls, auto_targeting_enabled, placement_group. To clear all targeting template IDs, set this field to ['0']. */
  targetingTemplateIds: Seq[String] = Nil ,
  trackingUrls: AdGroupTrackingURLsData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== autoTargetingEnabled validation ==================
        
        
        
        

        // ================== bidInMicroCurrency validation ==================
        
        
        
        

        // ================== bidMultiplier validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(bidMultiplier >= 0) then
            _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.bidMultiplier, s"value '$bidMultiplier' is not greater than the minimum value 0")
        }
        // validate against maximum 10
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(bidMultiplier <= 10) then
            _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.bidMultiplier, s"value '$bidMultiplier' is not greater than the maximum value 10")
        }
        
        
        
        

        // ================== bidStrategyType validation ==================
        
        
        
        

        // ================== billableEvent validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (billableEvent == null) {
            _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.billableEvent, "billableEvent is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== budgetInMicroCurrency validation ==================
        
        
        
        

        // ================== budgetType validation ==================
        
        
        
        

        // ================== campaignId validation ==================
        // validate against pattern '^[C]?\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^[C]?\\d+$"""
           if campaignId == null || !regex.r.matches(campaignId) then
              _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.campaignId, s"value '$campaignId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== customerSegmentId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if customerSegmentId == null || !regex.r.matches(customerSegmentId) then
              _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.customerSegmentId, s"value '$customerSegmentId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== endTime validation ==================
        
        
        
        

        // ================== extFeatures validation ==================
        
        
        
        
        // validating extFeatures
        if (_allValidationErrors.isEmpty || !failFast) {
            if extFeatures != null then _allValidationErrors ++= extFeatures.validationErrors(path :+ AdGroupCreateCreate.Fields.extFeatures, failFast)
        }

        // ================== feedProfileId validation ==================
        
        
        
        

        // ================== isCreativeOptimization validation ==================
        
        
        
        

        // ================== isLocalInventory validation ==================
        
        
        
        

        // ================== lifetimeFrequencyCap validation ==================
        
        
        
        

        // ================== localInventoryRadiusInMiles validation ==================
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== optimizationGoalMetadata validation ==================
        
        
        
        
        // validating optimizationGoalMetadata
        if (_allValidationErrors.isEmpty || !failFast) {
            if optimizationGoalMetadata != null then _allValidationErrors ++= optimizationGoalMetadata.validationErrors(path :+ AdGroupCreateCreate.Fields.optimizationGoalMetadata, failFast)
        }

        // ================== pacingDeliveryType validation ==================
        
        
        
        

        // ================== performancePlusCampaignSettings validation ==================
        
        
        
        
        // validating performancePlusCampaignSettings
        if (_allValidationErrors.isEmpty || !failFast) {
            if performancePlusCampaignSettings != null then _allValidationErrors ++= performancePlusCampaignSettings.validationErrors(path :+ AdGroupCreateCreate.Fields.performancePlusCampaignSettings, failFast)
        }

        // ================== placementGroup validation ==================
        
        
        
        

        // ================== placementTrafficType validation ==================
        
        
        
        

        // ================== promotionApplicationLevel validation ==================
        
        
        
        

        // ================== promotionId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if promotionId == null || !regex.r.matches(promotionId) then
              _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.promotionId, s"value '$promotionId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== promotionIds validation ==================
        
        
        
        

        // ================== startTime validation ==================
        
        
        
        

        // ================== status validation ==================
        
        
        
        

        // ================== targetingSpec validation ==================
        
        
        
        
        // validating targetingSpec
        if (_allValidationErrors.isEmpty || !failFast) {
            if targetingSpec != null then _allValidationErrors ++= targetingSpec.validationErrors(path :+ AdGroupCreateCreate.Fields.targetingSpec, failFast)
        }

        // ================== targetingTemplateIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if targetingTemplateIds == null then 0 else targetingTemplateIds.size
            if (len > 1) {
               _allValidationErrors += ValidationError(path :+ AdGroupCreateCreate.Fields.targetingTemplateIds, s"targetingTemplateIds has $len, which is greater than the max items 1")
            }
        }
        
        
        

        // ================== trackingUrls validation ==================
        
        
        
        
        // validating trackingUrls
        if (_allValidationErrors.isEmpty || !failFast) {
            if trackingUrls != null then _allValidationErrors ++= trackingUrls.validationErrors(path :+ AdGroupCreateCreate.Fields.trackingUrls, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AdGroupCreateCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AdGroupCreateCreate = {
    AdGroupCreateCreate(
        autoTargetingEnabled = Option(autoTargetingEnabled) /* 1 */,
        bidInMicroCurrency = Option(bidInMicroCurrency) /* 1 */,
        bidMultiplier = Option(bidMultiplier) /* 1 */,
        bidStrategyType = Option(bidStrategyType) /* 1 */,
        billableEvent = billableEvent /* 2 */,
        budgetInMicroCurrency = Option(budgetInMicroCurrency) /* 1 */,
        budgetType = Option(budgetType) /* 1 */,
        campaignId = campaignId /* 2 */,
        customerSegmentId = Option(customerSegmentId) /* 1 */,
        endTime = Option(endTime) /* 1 */,
        extFeatures = Option(extFeatures).map(_.asModel) /* 4 */,
        feedProfileId = Option(feedProfileId) /* 1 */,
        isCreativeOptimization = Option(isCreativeOptimization) /* 1 */,
        isLocalInventory = Option(isLocalInventory) /* 1 */,
        lifetimeFrequencyCap = Option(lifetimeFrequencyCap) /* 1 */,
        localInventoryRadiusInMiles = Option(localInventoryRadiusInMiles) /* 1 */,
        name = name /* 2 */,
        optimizationGoalMetadata = Option(optimizationGoalMetadata).map(_.asModel) /* 4 */,
        pacingDeliveryType = Option(pacingDeliveryType) /* 1 */,
        performancePlusCampaignSettings = Option(performancePlusCampaignSettings).map(_.asModel) /* 4 */,
        placementGroup = Option(placementGroup) /* 1 */,
        placementTrafficType = Option(placementTrafficType) /* 1 */,
        promotionApplicationLevel = Option(promotionApplicationLevel) /* 1 */,
        promotionId = Option(promotionId) /* 1 */,
        promotionIds = promotionIds /* 2 */,
        startTime = Option(startTime) /* 1 */,
        status = Option(status) /* 1 */,
        targetingSpec = Option(targetingSpec).map(_.asModel) /* 4 */,
        targetingTemplateIds = targetingTemplateIds /* 2 */,
        trackingUrls = Option(trackingUrls).map(_.asModel) /* 4 */
    
    )
  }
}

object AdGroupCreateCreateData {

  def validated(d8a : AdGroupCreateCreateData, failFast : Boolean) : scala.util.Try[AdGroupCreateCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AdGroupCreateCreateData = try {
        val data = read[AdGroupCreateCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AdGroupCreateCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AdGroupCreateCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AdGroupCreateCreateData] = try {
        read[List[AdGroupCreateCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AdGroupCreateCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AdGroupCreateCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AdGroupCreateCreateData] = try {
        read[Map[String, AdGroupCreateCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AdGroupCreateCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AdGroupCreateCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

