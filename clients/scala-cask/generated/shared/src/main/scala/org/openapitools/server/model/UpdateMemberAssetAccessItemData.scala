/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** UpdateMemberAssetAccessItemData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdateMemberAssetAccessItemData(
/* Id of the asset to update. */
  assetId: String,
/* Unique identifier of the member on which to perform the update */
  memberId: String,
/* A non-empty array of permissions to assign to the member. */
  permissions: Seq[Permissions]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== assetId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if assetId == null || !regex.r.matches(assetId) then
              _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.assetId, s"value '$assetId' doesn't match pattern $regex")
        }
        // validate max length 25
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 25) {
               _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.assetId, s"length $len is longer than the max length 25")
            }
        }
        
        
        
        

        // ================== memberId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if memberId == null || !regex.r.matches(memberId) then
              _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.memberId, s"value '$memberId' doesn't match pattern $regex")
        }
        // validate max length 25
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if memberId == null then 0 else memberId.length
            if (len < 25) {
               _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.memberId, s"length $len is longer than the max length 25")
            }
        }
        
        
        
        

        // ================== permissions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (permissions == null) {
            _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.permissions, "permissions is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if permissions == null then 0 else permissions.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.permissions, s"permissions has $len, which is less than the min items 1")
            }
        }
        // validate min items 50
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if permissions == null then 0 else permissions.size
            if (len > 50) {
               _allValidationErrors += ValidationError(path :+ UpdateMemberAssetAccessItem.Fields.permissions, s"permissions has $len, which is greater than the max items 50")
            }
        }
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[UpdateMemberAssetAccessItem] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdateMemberAssetAccessItem = {
    UpdateMemberAssetAccessItem(
        assetId = assetId /* 2 */,
        memberId = memberId /* 2 */,
        permissions = permissions.map(_.asModel) /* 5 */
    
    )
  }
}

object UpdateMemberAssetAccessItemData {

  def validated(d8a : UpdateMemberAssetAccessItemData, failFast : Boolean) : scala.util.Try[UpdateMemberAssetAccessItem] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : UpdateMemberAssetAccessItemData = try {
        val data = read[UpdateMemberAssetAccessItemData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating UpdateMemberAssetAccessItemData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : UpdateMemberAssetAccessItemData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[UpdateMemberAssetAccessItemData] = try {
        read[List[UpdateMemberAssetAccessItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdateMemberAssetAccessItem]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdateMemberAssetAccessItem]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdateMemberAssetAccessItemData] = try {
        read[Map[String, UpdateMemberAssetAccessItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdateMemberAssetAccessItem]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdateMemberAssetAccessItem]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

