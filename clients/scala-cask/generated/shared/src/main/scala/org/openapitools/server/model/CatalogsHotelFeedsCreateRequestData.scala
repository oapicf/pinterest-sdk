/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CatalogsHotelFeedsCreateRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CatalogsHotelFeedsCreateRequestData(
/* Catalog id pertaining to the feed. If not provided, feed will use a default catalog based on type. */
  catalogId: String = "" ,
  catalogType: CatalogsHotelFeedsCreateRequest.CatalogTypeEnum,
  credentials: CatalogsFeedCredentialsData = null ,
  defaultCurrency: NullableCurrency = null ,
  defaultLocale: CatalogsCreativeAssetsFeedsCreateRequestDefaultLocaleData,
  format: CatalogsFormat,
/* The URL where a feed is available for download. This URL is what Pinterest will use to download a feed for processing. */
  location: String,
/* A human-friendly name associated to a given feed. */
  name: String,
  preferredProcessingSchedule: CatalogsFeedProcessingScheduleData = null ,
  status: CatalogsStatus = ACTIVE 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== catalogId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if catalogId == null || !regex.r.matches(catalogId) then
              _allValidationErrors += ValidationError(path :+ CatalogsHotelFeedsCreateRequest.Fields.catalogId, s"value '$catalogId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== catalogType validation ==================
        
        
        
        

        // ================== credentials validation ==================
        
        
        
        
        // validating credentials
        if (_allValidationErrors.isEmpty || !failFast) {
            if credentials != null then _allValidationErrors ++= credentials.validationErrors(path :+ CatalogsHotelFeedsCreateRequest.Fields.credentials, failFast)
        }

        // ================== defaultCurrency validation ==================
        
        
        
        

        // ================== defaultLocale validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (defaultLocale == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsHotelFeedsCreateRequest.Fields.defaultLocale, "defaultLocale is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating defaultLocale
        if (_allValidationErrors.isEmpty || !failFast) {
            if defaultLocale != null then _allValidationErrors ++= defaultLocale.validationErrors(path :+ CatalogsHotelFeedsCreateRequest.Fields.defaultLocale, failFast)
        }

        // ================== format validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (format == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsHotelFeedsCreateRequest.Fields.format, "format is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== location validation ==================
        // validate against pattern '^(http|https|ftp|sftp):\/\/'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^(http|https|ftp|sftp):\/\/"""
           if location == null || !regex.r.matches(location) then
              _allValidationErrors += ValidationError(path :+ CatalogsHotelFeedsCreateRequest.Fields.location, s"value '$location' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== preferredProcessingSchedule validation ==================
        
        
        
        
        // validating preferredProcessingSchedule
        if (_allValidationErrors.isEmpty || !failFast) {
            if preferredProcessingSchedule != null then _allValidationErrors ++= preferredProcessingSchedule.validationErrors(path :+ CatalogsHotelFeedsCreateRequest.Fields.preferredProcessingSchedule, failFast)
        }

        // ================== status validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CatalogsHotelFeedsCreateRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CatalogsHotelFeedsCreateRequest = {
    CatalogsHotelFeedsCreateRequest(
        catalogId = Option(catalogId) /* 1 */,
        catalogType = catalogType /* 2 */,
        credentials = Option(credentials).map(_.asModel) /* 4 */,
        defaultCurrency = Option(defaultCurrency) /* 1 */,
        defaultLocale = defaultLocale.asModel /* 6 */,
        format = format /* 2 */,
        location = location /* 2 */,
        name = name /* 2 */,
        preferredProcessingSchedule = Option(preferredProcessingSchedule).map(_.asModel) /* 4 */,
        status = Option(status) /* 1 */
    
    )
  }
}

object CatalogsHotelFeedsCreateRequestData {

  def validated(d8a : CatalogsHotelFeedsCreateRequestData, failFast : Boolean) : scala.util.Try[CatalogsHotelFeedsCreateRequest] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CatalogsHotelFeedsCreateRequestData = try {
        val data = read[CatalogsHotelFeedsCreateRequestData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CatalogsHotelFeedsCreateRequestData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CatalogsHotelFeedsCreateRequestData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CatalogsHotelFeedsCreateRequestData] = try {
        read[List[CatalogsHotelFeedsCreateRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CatalogsHotelFeedsCreateRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CatalogsHotelFeedsCreateRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CatalogsHotelFeedsCreateRequestData] = try {
        read[Map[String, CatalogsHotelFeedsCreateRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CatalogsHotelFeedsCreateRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CatalogsHotelFeedsCreateRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

