/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** TargetingTemplateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class TargetingTemplateData(
/* The ID of the advertiser that this targeting template belongs to. */
  adAccountId: String = "" ,
/* Enable auto-targeting for ad group. Also known as [\"expanded targeting\"](https://help.pinterest.com/en/business/article/expanded-targeting). */
  autoTargetingEnabled: Boolean = false ,
/* Targeting template created time. Unix timestamp in seconds. */
  createdTime: Int = 0 ,
/* Targeting template ID. */
  id: String = "" ,
  keywords: Seq[TargetingTemplateKeywordData] = Nil ,
/* targeting template name */
  name: String,
  placementGroup: PlacementGroupType = null ,
  sizing: TargetingTemplateAudienceSizingData = null ,
/* Indicate targeting template is active or Deleted */
  status: TargetingTemplateStatus = null ,
/* targeting profile attributes */
  targetingAttributes: TargetingSpecOptimalData,
  trackingUrls: TrackingUrlsData = null ,
/* Targeting template updated time.Unix timestamp in seconds. */
  updatedTime: Int = 0 ,
/* Inform if the targeting template is valid (ex. would be false if has revoked audience) */
  valid: Boolean = false 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ TargetingTemplate.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== autoTargetingEnabled validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ TargetingTemplate.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== keywords validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (keywords != null) {
                keywords.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TargetingTemplate.Fields.keywords :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== name validation ==================
        
        
        
        

        // ================== placementGroup validation ==================
        
        
        
        

        // ================== sizing validation ==================
        
        
        
        
        // validating sizing
        if (_allValidationErrors.isEmpty || !failFast) {
            if sizing != null then _allValidationErrors ++= sizing.validationErrors(path :+ TargetingTemplate.Fields.sizing, failFast)
        }

        // ================== status validation ==================
        
        
        
        

        // ================== targetingAttributes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (targetingAttributes == null) {
            _allValidationErrors += ValidationError(path :+ TargetingTemplate.Fields.targetingAttributes, "targetingAttributes is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating targetingAttributes
        if (_allValidationErrors.isEmpty || !failFast) {
            if targetingAttributes != null then _allValidationErrors ++= targetingAttributes.validationErrors(path :+ TargetingTemplate.Fields.targetingAttributes, failFast)
        }

        // ================== trackingUrls validation ==================
        
        
        
        
        // validating trackingUrls
        if (_allValidationErrors.isEmpty || !failFast) {
            if trackingUrls != null then _allValidationErrors ++= trackingUrls.validationErrors(path :+ TargetingTemplate.Fields.trackingUrls, failFast)
        }

        // ================== updatedTime validation ==================
        
        
        
        

        // ================== valid validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[TargetingTemplate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : TargetingTemplate = {
    TargetingTemplate(
        adAccountId = Option(adAccountId) /* 1 */,
        autoTargetingEnabled = Option(autoTargetingEnabled) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        id = Option(id) /* 1 */,
        keywords = keywords.map(_.asModel) /* 5 */,
        name = name /* 2 */,
        placementGroup = Option(placementGroup) /* 1 */,
        sizing = Option(sizing).map(_.asModel) /* 4 */,
        status = Option(status) /* 1 */,
        targetingAttributes = targetingAttributes.asModel /* 6 */,
        trackingUrls = Option(trackingUrls).map(_.asModel) /* 4 */,
        updatedTime = Option(updatedTime) /* 1 */,
        valid = Option(valid) /* 1 */
    
    )
  }
}

object TargetingTemplateData {

  def validated(d8a : TargetingTemplateData, failFast : Boolean) : scala.util.Try[TargetingTemplate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : TargetingTemplateData = try {
        val data = read[TargetingTemplateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating TargetingTemplateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : TargetingTemplateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[TargetingTemplateData] = try {
        read[List[TargetingTemplateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[TargetingTemplate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[TargetingTemplate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, TargetingTemplateData] = try {
        read[Map[String, TargetingTemplateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, TargetingTemplate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, TargetingTemplate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

