/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AssetIdWithPermissionsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AssetIdWithPermissionsData(
/* Unique identifier of a business asset. */
  id: String = "" ,
/* Permission levels member or partner has on an asset. */
  permissions: Seq[String] = Nil 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ AssetIdWithPermissions.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate min length 1
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AssetIdWithPermissions.Fields.id, s"length $len is shorter than the min length 1")
            }
        }
        // validate max length 20
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 20) {
               _allValidationErrors += ValidationError(path :+ AssetIdWithPermissions.Fields.id, s"length $len is longer than the max length 20")
            }
        }
        
        
        
        

        // ================== permissions validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AssetIdWithPermissions] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AssetIdWithPermissions = {
    AssetIdWithPermissions(
        id = Option(id) /* 1 */,
        permissions = permissions /* 2 */
    
    )
  }
}

object AssetIdWithPermissionsData {

  def validated(d8a : AssetIdWithPermissionsData, failFast : Boolean) : scala.util.Try[AssetIdWithPermissions] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AssetIdWithPermissionsData = try {
        val data = read[AssetIdWithPermissionsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AssetIdWithPermissionsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AssetIdWithPermissionsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AssetIdWithPermissionsData] = try {
        read[List[AssetIdWithPermissionsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AssetIdWithPermissions]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AssetIdWithPermissions]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AssetIdWithPermissionsData] = try {
        read[Map[String, AssetIdWithPermissionsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AssetIdWithPermissions]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AssetIdWithPermissions]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

