/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** SharedAudienceAccountData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SharedAudienceAccountData(
/* Account ID (ad account or business ID). */
  accountId: String,
/* Account name. */
  accountName: String,
/* account type */
  accountType: AudienceAccountType,
/* Epoch timestamp in seconds for the shared audience event */
  sharedOnTimestamp: Int
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== accountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if accountId == null || !regex.r.matches(accountId) then
              _allValidationErrors += ValidationError(path :+ SharedAudienceAccount.Fields.accountId, s"value '$accountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== accountName validation ==================
        
        
        
        

        // ================== accountType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (accountType == null) {
            _allValidationErrors += ValidationError(path :+ SharedAudienceAccount.Fields.accountType, "accountType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== sharedOnTimestamp validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[SharedAudienceAccount] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SharedAudienceAccount = {
    SharedAudienceAccount(
        accountId = accountId /* 2 */,
        accountName = accountName /* 2 */,
        accountType = accountType /* 2 */,
        sharedOnTimestamp = sharedOnTimestamp /* 2 */
    
    )
  }
}

object SharedAudienceAccountData {

  def validated(d8a : SharedAudienceAccountData, failFast : Boolean) : scala.util.Try[SharedAudienceAccount] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : SharedAudienceAccountData = try {
        val data = read[SharedAudienceAccountData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating SharedAudienceAccountData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : SharedAudienceAccountData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[SharedAudienceAccountData] = try {
        read[List[SharedAudienceAccountData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SharedAudienceAccount]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SharedAudienceAccount]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SharedAudienceAccountData] = try {
        read[Map[String, SharedAudienceAccountData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SharedAudienceAccount]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SharedAudienceAccount]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

