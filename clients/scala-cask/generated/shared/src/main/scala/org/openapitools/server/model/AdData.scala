/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AdData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AdData(
/* The ID of the advertiser that this ad belongs to. */
  adAccountId: String,
/* ID of the ad group that contains the ad. */
  adGroupId: String,
/* Deep link URL for Android devices. */
  androidDeepLink: String = "" ,
/* ID of the ad campaign that contains this ad. */
  campaignId: String,
/* Comma-separated deep links for the carousel pin on Android. */
  carouselAndroidDeepLinks: Seq[String] = Nil ,
/* Comma-separated destination URLs for the carousel pin to promote. */
  carouselDestinationUrls: Seq[String] = Nil ,
/* Comma-separated deep links for the carousel pin on iOS. */
  carouselIosDeepLinks: Seq[String] = Nil ,
/* The vendor platform type of the carting/WTB ad. */
  cartingPlatformType: Int = 0 ,
/* Array of carting/WTB products for the ad. */
  cartingProducts: Seq[CartingProductData] = Nil ,
/* Tracking url for the ad clicks. */
  clickTrackingUrl: String = "" ,
/* Destination URL template for all items within a collections drawer. */
  collectionItemsDestinationUrlTemplate: String = "" ,
  collectionsHeaderType: AdCollectionsHeaderType = null ,
/* Pin creation time. Unix timestamp in seconds. */
  createdTime: Int = 0 ,
  creativeType: CreativeType,
  customizableCtaType: CustomizableCTAType = null ,
/* Destination URL. */
  destinationUrl: String = "" ,
  disclosureType: DisclosureType = null ,
/* URL for a page that provides disclosures about a pharmaceutical product, such as potential side effects. Make sure the URL takes the user directly to the disclosure content and the referenced site is secure. */
  disclosureUrl: String = "" ,
  gridClickType: GridClickType = null ,
/* The ID of this ad. */
  id: String,
/* Deep link URL for iOS devices. */
  iosDeepLink: String = "" ,
/* Is the ad a carting/WTB ad? */
  isCarting: Boolean = false ,
/* Whether the advertiser has accepted the terms and conditions for collage ad. */
  isCollageAcceptedTerms: Boolean = false ,
/* Whether the collage ad has a single destination url override. */
  isCollageSingleDestination: Boolean = false ,
/* Is original pin deleted? */
  isPinDeleted: Boolean = false ,
/* Is pin repinnable? */
  isRemovable: Boolean = false ,
/* Lead form ID for lead ad generation. */
  leadFormId: String = "" ,
/* Name of the ad - 255 chars max. */
  name: String = "" ,
/* Pin ID. This field may only be updated for draft ads. */
  pinId: String,
/* Before creating a quiz ad, you must create an organic Pin using POST/Create Pin for each result in the quiz. Quiz ads cannot be saved by a Pinner. Quiz ad results can be saved. */
  quizPinData: QuizPinDataData = null ,
/* Enum reason why the pin was rejected. Returned if `review_status` is \"REJECTED\". */
  rejectedReasons: Seq[AdDisapprovalReasons],
/* Text reason why the pin was rejected. Returned if `review_status` is \"REJECTED\". */
  rejectionLabels: Seq[String],
/* Ad review status */
  reviewStatus: AdReviewStatus,
  status: EntityStatus = null ,
/* Ad summary status */
  summaryStatus: PinPromotionSummaryStatus,
  trackingUrls: TrackingUrlsData = null ,
/* Always \"ad\". */
  `type`: String,
/* Last update time. Unix timestamp in seconds. */
  updatedTime: Int = 0 ,
/* Tracking URL for ad impressions. */
  viewTrackingUrl: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== adGroupId validation ==================
        // validate against pattern '^(AG)?\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^(AG)?\\d+$"""
           if adGroupId == null || !regex.r.matches(adGroupId) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.adGroupId, s"value '$adGroupId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== androidDeepLink validation ==================
        
        
        
        

        // ================== campaignId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if campaignId == null || !regex.r.matches(campaignId) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.campaignId, s"value '$campaignId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== carouselAndroidDeepLinks validation ==================
        
        
        
        

        // ================== carouselDestinationUrls validation ==================
        
        
        
        

        // ================== carouselIosDeepLinks validation ==================
        
        
        
        

        // ================== cartingPlatformType validation ==================
        // validate against minimum 1
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(cartingPlatformType >= 1) then
            _allValidationErrors += ValidationError(path :+ Ad.Fields.cartingPlatformType, s"value '$cartingPlatformType' is not greater than the minimum value 1")
        }
        // validate against maximum 2
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(cartingPlatformType <= 2) then
            _allValidationErrors += ValidationError(path :+ Ad.Fields.cartingPlatformType, s"value '$cartingPlatformType' is not greater than the maximum value 2")
        }
        
        
        
        

        // ================== cartingProducts validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (cartingProducts != null) {
                cartingProducts.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ Ad.Fields.cartingProducts :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== clickTrackingUrl validation ==================
        
        
        
        

        // ================== collectionItemsDestinationUrlTemplate validation ==================
        
        
        
        

        // ================== collectionsHeaderType validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== creativeType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (creativeType == null) {
            _allValidationErrors += ValidationError(path :+ Ad.Fields.creativeType, "creativeType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== customizableCtaType validation ==================
        
        
        
        

        // ================== destinationUrl validation ==================
        
        
        
        

        // ================== disclosureType validation ==================
        
        
        
        

        // ================== disclosureUrl validation ==================
        
        
        
        

        // ================== gridClickType validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ Ad.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== iosDeepLink validation ==================
        
        
        
        

        // ================== isCarting validation ==================
        
        
        
        

        // ================== isCollageAcceptedTerms validation ==================
        
        
        
        

        // ================== isCollageSingleDestination validation ==================
        
        
        
        

        // ================== isPinDeleted validation ==================
        
        
        
        

        // ================== isRemovable validation ==================
        
        
        
        

        // ================== leadFormId validation ==================
        // validate against pattern '^(AG)?\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^(AG)?\\d+$"""
           if leadFormId == null || !regex.r.matches(leadFormId) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.leadFormId, s"value '$leadFormId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== pinId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if pinId == null || !regex.r.matches(pinId) then
              _allValidationErrors += ValidationError(path :+ Ad.Fields.pinId, s"value '$pinId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== quizPinData validation ==================
        
        
        
        
        // validating quizPinData
        if (_allValidationErrors.isEmpty || !failFast) {
            if quizPinData != null then _allValidationErrors ++= quizPinData.validationErrors(path :+ Ad.Fields.quizPinData, failFast)
        }

        // ================== rejectedReasons validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (rejectedReasons == null) {
            _allValidationErrors += ValidationError(path :+ Ad.Fields.rejectedReasons, "rejectedReasons is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== rejectionLabels validation ==================
        
        
        
        

        // ================== reviewStatus validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (reviewStatus == null) {
            _allValidationErrors += ValidationError(path :+ Ad.Fields.reviewStatus, "reviewStatus is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== status validation ==================
        
        
        
        

        // ================== summaryStatus validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (summaryStatus == null) {
            _allValidationErrors += ValidationError(path :+ Ad.Fields.summaryStatus, "summaryStatus is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== trackingUrls validation ==================
        
        
        
        
        // validating trackingUrls
        if (_allValidationErrors.isEmpty || !failFast) {
            if trackingUrls != null then _allValidationErrors ++= trackingUrls.validationErrors(path :+ Ad.Fields.trackingUrls, failFast)
        }

        // ================== `type` validation ==================
        
        
        
        

        // ================== updatedTime validation ==================
        
        
        
        

        // ================== viewTrackingUrl validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[Ad] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : Ad = {
    Ad(
        adAccountId = adAccountId /* 2 */,
        adGroupId = adGroupId /* 2 */,
        androidDeepLink = Option(androidDeepLink) /* 1 */,
        campaignId = campaignId /* 2 */,
        carouselAndroidDeepLinks = carouselAndroidDeepLinks /* 2 */,
        carouselDestinationUrls = carouselDestinationUrls /* 2 */,
        carouselIosDeepLinks = carouselIosDeepLinks /* 2 */,
        cartingPlatformType = Option(cartingPlatformType) /* 1 */,
        cartingProducts = cartingProducts.map(_.asModel) /* 5 */,
        clickTrackingUrl = Option(clickTrackingUrl) /* 1 */,
        collectionItemsDestinationUrlTemplate = Option(collectionItemsDestinationUrlTemplate) /* 1 */,
        collectionsHeaderType = Option(collectionsHeaderType) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        creativeType = creativeType /* 2 */,
        customizableCtaType = Option(customizableCtaType) /* 1 */,
        destinationUrl = Option(destinationUrl) /* 1 */,
        disclosureType = Option(disclosureType) /* 1 */,
        disclosureUrl = Option(disclosureUrl) /* 1 */,
        gridClickType = Option(gridClickType) /* 1 */,
        id = id /* 2 */,
        iosDeepLink = Option(iosDeepLink) /* 1 */,
        isCarting = Option(isCarting) /* 1 */,
        isCollageAcceptedTerms = Option(isCollageAcceptedTerms) /* 1 */,
        isCollageSingleDestination = Option(isCollageSingleDestination) /* 1 */,
        isPinDeleted = Option(isPinDeleted) /* 1 */,
        isRemovable = Option(isRemovable) /* 1 */,
        leadFormId = Option(leadFormId) /* 1 */,
        name = Option(name) /* 1 */,
        pinId = pinId /* 2 */,
        quizPinData = Option(quizPinData).map(_.asModel) /* 4 */,
        rejectedReasons = rejectedReasons.map(_.asModel) /* 5 */,
        rejectionLabels = rejectionLabels /* 2 */,
        reviewStatus = reviewStatus /* 2 */,
        status = Option(status) /* 1 */,
        summaryStatus = summaryStatus /* 2 */,
        trackingUrls = Option(trackingUrls).map(_.asModel) /* 4 */,
        `type` = `type` /* 2 */,
        updatedTime = Option(updatedTime) /* 1 */,
        viewTrackingUrl = Option(viewTrackingUrl) /* 1 */
    
    )
  }
}

object AdData {

  def validated(d8a : AdData, failFast : Boolean) : scala.util.Try[Ad] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AdData = try {
        val data = read[AdData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AdData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AdData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AdData] = try {
        read[List[AdData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[Ad]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[Ad]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AdData] = try {
        read[Map[String, AdData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, Ad]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, Ad]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

