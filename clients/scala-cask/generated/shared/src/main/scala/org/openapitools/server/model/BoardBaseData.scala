/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import java.time.OffsetDateTime
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** BoardBaseData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BoardBaseData(
/* Date and time of last board pins modified. */
  boardPinsModifiedAt: OffsetDateTime = null ,
/* Count of collaborators on the board. */
  collaboratorCount: Int = 0 ,
/* Date and time of board creation. */
  createdAt: OffsetDateTime = null ,
  description: String = "" ,
/* Board follower count. */
  followerCount: Int = 0 ,
  id: String,
/* If set to `true`, the board will be ad-only and can store ad-only Pins. */
  isAdsOnly: Boolean = false ,
/* Board media. */
  media: BoardMediaData = null ,
/*     Name of the board.      **Note:** If you create an ad-only board by setting `is_ads_only`     to `true`, the board name automatically becomes \"Ad-only Pins\". */
  name: String,
  owner: BoardOwnerData = null ,
/* Count of Pins on the board. */
  pinCount: Int = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== boardPinsModifiedAt validation ==================
        
        
        
        

        // ================== collaboratorCount validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(collaboratorCount >= 0) then
            _allValidationErrors += ValidationError(path :+ BoardBase.Fields.collaboratorCount, s"value '$collaboratorCount' is not greater than the minimum value 0")
        }
        
        
        
        

        // ================== createdAt validation ==================
        
        
        
        

        // ================== description validation ==================
        
        
        
        

        // ================== followerCount validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(followerCount >= 0) then
            _allValidationErrors += ValidationError(path :+ BoardBase.Fields.followerCount, s"value '$followerCount' is not greater than the minimum value 0")
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ BoardBase.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== isAdsOnly validation ==================
        
        
        
        

        // ================== media validation ==================
        
        
        
        
        // validating media
        if (_allValidationErrors.isEmpty || !failFast) {
            if media != null then _allValidationErrors ++= media.validationErrors(path :+ BoardBase.Fields.media, failFast)
        }

        // ================== name validation ==================
        
        
        
        

        // ================== owner validation ==================
        
        
        
        
        // validating owner
        if (_allValidationErrors.isEmpty || !failFast) {
            if owner != null then _allValidationErrors ++= owner.validationErrors(path :+ BoardBase.Fields.owner, failFast)
        }

        // ================== pinCount validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(pinCount >= 0) then
            _allValidationErrors += ValidationError(path :+ BoardBase.Fields.pinCount, s"value '$pinCount' is not greater than the minimum value 0")
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[BoardBase] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BoardBase = {
    BoardBase(
        boardPinsModifiedAt = Option(boardPinsModifiedAt) /* 1 */,
        collaboratorCount = Option(collaboratorCount) /* 1 */,
        createdAt = Option(createdAt) /* 1 */,
        description = Option(description) /* 1 */,
        followerCount = Option(followerCount) /* 1 */,
        id = id /* 2 */,
        isAdsOnly = Option(isAdsOnly) /* 1 */,
        media = Option(media).map(_.asModel) /* 4 */,
        name = name /* 2 */,
        owner = Option(owner).map(_.asModel) /* 4 */,
        pinCount = Option(pinCount) /* 1 */
    
    )
  }
}

object BoardBaseData {

  def validated(d8a : BoardBaseData, failFast : Boolean) : scala.util.Try[BoardBase] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : BoardBaseData = try {
        val data = read[BoardBaseData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating BoardBaseData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : BoardBaseData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[BoardBaseData] = try {
        read[List[BoardBaseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BoardBase]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BoardBase]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BoardBaseData] = try {
        read[Map[String, BoardBaseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BoardBase]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BoardBase]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

