/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** IntegrationLogData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class IntegrationLogData(
  advertiserId: String = "" ,
/* Version number of the integration application. */
  appVersionNumber: String = "" ,
/* Timestamp in milliseconds of when the log was executed at the client. */
  clientTimestamp: Int,
  error: IntegrationLogClientErrorData = null ,
/* Log event type */
  eventType: IntegrationLogEventType,
  externalBusinessId: String = "" ,
  feedProfileId: String = "" ,
/* Log level type */
  logLevel: IntegrationLogLevel,
  merchantId: String = "" ,
/* Explanation of the event that occured. */
  message: String = "" ,
/* Version number of the platform the integration application is running on. */
  platformVersionNumber: String = "" ,
  request: IntegrationLogClientRequestData = null ,
  tagId: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== advertiserId validation ==================
        // validate max length 128
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if advertiserId == null then 0 else advertiserId.length
            if (len < 128) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.advertiserId, s"length $len is longer than the max length 128")
            }
        }
        
        
        
        

        // ================== appVersionNumber validation ==================
        // validate max length 20
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appVersionNumber == null then 0 else appVersionNumber.length
            if (len < 20) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.appVersionNumber, s"length $len is longer than the max length 20")
            }
        }
        
        
        
        

        // ================== clientTimestamp validation ==================
        
        
        
        

        // ================== error validation ==================
        
        
        
        
        // validating error
        if (_allValidationErrors.isEmpty || !failFast) {
            if error != null then _allValidationErrors ++= error.validationErrors(path :+ IntegrationLog.Fields.error, failFast)
        }

        // ================== eventType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (eventType == null) {
            _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.eventType, "eventType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== externalBusinessId validation ==================
        // validate max length 2048
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if externalBusinessId == null then 0 else externalBusinessId.length
            if (len < 2048) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.externalBusinessId, s"length $len is longer than the max length 2048")
            }
        }
        
        
        
        

        // ================== feedProfileId validation ==================
        // validate max length 128
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if feedProfileId == null then 0 else feedProfileId.length
            if (len < 128) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.feedProfileId, s"length $len is longer than the max length 128")
            }
        }
        
        
        
        

        // ================== logLevel validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (logLevel == null) {
            _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.logLevel, "logLevel is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== merchantId validation ==================
        // validate max length 128
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if merchantId == null then 0 else merchantId.length
            if (len < 128) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.merchantId, s"length $len is longer than the max length 128")
            }
        }
        
        
        
        

        // ================== message validation ==================
        // validate max length 8192
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if message == null then 0 else message.length
            if (len < 8192) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.message, s"length $len is longer than the max length 8192")
            }
        }
        
        
        
        

        // ================== platformVersionNumber validation ==================
        // validate max length 20
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if platformVersionNumber == null then 0 else platformVersionNumber.length
            if (len < 20) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.platformVersionNumber, s"length $len is longer than the max length 20")
            }
        }
        
        
        
        

        // ================== request validation ==================
        
        
        
        
        // validating request
        if (_allValidationErrors.isEmpty || !failFast) {
            if request != null then _allValidationErrors ++= request.validationErrors(path :+ IntegrationLog.Fields.request, failFast)
        }

        // ================== tagId validation ==================
        // validate max length 128
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if tagId == null then 0 else tagId.length
            if (len < 128) {
               _allValidationErrors += ValidationError(path :+ IntegrationLog.Fields.tagId, s"length $len is longer than the max length 128")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[IntegrationLog] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : IntegrationLog = {
    IntegrationLog(
        advertiserId = Option(advertiserId) /* 1 */,
        appVersionNumber = Option(appVersionNumber) /* 1 */,
        clientTimestamp = clientTimestamp /* 2 */,
        error = Option(error).map(_.asModel) /* 4 */,
        eventType = eventType /* 2 */,
        externalBusinessId = Option(externalBusinessId) /* 1 */,
        feedProfileId = Option(feedProfileId) /* 1 */,
        logLevel = logLevel /* 2 */,
        merchantId = Option(merchantId) /* 1 */,
        message = Option(message) /* 1 */,
        platformVersionNumber = Option(platformVersionNumber) /* 1 */,
        request = Option(request).map(_.asModel) /* 4 */,
        tagId = Option(tagId) /* 1 */
    
    )
  }
}

object IntegrationLogData {

  def validated(d8a : IntegrationLogData, failFast : Boolean) : scala.util.Try[IntegrationLog] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : IntegrationLogData = try {
        val data = read[IntegrationLogData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating IntegrationLogData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : IntegrationLogData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[IntegrationLogData] = try {
        read[List[IntegrationLogData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[IntegrationLog]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[IntegrationLog]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, IntegrationLogData] = try {
        read[Map[String, IntegrationLogData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, IntegrationLog]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, IntegrationLog]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

