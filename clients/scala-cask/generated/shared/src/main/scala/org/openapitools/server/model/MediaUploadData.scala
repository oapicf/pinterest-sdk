/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** MediaUploadData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class MediaUploadData(
/* Unique identifier for this media upload. Used to track status and for attaching during Pin creation. */
  mediaId: String,
  mediaType: MediaUploadType,
/* The list of parameter key/value pairs you will need to send with your POST request to upload your media file. */
  uploadParameters: MediaUploadParametersData = null ,
/* The URL where you will POST your media file. */
  uploadUrl: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== mediaId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if mediaId == null || !regex.r.matches(mediaId) then
              _allValidationErrors += ValidationError(path :+ MediaUpload.Fields.mediaId, s"value '$mediaId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== mediaType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (mediaType == null) {
            _allValidationErrors += ValidationError(path :+ MediaUpload.Fields.mediaType, "mediaType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== uploadParameters validation ==================
        
        
        
        
        // validating uploadParameters
        if (_allValidationErrors.isEmpty || !failFast) {
            if uploadParameters != null then _allValidationErrors ++= uploadParameters.validationErrors(path :+ MediaUpload.Fields.uploadParameters, failFast)
        }

        // ================== uploadUrl validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[MediaUpload] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : MediaUpload = {
    MediaUpload(
        mediaId = mediaId /* 2 */,
        mediaType = mediaType /* 2 */,
        uploadParameters = Option(uploadParameters).map(_.asModel) /* 4 */,
        uploadUrl = Option(uploadUrl) /* 1 */
    
    )
  }
}

object MediaUploadData {

  def validated(d8a : MediaUploadData, failFast : Boolean) : scala.util.Try[MediaUpload] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : MediaUploadData = try {
        val data = read[MediaUploadData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating MediaUploadData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : MediaUploadData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[MediaUploadData] = try {
        read[List[MediaUploadData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[MediaUpload]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[MediaUpload]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, MediaUploadData] = try {
        read[Map[String, MediaUploadData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, MediaUpload]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, MediaUpload]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

