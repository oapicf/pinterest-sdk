/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import java.time.OffsetDateTime
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CatalogsFeedProcessingResultData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CatalogsFeedProcessingResultData(
  createdAt: OffsetDateTime,
/* ID of the feed processing result. */
  id: String,
  ingestionDetails: CatalogsFeedIngestionDetailsData,
  productCounts: CatalogsFeedProductCountsData,
  status: CatalogsFeedProcessingStatus,
  updatedAt: OffsetDateTime,
  validationDetails: CatalogsFeedValidationDetailsData,
  videoCounts: CatalogsFeedVideoCountsData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== createdAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (createdAt == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.createdAt, "createdAt is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== ingestionDetails validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (ingestionDetails == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.ingestionDetails, "ingestionDetails is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating ingestionDetails
        if (_allValidationErrors.isEmpty || !failFast) {
            if ingestionDetails != null then _allValidationErrors ++= ingestionDetails.validationErrors(path :+ CatalogsFeedProcessingResult.Fields.ingestionDetails, failFast)
        }

        // ================== productCounts validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (productCounts == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.productCounts, "productCounts is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating productCounts
        if (_allValidationErrors.isEmpty || !failFast) {
            if productCounts != null then _allValidationErrors ++= productCounts.validationErrors(path :+ CatalogsFeedProcessingResult.Fields.productCounts, failFast)
        }

        // ================== status validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (status == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.status, "status is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== updatedAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (updatedAt == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.updatedAt, "updatedAt is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== validationDetails validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (validationDetails == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsFeedProcessingResult.Fields.validationDetails, "validationDetails is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating validationDetails
        if (_allValidationErrors.isEmpty || !failFast) {
            if validationDetails != null then _allValidationErrors ++= validationDetails.validationErrors(path :+ CatalogsFeedProcessingResult.Fields.validationDetails, failFast)
        }

        // ================== videoCounts validation ==================
        
        
        
        
        // validating videoCounts
        if (_allValidationErrors.isEmpty || !failFast) {
            if videoCounts != null then _allValidationErrors ++= videoCounts.validationErrors(path :+ CatalogsFeedProcessingResult.Fields.videoCounts, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CatalogsFeedProcessingResult] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CatalogsFeedProcessingResult = {
    CatalogsFeedProcessingResult(
        createdAt = createdAt /* 2 */,
        id = id /* 2 */,
        ingestionDetails = ingestionDetails.asModel /* 6 */,
        productCounts = productCounts.asModel /* 6 */,
        status = status /* 2 */,
        updatedAt = updatedAt /* 2 */,
        validationDetails = validationDetails.asModel /* 6 */,
        videoCounts = Option(videoCounts).map(_.asModel) /* 4 */
    
    )
  }
}

object CatalogsFeedProcessingResultData {

  def validated(d8a : CatalogsFeedProcessingResultData, failFast : Boolean) : scala.util.Try[CatalogsFeedProcessingResult] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CatalogsFeedProcessingResultData = try {
        val data = read[CatalogsFeedProcessingResultData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CatalogsFeedProcessingResultData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CatalogsFeedProcessingResultData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CatalogsFeedProcessingResultData] = try {
        read[List[CatalogsFeedProcessingResultData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CatalogsFeedProcessingResult]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CatalogsFeedProcessingResult]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CatalogsFeedProcessingResultData] = try {
        read[Map[String, CatalogsFeedProcessingResultData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CatalogsFeedProcessingResult]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CatalogsFeedProcessingResult]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

