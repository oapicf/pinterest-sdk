/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CatalogsItemValidationErrorsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CatalogsItemValidationErrorsData(
/* Item has an invalid adult value. */
  ADULT_INVALID: CatalogsItemValidationDetailsData = null ,
/* Adword link contains too many characters. */
  ADWORDS_FORMAT_INVALID: CatalogsItemValidationDetailsData = null ,
/* Item is missing availability value in its product metadata, this item will not be published. */
  AVAILABILITY_INVALID: CatalogsItemValidationDetailsData = null ,
/* Item will not be published because it doesn't meet Pinterest's Merchant Guidelines. */
  BLOCKLISTED_IMAGE_SIGNATURE: CatalogsItemValidationDetailsData = null ,
/* Item is missing description in its product metadata, this item will not be published. */
  DESCRIPTION_MISSING: CatalogsItemValidationDetailsData = null ,
/* This product is duplicated. The duplicate entry will not be published. */
  DUPLICATE_PRODUCTS: CatalogsItemValidationDetailsData = null ,
/* Image link is invalid. */
  IMAGE_LINK_INVALID: CatalogsItemValidationDetailsData = null ,
/* Item has image_link URL that contains too many characters, so the item will not be published. */
  IMAGE_LINK_LENGTH_TOO_LONG: CatalogsItemValidationDetailsData = null ,
/* Item is missing an image link URL in its product metadata, this item will not be published. */
  IMAGE_LINK_MISSING: CatalogsItemValidationDetailsData = null ,
/* Product link value doesn't match the verified domain associated with this account. */
  INVALID_DOMAIN: CatalogsItemValidationDetailsData = null ,
/* Main image can't be found. */
  ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE: CatalogsItemValidationDetailsData = null ,
/* Item is missing item id in its product metadata, this item will not be published. */
  ITEMID_MISSING: CatalogsItemValidationDetailsData = null ,
/* Link is invalid. */
  LINK_FORMAT_INVALID: CatalogsItemValidationDetailsData = null ,
/* Product link contains too many characters, this item will not be published. */
  LINK_LENGTH_TOO_LONG: CatalogsItemValidationDetailsData = null ,
/* Item has a list price formatting error, this item will not be published. */
  LIST_PRICE_INVALID: CatalogsItemValidationDetailsData = null ,
/* Item exceed the maximum number of items per item group, this item will not be published. */
  MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED: CatalogsItemValidationDetailsData = null ,
/* Item contains formating errors. */
  PARSE_LINE_ERROR: CatalogsItemValidationDetailsData = null ,
/* Item will not be published because it doesn't meet Pinterest's Merchant Guidelines. */
  PINJOIN_CONTENT_UNSAFE: CatalogsItemValidationDetailsData = null ,
/* Item price cannot be determined because the price, list price, and sale price are all different. */
  PRICE_CANNOT_BE_DETERMINED: CatalogsItemValidationDetailsData = null ,
/* Product is missing a price, this item will not be published. */
  PRICE_MISSING: CatalogsItemValidationDetailsData = null ,
/* Item is missing a link URL in its product metadata, this item will not be published. */
  PRODUCT_LINK_MISSING: CatalogsItemValidationDetailsData = null ,
/* Item has a price formatting error in its product metadata, this item will not be published. */
  PRODUCT_PRICE_INVALID: CatalogsItemValidationDetailsData = null ,
/* Item is missing title in its product metadata, this item will not be published. */
  TITLE_MISSING: CatalogsItemValidationDetailsData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== ADULT_INVALID validation ==================
        
        
        
        
        // validating ADULT_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if ADULT_INVALID != null then _allValidationErrors ++= ADULT_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.ADULT_INVALID, failFast)
        }

        // ================== ADWORDS_FORMAT_INVALID validation ==================
        
        
        
        
        // validating ADWORDS_FORMAT_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if ADWORDS_FORMAT_INVALID != null then _allValidationErrors ++= ADWORDS_FORMAT_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.ADWORDS_FORMAT_INVALID, failFast)
        }

        // ================== AVAILABILITY_INVALID validation ==================
        
        
        
        
        // validating AVAILABILITY_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if AVAILABILITY_INVALID != null then _allValidationErrors ++= AVAILABILITY_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.AVAILABILITY_INVALID, failFast)
        }

        // ================== BLOCKLISTED_IMAGE_SIGNATURE validation ==================
        
        
        
        
        // validating BLOCKLISTED_IMAGE_SIGNATURE
        if (_allValidationErrors.isEmpty || !failFast) {
            if BLOCKLISTED_IMAGE_SIGNATURE != null then _allValidationErrors ++= BLOCKLISTED_IMAGE_SIGNATURE.validationErrors(path :+ CatalogsItemValidationErrors.Fields.BLOCKLISTED_IMAGE_SIGNATURE, failFast)
        }

        // ================== DESCRIPTION_MISSING validation ==================
        
        
        
        
        // validating DESCRIPTION_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if DESCRIPTION_MISSING != null then _allValidationErrors ++= DESCRIPTION_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.DESCRIPTION_MISSING, failFast)
        }

        // ================== DUPLICATE_PRODUCTS validation ==================
        
        
        
        
        // validating DUPLICATE_PRODUCTS
        if (_allValidationErrors.isEmpty || !failFast) {
            if DUPLICATE_PRODUCTS != null then _allValidationErrors ++= DUPLICATE_PRODUCTS.validationErrors(path :+ CatalogsItemValidationErrors.Fields.DUPLICATE_PRODUCTS, failFast)
        }

        // ================== IMAGE_LINK_INVALID validation ==================
        
        
        
        
        // validating IMAGE_LINK_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if IMAGE_LINK_INVALID != null then _allValidationErrors ++= IMAGE_LINK_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.IMAGE_LINK_INVALID, failFast)
        }

        // ================== IMAGE_LINK_LENGTH_TOO_LONG validation ==================
        
        
        
        
        // validating IMAGE_LINK_LENGTH_TOO_LONG
        if (_allValidationErrors.isEmpty || !failFast) {
            if IMAGE_LINK_LENGTH_TOO_LONG != null then _allValidationErrors ++= IMAGE_LINK_LENGTH_TOO_LONG.validationErrors(path :+ CatalogsItemValidationErrors.Fields.IMAGE_LINK_LENGTH_TOO_LONG, failFast)
        }

        // ================== IMAGE_LINK_MISSING validation ==================
        
        
        
        
        // validating IMAGE_LINK_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if IMAGE_LINK_MISSING != null then _allValidationErrors ++= IMAGE_LINK_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.IMAGE_LINK_MISSING, failFast)
        }

        // ================== INVALID_DOMAIN validation ==================
        
        
        
        
        // validating INVALID_DOMAIN
        if (_allValidationErrors.isEmpty || !failFast) {
            if INVALID_DOMAIN != null then _allValidationErrors ++= INVALID_DOMAIN.validationErrors(path :+ CatalogsItemValidationErrors.Fields.INVALID_DOMAIN, failFast)
        }

        // ================== ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE validation ==================
        
        
        
        
        // validating ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE
        if (_allValidationErrors.isEmpty || !failFast) {
            if ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE != null then _allValidationErrors ++= ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE.validationErrors(path :+ CatalogsItemValidationErrors.Fields.ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE, failFast)
        }

        // ================== ITEMID_MISSING validation ==================
        
        
        
        
        // validating ITEMID_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if ITEMID_MISSING != null then _allValidationErrors ++= ITEMID_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.ITEMID_MISSING, failFast)
        }

        // ================== LINK_FORMAT_INVALID validation ==================
        
        
        
        
        // validating LINK_FORMAT_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if LINK_FORMAT_INVALID != null then _allValidationErrors ++= LINK_FORMAT_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.LINK_FORMAT_INVALID, failFast)
        }

        // ================== LINK_LENGTH_TOO_LONG validation ==================
        
        
        
        
        // validating LINK_LENGTH_TOO_LONG
        if (_allValidationErrors.isEmpty || !failFast) {
            if LINK_LENGTH_TOO_LONG != null then _allValidationErrors ++= LINK_LENGTH_TOO_LONG.validationErrors(path :+ CatalogsItemValidationErrors.Fields.LINK_LENGTH_TOO_LONG, failFast)
        }

        // ================== LIST_PRICE_INVALID validation ==================
        
        
        
        
        // validating LIST_PRICE_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if LIST_PRICE_INVALID != null then _allValidationErrors ++= LIST_PRICE_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.LIST_PRICE_INVALID, failFast)
        }

        // ================== MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED validation ==================
        
        
        
        
        // validating MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED
        if (_allValidationErrors.isEmpty || !failFast) {
            if MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED != null then _allValidationErrors ++= MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED.validationErrors(path :+ CatalogsItemValidationErrors.Fields.MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED, failFast)
        }

        // ================== PARSE_LINE_ERROR validation ==================
        
        
        
        
        // validating PARSE_LINE_ERROR
        if (_allValidationErrors.isEmpty || !failFast) {
            if PARSE_LINE_ERROR != null then _allValidationErrors ++= PARSE_LINE_ERROR.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PARSE_LINE_ERROR, failFast)
        }

        // ================== PINJOIN_CONTENT_UNSAFE validation ==================
        
        
        
        
        // validating PINJOIN_CONTENT_UNSAFE
        if (_allValidationErrors.isEmpty || !failFast) {
            if PINJOIN_CONTENT_UNSAFE != null then _allValidationErrors ++= PINJOIN_CONTENT_UNSAFE.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PINJOIN_CONTENT_UNSAFE, failFast)
        }

        // ================== PRICE_CANNOT_BE_DETERMINED validation ==================
        
        
        
        
        // validating PRICE_CANNOT_BE_DETERMINED
        if (_allValidationErrors.isEmpty || !failFast) {
            if PRICE_CANNOT_BE_DETERMINED != null then _allValidationErrors ++= PRICE_CANNOT_BE_DETERMINED.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PRICE_CANNOT_BE_DETERMINED, failFast)
        }

        // ================== PRICE_MISSING validation ==================
        
        
        
        
        // validating PRICE_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if PRICE_MISSING != null then _allValidationErrors ++= PRICE_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PRICE_MISSING, failFast)
        }

        // ================== PRODUCT_LINK_MISSING validation ==================
        
        
        
        
        // validating PRODUCT_LINK_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if PRODUCT_LINK_MISSING != null then _allValidationErrors ++= PRODUCT_LINK_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PRODUCT_LINK_MISSING, failFast)
        }

        // ================== PRODUCT_PRICE_INVALID validation ==================
        
        
        
        
        // validating PRODUCT_PRICE_INVALID
        if (_allValidationErrors.isEmpty || !failFast) {
            if PRODUCT_PRICE_INVALID != null then _allValidationErrors ++= PRODUCT_PRICE_INVALID.validationErrors(path :+ CatalogsItemValidationErrors.Fields.PRODUCT_PRICE_INVALID, failFast)
        }

        // ================== TITLE_MISSING validation ==================
        
        
        
        
        // validating TITLE_MISSING
        if (_allValidationErrors.isEmpty || !failFast) {
            if TITLE_MISSING != null then _allValidationErrors ++= TITLE_MISSING.validationErrors(path :+ CatalogsItemValidationErrors.Fields.TITLE_MISSING, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CatalogsItemValidationErrors] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CatalogsItemValidationErrors = {
    CatalogsItemValidationErrors(
        ADULT_INVALID = Option(ADULT_INVALID).map(_.asModel) /* 4 */,
        ADWORDS_FORMAT_INVALID = Option(ADWORDS_FORMAT_INVALID).map(_.asModel) /* 4 */,
        AVAILABILITY_INVALID = Option(AVAILABILITY_INVALID).map(_.asModel) /* 4 */,
        BLOCKLISTED_IMAGE_SIGNATURE = Option(BLOCKLISTED_IMAGE_SIGNATURE).map(_.asModel) /* 4 */,
        DESCRIPTION_MISSING = Option(DESCRIPTION_MISSING).map(_.asModel) /* 4 */,
        DUPLICATE_PRODUCTS = Option(DUPLICATE_PRODUCTS).map(_.asModel) /* 4 */,
        IMAGE_LINK_INVALID = Option(IMAGE_LINK_INVALID).map(_.asModel) /* 4 */,
        IMAGE_LINK_LENGTH_TOO_LONG = Option(IMAGE_LINK_LENGTH_TOO_LONG).map(_.asModel) /* 4 */,
        IMAGE_LINK_MISSING = Option(IMAGE_LINK_MISSING).map(_.asModel) /* 4 */,
        INVALID_DOMAIN = Option(INVALID_DOMAIN).map(_.asModel) /* 4 */,
        ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE = Option(ITEM_MAIN_IMAGE_DOWNLOAD_FAILURE).map(_.asModel) /* 4 */,
        ITEMID_MISSING = Option(ITEMID_MISSING).map(_.asModel) /* 4 */,
        LINK_FORMAT_INVALID = Option(LINK_FORMAT_INVALID).map(_.asModel) /* 4 */,
        LINK_LENGTH_TOO_LONG = Option(LINK_LENGTH_TOO_LONG).map(_.asModel) /* 4 */,
        LIST_PRICE_INVALID = Option(LIST_PRICE_INVALID).map(_.asModel) /* 4 */,
        MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED = Option(MAX_ITEMS_PER_ITEM_GROUP_EXCEEDED).map(_.asModel) /* 4 */,
        PARSE_LINE_ERROR = Option(PARSE_LINE_ERROR).map(_.asModel) /* 4 */,
        PINJOIN_CONTENT_UNSAFE = Option(PINJOIN_CONTENT_UNSAFE).map(_.asModel) /* 4 */,
        PRICE_CANNOT_BE_DETERMINED = Option(PRICE_CANNOT_BE_DETERMINED).map(_.asModel) /* 4 */,
        PRICE_MISSING = Option(PRICE_MISSING).map(_.asModel) /* 4 */,
        PRODUCT_LINK_MISSING = Option(PRODUCT_LINK_MISSING).map(_.asModel) /* 4 */,
        PRODUCT_PRICE_INVALID = Option(PRODUCT_PRICE_INVALID).map(_.asModel) /* 4 */,
        TITLE_MISSING = Option(TITLE_MISSING).map(_.asModel) /* 4 */
    
    )
  }
}

object CatalogsItemValidationErrorsData {

  def validated(d8a : CatalogsItemValidationErrorsData, failFast : Boolean) : scala.util.Try[CatalogsItemValidationErrors] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CatalogsItemValidationErrorsData = try {
        val data = read[CatalogsItemValidationErrorsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CatalogsItemValidationErrorsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CatalogsItemValidationErrorsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CatalogsItemValidationErrorsData] = try {
        read[List[CatalogsItemValidationErrorsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CatalogsItemValidationErrors]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CatalogsItemValidationErrors]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CatalogsItemValidationErrorsData] = try {
        read[Map[String, CatalogsItemValidationErrorsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CatalogsItemValidationErrors]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CatalogsItemValidationErrors]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

