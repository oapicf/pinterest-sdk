/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AudienceInsightsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AudienceInsightsData(
/* Category interest distribution */
  categories: Seq[AudienceCategoryData] = Nil ,
/* Generation date */
  date: String = "" ,
  demographics: AudienceDemographicsData = null ,
/* Population count. */
  size: Int = 0 ,
/* Indicates whether the audience size has been rounded up to the next highest upper boundary. */
  sizeIsUpperBound: Boolean = false ,
  `type`: AudienceInsightType = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== categories validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (categories != null) {
                categories.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceInsights.Fields.categories :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== date validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if date == null || !regex.r.matches(date) then
              _allValidationErrors += ValidationError(path :+ AudienceInsights.Fields.date, s"value '$date' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== demographics validation ==================
        
        
        
        
        // validating demographics
        if (_allValidationErrors.isEmpty || !failFast) {
            if demographics != null then _allValidationErrors ++= demographics.validationErrors(path :+ AudienceInsights.Fields.demographics, failFast)
        }

        // ================== size validation ==================
        
        
        
        

        // ================== sizeIsUpperBound validation ==================
        
        
        
        

        // ================== `type` validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AudienceInsights] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AudienceInsights = {
    AudienceInsights(
        categories = categories.map(_.asModel) /* 5 */,
        date = Option(date) /* 1 */,
        demographics = Option(demographics).map(_.asModel) /* 4 */,
        size = Option(size) /* 1 */,
        sizeIsUpperBound = Option(sizeIsUpperBound) /* 1 */,
        `type` = Option(`type`) /* 1 */
    
    )
  }
}

object AudienceInsightsData {

  def validated(d8a : AudienceInsightsData, failFast : Boolean) : scala.util.Try[AudienceInsights] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AudienceInsightsData = try {
        val data = read[AudienceInsightsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AudienceInsightsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AudienceInsightsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AudienceInsightsData] = try {
        read[List[AudienceInsightsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AudienceInsights]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AudienceInsights]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AudienceInsightsData] = try {
        read[Map[String, AudienceInsightsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AudienceInsights]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AudienceInsights]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

