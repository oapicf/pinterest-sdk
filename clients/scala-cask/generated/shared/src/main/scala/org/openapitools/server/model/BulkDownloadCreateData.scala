/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** BulkDownloadCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BulkDownloadCreateData(
  campaignFilter: BulkDownloadCampaignFilterData = null ,
/* All entities specified by these IDs as well as their children and grandchildren will be downloaded if the entity type is one of the types requested to be downloaded. */
  entityIds: Seq[String] = Nil ,
/* All entity types specified will be downloaded. Fewer types result in faster downloads. */
  entityTypes: Seq[BulkEntityType] = Nil ,
  outputFormat: BulkOutputFormat = JSON ,
/* Unix UTC timestamp to retrieve all entities that have changed since this time. */
  updatedSince: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== campaignFilter validation ==================
        
        
        
        
        // validating campaignFilter
        if (_allValidationErrors.isEmpty || !failFast) {
            if campaignFilter != null then _allValidationErrors ++= campaignFilter.validationErrors(path :+ BulkDownloadCreate.Fields.campaignFilter, failFast)
        }

        // ================== entityIds validation ==================
        
        
        
        

        // ================== entityTypes validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if entityTypes == null then 0 else entityTypes.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ BulkDownloadCreate.Fields.entityTypes, s"entityTypes has $len, which is less than the min items 1")
            }
        }
        // validate min items 6
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if entityTypes == null then 0 else entityTypes.size
            if (len > 6) {
               _allValidationErrors += ValidationError(path :+ BulkDownloadCreate.Fields.entityTypes, s"entityTypes has $len, which is greater than the max items 6")
            }
        }
        
        
        

        // ================== outputFormat validation ==================
        
        
        
        

        // ================== updatedSince validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if updatedSince == null || !regex.r.matches(updatedSince) then
              _allValidationErrors += ValidationError(path :+ BulkDownloadCreate.Fields.updatedSince, s"value '$updatedSince' doesn't match pattern $regex")
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[BulkDownloadCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BulkDownloadCreate = {
    BulkDownloadCreate(
        campaignFilter = Option(campaignFilter).map(_.asModel) /* 4 */,
        entityIds = entityIds /* 2 */,
        entityTypes = entityTypes.map(_.asModel) /* 5 */,
        outputFormat = Option(outputFormat) /* 1 */,
        updatedSince = Option(updatedSince) /* 1 */
    
    )
  }
}

object BulkDownloadCreateData {

  def validated(d8a : BulkDownloadCreateData, failFast : Boolean) : scala.util.Try[BulkDownloadCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : BulkDownloadCreateData = try {
        val data = read[BulkDownloadCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating BulkDownloadCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : BulkDownloadCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[BulkDownloadCreateData] = try {
        read[List[BulkDownloadCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BulkDownloadCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BulkDownloadCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BulkDownloadCreateData] = try {
        read[Map[String, BulkDownloadCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BulkDownloadCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BulkDownloadCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

