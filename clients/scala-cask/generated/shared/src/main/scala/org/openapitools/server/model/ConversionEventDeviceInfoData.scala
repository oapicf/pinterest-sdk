/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ConversionEventDeviceInfoData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ConversionEventDeviceInfoData(
/* Battery charge level percentage */
  batteryLevel: Int = 0 ,
/* Device brand */
  brand: String = "" ,
/* User device's mobile carrier. */
  carrier: String = "" ,
/* Number of CPU cores */
  cpuCores: Int = 0 ,
/* External storage size in GB */
  externalStorageFreeSpace: Int = 0 ,
/* External storage size in GB */
  externalStorageSize: Int = 0 ,
/* Device form factor */
  formFactor: FormFactor = null ,
/* Kernel version. Examples: Linux: 6.15. Obtain by running: uname -r MacOS: 24.3.0. Obtain by running: sysctl kern.version Android: 6.6. Obtain from OS.uname().release */
  kernelVersion: String = "" ,
/* List of user installed languages. ISO 639-1 format */
  languages: Seq[String] = Nil ,
/* Device locale BCP-47 format */
  locale: String = "" ,
/* Device model name */
  model: String = "" ,
/* Network type: 4G, 5G, ethernet, wifi In Android: NetworkCapabilities.getNetworkCapabilities() */
  networkType: NetworkType = null ,
/* OS Family */
  osFamily: OsFamily = null ,
/* Short name of the OS. This value if specific to os family. Examples: Windows: 10, 11; Android: 16; iOS: 18; MacOS: 15; Linux: Debian, Ubuntu, Arch */
  osName: String = "" ,
/* Marketing name for the release version iOS: Dawn Android: Baklava MacOS: Sequoia Ubuntu Linux: Plucky Puffin */
  osReleaseName: String = "" ,
/* Full name of the version. Examples: iOS: 18.3 Android: 16.1 MacOS: 15.5 Windows: 24H2 Ubuntu Linux: 25.04 */
  osVersion: String = "" ,
/* Screen density, PPI */
  screenDensity: Int = 0 ,
/* Screen height in pixels */
  screenHeight: Int = 0 ,
/* Screen width in pixels */
  screenWidth: Int = 0 ,
/* Internal storage size in GB */
  storageFreeSpace: Int = 0 ,
/* Internal storage size in GB */
  storageSize: Int = 0 ,
/* Device timezone */
  timezone: String = "" ,
/* Timezone abbreviation */
  timezoneAbbr: String = "" ,
/* Device type */
  `type`: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== batteryLevel validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(batteryLevel >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.batteryLevel, s"value '$batteryLevel' is not greater than the minimum value 0")
        }
        // validate against maximum 100
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(batteryLevel <= 100) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.batteryLevel, s"value '$batteryLevel' is not greater than the maximum value 100")
        }
        
        
        
        

        // ================== brand validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if brand == null then 0 else brand.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.brand, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== carrier validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if carrier == null then 0 else carrier.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.carrier, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== cpuCores validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(cpuCores >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.cpuCores, s"value '$cpuCores' is not greater than the minimum value 0")
        }
        // validate against maximum 1152
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(cpuCores <= 1152) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.cpuCores, s"value '$cpuCores' is not greater than the maximum value 1152")
        }
        
        
        
        

        // ================== externalStorageFreeSpace validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(externalStorageFreeSpace >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.externalStorageFreeSpace, s"value '$externalStorageFreeSpace' is not greater than the minimum value 0")
        }
        // validate against maximum 1048576
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(externalStorageFreeSpace <= 1048576) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.externalStorageFreeSpace, s"value '$externalStorageFreeSpace' is not greater than the maximum value 1048576")
        }
        
        
        
        

        // ================== externalStorageSize validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(externalStorageSize >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.externalStorageSize, s"value '$externalStorageSize' is not greater than the minimum value 0")
        }
        // validate against maximum 1048576
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(externalStorageSize <= 1048576) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.externalStorageSize, s"value '$externalStorageSize' is not greater than the maximum value 1048576")
        }
        
        
        
        

        // ================== formFactor validation ==================
        
        
        
        

        // ================== kernelVersion validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if kernelVersion == null then 0 else kernelVersion.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.kernelVersion, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== languages validation ==================
        
        // validate min items 0
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if languages == null then 0 else languages.size
            if (len < 0) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.languages, s"languages has $len, which is less than the min items 0")
            }
        }
        // validate min items 100
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if languages == null then 0 else languages.size
            if (len > 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.languages, s"languages has $len, which is greater than the max items 100")
            }
        }
        
        
        

        // ================== locale validation ==================
        // validate max length 35
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if locale == null then 0 else locale.length
            if (len < 35) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.locale, s"length $len is longer than the max length 35")
            }
        }
        
        
        
        

        // ================== model validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if model == null then 0 else model.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.model, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== networkType validation ==================
        
        
        
        

        // ================== osFamily validation ==================
        
        
        
        

        // ================== osName validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if osName == null then 0 else osName.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.osName, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== osReleaseName validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if osReleaseName == null then 0 else osReleaseName.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.osReleaseName, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== osVersion validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if osVersion == null then 0 else osVersion.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.osVersion, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== screenDensity validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenDensity >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenDensity, s"value '$screenDensity' is not greater than the minimum value 0")
        }
        // validate against maximum 100000
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenDensity <= 100000) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenDensity, s"value '$screenDensity' is not greater than the maximum value 100000")
        }
        
        
        
        

        // ================== screenHeight validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenHeight >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenHeight, s"value '$screenHeight' is not greater than the minimum value 0")
        }
        // validate against maximum 30720
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenHeight <= 30720) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenHeight, s"value '$screenHeight' is not greater than the maximum value 30720")
        }
        
        
        
        

        // ================== screenWidth validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenWidth >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenWidth, s"value '$screenWidth' is not greater than the minimum value 0")
        }
        // validate against maximum 30720
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(screenWidth <= 30720) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.screenWidth, s"value '$screenWidth' is not greater than the maximum value 30720")
        }
        
        
        
        

        // ================== storageFreeSpace validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(storageFreeSpace >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.storageFreeSpace, s"value '$storageFreeSpace' is not greater than the minimum value 0")
        }
        // validate against maximum 1048576
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(storageFreeSpace <= 1048576) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.storageFreeSpace, s"value '$storageFreeSpace' is not greater than the maximum value 1048576")
        }
        
        
        
        

        // ================== storageSize validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(storageSize >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.storageSize, s"value '$storageSize' is not greater than the minimum value 0")
        }
        // validate against maximum 1048576
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(storageSize <= 1048576) then
            _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.storageSize, s"value '$storageSize' is not greater than the maximum value 1048576")
        }
        
        
        
        

        // ================== timezone validation ==================
        // validate max length 40
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if timezone == null then 0 else timezone.length
            if (len < 40) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.timezone, s"length $len is longer than the max length 40")
            }
        }
        
        
        
        

        // ================== timezoneAbbr validation ==================
        // validate max length 5
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if timezoneAbbr == null then 0 else timezoneAbbr.length
            if (len < 5) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.timezoneAbbr, s"length $len is longer than the max length 5")
            }
        }
        
        
        
        

        // ================== `type` validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if `type` == null then 0 else `type`.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventDeviceInfo.Fields.`type`, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ConversionEventDeviceInfo] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ConversionEventDeviceInfo = {
    ConversionEventDeviceInfo(
        batteryLevel = Option(batteryLevel) /* 1 */,
        brand = Option(brand) /* 1 */,
        carrier = Option(carrier) /* 1 */,
        cpuCores = Option(cpuCores) /* 1 */,
        externalStorageFreeSpace = Option(externalStorageFreeSpace) /* 1 */,
        externalStorageSize = Option(externalStorageSize) /* 1 */,
        formFactor = Option(formFactor) /* 1 */,
        kernelVersion = Option(kernelVersion) /* 1 */,
        languages = languages /* 2 */,
        locale = Option(locale) /* 1 */,
        model = Option(model) /* 1 */,
        networkType = Option(networkType) /* 1 */,
        osFamily = Option(osFamily) /* 1 */,
        osName = Option(osName) /* 1 */,
        osReleaseName = Option(osReleaseName) /* 1 */,
        osVersion = Option(osVersion) /* 1 */,
        screenDensity = Option(screenDensity) /* 1 */,
        screenHeight = Option(screenHeight) /* 1 */,
        screenWidth = Option(screenWidth) /* 1 */,
        storageFreeSpace = Option(storageFreeSpace) /* 1 */,
        storageSize = Option(storageSize) /* 1 */,
        timezone = Option(timezone) /* 1 */,
        timezoneAbbr = Option(timezoneAbbr) /* 1 */,
        `type` = Option(`type`) /* 1 */
    
    )
  }
}

object ConversionEventDeviceInfoData {

  def validated(d8a : ConversionEventDeviceInfoData, failFast : Boolean) : scala.util.Try[ConversionEventDeviceInfo] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ConversionEventDeviceInfoData = try {
        val data = read[ConversionEventDeviceInfoData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ConversionEventDeviceInfoData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ConversionEventDeviceInfoData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ConversionEventDeviceInfoData] = try {
        read[List[ConversionEventDeviceInfoData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ConversionEventDeviceInfo]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ConversionEventDeviceInfo]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ConversionEventDeviceInfoData] = try {
        read[Map[String, ConversionEventDeviceInfoData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ConversionEventDeviceInfo]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ConversionEventDeviceInfo]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

