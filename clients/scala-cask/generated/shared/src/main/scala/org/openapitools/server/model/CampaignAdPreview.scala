/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using model.mustache
package org.openapitools.server.model

import scala.util.control.NonFatal

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        
case class CampaignAdPreview(
        /* Advertiser ID for this preview. */
    adAccountId: String,
        /* Ad group ID to create a preview record for. */
    adGroupId: String,
        /* Client ID that created preview. */
    clientId: Option[Int] = None ,
        /* Unix timestamp in milliseconds for preview expiration. */
    expiresAt: Int,
        /* Whether preview link is active. */
    isActive: Boolean,
        /* Pin ID for pin promotion preview. */
    pinId: Option[Int] = None ,
        /* Pin promotion ID for this preview. */
    pinPromotionId: Option[Int] = None ,
        /* Promoted product group ID for catalog previews. */
    promotedProductGroupId: Option[Int] = None ,
        /* Campaign ad preview URL. */
    url: String,
        /* User ID that created preview. */
    userId: Int,
        /* Pin promotion preview key. */
    uuid: String


) {

def asJsonString: String = asData.asJsonString
def asJson: ujson.Value = asData.asJson

def asData : CampaignAdPreviewData = {
CampaignAdPreviewData(
    adAccountId = adAccountId /* 2 */,
    adGroupId = adGroupId /* 2 */,
    clientId = clientId.getOrElse(0) /*  1 */,
    expiresAt = expiresAt /* 2 */,
    isActive = isActive /* 2 */,
    pinId = pinId.getOrElse(0) /*  1 */,
    pinPromotionId = pinPromotionId.getOrElse(0) /*  1 */,
    promotedProductGroupId = promotedProductGroupId.getOrElse(0) /*  1 */,
    url = url /* 2 */,
    userId = userId /* 2 */,
    uuid = uuid /* 2 */

)
}
}

object CampaignAdPreview {
given RW[CampaignAdPreview] = summon[RW[ujson.Value]].bimap[CampaignAdPreview](_.asJson, json => read[CampaignAdPreviewData](json).asModel)

enum Fields(val fieldName : String) extends Field(fieldName) {
    case adAccountId extends Fields("adAccountId")
    case adGroupId extends Fields("adGroupId")
    case clientId extends Fields("clientId")
    case expiresAt extends Fields("expiresAt")
    case isActive extends Fields("isActive")
    case pinId extends Fields("pinId")
    case pinPromotionId extends Fields("pinPromotionId")
    case promotedProductGroupId extends Fields("promotedProductGroupId")
    case url extends Fields("url")
    case userId extends Fields("userId")
    case uuid extends Fields("uuid")
}


}


