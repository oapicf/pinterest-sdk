/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ConversionEventsCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ConversionEventsCreateData(
/* A list of events (one or more) encapsulated by a data object. */
  data: Seq[ConversionEventsDataItemsData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== data validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (data == null) {
            _allValidationErrors += ValidationError(path :+ ConversionEventsCreate.Fields.data, "data is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if data == null then 0 else data.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ ConversionEventsCreate.Fields.data, s"data has $len, which is less than the min items 1")
            }
        }
        // validate min items 1000
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if data == null then 0 else data.size
            if (len > 1000) {
               _allValidationErrors += ValidationError(path :+ ConversionEventsCreate.Fields.data, s"data has $len, which is greater than the max items 1000")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (data != null) {
                data.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ConversionEventsCreate.Fields.data :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ConversionEventsCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ConversionEventsCreate = {
    ConversionEventsCreate(
        data = data.map(_.asModel) /* 5 */
    
    )
  }
}

object ConversionEventsCreateData {

  def validated(d8a : ConversionEventsCreateData, failFast : Boolean) : scala.util.Try[ConversionEventsCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ConversionEventsCreateData = try {
        val data = read[ConversionEventsCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ConversionEventsCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ConversionEventsCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ConversionEventsCreateData] = try {
        read[List[ConversionEventsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ConversionEventsCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ConversionEventsCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ConversionEventsCreateData] = try {
        read[Map[String, ConversionEventsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ConversionEventsCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ConversionEventsCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

