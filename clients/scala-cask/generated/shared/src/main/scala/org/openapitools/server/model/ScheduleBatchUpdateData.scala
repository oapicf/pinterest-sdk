/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ScheduleBatchUpdateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ScheduleBatchUpdateData(
  deltaValue: ScheduleDeltaValueData = null ,
/* Schedule end time. Unix timestamp in seconds. */
  endTimestamp: Int = 0 ,
/* entity ID. */
  entityId: String = "" ,
/* Specify the entity_type to get summary information */
  entityType: AdAccountEntityType = null ,
  id: String,
/* Schedule name. */
  name: String = "" ,
/* The schedule action. */
  scheduleAction: ScheduleAction = null ,
/* Schedule ID. */
  scheduleId: String,
/* Schedule status. */
  scheduleStatus: ScheduleStatus = null ,
/* The schedule type. */
  scheduleType: ScheduleType = null ,
/* Schedule start time. Unix timestamp in seconds. */
  startTimestamp: Int = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== deltaValue validation ==================
        
        
        
        
        // validating deltaValue
        if (_allValidationErrors.isEmpty || !failFast) {
            if deltaValue != null then _allValidationErrors ++= deltaValue.validationErrors(path :+ ScheduleBatchUpdate.Fields.deltaValue, failFast)
        }

        // ================== endTimestamp validation ==================
        
        
        
        

        // ================== entityId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if entityId == null || !regex.r.matches(entityId) then
              _allValidationErrors += ValidationError(path :+ ScheduleBatchUpdate.Fields.entityId, s"value '$entityId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== entityType validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ ScheduleBatchUpdate.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ ScheduleBatchUpdate.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== scheduleAction validation ==================
        
        
        
        

        // ================== scheduleId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if scheduleId == null || !regex.r.matches(scheduleId) then
              _allValidationErrors += ValidationError(path :+ ScheduleBatchUpdate.Fields.scheduleId, s"value '$scheduleId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== scheduleStatus validation ==================
        
        
        
        

        // ================== scheduleType validation ==================
        
        
        
        

        // ================== startTimestamp validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ScheduleBatchUpdate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ScheduleBatchUpdate = {
    ScheduleBatchUpdate(
        deltaValue = Option(deltaValue).map(_.asModel) /* 4 */,
        endTimestamp = Option(endTimestamp) /* 1 */,
        entityId = Option(entityId) /* 1 */,
        entityType = Option(entityType) /* 1 */,
        id = id /* 2 */,
        name = Option(name) /* 1 */,
        scheduleAction = Option(scheduleAction) /* 1 */,
        scheduleId = scheduleId /* 2 */,
        scheduleStatus = Option(scheduleStatus) /* 1 */,
        scheduleType = Option(scheduleType) /* 1 */,
        startTimestamp = Option(startTimestamp) /* 1 */
    
    )
  }
}

object ScheduleBatchUpdateData {

  def validated(d8a : ScheduleBatchUpdateData, failFast : Boolean) : scala.util.Try[ScheduleBatchUpdate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ScheduleBatchUpdateData = try {
        val data = read[ScheduleBatchUpdateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ScheduleBatchUpdateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ScheduleBatchUpdateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ScheduleBatchUpdateData] = try {
        read[List[ScheduleBatchUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ScheduleBatchUpdate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ScheduleBatchUpdate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ScheduleBatchUpdateData] = try {
        read[Map[String, ScheduleBatchUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ScheduleBatchUpdate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ScheduleBatchUpdate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

