/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.math.BigDecimal
import ujson.Value
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CustomerListData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CustomerListData(
/* Associated ad account ID. */
  adAccountId: String = "" ,
/* Creation time. Unix timestamp in seconds. */
  createdTime: BigDecimal = 0 ,
/* Customer list errors. */
  exceptions: Value = ujson.Null ,
/* Customer list ID. */
  id: String,
/* Whether the list was uploaded for new customer acquisition (expanded matching). Immutable after creation. */
  isNca: Boolean = false ,
/* Customer list name. */
  name: String,
/* Total number of list updates. List creation counts as one batch. Each [Append](/docs/redoc/#operation/ads_v3_customer_list_add_handler_PUT) or [Remove API](/docs/redoc/#operation/ads_v3_customer_list_remove_handler_PUT) call counts as another. List creation via the **Ads Manager** UI could result in more than one batch since the UI breaks up large lists. */
  numBatches: BigDecimal = 0 ,
/* Number of removed user records. In a [Remove API](/docs/redoc/#operation/ads_v3_customer_list_remove_handler_PUT) call, this counter increases even if the user is not found in the list. */
  numRemovedUserRecords: BigDecimal = 0 ,
/* Number of uploaded user records. In an [Append API](/docs/redoc/#operation/ads_v3_customer_list_add_handler_PUT) call, this counter increases even if the uploaded user is already in the list. */
  numUploadedUserRecords: BigDecimal = 0 ,
/* Customer list status. `TOO_SMALL` means the list has fewer than 100 Pinterest users. */
  status: CustomerListStatus = null ,
/* Always `customerlist`. */
  `type`: String = "" ,
/* Last update time. Unix timestamp in seconds. */
  updatedTime: BigDecimal = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== exceptions validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ CustomerList.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ CustomerList.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== isNca validation ==================
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== numBatches validation ==================
        
        
        
        

        // ================== numRemovedUserRecords validation ==================
        
        
        
        

        // ================== numUploadedUserRecords validation ==================
        
        
        
        

        // ================== status validation ==================
        
        
        
        

        // ================== `type` validation ==================
        
        
        
        

        // ================== updatedTime validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CustomerList] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CustomerList = {
    CustomerList(
        adAccountId = Option(adAccountId) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        exceptions = Option(exceptions) /* 1 */,
        id = id /* 2 */,
        isNca = Option(isNca) /* 1 */,
        name = name /* 2 */,
        numBatches = Option(numBatches) /* 1 */,
        numRemovedUserRecords = Option(numRemovedUserRecords) /* 1 */,
        numUploadedUserRecords = Option(numUploadedUserRecords) /* 1 */,
        status = Option(status) /* 1 */,
        `type` = Option(`type`) /* 1 */,
        updatedTime = Option(updatedTime) /* 1 */
    
    )
  }
}

object CustomerListData {

  def validated(d8a : CustomerListData, failFast : Boolean) : scala.util.Try[CustomerList] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CustomerListData = try {
        val data = read[CustomerListData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CustomerListData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CustomerListData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CustomerListData] = try {
        read[List[CustomerListData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CustomerList]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CustomerList]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CustomerListData] = try {
        read[Map[String, CustomerListData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CustomerList]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CustomerList]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

