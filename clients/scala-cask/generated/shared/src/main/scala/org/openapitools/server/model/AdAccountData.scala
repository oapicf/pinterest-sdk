/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AdAccountData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AdAccountData(
  country: Country = null ,
/*  Creation time. Unix timestamp in seconds. */
  createdTime: Int = 0 ,
  currency: Currency = null ,
  id: String,
/* Ad account name. */
  name: String = "" ,
/* Ad account owner */
  owner: AdAccountOwnerData = null ,
  permissions: Seq[BusinessAccessRole] = Nil ,
/* The time zone of the ad account, in IANA format (e.g., \"America/Los_Angeles\"). Adding your local time zone lets you view your campaigns and ad reporting in your preferred time zone. Future reports will be available in both your local time zone and default UTC time zone. Historical data takes 1-2 months to backfill. Your billing and order lines will remain in UTC. */
  timeZone: String = "" ,
  updatedTime: Int = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== country validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== currency validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ AdAccount.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ AdAccount.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== name validation ==================
        // validate max length 256
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 256) {
               _allValidationErrors += ValidationError(path :+ AdAccount.Fields.name, s"length $len is longer than the max length 256")
            }
        }
        
        
        
        

        // ================== owner validation ==================
        
        
        
        
        // validating owner
        if (_allValidationErrors.isEmpty || !failFast) {
            if owner != null then _allValidationErrors ++= owner.validationErrors(path :+ AdAccount.Fields.owner, failFast)
        }

        // ================== permissions validation ==================
        
        
        
        

        // ================== timeZone validation ==================
        
        
        
        

        // ================== updatedTime validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AdAccount] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AdAccount = {
    AdAccount(
        country = Option(country) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        currency = Option(currency) /* 1 */,
        id = id /* 2 */,
        name = Option(name) /* 1 */,
        owner = Option(owner).map(_.asModel) /* 4 */,
        permissions = permissions.map(_.asModel) /* 5 */,
        timeZone = Option(timeZone) /* 1 */,
        updatedTime = Option(updatedTime) /* 1 */
    
    )
  }
}

object AdAccountData {

  def validated(d8a : AdAccountData, failFast : Boolean) : scala.util.Try[AdAccount] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AdAccountData = try {
        val data = read[AdAccountData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AdAccountData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AdAccountData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AdAccountData] = try {
        read[List[AdAccountData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AdAccount]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AdAccount]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AdAccountData] = try {
        read[Map[String, AdAccountData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AdAccount]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AdAccount]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

