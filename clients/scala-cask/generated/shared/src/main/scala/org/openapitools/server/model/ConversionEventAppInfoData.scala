/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ConversionEventAppInfoData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ConversionEventAppInfoData(
/* App ID in Google Play Store, AppStore or other stores. */
  appId: String = "" ,
/* Name of the app. Primarily used for Mobile Apps. */
  appName: String = "" ,
/* App package name */
  appPackageName: String = "" ,
/* The name of the app distributor or store from which the app was installed. Some options: Samsung Galaxy Store, Google Play Store, Amazon Store, Apple App Store, F-Droid, Aptoide, Obtanium, Huawei AppGallery, Xiaomi Mi GetApps */
  appStore: String = "" ,
/* App version. Primarily used for mobile apps */
  appVersion: String = "" ,
/* App install time. Unix timestamp in seconds */
  installTime: Int = 0 ,
/* User Agent request header. Primarily used for Web events */
  userAgent: String = "" ,
/* Inner height of the window or viewport. */
  windowHeight: Int = 0 ,
/* Inner width of the window or viewport. */
  windowWidth: Int = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== appId validation ==================
        // validate max length 200
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appId == null then 0 else appId.length
            if (len < 200) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.appId, s"length $len is longer than the max length 200")
            }
        }
        
        
        
        

        // ================== appName validation ==================
        // validate max length 200
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appName == null then 0 else appName.length
            if (len < 200) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.appName, s"length $len is longer than the max length 200")
            }
        }
        
        
        
        

        // ================== appPackageName validation ==================
        // validate max length 200
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appPackageName == null then 0 else appPackageName.length
            if (len < 200) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.appPackageName, s"length $len is longer than the max length 200")
            }
        }
        
        
        
        

        // ================== appStore validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appStore == null then 0 else appStore.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.appStore, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== appVersion validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if appVersion == null then 0 else appVersion.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.appVersion, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== installTime validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(installTime >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.installTime, s"value '$installTime' is not greater than the minimum value 0")
        }
        
        
        
        

        // ================== userAgent validation ==================
        // validate max length 16384
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if userAgent == null then 0 else userAgent.length
            if (len < 16384) {
               _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.userAgent, s"length $len is longer than the max length 16384")
            }
        }
        
        
        
        

        // ================== windowHeight validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(windowHeight >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.windowHeight, s"value '$windowHeight' is not greater than the minimum value 0")
        }
        // validate against maximum 30720
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(windowHeight <= 30720) then
            _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.windowHeight, s"value '$windowHeight' is not greater than the maximum value 30720")
        }
        
        
        
        

        // ================== windowWidth validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(windowWidth >= 0) then
            _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.windowWidth, s"value '$windowWidth' is not greater than the minimum value 0")
        }
        // validate against maximum 30720
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(windowWidth <= 30720) then
            _allValidationErrors += ValidationError(path :+ ConversionEventAppInfo.Fields.windowWidth, s"value '$windowWidth' is not greater than the maximum value 30720")
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ConversionEventAppInfo] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ConversionEventAppInfo = {
    ConversionEventAppInfo(
        appId = Option(appId) /* 1 */,
        appName = Option(appName) /* 1 */,
        appPackageName = Option(appPackageName) /* 1 */,
        appStore = Option(appStore) /* 1 */,
        appVersion = Option(appVersion) /* 1 */,
        installTime = Option(installTime) /* 1 */,
        userAgent = Option(userAgent) /* 1 */,
        windowHeight = Option(windowHeight) /* 1 */,
        windowWidth = Option(windowWidth) /* 1 */
    
    )
  }
}

object ConversionEventAppInfoData {

  def validated(d8a : ConversionEventAppInfoData, failFast : Boolean) : scala.util.Try[ConversionEventAppInfo] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ConversionEventAppInfoData = try {
        val data = read[ConversionEventAppInfoData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ConversionEventAppInfoData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ConversionEventAppInfoData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ConversionEventAppInfoData] = try {
        read[List[ConversionEventAppInfoData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ConversionEventAppInfo]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ConversionEventAppInfo]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ConversionEventAppInfoData] = try {
        read[Map[String, ConversionEventAppInfoData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ConversionEventAppInfo]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ConversionEventAppInfo]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

