/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PinUpdateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PinUpdateData(
/* AI disclosure declarations the creator has made about this Pin. */
  aiDisclosures: AiDisclosuresUpdateData = null ,
  altText: String = "" ,
/* The board to which this Pin belongs. */
  boardId: String = "" ,
/* The board section to which this Pin belongs. */
  boardSectionId: String = "" ,
/* Carousel Pin slots data. */
  carouselSlots: Seq[CarouselSlotData] = Nil ,
  description: String = "" ,
  link: String = "" ,
  title: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== aiDisclosures validation ==================
        
        
        
        
        // validating aiDisclosures
        if (_allValidationErrors.isEmpty || !failFast) {
            if aiDisclosures != null then _allValidationErrors ++= aiDisclosures.validationErrors(path :+ PinUpdate.Fields.aiDisclosures, failFast)
        }

        // ================== altText validation ==================
        // validate max length 500
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if altText == null then 0 else altText.length
            if (len < 500) {
               _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.altText, s"length $len is longer than the max length 500")
            }
        }
        
        
        
        

        // ================== boardId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if boardId == null || !regex.r.matches(boardId) then
              _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.boardId, s"value '$boardId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== boardSectionId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if boardSectionId == null || !regex.r.matches(boardSectionId) then
              _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.boardSectionId, s"value '$boardSectionId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== carouselSlots validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (carouselSlots != null) {
                carouselSlots.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PinUpdate.Fields.carouselSlots :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== description validation ==================
        // validate max length 800
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if description == null then 0 else description.length
            if (len < 800) {
               _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.description, s"length $len is longer than the max length 800")
            }
        }
        
        
        
        

        // ================== link validation ==================
        // validate max length 2048
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if link == null then 0 else link.length
            if (len < 2048) {
               _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.link, s"length $len is longer than the max length 2048")
            }
        }
        
        
        
        

        // ================== title validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if title == null then 0 else title.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PinUpdate.Fields.title, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PinUpdate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PinUpdate = {
    PinUpdate(
        aiDisclosures = Option(aiDisclosures).map(_.asModel) /* 4 */,
        altText = Option(altText) /* 1 */,
        boardId = Option(boardId) /* 1 */,
        boardSectionId = Option(boardSectionId) /* 1 */,
        carouselSlots = carouselSlots.map(_.asModel) /* 5 */,
        description = Option(description) /* 1 */,
        link = Option(link) /* 1 */,
        title = Option(title) /* 1 */
    
    )
  }
}

object PinUpdateData {

  def validated(d8a : PinUpdateData, failFast : Boolean) : scala.util.Try[PinUpdate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PinUpdateData = try {
        val data = read[PinUpdateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PinUpdateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PinUpdateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PinUpdateData] = try {
        read[List[PinUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PinUpdate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PinUpdate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PinUpdateData] = try {
        read[Map[String, PinUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PinUpdate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PinUpdate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

