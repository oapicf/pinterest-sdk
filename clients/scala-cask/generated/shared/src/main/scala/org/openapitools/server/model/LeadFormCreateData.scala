/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LeadFormCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LeadFormCreateData(
/* A message for people who complete the form to let them know what happens next. */
  completionMessage: String,
/* Additional disclosure language to be included in the lead form. */
  disclosureLanguage: String = "" ,
/* Whether the advertiser has accepted Pinterest's terms of service for creating a lead ad.  By sending us TRUE for this parameter, you agree that (i) you will use any personal information received in compliance with the privacy policy you share with Pinterest, and (ii) you will comply with Pinterest's [Lead Ad Terms](https://policy.pinterest.com/en/lead-ad-terms). As a reminder, all advertising on Pinterest is subject to the [Pinterest Advertising Services Agreement](https://business.pinterest.com/en/pinterest-advertising-services-agreement/) or an equivalent agreement as set forth on an IO */
  hasAcceptedTerms: Boolean,
/* Internal name of the lead form. */
  name: String,
/* List of additional policy links to be displayed on the lead form. */
  policyLinks: Seq[LeadFormPolicyLinkData] = Nil ,
/* A link to the advertiser's privacy policy. This will be included in the lead form's disclosure language. */
  privacyPolicyLink: String,
/* List of questions to be displayed on the lead form. */
  questions: Seq[LeadFormQuestionData],
  status: LeadFormStatus = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== completionMessage validation ==================
        
        
        
        

        // ================== disclosureLanguage validation ==================
        
        
        
        

        // ================== hasAcceptedTerms validation ==================
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== policyLinks validation ==================
        
        // validate min items 0
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if policyLinks == null then 0 else policyLinks.size
            if (len < 0) {
               _allValidationErrors += ValidationError(path :+ LeadFormCreate.Fields.policyLinks, s"policyLinks has $len, which is less than the min items 0")
            }
        }
        // validate min items 3
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if policyLinks == null then 0 else policyLinks.size
            if (len > 3) {
               _allValidationErrors += ValidationError(path :+ LeadFormCreate.Fields.policyLinks, s"policyLinks has $len, which is greater than the max items 3")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (policyLinks != null) {
                policyLinks.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ LeadFormCreate.Fields.policyLinks :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== privacyPolicyLink validation ==================
        
        
        
        

        // ================== questions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (questions == null) {
            _allValidationErrors += ValidationError(path :+ LeadFormCreate.Fields.questions, "questions is a required field and cannot be null")
            }
        }
        
        // validate min items 0
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if questions == null then 0 else questions.size
            if (len < 0) {
               _allValidationErrors += ValidationError(path :+ LeadFormCreate.Fields.questions, s"questions has $len, which is less than the min items 0")
            }
        }
        // validate min items 10
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if questions == null then 0 else questions.size
            if (len > 10) {
               _allValidationErrors += ValidationError(path :+ LeadFormCreate.Fields.questions, s"questions has $len, which is greater than the max items 10")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (questions != null) {
                questions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ LeadFormCreate.Fields.questions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== status validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LeadFormCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LeadFormCreate = {
    LeadFormCreate(
        completionMessage = completionMessage /* 2 */,
        disclosureLanguage = Option(disclosureLanguage) /* 1 */,
        hasAcceptedTerms = hasAcceptedTerms /* 2 */,
        name = name /* 2 */,
        policyLinks = policyLinks.map(_.asModel) /* 5 */,
        privacyPolicyLink = privacyPolicyLink /* 2 */,
        questions = questions.map(_.asModel) /* 5 */,
        status = Option(status) /* 1 */
    
    )
  }
}

object LeadFormCreateData {

  def validated(d8a : LeadFormCreateData, failFast : Boolean) : scala.util.Try[LeadFormCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LeadFormCreateData = try {
        val data = read[LeadFormCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LeadFormCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LeadFormCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LeadFormCreateData] = try {
        read[List[LeadFormCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LeadFormCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LeadFormCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LeadFormCreateData] = try {
        read[Map[String, LeadFormCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LeadFormCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LeadFormCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

