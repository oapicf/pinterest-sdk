/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AssetIdPermissionsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AssetIdPermissionsData(
/* An object containing all the information specific to the provided asset group. This field will be populated only if asset_type equals 'ASSET_GROUP'. */
  assetGroupInfo: AssetGroupBindingData = null ,
/* Unique identifier of a business asset. */
  assetId: String,
  assetType: AssetTypeResponse,
/* Permission levels member or partner has on an asset. */
  permissions: Seq[String]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== assetGroupInfo validation ==================
        
        
        
        
        // validating assetGroupInfo
        if (_allValidationErrors.isEmpty || !failFast) {
            if assetGroupInfo != null then _allValidationErrors ++= assetGroupInfo.validationErrors(path :+ AssetIdPermissions.Fields.assetGroupInfo, failFast)
        }

        // ================== assetId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if assetId == null || !regex.r.matches(assetId) then
              _allValidationErrors += ValidationError(path :+ AssetIdPermissions.Fields.assetId, s"value '$assetId' doesn't match pattern $regex")
        }
        // validate min length 1
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AssetIdPermissions.Fields.assetId, s"length $len is shorter than the min length 1")
            }
        }
        // validate max length 20
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 20) {
               _allValidationErrors += ValidationError(path :+ AssetIdPermissions.Fields.assetId, s"length $len is longer than the max length 20")
            }
        }
        
        
        
        

        // ================== assetType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (assetType == null) {
            _allValidationErrors += ValidationError(path :+ AssetIdPermissions.Fields.assetType, "assetType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== permissions validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AssetIdPermissions] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AssetIdPermissions = {
    AssetIdPermissions(
        assetGroupInfo = Option(assetGroupInfo).map(_.asModel) /* 4 */,
        assetId = assetId /* 2 */,
        assetType = assetType /* 2 */,
        permissions = permissions /* 2 */
    
    )
  }
}

object AssetIdPermissionsData {

  def validated(d8a : AssetIdPermissionsData, failFast : Boolean) : scala.util.Try[AssetIdPermissions] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AssetIdPermissionsData = try {
        val data = read[AssetIdPermissionsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AssetIdPermissionsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AssetIdPermissionsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AssetIdPermissionsData] = try {
        read[List[AssetIdPermissionsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AssetIdPermissions]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AssetIdPermissions]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AssetIdPermissionsData] = try {
        read[Map[String, AssetIdPermissionsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AssetIdPermissions]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AssetIdPermissions]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

