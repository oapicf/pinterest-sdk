/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** MMMReportCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class MMMReportCreateData(
/* Advertiser IDs for multi-advertiser report */
  advertiserIds: Seq[String] = Nil ,
/* Metric and entity columns */
  columns: Seq[MMMReportingColumn],
/* A List of countries for filtering */
  countries: Seq[TargetingAdvertiserCountry] = Nil ,
/* List of custom column IDs */
  customColumnIds: Seq[String] = Nil ,
/* Metric report end date (UTC). Format: YYYY-MM-DD */
  endDate: String,
/*   DAY - metrics are broken down daily.    WEEK - metrics are broken down weekly. */
  granularity: MMMReportGranularity,
/* Level of the report */
  level: MMMReportLevel,
/* Name of the Marketing Mix Modeling (MMM) report */
  reportName: String,
/* Metric report start date (UTC). Format: YYYY-MM-DD */
  startDate: String,
/* List of targeting types */
  targetingTypes: Seq[MMMReportingTargetingType]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== advertiserIds validation ==================
        
        
        
        

        // ================== columns validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (columns == null) {
            _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.columns, "columns is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== countries validation ==================
        
        
        
        

        // ================== customColumnIds validation ==================
        
        // validate min items 20
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if customColumnIds == null then 0 else customColumnIds.size
            if (len > 20) {
               _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.customColumnIds, s"customColumnIds has $len, which is greater than the max items 20")
            }
        }
        
        
        

        // ================== endDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if endDate == null || !regex.r.matches(endDate) then
              _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.endDate, s"value '$endDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== granularity validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (granularity == null) {
            _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.granularity, "granularity is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== level validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (level == null) {
            _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.level, "level is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== reportName validation ==================
        
        
        
        

        // ================== startDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if startDate == null || !regex.r.matches(startDate) then
              _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.startDate, s"value '$startDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== targetingTypes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (targetingTypes == null) {
            _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.targetingTypes, "targetingTypes is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if targetingTypes == null then 0 else targetingTypes.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.targetingTypes, s"targetingTypes has $len, which is less than the min items 1")
            }
        }
        // validate min items 7
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if targetingTypes == null then 0 else targetingTypes.size
            if (len > 7) {
               _allValidationErrors += ValidationError(path :+ MMMReportCreate.Fields.targetingTypes, s"targetingTypes has $len, which is greater than the max items 7")
            }
        }
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[MMMReportCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : MMMReportCreate = {
    MMMReportCreate(
        advertiserIds = advertiserIds /* 2 */,
        columns = columns.map(_.asModel) /* 5 */,
        countries = countries.map(_.asModel) /* 5 */,
        customColumnIds = customColumnIds /* 2 */,
        endDate = endDate /* 2 */,
        granularity = granularity /* 2 */,
        level = level /* 2 */,
        reportName = reportName /* 2 */,
        startDate = startDate /* 2 */,
        targetingTypes = targetingTypes.map(_.asModel) /* 5 */
    
    )
  }
}

object MMMReportCreateData {

  def validated(d8a : MMMReportCreateData, failFast : Boolean) : scala.util.Try[MMMReportCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : MMMReportCreateData = try {
        val data = read[MMMReportCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating MMMReportCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : MMMReportCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[MMMReportCreateData] = try {
        read[List[MMMReportCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[MMMReportCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[MMMReportCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, MMMReportCreateData] = try {
        read[Map[String, MMMReportCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, MMMReportCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, MMMReportCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

