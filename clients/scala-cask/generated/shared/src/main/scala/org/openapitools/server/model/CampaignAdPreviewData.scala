/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CampaignAdPreviewData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CampaignAdPreviewData(
/* Advertiser ID for this preview. */
  adAccountId: String,
/* Ad group ID to create a preview record for. */
  adGroupId: String,
/* Client ID that created preview. */
  clientId: Int = 0 ,
/* Unix timestamp in milliseconds for preview expiration. */
  expiresAt: Int,
/* Whether preview link is active. */
  isActive: Boolean,
/* Pin ID for pin promotion preview. */
  pinId: Int = 0 ,
/* Pin promotion ID for this preview. */
  pinPromotionId: Int = 0 ,
/* Promoted product group ID for catalog previews. */
  promotedProductGroupId: Int = 0 ,
/* Campaign ad preview URL. */
  url: String,
/* User ID that created preview. */
  userId: Int,
/* Pin promotion preview key. */
  uuid: String
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ CampaignAdPreview.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== adGroupId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adGroupId == null || !regex.r.matches(adGroupId) then
              _allValidationErrors += ValidationError(path :+ CampaignAdPreview.Fields.adGroupId, s"value '$adGroupId' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if adGroupId == null then 0 else adGroupId.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ CampaignAdPreview.Fields.adGroupId, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== clientId validation ==================
        
        
        
        

        // ================== expiresAt validation ==================
        
        
        
        

        // ================== isActive validation ==================
        
        
        
        

        // ================== pinId validation ==================
        
        
        
        

        // ================== pinPromotionId validation ==================
        
        
        
        

        // ================== promotedProductGroupId validation ==================
        
        
        
        

        // ================== url validation ==================
        
        
        
        

        // ================== userId validation ==================
        
        
        
        

        // ================== uuid validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CampaignAdPreview] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CampaignAdPreview = {
    CampaignAdPreview(
        adAccountId = adAccountId /* 2 */,
        adGroupId = adGroupId /* 2 */,
        clientId = Option(clientId) /* 1 */,
        expiresAt = expiresAt /* 2 */,
        isActive = isActive /* 2 */,
        pinId = Option(pinId) /* 1 */,
        pinPromotionId = Option(pinPromotionId) /* 1 */,
        promotedProductGroupId = Option(promotedProductGroupId) /* 1 */,
        url = url /* 2 */,
        userId = userId /* 2 */,
        uuid = uuid /* 2 */
    
    )
  }
}

object CampaignAdPreviewData {

  def validated(d8a : CampaignAdPreviewData, failFast : Boolean) : scala.util.Try[CampaignAdPreview] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CampaignAdPreviewData = try {
        val data = read[CampaignAdPreviewData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CampaignAdPreviewData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CampaignAdPreviewData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CampaignAdPreviewData] = try {
        read[List[CampaignAdPreviewData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CampaignAdPreview]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CampaignAdPreview]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CampaignAdPreviewData] = try {
        read[Map[String, CampaignAdPreviewData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CampaignAdPreview]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CampaignAdPreview]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

