/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AuthRespondInvitesBodyItemData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AuthRespondInvitesBodyItemData(
  action: AuthRespondInviteActionData,
/* Unique identifier of an invite. */
  inviteId: String
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== action validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (action == null) {
            _allValidationErrors += ValidationError(path :+ AuthRespondInvitesBodyItem.Fields.action, "action is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating action
        if (_allValidationErrors.isEmpty || !failFast) {
            if action != null then _allValidationErrors ++= action.validationErrors(path :+ AuthRespondInvitesBodyItem.Fields.action, failFast)
        }

        // ================== inviteId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if inviteId == null || !regex.r.matches(inviteId) then
              _allValidationErrors += ValidationError(path :+ AuthRespondInvitesBodyItem.Fields.inviteId, s"value '$inviteId' doesn't match pattern $regex")
        }
        // validate min length 1
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if inviteId == null then 0 else inviteId.length
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AuthRespondInvitesBodyItem.Fields.inviteId, s"length $len is shorter than the min length 1")
            }
        }
        // validate max length 25
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if inviteId == null then 0 else inviteId.length
            if (len < 25) {
               _allValidationErrors += ValidationError(path :+ AuthRespondInvitesBodyItem.Fields.inviteId, s"length $len is longer than the max length 25")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AuthRespondInvitesBodyItem] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AuthRespondInvitesBodyItem = {
    AuthRespondInvitesBodyItem(
        action = action.asModel /* 6 */,
        inviteId = inviteId /* 2 */
    
    )
  }
}

object AuthRespondInvitesBodyItemData {

  def validated(d8a : AuthRespondInvitesBodyItemData, failFast : Boolean) : scala.util.Try[AuthRespondInvitesBodyItem] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AuthRespondInvitesBodyItemData = try {
        val data = read[AuthRespondInvitesBodyItemData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AuthRespondInvitesBodyItemData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AuthRespondInvitesBodyItemData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AuthRespondInvitesBodyItemData] = try {
        read[List[AuthRespondInvitesBodyItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AuthRespondInvitesBodyItem]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AuthRespondInvitesBodyItem]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AuthRespondInvitesBodyItemData] = try {
        read[Map[String, AuthRespondInvitesBodyItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AuthRespondInvitesBodyItem]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AuthRespondInvitesBodyItem]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

