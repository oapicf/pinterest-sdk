/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** BusinessMembersAssetAccessDeleteBodyData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BusinessMembersAssetAccessDeleteBodyData(
/* List of members asset access to be deleted */
  accesses: Seq[DeleteMemberAssetAccessItemData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== accesses validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (accesses == null) {
            _allValidationErrors += ValidationError(path :+ BusinessMembersAssetAccessDeleteBody.Fields.accesses, "accesses is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if accesses == null then 0 else accesses.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ BusinessMembersAssetAccessDeleteBody.Fields.accesses, s"accesses has $len, which is less than the min items 1")
            }
        }
        // validate min items 100
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if accesses == null then 0 else accesses.size
            if (len > 100) {
               _allValidationErrors += ValidationError(path :+ BusinessMembersAssetAccessDeleteBody.Fields.accesses, s"accesses has $len, which is greater than the max items 100")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (accesses != null) {
                accesses.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ BusinessMembersAssetAccessDeleteBody.Fields.accesses :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[BusinessMembersAssetAccessDeleteBody] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BusinessMembersAssetAccessDeleteBody = {
    BusinessMembersAssetAccessDeleteBody(
        accesses = accesses.map(_.asModel) /* 5 */
    
    )
  }
}

object BusinessMembersAssetAccessDeleteBodyData {

  def validated(d8a : BusinessMembersAssetAccessDeleteBodyData, failFast : Boolean) : scala.util.Try[BusinessMembersAssetAccessDeleteBody] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : BusinessMembersAssetAccessDeleteBodyData = try {
        val data = read[BusinessMembersAssetAccessDeleteBodyData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating BusinessMembersAssetAccessDeleteBodyData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : BusinessMembersAssetAccessDeleteBodyData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[BusinessMembersAssetAccessDeleteBodyData] = try {
        read[List[BusinessMembersAssetAccessDeleteBodyData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BusinessMembersAssetAccessDeleteBody]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BusinessMembersAssetAccessDeleteBody]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BusinessMembersAssetAccessDeleteBodyData] = try {
        read[Map[String, BusinessMembersAssetAccessDeleteBodyData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BusinessMembersAssetAccessDeleteBody]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BusinessMembersAssetAccessDeleteBody]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

