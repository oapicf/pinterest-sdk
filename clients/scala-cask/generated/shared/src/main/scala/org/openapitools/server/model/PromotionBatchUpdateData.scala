/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PromotionBatchUpdateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PromotionBatchUpdateData(
  discountStatus: DiscountStatus = null ,
/* Promotion end time. Unix timestamp in seconds. Independent of campaign end time. */
  endTime: Int = 0 ,
/* Platform-specific ID for this promotion. Will be null for promotions first created within Pinterest. */
  externalId: String = "" ,
/* Promotion ID */
  id: String,
/* The source integration platform used when creating the promotion. Currently supported values are 'DEFAULT' and 'SHOPIFY'. */
  platformType: String = "" ,
/* Code that can be used to redeem a promotion. */
  promotionCode: String = "" ,
/* An optional field for user defined promotion ID for this promotion. Will copy from Pinterest system generated ID if user did not provide one. */
  promotionCustomId: String = "" ,
/* Internal name for the promotion. */
  promotionTitle: String = "" ,
  promotionType: PromotionType = null ,
/* Promotion start time. Unix timestamp in seconds. Independent of campaign start time. */
  startTime: Int = 0 ,
/* List of values to be inserted in the promotion type-specific template. */
  templateValues: Seq[PromotionTemplateValueData] = Nil 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== discountStatus validation ==================
        
        
        
        

        // ================== endTime validation ==================
        
        
        
        

        // ================== externalId validation ==================
        // validate max length 64
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if externalId == null then 0 else externalId.length
            if (len < 64) {
               _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.externalId, s"length $len is longer than the max length 64")
            }
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== platformType validation ==================
        
        
        
        

        // ================== promotionCode validation ==================
        
        
        
        

        // ================== promotionCustomId validation ==================
        // validate max length 50
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if promotionCustomId == null then 0 else promotionCustomId.length
            if (len < 50) {
               _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.promotionCustomId, s"length $len is longer than the max length 50")
            }
        }
        
        
        
        

        // ================== promotionTitle validation ==================
        
        
        
        

        // ================== promotionType validation ==================
        
        
        
        

        // ================== startTime validation ==================
        
        
        
        

        // ================== templateValues validation ==================
        
        // validate min items 0
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if templateValues == null then 0 else templateValues.size
            if (len < 0) {
               _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.templateValues, s"templateValues has $len, which is less than the min items 0")
            }
        }
        // validate min items 2
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if templateValues == null then 0 else templateValues.size
            if (len > 2) {
               _allValidationErrors += ValidationError(path :+ PromotionBatchUpdate.Fields.templateValues, s"templateValues has $len, which is greater than the max items 2")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (templateValues != null) {
                templateValues.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PromotionBatchUpdate.Fields.templateValues :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PromotionBatchUpdate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PromotionBatchUpdate = {
    PromotionBatchUpdate(
        discountStatus = Option(discountStatus) /* 1 */,
        endTime = Option(endTime) /* 1 */,
        externalId = Option(externalId) /* 1 */,
        id = id /* 2 */,
        platformType = Option(platformType) /* 1 */,
        promotionCode = Option(promotionCode) /* 1 */,
        promotionCustomId = Option(promotionCustomId) /* 1 */,
        promotionTitle = Option(promotionTitle) /* 1 */,
        promotionType = Option(promotionType) /* 1 */,
        startTime = Option(startTime) /* 1 */,
        templateValues = templateValues.map(_.asModel) /* 5 */
    
    )
  }
}

object PromotionBatchUpdateData {

  def validated(d8a : PromotionBatchUpdateData, failFast : Boolean) : scala.util.Try[PromotionBatchUpdate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PromotionBatchUpdateData = try {
        val data = read[PromotionBatchUpdateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PromotionBatchUpdateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PromotionBatchUpdateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PromotionBatchUpdateData] = try {
        read[List[PromotionBatchUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PromotionBatchUpdate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PromotionBatchUpdate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PromotionBatchUpdateData] = try {
        read[Map[String, PromotionBatchUpdateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PromotionBatchUpdate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PromotionBatchUpdate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

