/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** KeywordData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class KeywordData(
  archived: Boolean = false ,
/* **Note:** bid field has been deprecated. Input will not be set and field will return null. Keyword custom bid in microcurrency - null if inherited from parent ad group. */
  bid: Int = 0 ,
/* Keyword ID . */
  id: String,
/* Keyword [match type](/docs/api-features/targeting-overview/) */
  matchType: MatchType,
/* Keyword parent entity ID (advertiser, campaign, ad group). */
  parentId: String,
/* Parent entity type (advertiser, campaign, ad group). */
  parentType: String = "" ,
/* Always keyword */
  `type`: String = "" ,
/* Keyword value (120 chars max). */
  value: String
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== archived validation ==================
        
        
        
        

        // ================== bid validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ Keyword.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== matchType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (matchType == null) {
            _allValidationErrors += ValidationError(path :+ Keyword.Fields.matchType, "matchType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== parentId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if parentId == null || !regex.r.matches(parentId) then
              _allValidationErrors += ValidationError(path :+ Keyword.Fields.parentId, s"value '$parentId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== parentType validation ==================
        
        
        
        

        // ================== `type` validation ==================
        
        
        
        

        // ================== value validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[Keyword] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : Keyword = {
    Keyword(
        archived = Option(archived) /* 1 */,
        bid = Option(bid) /* 1 */,
        id = id /* 2 */,
        matchType = matchType /* 2 */,
        parentId = parentId /* 2 */,
        parentType = Option(parentType) /* 1 */,
        `type` = Option(`type`) /* 1 */,
        value = value /* 2 */
    
    )
  }
}

object KeywordData {

  def validated(d8a : KeywordData, failFast : Boolean) : scala.util.Try[Keyword] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : KeywordData = try {
        val data = read[KeywordData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating KeywordData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : KeywordData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[KeywordData] = try {
        read[List[KeywordData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[Keyword]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[Keyword]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, KeywordData] = try {
        read[Map[String, KeywordData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, Keyword]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, Keyword]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

