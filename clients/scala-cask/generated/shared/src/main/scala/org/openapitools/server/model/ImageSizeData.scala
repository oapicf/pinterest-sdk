/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ImageSizeData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ImageSizeData(
  `1200x`: ImageDetailsData = null ,
  `150x150`: ImageDetailsData = null ,
  `400x300`: ImageDetailsData = null ,
  `600x`: ImageDetailsData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== `1200x` validation ==================
        
        
        
        
        // validating `1200x`
        if (_allValidationErrors.isEmpty || !failFast) {
            if `1200x` != null then _allValidationErrors ++= `1200x`.validationErrors(path :+ ImageSize.Fields.`1200x`, failFast)
        }

        // ================== `150x150` validation ==================
        
        
        
        
        // validating `150x150`
        if (_allValidationErrors.isEmpty || !failFast) {
            if `150x150` != null then _allValidationErrors ++= `150x150`.validationErrors(path :+ ImageSize.Fields.`150x150`, failFast)
        }

        // ================== `400x300` validation ==================
        
        
        
        
        // validating `400x300`
        if (_allValidationErrors.isEmpty || !failFast) {
            if `400x300` != null then _allValidationErrors ++= `400x300`.validationErrors(path :+ ImageSize.Fields.`400x300`, failFast)
        }

        // ================== `600x` validation ==================
        
        
        
        
        // validating `600x`
        if (_allValidationErrors.isEmpty || !failFast) {
            if `600x` != null then _allValidationErrors ++= `600x`.validationErrors(path :+ ImageSize.Fields.`600x`, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ImageSize] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ImageSize = {
    ImageSize(
        `1200x` = Option(`1200x`).map(_.asModel) /* 4 */,
        `150x150` = Option(`150x150`).map(_.asModel) /* 4 */,
        `400x300` = Option(`400x300`).map(_.asModel) /* 4 */,
        `600x` = Option(`600x`).map(_.asModel) /* 4 */
    
    )
  }
}

object ImageSizeData {

  def validated(d8a : ImageSizeData, failFast : Boolean) : scala.util.Try[ImageSize] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ImageSizeData = try {
        val data = read[ImageSizeData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ImageSizeData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ImageSizeData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ImageSizeData] = try {
        read[List[ImageSizeData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ImageSize]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ImageSize]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ImageSizeData] = try {
        read[Map[String, ImageSizeData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ImageSize]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ImageSize]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

