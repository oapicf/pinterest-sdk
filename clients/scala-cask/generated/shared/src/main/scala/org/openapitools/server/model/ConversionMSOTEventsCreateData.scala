/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ConversionMSOTEventsCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ConversionMSOTEventsCreateData(
/* Timestamp(s) when the ad action(s) happened. Unix timestamp in seconds. */
  actionTimestamps: Seq[Long] = Nil ,
/* The ID of the ad group that was attributed to the conversion event. */
  adGroupId: String,
/* The attribution model used to attribute the conversion event. */
  attributionModel: AttributionModel = null ,
/* Ad event type. */
  attributionScope: AttributionScope = null ,
/* Credit given to the attributed ad actions. Allowed values are > 0 and <= 1. */
  attributionScore: Double = 0 ,
/* The ID of the campaign that was attributed to the conversion event. */
  campaignId: String = "" ,
/* Click window used for attribution (for example, `1d`, `7d`, `30d`, `lifetime`). */
  clickWindow: String = "" ,
/* Currency code for the `value` field, required if `value` is present. Currency Codes should be in ISO 4217 standard. */
  currency: Currency = null ,
/* A unique id string that identifies this event. If you are already sending us events through Conversions API, then this id should match the event_id sent through Conversions API. */
  eventId: String,
/* Type of user event. */
  eventName: MsotEventName,
/* The time when the event occurred. Unix timestamp in seconds. */
  eventTimestamp: Long,
/* Total number of ad events including other non-Pinterest ad platforms. */
  totalEventTouchpoints: Int = 0 ,
/* Deprecated: use `total_events_fractional` instead to avoid rounding errors. Total number of conversion events that are reported in one API call.  If you are sending one API request for one attributed conversion event then this value should be 1. If you are sending multiple attributed conversion events in one API request then this value should be the total number of attributed conversion events in the request. */
  totalEvents: Int = 0 ,
/* Total number of conversion events that are reported in one API call. Use this field instead of `total_events` to send precise fractional values.  If you are sending one API request for one attributed conversion event with full credit, this value should be 1.0. For partial attribution, send the exact fractional value (e.g., 0.5 for half credit). */
  totalEventsFractional: Double = 0 ,
/* Order value of the conversion event. Required if `event_name` is `add_to_cart` or `checkout`. */
  value: Double = 0 ,
/* View window used for attribution (for example, `1d`, `7d`, `30d`). */
  viewWindow: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== actionTimestamps validation ==================
        
        
        
        

        // ================== adGroupId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adGroupId == null || !regex.r.matches(adGroupId) then
              _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.adGroupId, s"value '$adGroupId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== attributionModel validation ==================
        
        
        
        

        // ================== attributionScope validation ==================
        
        
        
        

        // ================== attributionScore validation ==================
        // validate against exclusive minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(attributionScore > 0) then
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.attributionScore, s"value '$attributionScore' is not greater than the exclusive minimum value 0")
        }
        // validate against maximum 1
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(attributionScore <= 1) then
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.attributionScore, s"value '$attributionScore' is not greater than the maximum value 1")
        }
        
        
        
        

        // ================== campaignId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if campaignId == null || !regex.r.matches(campaignId) then
              _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.campaignId, s"value '$campaignId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== clickWindow validation ==================
        
        
        
        

        // ================== currency validation ==================
        
        
        
        

        // ================== eventId validation ==================
        // validate max length 256
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if eventId == null then 0 else eventId.length
            if (len < 256) {
               _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.eventId, s"length $len is longer than the max length 256")
            }
        }
        
        
        
        

        // ================== eventName validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (eventName == null) {
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.eventName, "eventName is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== eventTimestamp validation ==================
        
        
        
        

        // ================== totalEventTouchpoints validation ==================
        // validate against minimum 1
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(totalEventTouchpoints >= 1) then
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.totalEventTouchpoints, s"value '$totalEventTouchpoints' is not greater than the minimum value 1")
        }
        
        
        
        

        // ================== totalEvents validation ==================
        // validate against minimum 1
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(totalEvents >= 1) then
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.totalEvents, s"value '$totalEvents' is not greater than the minimum value 1")
        }
        
        
        
        

        // ================== totalEventsFractional validation ==================
        // validate against exclusive minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(totalEventsFractional > 0) then
            _allValidationErrors += ValidationError(path :+ ConversionMSOTEventsCreate.Fields.totalEventsFractional, s"value '$totalEventsFractional' is not greater than the exclusive minimum value 0")
        }
        
        
        
        

        // ================== value validation ==================
        
        
        
        

        // ================== viewWindow validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ConversionMSOTEventsCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ConversionMSOTEventsCreate = {
    ConversionMSOTEventsCreate(
        actionTimestamps = actionTimestamps /* 2 */,
        adGroupId = adGroupId /* 2 */,
        attributionModel = Option(attributionModel) /* 1 */,
        attributionScope = Option(attributionScope) /* 1 */,
        attributionScore = Option(attributionScore) /* 1 */,
        campaignId = Option(campaignId) /* 1 */,
        clickWindow = Option(clickWindow) /* 1 */,
        currency = Option(currency) /* 1 */,
        eventId = eventId /* 2 */,
        eventName = eventName /* 2 */,
        eventTimestamp = eventTimestamp /* 2 */,
        totalEventTouchpoints = Option(totalEventTouchpoints) /* 1 */,
        totalEvents = Option(totalEvents) /* 1 */,
        totalEventsFractional = Option(totalEventsFractional) /* 1 */,
        value = Option(value) /* 1 */,
        viewWindow = Option(viewWindow) /* 1 */
    
    )
  }
}

object ConversionMSOTEventsCreateData {

  def validated(d8a : ConversionMSOTEventsCreateData, failFast : Boolean) : scala.util.Try[ConversionMSOTEventsCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ConversionMSOTEventsCreateData = try {
        val data = read[ConversionMSOTEventsCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ConversionMSOTEventsCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ConversionMSOTEventsCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ConversionMSOTEventsCreateData] = try {
        read[List[ConversionMSOTEventsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ConversionMSOTEventsCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ConversionMSOTEventsCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ConversionMSOTEventsCreateData] = try {
        read[Map[String, ConversionMSOTEventsCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ConversionMSOTEventsCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ConversionMSOTEventsCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

