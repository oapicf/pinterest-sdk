/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using model.mustache
package org.openapitools.server.model

import scala.util.control.NonFatal

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        
case class BulkCampaignDeliveryEstimatesItem(
        /* Range audience sizes for each ad group, in the same order as the ad groups in the request. */
    adgroupAudienceSizes: Seq[CampaignPlanningAdGroupAudienceSize] = Nil ,
        /* Conversion rate estimate. Used to translate impressions into conversions for oCPM ads, adapted based on the input conversion event and attribution windows. */
    conversionRate: Option[Float] = None ,
        /* Conversion rate estimates. Used to translate impressions into conversions for oCPM ads based on different conversion event and attribution windows settings. */
    conversionRates: Seq[CampaignPlanningConversionRate] = Nil ,
        /* Estimated curves. Each curve will pertain to a single estimation type. */
    curves: Seq[CampaignPlanningCurveEstimate] = Nil ,
    derivedMetrics: Option[CampaignDeliveryEstimatesDerivedMetrics] = None ,
        /* Errors encountered during estimation for this campaign. */
    errors: Seq[CampaignPlanningResponseError] = Nil ,
        /* UUID used to track delivery estimates when they are generated as part of a saved campaign. */
    estimateId: Option[String] = None ,
        /* Maximum potential spend estimate. */
    maxPotentialSpend: Option[Int] = None 


) {

def asJsonString: String = asData.asJsonString
def asJson: ujson.Value = asData.asJson

def asData : BulkCampaignDeliveryEstimatesItemData = {
BulkCampaignDeliveryEstimatesItemData(
    adgroupAudienceSizes = adgroupAudienceSizes.map(_.asData) /* 6 */,
    conversionRate = conversionRate.getOrElse(0) /*  1 */,
    conversionRates = conversionRates.map(_.asData) /* 6 */,
    curves = curves.map(_.asData) /* 6 */,
    derivedMetrics = derivedMetrics.map(_.asData).getOrElse(null) /* 4 */,
    errors = errors.map(_.asData) /* 6 */,
    estimateId = estimateId.getOrElse("") /*  1 */,
    maxPotentialSpend = maxPotentialSpend.getOrElse(0) /*  1 */

)
}
}

object BulkCampaignDeliveryEstimatesItem {
given RW[BulkCampaignDeliveryEstimatesItem] = summon[RW[ujson.Value]].bimap[BulkCampaignDeliveryEstimatesItem](_.asJson, json => read[BulkCampaignDeliveryEstimatesItemData](json).asModel)

enum Fields(val fieldName : String) extends Field(fieldName) {
    case adgroupAudienceSizes extends Fields("adgroupAudienceSizes")
    case conversionRate extends Fields("conversionRate")
    case conversionRates extends Fields("conversionRates")
    case curves extends Fields("curves")
    case derivedMetrics extends Fields("derivedMetrics")
    case errors extends Fields("errors")
    case estimateId extends Fields("estimateId")
    case maxPotentialSpend extends Fields("maxPotentialSpend")
}


}


