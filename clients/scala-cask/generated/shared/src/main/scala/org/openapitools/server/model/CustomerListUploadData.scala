/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CustomerListUploadData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CustomerListUploadData(
/* Advertiser ID. */
  adAccountId: String,
/* Customer List Upload creation_time. Epoch (seconds). */
  creationTime: Int,
/* ID of the customer list associated with this upload. */
  customerListId: String,
/* Error counts by error code */
  errorCounts: Seq[ErrorDetailData] = Nil ,
/* Customer List Upload ID. */
  id: String,
  operation: UserListOperationType,
/* Record processing counts */
  recordCounts: RecordCountsData = null ,
  state: WorkloadState,
/* Customer List Upload updated_time. Epoch (seconds). */
  updatedTime: Int
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== creationTime validation ==================
        
        
        
        

        // ================== customerListId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if customerListId == null || !regex.r.matches(customerListId) then
              _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.customerListId, s"value '$customerListId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== errorCounts validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (errorCounts != null) {
                errorCounts.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ CustomerListUpload.Fields.errorCounts :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        // validate max length 18
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if id == null then 0 else id.length
            if (len < 18) {
               _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.id, s"length $len is longer than the max length 18")
            }
        }
        
        
        
        

        // ================== operation validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (operation == null) {
            _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.operation, "operation is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== recordCounts validation ==================
        
        
        
        
        // validating recordCounts
        if (_allValidationErrors.isEmpty || !failFast) {
            if recordCounts != null then _allValidationErrors ++= recordCounts.validationErrors(path :+ CustomerListUpload.Fields.recordCounts, failFast)
        }

        // ================== state validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (state == null) {
            _allValidationErrors += ValidationError(path :+ CustomerListUpload.Fields.state, "state is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== updatedTime validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CustomerListUpload] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CustomerListUpload = {
    CustomerListUpload(
        adAccountId = adAccountId /* 2 */,
        creationTime = creationTime /* 2 */,
        customerListId = customerListId /* 2 */,
        errorCounts = errorCounts.map(_.asModel) /* 5 */,
        id = id /* 2 */,
        operation = operation /* 2 */,
        recordCounts = Option(recordCounts).map(_.asModel) /* 4 */,
        state = state /* 2 */,
        updatedTime = updatedTime /* 2 */
    
    )
  }
}

object CustomerListUploadData {

  def validated(d8a : CustomerListUploadData, failFast : Boolean) : scala.util.Try[CustomerListUpload] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CustomerListUploadData = try {
        val data = read[CustomerListUploadData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CustomerListUploadData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CustomerListUploadData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CustomerListUploadData] = try {
        read[List[CustomerListUploadData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CustomerListUpload]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CustomerListUpload]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CustomerListUploadData] = try {
        read[Map[String, CustomerListUploadData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CustomerListUpload]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CustomerListUpload]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

