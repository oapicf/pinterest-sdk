/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LabelErrorDataData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LabelErrorDataData(
/* Label ID. */
  id: String = "" ,
  labelType: NullableLabelType = null ,
  status: NullableLabelStatus = null ,
/* Label name. 100-character limit. */
  value: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ LabelErrorData.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== labelType validation ==================
        
        
        
        

        // ================== status validation ==================
        
        
        
        

        // ================== value validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if value == null then 0 else value.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ LabelErrorData.Fields.value, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LabelErrorData] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LabelErrorData = {
    LabelErrorData(
        id = Option(id) /* 1 */,
        labelType = Option(labelType) /* 1 */,
        status = Option(status) /* 1 */,
        value = Option(value) /* 1 */
    
    )
  }
}

object LabelErrorDataData {

  def validated(d8a : LabelErrorDataData, failFast : Boolean) : scala.util.Try[LabelErrorData] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LabelErrorDataData = try {
        val data = read[LabelErrorDataData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LabelErrorDataData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LabelErrorDataData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LabelErrorDataData] = try {
        read[List[LabelErrorDataData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LabelErrorData]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LabelErrorData]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LabelErrorDataData] = try {
        read[Map[String, LabelErrorDataData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LabelErrorData]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LabelErrorData]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

