/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import java.time.OffsetDateTime
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CatalogsCreativeAssetsFeedData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CatalogsCreativeAssetsFeedData(
/* Catalog id pertaining to the feed. If not provided, feed will use a default catalog based on type. */
  catalogId: String,
  catalogType: CatalogsCreativeAssetsFeed.CatalogTypeEnum,
  createdAt: OffsetDateTime,
  credentials: CatalogsFeedCredentialsData = null ,
  defaultCountry: Country,
  defaultCurrency: NullableCurrency = null ,
/* The locale used within a feed for product descriptions. */
  defaultLocale: String,
  format: CatalogsFormat,
/* ID of the feed entity. */
  id: String,
/* The URL where a feed is available for download. This URL is what Pinterest will use to download a feed for processing. */
  location: String,
/* A human-friendly name associated to a given feed. This value is currently nullable due to historical reasons. It is expected to become non-nullable in the future. */
  name: String,
  preferredProcessingSchedule: CatalogsFeedProcessingScheduleData = null ,
  status: CatalogsStatus,
  updatedAt: OffsetDateTime
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== catalogId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if catalogId == null || !regex.r.matches(catalogId) then
              _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.catalogId, s"value '$catalogId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== catalogType validation ==================
        
        
        
        

        // ================== createdAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (createdAt == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.createdAt, "createdAt is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== credentials validation ==================
        
        
        
        
        // validating credentials
        if (_allValidationErrors.isEmpty || !failFast) {
            if credentials != null then _allValidationErrors ++= credentials.validationErrors(path :+ CatalogsCreativeAssetsFeed.Fields.credentials, failFast)
        }

        // ================== defaultCountry validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (defaultCountry == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.defaultCountry, "defaultCountry is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== defaultCurrency validation ==================
        
        
        
        

        // ================== defaultLocale validation ==================
        
        
        
        

        // ================== format validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (format == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.format, "format is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== location validation ==================
        // validate against pattern '^(http|https|ftp|sftp):\/\/'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^(http|https|ftp|sftp):\/\/"""
           if location == null || !regex.r.matches(location) then
              _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.location, s"value '$location' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== name validation ==================
        
        
        
        

        // ================== preferredProcessingSchedule validation ==================
        
        
        
        
        // validating preferredProcessingSchedule
        if (_allValidationErrors.isEmpty || !failFast) {
            if preferredProcessingSchedule != null then _allValidationErrors ++= preferredProcessingSchedule.validationErrors(path :+ CatalogsCreativeAssetsFeed.Fields.preferredProcessingSchedule, failFast)
        }

        // ================== status validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (status == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.status, "status is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== updatedAt validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (updatedAt == null) {
            _allValidationErrors += ValidationError(path :+ CatalogsCreativeAssetsFeed.Fields.updatedAt, "updatedAt is a required field and cannot be null")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CatalogsCreativeAssetsFeed] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CatalogsCreativeAssetsFeed = {
    CatalogsCreativeAssetsFeed(
        catalogId = catalogId /* 2 */,
        catalogType = catalogType /* 2 */,
        createdAt = createdAt /* 2 */,
        credentials = Option(credentials).map(_.asModel) /* 4 */,
        defaultCountry = defaultCountry /* 2 */,
        defaultCurrency = Option(defaultCurrency) /* 1 */,
        defaultLocale = defaultLocale /* 2 */,
        format = format /* 2 */,
        id = id /* 2 */,
        location = location /* 2 */,
        name = name /* 2 */,
        preferredProcessingSchedule = Option(preferredProcessingSchedule).map(_.asModel) /* 4 */,
        status = status /* 2 */,
        updatedAt = updatedAt /* 2 */
    
    )
  }
}

object CatalogsCreativeAssetsFeedData {

  def validated(d8a : CatalogsCreativeAssetsFeedData, failFast : Boolean) : scala.util.Try[CatalogsCreativeAssetsFeed] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CatalogsCreativeAssetsFeedData = try {
        val data = read[CatalogsCreativeAssetsFeedData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CatalogsCreativeAssetsFeedData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CatalogsCreativeAssetsFeedData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CatalogsCreativeAssetsFeedData] = try {
        read[List[CatalogsCreativeAssetsFeedData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CatalogsCreativeAssetsFeed]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CatalogsCreativeAssetsFeed]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CatalogsCreativeAssetsFeedData] = try {
        read[Map[String, CatalogsCreativeAssetsFeedData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CatalogsCreativeAssetsFeed]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CatalogsCreativeAssetsFeed]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

