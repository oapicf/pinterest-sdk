/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** BaseBusinessAssetsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BaseBusinessAssetsData(
/* An object containing all the information specific to the provided asset group. This field will be populated only if asset_type equals 'ASSET_GROUP'. */
  assetGroupInfo: AssetGroupBindingData = null ,
/* Unique identifier of a business asset. */
  assetId: String = "" ,
  assetType: AssetTypeResponse = null ,
/* Permission levels the requesting business has on an asset. */
  permissions: Seq[String] = Nil 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== assetGroupInfo validation ==================
        
        
        
        
        // validating assetGroupInfo
        if (_allValidationErrors.isEmpty || !failFast) {
            if assetGroupInfo != null then _allValidationErrors ++= assetGroupInfo.validationErrors(path :+ BaseBusinessAssets.Fields.assetGroupInfo, failFast)
        }

        // ================== assetId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if assetId == null || !regex.r.matches(assetId) then
              _allValidationErrors += ValidationError(path :+ BaseBusinessAssets.Fields.assetId, s"value '$assetId' doesn't match pattern $regex")
        }
        // validate min length 1
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ BaseBusinessAssets.Fields.assetId, s"length $len is shorter than the min length 1")
            }
        }
        // validate max length 20
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if assetId == null then 0 else assetId.length
            if (len < 20) {
               _allValidationErrors += ValidationError(path :+ BaseBusinessAssets.Fields.assetId, s"length $len is longer than the max length 20")
            }
        }
        
        
        
        

        // ================== assetType validation ==================
        
        
        
        

        // ================== permissions validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[BaseBusinessAssets] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BaseBusinessAssets = {
    BaseBusinessAssets(
        assetGroupInfo = Option(assetGroupInfo).map(_.asModel) /* 4 */,
        assetId = Option(assetId) /* 1 */,
        assetType = Option(assetType) /* 1 */,
        permissions = permissions /* 2 */
    
    )
  }
}

object BaseBusinessAssetsData {

  def validated(d8a : BaseBusinessAssetsData, failFast : Boolean) : scala.util.Try[BaseBusinessAssets] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : BaseBusinessAssetsData = try {
        val data = read[BaseBusinessAssetsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating BaseBusinessAssetsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : BaseBusinessAssetsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[BaseBusinessAssetsData] = try {
        read[List[BaseBusinessAssetsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BaseBusinessAssets]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BaseBusinessAssets]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BaseBusinessAssetsData] = try {
        read[Map[String, BaseBusinessAssetsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BaseBusinessAssets]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BaseBusinessAssets]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

