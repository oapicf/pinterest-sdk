/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LeadSubscriptionPostParamsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LeadSubscriptionPostParamsData(
/* The Ad Account ID that this lead form belongs to. */
  adAccountId: String = "" ,
/* API version. */
  apiVersion: String = "" ,
/* Subscription creation time. Unix timestamp in milliseconds. */
  createdTime: Int = 0 ,
/* Lead data encryption algorithm. */
  cryptographicAlgorithm: String = "" ,
/* Base64 encoded key for client to decrypt lead data. */
  cryptographicKey: String = "" ,
/* Subscription ID. */
  id: String = "" ,
/* Lead form ID. */
  leadFormId: String = "" ,
/* User account used to subscribe lead data. */
  userAccountId: String = "" ,
/* Standard HTTPS webhook URL. */
  webhookUrl: String = "" ,
/* Partner access token. Only for clients that requires authentication. We recommend to avoid this param. */
  partnerAccessToken: String = "" ,
/* Partner metadata. Only for clients that requires special handling. We recommend to avoid this param. */
  partnerMetadata: PartnerMetadataData = null ,
/* Partner refresh token. Only for clients that requires authentication. We recommend to avoid this param. */
  partnerRefreshToken: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscriptionPostParams.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== apiVersion validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== cryptographicAlgorithm validation ==================
        
        
        
        

        // ================== cryptographicKey validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ LeadSubscriptionPostParams.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== leadFormId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if leadFormId == null || !regex.r.matches(leadFormId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscriptionPostParams.Fields.leadFormId, s"value '$leadFormId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== userAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if userAccountId == null || !regex.r.matches(userAccountId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscriptionPostParams.Fields.userAccountId, s"value '$userAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== webhookUrl validation ==================
        
        
        
        

        // ================== partnerAccessToken validation ==================
        
        
        
        

        // ================== partnerMetadata validation ==================
        
        
        
        
        // validating partnerMetadata
        if (_allValidationErrors.isEmpty || !failFast) {
            if partnerMetadata != null then _allValidationErrors ++= partnerMetadata.validationErrors(path :+ LeadSubscriptionPostParams.Fields.partnerMetadata, failFast)
        }

        // ================== partnerRefreshToken validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LeadSubscriptionPostParams] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LeadSubscriptionPostParams = {
    LeadSubscriptionPostParams(
        adAccountId = Option(adAccountId) /* 1 */,
        apiVersion = Option(apiVersion) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        cryptographicAlgorithm = Option(cryptographicAlgorithm) /* 1 */,
        cryptographicKey = Option(cryptographicKey) /* 1 */,
        id = Option(id) /* 1 */,
        leadFormId = Option(leadFormId) /* 1 */,
        userAccountId = Option(userAccountId) /* 1 */,
        webhookUrl = Option(webhookUrl) /* 1 */,
        partnerAccessToken = Option(partnerAccessToken) /* 1 */,
        partnerMetadata = Option(partnerMetadata).map(_.asModel) /* 4 */,
        partnerRefreshToken = Option(partnerRefreshToken) /* 1 */
    
    )
  }
}

object LeadSubscriptionPostParamsData {

  def validated(d8a : LeadSubscriptionPostParamsData, failFast : Boolean) : scala.util.Try[LeadSubscriptionPostParams] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LeadSubscriptionPostParamsData = try {
        val data = read[LeadSubscriptionPostParamsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LeadSubscriptionPostParamsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LeadSubscriptionPostParamsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LeadSubscriptionPostParamsData] = try {
        read[List[LeadSubscriptionPostParamsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LeadSubscriptionPostParams]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LeadSubscriptionPostParams]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LeadSubscriptionPostParamsData] = try {
        read[Map[String, LeadSubscriptionPostParamsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LeadSubscriptionPostParams]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LeadSubscriptionPostParams]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

