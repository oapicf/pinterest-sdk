/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** BulkCampaignDeliveryEstimatesItemData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BulkCampaignDeliveryEstimatesItemData(
/* Range audience sizes for each ad group, in the same order as the ad groups in the request. */
  adgroupAudienceSizes: Seq[CampaignPlanningAdGroupAudienceSizeData] = Nil ,
/* Conversion rate estimate. Used to translate impressions into conversions for oCPM ads, adapted based on the input conversion event and attribution windows. */
  conversionRate: Float = 0 ,
/* Conversion rate estimates. Used to translate impressions into conversions for oCPM ads based on different conversion event and attribution windows settings. */
  conversionRates: Seq[CampaignPlanningConversionRateData] = Nil ,
/* Estimated curves. Each curve will pertain to a single estimation type. */
  curves: Seq[CampaignPlanningCurveEstimateData] = Nil ,
  derivedMetrics: CampaignDeliveryEstimatesDerivedMetricsData = null ,
/* Errors encountered during estimation for this campaign. */
  errors: Seq[CampaignPlanningResponseErrorData] = Nil ,
/* UUID used to track delivery estimates when they are generated as part of a saved campaign. */
  estimateId: String = "" ,
/* Maximum potential spend estimate. */
  maxPotentialSpend: Int = 0 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adgroupAudienceSizes validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (adgroupAudienceSizes != null) {
                adgroupAudienceSizes.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ BulkCampaignDeliveryEstimatesItem.Fields.adgroupAudienceSizes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== conversionRate validation ==================
        
        
        
        

        // ================== conversionRates validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (conversionRates != null) {
                conversionRates.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ BulkCampaignDeliveryEstimatesItem.Fields.conversionRates :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== curves validation ==================
        
        // validate min items 20
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if curves == null then 0 else curves.size
            if (len > 20) {
               _allValidationErrors += ValidationError(path :+ BulkCampaignDeliveryEstimatesItem.Fields.curves, s"curves has $len, which is greater than the max items 20")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (curves != null) {
                curves.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ BulkCampaignDeliveryEstimatesItem.Fields.curves :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== derivedMetrics validation ==================
        
        
        
        
        // validating derivedMetrics
        if (_allValidationErrors.isEmpty || !failFast) {
            if derivedMetrics != null then _allValidationErrors ++= derivedMetrics.validationErrors(path :+ BulkCampaignDeliveryEstimatesItem.Fields.derivedMetrics, failFast)
        }

        // ================== errors validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (errors != null) {
                errors.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ BulkCampaignDeliveryEstimatesItem.Fields.errors :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== estimateId validation ==================
        
        
        
        

        // ================== maxPotentialSpend validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[BulkCampaignDeliveryEstimatesItem] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BulkCampaignDeliveryEstimatesItem = {
    BulkCampaignDeliveryEstimatesItem(
        adgroupAudienceSizes = adgroupAudienceSizes.map(_.asModel) /* 5 */,
        conversionRate = Option(conversionRate) /* 1 */,
        conversionRates = conversionRates.map(_.asModel) /* 5 */,
        curves = curves.map(_.asModel) /* 5 */,
        derivedMetrics = Option(derivedMetrics).map(_.asModel) /* 4 */,
        errors = errors.map(_.asModel) /* 5 */,
        estimateId = Option(estimateId) /* 1 */,
        maxPotentialSpend = Option(maxPotentialSpend) /* 1 */
    
    )
  }
}

object BulkCampaignDeliveryEstimatesItemData {

  def validated(d8a : BulkCampaignDeliveryEstimatesItemData, failFast : Boolean) : scala.util.Try[BulkCampaignDeliveryEstimatesItem] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : BulkCampaignDeliveryEstimatesItemData = try {
        val data = read[BulkCampaignDeliveryEstimatesItemData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating BulkCampaignDeliveryEstimatesItemData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : BulkCampaignDeliveryEstimatesItemData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[BulkCampaignDeliveryEstimatesItemData] = try {
        read[List[BulkCampaignDeliveryEstimatesItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BulkCampaignDeliveryEstimatesItem]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BulkCampaignDeliveryEstimatesItem]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BulkCampaignDeliveryEstimatesItemData] = try {
        read[Map[String, BulkCampaignDeliveryEstimatesItemData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BulkCampaignDeliveryEstimatesItem]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BulkCampaignDeliveryEstimatesItem]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

