/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** QualityComponentsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class QualityComponentsData(
  advertiserExternalId: Map[String, QualityComponentDetails] = Map.empty ,
  clickIdEpik: Map[String, QualityComponentDetails] = Map.empty ,
/* Dedup components. */
  externalEventId: Map[String, QualityComponentDetails] = Map.empty ,
/* User matching identifiers. */
  hashedEmail: Map[String, QualityComponentDetails] = Map.empty ,
  hashedMaid: Map[String, QualityComponentDetails] = Map.empty ,
  ipAddress: Map[String, QualityComponentDetails] = Map.empty ,
  orderId: Map[String, QualityComponentDetails] = Map.empty ,
  orderValue: Map[String, QualityComponentDetails] = Map.empty ,
/* Product/event metadata. */
  productId: Map[String, QualityComponentDetails] = Map.empty ,
  sourceUrl: Map[String, QualityComponentDetails] = Map.empty ,
  userAgent: Map[String, QualityComponentDetails] = Map.empty 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== advertiserExternalId validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== clickIdEpik validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== externalEventId validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== hashedEmail validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== hashedMaid validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== ipAddress validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== orderId validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== orderValue validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== productId validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== sourceUrl validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== userAgent validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (inner != null) {
                inner.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ QualityComponents.Fields.inner :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[QualityComponents] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : QualityComponents = {
    QualityComponents(
        advertiserExternalId = advertiserExternalId /* 5.1 */,
        clickIdEpik = clickIdEpik /* 5.1 */,
        externalEventId = externalEventId /* 5.1 */,
        hashedEmail = hashedEmail /* 5.1 */,
        hashedMaid = hashedMaid /* 5.1 */,
        ipAddress = ipAddress /* 5.1 */,
        orderId = orderId /* 5.1 */,
        orderValue = orderValue /* 5.1 */,
        productId = productId /* 5.1 */,
        sourceUrl = sourceUrl /* 5.1 */,
        userAgent = userAgent /* 5.1 */
    
    )
  }
}

object QualityComponentsData {

  def validated(d8a : QualityComponentsData, failFast : Boolean) : scala.util.Try[QualityComponents] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : QualityComponentsData = try {
        val data = read[QualityComponentsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating QualityComponentsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : QualityComponentsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[QualityComponentsData] = try {
        read[List[QualityComponentsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[QualityComponents]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[QualityComponents]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, QualityComponentsData] = try {
        read[Map[String, QualityComponentsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, QualityComponents]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, QualityComponents]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

