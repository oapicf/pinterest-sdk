/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** LeadSubscriptionData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LeadSubscriptionData(
/* The Ad Account ID that this lead form belongs to. */
  adAccountId: String = "" ,
/* API version. */
  apiVersion: String = "" ,
/* Subscription creation time. Unix timestamp in milliseconds. */
  createdTime: Int = 0 ,
/* Lead data encryption algorithm. */
  cryptographicAlgorithm: String = "" ,
/* Base64 encoded key for client to decrypt lead data. */
  cryptographicKey: String = "" ,
/* Subscription ID. */
  id: String = "" ,
/* Lead form ID. */
  leadFormId: String = "" ,
/* User account used to subscribe lead data. */
  userAccountId: String = "" ,
/* Standard HTTPS webhook URL. */
  webhookUrl: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if adAccountId == null || !regex.r.matches(adAccountId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscription.Fields.adAccountId, s"value '$adAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== apiVersion validation ==================
        
        
        
        

        // ================== createdTime validation ==================
        
        
        
        

        // ================== cryptographicAlgorithm validation ==================
        
        
        
        

        // ================== cryptographicKey validation ==================
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ LeadSubscription.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== leadFormId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if leadFormId == null || !regex.r.matches(leadFormId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscription.Fields.leadFormId, s"value '$leadFormId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== userAccountId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if userAccountId == null || !regex.r.matches(userAccountId) then
              _allValidationErrors += ValidationError(path :+ LeadSubscription.Fields.userAccountId, s"value '$userAccountId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== webhookUrl validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[LeadSubscription] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LeadSubscription = {
    LeadSubscription(
        adAccountId = Option(adAccountId) /* 1 */,
        apiVersion = Option(apiVersion) /* 1 */,
        createdTime = Option(createdTime) /* 1 */,
        cryptographicAlgorithm = Option(cryptographicAlgorithm) /* 1 */,
        cryptographicKey = Option(cryptographicKey) /* 1 */,
        id = Option(id) /* 1 */,
        leadFormId = Option(leadFormId) /* 1 */,
        userAccountId = Option(userAccountId) /* 1 */,
        webhookUrl = Option(webhookUrl) /* 1 */
    
    )
  }
}

object LeadSubscriptionData {

  def validated(d8a : LeadSubscriptionData, failFast : Boolean) : scala.util.Try[LeadSubscription] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : LeadSubscriptionData = try {
        val data = read[LeadSubscriptionData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating LeadSubscriptionData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : LeadSubscriptionData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[LeadSubscriptionData] = try {
        read[List[LeadSubscriptionData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LeadSubscription]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LeadSubscription]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LeadSubscriptionData] = try {
        read[Map[String, LeadSubscriptionData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LeadSubscription]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LeadSubscription]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

