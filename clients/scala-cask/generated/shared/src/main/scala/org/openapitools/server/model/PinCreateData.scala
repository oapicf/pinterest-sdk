/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PinCreateData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PinCreateData(
/* AI disclosure declarations the creator has made about this Pin. */
  aiDisclosures: AiDisclosuresData = null ,
  altText: String = "" ,
/* The board to which this Pin belongs. */
  boardId: String = "" ,
/* The board section to which this Pin belongs. */
  boardSectionId: String = "" ,
  description: String = "" ,
/* Dominant pin color. Hex number, e.g. `#6E7874`. */
  dominantColor: String = "" ,
  link: String = "" ,
  mediaSource: PinMediaSourceData = null ,
/* The source pin id if this pin was saved from another pin. [Learn more](https://help.pinterest.com/article/save-pins-on-pinterest). */
  parentPinId: String = "" ,
/* The sponsor account id to request paid partnership from.  Currently the field is only available to a list of users in a closed beta. */
  sponsorId: String = "" ,
  title: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== aiDisclosures validation ==================
        
        
        
        
        // validating aiDisclosures
        if (_allValidationErrors.isEmpty || !failFast) {
            if aiDisclosures != null then _allValidationErrors ++= aiDisclosures.validationErrors(path :+ PinCreate.Fields.aiDisclosures, failFast)
        }

        // ================== altText validation ==================
        // validate max length 500
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if altText == null then 0 else altText.length
            if (len < 500) {
               _allValidationErrors += ValidationError(path :+ PinCreate.Fields.altText, s"length $len is longer than the max length 500")
            }
        }
        
        
        
        

        // ================== boardId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if boardId == null || !regex.r.matches(boardId) then
              _allValidationErrors += ValidationError(path :+ PinCreate.Fields.boardId, s"value '$boardId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== boardSectionId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if boardSectionId == null || !regex.r.matches(boardSectionId) then
              _allValidationErrors += ValidationError(path :+ PinCreate.Fields.boardSectionId, s"value '$boardSectionId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== description validation ==================
        // validate max length 800
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if description == null then 0 else description.length
            if (len < 800) {
               _allValidationErrors += ValidationError(path :+ PinCreate.Fields.description, s"length $len is longer than the max length 800")
            }
        }
        
        
        
        

        // ================== dominantColor validation ==================
        
        
        
        

        // ================== link validation ==================
        // validate max length 2048
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if link == null then 0 else link.length
            if (len < 2048) {
               _allValidationErrors += ValidationError(path :+ PinCreate.Fields.link, s"length $len is longer than the max length 2048")
            }
        }
        
        
        
        

        // ================== mediaSource validation ==================
        
        
        
        
        // validating mediaSource
        if (_allValidationErrors.isEmpty || !failFast) {
            if mediaSource != null then _allValidationErrors ++= mediaSource.validationErrors(path :+ PinCreate.Fields.mediaSource, failFast)
        }

        // ================== parentPinId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if parentPinId == null || !regex.r.matches(parentPinId) then
              _allValidationErrors += ValidationError(path :+ PinCreate.Fields.parentPinId, s"value '$parentPinId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== sponsorId validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if sponsorId == null || !regex.r.matches(sponsorId) then
              _allValidationErrors += ValidationError(path :+ PinCreate.Fields.sponsorId, s"value '$sponsorId' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== title validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if title == null then 0 else title.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PinCreate.Fields.title, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PinCreate] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PinCreate = {
    PinCreate(
        aiDisclosures = Option(aiDisclosures).map(_.asModel) /* 4 */,
        altText = Option(altText) /* 1 */,
        boardId = Option(boardId) /* 1 */,
        boardSectionId = Option(boardSectionId) /* 1 */,
        description = Option(description) /* 1 */,
        dominantColor = Option(dominantColor) /* 1 */,
        link = Option(link) /* 1 */,
        mediaSource = Option(mediaSource).map(_.asModel) /* 4 */,
        parentPinId = Option(parentPinId) /* 1 */,
        sponsorId = Option(sponsorId) /* 1 */,
        title = Option(title) /* 1 */
    
    )
  }
}

object PinCreateData {

  def validated(d8a : PinCreateData, failFast : Boolean) : scala.util.Try[PinCreate] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PinCreateData = try {
        val data = read[PinCreateData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PinCreateData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PinCreateData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PinCreateData] = try {
        read[List[PinCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PinCreate]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PinCreate]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PinCreateData] = try {
        read[Map[String, PinCreateData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PinCreate]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PinCreate]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

