/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** SummaryPinData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SummaryPinData(
  altText: String = "" ,
  description: String = "" ,
  id: String,
  link: String = "" ,
  media: PinMediaData = null ,
  title: String = "" 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== altText validation ==================
        // validate max length 500
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if altText == null then 0 else altText.length
            if (len < 500) {
               _allValidationErrors += ValidationError(path :+ SummaryPin.Fields.altText, s"length $len is longer than the max length 500")
            }
        }
        
        
        
        

        // ================== description validation ==================
        // validate max length 800
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if description == null then 0 else description.length
            if (len < 800) {
               _allValidationErrors += ValidationError(path :+ SummaryPin.Fields.description, s"length $len is longer than the max length 800")
            }
        }
        
        
        
        

        // ================== id validation ==================
        // validate against pattern '^\\d+$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d+$"""
           if id == null || !regex.r.matches(id) then
              _allValidationErrors += ValidationError(path :+ SummaryPin.Fields.id, s"value '$id' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== link validation ==================
        // validate max length 2048
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if link == null then 0 else link.length
            if (len < 2048) {
               _allValidationErrors += ValidationError(path :+ SummaryPin.Fields.link, s"length $len is longer than the max length 2048")
            }
        }
        
        
        
        

        // ================== media validation ==================
        
        
        
        
        // validating media
        if (_allValidationErrors.isEmpty || !failFast) {
            if media != null then _allValidationErrors ++= media.validationErrors(path :+ SummaryPin.Fields.media, failFast)
        }

        // ================== title validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if title == null then 0 else title.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ SummaryPin.Fields.title, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[SummaryPin] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SummaryPin = {
    SummaryPin(
        altText = Option(altText) /* 1 */,
        description = Option(description) /* 1 */,
        id = id /* 2 */,
        link = Option(link) /* 1 */,
        media = Option(media).map(_.asModel) /* 4 */,
        title = Option(title) /* 1 */
    
    )
  }
}

object SummaryPinData {

  def validated(d8a : SummaryPinData, failFast : Boolean) : scala.util.Try[SummaryPin] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : SummaryPinData = try {
        val data = read[SummaryPinData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating SummaryPinData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : SummaryPinData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[SummaryPinData] = try {
        read[List[SummaryPinData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SummaryPin]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SummaryPin]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SummaryPinData] = try {
        read[Map[String, SummaryPinData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SummaryPin]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SummaryPin]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

