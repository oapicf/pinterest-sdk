/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AdsAnalyticsCreateAsyncRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AdsAnalyticsCreateAsyncRequestData(
/* List of ad group ids */
  adGroupIds: Seq[String] = Nil ,
/* List of values for filtering */
  adGroupStatuses: Seq[AdGroupSummaryStatus] = Nil ,
/* List of ad ids. This parameter is not supported for Product Item level reports. */
  adIds: Seq[String] = Nil ,
/* List of values for filtering. This parameter is not supported for Product Item level reports. */
  adStatuses: Seq[PinPromotionSummaryStatus] = Nil ,
/* List of attribution types for the conversion report. */
  attributionTypes: Seq[ConversionReportAttributionType] = Nil ,
/* Campaign brand label for filtering. */
  campaignBrandLabel: String = "" ,
/* Campaign custom label for filtering. */
  campaignCustomLabel: String = "" ,
/* List of campaign ids */
  campaignIds: Seq[String] = Nil ,
/* List of values for filtering. [\"WEB_SESSIONS\"] is in BETA. */
  campaignObjectiveTypes: Seq[CampaignObjectiveType] = Nil ,
/* List of status values for filtering */
  campaignStatuses: Seq[CampaignSummaryStatus] = Nil ,
/* Number of days to use as the conversion attribution window for a pin click action. */
  clickWindowDays: ConversionAttributionWindowDays = 30 ,
/* Metric and entity columns. Pin promotion and ad related columns are not supported for Product Item level reports. */
  columns: Seq[ReportingColumnAsync] = Nil ,
/* Determines if the targeting types included in the request should be consolidated into a single breakdown. */
  combineTargetingTypes: Boolean = false ,
/* Date dimension for conversion metrics. */
  conversionReportTime: ConversionReportTimeType = TIME_OF_AD_ACTION ,
/* List of advertiser-defined custom conversion event metrics to include in the report */
  customConversionEventMetrics: Seq[CustomConversionEventMetricsData] = Nil ,
/* Metric report end date (UTC). Format: YYYY-MM-DD */
  endDate: String,
/* Which hour of the end date to stop the report (inclusive). Only allowed for hourly reports. */
  endHour: Int = 0 ,
/* Number of days to use as the conversion attribution window for an engagement action. */
  engagementWindowDays: ConversionAttributionWindowDays = 30 ,
/*   TOTAL - metrics are aggregated over the specified date range.   DAY - metrics are broken down daily.   HOUR - metrics are broken down hourly.   WEEKLY - metrics are broken down weekly.   MONTHLY - metrics are broken down monthly. */
  granularity: Granularity,
/* Level of the report */
  level: MetricsReportingLevel = null ,
/* List of metrics filters */
  metricsFilters: Seq[AdsAnalyticsMetricsFilterData] = Nil ,
  primarySort: PrimarySort = BY_ID ,
/* List of product group ids */
  productGroupIds: Seq[String] = Nil ,
/* List of values for filtering */
  productGroupStatuses: Seq[ProductGroupSummaryStatus] = Nil ,
/* List of product item ids */
  productItemIds: Seq[String] = Nil ,
  reportFormat: DataOutputFormat = JSON ,
/* Specify the timezone to be applied for the reporting. */
  reportingTimezone: ReportingTimeZone = null ,
/* Metric report start date (UTC). Format: YYYY-MM-DD */
  startDate: String,
/* Which hour of the start date to begin the report. Only allowed for hourly reports. */
  startHour: Int = 0 ,
/* List of targeting types. Requires `level` to be a value ending in `_TARGETING`. */
  targetingTypes: Seq[AdAdsAnalyticsAsyncTargetingTypes] = Nil ,
/* Number of days to use as the conversion attribution window for a view action. */
  viewWindowDays: ConversionAttributionWindowDays = 1 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adGroupIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupIds == null then 0 else adGroupIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adGroupIds, s"adGroupIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupIds == null then 0 else adGroupIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adGroupIds, s"adGroupIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== adGroupStatuses validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupStatuses == null then 0 else adGroupStatuses.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adGroupStatuses, s"adGroupStatuses has $len, which is less than the min items 1")
            }
        }
        // validate min items 6
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroupStatuses == null then 0 else adGroupStatuses.size
            if (len > 6) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adGroupStatuses, s"adGroupStatuses has $len, which is greater than the max items 6")
            }
        }
        
        
        

        // ================== adIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adIds == null then 0 else adIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adIds, s"adIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adIds == null then 0 else adIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adIds, s"adIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== adStatuses validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adStatuses == null then 0 else adStatuses.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adStatuses, s"adStatuses has $len, which is less than the min items 1")
            }
        }
        // validate min items 6
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adStatuses == null then 0 else adStatuses.size
            if (len > 6) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.adStatuses, s"adStatuses has $len, which is greater than the max items 6")
            }
        }
        
        
        

        // ================== attributionTypes validation ==================
        
        
        
        

        // ================== campaignBrandLabel validation ==================
        
        
        
        

        // ================== campaignCustomLabel validation ==================
        
        
        
        

        // ================== campaignIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignIds, s"campaignIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignIds, s"campaignIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== campaignObjectiveTypes validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignObjectiveTypes == null then 0 else campaignObjectiveTypes.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignObjectiveTypes, s"campaignObjectiveTypes has $len, which is less than the min items 1")
            }
        }
        // validate min items 7
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignObjectiveTypes == null then 0 else campaignObjectiveTypes.size
            if (len > 7) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignObjectiveTypes, s"campaignObjectiveTypes has $len, which is greater than the max items 7")
            }
        }
        
        
        

        // ================== campaignStatuses validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignStatuses == null then 0 else campaignStatuses.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignStatuses, s"campaignStatuses has $len, which is less than the min items 1")
            }
        }
        // validate min items 6
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if campaignStatuses == null then 0 else campaignStatuses.size
            if (len > 6) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.campaignStatuses, s"campaignStatuses has $len, which is greater than the max items 6")
            }
        }
        
        
        

        // ================== clickWindowDays validation ==================
        
        
        
        

        // ================== columns validation ==================
        
        
        
        

        // ================== combineTargetingTypes validation ==================
        
        
        
        

        // ================== conversionReportTime validation ==================
        
        
        
        

        // ================== customConversionEventMetrics validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (customConversionEventMetrics != null) {
                customConversionEventMetrics.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AdsAnalyticsCreateAsyncRequest.Fields.customConversionEventMetrics :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== endDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if endDate == null || !regex.r.matches(endDate) then
              _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.endDate, s"value '$endDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== endHour validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(endHour >= 0) then
            _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.endHour, s"value '$endHour' is not greater than the minimum value 0")
        }
        // validate against maximum 23
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(endHour <= 23) then
            _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.endHour, s"value '$endHour' is not greater than the maximum value 23")
        }
        
        
        
        

        // ================== engagementWindowDays validation ==================
        
        
        
        

        // ================== granularity validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (granularity == null) {
            _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.granularity, "granularity is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== level validation ==================
        
        
        
        

        // ================== metricsFilters validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if metricsFilters == null then 0 else metricsFilters.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.metricsFilters, s"metricsFilters has $len, which is less than the min items 1")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (metricsFilters != null) {
                metricsFilters.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AdsAnalyticsCreateAsyncRequest.Fields.metricsFilters :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== primarySort validation ==================
        
        
        
        

        // ================== productGroupIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productGroupIds == null then 0 else productGroupIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productGroupIds, s"productGroupIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productGroupIds == null then 0 else productGroupIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productGroupIds, s"productGroupIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== productGroupStatuses validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productGroupStatuses == null then 0 else productGroupStatuses.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productGroupStatuses, s"productGroupStatuses has $len, which is less than the min items 1")
            }
        }
        // validate min items 6
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productGroupStatuses == null then 0 else productGroupStatuses.size
            if (len > 6) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productGroupStatuses, s"productGroupStatuses has $len, which is greater than the max items 6")
            }
        }
        
        
        

        // ================== productItemIds validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productItemIds == null then 0 else productItemIds.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productItemIds, s"productItemIds has $len, which is less than the min items 1")
            }
        }
        // validate min items 500
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if productItemIds == null then 0 else productItemIds.size
            if (len > 500) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.productItemIds, s"productItemIds has $len, which is greater than the max items 500")
            }
        }
        
        
        

        // ================== reportFormat validation ==================
        
        
        
        

        // ================== reportingTimezone validation ==================
        
        
        
        

        // ================== startDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if startDate == null || !regex.r.matches(startDate) then
              _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.startDate, s"value '$startDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== startHour validation ==================
        // validate against minimum 0
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(startHour >= 0) then
            _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.startHour, s"value '$startHour' is not greater than the minimum value 0")
        }
        // validate against maximum 23
        if (_allValidationErrors.isEmpty || !failFast) {
            if !(startHour <= 23) then
            _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.startHour, s"value '$startHour' is not greater than the maximum value 23")
        }
        
        
        
        

        // ================== targetingTypes validation ==================
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if targetingTypes == null then 0 else targetingTypes.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.targetingTypes, s"targetingTypes has $len, which is less than the min items 1")
            }
        }
        // validate min items 5
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if targetingTypes == null then 0 else targetingTypes.size
            if (len > 5) {
               _allValidationErrors += ValidationError(path :+ AdsAnalyticsCreateAsyncRequest.Fields.targetingTypes, s"targetingTypes has $len, which is greater than the max items 5")
            }
        }
        
        
        

        // ================== viewWindowDays validation ==================
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AdsAnalyticsCreateAsyncRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AdsAnalyticsCreateAsyncRequest = {
    AdsAnalyticsCreateAsyncRequest(
        adGroupIds = adGroupIds /* 2 */,
        adGroupStatuses = adGroupStatuses.map(_.asModel) /* 5 */,
        adIds = adIds /* 2 */,
        adStatuses = adStatuses.map(_.asModel) /* 5 */,
        attributionTypes = attributionTypes.map(_.asModel) /* 5 */,
        campaignBrandLabel = Option(campaignBrandLabel) /* 1 */,
        campaignCustomLabel = Option(campaignCustomLabel) /* 1 */,
        campaignIds = campaignIds /* 2 */,
        campaignObjectiveTypes = campaignObjectiveTypes.map(_.asModel) /* 5 */,
        campaignStatuses = campaignStatuses.map(_.asModel) /* 5 */,
        clickWindowDays = Option(clickWindowDays) /* 1 */,
        columns = columns.map(_.asModel) /* 5 */,
        combineTargetingTypes = Option(combineTargetingTypes) /* 1 */,
        conversionReportTime = Option(conversionReportTime) /* 1 */,
        customConversionEventMetrics = customConversionEventMetrics.map(_.asModel) /* 5 */,
        endDate = endDate /* 2 */,
        endHour = Option(endHour) /* 1 */,
        engagementWindowDays = Option(engagementWindowDays) /* 1 */,
        granularity = granularity /* 2 */,
        level = Option(level) /* 1 */,
        metricsFilters = metricsFilters.map(_.asModel) /* 5 */,
        primarySort = Option(primarySort) /* 1 */,
        productGroupIds = productGroupIds /* 2 */,
        productGroupStatuses = productGroupStatuses.map(_.asModel) /* 5 */,
        productItemIds = productItemIds /* 2 */,
        reportFormat = Option(reportFormat) /* 1 */,
        reportingTimezone = Option(reportingTimezone) /* 1 */,
        startDate = startDate /* 2 */,
        startHour = Option(startHour) /* 1 */,
        targetingTypes = targetingTypes.map(_.asModel) /* 5 */,
        viewWindowDays = Option(viewWindowDays) /* 1 */
    
    )
  }
}

object AdsAnalyticsCreateAsyncRequestData {

  def validated(d8a : AdsAnalyticsCreateAsyncRequestData, failFast : Boolean) : scala.util.Try[AdsAnalyticsCreateAsyncRequest] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AdsAnalyticsCreateAsyncRequestData = try {
        val data = read[AdsAnalyticsCreateAsyncRequestData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AdsAnalyticsCreateAsyncRequestData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AdsAnalyticsCreateAsyncRequestData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AdsAnalyticsCreateAsyncRequestData] = try {
        read[List[AdsAnalyticsCreateAsyncRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AdsAnalyticsCreateAsyncRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AdsAnalyticsCreateAsyncRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AdsAnalyticsCreateAsyncRequestData] = try {
        read[Map[String, AdsAnalyticsCreateAsyncRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AdsAnalyticsCreateAsyncRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AdsAnalyticsCreateAsyncRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

