/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** CampaignDeliveryEstimatesCampaignData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CampaignDeliveryEstimatesCampaignData(
  adGroups: Seq[AdGroupDeliveryEstimatesData],
/* Duration type of the budget */
  budgetDurationType: BudgetDurationType,
/* Campaign daily spend cap. When the budget_duration_type is FIXED_DAILY or FLEXIBLE_DAILY, this field is required. */
  dailySpendCap: Int = 0 ,
/* End date of the date range for an ad campaign, pattern YYYY-MM-DD. Required when budget_duration_type is LIFETIME. */
  endDate: String = "" ,
/* Campaign total spending cap. When the budget_duration_type is LIFETIME, this field is required. The end date field is also required for lifetime spending. */
  lifetimeSpendCap: Int = 0 ,
  objectiveType: DeliveryEstimateObjectiveType,
/* Start date of the date range for an ad campaign, pattern YYYY-MM-DD. */
  startDate: String
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== adGroups validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (adGroups == null) {
            _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.adGroups, "adGroups is a required field and cannot be null")
            }
        }
        
        // validate min items 1
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroups == null then 0 else adGroups.size
            if (len < 1) {
               _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.adGroups, s"adGroups has $len, which is less than the min items 1")
            }
        }
        // validate min items 10
        if (_allValidationErrors.isEmpty || !failFast) {
          val len = if adGroups == null then 0 else adGroups.size
            if (len > 10) {
               _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.adGroups, s"adGroups has $len, which is greater than the max items 10")
            }
        }
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (adGroups != null) {
                adGroups.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ CampaignDeliveryEstimatesCampaign.Fields.adGroups :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== budgetDurationType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (budgetDurationType == null) {
            _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.budgetDurationType, "budgetDurationType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== dailySpendCap validation ==================
        
        
        
        

        // ================== endDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if endDate == null || !regex.r.matches(endDate) then
              _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.endDate, s"value '$endDate' doesn't match pattern $regex")
        }
        
        
        
        

        // ================== lifetimeSpendCap validation ==================
        
        
        
        

        // ================== objectiveType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (objectiveType == null) {
            _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.objectiveType, "objectiveType is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== startDate validation ==================
        // validate against pattern '^\\d{4}-\\d{2}-\\d{2}$'
        if (_allValidationErrors.isEmpty || !failFast) {
           val regex = """^\\d{4}-\\d{2}-\\d{2}$"""
           if startDate == null || !regex.r.matches(startDate) then
              _allValidationErrors += ValidationError(path :+ CampaignDeliveryEstimatesCampaign.Fields.startDate, s"value '$startDate' doesn't match pattern $regex")
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[CampaignDeliveryEstimatesCampaign] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CampaignDeliveryEstimatesCampaign = {
    CampaignDeliveryEstimatesCampaign(
        adGroups = adGroups.map(_.asModel) /* 5 */,
        budgetDurationType = budgetDurationType /* 2 */,
        dailySpendCap = Option(dailySpendCap) /* 1 */,
        endDate = Option(endDate) /* 1 */,
        lifetimeSpendCap = Option(lifetimeSpendCap) /* 1 */,
        objectiveType = objectiveType /* 2 */,
        startDate = startDate /* 2 */
    
    )
  }
}

object CampaignDeliveryEstimatesCampaignData {

  def validated(d8a : CampaignDeliveryEstimatesCampaignData, failFast : Boolean) : scala.util.Try[CampaignDeliveryEstimatesCampaign] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : CampaignDeliveryEstimatesCampaignData = try {
        val data = read[CampaignDeliveryEstimatesCampaignData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating CampaignDeliveryEstimatesCampaignData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : CampaignDeliveryEstimatesCampaignData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[CampaignDeliveryEstimatesCampaignData] = try {
        read[List[CampaignDeliveryEstimatesCampaignData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CampaignDeliveryEstimatesCampaign]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CampaignDeliveryEstimatesCampaign]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CampaignDeliveryEstimatesCampaignData] = try {
        read[Map[String, CampaignDeliveryEstimatesCampaignData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CampaignDeliveryEstimatesCampaign]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CampaignDeliveryEstimatesCampaign]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

