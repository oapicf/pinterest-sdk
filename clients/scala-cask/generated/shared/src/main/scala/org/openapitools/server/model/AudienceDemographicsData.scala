/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * OpenAPI spec version: 5.28.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** AudienceDemographicsData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class AudienceDemographicsData(
/* Ages distribution. */
  ages: Seq[AudienceDemographicValueData] = Nil ,
/* Country area distribution. */
  countries: Seq[AudienceDemographicValueData] = Nil ,
/* Device usage distribution. */
  devices: Seq[AudienceDemographicValueData] = Nil ,
/* Gender distribution. */
  genders: Seq[AudienceDemographicValueData] = Nil ,
/* Geographic metro area distribution. */
  metros: Seq[AudienceDemographicValueData] = Nil 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== ages validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (ages != null) {
                ages.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceDemographics.Fields.ages :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== countries validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (countries != null) {
                countries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceDemographics.Fields.countries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== devices validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (devices != null) {
                devices.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceDemographics.Fields.devices :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== genders validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (genders != null) {
                genders.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceDemographics.Fields.genders :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== metros validation ==================
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (metros != null) {
                metros.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ AudienceDemographics.Fields.metros :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[AudienceDemographics] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : AudienceDemographics = {
    AudienceDemographics(
        ages = ages.map(_.asModel) /* 5 */,
        countries = countries.map(_.asModel) /* 5 */,
        devices = devices.map(_.asModel) /* 5 */,
        genders = genders.map(_.asModel) /* 5 */,
        metros = metros.map(_.asModel) /* 5 */
    
    )
  }
}

object AudienceDemographicsData {

  def validated(d8a : AudienceDemographicsData, failFast : Boolean) : scala.util.Try[AudienceDemographics] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : AudienceDemographicsData = try {
        val data = read[AudienceDemographicsData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating AudienceDemographicsData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : AudienceDemographicsData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[AudienceDemographicsData] = try {
        read[List[AudienceDemographicsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[AudienceDemographics]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[AudienceDemographics]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, AudienceDemographicsData] = try {
        read[Map[String, AudienceDemographicsData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, AudienceDemographics]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, AudienceDemographics]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

