/*
 * Pinterest REST API
 *
 * Pinterest&#39;s REST API
 *
 * The version of the OpenAPI document: 5.28.0
 *
 * Generated by: https://openapi-generator.tech
 */

//! Pinterest REST API handlers
//!
//! API version: 5.28.0
//!

use salvo::prelude::*;
use salvo::oapi::extract::{JsonBody, PathParam, QueryParam, HeaderParam, FormBody};
use serde::{Deserialize, Serialize};
use validator::Validate;

use crate::models::{self, *};

/// Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
#[salvo::oapi::endpoint(
    operation_id = "oauth_slash_conversion_token",
    tags("oauth"),
    security(("OAuth2" = ["ads:write"]))
)]
pub async fn oauth_slash_conversion_token(
    res: &mut Response,
) -> Result<(), StatusError> {
    // Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.

    res.status_code(StatusCode::NOT_IMPLEMENTED);
    Ok(())
}

/// Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the &#x60;continuous_refresh&#x60; parameter to &#x60;true&#x60; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 
#[salvo::oapi::endpoint(
    operation_id = "oauth_slash_token",
    tags("oauth"),
    security(("BasicAuth" = []))
)]
pub async fn oauth_slash_token(
    grant_type: FormBody<models::TokenGrantType>,
    code: FormBody<String>,
    continuous_refresh: FormBody<String>,
    redirect_uri: FormBody<String>,
    refresh_token: FormBody<String>,
    scope: FormBody<String>,
    res: &mut Response,
) -> Result<(), StatusError> {
    // Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for [requesting and refreshing tokens](/docs/getting-started/set-up-authentication-and-authorization/).  **Note:** If your app was created **before September 25, 2025**, make sure to set the &#x60;continuous_refresh&#x60; parameter to &#x60;true&#x60; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use [Token Debugger](/docs/developer-tools/token-debugger/) to validate and inspect your access token. 

    res.status_code(StatusCode::NOT_IMPLEMENTED);
    Ok(())
}

/// Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
#[salvo::oapi::endpoint(
    operation_id = "token_slash_revoke",
    tags("oauth"),
    security(("BasicAuth" = []))
)]
pub async fn token_slash_revoke(
    token: FormBody<String>,
    token_type_hint: FormBody<models::TokenTypeHint>,
    res: &mut Response,
) -> Result<(), StatusError> {
    // Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.

    res.status_code(StatusCode::NOT_IMPLEMENTED);
    Ok(())
}

