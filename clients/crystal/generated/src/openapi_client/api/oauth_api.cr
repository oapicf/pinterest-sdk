# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "uri"

module OpenAPIClient
  class OauthApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Generate OAuth access token for conversion API
    # Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
    # @return [ConversionAccessTokenResponse]
    def oauth_conversion_token()
      data, _status_code, _headers = oauth_conversion_token_with_http_info()
      data
    end

    # Generate OAuth access token for conversion API
    # Generate a new and long-lived OAuth access token dedicated for sending conversions using a valid access token.
    # @return [Array<(ConversionAccessTokenResponse, Integer, Hash)>] ConversionAccessTokenResponse data, response status code and response headers
    def oauth_conversion_token_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: OauthApi.oauth_conversion_token ..."}
      end
      # resource path
      local_var_path = "/oauth/conversion_token"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "ConversionAccessTokenResponse"

      # auth_names
      auth_names = ["pinterest_oauth2"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"OauthApi.oauth_conversion_token",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: OauthApi#oauth_conversion_token\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return ConversionAccessTokenResponse.from_json(data), status_code, headers
    end

    # Generate OAuth access token
    # Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for <a href='/docs/getting-started/set-up-authentication-and-authorization/' target='blank'>requesting and refreshing tokens</a>.  <strong>Note:</strong> If your app was created <strong>before September 25, 2025</strong>, make sure to set the <code>continuous_refresh</code> parameter to <code>true</code> to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use <a href='/docs/developer-tools/token-debugger/' target='blank'>Token Debugger</a> to validate and inspect your access token.
    # @param grant_type [String] 
    # @return [OauthAccessTokenResponse]
    def oauth_token(grant_type : String)
      data, _status_code, _headers = oauth_token_with_http_info(grant_type)
      data
    end

    # Generate OAuth access token
    # Generate a new OAuth access token using an authorization code; or refresh an existing one using a continuous refresh token.  Follow the complete steps for &lt;a href&#x3D;&#39;/docs/getting-started/set-up-authentication-and-authorization/&#39; target&#x3D;&#39;blank&#39;&gt;requesting and refreshing tokens&lt;/a&gt;.  &lt;strong&gt;Note:&lt;/strong&gt; If your app was created &lt;strong&gt;before September 25, 2025&lt;/strong&gt;, make sure to set the &lt;code&gt;continuous_refresh&lt;/code&gt; parameter to &lt;code&gt;true&lt;/code&gt; to use the continuous refresh token (60-day expiration, refreshable indefinitely). Pinterest no longer supports the legacy refresh token (365-day expiration, hard limit).  Disregard this note if your app was activated on or after September 25, 2025. You are automatically using the continuous refresh token.  Use &lt;a href&#x3D;&#39;/docs/developer-tools/token-debugger/&#39; target&#x3D;&#39;blank&#39;&gt;Token Debugger&lt;/a&gt; to validate and inspect your access token.
    # @param grant_type [String] 
    # @return [Array<(OauthAccessTokenResponse, Integer, Hash)>] OauthAccessTokenResponse data, response status code and response headers
    def oauth_token_with_http_info(grant_type : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: OauthApi.oauth_token ..."}
      end
      # verify the required parameter "grant_type" is set
      if @api_client.config.client_side_validation && grant_type.nil?
        raise ArgumentError.new("Missing the required parameter 'grant_type' when calling OauthApi.oauth_token")
      end
      # verify enum value
      allowable_values = ["authorization_code", "refresh_token", "client_credentials"]
      if @api_client.config.client_side_validation && !allowable_values.includes?(grant_type)
        raise ArgumentError.new("invalid value for \"grant_type\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/oauth/token"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/x-www-form-urlencoded"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"grant_type"] = grant_type unless grant_type.nil?

      # http body (model)
      post_body = nil

      # return_type
      return_type = "OauthAccessTokenResponse"

      # auth_names
      auth_names = ["basic"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"OauthApi.oauth_token",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: OauthApi#oauth_token\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return OauthAccessTokenResponse.from_json(data), status_code, headers
    end

    # Revoke a token
    # Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
    # @param token [String] The token to revoke.
    # @return [nil]
    def token_revoke(token : String, token_type_hint : String? = nil)
      token_revoke_with_http_info(token, token_type_hint)
      nil
    end

    # Revoke a token
    # Revokes an access or refresh token. Only tokens issued for system users are currently supported. Revoked tokens become immediately invalid and unusable.
    # @param token [String] The token to revoke.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def token_revoke_with_http_info(token : String, token_type_hint : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: OauthApi.token_revoke ..."}
      end
      # verify the required parameter "token" is set
      if @api_client.config.client_side_validation && token.nil?
        raise ArgumentError.new("Missing the required parameter 'token' when calling OauthApi.token_revoke")
      end
      allowable_values = ["access_token", "refresh_token"]
      if @api_client.config.client_side_validation && token_type_hint && !allowable_values.includes?(token_type_hint)
        raise ArgumentError.new("invalid value for \"token_type_hint\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/oauth/token/revoke"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/x-www-form-urlencoded"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"token"] = token unless token.nil?
      form_params[:"token_type_hint"] = token_type_hint unless token_type_hint.nil?

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["basic"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"OauthApi.token_revoke",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: OauthApi#token_revoke\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end
  end
end
