# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Request containing OTP and Amazon storefront info called by Amazon
  class AmazonConnectRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The Amazon storefront name
    @[JSON::Field(key: "amazon_storefront_name", type: String, nillable: false, emit_null: false)]
    property amazon_storefront_name : String

    # The Amazon storefront url
    @[JSON::Field(key: "amazon_storefront_url", type: String, nillable: false, emit_null: false)]
    property amazon_storefront_url : String

    # The Amazon account linking status
    @[JSON::Field(key: "is_amazon_account_linked", type: Bool, nillable: false, emit_null: false)]
    property is_amazon_account_linked : Bool

    # Optional properties
    # The Amazon storefront id
    @[JSON::Field(key: "amazon_storefront_id", type: String?, nillable: true, emit_null: false)]
    property amazon_storefront_id : String?

    # The Amazon user id
    @[JSON::Field(key: "amazon_user_id", type: String?, nillable: true, emit_null: false)]
    property amazon_user_id : String?

    # The one time passcode for Pinterest-initiated linking requests
    @[JSON::Field(key: "one_time_passcode", type: String?, nillable: true, emit_null: false)]
    property one_time_passcode : String?

    # The Pinterest user id for Amazon-initiated linking requests
    @[JSON::Field(key: "pinterest_user_id", type: String?, nillable: true, emit_null: false)]
    property pinterest_user_id : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@amazon_storefront_name : String, @amazon_storefront_url : String, @is_amazon_account_linked : Bool, @amazon_storefront_id : String? = nil, @amazon_user_id : String? = nil, @one_time_passcode : String? = nil, @pinterest_user_id : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@pinterest_user_id.nil? && @pinterest_user_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"pinterest_user_id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@pinterest_user_id.nil? && @pinterest_user_id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] pinterest_user_id Value to be assigned
    def pinterest_user_id=(pinterest_user_id)
      pattern = /^\d+$/
      if !pinterest_user_id.nil? && pinterest_user_id !~ pattern
        raise ArgumentError.new("invalid value for \"pinterest_user_id\", must conform to the pattern #{pattern}.")
      end

      @pinterest_user_id = pinterest_user_id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          amazon_storefront_id == other.amazon_storefront_id &&
          amazon_storefront_name == other.amazon_storefront_name &&
          amazon_storefront_url == other.amazon_storefront_url &&
          amazon_user_id == other.amazon_user_id &&
          is_amazon_account_linked == other.is_amazon_account_linked &&
          one_time_passcode == other.one_time_passcode &&
          pinterest_user_id == other.pinterest_user_id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [amazon_storefront_id, amazon_storefront_name, amazon_storefront_url, amazon_user_id, is_amazon_account_linked, one_time_passcode, pinterest_user_id].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["amazon_storefront_id"] = _to_h(amazon_storefront_id)
      hash["amazon_storefront_name"] = _to_h(amazon_storefront_name)
      hash["amazon_storefront_url"] = _to_h(amazon_storefront_url)
      hash["amazon_user_id"] = _to_h(amazon_user_id)
      hash["is_amazon_account_linked"] = _to_h(is_amazon_account_linked)
      hash["one_time_passcode"] = _to_h(one_time_passcode)
      hash["pinterest_user_id"] = _to_h(pinterest_user_id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
