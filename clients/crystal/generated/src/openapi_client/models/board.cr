# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class Board
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    #      Name of the board.      **Note:** If you create an ad-only board by setting `is_ads_only`     to `true`, the board name automatically becomes \"Ad-only Pins\".
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    # Date and time of last board pins modified.
    @[JSON::Field(key: "board_pins_modified_at", type: Time?, nillable: true, emit_null: false)]
    property board_pins_modified_at : Time?

    # Count of collaborators on the board.
    @[JSON::Field(key: "collaborator_count", type: Int32?, nillable: true, emit_null: false)]
    property collaborator_count : Int32?

    # Date and time of board creation.
    @[JSON::Field(key: "created_at", type: Time?, nillable: true, emit_null: false)]
    property created_at : Time?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Board follower count.
    @[JSON::Field(key: "follower_count", type: Int32?, nillable: true, emit_null: false)]
    property follower_count : Int32?

    # If set to `true`, the board will be ad-only and can store ad-only Pins.
    @[JSON::Field(key: "is_ads_only", type: Bool?, default: false, nillable: true, emit_null: false)]
    property is_ads_only : Bool?

    # Board media.
    @[JSON::Field(key: "media", type: BoardMedia?, nillable: true, emit_null: false)]
    property media : BoardMedia?

    @[JSON::Field(key: "owner", type: BoardOwner?, nillable: true, emit_null: false)]
    property owner : BoardOwner?

    # Count of Pins on the board.
    @[JSON::Field(key: "pin_count", type: Int32?, nillable: true, emit_null: false)]
    property pin_count : Int32?

    #     Privacy setting for a board. Learn more about [secret](https://help.pinterest.com/en/article/secret-boards)     boards and [protected](https://help.pinterest.com/en/business/article/protected-boards) boards.      **Note:** If you create an ad-only board by setting `is_ads_only`     to `true`, the `privacy` settng automatically becomes `PROTECTED`. 
    @[JSON::Field(key: "privacy", type: BoardPrivacy?, nillable: true, emit_null: false)]
    property privacy : BoardPrivacy?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"BoardBase"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @name : String, @board_pins_modified_at : Time? = nil, @collaborator_count : Int32? = nil, @created_at : Time? = nil, @description : String? = nil, @follower_count : Int32? = nil, @is_ads_only : Bool? = nil, @media : BoardMedia? = nil, @owner : BoardOwner? = nil, @pin_count : Int32? = nil, @privacy : BoardPrivacy? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@collaborator_count.nil? && @collaborator_count.try &.< 0
        invalid_properties.push("invalid value for \"collaborator_count\", must be greater than or equal to 0.")
      end

      if !@follower_count.nil? && @follower_count.try &.< 0
        invalid_properties.push("invalid value for \"follower_count\", must be greater than or equal to 0.")
      end

      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      if !@pin_count.nil? && @pin_count.try &.< 0
        invalid_properties.push("invalid value for \"pin_count\", must be greater than or equal to 0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@collaborator_count.nil? && @collaborator_count.try &.< 0
      return false if !@follower_count.nil? && @follower_count.try &.< 0
      return false if @id.try &.!~ /^\d+$/
      return false if !@pin_count.nil? && @pin_count.try &.< 0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] collaborator_count Value to be assigned
    def collaborator_count=(collaborator_count)
      if !collaborator_count.nil? && collaborator_count < 0
        raise ArgumentError.new("invalid value for \"collaborator_count\", must be greater than or equal to 0.")
      end

      @collaborator_count = collaborator_count
    end

    # Custom attribute writer method with validation
    # @param [Object] follower_count Value to be assigned
    def follower_count=(follower_count)
      if !follower_count.nil? && follower_count < 0
        raise ArgumentError.new("invalid value for \"follower_count\", must be greater than or equal to 0.")
      end

      @follower_count = follower_count
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method with validation
    # @param [Object] pin_count Value to be assigned
    def pin_count=(pin_count)
      if !pin_count.nil? && pin_count < 0
        raise ArgumentError.new("invalid value for \"pin_count\", must be greater than or equal to 0.")
      end

      @pin_count = pin_count
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          board_pins_modified_at == other.board_pins_modified_at &&
          collaborator_count == other.collaborator_count &&
          created_at == other.created_at &&
          description == other.description &&
          follower_count == other.follower_count &&
          id == other.id &&
          is_ads_only == other.is_ads_only &&
          media == other.media &&
          name == other.name &&
          owner == other.owner &&
          pin_count == other.pin_count &&
          privacy == other.privacy
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [board_pins_modified_at, collaborator_count, created_at, description, follower_count, id, is_ads_only, media, name, owner, pin_count, privacy].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["board_pins_modified_at"] = _to_h(board_pins_modified_at)
      hash["collaborator_count"] = _to_h(collaborator_count)
      hash["created_at"] = _to_h(created_at)
      hash["description"] = _to_h(description)
      hash["follower_count"] = _to_h(follower_count)
      hash["id"] = _to_h(id)
      hash["is_ads_only"] = _to_h(is_ads_only)
      hash["media"] = _to_h(media)
      hash["name"] = _to_h(name)
      hash["owner"] = _to_h(owner)
      hash["pin_count"] = _to_h(pin_count)
      hash["privacy"] = _to_h(privacy)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
