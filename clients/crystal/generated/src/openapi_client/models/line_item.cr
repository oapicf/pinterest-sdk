# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class LineItem
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Product brand. For example, 'Parker'.
    @[JSON::Field(key: "product_brand", type: String?, nillable: true, emit_null: false)]
    property product_brand : String?

    # Product category. For example, 'Shoes'.
    @[JSON::Field(key: "product_category", type: String?, nillable: true, emit_null: false)]
    property product_category : String?

    # Product ID. For example, 1414.
    @[JSON::Field(key: "product_id", type: Int32?, nillable: true, emit_null: false)]
    property product_id : Int32?

    # Product name. For example, 'Parker Boots'.
    @[JSON::Field(key: "product_name", type: String?, nillable: true, emit_null: false)]
    property product_name : String?

    # Product price. For example, '99.99'.
    @[JSON::Field(key: "product_price", type: String?, nillable: true, emit_null: false)]
    property product_price : String?

    # Product quantity. For example, 2.
    @[JSON::Field(key: "product_quantity", type: Int32?, nillable: true, emit_null: false)]
    property product_quantity : Int32?

    # Product variant. For example, 'Red'.
    @[JSON::Field(key: "product_variant", type: String?, nillable: true, emit_null: false)]
    property product_variant : String?

    # Product variant ID. For example, '1414-34832'.
    @[JSON::Field(key: "product_variant_id", type: String?, nillable: true, emit_null: false)]
    property product_variant_id : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@product_brand : String? = nil, @product_category : String? = nil, @product_id : Int32? = nil, @product_name : String? = nil, @product_price : String? = nil, @product_quantity : Int32? = nil, @product_variant : String? = nil, @product_variant_id : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          product_brand == other.product_brand &&
          product_category == other.product_category &&
          product_id == other.product_id &&
          product_name == other.product_name &&
          product_price == other.product_price &&
          product_quantity == other.product_quantity &&
          product_variant == other.product_variant &&
          product_variant_id == other.product_variant_id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [product_brand, product_category, product_id, product_name, product_price, product_quantity, product_variant, product_variant_id].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["product_brand"] = _to_h(product_brand)
      hash["product_category"] = _to_h(product_category)
      hash["product_id"] = _to_h(product_id)
      hash["product_name"] = _to_h(product_name)
      hash["product_price"] = _to_h(product_price)
      hash["product_quantity"] = _to_h(product_quantity)
      hash["product_variant"] = _to_h(product_variant)
      hash["product_variant_id"] = _to_h(product_variant_id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
