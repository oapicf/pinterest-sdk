# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Set of quality components, with each component containing a event coverage and details.
  class QualityComponents
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "advertiser_external_id", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property advertiser_external_id : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "click_id_epik", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property click_id_epik : Hash(String, QualityComponentDetails)?

    # Dedup components.
    @[JSON::Field(key: "external_event_id", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property external_event_id : Hash(String, QualityComponentDetails)?

    # User matching identifiers.
    @[JSON::Field(key: "hashed_email", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property hashed_email : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "hashed_maid", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property hashed_maid : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "ip_address", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property ip_address : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "order_id", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property order_id : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "order_value", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property order_value : Hash(String, QualityComponentDetails)?

    # Product/event metadata.
    @[JSON::Field(key: "product_id", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property product_id : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "source_url", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property source_url : Hash(String, QualityComponentDetails)?

    @[JSON::Field(key: "user_agent", type: Hash(String, QualityComponentDetails)?, nillable: true, emit_null: false)]
    property user_agent : Hash(String, QualityComponentDetails)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@advertiser_external_id : Hash(String, QualityComponentDetails)? = nil, @click_id_epik : Hash(String, QualityComponentDetails)? = nil, @external_event_id : Hash(String, QualityComponentDetails)? = nil, @hashed_email : Hash(String, QualityComponentDetails)? = nil, @hashed_maid : Hash(String, QualityComponentDetails)? = nil, @ip_address : Hash(String, QualityComponentDetails)? = nil, @order_id : Hash(String, QualityComponentDetails)? = nil, @order_value : Hash(String, QualityComponentDetails)? = nil, @product_id : Hash(String, QualityComponentDetails)? = nil, @source_url : Hash(String, QualityComponentDetails)? = nil, @user_agent : Hash(String, QualityComponentDetails)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          advertiser_external_id == other.advertiser_external_id &&
          click_id_epik == other.click_id_epik &&
          external_event_id == other.external_event_id &&
          hashed_email == other.hashed_email &&
          hashed_maid == other.hashed_maid &&
          ip_address == other.ip_address &&
          order_id == other.order_id &&
          order_value == other.order_value &&
          product_id == other.product_id &&
          source_url == other.source_url &&
          user_agent == other.user_agent
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [advertiser_external_id, click_id_epik, external_event_id, hashed_email, hashed_maid, ip_address, order_id, order_value, product_id, source_url, user_agent].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["advertiser_external_id"] = _to_h(advertiser_external_id)
      hash["click_id_epik"] = _to_h(click_id_epik)
      hash["external_event_id"] = _to_h(external_event_id)
      hash["hashed_email"] = _to_h(hashed_email)
      hash["hashed_maid"] = _to_h(hashed_maid)
      hash["ip_address"] = _to_h(ip_address)
      hash["order_id"] = _to_h(order_id)
      hash["order_value"] = _to_h(order_value)
      hash["product_id"] = _to_h(product_id)
      hash["source_url"] = _to_h(source_url)
      hash["user_agent"] = _to_h(user_agent)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
