# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class AdPreviewShopping
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Catalog Product Group Id.
    @[JSON::Field(key: "catalog_product_group_id", type: String, nillable: false, emit_null: false)]
    property catalog_product_group_id : String

    # Ad format of the shopping ad preview.
    @[JSON::Field(key: "creative_type", type: String, nillable: false, emit_null: false)]
    property creative_type : String

    # Optional properties
    # Select a call to action (CTA) to display below your ad. CTA options for catalog sales campaigns are `SHOP_NOW`, `BOOK_NOW`, `ON_SALE`, `GET_DEAL`, `BUY_ONLINE_PICKUP_IN_STORE`
    @[JSON::Field(key: "customizable_cta_type", type: CustomizableCTAType?, nillable: true, emit_null: false)]
    property customizable_cta_type : CustomizableCTAType?

    # Title displayed below ad.
    @[JSON::Field(key: "hero_image_title", type: String?, nillable: true, emit_null: false)]
    property hero_image_title : String?

    # Hero image URL.
    @[JSON::Field(key: "hero_image_url", type: String?, nillable: true, emit_null: false)]
    property hero_image_url : String?

    # Pin id for the hero image. When creative type is COLLECTION, either hero_pin_id or (hero_image_url, hero_image_title) is required.
    @[JSON::Field(key: "hero_pin_id", type: String?, nillable: true, emit_null: false)]
    property hero_pin_id : String?

    # Multi image template tag.
    @[JSON::Field(key: "image_tag", type: String?, nillable: true, emit_null: false)]
    property image_tag : String?

    # Item id for product to preview standard shopping ads, optional and only applicable when creative type is SHOPPING.
    @[JSON::Field(key: "item_id", type: String?, nillable: true, emit_null: false)]
    property item_id : String?

    # Preferred media type.
    @[JSON::Field(key: "preferred_media_type", type: String?, nillable: true, emit_null: false)]
    property preferred_media_type : String?

    # Multi video template tag, image_tag and video_tag are mutual exclusive.
    @[JSON::Field(key: "video_tag", type: String?, nillable: true, emit_null: false)]
    property video_tag : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForCreative_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "creative_type"
        @allowable_values = ["SHOPPING", "CAROUSEL", "COLLECTION", "REGULAR"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForPreferred_media_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "preferred_media_type"
        @allowable_values = ["VIDEO", "IMAGE"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@catalog_product_group_id : String, @creative_type : String, @customizable_cta_type : CustomizableCTAType? = nil, @hero_image_title : String? = nil, @hero_image_url : String? = nil, @hero_pin_id : String? = nil, @image_tag : String? = nil, @item_id : String? = nil, @preferred_media_type : String? = nil, @video_tag : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if @catalog_product_group_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"catalog_product_group_id\", must conform to the pattern #{pattern}.")
      end

      creative_type_validator = EnumAttributeValidatorForCreative_type.new
      if !creative_type_validator.valid?(@creative_type)
        message = creative_type_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if !@hero_pin_id.nil? && @hero_pin_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"hero_pin_id\", must conform to the pattern #{pattern}.")
      end

      preferred_media_type_validator = EnumAttributeValidatorForPreferred_media_type.new
      if !preferred_media_type_validator.valid?(@preferred_media_type)
        message = preferred_media_type_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @catalog_product_group_id.try &.!~ /^\d+$/
      creative_type_validator = EnumAttributeValidatorForCreative_type.new
      return false unless creative_type_validator.valid?(@creative_type)
      return false if !@hero_pin_id.nil? && @hero_pin_id.try &.!~ /^\d+$/
      preferred_media_type_validator = EnumAttributeValidatorForPreferred_media_type.new
      return false unless preferred_media_type_validator.valid?(@preferred_media_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] catalog_product_group_id Value to be assigned
    def catalog_product_group_id=(catalog_product_group_id)
      pattern = /^\d+$/
      if catalog_product_group_id !~ pattern
        raise ArgumentError.new("invalid value for \"catalog_product_group_id\", must conform to the pattern #{pattern}.")
      end

      @catalog_product_group_id = catalog_product_group_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] creative_type Object to be assigned
    def creative_type=(creative_type)
      validator = EnumAttributeValidatorForCreative_type.new
      unless validator.valid?(creative_type)
        raise ArgumentError.new(validator.message)
      end
      @creative_type = creative_type
    end

    # Custom attribute writer method with validation
    # @param [Object] hero_pin_id Value to be assigned
    def hero_pin_id=(hero_pin_id)
      pattern = /^\d+$/
      if !hero_pin_id.nil? && hero_pin_id !~ pattern
        raise ArgumentError.new("invalid value for \"hero_pin_id\", must conform to the pattern #{pattern}.")
      end

      @hero_pin_id = hero_pin_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] preferred_media_type Object to be assigned
    def preferred_media_type=(preferred_media_type)
      validator = EnumAttributeValidatorForPreferred_media_type.new
      unless validator.valid?(preferred_media_type)
        raise ArgumentError.new(validator.message)
      end
      @preferred_media_type = preferred_media_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          catalog_product_group_id == other.catalog_product_group_id &&
          creative_type == other.creative_type &&
          customizable_cta_type == other.customizable_cta_type &&
          hero_image_title == other.hero_image_title &&
          hero_image_url == other.hero_image_url &&
          hero_pin_id == other.hero_pin_id &&
          image_tag == other.image_tag &&
          item_id == other.item_id &&
          preferred_media_type == other.preferred_media_type &&
          video_tag == other.video_tag
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [catalog_product_group_id, creative_type, customizable_cta_type, hero_image_title, hero_image_url, hero_pin_id, image_tag, item_id, preferred_media_type, video_tag].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["catalog_product_group_id"] = _to_h(catalog_product_group_id)
      hash["creative_type"] = _to_h(creative_type)
      hash["customizable_cta_type"] = _to_h(customizable_cta_type)
      hash["hero_image_title"] = _to_h(hero_image_title)
      hash["hero_image_url"] = _to_h(hero_image_url)
      hash["hero_pin_id"] = _to_h(hero_pin_id)
      hash["image_tag"] = _to_h(image_tag)
      hash["item_id"] = _to_h(item_id)
      hash["preferred_media_type"] = _to_h(preferred_media_type)
      hash["video_tag"] = _to_h(video_tag)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
