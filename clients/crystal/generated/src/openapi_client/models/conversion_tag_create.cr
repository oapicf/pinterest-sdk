# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class ConversionTagCreate
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Conversion tag name.
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    # Whether Automatic Enhanced Match email is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_enabled : Bool?

    # Metadata ingestion frequency.
    @[JSON::Field(key: "md_frequency", type: Float64?, default: 1, nillable: true, emit_null: false)]
    property md_frequency : Float64?

    # Whether Automatic Enhanced Match name is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_fnln_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_fnln_enabled : Bool?

    # Whether Automatic Enhanced Match phone is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_ph_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_ph_enabled : Bool?

    # Whether Automatic Enhanced Match gender is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_ge_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_ge_enabled : Bool?

    # Whether Automatic Enhanced Match birthdate is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_db_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_db_enabled : Bool?

    # Whether Automatic Enhanced Match location is enabled. See <a href=\"https://help.pinterest.com/en/business/article/enhanced-match\" target=\"_blank\">Enhanced match</a> for more information.
    @[JSON::Field(key: "aem_loc_enabled", type: Bool?, default: false, nillable: true, emit_null: false)]
    property aem_loc_enabled : Bool?

    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"ConversionTagConfigs"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @aem_enabled : Bool? = nil, @md_frequency : Float64? = nil, @aem_fnln_enabled : Bool? = nil, @aem_ph_enabled : Bool? = nil, @aem_ge_enabled : Bool? = nil, @aem_db_enabled : Bool? = nil, @aem_loc_enabled : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          aem_enabled == other.aem_enabled &&
          md_frequency == other.md_frequency &&
          aem_fnln_enabled == other.aem_fnln_enabled &&
          aem_ph_enabled == other.aem_ph_enabled &&
          aem_ge_enabled == other.aem_ge_enabled &&
          aem_db_enabled == other.aem_db_enabled &&
          aem_loc_enabled == other.aem_loc_enabled &&
          name == other.name
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [aem_enabled, md_frequency, aem_fnln_enabled, aem_ph_enabled, aem_ge_enabled, aem_db_enabled, aem_loc_enabled, name].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["aem_enabled"] = _to_h(aem_enabled)
      hash["md_frequency"] = _to_h(md_frequency)
      hash["aem_fnln_enabled"] = _to_h(aem_fnln_enabled)
      hash["aem_ph_enabled"] = _to_h(aem_ph_enabled)
      hash["aem_ge_enabled"] = _to_h(aem_ge_enabled)
      hash["aem_db_enabled"] = _to_h(aem_db_enabled)
      hash["aem_loc_enabled"] = _to_h(aem_loc_enabled)
      hash["name"] = _to_h(name)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
