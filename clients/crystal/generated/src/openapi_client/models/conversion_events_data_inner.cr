# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class ConversionEventsDataInner
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # <p>The source indicating where the conversion event occurred.</p> - `app_android` - `app_ios` - `web` - `offline`
    @[JSON::Field(key: "action_source", type: String, nillable: false, emit_null: false)]
    property action_source : String

    # A unique id string that identifies this event and can be used for deduping between events ingested via both the conversion API and Pinterest tracking. Without this, event's data is likely to be double counted and will cause report metric inflation. Third-party vendors make sure this field is updated on both Pinterest tag and Conversions API side before rolling out template for Conversions API.
    @[JSON::Field(key: "event_id", type: String, nillable: false, emit_null: false)]
    property event_id : String

    # <p>The type of the user event. Please use the right event_name; otherwise the event will not be accepted and show up correctly in reports.</p>  - `add_payment_info` - `add_to_cart` - `add_to_wishlist` - `app_install` - `checkout` - `custom` - `initiate_checkout` - `lead` - `page_visit` - `search` - `signup` - `subscribe` - `view_category` - `view_content` - `watch_video`
    @[JSON::Field(key: "event_name", type: String, nillable: false, emit_null: false)]
    property event_name : String

    # The time when the event happened. Unix timestamp in seconds.
    @[JSON::Field(key: "event_time", type: Int64, nillable: false, emit_null: false)]
    property event_time : Int64

    @[JSON::Field(key: "user_data", type: ConversionEventsUserData, nillable: false, emit_null: false)]
    property user_data : ConversionEventsUserData

    # Optional properties
    # The app store app ID.
    @[JSON::Field(key: "app_id", type: String?, nillable: true, emit_null: false)]
    property app_id : String?

    @[JSON::Field(key: "app_info", type: ConversionEventAppInfo?, nillable: true, emit_null: false)]
    property app_info : ConversionEventAppInfo?

    # Name of the app.
    @[JSON::Field(key: "app_name", type: String?, nillable: true, emit_null: false)]
    property app_name : String?

    # Version of the app.
    @[JSON::Field(key: "app_version", type: String?, nillable: true, emit_null: false)]
    property app_version : String?

    @[JSON::Field(key: "custom_data", type: ConversionEventsDataInnerCustomData?, nillable: true, emit_null: false)]
    property custom_data : ConversionEventsDataInnerCustomData?

    # Brand of the user device.
    @[JSON::Field(key: "device_brand", type: String?, nillable: true, emit_null: false)]
    property device_brand : String?

    # User device's mobile carrier.
    @[JSON::Field(key: "device_carrier", type: String?, nillable: true, emit_null: false)]
    property device_carrier : String?

    @[JSON::Field(key: "device_info", type: ConversionEventDeviceInfo?, nillable: true, emit_null: false)]
    property device_info : ConversionEventDeviceInfo?

    # Model of the user device.
    @[JSON::Field(key: "device_model", type: String?, nillable: true, emit_null: false)]
    property device_model : String?

    # Type of the user device.
    @[JSON::Field(key: "device_type", type: String?, nillable: true, emit_null: false)]
    property device_type : String?

    # URL of the web conversion event.
    @[JSON::Field(key: "event_source_url", type: String?, nillable: true, emit_null: false)]
    property event_source_url : String?

    # Two-character ISO-639-1 language code indicating the user's language.
    @[JSON::Field(key: "language", type: String?, nillable: true, emit_null: false)]
    property language : String?

    # When action_source is web or offline, it defines whether the user has opted out of tracking for web conversion events. While when action_source is app_android or app_ios, it defines whether the user has enabled Limit Ad Tracking on their iOS device, or opted out of Ads Personalization on their Android device.
    @[JSON::Field(key: "opt_out", type: Bool?, nillable: true, emit_null: false)]
    property opt_out : Bool?

    # Version of the device operating system.
    @[JSON::Field(key: "os_version", type: String?, nillable: true, emit_null: false)]
    property os_version : String?

    # The third party partner name responsible to send the event to Conversions API on behalf of the advertiser. The naming convention is \"ss-partnername\" lowercase. E.g ‘ss-shopify’
    @[JSON::Field(key: "partner_name", type: String?, nillable: true, emit_null: false)]
    property partner_name : String?

    # Whether the event occurred when the user device was connected to wifi.
    @[JSON::Field(key: "wifi", type: Bool?, nillable: true, emit_null: false)]
    property wifi : Bool?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@action_source : String, @event_id : String, @event_name : String, @event_time : Int64, @user_data : ConversionEventsUserData, @app_id : String? = nil, @app_info : ConversionEventAppInfo? = nil, @app_name : String? = nil, @app_version : String? = nil, @custom_data : ConversionEventsDataInnerCustomData? = nil, @device_brand : String? = nil, @device_carrier : String? = nil, @device_info : ConversionEventDeviceInfo? = nil, @device_model : String? = nil, @device_type : String? = nil, @event_source_url : String? = nil, @language : String? = nil, @opt_out : Bool? = nil, @os_version : String? = nil, @partner_name : String? = nil, @wifi : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          action_source == other.action_source &&
          app_id == other.app_id &&
          app_info == other.app_info &&
          app_name == other.app_name &&
          app_version == other.app_version &&
          custom_data == other.custom_data &&
          device_brand == other.device_brand &&
          device_carrier == other.device_carrier &&
          device_info == other.device_info &&
          device_model == other.device_model &&
          device_type == other.device_type &&
          event_id == other.event_id &&
          event_name == other.event_name &&
          event_source_url == other.event_source_url &&
          event_time == other.event_time &&
          language == other.language &&
          opt_out == other.opt_out &&
          os_version == other.os_version &&
          partner_name == other.partner_name &&
          user_data == other.user_data &&
          wifi == other.wifi
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [action_source, app_id, app_info, app_name, app_version, custom_data, device_brand, device_carrier, device_info, device_model, device_type, event_id, event_name, event_source_url, event_time, language, opt_out, os_version, partner_name, user_data, wifi].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["action_source"] = _to_h(action_source)
      hash["app_id"] = _to_h(app_id)
      hash["app_info"] = _to_h(app_info)
      hash["app_name"] = _to_h(app_name)
      hash["app_version"] = _to_h(app_version)
      hash["custom_data"] = _to_h(custom_data)
      hash["device_brand"] = _to_h(device_brand)
      hash["device_carrier"] = _to_h(device_carrier)
      hash["device_info"] = _to_h(device_info)
      hash["device_model"] = _to_h(device_model)
      hash["device_type"] = _to_h(device_type)
      hash["event_id"] = _to_h(event_id)
      hash["event_name"] = _to_h(event_name)
      hash["event_source_url"] = _to_h(event_source_url)
      hash["event_time"] = _to_h(event_time)
      hash["language"] = _to_h(language)
      hash["opt_out"] = _to_h(opt_out)
      hash["os_version"] = _to_h(os_version)
      hash["partner_name"] = _to_h(partner_name)
      hash["user_data"] = _to_h(user_data)
      hash["wifi"] = _to_h(wifi)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
