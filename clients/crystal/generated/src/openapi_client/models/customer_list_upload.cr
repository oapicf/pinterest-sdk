# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class CustomerListUpload
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Advertiser ID.
    @[JSON::Field(key: "ad_account_id", type: String, nillable: false, emit_null: false)]
    property ad_account_id : String

    # Customer List Upload creation_time. Epoch (seconds).
    @[JSON::Field(key: "creation_time", type: Int32, nillable: false, emit_null: false)]
    property creation_time : Int32

    # ID of the customer list associated with this upload.
    @[JSON::Field(key: "customer_list_id", type: String, nillable: false, emit_null: false)]
    property customer_list_id : String

    # Customer List Upload ID.
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    @[JSON::Field(key: "operation", type: UserListOperationType, nillable: false, emit_null: false)]
    property operation : UserListOperationType

    # Workload processing state
    @[JSON::Field(key: "state", type: String, nillable: false, emit_null: false)]
    property state : String

    # Customer List Upload updated_time. Epoch (seconds).
    @[JSON::Field(key: "updated_time", type: Int32, nillable: false, emit_null: false)]
    property updated_time : Int32

    # Optional properties
    # Error counts by error code
    @[JSON::Field(key: "error_counts", type: Array(ErrorDetail)?, nillable: true, emit_null: false)]
    property error_counts : Array(ErrorDetail)?

    @[JSON::Field(key: "record_counts", type: RecordCounts?, nillable: true, emit_null: false)]
    property record_counts : RecordCounts?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForState < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "state"
        @allowable_values = ["NOT_STARTED", "RUNNING", "PAUSED", "SUCCEEDED", "FAILED"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ad_account_id : String, @creation_time : Int32, @customer_list_id : String, @id : String, @operation : UserListOperationType, @state : String, @updated_time : Int32, @error_counts : Array(ErrorDetail)? = nil, @record_counts : RecordCounts? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if @ad_account_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if @customer_list_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"customer_list_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      state_validator = EnumAttributeValidatorForState.new
      if !state_validator.valid?(@state)
        message = state_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @ad_account_id.try &.!~ /^\d+$/
      return false if @customer_list_id.try &.!~ /^\d+$/
      return false if @id.try &.!~ /^\d+$/
      state_validator = EnumAttributeValidatorForState.new
      return false unless state_validator.valid?(@state)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ad_account_id Value to be assigned
    def ad_account_id=(ad_account_id)
      pattern = /^\d+$/
      if ad_account_id !~ pattern
        raise ArgumentError.new("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      @ad_account_id = ad_account_id
    end

    # Custom attribute writer method with validation
    # @param [Object] customer_list_id Value to be assigned
    def customer_list_id=(customer_list_id)
      pattern = /^\d+$/
      if customer_list_id !~ pattern
        raise ArgumentError.new("invalid value for \"customer_list_id\", must conform to the pattern #{pattern}.")
      end

      @customer_list_id = customer_list_id
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] state Object to be assigned
    def state=(state)
      validator = EnumAttributeValidatorForState.new
      unless validator.valid?(state)
        raise ArgumentError.new(validator.message)
      end
      @state = state
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          ad_account_id == other.ad_account_id &&
          creation_time == other.creation_time &&
          customer_list_id == other.customer_list_id &&
          error_counts == other.error_counts &&
          id == other.id &&
          operation == other.operation &&
          record_counts == other.record_counts &&
          state == other.state &&
          updated_time == other.updated_time
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ad_account_id, creation_time, customer_list_id, error_counts, id, operation, record_counts, state, updated_time].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ad_account_id"] = _to_h(ad_account_id)
      hash["creation_time"] = _to_h(creation_time)
      hash["customer_list_id"] = _to_h(customer_list_id)
      hash["error_counts"] = _to_h(error_counts)
      hash["id"] = _to_h(id)
      hash["operation"] = _to_h(operation)
      hash["record_counts"] = _to_h(record_counts)
      hash["state"] = _to_h(state)
      hash["updated_time"] = _to_h(updated_time)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
