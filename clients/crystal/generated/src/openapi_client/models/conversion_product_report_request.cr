# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Request for a brand, category, SKU report
  class ConversionProductReportRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Metric and entity columns
    @[JSON::Field(key: "columns", type: Array(ConversionProductReportingColumn), nillable: false, emit_null: false)]
    property columns : Array(ConversionProductReportingColumn)

    # Metric report end date (UTC). Format: YYYY-MM-DD.<br> A max of 1 year is allowed between the start and end date for reports.
    @[JSON::Field(key: "end_date", type: String, nillable: false, emit_null: false)]
    property end_date : String

    # TOTAL - metrics are aggregated over the specified date range.<br> WEEK - metrics are broken down weekly.<br> MONTH - metrics are broken down monthly.
    @[JSON::Field(key: "granularity", type: String, nillable: false, emit_null: false)]
    property granularity : String

    # Level of the report
    @[JSON::Field(key: "level", type: String, nillable: false, emit_null: false)]
    property level : String

    # Name of the conversion product report.
    @[JSON::Field(key: "report_name", type: String, nillable: false, emit_null: false)]
    property report_name : String

    # Metric report start date (UTC). Format: YYYY-MM-DD.<br> Start date must be after 2024-03-16. 7 day minimum time window for report is required.
    @[JSON::Field(key: "start_date", type: String, nillable: false, emit_null: false)]
    property start_date : String

    # Optional properties
    # List of ad group ids. <br>Only support ad_group_ids field when level of the report is AD_GROUP.
    @[JSON::Field(key: "ad_group_ids", type: Array(String)?, nillable: true, emit_null: false)]
    property ad_group_ids : Array(String)?

    # List of campaign ids. <br>Only support campaign_ids field when level of the report is CAMPAIGN.
    @[JSON::Field(key: "campaign_ids", type: Array(String)?, nillable: true, emit_null: false)]
    property campaign_ids : Array(String)?

    # List of values for filtering. Default is ['CONSIDERATION','AWARENESS','WEB_CONVERSION','VIDEO_COMPLETION'].
    @[JSON::Field(key: "campaign_objective_types", type: Array(ObjectiveType)?, nillable: true, emit_null: false)]
    property campaign_objective_types : Array(ObjectiveType)?

    # Number of days to use as the conversion attribution window for a pin click action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` days.
    @[JSON::Field(key: "click_window_days", type: ConversionAttributionWindowDays?, default: 30, nillable: true, emit_null: false)]
    property click_window_days : ConversionAttributionWindowDays?

    @[JSON::Field(key: "conversion_product_attribution_type", type: String?, nillable: true, emit_null: false)]
    property conversion_product_attribution_type : String?

    @[JSON::Field(key: "conversion_product_breakdown", type: String?, nillable: true, emit_null: false)]
    property conversion_product_breakdown : String?

    # The date by which the conversion metrics returned from this endpoint will be reported. There are two dates associated with a conversion event: the date that the user interacted with the ad, and the date that the user completed a conversion event.
    @[JSON::Field(key: "conversion_report_time", type: ConversionReportTimeType?, default: "TIME_OF_AD_ACTION", nillable: true, emit_null: false)]
    property conversion_report_time : ConversionReportTimeType?

    # List of SKU ids. <br>Only support product_sku_ids field when report breakdown type is PRODUCT_SKU_GROUP.
    @[JSON::Field(key: "product_sku_ids", type: Array(String)?, nillable: true, emit_null: false)]
    property product_sku_ids : Array(String)?

    # Number of days to use as the conversion attribution window for a view action. Applies to Pinterest Tag conversion metrics. Prior conversion tags use their defined attribution windows. If not specified, defaults to `30` day.
    @[JSON::Field(key: "view_window_days", type: ConversionAttributionWindowDays?, default: 30, nillable: true, emit_null: false)]
    property view_window_days : ConversionAttributionWindowDays?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForConversion_product_attribution_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "conversion_product_attribution_type"
        @allowable_values = ["DEFAULT", "BRAND_ATTRIBUTION"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForConversion_product_breakdown < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "conversion_product_breakdown"
        @allowable_values = ["PRODUCT_BRAND", "PRODUCT_CATEGORY", "PRODUCT_BRAND_AND_CATEGORY", "PRODUCT_SKU", "PRODUCT_SKU_GROUP"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForGranularity < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "granularity"
        @allowable_values = ["WEEK", "MONTH", "TOTAL"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForLevel < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "level"
        @allowable_values = ["ADVERTISER", "CAMPAIGN", "AD_GROUP"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@columns : Array(ConversionProductReportingColumn), @end_date : String, @granularity : String, @level : String, @report_name : String, @start_date : String, @ad_group_ids : Array(String)? = nil, @campaign_ids : Array(String)? = nil, @campaign_objective_types : Array(ObjectiveType)? = nil, @click_window_days : ConversionAttributionWindowDays? = nil, @conversion_product_attribution_type : String? = nil, @conversion_product_breakdown : String? = nil, @conversion_report_time : ConversionReportTimeType? = nil, @product_sku_ids : Array(String)? = nil, @view_window_days : ConversionAttributionWindowDays? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@ad_group_ids.nil? && @ad_group_ids.try &.size.try &.> 500
        invalid_properties.push("invalid value for \"ad_group_ids\", number of items must be less than or equal to 500."
      end

      if !@ad_group_ids.nil? && @ad_group_ids.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"ad_group_ids\", number of items must be greater than or equal to 1."
      end

      if !@campaign_ids.nil? && @campaign_ids.try &.size.try &.> 500
        invalid_properties.push("invalid value for \"campaign_ids\", number of items must be less than or equal to 500."
      end

      if !@campaign_ids.nil? && @campaign_ids.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"campaign_ids\", number of items must be greater than or equal to 1."
      end

      if !@campaign_objective_types.nil? && @campaign_objective_types.try &.size.try &.> 7
        invalid_properties.push("invalid value for \"campaign_objective_types\", number of items must be less than or equal to 7."
      end

      if !@campaign_objective_types.nil? && @campaign_objective_types.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"campaign_objective_types\", number of items must be greater than or equal to 1."
      end

      conversion_product_attribution_type_validator = EnumAttributeValidatorForConversion_product_attribution_type.new
      if !conversion_product_attribution_type_validator.valid?(@conversion_product_attribution_type)
        message = conversion_product_attribution_type_validator.message
        invalid_properties.push(message)
      end

      conversion_product_breakdown_validator = EnumAttributeValidatorForConversion_product_breakdown.new
      if !conversion_product_breakdown_validator.valid?(@conversion_product_breakdown)
        message = conversion_product_breakdown_validator.message
        invalid_properties.push(message)
      end

      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if @end_date.try &.!~ pattern
        invalid_properties.push("invalid value for \"end_date\", must conform to the pattern #{pattern}.")
      end

      granularity_validator = EnumAttributeValidatorForGranularity.new
      if !granularity_validator.valid?(@granularity)
        message = granularity_validator.message
        invalid_properties.push(message)
      end

      level_validator = EnumAttributeValidatorForLevel.new
      if !level_validator.valid?(@level)
        message = level_validator.message
        invalid_properties.push(message)
      end

      if !@product_sku_ids.nil? && @product_sku_ids.try &.size.try &.> 500
        invalid_properties.push("invalid value for \"product_sku_ids\", number of items must be less than or equal to 500."
      end

      if !@product_sku_ids.nil? && @product_sku_ids.try &.size.try &.< 2
        invalid_properties.push("invalid value for \"product_sku_ids\", number of items must be greater than or equal to 2."
      end

      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if @start_date.try &.!~ pattern
        invalid_properties.push("invalid value for \"start_date\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@ad_group_ids.nil? && @ad_group_ids.try &.size.try &.> 500
      return false if !@ad_group_ids.nil? && @ad_group_ids.try &.size.try &.< 1
      return false if !@campaign_ids.nil? && @campaign_ids.try &.size.try &.> 500
      return false if !@campaign_ids.nil? && @campaign_ids.try &.size.try &.< 1
      return false if !@campaign_objective_types.nil? && @campaign_objective_types.try &.size.try &.> 7
      return false if !@campaign_objective_types.nil? && @campaign_objective_types.try &.size.try &.< 1
      conversion_product_attribution_type_validator = EnumAttributeValidatorForConversion_product_attribution_type.new
      return false unless conversion_product_attribution_type_validator.valid?(@conversion_product_attribution_type)
      conversion_product_breakdown_validator = EnumAttributeValidatorForConversion_product_breakdown.new
      return false unless conversion_product_breakdown_validator.valid?(@conversion_product_breakdown)
      return false if @end_date.try &.!~ /^(\d{4})-(\d{2})-(\d{2})$/
      granularity_validator = EnumAttributeValidatorForGranularity.new
      return false unless granularity_validator.valid?(@granularity)
      level_validator = EnumAttributeValidatorForLevel.new
      return false unless level_validator.valid?(@level)
      return false if !@product_sku_ids.nil? && @product_sku_ids.try &.size.try &.> 500
      return false if !@product_sku_ids.nil? && @product_sku_ids.try &.size.try &.< 2
      return false if @start_date.try &.!~ /^(\d{4})-(\d{2})-(\d{2})$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ad_group_ids Value to be assigned
    def ad_group_ids=(ad_group_ids)
      if !ad_group_ids.nil? && ad_group_ids.size > 500
        raise ArgumentError.new("invalid value for \"ad_group_ids\", number of items must be less than or equal to 500.")
      end

      if !ad_group_ids.nil? && ad_group_ids.size < 1
        raise ArgumentError.new("invalid value for \"ad_group_ids\", number of items must be greater than or equal to 1.")
      end

      @ad_group_ids = ad_group_ids
    end

    # Custom attribute writer method with validation
    # @param [Object] campaign_ids Value to be assigned
    def campaign_ids=(campaign_ids)
      if !campaign_ids.nil? && campaign_ids.size > 500
        raise ArgumentError.new("invalid value for \"campaign_ids\", number of items must be less than or equal to 500.")
      end

      if !campaign_ids.nil? && campaign_ids.size < 1
        raise ArgumentError.new("invalid value for \"campaign_ids\", number of items must be greater than or equal to 1.")
      end

      @campaign_ids = campaign_ids
    end

    # Custom attribute writer method with validation
    # @param [Object] campaign_objective_types Value to be assigned
    def campaign_objective_types=(campaign_objective_types)
      if !campaign_objective_types.nil? && campaign_objective_types.size > 7
        raise ArgumentError.new("invalid value for \"campaign_objective_types\", number of items must be less than or equal to 7.")
      end

      if !campaign_objective_types.nil? && campaign_objective_types.size < 1
        raise ArgumentError.new("invalid value for \"campaign_objective_types\", number of items must be greater than or equal to 1.")
      end

      @campaign_objective_types = campaign_objective_types
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] conversion_product_attribution_type Object to be assigned
    def conversion_product_attribution_type=(conversion_product_attribution_type)
      validator = EnumAttributeValidatorForConversion_product_attribution_type.new
      unless validator.valid?(conversion_product_attribution_type)
        raise ArgumentError.new(validator.message)
      end
      @conversion_product_attribution_type = conversion_product_attribution_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] conversion_product_breakdown Object to be assigned
    def conversion_product_breakdown=(conversion_product_breakdown)
      validator = EnumAttributeValidatorForConversion_product_breakdown.new
      unless validator.valid?(conversion_product_breakdown)
        raise ArgumentError.new(validator.message)
      end
      @conversion_product_breakdown = conversion_product_breakdown
    end

    # Custom attribute writer method with validation
    # @param [Object] end_date Value to be assigned
    def end_date=(end_date)
      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if end_date !~ pattern
        raise ArgumentError.new("invalid value for \"end_date\", must conform to the pattern #{pattern}.")
      end

      @end_date = end_date
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] granularity Object to be assigned
    def granularity=(granularity)
      validator = EnumAttributeValidatorForGranularity.new
      unless validator.valid?(granularity)
        raise ArgumentError.new(validator.message)
      end
      @granularity = granularity
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] level Object to be assigned
    def level=(level)
      validator = EnumAttributeValidatorForLevel.new
      unless validator.valid?(level)
        raise ArgumentError.new(validator.message)
      end
      @level = level
    end

    # Custom attribute writer method with validation
    # @param [Object] product_sku_ids Value to be assigned
    def product_sku_ids=(product_sku_ids)
      if !product_sku_ids.nil? && product_sku_ids.size > 500
        raise ArgumentError.new("invalid value for \"product_sku_ids\", number of items must be less than or equal to 500.")
      end

      if !product_sku_ids.nil? && product_sku_ids.size < 2
        raise ArgumentError.new("invalid value for \"product_sku_ids\", number of items must be greater than or equal to 2.")
      end

      @product_sku_ids = product_sku_ids
    end

    # Custom attribute writer method with validation
    # @param [Object] start_date Value to be assigned
    def start_date=(start_date)
      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if start_date !~ pattern
        raise ArgumentError.new("invalid value for \"start_date\", must conform to the pattern #{pattern}.")
      end

      @start_date = start_date
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          ad_group_ids == other.ad_group_ids &&
          campaign_ids == other.campaign_ids &&
          campaign_objective_types == other.campaign_objective_types &&
          click_window_days == other.click_window_days &&
          columns == other.columns &&
          conversion_product_attribution_type == other.conversion_product_attribution_type &&
          conversion_product_breakdown == other.conversion_product_breakdown &&
          conversion_report_time == other.conversion_report_time &&
          end_date == other.end_date &&
          granularity == other.granularity &&
          level == other.level &&
          product_sku_ids == other.product_sku_ids &&
          report_name == other.report_name &&
          start_date == other.start_date &&
          view_window_days == other.view_window_days
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ad_group_ids, campaign_ids, campaign_objective_types, click_window_days, columns, conversion_product_attribution_type, conversion_product_breakdown, conversion_report_time, end_date, granularity, level, product_sku_ids, report_name, start_date, view_window_days].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ad_group_ids"] = _to_h(ad_group_ids)
      hash["campaign_ids"] = _to_h(campaign_ids)
      hash["campaign_objective_types"] = _to_h(campaign_objective_types)
      hash["click_window_days"] = _to_h(click_window_days)
      hash["columns"] = _to_h(columns)
      hash["conversion_product_attribution_type"] = _to_h(conversion_product_attribution_type)
      hash["conversion_product_breakdown"] = _to_h(conversion_product_breakdown)
      hash["conversion_report_time"] = _to_h(conversion_report_time)
      hash["end_date"] = _to_h(end_date)
      hash["granularity"] = _to_h(granularity)
      hash["level"] = _to_h(level)
      hash["product_sku_ids"] = _to_h(product_sku_ids)
      hash["report_name"] = _to_h(report_name)
      hash["start_date"] = _to_h(start_date)
      hash["view_window_days"] = _to_h(view_window_days)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
