# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class PromotionCommon
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Discount status based on the current time and start and end time of discount
    @[JSON::Field(key: "discount_status", type: String?, nillable: true, emit_null: false)]
    property discount_status : String?

    # Promotion end time. Unix timestamp in seconds. Independent of campaign end time.
    @[JSON::Field(key: "end_time", type: Int32?, nillable: true, emit_null: false)]
    property end_time : Int32?

    # Platform-specific ID for this promotion. Will be null for promotions first created within Pinterest.
    @[JSON::Field(key: "external_id", type: String?, nillable: true, emit_null: false)]
    property external_id : String?

    # The source integration platform used when creating the promotion. Currently supported values are 'DEFAULT' and 'SHOPIFY'.
    @[JSON::Field(key: "platform_type", type: String?, nillable: true, emit_null: false)]
    property platform_type : String?

    # Code that can be used to redeem a promotion.
    @[JSON::Field(key: "promotion_code", type: String?, nillable: true, emit_null: false)]
    property promotion_code : String?

    # An optional field for user defined promotion ID for this promotion. Will copy from Pinterest system generated ID if user did not provide one.
    @[JSON::Field(key: "promotion_custom_id", type: String?, nillable: true, emit_null: false)]
    property promotion_custom_id : String?

    # Internal name for the promotion.
    @[JSON::Field(key: "promotion_title", type: String?, nillable: true, emit_null: false)]
    property promotion_title : String?

    @[JSON::Field(key: "promotion_type", type: PromotionType?, nillable: true, emit_null: false)]
    property promotion_type : PromotionType?

    # Promotion start time. Unix timestamp in seconds. Independent of campaign start time.
    @[JSON::Field(key: "start_time", type: Int32?, nillable: true, emit_null: false)]
    property start_time : Int32?

    # List of values to be inserted in the promotion type-specific template.
    @[JSON::Field(key: "template_values", type: Array(PromotionTemplateValue)?, nillable: true, emit_null: false)]
    property template_values : Array(PromotionTemplateValue)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForDiscount_status < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "discount_status"
        @allowable_values = ["OTHER", "ACTIVE", "PAUSED", "SCHEDULED", "EXPIRED"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@discount_status : String? = nil, @end_time : Int32? = nil, @external_id : String? = nil, @platform_type : String? = nil, @promotion_code : String? = nil, @promotion_custom_id : String? = nil, @promotion_title : String? = nil, @promotion_type : PromotionType? = nil, @start_time : Int32? = nil, @template_values : Array(PromotionTemplateValue)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      discount_status_validator = EnumAttributeValidatorForDiscount_status.new
      if !discount_status_validator.valid?(@discount_status)
        message = discount_status_validator.message
        invalid_properties.push(message)
      end

      if !@external_id.nil? && @external_id.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"external_id\", the character length must be smaller than or equal to 64.")
      end

      if !@promotion_custom_id.nil? && @promotion_custom_id.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"promotion_custom_id\", the character length must be smaller than or equal to 50.")
      end

      if !@template_values.nil? && @template_values.try &.size.try &.> 2
        invalid_properties.push("invalid value for \"template_values\", number of items must be less than or equal to 2."
      end

      if !@template_values.nil? && @template_values.try &.size.try &.< 0
        invalid_properties.push("invalid value for \"template_values\", number of items must be greater than or equal to 0."
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      discount_status_validator = EnumAttributeValidatorForDiscount_status.new
      return false unless discount_status_validator.valid?(@discount_status)
      return false if !@external_id.nil? && @external_id.try &.to_s.try &.size.try &.> 64
      return false if !@promotion_custom_id.nil? && @promotion_custom_id.try &.to_s.try &.size.try &.> 50
      return false if !@template_values.nil? && @template_values.try &.size.try &.> 2
      return false if !@template_values.nil? && @template_values.try &.size.try &.< 0
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] discount_status Object to be assigned
    def discount_status=(discount_status)
      validator = EnumAttributeValidatorForDiscount_status.new
      unless validator.valid?(discount_status)
        raise ArgumentError.new(validator.message)
      end
      @discount_status = discount_status
    end

    # Custom attribute writer method with validation
    # @param [Object] external_id Value to be assigned
    def external_id=(external_id)
      if !external_id.nil? && external_id.to_s.size > 64
        raise ArgumentError.new("invalid value for \"external_id\", the character length must be smaller than or equal to 64.")
      end

      @external_id = external_id
    end

    # Custom attribute writer method with validation
    # @param [Object] promotion_custom_id Value to be assigned
    def promotion_custom_id=(promotion_custom_id)
      if !promotion_custom_id.nil? && promotion_custom_id.to_s.size > 50
        raise ArgumentError.new("invalid value for \"promotion_custom_id\", the character length must be smaller than or equal to 50.")
      end

      @promotion_custom_id = promotion_custom_id
    end

    # Custom attribute writer method with validation
    # @param [Object] template_values Value to be assigned
    def template_values=(template_values)
      if !template_values.nil? && template_values.size > 2
        raise ArgumentError.new("invalid value for \"template_values\", number of items must be less than or equal to 2.")
      end

      if !template_values.nil? && template_values.size < 0
        raise ArgumentError.new("invalid value for \"template_values\", number of items must be greater than or equal to 0.")
      end

      @template_values = template_values
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          discount_status == other.discount_status &&
          end_time == other.end_time &&
          external_id == other.external_id &&
          platform_type == other.platform_type &&
          promotion_code == other.promotion_code &&
          promotion_custom_id == other.promotion_custom_id &&
          promotion_title == other.promotion_title &&
          promotion_type == other.promotion_type &&
          start_time == other.start_time &&
          template_values == other.template_values
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [discount_status, end_time, external_id, platform_type, promotion_code, promotion_custom_id, promotion_title, promotion_type, start_time, template_values].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["discount_status"] = _to_h(discount_status)
      hash["end_time"] = _to_h(end_time)
      hash["external_id"] = _to_h(external_id)
      hash["platform_type"] = _to_h(platform_type)
      hash["promotion_code"] = _to_h(promotion_code)
      hash["promotion_custom_id"] = _to_h(promotion_custom_id)
      hash["promotion_title"] = _to_h(promotion_title)
      hash["promotion_type"] = _to_h(promotion_type)
      hash["start_time"] = _to_h(start_time)
      hash["template_values"] = _to_h(template_values)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
