# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class UpdateMemberAssetAccessBodyAccessesInner
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Id of the asset to update.
    @[JSON::Field(key: "asset_id", type: String, nillable: false, emit_null: false)]
    property asset_id : String

    # Unique identifier of the member on which to perform the update
    @[JSON::Field(key: "member_id", type: String, nillable: false, emit_null: false)]
    property member_id : String

    # A non-empty array of permissions to assign to the member.
    @[JSON::Field(key: "permissions", type: Array(Permissions), nillable: false, emit_null: false)]
    property permissions : Array(Permissions)

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@asset_id : String, @member_id : String, @permissions : Array(Permissions))
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @asset_id.try &.to_s.try &.size.try &.> 25
        invalid_properties.push("invalid value for \"asset_id\", the character length must be smaller than or equal to 25.")
      end

      pattern = /^\d+$/
      if @asset_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"asset_id\", must conform to the pattern #{pattern}.")
      end

      if @member_id.try &.to_s.try &.size.try &.> 25
        invalid_properties.push("invalid value for \"member_id\", the character length must be smaller than or equal to 25.")
      end

      pattern = /^\d+$/
      if @member_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"member_id\", must conform to the pattern #{pattern}.")
      end

      if @permissions.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"permissions\", number of items must be less than or equal to 50."
      end

      if @permissions.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"permissions\", number of items must be greater than or equal to 1."
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @asset_id.try &.to_s.try &.size.try &.> 25
      return false if @asset_id.try &.!~ /^\d+$/
      return false if @member_id.try &.to_s.try &.size.try &.> 25
      return false if @member_id.try &.!~ /^\d+$/
      return false if @permissions.try &.size.try &.> 50
      return false if @permissions.try &.size.try &.< 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] asset_id Value to be assigned
    def asset_id=(asset_id)
      if asset_id.to_s.size > 25
        raise ArgumentError.new("invalid value for \"asset_id\", the character length must be smaller than or equal to 25.")
      end

      pattern = /^\d+$/
      if asset_id !~ pattern
        raise ArgumentError.new("invalid value for \"asset_id\", must conform to the pattern #{pattern}.")
      end

      @asset_id = asset_id
    end

    # Custom attribute writer method with validation
    # @param [Object] member_id Value to be assigned
    def member_id=(member_id)
      if member_id.to_s.size > 25
        raise ArgumentError.new("invalid value for \"member_id\", the character length must be smaller than or equal to 25.")
      end

      pattern = /^\d+$/
      if member_id !~ pattern
        raise ArgumentError.new("invalid value for \"member_id\", must conform to the pattern #{pattern}.")
      end

      @member_id = member_id
    end

    # Custom attribute writer method with validation
    # @param [Object] permissions Value to be assigned
    def permissions=(permissions)
      if permissions.size > 50
        raise ArgumentError.new("invalid value for \"permissions\", number of items must be less than or equal to 50.")
      end

      if permissions.size < 1
        raise ArgumentError.new("invalid value for \"permissions\", number of items must be greater than or equal to 1.")
      end

      @permissions = permissions
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          asset_id == other.asset_id &&
          member_id == other.member_id &&
          permissions == other.permissions
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [asset_id, member_id, permissions].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["asset_id"] = _to_h(asset_id)
      hash["member_id"] = _to_h(member_id)
      hash["permissions"] = _to_h(permissions)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
