# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class LeadSubscription
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # The Ad Account ID that this lead form belongs to.
    @[JSON::Field(key: "ad_account_id", type: String?, nillable: true, emit_null: false)]
    property ad_account_id : String?

    # API version.
    @[JSON::Field(key: "api_version", type: String?, nillable: true, emit_null: false)]
    property api_version : String?

    # Subscription creation time. Unix timestamp in milliseconds.
    @[JSON::Field(key: "created_time", type: Int32?, nillable: true, emit_null: false)]
    property created_time : Int32?

    # Lead data encryption algorithm.
    @[JSON::Field(key: "cryptographic_algorithm", type: String?, nillable: true, emit_null: false)]
    property cryptographic_algorithm : String?

    # Base64 encoded key for client to decrypt lead data.
    @[JSON::Field(key: "cryptographic_key", type: String?, nillable: true, emit_null: false)]
    property cryptographic_key : String?

    # Subscription ID.
    @[JSON::Field(key: "id", type: String?, nillable: true, emit_null: false)]
    property id : String?

    # Lead form ID.
    @[JSON::Field(key: "lead_form_id", type: String?, nillable: true, emit_null: false)]
    property lead_form_id : String?

    # User account used to subscribe lead data.
    @[JSON::Field(key: "user_account_id", type: String?, nillable: true, emit_null: false)]
    property user_account_id : String?

    # Standard HTTPS webhook URL.
    @[JSON::Field(key: "webhook_url", type: String?, nillable: true, emit_null: false)]
    property webhook_url : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ad_account_id : String? = nil, @api_version : String? = nil, @created_time : Int32? = nil, @cryptographic_algorithm : String? = nil, @cryptographic_key : String? = nil, @id : String? = nil, @lead_form_id : String? = nil, @user_account_id : String? = nil, @webhook_url : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@ad_account_id.nil? && @ad_account_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if !@id.nil? && @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if !@lead_form_id.nil? && @lead_form_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"lead_form_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if !@user_account_id.nil? && @user_account_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"user_account_id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@ad_account_id.nil? && @ad_account_id.try &.!~ /^\d+$/
      return false if !@id.nil? && @id.try &.!~ /^\d+$/
      return false if !@lead_form_id.nil? && @lead_form_id.try &.!~ /^\d+$/
      return false if !@user_account_id.nil? && @user_account_id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ad_account_id Value to be assigned
    def ad_account_id=(ad_account_id)
      pattern = /^\d+$/
      if !ad_account_id.nil? && ad_account_id !~ pattern
        raise ArgumentError.new("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      @ad_account_id = ad_account_id
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if !id.nil? && id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method with validation
    # @param [Object] lead_form_id Value to be assigned
    def lead_form_id=(lead_form_id)
      pattern = /^\d+$/
      if !lead_form_id.nil? && lead_form_id !~ pattern
        raise ArgumentError.new("invalid value for \"lead_form_id\", must conform to the pattern #{pattern}.")
      end

      @lead_form_id = lead_form_id
    end

    # Custom attribute writer method with validation
    # @param [Object] user_account_id Value to be assigned
    def user_account_id=(user_account_id)
      pattern = /^\d+$/
      if !user_account_id.nil? && user_account_id !~ pattern
        raise ArgumentError.new("invalid value for \"user_account_id\", must conform to the pattern #{pattern}.")
      end

      @user_account_id = user_account_id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          ad_account_id == other.ad_account_id &&
          api_version == other.api_version &&
          created_time == other.created_time &&
          cryptographic_algorithm == other.cryptographic_algorithm &&
          cryptographic_key == other.cryptographic_key &&
          id == other.id &&
          lead_form_id == other.lead_form_id &&
          user_account_id == other.user_account_id &&
          webhook_url == other.webhook_url
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ad_account_id, api_version, created_time, cryptographic_algorithm, cryptographic_key, id, lead_form_id, user_account_id, webhook_url].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ad_account_id"] = _to_h(ad_account_id)
      hash["api_version"] = _to_h(api_version)
      hash["created_time"] = _to_h(created_time)
      hash["cryptographic_algorithm"] = _to_h(cryptographic_algorithm)
      hash["cryptographic_key"] = _to_h(cryptographic_key)
      hash["id"] = _to_h(id)
      hash["lead_form_id"] = _to_h(lead_form_id)
      hash["user_account_id"] = _to_h(user_account_id)
      hash["webhook_url"] = _to_h(webhook_url)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
