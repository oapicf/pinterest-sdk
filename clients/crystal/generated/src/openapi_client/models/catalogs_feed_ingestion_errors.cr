# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class CatalogsFeedIngestionErrors
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # We detected an issue with your account and are not currently ingesting your items. Please review our policies at policy.pinterest.com/community-guidelines#section-spam or contact us at help.pinterest.com/contact for more information.
    @[JSON::Field(key: "ACCOUNT_FLAGGED", type: Int32?, nillable: true, emit_null: false)]
    property account_flagged : Int32?

    # Update your Google Sheets sharing settings to 'Anyone with link' as a Viewer so that Pinterest can access your file.
    @[JSON::Field(key: "FETCH_GOOGLE_SHEET_NOT_SHARED", type: Int32?, nillable: true, emit_null: false)]
    property fetch_google_sheet_not_shared : Int32?

    # Image files are unreadable. Please upload new files to continue.
    @[JSON::Field(key: "IMAGE_FILE_NOT_ACCESSIBLE", type: Int32?, nillable: true, emit_null: false)]
    property image_file_not_accessible : Int32?

    # Image files are unreadable. Please upload new files to continue.
    @[JSON::Field(key: "IMAGE_FILE_NOT_FOUND", type: Int32?, nillable: true, emit_null: false)]
    property image_file_not_found : Int32?

    # Image files are unreadable. Please upload new files to continue.
    @[JSON::Field(key: "IMAGE_INVALID_FILE", type: Int32?, nillable: true, emit_null: false)]
    property image_invalid_file : Int32?

    # We experienced a technical difficulty and were unable to download some images. The next download attempt will happen in 24 hours.
    @[JSON::Field(key: "IMAGE_LEVEL_INTERNAL_ERROR", type: Int32?, nillable: true, emit_null: false)]
    property image_level_internal_error : Int32?

    # Image files are unreadable. Please check your link and upload new files to continue.
    @[JSON::Field(key: "IMAGE_MALFORMED_URL", type: Int32?, nillable: true, emit_null: false)]
    property image_malformed_url : Int32?

    # The product count has decreased by more than 99% compared to the last successful ingestion.
    @[JSON::Field(key: "LARGE_PRODUCT_COUNT_DECREASE", type: Int32?, nillable: true, emit_null: false)]
    property large_product_count_decrease : Int32?

    # We experienced a technical difficulty and were unable to ingest this some items. The next ingestion will happen in 24 hours.
    @[JSON::Field(key: "LINE_LEVEL_INTERNAL_ERROR", type: Int32?, nillable: true, emit_null: false)]
    property line_level_internal_error : Int32?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForLarge_product_count_decrease < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "large_product_count_decrease"
        @allowable_values = ["1"].map { |value| to(Int32, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@account_flagged : Int32? = nil, @fetch_google_sheet_not_shared : Int32? = nil, @image_file_not_accessible : Int32? = nil, @image_file_not_found : Int32? = nil, @image_invalid_file : Int32? = nil, @image_level_internal_error : Int32? = nil, @image_malformed_url : Int32? = nil, @large_product_count_decrease : Int32? = nil, @line_level_internal_error : Int32? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      large_product_count_decrease_validator = EnumAttributeValidatorForLarge_product_count_decrease.new
      if !large_product_count_decrease_validator.valid?(@large_product_count_decrease)
        message = large_product_count_decrease_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      large_product_count_decrease_validator = EnumAttributeValidatorForLarge_product_count_decrease.new
      return false unless large_product_count_decrease_validator.valid?(@large_product_count_decrease)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] large_product_count_decrease Object to be assigned
    def large_product_count_decrease=(large_product_count_decrease)
      validator = EnumAttributeValidatorForLarge_product_count_decrease.new
      unless validator.valid?(large_product_count_decrease)
        raise ArgumentError.new(validator.message)
      end
      @large_product_count_decrease = large_product_count_decrease
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          account_flagged == other.account_flagged &&
          fetch_google_sheet_not_shared == other.fetch_google_sheet_not_shared &&
          image_file_not_accessible == other.image_file_not_accessible &&
          image_file_not_found == other.image_file_not_found &&
          image_invalid_file == other.image_invalid_file &&
          image_level_internal_error == other.image_level_internal_error &&
          image_malformed_url == other.image_malformed_url &&
          large_product_count_decrease == other.large_product_count_decrease &&
          line_level_internal_error == other.line_level_internal_error
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [account_flagged, fetch_google_sheet_not_shared, image_file_not_accessible, image_file_not_found, image_invalid_file, image_level_internal_error, image_malformed_url, large_product_count_decrease, line_level_internal_error].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ACCOUNT_FLAGGED"] = _to_h(account_flagged)
      hash["FETCH_GOOGLE_SHEET_NOT_SHARED"] = _to_h(fetch_google_sheet_not_shared)
      hash["IMAGE_FILE_NOT_ACCESSIBLE"] = _to_h(image_file_not_accessible)
      hash["IMAGE_FILE_NOT_FOUND"] = _to_h(image_file_not_found)
      hash["IMAGE_INVALID_FILE"] = _to_h(image_invalid_file)
      hash["IMAGE_LEVEL_INTERNAL_ERROR"] = _to_h(image_level_internal_error)
      hash["IMAGE_MALFORMED_URL"] = _to_h(image_malformed_url)
      hash["LARGE_PRODUCT_COUNT_DECREASE"] = _to_h(large_product_count_decrease)
      hash["LINE_LEVEL_INTERNAL_ERROR"] = _to_h(line_level_internal_error)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
