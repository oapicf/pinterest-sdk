# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.12.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.4.0
#

require "big"
require "json"
require "time"

module OpenAPIClient
  class SSIOCreateInsertionOrderRequest
    include JSON::Serializable

    # Required properties
    # Starting date of time period. Format: YYYY-MM-DD
    @[JSON::Field(key: "start_date", type: String, nillable: false, emit_null: false)]
    property start_date : String

    # The po number
    @[JSON::Field(key: "po_number", type: String, nillable: false, emit_null: false)]
    property po_number : String

    # The billing contact first name
    @[JSON::Field(key: "billing_contact_firstname", type: String, nillable: false, emit_null: false)]
    property billing_contact_firstname : String

    # The billing contact last name
    @[JSON::Field(key: "billing_contact_lastname", type: String, nillable: false, emit_null: false)]
    property billing_contact_lastname : String

    # The billing contact email
    @[JSON::Field(key: "billing_contact_email", type: String, nillable: false, emit_null: false)]
    property billing_contact_email : String

    # The media contact first name
    @[JSON::Field(key: "media_contact_firstname", type: String, nillable: false, emit_null: false)]
    property media_contact_firstname : String

    # The media contact last name
    @[JSON::Field(key: "media_contact_lastname", type: String, nillable: false, emit_null: false)]
    property media_contact_lastname : String

    # The media contact email
    @[JSON::Field(key: "media_contact_email", type: String, nillable: false, emit_null: false)]
    property media_contact_email : String

    # The pmp id
    @[JSON::Field(key: "pmp_id", type: String, nillable: false, emit_null: false)]
    property pmp_id : String

    # The order name
    @[JSON::Field(key: "order_name", type: String, nillable: false, emit_null: false)]
    property order_name : String

    # Type can be Budget or Perpetual
    @[JSON::Field(key: "order_line_type", type: String, nillable: false, emit_null: false)]
    property order_line_type : String

    # The SFDC id for the terms
    @[JSON::Field(key: "accepted_terms_id", type: String, nillable: false, emit_null: false)]
    property accepted_terms_id : String

    # The bill-to company id
    @[JSON::Field(key: "billto_company_id", type: String, nillable: false, emit_null: false)]
    property billto_company_id : String

    # The bill-to business address id
    @[JSON::Field(key: "billto_business_address_id", type: String, nillable: false, emit_null: false)]
    property billto_business_address_id : String

    # The bill-to billing address id
    @[JSON::Field(key: "billto_billing_address_id", type: String, nillable: false, emit_null: false)]
    property billto_billing_address_id : String

    @[JSON::Field(key: "currency_info", type: Currency, nillable: false, emit_null: false)]
    property currency_info : Currency

    # Optional properties
    # End date of time period. Format: YYYY-MM-DD
    @[JSON::Field(key: "end_date", type: String?, nillable: true, emit_null: false)]
    property end_date : String?

    # If Budget order line, the budget amount.
    @[JSON::Field(key: "budget_amount", type: Float64?, nillable: true, emit_null: false)]
    property budget_amount : Float64?

    # URL link for agency
    @[JSON::Field(key: "agency_link", type: String?, nillable: true, emit_null: false)]
    property agency_link : String?

    # The email of user submitting the insertion order
    @[JSON::Field(key: "user_email", type: String?, nillable: true, emit_null: false)]
    property user_email : String?

    # The UTC timestamp (to the nearest sec) of when terms were accepted
    @[JSON::Field(key: "accepted_terms_time", type: Int32?, nillable: true, emit_null: false)]
    property accepted_terms_time : Int32?

    # If Ongoing (perpetual) order line, the estimated monthly spend
    @[JSON::Field(key: "estimated_monthly_spend", type: Float64?, nillable: true, emit_null: false)]
    property estimated_monthly_spend : Float64?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"SSIOInsertionOrderCommon"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@start_date : String, @po_number : String, @billing_contact_firstname : String, @billing_contact_lastname : String, @billing_contact_email : String, @media_contact_firstname : String, @media_contact_lastname : String, @media_contact_email : String, @pmp_id : String, @order_name : String, @order_line_type : String, @accepted_terms_id : String, @billto_company_id : String, @billto_business_address_id : String, @billto_billing_address_id : String, @currency_info : Currency, @end_date : String?, @budget_amount : Float64?, @agency_link : String?, @user_email : String?, @accepted_terms_time : Int32?, @estimated_monthly_spend : Float64?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      if @start_date !~ pattern
        invalid_properties.push("invalid value for \"start_date\", must conform to the pattern #{pattern}.")
      end

      pattern = Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      if !@end_date.nil? && @end_date !~ pattern
        invalid_properties.push("invalid value for \"end_date\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @start_date !~ Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      return false if !@end_date.nil? && @end_date !~ Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      order_line_type_validator = EnumAttributeValidator.new("String", ["BUDGET", "PERPETUALS"])
      return false unless order_line_type_validator.valid?(@order_line_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] start_date Value to be assigned
    def start_date=(start_date)
      pattern = Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      if start_date !~ pattern
        raise ArgumentError.new("invalid value for \"start_date\", must conform to the pattern #{pattern}.")
      end

      @start_date = start_date
    end

    # Custom attribute writer method with validation
    # @param [Object] end_date Value to be assigned
    def end_date=(end_date)
      pattern = Regexp.new(/^(\d{4})-(\d{2})-(\d{2})$/)
      if !end_date.nil? && end_date !~ pattern
        raise ArgumentError.new("invalid value for \"end_date\", must conform to the pattern #{pattern}.")
      end

      @end_date = end_date
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] order_line_type Object to be assigned
    def order_line_type=(order_line_type)
      validator = EnumAttributeValidator.new("String", ["BUDGET", "PERPETUALS"])
      unless validator.valid?(order_line_type)
        raise ArgumentError.new("invalid value for \"order_line_type\", must be one of #{validator.allowable_values}.")
      end
      @order_line_type = order_line_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
          start_date == o.start_date &&
          end_date == o.end_date &&
          po_number == o.po_number &&
          budget_amount == o.budget_amount &&
          billing_contact_firstname == o.billing_contact_firstname &&
          billing_contact_lastname == o.billing_contact_lastname &&
          billing_contact_email == o.billing_contact_email &&
          media_contact_firstname == o.media_contact_firstname &&
          media_contact_lastname == o.media_contact_lastname &&
          media_contact_email == o.media_contact_email &&
          agency_link == o.agency_link &&
          user_email == o.user_email &&
          accepted_terms_time == o.accepted_terms_time &&
          pmp_id == o.pmp_id &&
          order_name == o.order_name &&
          order_line_type == o.order_line_type &&
          accepted_terms_id == o.accepted_terms_id &&
          billto_company_id == o.billto_company_id &&
          billto_business_address_id == o.billto_business_address_id &&
          billto_billing_address_id == o.billto_billing_address_id &&
          estimated_monthly_spend == o.estimated_monthly_spend &&
          currency_info == o.currency_info
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [start_date, end_date, po_number, budget_amount, billing_contact_firstname, billing_contact_lastname, billing_contact_email, media_contact_firstname, media_contact_lastname, media_contact_email, agency_link, user_email, accepted_terms_time, pmp_id, order_name, order_line_type, accepted_terms_id, billto_company_id, billto_business_address_id, billto_billing_address_id, estimated_monthly_spend, currency_info].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
