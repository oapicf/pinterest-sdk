# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class AdsCreditDiscountsResponse
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # True if the offer code is currently active.
    @[JSON::Field(key: "active", type: Bool?, nillable: true, emit_null: false)]
    property active : Bool?

    # Advertiser ID the offer was applied to.
    @[JSON::Field(key: "advertiser_id", type: String?, nillable: true, emit_null: false)]
    property advertiser_id : String?

    # The type of discount of this credit
    @[JSON::Field(key: "discountType", type: String?, nillable: true, emit_null: false)]
    property discount_type : String?

    # The discount applied in the offer’s currency value.
    @[JSON::Field(key: "discountInMicroCurrency", type: Float64?, nillable: true, emit_null: false)]
    property discount_in_micro_currency : Float64?

    # Currency value for the discount.
    @[JSON::Field(key: "discountCurrency", type: String?, nillable: true, emit_null: false)]
    property discount_currency : String?

    # Human readable title of the offer code.
    @[JSON::Field(key: "title", type: String?, nillable: true, emit_null: false)]
    property title : String?

    # The credits left to spend.
    @[JSON::Field(key: "remainingDiscountInMicroCurrency", type: Float64?, nillable: true, emit_null: false)]
    property remaining_discount_in_micro_currency : Float64?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForDiscount_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "discount_type"
        @allowable_values = ["COUPON", "CREDIT", "COUPON_APPLIED", "CREDIT_APPLIED", "MARKETING_OFFER_CREDIT", "MARKETING_OFFER_CREDIT_APPLIED", "GOODWILL_CREDIT", "GOODWILL_CREDIT_APPLIED", "INTERNAL_CREDIT", "INTERNAL_CREDIT_APPLIED", "PREPAID_CREDIT", "PREPAID_CREDIT_APPLIED", "SALES_INCENTIVE_CREDIT", "SALES_INCENTIVE_CREDIT_APPLIED", "CREDIT_EXPIRED", "FUTURE_CREDIT", "REFERRAL_CREDIT", "INVOICE_SALES_INCENTIVE_CREDIT", "INVOICE_SALES_INCENTIVE_CREDIT_APPLIED", "PREPAID_CREDIT_REFUND"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@active : Bool? = nil, @advertiser_id : String? = nil, @discount_type : String? = nil, @discount_in_micro_currency : Float64? = nil, @discount_currency : String? = nil, @title : String? = nil, @remaining_discount_in_micro_currency : Float64? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@advertiser_id.nil? && @advertiser_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"advertiser_id\", must conform to the pattern #{pattern}.")
      end

      discount_type_validator = EnumAttributeValidatorForDiscount_type.new
      if !discount_type_validator.valid?(@discount_type)
        message = discount_type_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@advertiser_id.nil? && @advertiser_id.try &.!~ /^\d+$/
      discount_type_validator = EnumAttributeValidatorForDiscount_type.new
      return false unless discount_type_validator.valid?(@discount_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] advertiser_id Value to be assigned
    def advertiser_id=(advertiser_id)
      pattern = /^\d+$/
      if !advertiser_id.nil? && advertiser_id !~ pattern
        raise ArgumentError.new("invalid value for \"advertiser_id\", must conform to the pattern #{pattern}.")
      end

      @advertiser_id = advertiser_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] discount_type Object to be assigned
    def discount_type=(discount_type)
      validator = EnumAttributeValidatorForDiscount_type.new
      unless validator.valid?(discount_type)
        raise ArgumentError.new(validator.message)
      end
      @discount_type = discount_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          active == other.active &&
          advertiser_id == other.advertiser_id &&
          discount_type == other.discount_type &&
          discount_in_micro_currency == other.discount_in_micro_currency &&
          discount_currency == other.discount_currency &&
          title == other.title &&
          remaining_discount_in_micro_currency == other.remaining_discount_in_micro_currency
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [active, advertiser_id, discount_type, discount_in_micro_currency, discount_currency, title, remaining_discount_in_micro_currency].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["active"] = _to_h(active)
      hash["advertiser_id"] = _to_h(advertiser_id)
      hash["discountType"] = _to_h(discount_type)
      hash["discountInMicroCurrency"] = _to_h(discount_in_micro_currency)
      hash["discountCurrency"] = _to_h(discount_currency)
      hash["title"] = _to_h(title)
      hash["remainingDiscountInMicroCurrency"] = _to_h(remaining_discount_in_micro_currency)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
