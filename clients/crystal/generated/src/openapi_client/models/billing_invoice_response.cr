# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class BillingInvoiceResponse
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # The ID of the ad account this invoice belongs to
    @[JSON::Field(key: "ad_account_id", type: String?, nillable: true, emit_null: false)]
    property ad_account_id : String?

    # The name of the ad account this invoice belongs to
    @[JSON::Field(key: "ad_account_name", type: String?, nillable: true, emit_null: false)]
    property ad_account_name : String?

    # The amount billed in this invoice. Denoted in micro currency
    @[JSON::Field(key: "amount_billed_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property amount_billed_micro_currency : Int32?

    # The discount in this invoice. Denoted in micro currency
    @[JSON::Field(key: "amount_discount_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property amount_discount_micro_currency : Int32?

    # The net amount in this invoice. Denoted in micro currency
    @[JSON::Field(key: "amount_net_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property amount_net_micro_currency : Int32?

    # The tax in this invoice. Denoted in micro currency
    @[JSON::Field(key: "amount_tax_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property amount_tax_micro_currency : Int32?

    # The country of the bill to address
    @[JSON::Field(key: "bill_to_country", type: String?, nillable: true, emit_null: false)]
    property bill_to_country : String?

    # The end date of the billing period. Format: YYYY-MM-DD
    @[JSON::Field(key: "billing_period_end_date", type: Time?, nillable: true, emit_null: false)]
    property billing_period_end_date : Time?

    # The start date of the billing period. Format: YYYY-MM-DD
    @[JSON::Field(key: "billing_period_start_date", type: Time?, nillable: true, emit_null: false)]
    property billing_period_start_date : Time?

    @[JSON::Field(key: "currency", type: Currency?, nillable: true, emit_null: false)]
    property currency : Currency?

    # The type of the document
    @[JSON::Field(key: "document_type", type: String?, nillable: true, emit_null: false)]
    property document_type : String?

    # Unique identifier for the billing invoice
    @[JSON::Field(key: "id", type: String?, nillable: true, emit_null: false)]
    property id : String?

    # The date the invoice is due. Format: YYYY-MM-DD
    @[JSON::Field(key: "invoice_due_date", type: Time?, nillable: true, emit_null: false)]
    property invoice_due_date : Time?

    # The payment terms of the invoice
    @[JSON::Field(key: "payment_terms", type: String?, nillable: true, emit_null: false)]
    property payment_terms : String?

    # The status of the invoice
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForDocument_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "document_type"
        @allowable_values = ["INVOICE", "CREDIT_MEMO"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["OPEN", "CLOSED"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ad_account_id : String? = nil, @ad_account_name : String? = nil, @amount_billed_micro_currency : Int32? = nil, @amount_discount_micro_currency : Int32? = nil, @amount_net_micro_currency : Int32? = nil, @amount_tax_micro_currency : Int32? = nil, @bill_to_country : String? = nil, @billing_period_end_date : Time? = nil, @billing_period_start_date : Time? = nil, @currency : Currency? = nil, @document_type : String? = nil, @id : String? = nil, @invoice_due_date : Time? = nil, @payment_terms : String? = nil, @status : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@ad_account_id.nil? && @ad_account_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !@billing_period_end_date.nil? && @billing_period_end_date.try &.!~ pattern
        invalid_properties.push("invalid value for \"billing_period_end_date\", must conform to the pattern #{pattern}.")
      end

      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !@billing_period_start_date.nil? && @billing_period_start_date.try &.!~ pattern
        invalid_properties.push("invalid value for \"billing_period_start_date\", must conform to the pattern #{pattern}.")
      end

      document_type_validator = EnumAttributeValidatorForDocument_type.new
      if !document_type_validator.valid?(@document_type)
        message = document_type_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if !@id.nil? && @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !@invoice_due_date.nil? && @invoice_due_date.try &.!~ pattern
        invalid_properties.push("invalid value for \"invoice_due_date\", must conform to the pattern #{pattern}.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@ad_account_id.nil? && @ad_account_id.try &.!~ /^\d+$/
      return false if !@billing_period_end_date.nil? && @billing_period_end_date.try &.!~ /^(\d{4})-(\d{2})-(\d{2})$/
      return false if !@billing_period_start_date.nil? && @billing_period_start_date.try &.!~ /^(\d{4})-(\d{2})-(\d{2})$/
      document_type_validator = EnumAttributeValidatorForDocument_type.new
      return false unless document_type_validator.valid?(@document_type)
      return false if !@id.nil? && @id.try &.!~ /^\d+$/
      return false if !@invoice_due_date.nil? && @invoice_due_date.try &.!~ /^(\d{4})-(\d{2})-(\d{2})$/
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ad_account_id Value to be assigned
    def ad_account_id=(ad_account_id)
      pattern = /^\d+$/
      if !ad_account_id.nil? && ad_account_id !~ pattern
        raise ArgumentError.new("invalid value for \"ad_account_id\", must conform to the pattern #{pattern}.")
      end

      @ad_account_id = ad_account_id
    end

    # Custom attribute writer method with validation
    # @param [Object] billing_period_end_date Value to be assigned
    def billing_period_end_date=(billing_period_end_date)
      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !billing_period_end_date.nil? && billing_period_end_date !~ pattern
        raise ArgumentError.new("invalid value for \"billing_period_end_date\", must conform to the pattern #{pattern}.")
      end

      @billing_period_end_date = billing_period_end_date
    end

    # Custom attribute writer method with validation
    # @param [Object] billing_period_start_date Value to be assigned
    def billing_period_start_date=(billing_period_start_date)
      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !billing_period_start_date.nil? && billing_period_start_date !~ pattern
        raise ArgumentError.new("invalid value for \"billing_period_start_date\", must conform to the pattern #{pattern}.")
      end

      @billing_period_start_date = billing_period_start_date
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] document_type Object to be assigned
    def document_type=(document_type)
      validator = EnumAttributeValidatorForDocument_type.new
      unless validator.valid?(document_type)
        raise ArgumentError.new(validator.message)
      end
      @document_type = document_type
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if !id.nil? && id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method with validation
    # @param [Object] invoice_due_date Value to be assigned
    def invoice_due_date=(invoice_due_date)
      pattern = /^(\d{4})-(\d{2})-(\d{2})$/
      if !invoice_due_date.nil? && invoice_due_date !~ pattern
        raise ArgumentError.new("invalid value for \"invoice_due_date\", must conform to the pattern #{pattern}.")
      end

      @invoice_due_date = invoice_due_date
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          ad_account_id == other.ad_account_id &&
          ad_account_name == other.ad_account_name &&
          amount_billed_micro_currency == other.amount_billed_micro_currency &&
          amount_discount_micro_currency == other.amount_discount_micro_currency &&
          amount_net_micro_currency == other.amount_net_micro_currency &&
          amount_tax_micro_currency == other.amount_tax_micro_currency &&
          bill_to_country == other.bill_to_country &&
          billing_period_end_date == other.billing_period_end_date &&
          billing_period_start_date == other.billing_period_start_date &&
          currency == other.currency &&
          document_type == other.document_type &&
          id == other.id &&
          invoice_due_date == other.invoice_due_date &&
          payment_terms == other.payment_terms &&
          status == other.status
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ad_account_id, ad_account_name, amount_billed_micro_currency, amount_discount_micro_currency, amount_net_micro_currency, amount_tax_micro_currency, bill_to_country, billing_period_end_date, billing_period_start_date, currency, document_type, id, invoice_due_date, payment_terms, status].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ad_account_id"] = _to_h(ad_account_id)
      hash["ad_account_name"] = _to_h(ad_account_name)
      hash["amount_billed_micro_currency"] = _to_h(amount_billed_micro_currency)
      hash["amount_discount_micro_currency"] = _to_h(amount_discount_micro_currency)
      hash["amount_net_micro_currency"] = _to_h(amount_net_micro_currency)
      hash["amount_tax_micro_currency"] = _to_h(amount_tax_micro_currency)
      hash["bill_to_country"] = _to_h(bill_to_country)
      hash["billing_period_end_date"] = _to_h(billing_period_end_date)
      hash["billing_period_start_date"] = _to_h(billing_period_start_date)
      hash["currency"] = _to_h(currency)
      hash["document_type"] = _to_h(document_type)
      hash["id"] = _to_h(id)
      hash["invoice_due_date"] = _to_h(invoice_due_date)
      hash["payment_terms"] = _to_h(payment_terms)
      hash["status"] = _to_h(status)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
