# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Video ID-based media source.
  class PinMediaSourceVideoID
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "media_id", type: String, nillable: false, emit_null: false)]
    property media_id : String

    @[JSON::Field(key: "source_type", type: String, nillable: false, emit_null: false)]
    property source_type : String

    # Optional properties
    # Content type for cover image Base64.
    @[JSON::Field(key: "cover_image_content_type", type: ContentType?, nillable: true, emit_null: false)]
    property cover_image_content_type : ContentType?

    # Cover image Base64.
    @[JSON::Field(key: "cover_image_data", type: String?, nillable: true, emit_null: false)]
    property cover_image_data : String?

    # Keyframe timestamp for cover image (seconds). If entered time exceeds video duration, the last frame is used.
    @[JSON::Field(key: "cover_image_key_frame_time", type: Int32?, nillable: true, emit_null: false)]
    property cover_image_key_frame_time : Int32?

    # Cover image URL.
    @[JSON::Field(key: "cover_image_url", type: String?, nillable: true, emit_null: false)]
    property cover_image_url : String?

    # Set the parameter to false to create the new simplified Pin instead of the standard pin. Currently the field is only available to a list of beta users.
    @[JSON::Field(key: "is_standard", type: Bool?, default: true, nillable: true, emit_null: false)]
    property is_standard : Bool?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForSource_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "source_type"
        @allowable_values = ["video_id"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@media_id : String, @source_type : String, @cover_image_content_type : ContentType? = nil, @cover_image_data : String? = nil, @cover_image_key_frame_time : Int32? = nil, @cover_image_url : String? = nil, @is_standard : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@cover_image_key_frame_time.nil? && @cover_image_key_frame_time.try &.< 0
        invalid_properties.push("invalid value for \"cover_image_key_frame_time\", must be greater than or equal to 0.")
      end

      pattern = /^\d+$/
      if @media_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"media_id\", must conform to the pattern #{pattern}.")
      end

      source_type_validator = EnumAttributeValidatorForSource_type.new
      if !source_type_validator.valid?(@source_type)
        message = source_type_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@cover_image_key_frame_time.nil? && @cover_image_key_frame_time.try &.< 0
      return false if @media_id.try &.!~ /^\d+$/
      source_type_validator = EnumAttributeValidatorForSource_type.new
      return false unless source_type_validator.valid?(@source_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] cover_image_key_frame_time Value to be assigned
    def cover_image_key_frame_time=(cover_image_key_frame_time)
      if !cover_image_key_frame_time.nil? && cover_image_key_frame_time < 0
        raise ArgumentError.new("invalid value for \"cover_image_key_frame_time\", must be greater than or equal to 0.")
      end

      @cover_image_key_frame_time = cover_image_key_frame_time
    end

    # Custom attribute writer method with validation
    # @param [Object] media_id Value to be assigned
    def media_id=(media_id)
      pattern = /^\d+$/
      if media_id !~ pattern
        raise ArgumentError.new("invalid value for \"media_id\", must conform to the pattern #{pattern}.")
      end

      @media_id = media_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] source_type Object to be assigned
    def source_type=(source_type)
      validator = EnumAttributeValidatorForSource_type.new
      unless validator.valid?(source_type)
        raise ArgumentError.new(validator.message)
      end
      @source_type = source_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          cover_image_content_type == other.cover_image_content_type &&
          cover_image_data == other.cover_image_data &&
          cover_image_key_frame_time == other.cover_image_key_frame_time &&
          cover_image_url == other.cover_image_url &&
          is_standard == other.is_standard &&
          media_id == other.media_id &&
          source_type == other.source_type
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [cover_image_content_type, cover_image_data, cover_image_key_frame_time, cover_image_url, is_standard, media_id, source_type].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["cover_image_content_type"] = _to_h(cover_image_content_type)
      hash["cover_image_data"] = _to_h(cover_image_data)
      hash["cover_image_key_frame_time"] = _to_h(cover_image_key_frame_time)
      hash["cover_image_url"] = _to_h(cover_image_url)
      hash["is_standard"] = _to_h(is_standard)
      hash["media_id"] = _to_h(media_id)
      hash["source_type"] = _to_h(source_type)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
