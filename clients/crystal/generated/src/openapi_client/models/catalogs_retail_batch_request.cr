# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # A request object that can have multiple operations on a single retail batch
  class CatalogsRetailBatchRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "catalog_type", type: String, nillable: false, emit_null: false)]
    property catalog_type : String

    @[JSON::Field(key: "country", type: Country, nillable: false, emit_null: false)]
    property country : Country

    # Array with catalogs item operations
    @[JSON::Field(key: "items", type: Array(CatalogsRetailBatchRequestItemsInner), nillable: false, emit_null: false)]
    property items : Array(CatalogsRetailBatchRequestItemsInner)

    # We recommend using the CatalogsLocale values.
    @[JSON::Field(key: "language", type: String, nillable: false, emit_null: false)]
    property language : String

    # Optional properties
    # Catalog id pertaining to the retail item. If not provided, default to oldest retail catalog
    @[JSON::Field(key: "catalog_id", type: String?, nillable: true, emit_null: false)]
    property catalog_id : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForCatalog_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "catalog_type"
        @allowable_values = ["RETAIL"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForLanguage < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "language"
        @allowable_values = ["af-ZA", "ar-SA", "bg-BG", "bn-IN", "cs-CZ", "da-DK", "de", "el-GR", "en-AU", "en-CA", "en-GB", "en-IN", "en-US", "es-419", "es-AR", "es-ES", "es-MX", "fi-FI", "fr", "fr-CA", "he-IL", "hi-IN", "hr-HR", "hu-HU", "id-ID", "it", "ja", "ko-KR", "ms-MY", "nb-NO", "nl", "pl-PL", "pt-BR", "pt-PT", "ro-RO", "ru-RU", "sk-SK", "sv-SE", "te-IN", "th-TH", "tl-PH", "tr", "uk-UA", "vi-VN", "zh-CN", "zh-TW", "AM", "AR", "AZ", "BG", "BN", "BS", "CA", "CS", "DA", "DV", "DZ", "DE", "EL", "EN", "ES", "ET", "FA", "FI", "FR", "HE", "HI", "HR", "HU", "HY", "ID", "IN", "IS", "IT", "IW", "JA", "KA", "KM", "KO", "LO", "LT", "LV", "MK", "MN", "MS", "MY", "NB", "NE", "NL", "NO", "PL", "PT", "RO", "RU", "SK", "SL", "SQ", "SR", "SV", "TL", "UK", "VI", "TE", "TH", "TR", "XX", "ZH"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@catalog_type : String, @country : Country, @items : Array(CatalogsRetailBatchRequestItemsInner), @language : String, @catalog_id : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@catalog_id.nil? && @catalog_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"catalog_id\", must conform to the pattern #{pattern}.")
      end

      catalog_type_validator = EnumAttributeValidatorForCatalog_type.new
      if !catalog_type_validator.valid?(@catalog_type)
        message = catalog_type_validator.message
        invalid_properties.push(message)
      end

      if @items.try &.size.try &.> 1000
        invalid_properties.push("invalid value for \"items\", number of items must be less than or equal to 1000."
      end

      if @items.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"items\", number of items must be greater than or equal to 1."
      end

      language_validator = EnumAttributeValidatorForLanguage.new
      if !language_validator.valid?(@language)
        message = language_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@catalog_id.nil? && @catalog_id.try &.!~ /^\d+$/
      catalog_type_validator = EnumAttributeValidatorForCatalog_type.new
      return false unless catalog_type_validator.valid?(@catalog_type)
      return false if @items.try &.size.try &.> 1000
      return false if @items.try &.size.try &.< 1
      language_validator = EnumAttributeValidatorForLanguage.new
      return false unless language_validator.valid?(@language)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] catalog_id Value to be assigned
    def catalog_id=(catalog_id)
      pattern = /^\d+$/
      if !catalog_id.nil? && catalog_id !~ pattern
        raise ArgumentError.new("invalid value for \"catalog_id\", must conform to the pattern #{pattern}.")
      end

      @catalog_id = catalog_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] catalog_type Object to be assigned
    def catalog_type=(catalog_type)
      validator = EnumAttributeValidatorForCatalog_type.new
      unless validator.valid?(catalog_type)
        raise ArgumentError.new(validator.message)
      end
      @catalog_type = catalog_type
    end

    # Custom attribute writer method with validation
    # @param [Object] items Value to be assigned
    def items=(items)
      if items.size > 1000
        raise ArgumentError.new("invalid value for \"items\", number of items must be less than or equal to 1000.")
      end

      if items.size < 1
        raise ArgumentError.new("invalid value for \"items\", number of items must be greater than or equal to 1.")
      end

      @items = items
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] language Object to be assigned
    def language=(language)
      validator = EnumAttributeValidatorForLanguage.new
      unless validator.valid?(language)
        raise ArgumentError.new(validator.message)
      end
      @language = language
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          catalog_id == other.catalog_id &&
          catalog_type == other.catalog_type &&
          country == other.country &&
          items == other.items &&
          language == other.language
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [catalog_id, catalog_type, country, items, language].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["catalog_id"] = _to_h(catalog_id)
      hash["catalog_type"] = _to_h(catalog_type)
      hash["country"] = _to_h(country)
      hash["items"] = _to_h(items)
      hash["language"] = _to_h(language)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
