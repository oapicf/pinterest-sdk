# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Object containing the MSOT conversion events.
  class ConversionMSOTEvents
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The ID of the ad group that was attributed to the conversion event.
    @[JSON::Field(key: "ad_group_id", type: String, nillable: false, emit_null: false)]
    property ad_group_id : String

    # Ad event type.
    @[JSON::Field(key: "attribution_scope", type: String, nillable: false, emit_null: false)]
    property attribution_scope : String

    # A unique id string that identifies this event. If you are already sending us events through Conversions API, then this id should match the event_id sent through Conversions API.
    @[JSON::Field(key: "event_id", type: String, nillable: false, emit_null: false)]
    property event_id : String

    # Type of user event.
    @[JSON::Field(key: "event_name", type: String, nillable: false, emit_null: false)]
    property event_name : String

    # The time when the event occurred. Unix timestamp in seconds.
    @[JSON::Field(key: "event_timestamp", type: Int64, nillable: false, emit_null: false)]
    property event_timestamp : Int64

    # Optional properties
    # Timestamp(s) when the ad action(s) happened. Unix timestamp in seconds.
    @[JSON::Field(key: "action_timestamps", type: Array(Int64)?, nillable: true, emit_null: false)]
    property action_timestamps : Array(Int64)?

    # The attribution model used to attribute the conversion event.
    @[JSON::Field(key: "attribution_model", type: String?, nillable: true, emit_null: false)]
    property attribution_model : String?

    # Credit given to the attributed ad actions. Allowed values are > 0 and <= 1.
    @[JSON::Field(key: "attribution_score", type: Float64?, nillable: true, emit_null: false)]
    property attribution_score : Float64?

    # The ID of the campaign that was attributed to the conversion event.
    @[JSON::Field(key: "campaign_id", type: String?, nillable: true, emit_null: false)]
    property campaign_id : String?

    @[JSON::Field(key: "currency", type: Currency?, nillable: true, emit_null: false)]
    property currency : Currency?

    # Total number of ad events including other non-Pinterest ad platforms.
    @[JSON::Field(key: "total_event_touchpoints", type: Int32?, nillable: true, emit_null: false)]
    property total_event_touchpoints : Int32?

    # Total number of conversion events that are reported in one API call. <p>If you are sending one API request for one attributed conversion event then this value should be 1.</p> <p>If you are sending multiple attributed conversion events in one API request then this value should be the total number of attributed conversion events in the request.</p>
    @[JSON::Field(key: "total_events", type: Int32?, nillable: true, emit_null: false)]
    property total_events : Int32?

    # Order value of the conversion event. Required if <code>event_name</code> is 'add_to_cart' or 'checkout'.
    @[JSON::Field(key: "value", type: Float64?, nillable: true, emit_null: false)]
    property value : Float64?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForAttribution_model < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "attribution_model"
        @allowable_values = ["first_touch", "last_touch", "multi_touch"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForAttribution_scope < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "attribution_scope"
        @allowable_values = ["view", "engagement", "click"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForEvent_name < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "event_name"
        @allowable_values = ["add_to_cart", "checkout", "lead", "signup"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ad_group_id : String, @attribution_scope : String, @event_id : String, @event_name : String, @event_timestamp : Int64, @action_timestamps : Array(Int64)? = nil, @attribution_model : String? = nil, @attribution_score : Float64? = nil, @campaign_id : String? = nil, @currency : Currency? = nil, @total_event_touchpoints : Int32? = nil, @total_events : Int32? = nil, @value : Float64? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if @ad_group_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"ad_group_id\", must conform to the pattern #{pattern}.")
      end

      attribution_model_validator = EnumAttributeValidatorForAttribution_model.new
      if !attribution_model_validator.valid?(@attribution_model)
        message = attribution_model_validator.message
        invalid_properties.push(message)
      end

      attribution_scope_validator = EnumAttributeValidatorForAttribution_scope.new
      if !attribution_scope_validator.valid?(@attribution_scope)
        message = attribution_scope_validator.message
        invalid_properties.push(message)
      end

      if !@attribution_score.nil? && @attribution_score.try &.> 1
        invalid_properties.push("invalid value for \"attribution_score\", must be smaller than or equal to 1.")
      end

      if !@attribution_score.nil? && @attribution_score.try &.<= 0
        invalid_properties.push("invalid value for \"attribution_score\", must be greater than 0.")
      end

      pattern = /^\d+$/
      if !@campaign_id.nil? && @campaign_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"campaign_id\", must conform to the pattern #{pattern}.")
      end

      if @event_id.try &.to_s.try &.size.try &.> 256
        invalid_properties.push("invalid value for \"event_id\", the character length must be smaller than or equal to 256.")
      end

      event_name_validator = EnumAttributeValidatorForEvent_name.new
      if !event_name_validator.valid?(@event_name)
        message = event_name_validator.message
        invalid_properties.push(message)
      end

      if !@total_event_touchpoints.nil? && @total_event_touchpoints.try &.< 1
        invalid_properties.push("invalid value for \"total_event_touchpoints\", must be greater than or equal to 1.")
      end

      if !@total_events.nil? && @total_events.try &.< 1
        invalid_properties.push("invalid value for \"total_events\", must be greater than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @ad_group_id.try &.!~ /^\d+$/
      attribution_model_validator = EnumAttributeValidatorForAttribution_model.new
      return false unless attribution_model_validator.valid?(@attribution_model)
      attribution_scope_validator = EnumAttributeValidatorForAttribution_scope.new
      return false unless attribution_scope_validator.valid?(@attribution_scope)
      return false if !@attribution_score.nil? && @attribution_score.try &.> 1
      return false if !@attribution_score.nil? && @attribution_score.try &.<= 0
      return false if !@campaign_id.nil? && @campaign_id.try &.!~ /^\d+$/
      return false if @event_id.try &.to_s.try &.size.try &.> 256
      event_name_validator = EnumAttributeValidatorForEvent_name.new
      return false unless event_name_validator.valid?(@event_name)
      return false if !@total_event_touchpoints.nil? && @total_event_touchpoints.try &.< 1
      return false if !@total_events.nil? && @total_events.try &.< 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ad_group_id Value to be assigned
    def ad_group_id=(ad_group_id)
      pattern = /^\d+$/
      if ad_group_id !~ pattern
        raise ArgumentError.new("invalid value for \"ad_group_id\", must conform to the pattern #{pattern}.")
      end

      @ad_group_id = ad_group_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] attribution_model Object to be assigned
    def attribution_model=(attribution_model)
      validator = EnumAttributeValidatorForAttribution_model.new
      unless validator.valid?(attribution_model)
        raise ArgumentError.new(validator.message)
      end
      @attribution_model = attribution_model
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] attribution_scope Object to be assigned
    def attribution_scope=(attribution_scope)
      validator = EnumAttributeValidatorForAttribution_scope.new
      unless validator.valid?(attribution_scope)
        raise ArgumentError.new(validator.message)
      end
      @attribution_scope = attribution_scope
    end

    # Custom attribute writer method with validation
    # @param [Object] attribution_score Value to be assigned
    def attribution_score=(attribution_score)
      if !attribution_score.nil? && attribution_score > 1
        raise ArgumentError.new("invalid value for \"attribution_score\", must be smaller than or equal to 1.")
      end

      if !attribution_score.nil? && attribution_score <= 0
        raise ArgumentError.new("invalid value for \"attribution_score\", must be greater than 0.")
      end

      @attribution_score = attribution_score
    end

    # Custom attribute writer method with validation
    # @param [Object] campaign_id Value to be assigned
    def campaign_id=(campaign_id)
      pattern = /^\d+$/
      if !campaign_id.nil? && campaign_id !~ pattern
        raise ArgumentError.new("invalid value for \"campaign_id\", must conform to the pattern #{pattern}.")
      end

      @campaign_id = campaign_id
    end

    # Custom attribute writer method with validation
    # @param [Object] event_id Value to be assigned
    def event_id=(event_id)
      if event_id.to_s.size > 256
        raise ArgumentError.new("invalid value for \"event_id\", the character length must be smaller than or equal to 256.")
      end

      @event_id = event_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] event_name Object to be assigned
    def event_name=(event_name)
      validator = EnumAttributeValidatorForEvent_name.new
      unless validator.valid?(event_name)
        raise ArgumentError.new(validator.message)
      end
      @event_name = event_name
    end

    # Custom attribute writer method with validation
    # @param [Object] total_event_touchpoints Value to be assigned
    def total_event_touchpoints=(total_event_touchpoints)
      if !total_event_touchpoints.nil? && total_event_touchpoints < 1
        raise ArgumentError.new("invalid value for \"total_event_touchpoints\", must be greater than or equal to 1.")
      end

      @total_event_touchpoints = total_event_touchpoints
    end

    # Custom attribute writer method with validation
    # @param [Object] total_events Value to be assigned
    def total_events=(total_events)
      if !total_events.nil? && total_events < 1
        raise ArgumentError.new("invalid value for \"total_events\", must be greater than or equal to 1.")
      end

      @total_events = total_events
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          action_timestamps == other.action_timestamps &&
          ad_group_id == other.ad_group_id &&
          attribution_model == other.attribution_model &&
          attribution_scope == other.attribution_scope &&
          attribution_score == other.attribution_score &&
          campaign_id == other.campaign_id &&
          currency == other.currency &&
          event_id == other.event_id &&
          event_name == other.event_name &&
          event_timestamp == other.event_timestamp &&
          total_event_touchpoints == other.total_event_touchpoints &&
          total_events == other.total_events &&
          value == other.value
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [action_timestamps, ad_group_id, attribution_model, attribution_scope, attribution_score, campaign_id, currency, event_id, event_name, event_timestamp, total_event_touchpoints, total_events, value].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["action_timestamps"] = _to_h(action_timestamps)
      hash["ad_group_id"] = _to_h(ad_group_id)
      hash["attribution_model"] = _to_h(attribution_model)
      hash["attribution_scope"] = _to_h(attribution_scope)
      hash["attribution_score"] = _to_h(attribution_score)
      hash["campaign_id"] = _to_h(campaign_id)
      hash["currency"] = _to_h(currency)
      hash["event_id"] = _to_h(event_id)
      hash["event_name"] = _to_h(event_name)
      hash["event_timestamp"] = _to_h(event_timestamp)
      hash["total_event_touchpoints"] = _to_h(total_event_touchpoints)
      hash["total_events"] = _to_h(total_events)
      hash["value"] = _to_h(value)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
