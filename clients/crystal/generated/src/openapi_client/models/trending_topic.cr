# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Individual trending topic within an interest category
  class TrendingTopic
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Description of the trending topic
    @[JSON::Field(key: "description", type: String, nillable: false, emit_null: false)]
    property description : String

    # Month-over-month growth percentage
    @[JSON::Field(key: "percent_growth_mom", type: Int32, nillable: false, emit_null: false)]
    property percent_growth_mom : Int32

    # Array of pin images related to this trend (up to 6)
    @[JSON::Field(key: "pins", type: Array(TrendingPin), nillable: false, emit_null: false)]
    property pins : Array(TrendingPin)

    # List of related interest categories
    @[JSON::Field(key: "related_interests", type: Array(String), nillable: false, emit_null: false)]
    property related_interests : Array(String)

    # List of related search terms
    @[JSON::Field(key: "related_searches", type: Array(String), nillable: false, emit_null: false)]
    property related_searches : Array(String)

    # Time series data showing trend values over time, with dates as keys and values as numeric
    @[JSON::Field(key: "time_series", type: Hash(String, Float64), nillable: false, emit_null: false)]
    property time_series : Hash(String, Float64)

    # Title of the trending topic
    @[JSON::Field(key: "title", type: String, nillable: false, emit_null: false)]
    property title : String

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@description : String, @percent_growth_mom : Int32, @pins : Array(TrendingPin), @related_interests : Array(String), @related_searches : Array(String), @time_series : Hash(String, Float64), @title : String)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @pins.try &.size.try &.> 6
        invalid_properties.push("invalid value for \"pins\", number of items must be less than or equal to 6."
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @pins.try &.size.try &.> 6
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] pins Value to be assigned
    def pins=(pins)
      if pins.size > 6
        raise ArgumentError.new("invalid value for \"pins\", number of items must be less than or equal to 6.")
      end

      @pins = pins
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          description == other.description &&
          percent_growth_mom == other.percent_growth_mom &&
          pins == other.pins &&
          related_interests == other.related_interests &&
          related_searches == other.related_searches &&
          time_series == other.time_series &&
          title == other.title
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [description, percent_growth_mom, pins, related_interests, related_searches, time_series, title].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["description"] = _to_h(description)
      hash["percent_growth_mom"] = _to_h(percent_growth_mom)
      hash["pins"] = _to_h(pins)
      hash["related_interests"] = _to_h(related_interests)
      hash["related_searches"] = _to_h(related_searches)
      hash["time_series"] = _to_h(time_series)
      hash["title"] = _to_h(title)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
