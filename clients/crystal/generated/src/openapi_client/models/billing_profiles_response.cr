# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class BillingProfilesResponse
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Billing ID.
    @[JSON::Field(key: "id", type: String?, nillable: true, emit_null: false)]
    property id : String?

    # Type of the card.
    @[JSON::Field(key: "card_type", type: String?, nillable: true, emit_null: false)]
    property card_type : String?

    # Status of the billing.
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    # Advertiser ID of the billing.
    @[JSON::Field(key: "advertiser_id", type: String?, nillable: true, emit_null: false)]
    property advertiser_id : String?

    # Brand of the payment method.
    @[JSON::Field(key: "payment_method_brand", type: String?, nillable: true, emit_null: false)]
    property payment_method_brand : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForCard_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "card_type"
        @allowable_values = ["UNKNOWN", "VISA", "MASTERCARD", "AMERICAN_EXPRESS", "DISCOVER", "ELO"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["UNSPECIFIED", "VALID", "INVALID", "PENDING", "DELETED", "SECONDARY", "PENDING_SECONDARY"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForPayment_method_brand < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "payment_method_brand"
        @allowable_values = ["UNKNOWN", "VISA", "MASTERCARD", "AMERICAN_EXPRESS", "DISCOVER", "SOFORT", "DINERS_CLUB", "ELO", "CARTE_BANCAIRE"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String? = nil, @card_type : String? = nil, @status : String? = nil, @advertiser_id : String? = nil, @payment_method_brand : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if !@id.nil? && @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      card_type_validator = EnumAttributeValidatorForCard_type.new
      if !card_type_validator.valid?(@card_type)
        message = card_type_validator.message
        invalid_properties.push(message)
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if !@advertiser_id.nil? && @advertiser_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"advertiser_id\", must conform to the pattern #{pattern}.")
      end

      payment_method_brand_validator = EnumAttributeValidatorForPayment_method_brand.new
      if !payment_method_brand_validator.valid?(@payment_method_brand)
        message = payment_method_brand_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@id.nil? && @id.try &.!~ /^\d+$/
      card_type_validator = EnumAttributeValidatorForCard_type.new
      return false unless card_type_validator.valid?(@card_type)
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@advertiser_id.nil? && @advertiser_id.try &.!~ /^\d+$/
      payment_method_brand_validator = EnumAttributeValidatorForPayment_method_brand.new
      return false unless payment_method_brand_validator.valid?(@payment_method_brand)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if !id.nil? && id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] card_type Object to be assigned
    def card_type=(card_type)
      validator = EnumAttributeValidatorForCard_type.new
      unless validator.valid?(card_type)
        raise ArgumentError.new(validator.message)
      end
      @card_type = card_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] advertiser_id Value to be assigned
    def advertiser_id=(advertiser_id)
      pattern = /^\d+$/
      if !advertiser_id.nil? && advertiser_id !~ pattern
        raise ArgumentError.new("invalid value for \"advertiser_id\", must conform to the pattern #{pattern}.")
      end

      @advertiser_id = advertiser_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] payment_method_brand Object to be assigned
    def payment_method_brand=(payment_method_brand)
      validator = EnumAttributeValidatorForPayment_method_brand.new
      unless validator.valid?(payment_method_brand)
        raise ArgumentError.new(validator.message)
      end
      @payment_method_brand = payment_method_brand
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          id == other.id &&
          card_type == other.card_type &&
          status == other.status &&
          advertiser_id == other.advertiser_id &&
          payment_method_brand == other.payment_method_brand
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, card_type, status, advertiser_id, payment_method_brand].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["card_type"] = _to_h(card_type)
      hash["status"] = _to_h(status)
      hash["advertiser_id"] = _to_h(advertiser_id)
      hash["payment_method_brand"] = _to_h(payment_method_brand)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
