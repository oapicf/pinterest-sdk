# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Ad group targeting specification defining the ad group target audience. For example, `{\"APPTYPE\":[\"iphone\"], \"GENDER\":[\"male\"], \"LOCALE\":[\"en-US\"], \"LOCATION\":[\"501\"], \"MINIMUM_AGE\":\"18\", \"MAXIMUM_AGE\":\"65+\"}`
  class TargetingSpec
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted.
    @[JSON::Field(key: "AGE_BUCKET", type: Array(TargetingSpecAgeBucket)?, nillable: true, emit_null: false)]
    property age_bucket : Array(TargetingSpecAgeBucket)?

    # Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted.
    @[JSON::Field(key: "APPTYPE", type: Array(TargetingSpecAppType)?, nillable: true, emit_null: false)]
    property apptype : Array(TargetingSpecAppType)?

    # Excluded customer list IDs. Used to drive new customer acquisition goals. For example: [\"2542620905475\"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**.
    @[JSON::Field(key: "AUDIENCE_EXCLUDE", type: Array(String)?, nillable: true, emit_null: false)]
    property audience_exclude : Array(String)?

    # Targeted customer list IDs. For example: [\"2542620905473\"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**.
    @[JSON::Field(key: "AUDIENCE_INCLUDE", type: Array(String)?, nillable: true, emit_null: false)]
    property audience_include : Array(String)?

    # Targeted genders. Values: [\"unknown\",\"male\",\"female\"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**.
    @[JSON::Field(key: "GENDER", type: Array(TargetingSpecGender)?, nillable: true, emit_null: false)]
    property gender : Array(TargetingSpecGender)?

    # Location region codes, e.g., \"BE-VOV\" (East Flanders, Belgium) For complete list, <a href=\"https://help.pinterest.com/sub/helpcenter/partner/pinterest_location_targeting_codes.xlsx\" target=\"_blank\">click here</a> or postal codes, e.g., \"US-94107\". Use either region codes or postal codes but not both. At least one of LOCATION or GEO must be specified. If the GEO field is missing, then only LOCATION values will be targeted (see LOCATION field below).
    @[JSON::Field(key: "GEO", type: Array(String)?, nillable: true, emit_null: false)]
    property geo : Array(String)?

    # Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**.
    @[JSON::Field(key: "INTEREST", type: Array(String)?, nillable: true, emit_null: false)]
    property interest : Array(String)?

    # 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted.
    @[JSON::Field(key: "LOCALE", type: Array(String)?, nillable: true, emit_null: false)]
    property locale : Array(String)?

    # 22 ISO Alpha 2 two letter country codes or US Nielsen DMA (Designated Market Area) codes (location region codes) (e.g., [\"US\", \"807\"]). For complete list, <a href=\"https://help.pinterest.com/sub/helpcenter/partner/pinterest_location_targeting_codes.xlsx\" target=\"_blank\">click here</a>. Location-Country and Location-Metro codes apply. At least one of LOCATION or GEO must be specified. If the LOCATION field is missing, then only GEO values will be targeted (see GEO field above).
    @[JSON::Field(key: "LOCATION", type: Array(String)?, nillable: true, emit_null: false)]
    property location : Array(String)?

    # Maximum age to target (inclusive). Values: \"18\", \"19\", ..., \"65\", \"65+\". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted.
    @[JSON::Field(key: "MAXIMUM_AGE", type: String?, nillable: true, emit_null: false)]
    property maximum_age : String?

    # Minimum age to target (inclusive). Values: \"18\", \"19\", ..., \"65\". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted.
    @[JSON::Field(key: "MINIMUM_AGE", type: String?, nillable: true, emit_null: false)]
    property minimum_age : String?

    # Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting
    @[JSON::Field(key: "SHOPPING_RETARGETING", type: Array(TargetingSpecShoppingRetargeting)?, nillable: true, emit_null: false)]
    property shopping_retargeting : Array(TargetingSpecShoppingRetargeting)?

    # 
    @[JSON::Field(key: "TARGETING_STRATEGY", type: Array(String)?, nillable: true, emit_null: false)]
    property targeting_strategy : Array(String)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@age_bucket : Array(TargetingSpecAgeBucket)? = nil, @apptype : Array(TargetingSpecAppType)? = nil, @audience_exclude : Array(String)? = nil, @audience_include : Array(String)? = nil, @gender : Array(TargetingSpecGender)? = nil, @geo : Array(String)? = nil, @interest : Array(String)? = nil, @locale : Array(String)? = nil, @location : Array(String)? = nil, @maximum_age : String? = nil, @minimum_age : String? = nil, @shopping_retargeting : Array(TargetingSpecShoppingRetargeting)? = nil, @targeting_strategy : Array(String)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+\+?$/
      if !@maximum_age.nil? && @maximum_age.try &.!~ pattern
        invalid_properties.push("invalid value for \"maximum_age\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if !@minimum_age.nil? && @minimum_age.try &.!~ pattern
        invalid_properties.push("invalid value for \"minimum_age\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@maximum_age.nil? && @maximum_age.try &.!~ /^\d+\+?$/
      return false if !@minimum_age.nil? && @minimum_age.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] maximum_age Value to be assigned
    def maximum_age=(maximum_age)
      pattern = /^\d+\+?$/
      if !maximum_age.nil? && maximum_age !~ pattern
        raise ArgumentError.new("invalid value for \"maximum_age\", must conform to the pattern #{pattern}.")
      end

      @maximum_age = maximum_age
    end

    # Custom attribute writer method with validation
    # @param [Object] minimum_age Value to be assigned
    def minimum_age=(minimum_age)
      pattern = /^\d+$/
      if !minimum_age.nil? && minimum_age !~ pattern
        raise ArgumentError.new("invalid value for \"minimum_age\", must conform to the pattern #{pattern}.")
      end

      @minimum_age = minimum_age
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          age_bucket == other.age_bucket &&
          apptype == other.apptype &&
          audience_exclude == other.audience_exclude &&
          audience_include == other.audience_include &&
          gender == other.gender &&
          geo == other.geo &&
          interest == other.interest &&
          locale == other.locale &&
          location == other.location &&
          maximum_age == other.maximum_age &&
          minimum_age == other.minimum_age &&
          shopping_retargeting == other.shopping_retargeting &&
          targeting_strategy == other.targeting_strategy
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [age_bucket, apptype, audience_exclude, audience_include, gender, geo, interest, locale, location, maximum_age, minimum_age, shopping_retargeting, targeting_strategy].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["AGE_BUCKET"] = _to_h(age_bucket)
      hash["APPTYPE"] = _to_h(apptype)
      hash["AUDIENCE_EXCLUDE"] = _to_h(audience_exclude)
      hash["AUDIENCE_INCLUDE"] = _to_h(audience_include)
      hash["GENDER"] = _to_h(gender)
      hash["GEO"] = _to_h(geo)
      hash["INTEREST"] = _to_h(interest)
      hash["LOCALE"] = _to_h(locale)
      hash["LOCATION"] = _to_h(location)
      hash["MAXIMUM_AGE"] = _to_h(maximum_age)
      hash["MINIMUM_AGE"] = _to_h(minimum_age)
      hash["SHOPPING_RETARGETING"] = _to_h(shopping_retargeting)
      hash["TARGETING_STRATEGY"] = _to_h(targeting_strategy)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
