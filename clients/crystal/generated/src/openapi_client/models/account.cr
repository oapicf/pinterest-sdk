# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class Account
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Profile about description.
    @[JSON::Field(key: "about", type: String?, nillable: true, emit_null: false)]
    property about : String?

    # Type of account
    @[JSON::Field(key: "account_type", type: String?, nillable: true, emit_null: false)]
    property account_type : String?

    # User account board count.<br/>**Note**: Board count on user account level may differ from counts found elsewhere due to attribution of collaborative Boards.
    @[JSON::Field(key: "board_count", type: Int32?, nillable: true, emit_null: false)]
    property board_count : Int32?

    @[JSON::Field(key: "business_name", type: String?, nillable: true, emit_null: false)]
    property business_name : String?

    # User account follower count.
    @[JSON::Field(key: "follower_count", type: Int32?, nillable: true, emit_null: false)]
    property follower_count : Int32?

    # User account following count.
    @[JSON::Field(key: "following_count", type: Int32?, nillable: true, emit_null: false)]
    property following_count : Int32?

    # User account ID.
    @[JSON::Field(key: "id", type: String?, nillable: true, emit_null: false)]
    property id : String?

    # User account monthly views.
    @[JSON::Field(key: "monthly_views", type: Int32?, nillable: true, emit_null: false)]
    property monthly_views : Int32?

    # User account pin count. This includes both created and saved pins.
    @[JSON::Field(key: "pin_count", type: Int32?, nillable: true, emit_null: false)]
    property pin_count : Int32?

    @[JSON::Field(key: "profile_image", type: String?, nillable: true, emit_null: false)]
    property profile_image : String?

    @[JSON::Field(key: "username", type: String?, nillable: true, emit_null: false)]
    property username : String?

    @[JSON::Field(key: "website_url", type: String?, nillable: true, emit_null: false)]
    property website_url : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForAccount_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "account_type"
        @allowable_values = ["PINNER", "BUSINESS"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@about : String? = nil, @account_type : String? = nil, @board_count : Int32? = nil, @business_name : String? = nil, @follower_count : Int32? = nil, @following_count : Int32? = nil, @id : String? = nil, @monthly_views : Int32? = nil, @pin_count : Int32? = nil, @profile_image : String? = nil, @username : String? = nil, @website_url : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      account_type_validator = EnumAttributeValidatorForAccount_type.new
      if !account_type_validator.valid?(@account_type)
        message = account_type_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if !@id.nil? && @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      account_type_validator = EnumAttributeValidatorForAccount_type.new
      return false unless account_type_validator.valid?(@account_type)
      return false if !@id.nil? && @id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] account_type Object to be assigned
    def account_type=(account_type)
      validator = EnumAttributeValidatorForAccount_type.new
      unless validator.valid?(account_type)
        raise ArgumentError.new(validator.message)
      end
      @account_type = account_type
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if !id.nil? && id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          about == other.about &&
          account_type == other.account_type &&
          board_count == other.board_count &&
          business_name == other.business_name &&
          follower_count == other.follower_count &&
          following_count == other.following_count &&
          id == other.id &&
          monthly_views == other.monthly_views &&
          pin_count == other.pin_count &&
          profile_image == other.profile_image &&
          username == other.username &&
          website_url == other.website_url
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [about, account_type, board_count, business_name, follower_count, following_count, id, monthly_views, pin_count, profile_image, username, website_url].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["about"] = _to_h(about)
      hash["account_type"] = _to_h(account_type)
      hash["board_count"] = _to_h(board_count)
      hash["business_name"] = _to_h(business_name)
      hash["follower_count"] = _to_h(follower_count)
      hash["following_count"] = _to_h(following_count)
      hash["id"] = _to_h(id)
      hash["monthly_views"] = _to_h(monthly_views)
      hash["pin_count"] = _to_h(pin_count)
      hash["profile_image"] = _to_h(profile_image)
      hash["username"] = _to_h(username)
      hash["website_url"] = _to_h(website_url)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
