# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Catalogs Retail Feed object
  class CatalogsRetailFeed
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "created_at", type: Time, nillable: false, emit_null: false)]
    property created_at : Time

    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    @[JSON::Field(key: "updated_at", type: Time, nillable: false, emit_null: false)]
    property updated_at : Time

    @[JSON::Field(key: "catalog_type", type: CatalogsType, nillable: false, emit_null: false)]
    property catalog_type : CatalogsType

    @[JSON::Field(key: "credentials", type: CatalogsFeedCredentials, nillable: false, emit_null: false)]
    property credentials : CatalogsFeedCredentials

    @[JSON::Field(key: "default_availability", type: ProductAvailabilityType, nillable: false, emit_null: false)]
    property default_availability : ProductAvailabilityType

    @[JSON::Field(key: "default_country", type: Country, nillable: false, emit_null: false)]
    property default_country : Country

    @[JSON::Field(key: "default_currency", type: NullableCurrency, nillable: false, emit_null: false)]
    property default_currency : NullableCurrency

    # The locale used within a feed for product descriptions.
    @[JSON::Field(key: "default_locale", type: String, nillable: false, emit_null: false)]
    property default_locale : String

    @[JSON::Field(key: "format", type: CatalogsFormat, nillable: false, emit_null: false)]
    property format : CatalogsFormat

    # The URL where a feed is available for download. This URL is what Pinterest will use to download a feed for processing.
    @[JSON::Field(key: "location", type: String, nillable: false, emit_null: false)]
    property location : String

    # A human-friendly name associated to a given feed. This value is currently nullable due to historical reasons. It is expected to become non-nullable in the future.
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "preferred_processing_schedule", type: CatalogsFeedProcessingSchedule, nillable: false, emit_null: false)]
    property preferred_processing_schedule : CatalogsFeedProcessingSchedule

    @[JSON::Field(key: "status", type: CatalogsStatus, nillable: false, emit_null: false)]
    property status : CatalogsStatus

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"CatalogsDbItem"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@created_at : Time, @id : String, @updated_at : Time, @catalog_type : CatalogsType, @credentials : CatalogsFeedCredentials, @default_availability : ProductAvailabilityType, @default_country : Country, @default_currency : NullableCurrency, @default_locale : String, @format : CatalogsFormat, @location : String, @name : String, @preferred_processing_schedule : CatalogsFeedProcessingSchedule, @status : CatalogsStatus)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          created_at == other.created_at &&
          id == other.id &&
          updated_at == other.updated_at &&
          catalog_type == other.catalog_type &&
          credentials == other.credentials &&
          default_availability == other.default_availability &&
          default_country == other.default_country &&
          default_currency == other.default_currency &&
          default_locale == other.default_locale &&
          format == other.format &&
          location == other.location &&
          name == other.name &&
          preferred_processing_schedule == other.preferred_processing_schedule &&
          status == other.status
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [created_at, id, updated_at, catalog_type, credentials, default_availability, default_country, default_currency, default_locale, format, location, name, preferred_processing_schedule, status].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["created_at"] = _to_h(created_at)
      hash["id"] = _to_h(id)
      hash["updated_at"] = _to_h(updated_at)
      hash["catalog_type"] = _to_h(catalog_type)
      hash["credentials"] = _to_h(credentials)
      hash["default_availability"] = _to_h(default_availability)
      hash["default_country"] = _to_h(default_country)
      hash["default_currency"] = _to_h(default_currency)
      hash["default_locale"] = _to_h(default_locale)
      hash["format"] = _to_h(format)
      hash["location"] = _to_h(location)
      hash["name"] = _to_h(name)
      hash["preferred_processing_schedule"] = _to_h(preferred_processing_schedule)
      hash["status"] = _to_h(status)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
