# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class CatalogsRetailProductGroup
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "catalog_type", type: String, nillable: false, emit_null: false)]
    property catalog_type : String

    # ID of the catalog product group.
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    @[JSON::Field(key: "filters", type: CatalogsProductGroupFilters, nillable: false, emit_null: false)]
    property filters : CatalogsProductGroupFilters

    # Catalog id pertaining to the retail product group.
    @[JSON::Field(key: "catalog_id", type: String, nillable: false, emit_null: false)]
    property catalog_id : String

    # id of the catalogs feed belonging to this catalog product group
    @[JSON::Field(key: "feed_id", type: String, nillable: false, emit_null: false)]
    property feed_id : String

    # Optional properties
    # Name of catalog product group
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # boolean indicator of whether the product group is being featured or not
    @[JSON::Field(key: "is_featured", type: Bool?, nillable: true, emit_null: false)]
    property is_featured : Bool?

    @[JSON::Field(key: "type", type: CatalogsProductGroupType?, nillable: true, emit_null: false)]
    property _type : CatalogsProductGroupType?

    @[JSON::Field(key: "status", type: CatalogsProductGroupStatus?, nillable: true, emit_null: false)]
    property status : CatalogsProductGroupStatus?

    # Unix timestamp in seconds of when catalog product group was created.
    @[JSON::Field(key: "created_at", type: Int32?, nillable: true, emit_null: false)]
    property created_at : Int32?

    # Unix timestamp in seconds of last time catalog product group was updated.
    @[JSON::Field(key: "updated_at", type: Int32?, nillable: true, emit_null: false)]
    property updated_at : Int32?

    @[JSON::Field(key: "country", type: String?, nillable: true, emit_null: false)]
    property country : String?

    @[JSON::Field(key: "locale", type: String?, nillable: true, emit_null: false)]
    property locale : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForCatalog_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "catalog_type"
        @allowable_values = ["RETAIL"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@catalog_type : String, @id : String, @filters : CatalogsProductGroupFilters, @catalog_id : String, @feed_id : String, @name : String? = nil, @description : String? = nil, @is_featured : Bool? = nil, @_type : CatalogsProductGroupType? = nil, @status : CatalogsProductGroupStatus? = nil, @created_at : Int32? = nil, @updated_at : Int32? = nil, @country : String? = nil, @locale : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      catalog_type_validator = EnumAttributeValidatorForCatalog_type.new
      if !catalog_type_validator.valid?(@catalog_type)
        message = catalog_type_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if @catalog_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"catalog_id\", must conform to the pattern #{pattern}.")
      end

      pattern = /^\d+$/
      if @feed_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"feed_id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      catalog_type_validator = EnumAttributeValidatorForCatalog_type.new
      return false unless catalog_type_validator.valid?(@catalog_type)
      return false if @id.try &.!~ /^\d+$/
      return false if @catalog_id.try &.!~ /^\d+$/
      return false if @feed_id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] catalog_type Object to be assigned
    def catalog_type=(catalog_type)
      validator = EnumAttributeValidatorForCatalog_type.new
      unless validator.valid?(catalog_type)
        raise ArgumentError.new(validator.message)
      end
      @catalog_type = catalog_type
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Custom attribute writer method with validation
    # @param [Object] catalog_id Value to be assigned
    def catalog_id=(catalog_id)
      pattern = /^\d+$/
      if catalog_id !~ pattern
        raise ArgumentError.new("invalid value for \"catalog_id\", must conform to the pattern #{pattern}.")
      end

      @catalog_id = catalog_id
    end

    # Custom attribute writer method with validation
    # @param [Object] feed_id Value to be assigned
    def feed_id=(feed_id)
      pattern = /^\d+$/
      if feed_id !~ pattern
        raise ArgumentError.new("invalid value for \"feed_id\", must conform to the pattern #{pattern}.")
      end

      @feed_id = feed_id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          catalog_type == other.catalog_type &&
          id == other.id &&
          name == other.name &&
          description == other.description &&
          filters == other.filters &&
          is_featured == other.is_featured &&
          _type == other._type &&
          status == other.status &&
          created_at == other.created_at &&
          updated_at == other.updated_at &&
          catalog_id == other.catalog_id &&
          feed_id == other.feed_id &&
          country == other.country &&
          locale == other.locale
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [catalog_type, id, name, description, filters, is_featured, _type, status, created_at, updated_at, catalog_id, feed_id, country, locale].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["catalog_type"] = _to_h(catalog_type)
      hash["id"] = _to_h(id)
      hash["name"] = _to_h(name)
      hash["description"] = _to_h(description)
      hash["filters"] = _to_h(filters)
      hash["is_featured"] = _to_h(is_featured)
      hash["type"] = _to_h(_type)
      hash["status"] = _to_h(status)
      hash["created_at"] = _to_h(created_at)
      hash["updated_at"] = _to_h(updated_at)
      hash["catalog_id"] = _to_h(catalog_id)
      hash["feed_id"] = _to_h(feed_id)
      hash["country"] = _to_h(country)
      hash["locale"] = _to_h(locale)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
