# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class TrendingKeyword
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "demographics", type: TrendingKeywordDemographics?, nillable: true, emit_null: false)]
    property demographics : TrendingKeywordDemographics?

    # Indicates whether the keyword has a prediction available for the next 90 days.<br /> This field is only applicable when `include_prediction` query parameter is set to `true`. <br /> By default, the value is `false` and no prediction data is included in the response.
    @[JSON::Field(key: "has_prediction", type: Bool?, nillable: true, emit_null: false)]
    property has_prediction : Bool?

    # The keyword that is trending.
    @[JSON::Field(key: "keyword", type: String?, nillable: true, emit_null: false)]
    property keyword : String?

    # The month-over-month percent change in search volume for this keyword.<br /> For example, a value of \"400\" would represent a 400% increase in searches in the last 30 days compared to the month prior.<br /> **Note**: growth rates are rounded, with a maximum of +/- 10000% change.  A value of 10001 indicates that this keyword experienced > 10000% month-over-month growth.
    @[JSON::Field(key: "pct_growth_mom", type: Int32?, nillable: true, emit_null: false)]
    property pct_growth_mom : Int32?

    # The week-over-week percent change in search volume for this keyword.<br /> For example, a value of \"50\" would represent a 50% increase in searches in the last seven days compared to the week prior.<br /> **Note**: growth rates are rounded, with a maximum of +/- 10000% change.  A value of 10001 indicates that this keyword experienced > 10000% week-over-week growth.
    @[JSON::Field(key: "pct_growth_wow", type: Int32?, nillable: true, emit_null: false)]
    property pct_growth_wow : Int32?

    # The year-over-year percent change in search volume for this keyword.<br /> For example, a value of \"-5\" would represent a 5% decrease in searches in the last 365 days compared to the month prior.<br /> **Note**: growth rates are rounded, with a maximum of +/- 10000% change.  A value of 10001 indicates that this keyword experienced > 10000% year-over-year growth.
    @[JSON::Field(key: "pct_growth_yoy", type: Int32?, nillable: true, emit_null: false)]
    property pct_growth_yoy : Int32?

    @[JSON::Field(key: "predicted_time_series", type: PredictedTimeSeries?, nillable: true, emit_null: false)]
    property predicted_time_series : PredictedTimeSeries?

    @[JSON::Field(key: "time_series", type: TimeSeries?, nillable: true, emit_null: false)]
    property time_series : TimeSeries?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@demographics : TrendingKeywordDemographics? = nil, @has_prediction : Bool? = nil, @keyword : String? = nil, @pct_growth_mom : Int32? = nil, @pct_growth_wow : Int32? = nil, @pct_growth_yoy : Int32? = nil, @predicted_time_series : PredictedTimeSeries? = nil, @time_series : TimeSeries? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@keyword.nil? && @keyword.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"keyword\", the character length must be greater than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@keyword.nil? && @keyword.try &.to_s.try &.size.try &.< 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] keyword Value to be assigned
    def keyword=(keyword)
      if !keyword.nil? && keyword.to_s.size < 1
        raise ArgumentError.new("invalid value for \"keyword\", the character length must be greater than or equal to 1.")
      end

      @keyword = keyword
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          demographics == other.demographics &&
          has_prediction == other.has_prediction &&
          keyword == other.keyword &&
          pct_growth_mom == other.pct_growth_mom &&
          pct_growth_wow == other.pct_growth_wow &&
          pct_growth_yoy == other.pct_growth_yoy &&
          predicted_time_series == other.predicted_time_series &&
          time_series == other.time_series
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [demographics, has_prediction, keyword, pct_growth_mom, pct_growth_wow, pct_growth_yoy, predicted_time_series, time_series].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["demographics"] = _to_h(demographics)
      hash["has_prediction"] = _to_h(has_prediction)
      hash["keyword"] = _to_h(keyword)
      hash["pct_growth_mom"] = _to_h(pct_growth_mom)
      hash["pct_growth_wow"] = _to_h(pct_growth_wow)
      hash["pct_growth_yoy"] = _to_h(pct_growth_yoy)
      hash["predicted_time_series"] = _to_h(predicted_time_series)
      hash["time_series"] = _to_h(time_series)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
