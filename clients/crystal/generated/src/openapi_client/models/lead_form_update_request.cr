# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class LeadFormUpdateRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The ID of this lead form to be updated
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # Optional properties
    # A message for people who complete the form to let them know what happens next.
    @[JSON::Field(key: "completion_message", type: String?, nillable: true, emit_null: false)]
    property completion_message : String?

    # Additional disclosure language to be included in the lead form.
    @[JSON::Field(key: "disclosure_language", type: String?, nillable: true, emit_null: false)]
    property disclosure_language : String?

    # Whether the advertiser has accepted Pinterest's terms of service for creating a lead ad.  By sending us TRUE for this parameter, you agree that (i) you will use any personal information received in compliance with the privacy policy you share with Pinterest, and (ii) you will comply with Pinterest's <a href=\"https://policy.pinterest.com/en/lead-ad-terms\">Lead Ad Terms</a>. As a reminder, all advertising on Pinterest is subject to the <a href=\"https://business.pinterest.com/en/pinterest-advertising-services-agreement/\">Pinterest Advertising Services Agreement</a> or an equivalent agreement as set forth on an IO
    @[JSON::Field(key: "has_accepted_terms", type: Bool?, nillable: true, emit_null: false)]
    property has_accepted_terms : Bool?

    # Internal name of the lead form.
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    # List of additional policy links to be displayed on the lead form.
    @[JSON::Field(key: "policy_links", type: Array(LeadFormCommonPolicyLinksInner)?, nillable: true, emit_null: false)]
    property policy_links : Array(LeadFormCommonPolicyLinksInner)?

    # A link to the advertiser's privacy policy. This will be included in the lead form's disclosure language.
    @[JSON::Field(key: "privacy_policy_link", type: String?, nillable: true, emit_null: false)]
    property privacy_policy_link : String?

    # List of questions to be displayed on the lead form.
    @[JSON::Field(key: "questions", type: Array(LeadFormQuestion)?, nillable: true, emit_null: false)]
    property questions : Array(LeadFormQuestion)?

    @[JSON::Field(key: "status", type: LeadFormStatus?, nillable: true, emit_null: false)]
    property status : LeadFormStatus?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"LeadFormCommon"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @completion_message : String? = nil, @disclosure_language : String? = nil, @has_accepted_terms : Bool? = nil, @name : String? = nil, @policy_links : Array(LeadFormCommonPolicyLinksInner)? = nil, @privacy_policy_link : String? = nil, @questions : Array(LeadFormQuestion)? = nil, @status : LeadFormStatus? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@policy_links.nil? && @policy_links.try &.size.try &.> 3
        invalid_properties.push("invalid value for \"policy_links\", number of items must be less than or equal to 3."
      end

      if !@policy_links.nil? && @policy_links.try &.size.try &.< 0
        invalid_properties.push("invalid value for \"policy_links\", number of items must be greater than or equal to 0."
      end

      if !@questions.nil? && @questions.try &.size.try &.> 10
        invalid_properties.push("invalid value for \"questions\", number of items must be less than or equal to 10."
      end

      if !@questions.nil? && @questions.try &.size.try &.< 0
        invalid_properties.push("invalid value for \"questions\", number of items must be greater than or equal to 0."
      end

      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@policy_links.nil? && @policy_links.try &.size.try &.> 3
      return false if !@policy_links.nil? && @policy_links.try &.size.try &.< 0
      return false if !@questions.nil? && @questions.try &.size.try &.> 10
      return false if !@questions.nil? && @questions.try &.size.try &.< 0
      return false if @id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] policy_links Value to be assigned
    def policy_links=(policy_links)
      if !policy_links.nil? && policy_links.size > 3
        raise ArgumentError.new("invalid value for \"policy_links\", number of items must be less than or equal to 3.")
      end

      if !policy_links.nil? && policy_links.size < 0
        raise ArgumentError.new("invalid value for \"policy_links\", number of items must be greater than or equal to 0.")
      end

      @policy_links = policy_links
    end

    # Custom attribute writer method with validation
    # @param [Object] questions Value to be assigned
    def questions=(questions)
      if !questions.nil? && questions.size > 10
        raise ArgumentError.new("invalid value for \"questions\", number of items must be less than or equal to 10.")
      end

      if !questions.nil? && questions.size < 0
        raise ArgumentError.new("invalid value for \"questions\", number of items must be greater than or equal to 0.")
      end

      @questions = questions
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          completion_message == other.completion_message &&
          disclosure_language == other.disclosure_language &&
          has_accepted_terms == other.has_accepted_terms &&
          name == other.name &&
          policy_links == other.policy_links &&
          privacy_policy_link == other.privacy_policy_link &&
          questions == other.questions &&
          status == other.status &&
          id == other.id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [completion_message, disclosure_language, has_accepted_terms, name, policy_links, privacy_policy_link, questions, status, id].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["completion_message"] = _to_h(completion_message)
      hash["disclosure_language"] = _to_h(disclosure_language)
      hash["has_accepted_terms"] = _to_h(has_accepted_terms)
      hash["name"] = _to_h(name)
      hash["policy_links"] = _to_h(policy_links)
      hash["privacy_policy_link"] = _to_h(privacy_policy_link)
      hash["questions"] = _to_h(questions)
      hash["status"] = _to_h(status)
      hash["id"] = _to_h(id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
