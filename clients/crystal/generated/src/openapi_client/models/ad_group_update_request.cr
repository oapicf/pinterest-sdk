# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class AdGroupUpdateRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Ad group ID.
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # Optional properties
    # Enable auto-targeting for ad group. Default value is True. Also known as <a href=\"https://help.pinterest.com/en/business/article/performance-plus-targeting\" target=\"_blank\">\"Pinterest Performance+ targeting\"</a>.
    @[JSON::Field(key: "auto_targeting_enabled", type: Bool?, nillable: true, emit_null: false)]
    property auto_targeting_enabled : Bool?

    # Bid price in micro currency. This field is **REQUIRED** for the following campaign objective_type/billable_event combinations: AWARENESS/IMPRESSION, CONSIDERATION/CLICKTHROUGH, CATALOG_SALES/CLICKTHROUGH.
    @[JSON::Field(key: "bid_in_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property bid_in_micro_currency : Int32?

    # Bid strategy type. For Campaigns with Video Completion objectives, the only supported bid strategy type is AUTOMATIC_BID, also known as \"Pinterest Performance+ bidding\".
    @[JSON::Field(key: "bid_strategy_type", type: String?, nillable: true, emit_null: false)]
    property bid_strategy_type : String?

    @[JSON::Field(key: "billable_event", type: ActionType?, nillable: true, emit_null: false)]
    property billable_event : ActionType?

    # Budget in micro currency. This field is **REQUIRED** for non-CBO (campaign budget optimization) campaigns.  A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. A CBO campaign is limited to 70 or less ad groups.
    @[JSON::Field(key: "budget_in_micro_currency", type: Int32?, nillable: true, emit_null: false)]
    property budget_in_micro_currency : Int32?

    @[JSON::Field(key: "budget_type", type: BudgetType?, nillable: true, emit_null: false)]
    property budget_type : BudgetType?

    # Campaign ID of the ad group.
    @[JSON::Field(key: "campaign_id", type: String?, nillable: true, emit_null: false)]
    property campaign_id : String?

    # Timestamp in Unix format for scheduling when ads in the ad group stop appearing. If not specified, ads run indefinitely unless you update the ad group by changing their status to `paused`. Cannot occur after `end_time` for parent campaign (if specified). Learn about <a href=\"/docs/api-features/managing-ads/#step-2-create-an-ad-group\" target=\"blank\">scheduling ads</a>. For certain organizations (<a href=\"/docs/getting-started/using-beta-and-restricted-features/\" target=\"blank\" target=\"blank\">Closed beta</a>): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO.
    @[JSON::Field(key: "end_time", type: Int32?, nillable: true, emit_null: false)]
    property end_time : Int32?

    # Enable creative optimization for the ad group, default value is FALSE. When enabled, you allow Pinterest to automatically turn your product Pins into ads in different formats (collections and shopping) and deliver those ads to users at scale.
    @[JSON::Field(key: "is_creative_optimization", type: Bool?, nillable: true, emit_null: false)]
    property is_creative_optimization : Bool?

    # Set a limit to the number of times a promoted pin from this campaign can be impressed by a pinner within the past rolling 30 days. Only available for CPM (cost per mille (1000 impressions))  ad groups. A CPM ad group has an IMPRESSION <a href=\"/docs/redoc/#section/Billable-event\">billable_event</a> value. This field **REQUIRES** the `end_time` field.
    @[JSON::Field(key: "lifetime_frequency_cap", type: Int32?, nillable: true, emit_null: false)]
    property lifetime_frequency_cap : Int32?

    # Ad group name.
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    # Optimization goals for objective-based performance campaigns. **REQUIRED** when campaign's `objective_type` is set to `\"WEB_CONVERSION\"`.
    @[JSON::Field(key: "optimization_goal_metadata", type: OptimizationGoalMetadata?, nillable: true, emit_null: false)]
    property optimization_goal_metadata : OptimizationGoalMetadata?

    @[JSON::Field(key: "pacing_delivery_type", type: PacingDeliveryType?, nillable: true, emit_null: false)]
    property pacing_delivery_type : PacingDeliveryType?

    # <a href=\"/docs/redoc/#section/Placement-group\">Placement group</a>.
    @[JSON::Field(key: "placement_group", type: PlacementGroupType?, nillable: true, emit_null: false)]
    property placement_group : PlacementGroupType?

    # Specify if the promotion is applied at ad group or item level
    @[JSON::Field(key: "promotion_application_level", type: String?, nillable: true, emit_null: false)]
    property promotion_application_level : String?

    # Promotion ID. To clear this field, set to null.
    @[JSON::Field(key: "promotion_id", type: String?, default: "0", nillable: true, emit_null: false)]
    property promotion_id : String?

    # Timestamp in Unix format for scheduling when ads in the ad group start to appear. If not specified, ads appear during parent campaign's `start_time`. Cannot precede `start_time` for parent campaign (if specified). Learn about <a href=\"/docs/api-features/managing-ads/#step-2-create-an-ad-group\" target=\"blank\">scheduling ads</a>. For certain organizations (<a href=\"/docs/getting-started/using-beta-and-restricted-features/\" target=\"blank\" target=\"blank\">Closed beta</a>): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO.
    @[JSON::Field(key: "start_time", type: Int32?, nillable: true, emit_null: false)]
    property start_time : Int32?

    # Ad group/entity status.
    @[JSON::Field(key: "status", type: EntityStatus?, nillable: true, emit_null: false)]
    property status : EntityStatus?

    @[JSON::Field(key: "targeting_spec", type: TargetingSpec?, nillable: true, emit_null: false)]
    property targeting_spec : TargetingSpec?

    # Targeting template IDs applied to the ad group. We currently only support 1 targeting template per ad group. To use targeting templates, do not set any other targeting fields: targeting_spec, tracking_urls, auto_targeting_enabled, placement_group. To clear all targeting template IDs, set this field to ['0'].
    @[JSON::Field(key: "targeting_template_ids", type: Array(String)?, nillable: true, emit_null: false)]
    property targeting_template_ids : Array(String)?

    # Third-party tracking URLs.<br> JSON object with the format: {\"<a href=\"/docs/redoc/#section/Tracking-URL-event\">Tracking event enum</a>\":[URL string array],...}<br> For example: {\"impression\": [\"URL1\", \"URL2\"], \"click\": [\"URL1\", \"URL2\", \"URL3\"]}.<br>Up to three tracking URLs are supported for each event type. Tracking URLs set at the ad group or ad level can override those set at the campaign level. May be null. Pass in an empty object - {} - to remove tracking URLs.<br><br> For more information, see <a href=\"https://help.pinterest.com/en/business/article/third-party-and-dynamic-tracking\" target=\"_blank\">Third-party and dynamic tracking</a>.
    @[JSON::Field(key: "tracking_urls", type: TrackingUrls?, nillable: true, emit_null: false)]
    property tracking_urls : TrackingUrls?

    # <a href=\"/docs/getting-started/using-beta-and-restricted-features/\" target=\"blank>Open beta</a> Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Make sure the `bid_strategy` type for your ad group is set to `AUTOMATIC_BID`. - Not currently supported for <a href=\"/docs/api-features/pinterest-performance-plus-setup/\" target=\"blank\">Pinterest Performance+ campaigns</a>.
    @[JSON::Field(key: "bid_multiplier", type: Float64?, nillable: true, emit_null: false)]
    property bid_multiplier : Float64?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForBid_strategy_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "bid_strategy_type"
        @allowable_values = ["AUTOMATIC_BID", "MAX_BID", "TARGET_AVG"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForPromotion_application_level < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "promotion_application_level"
        @allowable_values = ["NONE", "ITEM", "AD_GROUP"].map { |value| to(String, value)}
      end
    end


    # List of class defined in allOf (OpenAPI v3)
    def self.openapi_all_of
      [
      :"AdGroupCommon"
      ]
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @auto_targeting_enabled : Bool? = nil, @bid_in_micro_currency : Int32? = nil, @bid_strategy_type : String? = nil, @billable_event : ActionType? = nil, @budget_in_micro_currency : Int32? = nil, @budget_type : BudgetType? = nil, @campaign_id : String? = nil, @end_time : Int32? = nil, @is_creative_optimization : Bool? = nil, @lifetime_frequency_cap : Int32? = nil, @name : String? = nil, @optimization_goal_metadata : OptimizationGoalMetadata? = nil, @pacing_delivery_type : PacingDeliveryType? = nil, @placement_group : PlacementGroupType? = nil, @promotion_application_level : String? = nil, @promotion_id : String? = nil, @start_time : Int32? = nil, @status : EntityStatus? = nil, @targeting_spec : TargetingSpec? = nil, @targeting_template_ids : Array(String)? = nil, @tracking_urls : TrackingUrls? = nil, @bid_multiplier : Float64? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      bid_strategy_type_validator = EnumAttributeValidatorForBid_strategy_type.new
      if !bid_strategy_type_validator.valid?(@bid_strategy_type)
        message = bid_strategy_type_validator.message
        invalid_properties.push(message)
      end

      pattern = /^[C]?\d+$/
      if !@campaign_id.nil? && @campaign_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"campaign_id\", must conform to the pattern #{pattern}.")
      end

      promotion_application_level_validator = EnumAttributeValidatorForPromotion_application_level.new
      if !promotion_application_level_validator.valid?(@promotion_application_level)
        message = promotion_application_level_validator.message
        invalid_properties.push(message)
      end

      pattern = /^\d+$/
      if !@promotion_id.nil? && @promotion_id.try &.!~ pattern
        invalid_properties.push("invalid value for \"promotion_id\", must conform to the pattern #{pattern}.")
      end

      if !@targeting_template_ids.nil? && @targeting_template_ids.try &.size.try &.> 1
        invalid_properties.push("invalid value for \"targeting_template_ids\", number of items must be less than or equal to 1."
      end

      if !@bid_multiplier.nil? && @bid_multiplier.try &.> 10
        invalid_properties.push("invalid value for \"bid_multiplier\", must be smaller than or equal to 10.")
      end

      if !@bid_multiplier.nil? && @bid_multiplier.try &.< 0
        invalid_properties.push("invalid value for \"bid_multiplier\", must be greater than or equal to 0.")
      end

      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      bid_strategy_type_validator = EnumAttributeValidatorForBid_strategy_type.new
      return false unless bid_strategy_type_validator.valid?(@bid_strategy_type)
      return false if !@campaign_id.nil? && @campaign_id.try &.!~ /^[C]?\d+$/
      promotion_application_level_validator = EnumAttributeValidatorForPromotion_application_level.new
      return false unless promotion_application_level_validator.valid?(@promotion_application_level)
      return false if !@promotion_id.nil? && @promotion_id.try &.!~ /^\d+$/
      return false if !@targeting_template_ids.nil? && @targeting_template_ids.try &.size.try &.> 1
      return false if !@bid_multiplier.nil? && @bid_multiplier.try &.> 10
      return false if !@bid_multiplier.nil? && @bid_multiplier.try &.< 0
      return false if @id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] bid_strategy_type Object to be assigned
    def bid_strategy_type=(bid_strategy_type)
      validator = EnumAttributeValidatorForBid_strategy_type.new
      unless validator.valid?(bid_strategy_type)
        raise ArgumentError.new(validator.message)
      end
      @bid_strategy_type = bid_strategy_type
    end

    # Custom attribute writer method with validation
    # @param [Object] campaign_id Value to be assigned
    def campaign_id=(campaign_id)
      pattern = /^[C]?\d+$/
      if !campaign_id.nil? && campaign_id !~ pattern
        raise ArgumentError.new("invalid value for \"campaign_id\", must conform to the pattern #{pattern}.")
      end

      @campaign_id = campaign_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] promotion_application_level Object to be assigned
    def promotion_application_level=(promotion_application_level)
      validator = EnumAttributeValidatorForPromotion_application_level.new
      unless validator.valid?(promotion_application_level)
        raise ArgumentError.new(validator.message)
      end
      @promotion_application_level = promotion_application_level
    end

    # Custom attribute writer method with validation
    # @param [Object] promotion_id Value to be assigned
    def promotion_id=(promotion_id)
      pattern = /^\d+$/
      if !promotion_id.nil? && promotion_id !~ pattern
        raise ArgumentError.new("invalid value for \"promotion_id\", must conform to the pattern #{pattern}.")
      end

      @promotion_id = promotion_id
    end

    # Custom attribute writer method with validation
    # @param [Object] targeting_template_ids Value to be assigned
    def targeting_template_ids=(targeting_template_ids)
      if !targeting_template_ids.nil? && targeting_template_ids.size > 1
        raise ArgumentError.new("invalid value for \"targeting_template_ids\", number of items must be less than or equal to 1.")
      end

      @targeting_template_ids = targeting_template_ids
    end

    # Custom attribute writer method with validation
    # @param [Object] bid_multiplier Value to be assigned
    def bid_multiplier=(bid_multiplier)
      if !bid_multiplier.nil? && bid_multiplier > 10
        raise ArgumentError.new("invalid value for \"bid_multiplier\", must be smaller than or equal to 10.")
      end

      if !bid_multiplier.nil? && bid_multiplier < 0
        raise ArgumentError.new("invalid value for \"bid_multiplier\", must be greater than or equal to 0.")
      end

      @bid_multiplier = bid_multiplier
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          auto_targeting_enabled == other.auto_targeting_enabled &&
          bid_in_micro_currency == other.bid_in_micro_currency &&
          bid_strategy_type == other.bid_strategy_type &&
          billable_event == other.billable_event &&
          budget_in_micro_currency == other.budget_in_micro_currency &&
          budget_type == other.budget_type &&
          campaign_id == other.campaign_id &&
          end_time == other.end_time &&
          is_creative_optimization == other.is_creative_optimization &&
          lifetime_frequency_cap == other.lifetime_frequency_cap &&
          name == other.name &&
          optimization_goal_metadata == other.optimization_goal_metadata &&
          pacing_delivery_type == other.pacing_delivery_type &&
          placement_group == other.placement_group &&
          promotion_application_level == other.promotion_application_level &&
          promotion_id == other.promotion_id &&
          start_time == other.start_time &&
          status == other.status &&
          targeting_spec == other.targeting_spec &&
          targeting_template_ids == other.targeting_template_ids &&
          tracking_urls == other.tracking_urls &&
          bid_multiplier == other.bid_multiplier &&
          id == other.id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [auto_targeting_enabled, bid_in_micro_currency, bid_strategy_type, billable_event, budget_in_micro_currency, budget_type, campaign_id, end_time, is_creative_optimization, lifetime_frequency_cap, name, optimization_goal_metadata, pacing_delivery_type, placement_group, promotion_application_level, promotion_id, start_time, status, targeting_spec, targeting_template_ids, tracking_urls, bid_multiplier, id].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["auto_targeting_enabled"] = _to_h(auto_targeting_enabled)
      hash["bid_in_micro_currency"] = _to_h(bid_in_micro_currency)
      hash["bid_strategy_type"] = _to_h(bid_strategy_type)
      hash["billable_event"] = _to_h(billable_event)
      hash["budget_in_micro_currency"] = _to_h(budget_in_micro_currency)
      hash["budget_type"] = _to_h(budget_type)
      hash["campaign_id"] = _to_h(campaign_id)
      hash["end_time"] = _to_h(end_time)
      hash["is_creative_optimization"] = _to_h(is_creative_optimization)
      hash["lifetime_frequency_cap"] = _to_h(lifetime_frequency_cap)
      hash["name"] = _to_h(name)
      hash["optimization_goal_metadata"] = _to_h(optimization_goal_metadata)
      hash["pacing_delivery_type"] = _to_h(pacing_delivery_type)
      hash["placement_group"] = _to_h(placement_group)
      hash["promotion_application_level"] = _to_h(promotion_application_level)
      hash["promotion_id"] = _to_h(promotion_id)
      hash["start_time"] = _to_h(start_time)
      hash["status"] = _to_h(status)
      hash["targeting_spec"] = _to_h(targeting_spec)
      hash["targeting_template_ids"] = _to_h(targeting_template_ids)
      hash["tracking_urls"] = _to_h(tracking_urls)
      hash["bid_multiplier"] = _to_h(bid_multiplier)
      hash["id"] = _to_h(id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
