# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.23.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class LocalStoreUpdate
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # The ID of the local store.
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # Optional properties
    # Primary address line of the store.
    @[JSON::Field(key: "address_primary", type: String?, nillable: true, emit_null: false)]
    property address_primary : String?

    # Secondary address line of the store.
    @[JSON::Field(key: "address_secondary", type: String?, nillable: true, emit_null: false)]
    property address_secondary : String?

    # City where the store is located.
    @[JSON::Field(key: "city", type: String?, nillable: true, emit_null: false)]
    property city : String?

    # Country code where the store is located.
    @[JSON::Field(key: "country", type: Country?, nillable: true, emit_null: false)]
    property country : Country?

    # Geographic latitude coordinate of the store.
    @[JSON::Field(key: "latitude", type: Float32?, nillable: true, emit_null: false)]
    property latitude : Float32?

    # Geographic longitude coordinate of the store.
    @[JSON::Field(key: "longitude", type: Float32?, nillable: true, emit_null: false)]
    property longitude : Float32?

    # The name of the local store.
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    # Postal or ZIP code of the store.
    @[JSON::Field(key: "postal_code", type: String?, nillable: true, emit_null: false)]
    property postal_code : String?

    # State or region code where the store is located.
    @[JSON::Field(key: "region", type: String?, nillable: true, emit_null: false)]
    property region : String?

    # Merchant provided code for the local store. Unique within the merchant's catalog.
    @[JSON::Field(key: "store_code", type: String?, nillable: true, emit_null: false)]
    property store_code : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @address_primary : String? = nil, @address_secondary : String? = nil, @city : String? = nil, @country : Country? = nil, @latitude : Float32? = nil, @longitude : Float32? = nil, @name : String? = nil, @postal_code : String? = nil, @region : String? = nil, @store_code : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d+$/
      if @id.try &.!~ pattern
        invalid_properties.push("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @id.try &.!~ /^\d+$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      pattern = /^\d+$/
      if id !~ pattern
        raise ArgumentError.new("invalid value for \"id\", must conform to the pattern #{pattern}.")
      end

      @id = id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          address_primary == other.address_primary &&
          address_secondary == other.address_secondary &&
          city == other.city &&
          country == other.country &&
          id == other.id &&
          latitude == other.latitude &&
          longitude == other.longitude &&
          name == other.name &&
          postal_code == other.postal_code &&
          region == other.region &&
          store_code == other.store_code
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [address_primary, address_secondary, city, country, id, latitude, longitude, name, postal_code, region, store_code].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["address_primary"] = _to_h(address_primary)
      hash["address_secondary"] = _to_h(address_secondary)
      hash["city"] = _to_h(city)
      hash["country"] = _to_h(country)
      hash["id"] = _to_h(id)
      hash["latitude"] = _to_h(latitude)
      hash["longitude"] = _to_h(longitude)
      hash["name"] = _to_h(name)
      hash["postal_code"] = _to_h(postal_code)
      hash["region"] = _to_h(region)
      hash["store_code"] = _to_h(store_code)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
