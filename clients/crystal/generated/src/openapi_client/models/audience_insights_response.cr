# #Pinterest REST API
#
##Pinterest's REST API
#
#The version of the OpenAPI document: 5.14.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  # Audience interests and demographics.
  class AudienceInsightsResponse
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Category interest distribution
    @[JSON::Field(key: "categories", type: Array(AudienceCategory)?, nillable: true, emit_null: false)]
    property categories : Array(AudienceCategory)?

    @[JSON::Field(key: "demographics", type: AudienceDemographics?, nillable: true, emit_null: false)]
    property demographics : AudienceDemographics?

    @[JSON::Field(key: "type", type: AudienceInsightType?, default: "YOUR_TOTAL_AUDIENCE", nillable: true, emit_null: false)]
    property _type : AudienceInsightType?

    # Generation date
    @[JSON::Field(key: "date", type: String?, nillable: true, emit_null: false)]
    property date : String?

    # Population count.
    @[JSON::Field(key: "size", type: Int32?, nillable: true, emit_null: false)]
    property size : Int32?

    # Indicates whether the audience size has been rounded up to the next highest upper boundary.
    @[JSON::Field(key: "size_is_upper_bound", type: Bool?, nillable: true, emit_null: false)]
    property size_is_upper_bound : Bool?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@categories : Array(AudienceCategory)? = nil, @demographics : AudienceDemographics? = nil, @_type : AudienceInsightType? = nil, @date : String? = nil, @size : Int32? = nil, @size_is_upper_bound : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      pattern = /^\d{4}-\d{2}-\d{2}$/
      if !@date.nil? && @date.try &.!~ pattern
        invalid_properties.push("invalid value for \"date\", must conform to the pattern #{pattern}.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@date.nil? && @date.try &.!~ /^\d{4}-\d{2}-\d{2}$/
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] date Value to be assigned
    def date=(date)
      pattern = /^\d{4}-\d{2}-\d{2}$/
      if !date.nil? && date !~ pattern
        raise ArgumentError.new("invalid value for \"date\", must conform to the pattern #{pattern}.")
      end

      @date = date
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          categories == other.categories &&
          demographics == other.demographics &&
          _type == other._type &&
          date == other.date &&
          size == other.size &&
          size_is_upper_bound == other.size_is_upper_bound
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [categories, demographics, _type, date, size, size_is_upper_bound].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["categories"] = _to_h(categories)
      hash["demographics"] = _to_h(demographics)
      hash["type"] = _to_h(_type)
      hash["date"] = _to_h(date)
      hash["size"] = _to_h(size)
      hash["size_is_upper_bound"] = _to_h(size_is_upper_bound)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
