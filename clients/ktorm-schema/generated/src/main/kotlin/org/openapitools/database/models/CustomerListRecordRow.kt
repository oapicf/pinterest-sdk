/**
* Pinterest REST API
* Pinterest's REST API
*
* The version of the OpenAPI document: 5.28.0
* Contact: blah+oapicf@cliffano.com
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
package org.openapitools.database.models

import org.ktorm.dsl.*
import org.ktorm.schema.*
import org.ktorm.database.Database
import .*


/**
 * A single row in a multi-field customer list (v2 format).
 * @param email Email address (plain or hashed with SHA1, SHA256, or MD5).
 * @param externalId External ID identifier (not hashed).
 * @param hashedPhoneNumber Hashed phone number (hashed with SHA1, SHA256, or MD5).
 * @param hashedPinnerId Hashed pinner ID (hashed with SHA1, SHA256, or MD5).
 * @param ipAddress IP address (not hashed).
 * @param liverampEnvelope LiveRamp envelope identifier (Base64-encoded, not hashed).
 * @param maid Mobile advertising ID (plain or hashed with SHA1, SHA256, or MD5).
 * @param userAgent User agent string (not hashed).
 */
object CustomerListRecordRows : BaseTable<CustomerListRecordRow>("CustomerListRecordRow") {
    val email = text("email") /* null */ /* Email address (plain or hashed with SHA1, SHA256, or MD5). */
    val externalId = text("external_id") /* null */ /* External ID identifier (not hashed). */
    val hashedPhoneNumber = text("hashed_phone_number") /* null */ /* Hashed phone number (hashed with SHA1, SHA256, or MD5). */
    val hashedPinnerId = text("hashed_pinner_id") /* null */ /* Hashed pinner ID (hashed with SHA1, SHA256, or MD5). */
    val ipAddress = text("ip_address") /* null */ /* IP address (not hashed). */
    val liverampEnvelope = text("liveramp_envelope") /* null */ /* LiveRamp envelope identifier (Base64-encoded, not hashed). */
    val maid = text("maid") /* null */ /* Mobile advertising ID (plain or hashed with SHA1, SHA256, or MD5). */
    val userAgent = text("user_agent") /* null */ /* User agent string (not hashed). */

    /**
     * Create an entity of type CustomerListRecordRow from the model
     */
    override fun doCreateEntity(row: QueryRowSet, withReferences: Boolean) = CustomerListRecordRow(
        email = row[email]  /* kotlin.String? */ /* Email address (plain or hashed with SHA1, SHA256, or MD5). */,
        externalId = row[externalId]  /* kotlin.String? */ /* External ID identifier (not hashed). */,
        hashedPhoneNumber = row[hashedPhoneNumber]  /* kotlin.String? */ /* Hashed phone number (hashed with SHA1, SHA256, or MD5). */,
        hashedPinnerId = row[hashedPinnerId]  /* kotlin.String? */ /* Hashed pinner ID (hashed with SHA1, SHA256, or MD5). */,
        ipAddress = row[ipAddress]  /* kotlin.String? */ /* IP address (not hashed). */,
        liverampEnvelope = row[liverampEnvelope]  /* kotlin.String? */ /* LiveRamp envelope identifier (Base64-encoded, not hashed). */,
        maid = row[maid]  /* kotlin.String? */ /* Mobile advertising ID (plain or hashed with SHA1, SHA256, or MD5). */,
        userAgent = row[userAgent]  /* kotlin.String? */ /* User agent string (not hashed). */
    )

    /**
    * Assign all the columns from the entity of type CustomerListRecordRow to the DML expression.
    *
    * Usage:
    *
    * ```kotlin
    * let entity = CustomerListRecordRow()
    * database.update(CustomerListRecordRows, {
    *     assignFrom(entity)
    * })
    * ```
    * @return the builder with the columns for the update or insert.
    */
    fun AssignmentsBuilder.assignFrom(entity: CustomerListRecordRow) {
        this.apply {
            set(CustomerListRecordRows.email, entity.email)
            set(CustomerListRecordRows.externalId, entity.externalId)
            set(CustomerListRecordRows.hashedPhoneNumber, entity.hashedPhoneNumber)
            set(CustomerListRecordRows.hashedPinnerId, entity.hashedPinnerId)
            set(CustomerListRecordRows.ipAddress, entity.ipAddress)
            set(CustomerListRecordRows.liverampEnvelope, entity.liverampEnvelope)
            set(CustomerListRecordRows.maid, entity.maid)
            set(CustomerListRecordRows.userAgent, entity.userAgent)
        }
    }

}


