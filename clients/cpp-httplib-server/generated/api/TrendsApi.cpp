/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "TrendsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_TRENDS_EDITORIAL = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_FEATURED_TREND = 200;
constexpr int HTTP_RESPONSE_CODE_PRODUCT_CATEGORY_DETAILS = 200;
constexpr int HTTP_RESPONSE_CODE_TRENDING_PRODUCT_CATEGORY = 200;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool Trends::parseTrendseditorialArticlesGetParams(const httplib::Request& req, Trends::TrendseditorialArticlesGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - region
    if (req.has_param("region"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("region", 0);
            params.m_region = nlohmann::json::parse(val).get<ProductCategoryRegion>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'region': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Trends::handleTrendseditorialArticlesGetResponse(const TrendseditorialArticlesGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::TrendsEditorial>)
        {
            res.status = HTTP_RESPONSE_CODE_TRENDS_EDITORIAL;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Trends::parseTrendstopicsfeaturedGetParams(const httplib::Request& req, Trends::TrendstopicsfeaturedGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - interest
    if (req.has_param("interest"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("interest", 0);
            params.m_interest = nlohmann::json::parse(val).get<models::InterestsEnum>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'interest': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_interest = std::make_shared<InterestsEnum>();
    }
    // Query Parameters - region
    if (req.has_param("region"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("region", 0);
            params.m_region = nlohmann::json::parse(val).get<ProductCategoryRegion>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'region': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Trends::handleTrendstopicsfeaturedGetResponse(const TrendstopicsfeaturedGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::FeaturedTrend>)
        {
            res.status = HTTP_RESPONSE_CODE_FEATURED_TREND;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Trends::parseTrendsproductCategoriesdetailsGetParams(const httplib::Request& req, Trends::TrendsproductCategoriesdetailsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - product_categories
    if (req.has_param("product_categories"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("product_categories");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("product_categories", i);

                    params.m_productCategories.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("product_categories", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_productCategories.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'product_categories': " + std::string(e.what()));
        }
    }
    // Query Parameters - region
    if (req.has_param("region"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("region", 0);
            params.m_region = nlohmann::json::parse(val).get<ProductCategoryRegion>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'region': " + std::string(e.what()));
        }
    }
    // Query Parameters - lookback_window
    if (req.has_param("lookback_window"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("lookback_window", 0);
            params.m_lookbackWindow = nlohmann::json::parse(val).get<models::ProductCategoryDetailLookbackWindow>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'lookback_window': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_lookbackWindow = std::make_shared<ProductCategoryDetailLookbackWindow>();
    }
    // Query Parameters - engagement_type
    if (req.has_param("engagement_type"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("engagement_type", 0);
            params.m_engagementType = nlohmann::json::parse(val).get<models::ProductCategoriesEngagementType>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'engagement_type': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_engagementType = std::make_shared<ProductCategoriesEngagementType>();
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Trends::handleTrendsproductCategoriesdetailsGetResponse(const TrendsproductCategoriesdetailsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductCategoryDetails>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_CATEGORY_DETAILS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Trends::parseTrendsproductCategoriestrendingGetParams(const httplib::Request& req, Trends::TrendsproductCategoriestrendingGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - region
    if (req.has_param("region"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("region", 0);
            params.m_region = nlohmann::json::parse(val).get<ProductCategoryRegion>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'region': " + std::string(e.what()));
        }
    }
    // Query Parameters - verticals
    if (req.has_param("verticals"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("verticals");
            if (!params.m_verticals.has_value())
            {
                params.m_verticals = std::vector<VerticalProductCategory>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("verticals", i);

                    params.m_verticals->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("verticals", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_verticals->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'verticals': " + std::string(e.what()));
        }
    }
    // Query Parameters - ages
    if (req.has_param("ages"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("ages");
            if (!params.m_ages.has_value())
            {
                params.m_ages = std::vector<AgeTrendsBucket>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("ages", i);

                    params.m_ages->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("ages", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_ages->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'ages': " + std::string(e.what()));
        }
    }
    // Query Parameters - genders
    if (req.has_param("genders"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("genders");
            if (!params.m_genders.has_value())
            {
                params.m_genders = std::vector<GenderBucket>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("genders", i);

                    params.m_genders->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("genders", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_genders->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'genders': " + std::string(e.what()));
        }
    }
    // Query Parameters - engagement_type
    if (req.has_param("engagement_type"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("engagement_type", 0);
            params.m_engagementType = nlohmann::json::parse(val).get<models::ProductCategoriesEngagementType>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'engagement_type': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_engagementType = std::make_shared<ProductCategoriesEngagementType>();
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Trends::handleTrendsproductCategoriestrendingGetResponse(const TrendsproductCategoriestrendingGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::TrendingProductCategory>)
        {
            res.status = HTTP_RESPONSE_CODE_TRENDING_PRODUCT_CATEGORY;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool Trends::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "user_accounts:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "user_accounts:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void Trends::handleTrendseditorialArticlesGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        TrendseditorialArticlesGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseTrendseditorialArticlesGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForTrendseditorialArticles(params);
        handleTrendseditorialArticlesGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Trends::handleTrendstopicsfeaturedGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        TrendstopicsfeaturedGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseTrendstopicsfeaturedGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForTrendstopicsfeatured(params);
        handleTrendstopicsfeaturedGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Trends::handleTrendsproductCategoriesdetailsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        TrendsproductCategoriesdetailsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseTrendsproductCategoriesdetailsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForTrendsproductCategoriesdetails(params);
        handleTrendsproductCategoriesdetailsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Trends::handleTrendsproductCategoriestrendingGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        TrendsproductCategoriestrendingGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseTrendsproductCategoriestrendingGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForTrendsproductCategoriestrending(params);
        handleTrendsproductCategoriestrendingGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Trends::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/trends/editorial_articles", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleTrendseditorialArticlesGetRequest(req, res, auth);
    });
    svr.Get("/trends/topics/featured", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleTrendstopicsfeaturedGetRequest(req, res, auth);
    });
    svr.Get("/trends/product_categories/details", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleTrendsproductCategoriesdetailsGetRequest(req, res, auth);
    });
    svr.Get("/trends/product_categories/trending", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleTrendsproductCategoriestrendingGetRequest(req, res, auth);
    });
}

} // namespace Api