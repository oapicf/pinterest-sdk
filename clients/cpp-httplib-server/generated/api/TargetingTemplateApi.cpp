/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "TargetingTemplateApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_TARGETING_TEMPLATE = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_TARGETING_TEMPLATE_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool TargetingTemplate::parseAdAccountsadAccountIdtargetingTemplatesPostParams(const httplib::Request& req, TargetingTemplate::AdAccountsadAccountIdtargetingTemplatesPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesPostResponse(const AdAccountsadAccountIdtargetingTemplatesPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::TargetingTemplate>)
        {
            res.status = HTTP_RESPONSE_CODE_TARGETING_TEMPLATE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool TargetingTemplate::parseAdAccountsadAccountIdtargetingTemplatesGetParams(const httplib::Request& req, TargetingTemplate::AdAccountsadAccountIdtargetingTemplatesGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }
    // Query Parameters - order
    if (req.has_param("order"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("order", 0);
            params.m_order = nlohmann::json::parse(val).get<models::PinterestLibPaginationOrder>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'order': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_order = std::make_shared<PinterestLibPaginationOrder>();
    }
    // Query Parameters - include_sizing
    if (req.has_param("include_sizing"))
    {
        try
        {
            params.m_includeSizing = (req.get_param_value("include_sizing") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'include_sizing': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_includeSizing = false;
    }
    // Query Parameters - search_query
    if (req.has_param("search_query"))
    {
        params.m_searchQuery = req.get_param_value("search_query");
    }
    else
    {
        // Use default value for optional parameter
        params.m_searchQuery = "";
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesGetResponse(const AdAccountsadAccountIdtargetingTemplatesGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::TargetingTemplateList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_TARGETING_TEMPLATE_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool TargetingTemplate::parseAdAccountsadAccountIdtargetingTemplatesPatchParams(const httplib::Request& req, TargetingTemplate::AdAccountsadAccountIdtargetingTemplatesPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesPatchResponse(const AdAccountsadAccountIdtargetingTemplatesPatchResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}

bool TargetingTemplate::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "ads:write" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdtargetingTemplatesPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdtargetingTemplatesPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdtargetingTemplates(params);
        handleAdAccountsadAccountIdtargetingTemplatesPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdtargetingTemplatesGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdtargetingTemplatesGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdtargetingTemplates(params);
        handleAdAccountsadAccountIdtargetingTemplatesGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void TargetingTemplate::handleAdAccountsadAccountIdtargetingTemplatesPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdtargetingTemplatesPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdtargetingTemplatesPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForAdAccountsadAccountIdtargetingTemplates(params);
        handleAdAccountsadAccountIdtargetingTemplatesPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void TargetingTemplate::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Post("/ad_accounts/{ad_account_id}/targeting_templates", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdtargetingTemplatesPostRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/targeting_templates", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdtargetingTemplatesGetRequest(req, res, auth);
    });
    svr.Patch("/ad_accounts/{ad_account_id}/targeting_templates", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdtargetingTemplatesPatchRequest(req, res, auth);
    });
}

} // namespace Api