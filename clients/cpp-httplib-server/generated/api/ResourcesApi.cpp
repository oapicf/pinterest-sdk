/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "ResourcesApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_AD_ACCOUNT_COUNTRIES_GET200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_DELIVERY_METRICS_GET200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_SINGLE_INTEREST_TARGETING_OPTION = 200;
constexpr int HTTP_RESPONSE_CODE_BOOK_CLOSED = 200;
constexpr int HTTP_RESPONSE_CODE_PRIMITIVE_OBJECT = 200;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

void Resources::handleResourcesadAccountCountriesGetResponse(const ResourcesadAccountCountriesGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdAccountCountriesGet200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_ACCOUNT_COUNTRIES_GET200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Resources::parseResourcesdeliveryMetricsGetParams(const httplib::Request& req, Resources::ResourcesdeliveryMetricsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - report_type
    if (req.has_param("report_type"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("report_type", 0);
            params.m_reportType = nlohmann::json::parse(val).get<models::ReportType>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'report_type': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_reportType = std::make_shared<ReportType>();
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Resources::handleResourcesdeliveryMetricsGetResponse(const ResourcesdeliveryMetricsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DeliveryMetricsGet200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_DELIVERY_METRICS_GET200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Resources::parseResourcestargetinginterestsinterestIdGetParams(const httplib::Request& req, Resources::ResourcestargetinginterestsinterestIdGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - interest_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'interest_id'");
    }
    else
    {
        try
        {
            params.m_interestId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'interest_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Resources::handleResourcestargetinginterestsinterestIdGetResponse(const ResourcestargetinginterestsinterestIdGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::SingleInterestTargetingOption>)
        {
            res.status = HTTP_RESPONSE_CODE_SINGLE_INTEREST_TARGETING_OPTION;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
void Resources::handleResourcesleadFormQuestionsGetResponse(const ResourcesleadFormQuestionsGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Resources::parseResourcesmetricsReadyStateGetParams(const httplib::Request& req, Resources::ResourcesmetricsReadyStateGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - date
    if (req.has_param("date"))
    {
        params.m_date = req.get_param_value("date");
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Resources::handleResourcesmetricsReadyStateGetResponse(const ResourcesmetricsReadyStateGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::BookClosed>)
        {
            res.status = HTTP_RESPONSE_CODE_BOOK_CLOSED;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Resources::parseResourcestargetingtargetingTypeGetParams(const httplib::Request& req, Resources::ResourcestargetingtargetingTypeGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - ad_account_id
    if (req.has_param("ad_account_id"))
    {
        params.m_adAccountId = req.get_param_value("ad_account_id");
    }
    else
    {
        // Use default value for optional parameter
        params.m_adAccountId = "";
    }
    // Query Parameters - client_id
    if (req.has_param("client_id"))
    {
        params.m_clientId = req.get_param_value("client_id");
    }
    else
    {
        // Use default value for optional parameter
        params.m_clientId = "";
    }
    // Query Parameters - oauth_signature
    if (req.has_param("oauth_signature"))
    {
        params.m_oauthSignature = req.get_param_value("oauth_signature");
    }
    else
    {
        // Use default value for optional parameter
        params.m_oauthSignature = "";
    }
    // Query Parameters - timestamp
    if (req.has_param("timestamp"))
    {
        params.m_timestamp = req.get_param_value("timestamp");
    }
    else
    {
        // Use default value for optional parameter
        params.m_timestamp = "";
    }

    // Path Parameters - targeting_type (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'targeting_type'");
    }
    else
    {
        try
        {
            // Parse JSON object from path param (rare)
            auto val = req.matches[1];
            params.m_targetingType = nlohmann::json::parse(val);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'targeting_type': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Resources::handleResourcestargetingtargetingTypeGetResponse(const ResourcestargetingtargetingTypeGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, nlohmann::json>)
        {
            res.status = HTTP_RESPONSE_CODE_PRIMITIVE_OBJECT;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool Resources::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void Resources::handleResourcesadAccountCountriesGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        auto result = handleGetForResourcesadAccountCountries();
        handleResourcesadAccountCountriesGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Resources::handleResourcesdeliveryMetricsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        ResourcesdeliveryMetricsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseResourcesdeliveryMetricsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForResourcesdeliveryMetrics(params);
        handleResourcesdeliveryMetricsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Resources::handleResourcestargetinginterestsinterestIdGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        ResourcestargetinginterestsinterestIdGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseResourcestargetinginterestsinterestIdGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForResourcestargetinginterestsinterestId(params);
        handleResourcestargetinginterestsinterestIdGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Resources::handleResourcesleadFormQuestionsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        auto result = handleGetForResourcesleadFormQuestions();
        handleResourcesleadFormQuestionsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Resources::handleResourcesmetricsReadyStateGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        ResourcesmetricsReadyStateGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseResourcesmetricsReadyStateGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForResourcesmetricsReadyState(params);
        handleResourcesmetricsReadyStateGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Resources::handleResourcestargetingtargetingTypeGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        ResourcestargetingtargetingTypeGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseResourcestargetingtargetingTypeGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForResourcestargetingtargetingType(params);
        handleResourcestargetingtargetingTypeGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Resources::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/resources/ad_account_countries", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcesadAccountCountriesGetRequest(req, res, auth);
    });
    svr.Get("/resources/delivery_metrics", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcesdeliveryMetricsGetRequest(req, res, auth);
    });
    svr.Get("/resources/targeting/interests/{interest_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcestargetinginterestsinterestIdGetRequest(req, res, auth);
    });
    svr.Get("/resources/lead_form_questions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcesleadFormQuestionsGetRequest(req, res, auth);
    });
    svr.Get("/resources/metrics_ready_state", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcesmetricsReadyStateGetRequest(req, res, auth);
    });
    svr.Get("/resources/targeting/{targeting_type}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleResourcestargetingtargetingTypeGetRequest(req, res, auth);
    });
}

} // namespace Api