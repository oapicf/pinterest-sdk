/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "LeadAdsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_LEAD_SUBSCRIPTION = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_AD_ACCOUNTS_SUBSCRIPTIONS_GET_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool LeadAds::parseAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteParams(const httplib::Request& req, LeadAds::AdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - subscription_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'subscription_id'");
    }
    else
    {
        try
        {
            params.m_subscriptionId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'subscription_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void LeadAds::handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteResponse(const AdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::LeadSubscription>)
        {
            res.status = HTTP_RESPONSE_CODE_LEAD_SUBSCRIPTION;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool LeadAds::parseAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetParams(const httplib::Request& req, LeadAds::AdAccountsadAccountIdleadssubscriptionssubscriptionIdGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - subscription_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'subscription_id'");
    }
    else
    {
        try
        {
            params.m_subscriptionId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'subscription_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void LeadAds::handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetResponse(const AdAccountsadAccountIdleadssubscriptionssubscriptionIdGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::LeadSubscription>)
        {
            res.status = HTTP_RESPONSE_CODE_LEAD_SUBSCRIPTION;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool LeadAds::parseAdAccountsadAccountIdleadssubscriptionsGetParams(const httplib::Request& req, LeadAds::AdAccountsadAccountIdleadssubscriptionsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void LeadAds::handleAdAccountsadAccountIdleadssubscriptionsGetResponse(const AdAccountsadAccountIdleadssubscriptionsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdAccountsSubscriptionsGetList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_ACCOUNTS_SUBSCRIPTIONS_GET_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool LeadAds::parseAdAccountsadAccountIdleadssubscriptionsPostParams(const httplib::Request& req, LeadAds::AdAccountsadAccountIdleadssubscriptionsPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void LeadAds::handleAdAccountsadAccountIdleadssubscriptionsPostResponse(const AdAccountsadAccountIdleadssubscriptionsPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::LeadSubscription>)
        {
            res.status = HTTP_RESPONSE_CODE_LEAD_SUBSCRIPTION;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool LeadAds::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "ads:write" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void LeadAds::handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleDeleteForAdAccountsadAccountIdleadssubscriptionssubscriptionId(params);
        handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void LeadAds::handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdleadssubscriptionssubscriptionIdGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdleadssubscriptionssubscriptionId(params);
        handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void LeadAds::handleAdAccountsadAccountIdleadssubscriptionsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdleadssubscriptionsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdleadssubscriptionsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdleadssubscriptions(params);
        handleAdAccountsadAccountIdleadssubscriptionsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void LeadAds::handleAdAccountsadAccountIdleadssubscriptionsPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdleadssubscriptionsPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdleadssubscriptionsPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdleadssubscriptions(params);
        handleAdAccountsadAccountIdleadssubscriptionsPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void LeadAds::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Delete("/ad_accounts/{ad_account_id}/leads/subscriptions/{subscription_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdDeleteRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/leads/subscriptions/{subscription_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdleadssubscriptionssubscriptionIdGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/leads/subscriptions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdleadssubscriptionsGetRequest(req, res, auth);
    });
    svr.Post("/ad_accounts/{ad_account_id}/leads/subscriptions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdleadssubscriptionsPostRequest(req, res, auth);
    });
}

} // namespace Api