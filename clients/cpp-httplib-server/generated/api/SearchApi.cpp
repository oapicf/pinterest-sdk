/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "SearchApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_SEARCH_PARTNER_PINS200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_SEARCH_USER_BOARDS_GET200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_SEARCH_USER_PINS_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool Search::parseSearchpartnerpinsGetParams(const httplib::Request& req, Search::SearchpartnerpinsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - term
    if (req.has_param("term"))
    {
        params.m_term = req.get_param_value("term");
    }
    // Query Parameters - country_code
    if (req.has_param("country_code"))
    {
        params.m_countryCode = req.get_param_value("country_code");
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - locale
    if (req.has_param("locale"))
    {
        params.m_locale = req.get_param_value("locale");
    }
    else
    {
        // Use default value for optional parameter
        params.m_locale = "";
    }
    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 10;
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Search::handleSearchpartnerpinsGetResponse(const SearchpartnerpinsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::SearchPartnerPins200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_SEARCH_PARTNER_PINS200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Search::parseSearchboardsGetParams(const httplib::Request& req, Search::SearchboardsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - ad_account_id
    if (req.has_param("ad_account_id"))
    {
        params.m_adAccountId = req.get_param_value("ad_account_id");
    }
    else
    {
        // Use default value for optional parameter
        params.m_adAccountId = "";
    }
    // Query Parameters - query
    if (req.has_param("query"))
    {
        params.m_query = req.get_param_value("query");
    }
    else
    {
        // Use default value for optional parameter
        params.m_query = "";
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Search::handleSearchboardsGetResponse(const SearchboardsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::SearchUserBoardsGet200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_SEARCH_USER_BOARDS_GET200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool Search::parseSearchpinsGetParams(const httplib::Request& req, Search::SearchpinsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - ad_account_id
    if (req.has_param("ad_account_id"))
    {
        params.m_adAccountId = req.get_param_value("ad_account_id");
    }
    else
    {
        // Use default value for optional parameter
        params.m_adAccountId = "";
    }
    // Query Parameters - query
    if (req.has_param("query"))
    {
        params.m_query = req.get_param_value("query");
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Search::handleSearchpinsGetResponse(const SearchpinsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::SearchUserPinsList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_SEARCH_USER_PINS_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool Search::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "boards:read", "pins:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "boards:read", "boards:read_secret" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void Search::handleSearchpartnerpinsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SearchpartnerpinsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSearchpartnerpinsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForSearchpartnerpins(params);
        handleSearchpartnerpinsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Search::handleSearchboardsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SearchboardsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSearchboardsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForSearchboards(params);
        handleSearchboardsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Search::handleSearchpinsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SearchpinsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSearchpinsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForSearchpins(params);
        handleSearchpinsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Search::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/search/partner/pins", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSearchpartnerpinsGetRequest(req, res, auth);
    });
    svr.Get("/search/boards", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSearchboardsGetRequest(req, res, auth);
    });
    svr.Get("/search/pins", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSearchpinsGetRequest(req, res, auth);
    });
}

} // namespace Api