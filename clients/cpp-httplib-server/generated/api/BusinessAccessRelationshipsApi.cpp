/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "BusinessAccessRelationshipsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_BRAND_ACCOUNT = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_DELETE_BUSINESS_MEMBERSHIP200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_DELETE_BUSINESS_PARTNERS = 200;
constexpr int HTTP_RESPONSE_CODE_GET_BUSINESS_EMPLOYERS200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_GET_BUSINESS_MEMBERS200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_GET_BUSINESS_PARTNERS200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_UPDATE_BUSINESS_MEMBERSHIPS_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool BusinessAccessRelationships::parseBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostParams(const httplib::Request& req, BusinessAccessRelationships::BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - business_hierarchy_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_hierarchy_id'");
    }
    else
    {
        try
        {
            params.m_businessHierarchyId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_hierarchy_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostResponse(const BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::BrandAccount>)
        {
            res.status = HTTP_RESPONSE_CODE_BRAND_ACCOUNT;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchParams(const httplib::Request& req, BusinessAccessRelationships::BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - brand_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'brand_account_id'");
    }
    else
    {
        try
        {
            params.m_brandAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'brand_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - business_hierarchy_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'business_hierarchy_id'");
    }
    else
    {
        try
        {
            params.m_businessHierarchyId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_hierarchy_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchResponse(const BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::BrandAccount>)
        {
            res.status = HTTP_RESPONSE_CODE_BRAND_ACCOUNT;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdmembersDeleteParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdmembersDeleteRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdmembersDeleteResponse(const BusinessesbusinessIdmembersDeleteResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DeleteBusinessMembership200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_DELETE_BUSINESS_MEMBERSHIP200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdpartnersDeleteParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdpartnersDeleteRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdpartnersDeleteResponse(const BusinessesbusinessIdpartnersDeleteResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DeleteBusinessPartners>)
        {
            res.status = HTTP_RESPONSE_CODE_DELETE_BUSINESS_PARTNERS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesemployersGetParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesemployersGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - assets_summary
    if (req.has_param("assets_summary"))
    {
        try
        {
            params.m_assetsSummary = (req.get_param_value("assets_summary") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'assets_summary': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_assetsSummary = true;
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesemployersGetResponse(const BusinessesemployersGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::GetBusinessEmployers200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_GET_BUSINESS_EMPLOYERS200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdmembersGetParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdmembersGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - fetch_system_users
    if (req.has_param("fetch_system_users"))
    {
        try
        {
            params.m_fetchSystemUsers = (req.get_param_value("fetch_system_users") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'fetch_system_users': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_fetchSystemUsers = false;
    }
    // Query Parameters - assets_summary
    if (req.has_param("assets_summary"))
    {
        try
        {
            params.m_assetsSummary = (req.get_param_value("assets_summary") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'assets_summary': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_assetsSummary = false;
    }
    // Query Parameters - business_roles
    if (req.has_param("business_roles"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("business_roles");
            if (!params.m_businessRoles.has_value())
            {
                params.m_businessRoles = std::vector<MemberBusinessRole>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("business_roles", i);

                    params.m_businessRoles->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("business_roles", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_businessRoles->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'business_roles': " + std::string(e.what()));
        }
    }
    // Query Parameters - member_ids
    if (req.has_param("member_ids"))
    {
        params.m_memberIds = req.get_param_value("member_ids");
    }
    else
    {
        // Use default value for optional parameter
        params.m_memberIds = "";
    }
    // Query Parameters - start_index
    if (req.has_param("start_index"))
    {
        try
        {
            params.m_startIndex = std::stoi(req.get_param_value("start_index"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'start_index': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_startIndex = 0;
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdmembersGetResponse(const BusinessesbusinessIdmembersGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::GetBusinessMembers200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_GET_BUSINESS_MEMBERS200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdpartnersGetParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdpartnersGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - assets_summary
    if (req.has_param("assets_summary"))
    {
        try
        {
            params.m_assetsSummary = (req.get_param_value("assets_summary") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'assets_summary': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_assetsSummary = false;
    }
    // Query Parameters - partner_type
    if (req.has_param("partner_type"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("partner_type", 0);
            params.m_partnerType = nlohmann::json::parse(val).get<models::PartnerType>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'partner_type': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_partnerType = std::make_shared<PartnerType>();
    }
    // Query Parameters - partner_ids
    if (req.has_param("partner_ids"))
    {
        params.m_partnerIds = req.get_param_value("partner_ids");
    }
    else
    {
        // Use default value for optional parameter
        params.m_partnerIds = "";
    }
    // Query Parameters - start_index
    if (req.has_param("start_index"))
    {
        try
        {
            params.m_startIndex = std::stoi(req.get_param_value("start_index"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'start_index': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_startIndex = 0;
    }
    // Query Parameters - sort_ascending
    if (req.has_param("sort_ascending"))
    {
        try
        {
            params.m_sortAscending = (req.get_param_value("sort_ascending") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'sort_ascending': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_sortAscending = false;
    }
    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdpartnersGetResponse(const BusinessesbusinessIdpartnersGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::GetBusinessPartners200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_GET_BUSINESS_PARTNERS200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdsystemUserssystemUserIdPatchParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdsystemUserssystemUserIdPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - system_user_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'system_user_id'");
    }
    else
    {
        try
        {
            params.m_systemUserId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'system_user_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdsystemUserssystemUserIdPatchResponse(const BusinessesbusinessIdsystemUserssystemUserIdPatchResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool BusinessAccessRelationships::parseBusinessesbusinessIdmembersPatchParams(const httplib::Request& req, BusinessAccessRelationships::BusinessesbusinessIdmembersPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - business_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'business_id'");
    }
    else
    {
        try
        {
            params.m_businessId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'business_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void BusinessAccessRelationships::handleBusinessesbusinessIdmembersPatchResponse(const BusinessesbusinessIdmembersPatchResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::UpdateBusinessMembershipsResponse>)
        {
            res.status = HTTP_RESPONSE_CODE_UPDATE_BUSINESS_MEMBERSHIPS_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool BusinessAccessRelationships::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "biz_access:read", "biz_access:write" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void BusinessAccessRelationships::handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccounts(params);
        handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountId(params);
        handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdmembersDeleteRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdmembersDeleteRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdmembersDeleteParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleDeleteForBusinessesbusinessIdmembers(params);
        handleBusinessesbusinessIdmembersDeleteResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdpartnersDeleteRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdpartnersDeleteRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdpartnersDeleteParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleDeleteForBusinessesbusinessIdpartners(params);
        handleBusinessesbusinessIdpartnersDeleteResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesemployersGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesemployersGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesemployersGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForBusinessesemployers(params);
        handleBusinessesemployersGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdmembersGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdmembersGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdmembersGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForBusinessesbusinessIdmembers(params);
        handleBusinessesbusinessIdmembersGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdpartnersGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdpartnersGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdpartnersGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForBusinessesbusinessIdpartners(params);
        handleBusinessesbusinessIdpartnersGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdsystemUserssystemUserIdPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdsystemUserssystemUserIdPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdsystemUserssystemUserIdPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForBusinessesbusinessIdsystemUserssystemUserId(params);
        handleBusinessesbusinessIdsystemUserssystemUserIdPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void BusinessAccessRelationships::handleBusinessesbusinessIdmembersPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        BusinessesbusinessIdmembersPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseBusinessesbusinessIdmembersPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForBusinessesbusinessIdmembers(params);
        handleBusinessesbusinessIdmembersPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void BusinessAccessRelationships::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Post("/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsPostRequest(req, res, auth);
    });
    svr.Patch("/business_access/business_hierarchy/{business_hierarchy_id}/brand_accounts/{brand_account_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessAccessbusinessHierarchybusinessHierarchyIdbrandAccountsbrandAccountIdPatchRequest(req, res, auth);
    });
    svr.Delete("/businesses/{business_id}/members", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdmembersDeleteRequest(req, res, auth);
    });
    svr.Delete("/businesses/{business_id}/partners", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdpartnersDeleteRequest(req, res, auth);
    });
    svr.Get("/businesses/employers", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesemployersGetRequest(req, res, auth);
    });
    svr.Get("/businesses/{business_id}/members", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdmembersGetRequest(req, res, auth);
    });
    svr.Get("/businesses/{business_id}/partners", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdpartnersGetRequest(req, res, auth);
    });
    svr.Patch("/businesses/{business_id}/system_users/{system_user_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdsystemUserssystemUserIdPatchRequest(req, res, auth);
    });
    svr.Patch("/businesses/{business_id}/members", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleBusinessesbusinessIdmembersPatchRequest(req, res, auth);
    });
}

} // namespace Api