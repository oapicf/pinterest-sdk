/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "ProductGroupPromotionsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTIONS = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTION = 200;
constexpr int HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTIONS_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_PRODUCT_GROUP_ANALYTICS_ITEMS = 200;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool ProductGroupPromotions::parseAdAccountsadAccountIdproductGroupPromotionsPostParams(const httplib::Request& req, ProductGroupPromotions::AdAccountsadAccountIdproductGroupPromotionsPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsPostResponse(const AdAccountsadAccountIdproductGroupPromotionsPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductGroupPromotions>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTIONS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool ProductGroupPromotions::parseAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetParams(const httplib::Request& req, ProductGroupPromotions::AdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - product_group_promotion_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'product_group_promotion_id'");
    }
    else
    {
        try
        {
            params.m_productGroupPromotionId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'product_group_promotion_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetResponse(const AdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductGroupPromotion>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTION;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool ProductGroupPromotions::parseAdAccountsadAccountIdproductGroupPromotionsGetParams(const httplib::Request& req, ProductGroupPromotions::AdAccountsadAccountIdproductGroupPromotionsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }
    // Query Parameters - order
    if (req.has_param("order"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("order", 0);
            params.m_order = nlohmann::json::parse(val).get<models::PinterestLibPaginationOrder>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'order': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_order = std::make_shared<PinterestLibPaginationOrder>();
    }
    // Query Parameters - product_group_promotion_ids
    if (req.has_param("product_group_promotion_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("product_group_promotion_ids");
            if (!params.m_productGroupPromotionIds.has_value())
            {
                params.m_productGroupPromotionIds = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("product_group_promotion_ids", i);

                    params.m_productGroupPromotionIds->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("product_group_promotion_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_productGroupPromotionIds->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'product_group_promotion_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - entity_statuses
    if (req.has_param("entity_statuses"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("entity_statuses");
            if (!params.m_entityStatuses.has_value())
            {
                params.m_entityStatuses = std::vector<EntityStatus>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("entity_statuses", i);

                    params.m_entityStatuses->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("entity_statuses", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_entityStatuses->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'entity_statuses': " + std::string(e.what()));
        }
    }
    // Query Parameters - ad_group_id
    if (req.has_param("ad_group_id"))
    {
        params.m_adGroupId = req.get_param_value("ad_group_id");
    }
    else
    {
        // Use default value for optional parameter
        params.m_adGroupId = "";
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsGetResponse(const AdAccountsadAccountIdproductGroupPromotionsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductGroupPromotionsList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTIONS_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool ProductGroupPromotions::parseAdAccountsadAccountIdproductGroupPromotionsPatchParams(const httplib::Request& req, ProductGroupPromotions::AdAccountsadAccountIdproductGroupPromotionsPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsPatchResponse(const AdAccountsadAccountIdproductGroupPromotionsPatchResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductGroupPromotions>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_GROUP_PROMOTIONS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool ProductGroupPromotions::parseAdAccountsadAccountIdproductGroupsanalyticsGetParams(const httplib::Request& req, ProductGroupPromotions::AdAccountsadAccountIdproductGroupsanalyticsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - start_date
    if (req.has_param("start_date"))
    {
        params.m_startDate = req.get_param_value("start_date");
    }
    // Query Parameters - end_date
    if (req.has_param("end_date"))
    {
        params.m_endDate = req.get_param_value("end_date");
    }
    // Query Parameters - product_group_ids
    if (req.has_param("product_group_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("product_group_ids");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("product_group_ids", i);

                    params.m_productGroupIds.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("product_group_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_productGroupIds.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'product_group_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - columns
    if (req.has_param("columns"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("columns");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("columns", i);

                    params.m_columns.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("columns", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_columns.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'columns': " + std::string(e.what()));
        }
    }
    // Query Parameters - granularity
    if (req.has_param("granularity"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("granularity", 0);
            params.m_granularity = nlohmann::json::parse(val).get<Granularity>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'granularity': " + std::string(e.what()));
        }
    }
    // Query Parameters - click_window_days
    if (req.has_param("click_window_days"))
    {
        try
        {
            params.m_clickWindowDays = (req.get_param_value("click_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'click_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - engagement_window_days
    if (req.has_param("engagement_window_days"))
    {
        try
        {
            params.m_engagementWindowDays = (req.get_param_value("engagement_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'engagement_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - view_window_days
    if (req.has_param("view_window_days"))
    {
        try
        {
            params.m_viewWindowDays = (req.get_param_value("view_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'view_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - conversion_report_time
    if (req.has_param("conversion_report_time"))
    {
        try
        {
            params.m_conversionReportTime = (req.get_param_value("conversion_report_time"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'conversion_report_time': " + std::string(e.what()));
        }
    }
    // Query Parameters - reporting_timezone
    if (req.has_param("reporting_timezone"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("reporting_timezone", 0);
            params.m_reportingTimezone = nlohmann::json::parse(val).get<models::ReportingTimeZone>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'reporting_timezone': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_reportingTimezone = std::make_shared<ReportingTimeZone>();
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupsanalyticsGetResponse(const AdAccountsadAccountIdproductGroupsanalyticsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::ProductGroupAnalyticsItems>)
        {
            res.status = HTTP_RESPONSE_CODE_PRODUCT_GROUP_ANALYTICS_ITEMS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool ProductGroupPromotions::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "ads:write" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdproductGroupPromotionsPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdproductGroupPromotionsPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdproductGroupPromotions(params);
        handleAdAccountsadAccountIdproductGroupPromotionsPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionId(params);
        handleAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdproductGroupPromotionsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdproductGroupPromotionsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdproductGroupPromotions(params);
        handleAdAccountsadAccountIdproductGroupPromotionsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupPromotionsPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdproductGroupPromotionsPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdproductGroupPromotionsPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForAdAccountsadAccountIdproductGroupPromotions(params);
        handleAdAccountsadAccountIdproductGroupPromotionsPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void ProductGroupPromotions::handleAdAccountsadAccountIdproductGroupsanalyticsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdproductGroupsanalyticsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdproductGroupsanalyticsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdproductGroupsanalytics(params);
        handleAdAccountsadAccountIdproductGroupsanalyticsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void ProductGroupPromotions::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Post("/ad_accounts/{ad_account_id}/product_group_promotions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdproductGroupPromotionsPostRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/product_group_promotions/{product_group_promotion_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdproductGroupPromotionsproductGroupPromotionIdGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/product_group_promotions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdproductGroupPromotionsGetRequest(req, res, auth);
    });
    svr.Patch("/ad_accounts/{ad_account_id}/product_group_promotions", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdproductGroupPromotionsPatchRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/product_groups/analytics", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdproductGroupsanalyticsGetRequest(req, res, auth);
    });
}

} // namespace Api