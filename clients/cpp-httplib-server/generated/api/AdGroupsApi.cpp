/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "AdGroupsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_BID_FLOOR = 200;
constexpr int HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR = 400;
constexpr int HTTP_RESPONSE_CODE_DYNAMIC_TITLES_DOWNLOAD_CSV = 200;
constexpr int HTTP_RESPONSE_CODE_DYNAMIC_TITLES_GET_STATUS = 200;
constexpr int HTTP_RESPONSE_CODE_DYNAMIC_TITLES_UPLOAD_URL = 200;
constexpr int HTTP_RESPONSE_CODE_DYNAMIC_TITLES_PROCESS_CSV = 200;
constexpr int HTTP_RESPONSE_CODE_METRICS_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUPS_ANALYTICS_METRICS = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUP_AUDIENCE_SIZING = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUPS_CREATE200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUP = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUPS_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_AD_GROUPS_UPDATE200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_GET_AD_GROUPS_BY_PROMOTION_IDS_LIST200_RESPONSE = 200;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool AdGroups::parseAdAccountsadAccountIdbidFloorPostParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdbidFloorPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdbidFloorPostResponse(const AdAccountsadAccountIdbidFloorPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::BidFloor>)
        {
            res.status = HTTP_RESPONSE_CODE_BID_FLOOR;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - ad_group_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'ad_group_id'");
    }
    else
    {
        try
        {
            params.m_adGroupId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_group_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetResponse(const AdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DynamicTitlesDownloadCsv>)
        {
            res.status = HTTP_RESPONSE_CODE_DYNAMIC_TITLES_DOWNLOAD_CSV;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - ad_group_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'ad_group_id'");
    }
    else
    {
        try
        {
            params.m_adGroupId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_group_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetResponse(const AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DynamicTitlesGetStatus>)
        {
            res.status = HTTP_RESPONSE_CODE_DYNAMIC_TITLES_GET_STATUS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - ad_group_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'ad_group_id'");
    }
    else
    {
        try
        {
            params.m_adGroupId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_group_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetResponse(const AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DynamicTitlesUploadUrl>)
        {
            res.status = HTTP_RESPONSE_CODE_DYNAMIC_TITLES_UPLOAD_URL;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - ad_group_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'ad_group_id'");
    }
    else
    {
        try
        {
            params.m_adGroupId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_group_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostResponse(const AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::DynamicTitlesProcessCsv>)
        {
            res.status = HTTP_RESPONSE_CODE_DYNAMIC_TITLES_PROCESS_CSV;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupstargetingAnalyticsGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupstargetingAnalyticsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - ad_group_ids
    if (req.has_param("ad_group_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("ad_group_ids");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("ad_group_ids", i);

                    params.m_adGroupIds.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("ad_group_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_adGroupIds.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'ad_group_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - start_date
    if (req.has_param("start_date"))
    {
        params.m_startDate = req.get_param_value("start_date");
    }
    // Query Parameters - end_date
    if (req.has_param("end_date"))
    {
        params.m_endDate = req.get_param_value("end_date");
    }
    // Query Parameters - targeting_types
    if (req.has_param("targeting_types"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("targeting_types");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("targeting_types", i);

                    params.m_targetingTypes.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("targeting_types", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_targetingTypes.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'targeting_types': " + std::string(e.what()));
        }
    }
    // Query Parameters - columns
    if (req.has_param("columns"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("columns");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("columns", i);

                    params.m_columns.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("columns", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_columns.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'columns': " + std::string(e.what()));
        }
    }
    // Query Parameters - granularity
    if (req.has_param("granularity"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("granularity", 0);
            params.m_granularity = nlohmann::json::parse(val).get<Granularity>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'granularity': " + std::string(e.what()));
        }
    }
    // Query Parameters - click_window_days
    if (req.has_param("click_window_days"))
    {
        try
        {
            params.m_clickWindowDays = (req.get_param_value("click_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'click_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - engagement_window_days
    if (req.has_param("engagement_window_days"))
    {
        try
        {
            params.m_engagementWindowDays = (req.get_param_value("engagement_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'engagement_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - view_window_days
    if (req.has_param("view_window_days"))
    {
        try
        {
            params.m_viewWindowDays = (req.get_param_value("view_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'view_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - conversion_report_time
    if (req.has_param("conversion_report_time"))
    {
        try
        {
            params.m_conversionReportTime = (req.get_param_value("conversion_report_time"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'conversion_report_time': " + std::string(e.what()));
        }
    }
    // Query Parameters - attribution_types
    if (req.has_param("attribution_types"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("attribution_types");
            if (!params.m_attributionTypes.has_value())
            {
                params.m_attributionTypes = std::vector<ConversionReportAttributionType>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("attribution_types", i);

                    params.m_attributionTypes->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("attribution_types", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_attributionTypes->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'attribution_types': " + std::string(e.what()));
        }
    }
    // Query Parameters - reporting_timezone
    if (req.has_param("reporting_timezone"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("reporting_timezone", 0);
            params.m_reportingTimezone = nlohmann::json::parse(val).get<models::ReportingTimeZone>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'reporting_timezone': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_reportingTimezone = std::make_shared<ReportingTimeZone>();
    }
    // Query Parameters - sort_columns
    if (req.has_param("sort_columns"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("sort_columns");
            if (!params.m_sortColumns.has_value())
            {
                params.m_sortColumns = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("sort_columns", i);

                    params.m_sortColumns->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("sort_columns", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_sortColumns->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'sort_columns': " + std::string(e.what()));
        }
    }
    // Query Parameters - sort_ascending
    if (req.has_param("sort_ascending"))
    {
        try
        {
            params.m_sortAscending = (req.get_param_value("sort_ascending") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'sort_ascending': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_sortAscending = false;
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupstargetingAnalyticsGetResponse(const AdAccountsadAccountIdadGroupstargetingAnalyticsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::MetricsResponse>)
        {
            res.status = HTTP_RESPONSE_CODE_METRICS_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsanalyticsGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsanalyticsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - start_date
    if (req.has_param("start_date"))
    {
        params.m_startDate = req.get_param_value("start_date");
    }
    // Query Parameters - end_date
    if (req.has_param("end_date"))
    {
        params.m_endDate = req.get_param_value("end_date");
    }
    // Query Parameters - ad_group_ids
    if (req.has_param("ad_group_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("ad_group_ids");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("ad_group_ids", i);

                    params.m_adGroupIds.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("ad_group_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_adGroupIds.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'ad_group_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - columns
    if (req.has_param("columns"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("columns");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("columns", i);

                    params.m_columns.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("columns", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_columns.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'columns': " + std::string(e.what()));
        }
    }
    // Query Parameters - granularity
    if (req.has_param("granularity"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("granularity", 0);
            params.m_granularity = nlohmann::json::parse(val).get<Granularity>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'granularity': " + std::string(e.what()));
        }
    }
    // Query Parameters - click_window_days
    if (req.has_param("click_window_days"))
    {
        try
        {
            params.m_clickWindowDays = (req.get_param_value("click_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'click_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - engagement_window_days
    if (req.has_param("engagement_window_days"))
    {
        try
        {
            params.m_engagementWindowDays = (req.get_param_value("engagement_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'engagement_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - view_window_days
    if (req.has_param("view_window_days"))
    {
        try
        {
            params.m_viewWindowDays = (req.get_param_value("view_window_days"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'view_window_days': " + std::string(e.what()));
        }
    }
    // Query Parameters - conversion_report_time
    if (req.has_param("conversion_report_time"))
    {
        try
        {
            params.m_conversionReportTime = (req.get_param_value("conversion_report_time"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'conversion_report_time': " + std::string(e.what()));
        }
    }
    // Query Parameters - aggregate_report_rows
    if (req.has_param("aggregate_report_rows"))
    {
        try
        {
            params.m_aggregateReportRows = (req.get_param_value("aggregate_report_rows") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'aggregate_report_rows': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_aggregateReportRows = false;
    }
    // Query Parameters - reporting_timezone
    if (req.has_param("reporting_timezone"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("reporting_timezone", 0);
            params.m_reportingTimezone = nlohmann::json::parse(val).get<models::ReportingTimeZone>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'reporting_timezone': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_reportingTimezone = std::make_shared<ReportingTimeZone>();
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsanalyticsGetResponse(const AdAccountsadAccountIdadGroupsanalyticsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroupsAnalyticsMetrics>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUPS_ANALYTICS_METRICS;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsaudienceSizingPostParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsaudienceSizingPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsaudienceSizingPostResponse(const AdAccountsadAccountIdadGroupsaudienceSizingPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroupAudienceSizing>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUP_AUDIENCE_SIZING;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsPostParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsPostResponse(const AdAccountsadAccountIdadGroupsPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroupsCreate200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUPS_CREATE200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsadGroupIdGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsadGroupIdGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - ad_group_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_group_id'");
    }
    else
    {
        try
        {
            params.m_adGroupId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_group_id': " + std::string(e.what()));
        }
    }
    // Path Parameters - ad_account_id (index: 2)
    if (req.matches.size() < 2 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[2];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIdGetResponse(const AdAccountsadAccountIdadGroupsadGroupIdGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroup>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUP;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }
    // Query Parameters - order
    if (req.has_param("order"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("order", 0);
            params.m_order = nlohmann::json::parse(val).get<models::PinterestLibPaginationOrder>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'order': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_order = std::make_shared<PinterestLibPaginationOrder>();
    }
    // Query Parameters - campaign_ids
    if (req.has_param("campaign_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("campaign_ids");
            if (!params.m_campaignIds.has_value())
            {
                params.m_campaignIds = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("campaign_ids", i);

                    params.m_campaignIds->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("campaign_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_campaignIds->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'campaign_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - ad_group_ids
    if (req.has_param("ad_group_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("ad_group_ids");
            if (!params.m_adGroupIds.has_value())
            {
                params.m_adGroupIds = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("ad_group_ids", i);

                    params.m_adGroupIds->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("ad_group_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_adGroupIds->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'ad_group_ids': " + std::string(e.what()));
        }
    }
    // Query Parameters - entity_statuses
    if (req.has_param("entity_statuses"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("entity_statuses");
            if (!params.m_entityStatuses.has_value())
            {
                params.m_entityStatuses = std::vector<EntityStatus>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("entity_statuses", i);

                    params.m_entityStatuses->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("entity_statuses", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_entityStatuses->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'entity_statuses': " + std::string(e.what()));
        }
    }
    // Query Parameters - translate_interests_to_names
    if (req.has_param("translate_interests_to_names"))
    {
        try
        {
            params.m_translateInterestsToNames = (req.get_param_value("translate_interests_to_names") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'translate_interests_to_names': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_translateInterestsToNames = false;
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsGetResponse(const AdAccountsadAccountIdadGroupsGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroupsList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUPS_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdadGroupsPatchParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdadGroupsPatchRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;
    if (!req.body.empty())
    {
        try
        {
            nlohmann::json json = nlohmann::json::parse(req.body);
            from_json(json, params.m_request);
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid request body: " + std::string(e.what()));
        }
    }
    else
    {
        errors.push_back("Missing required request body");
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdadGroupsPatchResponse(const AdAccountsadAccountIdadGroupsPatchResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::AdGroupsUpdate200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_AD_GROUPS_UPDATE200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
bool AdGroups::parseAdAccountsadAccountIdpromotionAppliedEntitiesGetParams(const httplib::Request& req, AdGroups::AdAccountsadAccountIdpromotionAppliedEntitiesGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bookmark
    if (req.has_param("bookmark"))
    {
        params.m_bookmark = req.get_param_value("bookmark");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bookmark = "";
    }
    // Query Parameters - page_size
    if (req.has_param("page_size"))
    {
        try
        {
            params.m_pageSize = std::stoi(req.get_param_value("page_size"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'page_size': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_pageSize = 25;
    }
    // Query Parameters - order
    if (req.has_param("order"))
    {
        try
        {
            // Parse JSON object from query
            auto val = req.get_param_value("order", 0);
            params.m_order = nlohmann::json::parse(val).get<models::PinterestLibPaginationOrder>();
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'order': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_order = std::make_shared<PinterestLibPaginationOrder>();
    }
    // Query Parameters - promotion_ids
    if (req.has_param("promotion_ids"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("promotion_ids");
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("promotion_ids", i);

                    params.m_promotionIds.emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("promotion_ids", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_promotionIds.emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'promotion_ids': " + std::string(e.what()));
        }
    }

    // Path Parameters - ad_account_id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'ad_account_id'");
    }
    else
    {
        try
        {
            params.m_adAccountId = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'ad_account_id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void AdGroups::handleAdAccountsadAccountIdpromotionAppliedEntitiesGetResponse(const AdAccountsadAccountIdpromotionAppliedEntitiesGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::GetAdGroupsByPromotionIdsList200Response>)
        {
            res.status = HTTP_RESPONSE_CODE_GET_AD_GROUPS_BY_PROMOTION_IDS_LIST200_RESPONSE;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, models::Pinterest.Lib.Error>)
        {
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool AdGroups::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }




    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Bearer ") == 0)
        {
            std::string token = authHeader.substr(7);
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
            std::vector<std::string> requiredScopes = { "ads:read" };
            if (auth->validateOAuth2(token, requiredScopes)) return true;
        }
    }

    return false;
}

void AdGroups::handleAdAccountsadAccountIdbidFloorPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdbidFloorPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdbidFloorPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdbidFloor(params);
        handleAdAccountsadAccountIdbidFloorPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsv(params);
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatus(params);
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploads(params);
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdadGroupsadGroupIddynamicTitles(params);
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupstargetingAnalyticsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupstargetingAnalyticsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupstargetingAnalyticsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupstargetingAnalytics(params);
        handleAdAccountsadAccountIdadGroupstargetingAnalyticsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsanalyticsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsanalyticsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsanalyticsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupsanalytics(params);
        handleAdAccountsadAccountIdadGroupsanalyticsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsaudienceSizingPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsaudienceSizingPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsaudienceSizingPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdadGroupsaudienceSizing(params);
        handleAdAccountsadAccountIdadGroupsaudienceSizingPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForAdAccountsadAccountIdadGroups(params);
        handleAdAccountsadAccountIdadGroupsPostResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsadGroupIdGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsadGroupIdGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsadGroupIdGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroupsadGroupId(params);
        handleAdAccountsadAccountIdadGroupsadGroupIdGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdadGroups(params);
        handleAdAccountsadAccountIdadGroupsGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdadGroupsPatchRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdadGroupsPatchRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdadGroupsPatchParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePatchForAdAccountsadAccountIdadGroups(params);
        handleAdAccountsadAccountIdadGroupsPatchResponse(result, res);

    }
    catch (const nlohmann::json::parse_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::invalid_iterator& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::type_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::out_of_range& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
    catch (const nlohmann::json::other_error& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Invalid JSON: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void AdGroups::handleAdAccountsadAccountIdpromotionAppliedEntitiesGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AdAccountsadAccountIdpromotionAppliedEntitiesGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAdAccountsadAccountIdpromotionAppliedEntitiesGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_PINTEREST/LIB/ERROR;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForAdAccountsadAccountIdpromotionAppliedEntities(params);
        handleAdAccountsadAccountIdpromotionAppliedEntitiesGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void AdGroups::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Post("/ad_accounts/{ad_account_id}/bid_floor", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdbidFloorPostRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/csv", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlescsvGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/status", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesstatusGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles/uploads", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesuploadsGetRequest(req, res, auth);
    });
    svr.Post("/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}/dynamic_titles", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsadGroupIddynamicTitlesPostRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/targeting_analytics", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupstargetingAnalyticsGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/analytics", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsanalyticsGetRequest(req, res, auth);
    });
    svr.Post("/ad_accounts/{ad_account_id}/ad_groups/audience_sizing", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsaudienceSizingPostRequest(req, res, auth);
    });
    svr.Post("/ad_accounts/{ad_account_id}/ad_groups", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsPostRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups/{ad_group_id}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsadGroupIdGetRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/ad_groups", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsGetRequest(req, res, auth);
    });
    svr.Patch("/ad_accounts/{ad_account_id}/ad_groups", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdadGroupsPatchRequest(req, res, auth);
    });
    svr.Get("/ad_accounts/{ad_account_id}/promotion_applied_entities", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAdAccountsadAccountIdpromotionAppliedEntitiesGetRequest(req, res, auth);
    });
}

} // namespace Api