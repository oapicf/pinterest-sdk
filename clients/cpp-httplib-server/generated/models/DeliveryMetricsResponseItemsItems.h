/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#pragma once
// System headers
#include <nlohmann/json.hpp>
#include <optional>
#include <string>



namespace models {


class DeliveryMetricsResponseItemsItems
{
public:
    // ===================
    // ===== Enums =======
    // ===================
    enum class CategoryEnum {
        UNSPECIFIED,
        ADS,
        ORGANIC
    };

    // Enum conversion functions (definitions in .cpp)
    static std::string CategoryEnumToString(CategoryEnum value);
    static CategoryEnum CategoryEnumFromString(const std::string& str);


    DeliveryMetricsResponseItemsItems();
    virtual ~DeliveryMetricsResponseItemsItems() = default;

    // Getters and setters
    [[nodiscard]] std::optional<CategoryEnum> getCategory() const;
    void setCategory(const std::optional<CategoryEnum>& category);
    [[nodiscard]] std::string getDefinition() const;
    void setDefinition(const std::string& definition);
    [[nodiscard]] std::string getDisplayName() const;
    void setDisplayName(const std::string& displayName);
    [[nodiscard]] std::string getName() const;
    void setName(const std::string& name);

    // =============================================================================
    // Friend Enum Serialization Helpers (Required for NLOHMANN JSON serialization)
    // =============================================================================
    // These friend functions must be declared inside the class because:
    // 1. The enum types are nested and don't exist before the class is defined
    // 2. NLOHMANN macros need custom serialization for enum types (uses ToString/FromString)
    // 3. The 'friend' keyword makes them free functions in the namespace (not member functions)
    //    so ADL (Argument-Dependent Lookup) can find them during JSON serialization
    // =============================================================================

    // Serialization helpers for std::optional<CategoryEnum>
    friend inline void to_json(nlohmann::json& j, const std::optional<CategoryEnum>& opt)
    {
        if (opt.has_value())
        {
            j = CategoryEnumToString(opt.value());
        }
        else
        {
            j = nullptr;
        }
    }

    friend inline void from_json(const nlohmann::json& j, std::optional<CategoryEnum>& opt)
    {
        if (j.is_null())
        {
            opt = std::nullopt;
        }
        else
        {
            opt = CategoryEnumFromString(j.get<std::string>());
        }
    }



    // JSON serialization using NLOHMANN INTRUSIVE macro (must be inside class to access private members)
    NLOHMANN_DEFINE_TYPE_INTRUSIVE(DeliveryMetricsResponseItemsItems,
         category, definition, displayName, name)

private:
    std::optional<CategoryEnum> category;
    std::string definition;
    std::string displayName;
    std::string name;
};


} // namespace models

