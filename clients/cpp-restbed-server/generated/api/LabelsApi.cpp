/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.18.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "LabelsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

LabelsApiException::LabelsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int LabelsApiException::getStatus() const
{
    return m_status;
}
const char* LabelsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace LabelsApiResources {
Ad_accountsAd_account_idLabelsResource::Ad_accountsAd_account_idLabelsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/labels");
	this->set_method_handler("POST",
		std::bind(&Ad_accountsAd_account_idLabelsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idLabelsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&Ad_accountsAd_account_idLabelsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> Ad_accountsAd_account_idLabelsResource::handleLabelsApiException(const LabelsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idLabelsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idLabelsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idLabelsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idLabelsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idLabelsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idLabelsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto labelCreateRequest = extractJsonModelBodyParam<LabelCreateRequest>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    LabelsResponse resultObject = LabelsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(adAccountId, labelCreateRequest);
    }
    catch(const LabelsApiException& e) {
        std::tie(status_code, result) = handleLabelsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Unexpected error";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void Ad_accountsAd_account_idLabelsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string campaignIds_raw = request->get_query_parameter("campaignIds");
    std::vector<std::string> campaignIds;
    std::vector<std::string> campaignIds_temp;
    boost::split(campaignIds_temp, campaignIds_raw, boost::is_any_of(","));
    std::copy(campaignIds_temp.begin(), campaignIds_temp.end(), std::inserter(campaignIds, std::next(campaignIds.begin())));
    std::string labelIds_raw = request->get_query_parameter("labelIds");
    std::vector<std::string> labelIds;
    std::vector<std::string> labelIds_temp;
    boost::split(labelIds_temp, labelIds_raw, boost::is_any_of(","));
    std::copy(labelIds_temp.begin(), labelIds_temp.end(), std::inserter(labelIds, std::next(labelIds.begin())));
    std::string entityStatuses_raw = request->get_query_parameter("entityStatuses");
    std::vector<std::string> entityStatuses;
    std::vector<std::string> entityStatuses_temp;
    boost::split(entityStatuses_temp, entityStatuses_raw, boost::is_any_of(","));
    std::copy(entityStatuses_temp.begin(), entityStatuses_temp.end(), std::inserter(entityStatuses, std::next(entityStatuses.begin())));
    std::string labelTypes_raw = request->get_query_parameter("labelTypes");
    std::vector<std::string> labelTypes;
    std::vector<std::string> labelTypes_temp;
    boost::split(labelTypes_temp, labelTypes_raw, boost::is_any_of(","));
    std::copy(labelTypes_temp.begin(), labelTypes_temp.end(), std::inserter(labelTypes, std::next(labelTypes.begin())));
    int32_t pageSize = request->get_query_parameter("pageSize", 25);
    std::string bookmark = request->get_query_parameter("bookmark", "");
    
    int status_code = 500;
    Labels_list_200_response resultObject = Labels_list_200_response{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, campaignIds, labelIds, entityStatuses, labelTypes, pageSize, bookmark);
    }
    catch(const LabelsApiException& e) {
        std::tie(status_code, result) = handleLabelsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Invalid ad account ads parameters.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Unexpected error";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}
// x-extension
void Ad_accountsAd_account_idLabelsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto labelUpdateRequest = extractJsonModelBodyParam<LabelUpdateRequest>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    LabelsResponse resultObject = LabelsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, labelUpdateRequest);
    }
    catch(const LabelsApiException& e) {
        std::tie(status_code, result) = handleLabelsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Unexpected error";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, LabelsResponse> Ad_accountsAd_account_idLabelsResource::handler_POST(
        std::string & adAccountId, LabelCreateRequest & labelCreateRequest)
{
    return handler_POST_func(adAccountId, labelCreateRequest);
}

std::pair<int, Labels_list_200_response> Ad_accountsAd_account_idLabelsResource::handler_GET(
    std::string & adAccountId, std::vector<std::string> & campaignIds, std::vector<std::string> & labelIds, std::vector<std::string> & entityStatuses, std::vector<std::string> & labelTypes, int32_t & pageSize, std::string & bookmark)
{
    return handler_GET_func(adAccountId, campaignIds, labelIds, entityStatuses, labelTypes, pageSize, bookmark);
}
std::pair<int, LabelsResponse> Ad_accountsAd_account_idLabelsResource::handler_PATCH(
    std::string & adAccountId, LabelUpdateRequest & labelUpdateRequest)
{
    return handler_PATCH_func(adAccountId, labelUpdateRequest);
}

std::string Ad_accountsAd_account_idLabelsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idLabelsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace LabelsApiResources */

LabelsApi::LabelsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

LabelsApi::~LabelsApi() {}

std::shared_ptr<LabelsApiResources::Ad_accountsAd_account_idLabelsResource> LabelsApi::getAd_accountsAd_account_idLabelsResource() {
    if (!m_spAd_accountsAd_account_idLabelsResource) {
        setResource(std::make_shared<LabelsApiResources::Ad_accountsAd_account_idLabelsResource>());
    }
    return m_spAd_accountsAd_account_idLabelsResource;
}
void LabelsApi::setResource(std::shared_ptr<LabelsApiResources::Ad_accountsAd_account_idLabelsResource> resource) {
    m_spAd_accountsAd_account_idLabelsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idLabelsResource);
}
void LabelsApi::setLabelsApiAd_accountsAd_account_idLabelsResource(std::shared_ptr<LabelsApiResources::Ad_accountsAd_account_idLabelsResource> spAd_accountsAd_account_idLabelsResource) {
    m_spAd_accountsAd_account_idLabelsResource = spAd_accountsAd_account_idLabelsResource;
    m_service->publish(m_spAd_accountsAd_account_idLabelsResource);
}


void LabelsApi::publishDefaultResources() {
    if (!m_spAd_accountsAd_account_idLabelsResource) {
        setResource(std::make_shared<LabelsApiResources::Ad_accountsAd_account_idLabelsResource>());
    }
}

std::shared_ptr<restbed::Service> LabelsApi::service() {
    return m_service;
}


}
}
}
}

