/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "KeywordsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

KeywordsApiException::KeywordsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int KeywordsApiException::getStatus() const
{
    return m_status;
}
const char* KeywordsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace KeywordsApiResources {
Ad_accountsAd_account_idKeywordsMetricsResource::Ad_accountsAd_account_idKeywordsMetricsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/keywords/metrics");
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idKeywordsMetricsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idKeywordsMetricsResource::~Ad_accountsAd_account_idKeywordsMetricsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsMetricsResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsMetricsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsMetricsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idKeywordsMetricsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idKeywordsMetricsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idKeywordsMetricsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idKeywordsMetricsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string countryCode = request->get_query_parameter("countryCode", "");
    std::string keywords_raw = request->get_query_parameter("keywords");
    std::vector<std::string> keywords;
    std::vector<std::string> keywords_temp;
    boost::split(keywords_temp, keywords_raw, boost::is_any_of(","));
    std::copy(keywords_temp.begin(), keywords_temp.end(), std::inserter(keywords, std::next(keywords.begin())));
    
    int status_code = 500;
    KeywordsMetricsArrayResponse resultObject = KeywordsMetricsArrayResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, countryCode, keywords);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, KeywordsMetricsArrayResponse> Ad_accountsAd_account_idKeywordsMetricsResource::handler_GET(
        std::string & adAccountId, std::string & countryCode, std::vector<std::string> & keywords)
{
    return handler_GET_func(adAccountId, countryCode, keywords);
}


std::string Ad_accountsAd_account_idKeywordsMetricsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idKeywordsMetricsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idKeywordsResource::Ad_accountsAd_account_idKeywordsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/keywords");
	this->set_method_handler("POST",
		std::bind(&Ad_accountsAd_account_idKeywordsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idKeywordsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&Ad_accountsAd_account_idKeywordsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idKeywordsResource::~Ad_accountsAd_account_idKeywordsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idKeywordsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idKeywordsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idKeywordsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idKeywordsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idKeywordsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto keywordsCreate = extractJsonModelBodyParam<KeywordsCreate>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    Keywords resultObject = Keywords{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(adAccountId, keywordsCreate);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 201) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 201, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void Ad_accountsAd_account_idKeywordsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string campaignId = request->get_query_parameter("campaignId", "");
    std::string adGroupId = request->get_query_parameter("adGroupId", "");
    std::string adGroupIds_raw = request->get_query_parameter("adGroupIds");
    std::vector<std::string> adGroupIds;
    std::vector<std::string> adGroupIds_temp;
    boost::split(adGroupIds_temp, adGroupIds_raw, boost::is_any_of(","));
    std::copy(adGroupIds_temp.begin(), adGroupIds_temp.end(), std::inserter(adGroupIds, std::next(adGroupIds.begin())));
    std::string matchTypes_raw = request->get_query_parameter("matchTypes");
    std::vector<MatchType> matchTypes;
    std::vector<std::string> matchTypes_temp;
    boost::split(matchTypes_temp, matchTypes_raw, boost::is_any_of(","));
    std::transform(matchTypes_temp.begin(), matchTypes_temp.end(), std::back_inserter(matchTypes), [](const auto& i){ MatchType ret; ret.fromString(i); return ret;});
    std::string bookmark = request->get_query_parameter("bookmark", "");
    int32_t pageSize = request->get_query_parameter("pageSize", 25);
    
    int status_code = 500;
    Keywords_get_200_response resultObject = Keywords_get_200_response{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, campaignId, adGroupId, adGroupIds, matchTypes, bookmark, pageSize);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}
// x-extension
void Ad_accountsAd_account_idKeywordsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto keywordsUpdate = extractJsonModelBodyParam<KeywordsUpdate>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    Keywords resultObject = Keywords{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, keywordsUpdate);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, Keywords> Ad_accountsAd_account_idKeywordsResource::handler_POST(
        std::string & adAccountId, KeywordsCreate & keywordsCreate)
{
    return handler_POST_func(adAccountId, keywordsCreate);
}

std::pair<int, Keywords_get_200_response> Ad_accountsAd_account_idKeywordsResource::handler_GET(
    std::string & adAccountId, std::string & campaignId, std::string & adGroupId, std::vector<std::string> & adGroupIds, std::vector<MatchType> & matchTypes, std::string & bookmark, int32_t & pageSize)
{
    return handler_GET_func(adAccountId, campaignId, adGroupId, adGroupIds, matchTypes, bookmark, pageSize);
}
std::pair<int, Keywords> Ad_accountsAd_account_idKeywordsResource::handler_PATCH(
    std::string & adAccountId, KeywordsUpdate & keywordsUpdate)
{
    return handler_PATCH_func(adAccountId, keywordsUpdate);
}

std::string Ad_accountsAd_account_idKeywordsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idKeywordsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
TrendsKeywordsRegionTopTrend_typeResource::TrendsKeywordsRegionTopTrend_typeResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/keywords/{region: .*}/top/{trend_type: .*}");
	this->set_method_handler("GET",
		std::bind(&TrendsKeywordsRegionTopTrend_typeResource::handler_GET_internal, this,
			std::placeholders::_1));
}

TrendsKeywordsRegionTopTrend_typeResource::~TrendsKeywordsRegionTopTrend_typeResource() = default;

std::pair<int, std::string> TrendsKeywordsRegionTopTrend_typeResource::handleKeywordsApiException(const KeywordsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsKeywordsRegionTopTrend_typeResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsKeywordsRegionTopTrend_typeResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsKeywordsRegionTopTrend_typeResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsKeywordsRegionTopTrend_typeResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void TrendsKeywordsRegionTopTrend_typeResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsKeywordsRegionTopTrend_typeResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    // Getting the query params
    std::string interests_raw = request->get_query_parameter("interests");
    std::vector<TrendsL1Interest> interests;
    std::vector<std::string> interests_temp;
    boost::split(interests_temp, interests_raw, boost::is_any_of(","));
    std::transform(interests_temp.begin(), interests_temp.end(), std::back_inserter(interests), [](const auto& i){ TrendsL1Interest ret; ret.fromString(i); return ret;});
    std::string genders_raw = request->get_query_parameter("genders");
    std::vector<TrendsGenderFilter> genders;
    std::vector<std::string> genders_temp;
    boost::split(genders_temp, genders_raw, boost::is_any_of(","));
    std::transform(genders_temp.begin(), genders_temp.end(), std::back_inserter(genders), [](const auto& i){ TrendsGenderFilter ret; ret.fromString(i); return ret;});
    std::string ages_raw = request->get_query_parameter("ages");
    std::vector<TrendsAgeBucket> ages;
    std::vector<std::string> ages_temp;
    boost::split(ages_temp, ages_raw, boost::is_any_of(","));
    std::transform(ages_temp.begin(), ages_temp.end(), std::back_inserter(ages), [](const auto& i){ TrendsAgeBucket ret; ret.fromString(i); return ret;});
    std::string includeKeywords_raw = request->get_query_parameter("includeKeywords");
    std::vector<std::string> includeKeywords;
    std::vector<std::string> includeKeywords_temp;
    boost::split(includeKeywords_temp, includeKeywords_raw, boost::is_any_of(","));
    std::copy(includeKeywords_temp.begin(), includeKeywords_temp.end(), std::inserter(includeKeywords, std::next(includeKeywords.begin())));
    bool normalizeAgainstGroup = request->get_query_parameter("normalizeAgainstGroup", false);
    int32_t limit = request->get_query_parameter("limit", 50);
    bool includeDemographics = request->get_query_parameter("includeDemographics", false);
    
    int status_code = 500;
    TrendingKeywordsResponse resultObject = TrendingKeywordsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(region, trendType, interests, genders, ages, includeKeywords, normalizeAgainstGroup, limit, includeDemographics);
    }
    catch(const KeywordsApiException& e) {
        std::tie(status_code, result) = handleKeywordsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, TrendingKeywordsResponse> TrendsKeywordsRegionTopTrend_typeResource::handler_GET(
        TrendsSupportedRegion & region, TrendType & trendType, std::vector<TrendsL1Interest> & interests, std::vector<TrendsGenderFilter> & genders, std::vector<TrendsAgeBucket> & ages, std::vector<std::string> & includeKeywords, bool & normalizeAgainstGroup, int32_t & limit, bool & includeDemographics)
{
    return handler_GET_func(region, trendType, interests, genders, ages, includeKeywords, normalizeAgainstGroup, limit, includeDemographics);
}


std::string TrendsKeywordsRegionTopTrend_typeResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string TrendsKeywordsRegionTopTrend_typeResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace KeywordsApiResources */

KeywordsApi::KeywordsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

KeywordsApi::~KeywordsApi() {}

std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsMetricsResource> KeywordsApi::getAd_accountsAd_account_idKeywordsMetricsResource() {
    if (!m_spAd_accountsAd_account_idKeywordsMetricsResource) {
        setResource(std::make_shared<KeywordsApiResources::Ad_accountsAd_account_idKeywordsMetricsResource>());
    }
    return m_spAd_accountsAd_account_idKeywordsMetricsResource;
}
std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsResource> KeywordsApi::getAd_accountsAd_account_idKeywordsResource() {
    if (!m_spAd_accountsAd_account_idKeywordsResource) {
        setResource(std::make_shared<KeywordsApiResources::Ad_accountsAd_account_idKeywordsResource>());
    }
    return m_spAd_accountsAd_account_idKeywordsResource;
}
std::shared_ptr<KeywordsApiResources::TrendsKeywordsRegionTopTrend_typeResource> KeywordsApi::getTrendsKeywordsRegionTopTrend_typeResource() {
    if (!m_spTrendsKeywordsRegionTopTrend_typeResource) {
        setResource(std::make_shared<KeywordsApiResources::TrendsKeywordsRegionTopTrend_typeResource>());
    }
    return m_spTrendsKeywordsRegionTopTrend_typeResource;
}
void KeywordsApi::setResource(std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsMetricsResource> resource) {
    m_spAd_accountsAd_account_idKeywordsMetricsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idKeywordsMetricsResource);
}
void KeywordsApi::setResource(std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsResource> resource) {
    m_spAd_accountsAd_account_idKeywordsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idKeywordsResource);
}
void KeywordsApi::setResource(std::shared_ptr<KeywordsApiResources::TrendsKeywordsRegionTopTrend_typeResource> resource) {
    m_spTrendsKeywordsRegionTopTrend_typeResource = resource;
    m_service->publish(m_spTrendsKeywordsRegionTopTrend_typeResource);
}
void KeywordsApi::setKeywordsApiAd_accountsAd_account_idKeywordsMetricsResource(std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsMetricsResource> spAd_accountsAd_account_idKeywordsMetricsResource) {
    m_spAd_accountsAd_account_idKeywordsMetricsResource = spAd_accountsAd_account_idKeywordsMetricsResource;
    m_service->publish(m_spAd_accountsAd_account_idKeywordsMetricsResource);
}
void KeywordsApi::setKeywordsApiAd_accountsAd_account_idKeywordsResource(std::shared_ptr<KeywordsApiResources::Ad_accountsAd_account_idKeywordsResource> spAd_accountsAd_account_idKeywordsResource) {
    m_spAd_accountsAd_account_idKeywordsResource = spAd_accountsAd_account_idKeywordsResource;
    m_service->publish(m_spAd_accountsAd_account_idKeywordsResource);
}
void KeywordsApi::setKeywordsApiTrendsKeywordsRegionTopTrend_typeResource(std::shared_ptr<KeywordsApiResources::TrendsKeywordsRegionTopTrend_typeResource> spTrendsKeywordsRegionTopTrend_typeResource) {
    m_spTrendsKeywordsRegionTopTrend_typeResource = spTrendsKeywordsRegionTopTrend_typeResource;
    m_service->publish(m_spTrendsKeywordsRegionTopTrend_typeResource);
}


void KeywordsApi::publishDefaultResources() {
    if (!m_spAd_accountsAd_account_idKeywordsMetricsResource) {
        setResource(std::make_shared<KeywordsApiResources::Ad_accountsAd_account_idKeywordsMetricsResource>());
    }
    if (!m_spAd_accountsAd_account_idKeywordsResource) {
        setResource(std::make_shared<KeywordsApiResources::Ad_accountsAd_account_idKeywordsResource>());
    }
    if (!m_spTrendsKeywordsRegionTopTrend_typeResource) {
        setResource(std::make_shared<KeywordsApiResources::TrendsKeywordsRegionTopTrend_typeResource>());
    }
}

std::shared_ptr<restbed::Service> KeywordsApi::service() {
    return m_service;
}


}
}
}
}

