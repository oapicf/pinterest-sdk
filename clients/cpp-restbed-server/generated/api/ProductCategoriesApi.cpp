/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.23.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.18.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "ProductCategoriesApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

ProductCategoriesApiException::ProductCategoriesApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int ProductCategoriesApiException::getStatus() const
{
    return m_status;
}
const char* ProductCategoriesApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace ProductCategoriesApiResources {
TrendsTopicsFeaturedResource::TrendsTopicsFeaturedResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/topics/featured");
	this->set_method_handler("GET",
		std::bind(&TrendsTopicsFeaturedResource::handler_GET_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> TrendsTopicsFeaturedResource::handleProductCategoriesApiException(const ProductCategoriesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsTopicsFeaturedResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsTopicsFeaturedResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsTopicsFeaturedResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsTopicsFeaturedResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void TrendsTopicsFeaturedResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsTopicsFeaturedResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the query params
    
    int status_code = 500;
    std::vector<FeaturedTrend> resultObject = std::vector<FeaturedTrend>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(region, interest);
    }
    catch(const ProductCategoriesApiException& e) {
        std::tie(status_code, result) = handleProductCategoriesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, std::vector<FeaturedTrend>> TrendsTopicsFeaturedResource::handler_GET(
        ProductCategoryRegion & region, InterestsEnum & interest)
{
    return handler_GET_func(region, interest);
}


std::string TrendsTopicsFeaturedResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string TrendsTopicsFeaturedResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
TrendsProduct_categoriesDetailsResource::TrendsProduct_categoriesDetailsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/product_categories/details");
	this->set_method_handler("GET",
		std::bind(&TrendsProduct_categoriesDetailsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> TrendsProduct_categoriesDetailsResource::handleProductCategoriesApiException(const ProductCategoriesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsProduct_categoriesDetailsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsProduct_categoriesDetailsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsProduct_categoriesDetailsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsProduct_categoriesDetailsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void TrendsProduct_categoriesDetailsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsProduct_categoriesDetailsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the query params
    std::string productCategories_raw = request->get_query_parameter("productCategories");
    std::vector<ProductCategoryEnum> productCategories;
    std::vector<std::string> productCategories_temp;
    boost::split(productCategories_temp, productCategories_raw, boost::is_any_of(","));
    std::transform(productCategories_temp.begin(), productCategories_temp.end(), std::back_inserter(productCategories), [](const auto& i){ ProductCategoryEnum ret; ret.fromString(i); return ret;});
    
    int status_code = 500;
    std::vector<ProductCategoryDetails> resultObject = std::vector<ProductCategoryDetails>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(productCategories, region, lookbackWindow, engagementType);
    }
    catch(const ProductCategoriesApiException& e) {
        std::tie(status_code, result) = handleProductCategoriesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, std::vector<ProductCategoryDetails>> TrendsProduct_categoriesDetailsResource::handler_GET(
        std::vector<ProductCategoryEnum> & productCategories, ProductCategoryRegion & region, ProductCategoryDetailLookbackWindow & lookbackWindow, ProductCategoriesEngagementType & engagementType)
{
    return handler_GET_func(productCategories, region, lookbackWindow, engagementType);
}


std::string TrendsProduct_categoriesDetailsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string TrendsProduct_categoriesDetailsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
TrendsProduct_categoriesTrendingResource::TrendsProduct_categoriesTrendingResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/trends/product_categories/trending");
	this->set_method_handler("GET",
		std::bind(&TrendsProduct_categoriesTrendingResource::handler_GET_internal, this,
			std::placeholders::_1));
}

std::pair<int, std::string> TrendsProduct_categoriesTrendingResource::handleProductCategoriesApiException(const ProductCategoriesApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> TrendsProduct_categoriesTrendingResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> TrendsProduct_categoriesTrendingResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void TrendsProduct_categoriesTrendingResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void TrendsProduct_categoriesTrendingResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void TrendsProduct_categoriesTrendingResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void TrendsProduct_categoriesTrendingResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the query params
    std::string verticals_raw = request->get_query_parameter("verticals");
    std::vector<VerticalProductCategory> verticals;
    std::vector<std::string> verticals_temp;
    boost::split(verticals_temp, verticals_raw, boost::is_any_of(","));
    std::transform(verticals_temp.begin(), verticals_temp.end(), std::back_inserter(verticals), [](const auto& i){ VerticalProductCategory ret; ret.fromString(i); return ret;});
    std::string ages_raw = request->get_query_parameter("ages");
    std::vector<AgeTrendsBucket> ages;
    std::vector<std::string> ages_temp;
    boost::split(ages_temp, ages_raw, boost::is_any_of(","));
    std::transform(ages_temp.begin(), ages_temp.end(), std::back_inserter(ages), [](const auto& i){ AgeTrendsBucket ret; ret.fromString(i); return ret;});
    std::string genders_raw = request->get_query_parameter("genders");
    std::vector<GenderBucket> genders;
    std::vector<std::string> genders_temp;
    boost::split(genders_temp, genders_raw, boost::is_any_of(","));
    std::transform(genders_temp.begin(), genders_temp.end(), std::back_inserter(genders), [](const auto& i){ GenderBucket ret; ret.fromString(i); return ret;});
    
    int status_code = 500;
    std::vector<TrendingProductCategory> resultObject = std::vector<TrendingProductCategory>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(region, verticals, ages, genders, engagementType);
    }
    catch(const ProductCategoriesApiException& e) {
        std::tie(status_code, result) = handleProductCategoriesApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, std::vector<TrendingProductCategory>> TrendsProduct_categoriesTrendingResource::handler_GET(
        ProductCategoryRegion & region, std::vector<VerticalProductCategory> & verticals, std::vector<AgeTrendsBucket> & ages, std::vector<GenderBucket> & genders, ProductCategoriesEngagementType & engagementType)
{
    return handler_GET_func(region, verticals, ages, genders, engagementType);
}


std::string TrendsProduct_categoriesTrendingResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string TrendsProduct_categoriesTrendingResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace ProductCategoriesApiResources */

ProductCategoriesApi::ProductCategoriesApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

ProductCategoriesApi::~ProductCategoriesApi() {}

std::shared_ptr<ProductCategoriesApiResources::TrendsTopicsFeaturedResource> ProductCategoriesApi::getTrendsTopicsFeaturedResource() {
    if (!m_spTrendsTopicsFeaturedResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsTopicsFeaturedResource>());
    }
    return m_spTrendsTopicsFeaturedResource;
}
std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesDetailsResource> ProductCategoriesApi::getTrendsProduct_categoriesDetailsResource() {
    if (!m_spTrendsProduct_categoriesDetailsResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsProduct_categoriesDetailsResource>());
    }
    return m_spTrendsProduct_categoriesDetailsResource;
}
std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesTrendingResource> ProductCategoriesApi::getTrendsProduct_categoriesTrendingResource() {
    if (!m_spTrendsProduct_categoriesTrendingResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsProduct_categoriesTrendingResource>());
    }
    return m_spTrendsProduct_categoriesTrendingResource;
}
void ProductCategoriesApi::setResource(std::shared_ptr<ProductCategoriesApiResources::TrendsTopicsFeaturedResource> resource) {
    m_spTrendsTopicsFeaturedResource = resource;
    m_service->publish(m_spTrendsTopicsFeaturedResource);
}
void ProductCategoriesApi::setResource(std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesDetailsResource> resource) {
    m_spTrendsProduct_categoriesDetailsResource = resource;
    m_service->publish(m_spTrendsProduct_categoriesDetailsResource);
}
void ProductCategoriesApi::setResource(std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesTrendingResource> resource) {
    m_spTrendsProduct_categoriesTrendingResource = resource;
    m_service->publish(m_spTrendsProduct_categoriesTrendingResource);
}
void ProductCategoriesApi::setProductCategoriesApiTrendsTopicsFeaturedResource(std::shared_ptr<ProductCategoriesApiResources::TrendsTopicsFeaturedResource> spTrendsTopicsFeaturedResource) {
    m_spTrendsTopicsFeaturedResource = spTrendsTopicsFeaturedResource;
    m_service->publish(m_spTrendsTopicsFeaturedResource);
}
void ProductCategoriesApi::setProductCategoriesApiTrendsProduct_categoriesDetailsResource(std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesDetailsResource> spTrendsProduct_categoriesDetailsResource) {
    m_spTrendsProduct_categoriesDetailsResource = spTrendsProduct_categoriesDetailsResource;
    m_service->publish(m_spTrendsProduct_categoriesDetailsResource);
}
void ProductCategoriesApi::setProductCategoriesApiTrendsProduct_categoriesTrendingResource(std::shared_ptr<ProductCategoriesApiResources::TrendsProduct_categoriesTrendingResource> spTrendsProduct_categoriesTrendingResource) {
    m_spTrendsProduct_categoriesTrendingResource = spTrendsProduct_categoriesTrendingResource;
    m_service->publish(m_spTrendsProduct_categoriesTrendingResource);
}


void ProductCategoriesApi::publishDefaultResources() {
    if (!m_spTrendsTopicsFeaturedResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsTopicsFeaturedResource>());
    }
    if (!m_spTrendsProduct_categoriesDetailsResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsProduct_categoriesDetailsResource>());
    }
    if (!m_spTrendsProduct_categoriesTrendingResource) {
        setResource(std::make_shared<ProductCategoriesApiResources::TrendsProduct_categoriesTrendingResource>());
    }
}

std::shared_ptr<restbed::Service> ProductCategoriesApi::service() {
    return m_service;
}


}
}
}
}

