/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "ProductTagsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

ProductTagsApiException::ProductTagsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int ProductTagsApiException::getStatus() const
{
    return m_status;
}
const char* ProductTagsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace ProductTagsApiResources {
PinsPin_idProduct_tagsResource::PinsPin_idProduct_tagsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/pins/{pin_id: .*}/product_tags");
	this->set_method_handler("POST",
		std::bind(&PinsPin_idProduct_tagsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&PinsPin_idProduct_tagsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

PinsPin_idProduct_tagsResource::~PinsPin_idProduct_tagsResource() = default;

std::pair<int, std::string> PinsPin_idProduct_tagsResource::handleProductTagsApiException(const ProductTagsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> PinsPin_idProduct_tagsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> PinsPin_idProduct_tagsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void PinsPin_idProduct_tagsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void PinsPin_idProduct_tagsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void PinsPin_idProduct_tagsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void PinsPin_idProduct_tagsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto productTagsBulkAddRequest = extractJsonModelBodyParam<ProductTagsBulkAddRequest>(bodyContent);
    // Getting the path params
    std::string pinId = request->get_path_parameter("pin_id", "");
    
    int status_code = 500;
    ProductTagsResponse resultObject = ProductTagsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(pinId, productTagsBulkAddRequest);
    }
    catch(const ProductTagsApiException& e) {
        std::tie(status_code, result) = handleProductTagsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request contains ineligible product tags.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void PinsPin_idProduct_tagsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string pinId = request->get_path_parameter("pin_id", "");
    
    int status_code = 500;
    ProductTagsResponse resultObject = ProductTagsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(pinId);
    }
    catch(const ProductTagsApiException& e) {
        std::tie(status_code, result) = handleProductTagsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, ProductTagsResponse> PinsPin_idProduct_tagsResource::handler_POST(
        std::string & pinId, ProductTagsBulkAddRequest & productTagsBulkAddRequest)
{
    return handler_POST_func(pinId, productTagsBulkAddRequest);
}

std::pair<int, ProductTagsResponse> PinsPin_idProduct_tagsResource::handler_GET(
    std::string & pinId)
{
    return handler_GET_func(pinId);
}

std::string PinsPin_idProduct_tagsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string PinsPin_idProduct_tagsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
PinsPin_idProduct_tagsBulk_deleteResource::PinsPin_idProduct_tagsBulk_deleteResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/pins/{pin_id: .*}/product_tags/bulk-delete");
	this->set_method_handler("POST",
		std::bind(&PinsPin_idProduct_tagsBulk_deleteResource::handler_POST_internal, this,
			std::placeholders::_1));
}

PinsPin_idProduct_tagsBulk_deleteResource::~PinsPin_idProduct_tagsBulk_deleteResource() = default;

std::pair<int, std::string> PinsPin_idProduct_tagsBulk_deleteResource::handleProductTagsApiException(const ProductTagsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> PinsPin_idProduct_tagsBulk_deleteResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> PinsPin_idProduct_tagsBulk_deleteResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void PinsPin_idProduct_tagsBulk_deleteResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void PinsPin_idProduct_tagsBulk_deleteResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void PinsPin_idProduct_tagsBulk_deleteResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void PinsPin_idProduct_tagsBulk_deleteResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto productTagsBulkDeleteRequest = extractJsonModelBodyParam<ProductTagsBulkDeleteRequest>(bodyContent);
    // Getting the path params
    std::string pinId = request->get_path_parameter("pin_id", "");
    
    int status_code = 500;
    std::string result = "";
    
    try {
        status_code =
            handler_POST(pinId, productTagsBulkDeleteRequest);
    }
    catch(const ProductTagsApiException& e) {
        std::tie(status_code, result) = handleProductTagsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 204) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 204, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


int PinsPin_idProduct_tagsBulk_deleteResource::handler_POST(
        std::string & pinId, ProductTagsBulkDeleteRequest & productTagsBulkDeleteRequest)
{
    return handler_POST_func(pinId, productTagsBulkDeleteRequest);
}


std::string PinsPin_idProduct_tagsBulk_deleteResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string PinsPin_idProduct_tagsBulk_deleteResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace ProductTagsApiResources */

ProductTagsApi::ProductTagsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

ProductTagsApi::~ProductTagsApi() {}

std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsResource> ProductTagsApi::getPinsPin_idProduct_tagsResource() {
    if (!m_spPinsPin_idProduct_tagsResource) {
        setResource(std::make_shared<ProductTagsApiResources::PinsPin_idProduct_tagsResource>());
    }
    return m_spPinsPin_idProduct_tagsResource;
}
std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsBulk_deleteResource> ProductTagsApi::getPinsPin_idProduct_tagsBulk_deleteResource() {
    if (!m_spPinsPin_idProduct_tagsBulk_deleteResource) {
        setResource(std::make_shared<ProductTagsApiResources::PinsPin_idProduct_tagsBulk_deleteResource>());
    }
    return m_spPinsPin_idProduct_tagsBulk_deleteResource;
}
void ProductTagsApi::setResource(std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsResource> resource) {
    m_spPinsPin_idProduct_tagsResource = resource;
    m_service->publish(m_spPinsPin_idProduct_tagsResource);
}
void ProductTagsApi::setResource(std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsBulk_deleteResource> resource) {
    m_spPinsPin_idProduct_tagsBulk_deleteResource = resource;
    m_service->publish(m_spPinsPin_idProduct_tagsBulk_deleteResource);
}
void ProductTagsApi::setProductTagsApiPinsPin_idProduct_tagsResource(std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsResource> spPinsPin_idProduct_tagsResource) {
    m_spPinsPin_idProduct_tagsResource = spPinsPin_idProduct_tagsResource;
    m_service->publish(m_spPinsPin_idProduct_tagsResource);
}
void ProductTagsApi::setProductTagsApiPinsPin_idProduct_tagsBulk_deleteResource(std::shared_ptr<ProductTagsApiResources::PinsPin_idProduct_tagsBulk_deleteResource> spPinsPin_idProduct_tagsBulk_deleteResource) {
    m_spPinsPin_idProduct_tagsBulk_deleteResource = spPinsPin_idProduct_tagsBulk_deleteResource;
    m_service->publish(m_spPinsPin_idProduct_tagsBulk_deleteResource);
}


void ProductTagsApi::publishDefaultResources() {
    if (!m_spPinsPin_idProduct_tagsResource) {
        setResource(std::make_shared<ProductTagsApiResources::PinsPin_idProduct_tagsResource>());
    }
    if (!m_spPinsPin_idProduct_tagsBulk_deleteResource) {
        setResource(std::make_shared<ProductTagsApiResources::PinsPin_idProduct_tagsBulk_deleteResource>());
    }
}

std::shared_ptr<restbed::Service> ProductTagsApi::service() {
    return m_service;
}


}
}
}
}

