/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CatalogSupplementalApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

CatalogSupplementalApiException::CatalogSupplementalApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CatalogSupplementalApiException::getStatus() const
{
    return m_status;
}
const char* CatalogSupplementalApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace CatalogSupplementalApiResources {
CatalogsCatalog_idLocal_inventory_itemsBatchResource::CatalogsCatalog_idLocal_inventory_itemsBatchResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_inventory_items/batch");
	this->set_method_handler("POST",
		std::bind(&CatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CatalogsCatalog_idLocal_inventory_itemsBatchResource::~CatalogsCatalog_idLocal_inventory_itemsBatchResource() = default;

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsBatchResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsBatchResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsBatchResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogsCatalog_idLocal_inventory_itemsBatchResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogsCatalog_idLocal_inventory_itemsBatchResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void CatalogsCatalog_idLocal_inventory_itemsBatchResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto localInventoryItemsBatchCreate = extractJsonModelBodyParam<LocalInventoryItemsBatchCreate>(bodyContent);
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    SupplementalItemsBatchResponse resultObject = SupplementalItemsBatchResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(catalogId, localInventoryItemsBatchCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 201) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 201, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, SupplementalItemsBatchResponse> CatalogsCatalog_idLocal_inventory_itemsBatchResource::handler_POST(
        std::string & catalogId, LocalInventoryItemsBatchCreate & localInventoryItemsBatchCreate, std::string & adAccountId)
{
    return handler_POST_func(catalogId, localInventoryItemsBatchCreate, adAccountId);
}


std::string CatalogsCatalog_idLocal_inventory_itemsBatchResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string CatalogsCatalog_idLocal_inventory_itemsBatchResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
CatalogsCatalog_idLocal_inventory_itemsQueryResource::CatalogsCatalog_idLocal_inventory_itemsQueryResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_inventory_items/query");
	this->set_method_handler("POST",
		std::bind(&CatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST_internal, this,
			std::placeholders::_1));
}

CatalogsCatalog_idLocal_inventory_itemsQueryResource::~CatalogsCatalog_idLocal_inventory_itemsQueryResource() = default;

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsQueryResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsQueryResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_inventory_itemsQueryResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogsCatalog_idLocal_inventory_itemsQueryResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogsCatalog_idLocal_inventory_itemsQueryResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void CatalogsCatalog_idLocal_inventory_itemsQueryResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto localInventoryItemsGetCreate = extractJsonModelBodyParam<LocalInventoryItemsGetCreate>(bodyContent);
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    LocalInventoryItemsGet resultObject = LocalInventoryItemsGet{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(catalogId, localInventoryItemsGetCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 201) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 201, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, LocalInventoryItemsGet> CatalogsCatalog_idLocal_inventory_itemsQueryResource::handler_POST(
        std::string & catalogId, LocalInventoryItemsGetCreate & localInventoryItemsGetCreate, std::string & adAccountId)
{
    return handler_POST_func(catalogId, localInventoryItemsGetCreate, adAccountId);
}


std::string CatalogsCatalog_idLocal_inventory_itemsQueryResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string CatalogsCatalog_idLocal_inventory_itemsQueryResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
CatalogsCatalog_idLocal_storesResource::CatalogsCatalog_idLocal_storesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/local_stores");
	this->set_method_handler("POST",
		std::bind(&CatalogsCatalog_idLocal_storesResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("DELETE",
		std::bind(&CatalogsCatalog_idLocal_storesResource::handler_DELETE_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&CatalogsCatalog_idLocal_storesResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&CatalogsCatalog_idLocal_storesResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

CatalogsCatalog_idLocal_storesResource::~CatalogsCatalog_idLocal_storesResource() = default;

std::pair<int, std::string> CatalogsCatalog_idLocal_storesResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_storesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogsCatalog_idLocal_storesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogsCatalog_idLocal_storesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogsCatalog_idLocal_storesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void CatalogsCatalog_idLocal_storesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogsCatalog_idLocal_storesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto localStoreCreate = extractJsonArrayBodyParam<LocalStoreCreate>(bodyContent);
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    std::vector<Catalogs_local_stores_create_200_response_inner> resultObject = std::vector<Catalogs_local_stores_create_200_response_inner>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(catalogId, localStoreCreate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 201) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 201, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void CatalogsCatalog_idLocal_storesResource::handler_DELETE_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string ids_raw = request->get_query_parameter("ids");
    std::vector<std::string> ids;
    std::vector<std::string> ids_temp;
    boost::split(ids_temp, ids_raw, boost::is_any_of(","));
    std::copy(ids_temp.begin(), ids_temp.end(), std::inserter(ids, std::next(ids.begin())));
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    std::vector<Catalogs_local_stores_delete_200_response_inner> resultObject = std::vector<Catalogs_local_stores_delete_200_response_inner>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_DELETE(catalogId, ids, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 204) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 204, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}
// x-extension
void CatalogsCatalog_idLocal_storesResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string ids_raw = request->get_query_parameter("ids");
    std::vector<std::string> ids;
    std::vector<std::string> ids_temp;
    boost::split(ids_temp, ids_raw, boost::is_any_of(","));
    std::copy(ids_temp.begin(), ids_temp.end(), std::inserter(ids, std::next(ids.begin())));
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    std::string bookmark = request->get_query_parameter("bookmark", "");
    int32_t pageSize = request->get_query_parameter("pageSize", 25);
    
    int status_code = 500;
    Catalogs_local_stores_list_200_response resultObject = Catalogs_local_stores_list_200_response{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(catalogId, ids, adAccountId, bookmark, pageSize);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}
// x-extension
void CatalogsCatalog_idLocal_storesResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto localStoreBatchUpdate = extractJsonArrayBodyParam<LocalStoreBatchUpdate>(bodyContent);
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    // Getting the query params
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    std::vector<Catalogs_local_stores_create_200_response_inner> resultObject = std::vector<Catalogs_local_stores_create_200_response_inner>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(catalogId, localStoreBatchUpdate, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, std::vector<Catalogs_local_stores_create_200_response_inner>> CatalogsCatalog_idLocal_storesResource::handler_POST(
        std::string & catalogId, std::vector<LocalStoreCreate> & localStoreCreate, std::string & adAccountId)
{
    return handler_POST_func(catalogId, localStoreCreate, adAccountId);
}

std::pair<int, std::vector<Catalogs_local_stores_delete_200_response_inner>> CatalogsCatalog_idLocal_storesResource::handler_DELETE(
    std::string & catalogId, std::vector<std::string> & ids, std::string & adAccountId)
{
    return handler_DELETE_func(catalogId, ids, adAccountId);
}
std::pair<int, Catalogs_local_stores_list_200_response> CatalogsCatalog_idLocal_storesResource::handler_GET(
    std::string & catalogId, std::vector<std::string> & ids, std::string & adAccountId, std::string & bookmark, int32_t & pageSize)
{
    return handler_GET_func(catalogId, ids, adAccountId, bookmark, pageSize);
}
std::pair<int, std::vector<Catalogs_local_stores_create_200_response_inner>> CatalogsCatalog_idLocal_storesResource::handler_PATCH(
    std::string & catalogId, std::vector<LocalStoreBatchUpdate> & localStoreBatchUpdate, std::string & adAccountId)
{
    return handler_PATCH_func(catalogId, localStoreBatchUpdate, adAccountId);
}

std::string CatalogsCatalog_idLocal_storesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string CatalogsCatalog_idLocal_storesResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/catalogs/{catalog_id: .*}/supplemental_items/batch/{batch_id: .*}");
	this->set_method_handler("GET",
		std::bind(&CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::~CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource() = default;

std::pair<int, std::string> CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleCatalogSupplementalApiException(const CatalogSupplementalApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string catalogId = request->get_path_parameter("catalog_id", "");
    std::string batchId = request->get_path_parameter("batch_id", "");
    // Getting the query params
    std::string adAccountId = request->get_query_parameter("adAccountId", "");
    
    int status_code = 500;
    SupplementalItemsBatchResponse resultObject = SupplementalItemsBatchResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(catalogId, batchId, adAccountId);
    }
    catch(const CatalogSupplementalApiException& e) {
        std::tie(status_code, result) = handleCatalogSupplementalApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, SupplementalItemsBatchResponse> CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::handler_GET(
        std::string & catalogId, std::string & batchId, std::string & adAccountId)
{
    return handler_GET_func(catalogId, batchId, adAccountId);
}


std::string CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace CatalogSupplementalApiResources */

CatalogSupplementalApi::CatalogSupplementalApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CatalogSupplementalApi::~CatalogSupplementalApi() {}

std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsBatchResource> CatalogSupplementalApi::getCatalogsCatalog_idLocal_inventory_itemsBatchResource() {
    if (!m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsBatchResource>());
    }
    return m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource;
}
std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsQueryResource> CatalogSupplementalApi::getCatalogsCatalog_idLocal_inventory_itemsQueryResource() {
    if (!m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsQueryResource>());
    }
    return m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource;
}
std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_storesResource> CatalogSupplementalApi::getCatalogsCatalog_idLocal_storesResource() {
    if (!m_spCatalogsCatalog_idLocal_storesResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_storesResource>());
    }
    return m_spCatalogsCatalog_idLocal_storesResource;
}
std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource> CatalogSupplementalApi::getCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource() {
    if (!m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource>());
    }
    return m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource;
}
void CatalogSupplementalApi::setResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsBatchResource> resource) {
    m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource = resource;
    m_service->publish(m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource);
}
void CatalogSupplementalApi::setResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsQueryResource> resource) {
    m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource = resource;
    m_service->publish(m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource);
}
void CatalogSupplementalApi::setResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_storesResource> resource) {
    m_spCatalogsCatalog_idLocal_storesResource = resource;
    m_service->publish(m_spCatalogsCatalog_idLocal_storesResource);
}
void CatalogSupplementalApi::setResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource> resource) {
    m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource = resource;
    m_service->publish(m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsBatchResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsBatchResource> spCatalogsCatalog_idLocal_inventory_itemsBatchResource) {
    m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource = spCatalogsCatalog_idLocal_inventory_itemsBatchResource;
    m_service->publish(m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_inventory_itemsQueryResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsQueryResource> spCatalogsCatalog_idLocal_inventory_itemsQueryResource) {
    m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource = spCatalogsCatalog_idLocal_inventory_itemsQueryResource;
    m_service->publish(m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idLocal_storesResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_storesResource> spCatalogsCatalog_idLocal_storesResource) {
    m_spCatalogsCatalog_idLocal_storesResource = spCatalogsCatalog_idLocal_storesResource;
    m_service->publish(m_spCatalogsCatalog_idLocal_storesResource);
}
void CatalogSupplementalApi::setCatalogSupplementalApiCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource(std::shared_ptr<CatalogSupplementalApiResources::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource> spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource) {
    m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource = spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource;
    m_service->publish(m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource);
}


void CatalogSupplementalApi::publishDefaultResources() {
    if (!m_spCatalogsCatalog_idLocal_inventory_itemsBatchResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsBatchResource>());
    }
    if (!m_spCatalogsCatalog_idLocal_inventory_itemsQueryResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_inventory_itemsQueryResource>());
    }
    if (!m_spCatalogsCatalog_idLocal_storesResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idLocal_storesResource>());
    }
    if (!m_spCatalogsCatalog_idSupplemental_itemsBatchBatch_idResource) {
        setResource(std::make_shared<CatalogSupplementalApiResources::CatalogsCatalog_idSupplemental_itemsBatchBatch_idResource>());
    }
}

std::shared_ptr<restbed::Service> CatalogSupplementalApi::service() {
    return m_service;
}


}
}
}
}

