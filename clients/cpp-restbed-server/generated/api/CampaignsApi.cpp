/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.28.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "CampaignsApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

namespace {
[[maybe_unused]]
std::string selectPreferredContentType(const std::vector<std::string>& contentTypes) {
    if (contentTypes.size() == 0) {
        return "application/json";
    }

    if (contentTypes.size() == 1) {
        return contentTypes.at(0);
    }

    static const std::array<std::string, 2> preferredTypes = {"json", "xml"};
    for (const auto& preferredType: preferredTypes) {
        const auto ret = std::find_if(contentTypes.cbegin(),
        contentTypes.cend(),
        [preferredType](const std::string& str) {
            return str.find(preferredType) != std::string::npos;});
        if (ret != contentTypes.cend()) {
            return *ret;
        }
    }

    return contentTypes.at(0);
}
}

CampaignsApiException::CampaignsApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int CampaignsApiException::getStatus() const
{
    return m_status;
}
const char* CampaignsApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
MODEL_T extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = MODEL_T(pt);
    return model;
}

template<class MODEL_T>
std::vector<MODEL_T> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<MODEL_T>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(MODEL_T(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

namespace CampaignsApiResources {
Ad_accountsAd_account_idPinsAnalyticsResource::Ad_accountsAd_account_idPinsAnalyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/pins/analytics");
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idPinsAnalyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idPinsAnalyticsResource::~Ad_accountsAd_account_idPinsAnalyticsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idPinsAnalyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idPinsAnalyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idPinsAnalyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idPinsAnalyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idPinsAnalyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idPinsAnalyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idPinsAnalyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string campaignId = request->get_query_parameter("campaignId", "");
    std::string pinIds_raw = request->get_query_parameter("pinIds");
    std::vector<std::string> pinIds;
    std::vector<std::string> pinIds_temp;
    boost::split(pinIds_temp, pinIds_raw, boost::is_any_of(","));
    std::copy(pinIds_temp.begin(), pinIds_temp.end(), std::inserter(pinIds, std::next(pinIds.begin())));
    std::string startDate = request->get_query_parameter("startDate", "");
    std::string endDate = request->get_query_parameter("endDate", "");
    std::string columns_raw = request->get_query_parameter("columns");
    std::vector<ReportingColumnSync> columns;
    std::vector<std::string> columns_temp;
    boost::split(columns_temp, columns_raw, boost::is_any_of(","));
    std::transform(columns_temp.begin(), columns_temp.end(), std::back_inserter(columns), [](const auto& i){ ReportingColumnSync ret; ret.fromString(i); return ret;});
    double clickWindowDays = request->get_query_parameter("clickWindowDays", 30);
    double engagementWindowDays = request->get_query_parameter("engagementWindowDays", 30);
    double viewWindowDays = request->get_query_parameter("viewWindowDays", 1);
    std::string conversionReportTime = request->get_query_parameter("conversionReportTime", "TIME_OF_AD_ACTION");
    
    int status_code = 500;
    std::vector<AdPinAnalytics> resultObject = std::vector<AdPinAnalytics>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(campaignId, pinIds, startDate, endDate, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, std::vector<AdPinAnalytics>> Ad_accountsAd_account_idPinsAnalyticsResource::handler_GET(
        std::string & campaignId, std::vector<std::string> & pinIds, std::string & startDate, std::string & endDate, std::vector<ReportingColumnSync> & columns, Granularity & granularity, std::string & adAccountId, double & clickWindowDays, double & engagementWindowDays, double & viewWindowDays, std::string & conversionReportTime)
{
    return handler_GET_func(campaignId, pinIds, startDate, endDate, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime);
}


std::string Ad_accountsAd_account_idPinsAnalyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idPinsAnalyticsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/targeting_analytics");
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::~Ad_accountsAd_account_idCampaignsTargeting_analyticsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string campaignIds_raw = request->get_query_parameter("campaignIds");
    std::vector<std::string> campaignIds;
    std::vector<std::string> campaignIds_temp;
    boost::split(campaignIds_temp, campaignIds_raw, boost::is_any_of(","));
    std::copy(campaignIds_temp.begin(), campaignIds_temp.end(), std::inserter(campaignIds, std::next(campaignIds.begin())));
    std::string startDate = request->get_query_parameter("startDate", "");
    std::string endDate = request->get_query_parameter("endDate", "");
    std::string targetingTypes_raw = request->get_query_parameter("targetingTypes");
    std::vector<AdsAnalyticsCampaignTargetingType> targetingTypes;
    std::vector<std::string> targetingTypes_temp;
    boost::split(targetingTypes_temp, targetingTypes_raw, boost::is_any_of(","));
    std::transform(targetingTypes_temp.begin(), targetingTypes_temp.end(), std::back_inserter(targetingTypes), [](const auto& i){ AdsAnalyticsCampaignTargetingType ret; ret.fromString(i); return ret;});
    std::string columns_raw = request->get_query_parameter("columns");
    std::vector<ReportingColumnSync> columns;
    std::vector<std::string> columns_temp;
    boost::split(columns_temp, columns_raw, boost::is_any_of(","));
    std::transform(columns_temp.begin(), columns_temp.end(), std::back_inserter(columns), [](const auto& i){ ReportingColumnSync ret; ret.fromString(i); return ret;});
    double clickWindowDays = request->get_query_parameter("clickWindowDays", 30);
    double engagementWindowDays = request->get_query_parameter("engagementWindowDays", 30);
    double viewWindowDays = request->get_query_parameter("viewWindowDays", 1);
    std::string conversionReportTime = request->get_query_parameter("conversionReportTime", "TIME_OF_AD_ACTION");
    std::string attributionTypes_raw = request->get_query_parameter("attributionTypes");
    std::vector<ConversionReportAttributionType> attributionTypes;
    std::vector<std::string> attributionTypes_temp;
    boost::split(attributionTypes_temp, attributionTypes_raw, boost::is_any_of(","));
    std::transform(attributionTypes_temp.begin(), attributionTypes_temp.end(), std::back_inserter(attributionTypes), [](const auto& i){ ConversionReportAttributionType ret; ret.fromString(i); return ret;});
    
    int status_code = 500;
    MetricsResponse resultObject = MetricsResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, campaignIds, startDate, endDate, targetingTypes, columns, granularity, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, attributionTypes, reportingTimezone);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, MetricsResponse> Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::handler_GET(
        std::string & adAccountId, std::vector<std::string> & campaignIds, std::string & startDate, std::string & endDate, std::vector<AdsAnalyticsCampaignTargetingType> & targetingTypes, std::vector<ReportingColumnSync> & columns, Granularity & granularity, double & clickWindowDays, double & engagementWindowDays, double & viewWindowDays, std::string & conversionReportTime, std::vector<ConversionReportAttributionType> & attributionTypes, ReportingTimeZone & reportingTimezone)
{
    return handler_GET_func(adAccountId, campaignIds, startDate, endDate, targetingTypes, columns, granularity, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, attributionTypes, reportingTimezone);
}


std::string Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idCampaignsTargeting_analyticsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idCampaignsAnalyticsResource::Ad_accountsAd_account_idCampaignsAnalyticsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/analytics");
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idCampaignsAnalyticsResource::handler_GET_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idCampaignsAnalyticsResource::~Ad_accountsAd_account_idCampaignsAnalyticsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsAnalyticsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsAnalyticsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsAnalyticsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idCampaignsAnalyticsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idCampaignsAnalyticsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idCampaignsAnalyticsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idCampaignsAnalyticsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string startDate = request->get_query_parameter("startDate", "");
    std::string endDate = request->get_query_parameter("endDate", "");
    std::string campaignIds_raw = request->get_query_parameter("campaignIds");
    std::vector<std::string> campaignIds;
    std::vector<std::string> campaignIds_temp;
    boost::split(campaignIds_temp, campaignIds_raw, boost::is_any_of(","));
    std::copy(campaignIds_temp.begin(), campaignIds_temp.end(), std::inserter(campaignIds, std::next(campaignIds.begin())));
    std::string columns_raw = request->get_query_parameter("columns");
    std::vector<ReportingColumnSync> columns;
    std::vector<std::string> columns_temp;
    boost::split(columns_temp, columns_raw, boost::is_any_of(","));
    std::transform(columns_temp.begin(), columns_temp.end(), std::back_inserter(columns), [](const auto& i){ ReportingColumnSync ret; ret.fromString(i); return ret;});
    double clickWindowDays = request->get_query_parameter("clickWindowDays", 30);
    double engagementWindowDays = request->get_query_parameter("engagementWindowDays", 30);
    double viewWindowDays = request->get_query_parameter("viewWindowDays", 1);
    std::string conversionReportTime = request->get_query_parameter("conversionReportTime", "TIME_OF_AD_ACTION");
    bool aggregateReportRows = request->get_query_parameter("aggregateReportRows", false);
    
    int status_code = 500;
    std::vector<CampaignsAnalyticsMetrics> resultObject = std::vector<CampaignsAnalyticsMetrics>();
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(startDate, endDate, campaignIds, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, aggregateReportRows, reportingTimezone);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, std::vector<CampaignsAnalyticsMetrics>> Ad_accountsAd_account_idCampaignsAnalyticsResource::handler_GET(
        std::string & startDate, std::string & endDate, std::vector<std::string> & campaignIds, std::vector<ReportingColumnSync> & columns, Granularity & granularity, std::string & adAccountId, double & clickWindowDays, double & engagementWindowDays, double & viewWindowDays, std::string & conversionReportTime, bool & aggregateReportRows, ReportingTimeZone & reportingTimezone)
{
    return handler_GET_func(startDate, endDate, campaignIds, columns, granularity, adAccountId, clickWindowDays, engagementWindowDays, viewWindowDays, conversionReportTime, aggregateReportRows, reportingTimezone);
}


std::string Ad_accountsAd_account_idCampaignsAnalyticsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idCampaignsAnalyticsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idCampaignsResource::Ad_accountsAd_account_idCampaignsResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns");
	this->set_method_handler("POST",
		std::bind(&Ad_accountsAd_account_idCampaignsResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idCampaignsResource::handler_GET_internal, this,
			std::placeholders::_1));
	this->set_method_handler("PATCH",
		std::bind(&Ad_accountsAd_account_idCampaignsResource::handler_PATCH_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idCampaignsResource::~Ad_accountsAd_account_idCampaignsResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idCampaignsResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idCampaignsResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idCampaignsResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idCampaignsResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto campaignCreateItem = extractJsonArrayBodyParam<CampaignCreateItem>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    CampaignBatchWriteResponseModel resultObject = CampaignBatchWriteResponseModel{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(adAccountId, campaignCreateItem);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

// x-extension
void Ad_accountsAd_account_idCampaignsResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    // Getting the query params
    std::string bookmark = request->get_query_parameter("bookmark", "");
    int32_t pageSize = request->get_query_parameter("pageSize", 25);
    std::string campaignIds_raw = request->get_query_parameter("campaignIds");
    std::vector<std::string> campaignIds;
    std::vector<std::string> campaignIds_temp;
    boost::split(campaignIds_temp, campaignIds_raw, boost::is_any_of(","));
    std::copy(campaignIds_temp.begin(), campaignIds_temp.end(), std::inserter(campaignIds, std::next(campaignIds.begin())));
    std::string entityStatuses_raw = request->get_query_parameter("entityStatuses");
    std::vector<EntityStatus> entityStatuses;
    std::vector<std::string> entityStatuses_temp;
    boost::split(entityStatuses_temp, entityStatuses_raw, boost::is_any_of(","));
    std::transform(entityStatuses_temp.begin(), entityStatuses_temp.end(), std::back_inserter(entityStatuses), [](const auto& i){ EntityStatus ret; ret.fromString(i); return ret;});
    
    int status_code = 500;
    Campaigns_list_200_response resultObject = Campaigns_list_200_response{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(adAccountId, bookmark, pageSize, order, campaignIds, entityStatuses);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}
// x-extension
void Ad_accountsAd_account_idCampaignsResource::handler_PATCH_internal(const std::shared_ptr<restbed::Session> session) {
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto campaignBatchUpdateItem = extractJsonArrayBodyParam<CampaignBatchUpdateItem>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    CampaignBatchWriteResponseModel resultObject = CampaignBatchWriteResponseModel{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_PATCH(adAccountId, campaignBatchUpdateItem);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}

std::pair<int, CampaignBatchWriteResponseModel> Ad_accountsAd_account_idCampaignsResource::handler_POST(
        std::string & adAccountId, std::vector<CampaignCreateItem> & campaignCreateItem)
{
    return handler_POST_func(adAccountId, campaignCreateItem);
}

std::pair<int, Campaigns_list_200_response> Ad_accountsAd_account_idCampaignsResource::handler_GET(
    std::string & adAccountId, std::string & bookmark, int32_t & pageSize, Pinterest_Lib_PaginationOrder & order, std::vector<std::string> & campaignIds, std::vector<EntityStatus> & entityStatuses)
{
    return handler_GET_func(adAccountId, bookmark, pageSize, order, campaignIds, entityStatuses);
}
std::pair<int, CampaignBatchWriteResponseModel> Ad_accountsAd_account_idCampaignsResource::handler_PATCH(
    std::string & adAccountId, std::vector<CampaignBatchUpdateItem> & campaignBatchUpdateItem)
{
    return handler_PATCH_func(adAccountId, campaignBatchUpdateItem);
}

std::string Ad_accountsAd_account_idCampaignsResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idCampaignsResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idCampaignsCampaign_idResource::Ad_accountsAd_account_idCampaignsCampaign_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/{campaign_id: .*}");
	this->set_method_handler("GET",
		std::bind(&Ad_accountsAd_account_idCampaignsCampaign_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idCampaignsCampaign_idResource::~Ad_accountsAd_account_idCampaignsCampaign_idResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsCampaign_idResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsCampaign_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsCampaign_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idCampaignsCampaign_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idCampaignsCampaign_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idCampaignsCampaign_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idCampaignsCampaign_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // Getting the path params
    std::string campaignId = request->get_path_parameter("campaign_id", "");
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    Campaign resultObject = Campaign{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_GET(campaignId, adAccountId);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, Campaign> Ad_accountsAd_account_idCampaignsCampaign_idResource::handler_GET(
        std::string & campaignId, std::string & adAccountId)
{
    return handler_GET_func(campaignId, adAccountId);
}


std::string Ad_accountsAd_account_idCampaignsCampaign_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idCampaignsCampaign_idResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}
Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/ad_accounts/{ad_account_id: .*}/campaigns/delivery_estimates");
	this->set_method_handler("POST",
		std::bind(&Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST_internal, this,
			std::placeholders::_1));
}

Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::~Ad_accountsAd_account_idCampaignsDelivery_estimatesResource() = default;

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handleCampaignsApiException(const CampaignsApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, std::multimap<std::string, std::string>& responseHeaders)
{
    responseHeaders.insert(std::make_pair("Connection", "close"));
    session->close(status, result, responseHeaders);
}

void Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    // body params or form params here from the body content string
    std::string bodyContent = extractBodyContent(session);
    auto campaignDeliveryEstimatesCampaign = extractJsonArrayBodyParam<CampaignDeliveryEstimatesCampaign>(bodyContent);
    // Getting the path params
    std::string adAccountId = request->get_path_parameter("ad_account_id", "");
    
    int status_code = 500;
    CampaignDeliveryEstimatesResponse resultObject = CampaignDeliveryEstimatesResponse{};
    std::string result = "";
    
    try {
        std::tie(status_code, resultObject) =
            handler_POST(adAccountId, campaignDeliveryEstimatesCampaign);
    }
    catch(const CampaignsApiException& e) {
        std::tie(status_code, result) = handleCampaignsApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }
    
    std::multimap< std::string, std::string > responseHeaders {};
    static const std::vector<std::string> contentTypes{
        "application/json",
    };
    static const std::string acceptTypes{
        "application/json, "
    };
    
    if (status_code == 200) {
        responseHeaders.insert(std::make_pair("Content-Type", selectPreferredContentType(contentTypes)));
        if (!acceptTypes.empty()) {
            responseHeaders.insert(std::make_pair("Accept", acceptTypes));
        }
    
        result = resultObject.toJsonString();
        returnResponse(session, 200, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 400) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request could not be understood by the server due to unexpected data.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 400, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 401) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "Authentication is required and has either failed or not been provided.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 401, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 403) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The request was valid, but the server is refusing action. The user might not have the necessary permissions for a resource.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 403, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 404) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The requested resource could not be found on this server.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 404, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 429) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The user has sent too many requests in a given amount of time and is being rate limited.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 429, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 503) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "The service is temporarily unavailable.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 503, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    if (status_code == 0) {
        responseHeaders.insert(std::make_pair("Content-Type", "text/plain"));
        result = "An unexpected error response.";
    
        result = resultObject.toJsonString();
        returnResponse(session, 0, result.empty() ? "{}" : result, responseHeaders);
        return;
    }
    defaultSessionClose(session, status_code, result);
    
    
}


std::pair<int, CampaignDeliveryEstimatesResponse> Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::handler_POST(
        std::string & adAccountId, std::vector<CampaignDeliveryEstimatesCampaign> & campaignDeliveryEstimatesCampaign)
{
    return handler_POST_func(adAccountId, campaignDeliveryEstimatesCampaign);
}


std::string Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

std::string Ad_accountsAd_account_idCampaignsDelivery_estimatesResource::extractFormParamsFromBody(const std::string& paramName, const std::string& body) {
    const auto uri = restbed::Uri("urlencoded?" + body, true);
    const auto params = uri.get_query_parameters();
    const auto result = params.find(paramName);
    if (result != params.cend()) {
        return result->second;
    }
    return "";
}

} /* namespace CampaignsApiResources */

CampaignsApi::CampaignsApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

CampaignsApi::~CampaignsApi() {}

std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idPinsAnalyticsResource> CampaignsApi::getAd_accountsAd_account_idPinsAnalyticsResource() {
    if (!m_spAd_accountsAd_account_idPinsAnalyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idPinsAnalyticsResource>());
    }
    return m_spAd_accountsAd_account_idPinsAnalyticsResource;
}
std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource> CampaignsApi::getAd_accountsAd_account_idCampaignsTargeting_analyticsResource() {
    if (!m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource>());
    }
    return m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource;
}
std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsAnalyticsResource> CampaignsApi::getAd_accountsAd_account_idCampaignsAnalyticsResource() {
    if (!m_spAd_accountsAd_account_idCampaignsAnalyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsAnalyticsResource>());
    }
    return m_spAd_accountsAd_account_idCampaignsAnalyticsResource;
}
std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsResource> CampaignsApi::getAd_accountsAd_account_idCampaignsResource() {
    if (!m_spAd_accountsAd_account_idCampaignsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsResource>());
    }
    return m_spAd_accountsAd_account_idCampaignsResource;
}
std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsCampaign_idResource> CampaignsApi::getAd_accountsAd_account_idCampaignsCampaign_idResource() {
    if (!m_spAd_accountsAd_account_idCampaignsCampaign_idResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsCampaign_idResource>());
    }
    return m_spAd_accountsAd_account_idCampaignsCampaign_idResource;
}
std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource> CampaignsApi::getAd_accountsAd_account_idCampaignsDelivery_estimatesResource() {
    if (!m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource>());
    }
    return m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource;
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idPinsAnalyticsResource> resource) {
    m_spAd_accountsAd_account_idPinsAnalyticsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idPinsAnalyticsResource);
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource> resource) {
    m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource);
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsAnalyticsResource> resource) {
    m_spAd_accountsAd_account_idCampaignsAnalyticsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsAnalyticsResource);
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsResource> resource) {
    m_spAd_accountsAd_account_idCampaignsResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsResource);
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsCampaign_idResource> resource) {
    m_spAd_accountsAd_account_idCampaignsCampaign_idResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsCampaign_idResource);
}
void CampaignsApi::setResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource> resource) {
    m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource = resource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idPinsAnalyticsResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idPinsAnalyticsResource> spAd_accountsAd_account_idPinsAnalyticsResource) {
    m_spAd_accountsAd_account_idPinsAnalyticsResource = spAd_accountsAd_account_idPinsAnalyticsResource;
    m_service->publish(m_spAd_accountsAd_account_idPinsAnalyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsTargeting_analyticsResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource> spAd_accountsAd_account_idCampaignsTargeting_analyticsResource) {
    m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource = spAd_accountsAd_account_idCampaignsTargeting_analyticsResource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsAnalyticsResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsAnalyticsResource> spAd_accountsAd_account_idCampaignsAnalyticsResource) {
    m_spAd_accountsAd_account_idCampaignsAnalyticsResource = spAd_accountsAd_account_idCampaignsAnalyticsResource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsAnalyticsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsResource> spAd_accountsAd_account_idCampaignsResource) {
    m_spAd_accountsAd_account_idCampaignsResource = spAd_accountsAd_account_idCampaignsResource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsCampaign_idResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsCampaign_idResource> spAd_accountsAd_account_idCampaignsCampaign_idResource) {
    m_spAd_accountsAd_account_idCampaignsCampaign_idResource = spAd_accountsAd_account_idCampaignsCampaign_idResource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsCampaign_idResource);
}
void CampaignsApi::setCampaignsApiAd_accountsAd_account_idCampaignsDelivery_estimatesResource(std::shared_ptr<CampaignsApiResources::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource> spAd_accountsAd_account_idCampaignsDelivery_estimatesResource) {
    m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource = spAd_accountsAd_account_idCampaignsDelivery_estimatesResource;
    m_service->publish(m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource);
}


void CampaignsApi::publishDefaultResources() {
    if (!m_spAd_accountsAd_account_idPinsAnalyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idPinsAnalyticsResource>());
    }
    if (!m_spAd_accountsAd_account_idCampaignsTargeting_analyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsTargeting_analyticsResource>());
    }
    if (!m_spAd_accountsAd_account_idCampaignsAnalyticsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsAnalyticsResource>());
    }
    if (!m_spAd_accountsAd_account_idCampaignsResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsResource>());
    }
    if (!m_spAd_accountsAd_account_idCampaignsCampaign_idResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsCampaign_idResource>());
    }
    if (!m_spAd_accountsAd_account_idCampaignsDelivery_estimatesResource) {
        setResource(std::make_shared<CampaignsApiResources::Ad_accountsAd_account_idCampaignsDelivery_estimatesResource>());
    }
}

std::shared_ptr<restbed::Service> CampaignsApi::service() {
    return m_service;
}


}
}
}
}

