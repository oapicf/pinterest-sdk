/**
 * Pinterest REST API
 * Pinterest's REST API
 *
 * The version of the OpenAPI document: 5.3.0
 * Contact: blah@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 5.4.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include <corvusoft/restbed/byte.hpp>
#include <corvusoft/restbed/string.hpp>
#include <corvusoft/restbed/settings.hpp>
#include <corvusoft/restbed/request.hpp>
#include <corvusoft/restbed/uri.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include <boost/lexical_cast.hpp>
#include <boost/algorithm/string.hpp>

#include "MediaApi.h"

namespace org {
namespace openapitools {
namespace server {
namespace api {

using namespace org::openapitools::server::model;

MediaApiException::MediaApiException(int status_code, std::string what)
  : m_status(status_code),
    m_what(what)
{

}
int MediaApiException::getStatus() const
{
    return m_status;
}
const char* MediaApiException::what() const noexcept
{
    return m_what.c_str();
}


template<class MODEL_T>
std::shared_ptr<MODEL_T> extractJsonModelBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto model = std::make_shared<MODEL_T>(pt);
    return model;
}

template<class MODEL_T>
std::vector<std::shared_ptr<MODEL_T>> extractJsonArrayBodyParam(const std::string& bodyContent)
{
    std::stringstream sstream(bodyContent);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream, pt);

    auto arrayRet = std::vector<std::shared_ptr<MODEL_T>>();
    for (const auto& child: pt) {
        arrayRet.emplace_back(std::make_shared<MODEL_T>(child.second));
    }
    return arrayRet;
}

template <class KEY_T, class VAL_T>
std::string convertMapResponse(const std::map<KEY_T, VAL_T>& map)
{
    boost::property_tree::ptree pt;
    for(const auto &kv: map) {
    pt.push_back(boost::property_tree::ptree::value_type(
        boost::lexical_cast<std::string>(kv.first),
        boost::property_tree::ptree(
        boost::lexical_cast<std::string>(kv.second))));
    }
    std::stringstream sstream;
    write_json(sstream, pt);
    std::string result = sstream.str();
    return result;
}

MediaApiMediaResource::MediaApiMediaResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/media/");
	this->set_method_handler("POST",
		std::bind(&MediaApiMediaResource::handler_POST_internal, this,
			std::placeholders::_1));
	this->set_method_handler("GET",
		std::bind(&MediaApiMediaResource::handler_GET_internal, this,
			std::placeholders::_1));
}

MediaApiMediaResource::~MediaApiMediaResource()
{
}

std::pair<int, std::string> MediaApiMediaResource::handleMediaApiException(const MediaApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> MediaApiMediaResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> MediaApiMediaResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void MediaApiMediaResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void MediaApiMediaResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void MediaApiMediaResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void MediaApiMediaResource::handler_POST_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();
    std::string bodyContent = extractBodyContent(session);

    // Get body params or form params here from the body content string
    auto mediaUploadRequest = extractJsonModelBodyParam<MediaUploadRequest>(bodyContent);




    int status_code = 500;
    std::shared_ptr<MediaUpload> resultObject = std::make_shared<MediaUpload>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_POST(mediaUploadRequest);
    }
    catch(const MediaApiException& e) {
        std::tie(status_code, result) = handleMediaApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 201) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 201, result.empty() ? "response" : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "Unexpected error" : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

// x-extension
void MediaApiMediaResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session) {

    const auto request = session->get_request();

    // Getting the query params
    const std::string bookmark = getQueryParam_bookmark_x_extension(request);

    const int32_t pageSize = getQueryParam_pageSize_x_extension(request);


    int status_code = 500;
    std::shared_ptr<Paginated> resultObject = nullptr;
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
            handler_GET(bookmark, pageSize);
    }
    catch(const MediaApiException& e) {
        std::tie(status_code, result) = handleMediaApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "response" : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "Unexpected error" : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}

std::pair<int, std::shared_ptr<MediaUpload>> MediaApiMediaResource::handler_POST(
        std::shared_ptr<MediaUploadRequest> const & mediaUploadRequest)
{
    throw MediaApiException(501, "Not implemented");
}

std::pair<int, std::shared_ptr<Paginated>> MediaApiMediaResource::handler_GET(
    std::string const & bookmark, int32_t const & pageSize)
{
    throw MediaApiException(501, "Not implemented");
}

std::string MediaApiMediaResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}
MediaApiMediaMedia_idResource::MediaApiMediaMedia_idResource(const std::string& context /* = "/v5" */)
{
	this->set_path(context + "/media/{media_id: .*}/");
	this->set_method_handler("GET",
		std::bind(&MediaApiMediaMedia_idResource::handler_GET_internal, this,
			std::placeholders::_1));
}

MediaApiMediaMedia_idResource::~MediaApiMediaMedia_idResource()
{
}

std::pair<int, std::string> MediaApiMediaMedia_idResource::handleMediaApiException(const MediaApiException& e)
{
    return std::make_pair<int, std::string>(e.getStatus(), e.what());
}

std::pair<int, std::string> MediaApiMediaMedia_idResource::handleStdException(const std::exception& e)
{
    return std::make_pair<int, std::string>(500, e.what());
}

std::pair<int, std::string> MediaApiMediaMedia_idResource::handleUnspecifiedException()
{
    return std::make_pair<int, std::string>(500, "Unknown exception occurred");
}

void MediaApiMediaMedia_idResource::setResponseHeader(const std::shared_ptr<restbed::Session>& session, const std::string& header)
{
    session->set_header(header, "");
}

void MediaApiMediaMedia_idResource::returnResponse(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result, const std::string& contentType)
{
    session->close(status, result, { {"Connection", "close"}, {"Content-Type", contentType} });
}

void MediaApiMediaMedia_idResource::defaultSessionClose(const std::shared_ptr<restbed::Session>& session, const int status, const std::string& result)
{
    session->close(status, result, { {"Connection", "close"} });
}

void MediaApiMediaMedia_idResource::handler_GET_internal(const std::shared_ptr<restbed::Session> session)
{
    const auto request = session->get_request();

    // Getting the path params
    const std::string mediaId = getPathParam_mediaId(request);



    int status_code = 500;
    std::shared_ptr<MediaUploadDetails> resultObject = std::make_shared<MediaUploadDetails>();
    std::string result = "";

    try {
        std::tie(status_code, resultObject) =
             handler_GET(mediaId);
    }
    catch(const MediaApiException& e) {
        std::tie(status_code, result) = handleMediaApiException(e);
    }
    catch(const std::exception& e) {
        std::tie(status_code, result) = handleStdException(e);
    }
    catch(...) {
        std::tie(status_code, result) = handleUnspecifiedException();
    }

    if (status_code == 200) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 200, result.empty() ? "response" : result, contentType);
        return;
    }
    if (status_code == 404) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 404, result.empty() ? "Media upload not found" : result, contentType);
        return;
    }
    if (status_code == 0) {
        result = resultObject->toJsonString();

        const constexpr auto contentType = "application/json";
        returnResponse(session, 0, result.empty() ? "Unexpected error" : result, contentType);
        return;
    }
    defaultSessionClose(session, status_code, result);
}


std::pair<int, std::shared_ptr<MediaUploadDetails>> MediaApiMediaMedia_idResource::handler_GET(
        std::string const & mediaId)
{
    throw MediaApiException(501, "Not implemented");
}


std::string MediaApiMediaMedia_idResource::extractBodyContent(const std::shared_ptr<restbed::Session>& session) {
  const auto request = session->get_request();
  int content_length = request->get_header("Content-Length", 0);
  std::string bodyContent;
  session->fetch(content_length,
                 [&bodyContent](const std::shared_ptr<restbed::Session> session,
                                const restbed::Bytes &body) {
                   bodyContent = restbed::String::format(
                       "%.*s\n", (int)body.size(), body.data());
                 });
  return bodyContent;
}

MediaApi::MediaApi(std::shared_ptr<restbed::Service> const& restbedService)
: m_service(restbedService)
{
}

MediaApi::~MediaApi() {}

void MediaApi::setMediaApiMediaResource(std::shared_ptr<MediaApiMediaResource> spMediaApiMediaResource) {
    m_spMediaApiMediaResource = spMediaApiMediaResource;
    m_service->publish(m_spMediaApiMediaResource);
}
void MediaApi::setMediaApiMediaMedia_idResource(std::shared_ptr<MediaApiMediaMedia_idResource> spMediaApiMediaMedia_idResource) {
    m_spMediaApiMediaMedia_idResource = spMediaApiMediaMedia_idResource;
    m_service->publish(m_spMediaApiMediaMedia_idResource);
}


void MediaApi::publishDefaultResources() {
    if (!m_spMediaApiMediaResource) {
        setMediaApiMediaResource(std::make_shared<MediaApiMediaResource>());
    }
    if (!m_spMediaApiMediaMedia_idResource) {
        setMediaApiMediaMedia_idResource(std::make_shared<MediaApiMediaMedia_idResource>());
    }
}

std::shared_ptr<restbed::Service> MediaApi::service() {
    return m_service;
}


}
}
}
}

